<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>AWT使用</title>
    <url>/2018/06/21/AWT%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>GUI编程的基础AWT。</p>
<a id="more"></a>
<p><img src="/2018/06/21/AWT%E4%BD%BF%E7%94%A8/%E4%BD%93%E7%B3%BB.png" alt="体系"></p>
<p><img src="/2018/06/21/AWT%E4%BD%BF%E7%94%A8/%E5%AE%B9%E5%99%A8.png" alt="容器"></p>
<p><img src="/2018/06/21/AWT%E4%BD%BF%E7%94%A8/%E5%85%B3%E7%B3%BB.png" alt="image"></p>
<p> Panel不能独立存在，必须存在于其他容器中，默认使用FlowLayout布局管理器<br> ScrollPane是一个带滚动条的容器，当组件占用空间过大时自动产生滚动条，也可以指定默认具有滚动条，同样不能单独存在，默认使用BorderLayout布局管理器，且通常不允许改变（因为通常用于装其他组件）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/6/21 11:12</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DivisionFrame</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试窗口"</span>);</span><br><span class="line">        ScrollPane sp = <span class="keyword">new</span> ScrollPane(ScrollPane.SCROLLBARS_ALWAYS);</span><br><span class="line">        sp.add(<span class="keyword">new</span> TextField(<span class="number">20</span>));</span><br><span class="line">        sp.add(<span class="keyword">new</span> JButton(<span class="string">"确定"</span>));</span><br><span class="line">        f.add(sp);</span><br><span class="line">        f.setBounds(<span class="number">30</span>, <span class="number">30</span>, <span class="number">250</span>, <span class="number">120</span>);</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="布局管理器"><a href="#布局管理器" class="headerlink" title="布局管理器"></a>布局管理器</h1><p>&emsp;所有AWT容器都有默认的布局管理器，也可以使用容器对象的setLayout(LayoutManger lm)方法来修改。</p>
<h2 id="FlowLayout布局管理器"><a href="#FlowLayout布局管理器" class="headerlink" title="FlowLayout布局管理器"></a>FlowLayout布局管理器</h2><p>&emsp;当读者在电脑上输入一篇文章时，所使用的就是FlowLayout布局管理器，所有的文字默认从左向右排列，遇到边界就会折回下一行重新开始。AWT中的FlowLayout布局管理器与此完全类似，只是此时排列的是AWT组件，而不是文字。</p>
<p>&emsp;FlowLayout([int align],[int hgap,int vgap])，三个参数依次代表为：设置组件的排列方向（从左向右、从右向左、从中间向两边），垂直间距，水平间距。期中第一个参数使用FlowLayout类的静态常量:FlowLayoutLEFT、FlowLayout.CENTER、FlowLayout.RIGHT。</p>
<blockquote>
<p>int hgap, int vgap参数要同时出现。</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/6/21 11:12</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DivisionFrame</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试窗口"</span>);</span><br><span class="line">        f.setLayout(<span class="keyword">new</span> FlowLayout(FlowLayout.LEFT, <span class="number">20</span>, <span class="number">5</span>));</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            f.add(<span class="keyword">new</span> JButton(<span class="string">"按钮"</span> + i));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//pack()方法是Windows容器提供的方法，用于将窗口调整到最佳大小</span></span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="BorderLayout布局管理器"><a href="#BorderLayout布局管理器" class="headerlink" title="BorderLayout布局管理器"></a>BorderLayout布局管理器</h2><p>&emsp;BorderLayout将容器分为east，west，north，center五个区域。当向容器中添加组件时默认为添加在中间，向同一个区域添加会使得组件被覆盖，当添加的组件少于五个时，旁边的组件会自动占据空白位置。</p>
<p>&emsp;BorderLayout([int hgap,int vgap])。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="keyword">static</span> java.awt.BorderLayout.*;<span class="comment">//静态导入</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/6/21 11:12</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DivisionFrame</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试窗口"</span>);</span><br><span class="line">        f.setLayout(<span class="keyword">new</span> BorderLayout(<span class="number">30</span>,<span class="number">5</span>));</span><br><span class="line">        f.add(<span class="keyword">new</span> JButton(<span class="string">"中"</span>),CENTER);</span><br><span class="line">        f.add(<span class="keyword">new</span> JButton(<span class="string">"南"</span>),SOUTH);</span><br><span class="line">        f.add(<span class="keyword">new</span> JButton(<span class="string">"北"</span>),NORTH);</span><br><span class="line">        f.add(<span class="keyword">new</span> JButton(<span class="string">"西"</span>),WEST);</span><br><span class="line">        f.add(<span class="keyword">new</span> JButton(<span class="string">"东"</span>),EAST);</span><br><span class="line">        <span class="comment">//pack()方法是Windows容器提供的方法，用于将窗口调整到最佳大小</span></span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;BorderLayout布局只能添加五个组件，但是可以在组件中包含容器，例如Panel容器，并在容器中添加更多组件。</p>
<h2 id="GridLayout布局管理器"><a href="#GridLayout布局管理器" class="headerlink" title="GridLayout布局管理器"></a>GridLayout布局管理器</h2><p>&emsp;GridLayout布局管理器将容器分割成纵横线分隔的网格，每个网格所占的区域大小相同。当向使用 GridLayout布局管理器的容器中添加组件时，默认从左向右、从上向下依次添加到每个网格中。与 FlowLayout不同的是，放置在GridLayout布局管理器中的各组件的大小由组件所处的区域来决定(每个组件将自动占满整个区域)。以FlowLayout的代码为例，原代码的窗口变大后按钮大小不会变化，而使用GridLayout布局管理器会使得按钮大小变化，使各组件占满Frame。</p>
<p>&emsp;GridLayout(int rows,int cols,[int hgap,int vgap]):指定行数和列数或者同时指定出间距。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="keyword">static</span> java.awt.BorderLayout.NORTH;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/6/21 11:12</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DivisionFrame</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Frame f = <span class="keyword">new</span> Frame(<span class="string">"计算器"</span>);</span><br><span class="line">        Panel p1 = <span class="keyword">new</span> Panel();</span><br><span class="line">        p1.add(<span class="keyword">new</span> TextField(<span class="number">30</span>));</span><br><span class="line">        f.add(p1,NORTH);</span><br><span class="line">        Panel p2 = <span class="keyword">new</span> Panel();</span><br><span class="line">        p2.setLayout(<span class="keyword">new</span> GridLayout(<span class="number">3</span>, <span class="number">5</span>, <span class="number">4</span>, <span class="number">4</span>));</span><br><span class="line">        String[] buttonName = &#123;<span class="string">"0"</span>, <span class="string">"1"</span>, <span class="string">"2"</span>, <span class="string">"3"</span>, <span class="string">"4"</span>, <span class="string">"5"</span>, <span class="string">"6"</span>, <span class="string">"7"</span>, <span class="string">"8"</span>, <span class="string">"9"</span>, <span class="string">"+"</span>, <span class="string">"-"</span>, <span class="string">"*"</span>, <span class="string">"/"</span>&#125;;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; buttonName.length; i++) &#123;</span><br><span class="line">            p2.add(<span class="keyword">new</span> JButton(buttonName[i]));</span><br><span class="line">        &#125;</span><br><span class="line">        f.add(p2);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="GridBagLayout布局管理器"><a href="#GridBagLayout布局管理器" class="headerlink" title="GridBagLayout布局管理器"></a>GridBagLayout布局管理器</h2><p>&emsp;GridBagLayout布局管理器是最灵活的，在该布局管理器中，有个组件可以跨越一个或多个网格，可以设置网格的大小。在窗口大小发生变化时，也可以更精确控制窗口各部分的拉伸。为了达到这种精确的控制，该布局管理器使用一个GridBagConstraints（网格包约束）对象。具体方式是，先创建GridBagLayout布局管理器对象，并将某个容器设置为该管理器，随后创建GridBagConstraints对象并指定一些相关属性，然后用GridBagLayout对象的setConstrains方法将GridBagConstraints对象与某个组件关联。简而言之，创建一个针对具体组件的“约束”。GridBagConstraints的具体属性如下：</p>
<ul>
<li><p>gridx、gridy：设置受该对象控制的GUI组件左上角所在网格的横向索引、纵向索引 (GridBagLayout左上角网格的索引为0、0)。这两个值还可以是GridBagConstraints.RELATIVE (默认值)，它表明当前组件紧跟在上一个组件之后。（索引即位置）</p>
</li>
<li><p>gridwidth、gridheight：设置受该对象控制的GUI组件横向、纵向跨越多少个网格，两个属性值的默认值都是1。如果设置这两个属性值为GridBagConstraints.REMAINDER，这表明受该对象控制 的GUI组件是横向、纵向最后一个组件;如果设置这两个属性值为GridBagConstraints. RELATIVE, 这表明受该对象控制的GUI组件是横向、纵向倒数第二个组件。</p>
</li>
<li><p>fill：设置受该对象控制的GUI组件如何占据空白区域。该属性的取值如下。</p>
<p>• GridBagConstraints.NONE： GUI 组件不扩大。 </p>
<p>• GridBagConstraints.HORIZONTAL： GUI组件水平扩大以占据空白区域。 </p>
<p>• GridBagConstraints.VERTICAL： GUI组件垂直扩大以占据空白区域。 </p>
<p>• GridBagConstraints.BOTH: GUI组件水平、垂直同时扩大以占据空白区</p>
</li>
<li><p>ipadx、ipady：设置受该对象控制的GUI组件横向、纵向内部填充的大小，即在该组件最小尺寸的基础上还需要增大多少。如果设置了这两个属性，则组件横向大小为最小宽度再加ipadx*2像素，纵向大小为最小高度再加ipady*2像素。</p>
</li>
<li><p>insets：设置受该对象控制的GUI组件的外部填充的大小，即该组件边界和显示区域边界之间的 距离。</p>
</li>
<li><p>anchor：设置受该对象控制的GUI组件在其显示区域中的定位方式。定位方式如下。</p>
<ul>
<li>GridBagConstraints.CENTER (中间)</li>
<li>GridBagConstraints.NORTH (上中)</li>
<li>GridBagConstraints.NORTHWEST(左上角)</li>
<li>GridBagConstraints.NORTHEAST(右上角)</li>
<li>GridBagConstraints.SOUTH (下中)</li>
<li>GridBagConstraints.SOUTHEAST(右下角)</li>
<li>GridBagConstraints.SOUTHWEST(左下角)</li>
<li>GridBagConstraints.EAST(右中)</li>
<li>GridBagConstraints.WEST(左中)</li>
</ul>
</li>
<li><p>weightx, weighty：设置受该对象控制的GUI组件占据多余空间的水平、垂直增加比例（也叫权重，即weight的直译），这两个属性的默认值是0 , 即该组件不占据多余空间。假设某个容器的水平线上包括三个GUI组件，它们的水平增加比例分别是1、2、3 ,但容器宽度增加60像素时，则第一个组件宽度增加10像素，第二个组件宽度增加20像素，第三个组件宽度增加30像素。如果其增加比例为0 , 则表示不会增加。</p>
</li>
</ul>
<p>&emsp;如果希望某个组件的大小随容器的增大而增大，则必须同时设置控制该组件的GridBagConstraints 对象的fill属性和weightx、weighty属性。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/16 16:40</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GridBagTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试窗口"</span>);</span><br><span class="line">    <span class="keyword">private</span> GridBagLayout gb = <span class="keyword">new</span> GridBagLayout();</span><br><span class="line">    <span class="keyword">private</span> GridBagConstraints gbc = <span class="keyword">new</span> GridBagConstraints();</span><br><span class="line">    <span class="keyword">private</span> JButton[] bs = <span class="keyword">new</span> JButton[<span class="number">10</span>];</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        f.setLayout(gb);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; bs.length; i++) &#123;</span><br><span class="line">            bs[i] = <span class="keyword">new</span> JButton(<span class="string">"按钮"</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line"><span class="comment">//        首先设置占据空白区域方式，所有组件都一样，没有改</span></span><br><span class="line">        gbc.fill = GridBagConstraints.BOTH;</span><br><span class="line"><span class="comment">//        设置水平大小增加比例，0,1,2,3四个按钮比例是1:1:1:1</span></span><br><span class="line">        gbc.weightx = <span class="number">1</span>;</span><br><span class="line">        addButton(bs[<span class="number">0</span>]);</span><br><span class="line">        addButton(bs[<span class="number">1</span>]);</span><br><span class="line">        addButton(bs[<span class="number">2</span>]);</span><br><span class="line"><span class="comment">//        3为第一行最后一个</span></span><br><span class="line">        gbc.gridwidth = GridBagConstraints.REMAINDER;</span><br><span class="line">        addButton(bs[<span class="number">3</span>]);</span><br><span class="line"></span><br><span class="line"><span class="comment">//        4原本是不按比例增加，但是因为设置的填充方式，会把整行占满</span></span><br><span class="line">        gbc.weightx = <span class="number">0</span>;</span><br><span class="line">        addButton(bs[<span class="number">4</span>]);</span><br><span class="line"></span><br><span class="line">        gbc.gridwidth = <span class="number">2</span>;</span><br><span class="line">        addButton(bs[<span class="number">5</span>]);</span><br><span class="line"><span class="comment">//        这里发现把数值改来改去出来的都不会变</span></span><br><span class="line">        gbc.gridwidth = <span class="number">1</span>;</span><br><span class="line">        gbc.gridheight = <span class="number">2</span>;</span><br><span class="line">        gbc.gridwidth = GridBagConstraints.REMAINDER;</span><br><span class="line">        addButton(bs[<span class="number">6</span>]);</span><br><span class="line"></span><br><span class="line">        gbc.gridwidth = <span class="number">1</span>;</span><br><span class="line">        gbc.gridheight = <span class="number">2</span>;</span><br><span class="line">        gbc.weighty = <span class="number">1</span>;</span><br><span class="line">        addButton(bs[<span class="number">7</span>]);</span><br><span class="line"></span><br><span class="line">        gbc.gridwidth = GridBagConstraints.REMAINDER;</span><br><span class="line">        gbc.gridheight = <span class="number">1</span>;</span><br><span class="line">        addButton(bs[<span class="number">8</span>]);</span><br><span class="line">        addButton(bs[<span class="number">9</span>]);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">addButton</span><span class="params">(JButton b)</span> </span>&#123;</span><br><span class="line">        gb.setConstraints(b, gbc);</span><br><span class="line">        f.add(b);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> GridBagTest().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这段代码的注释写着写着就不想写了，因为发现这个布局真的很烦，明明指定的效果却会因为其他一些不说清的因素而不变。。。反正也不重要，鸽了。</p>
<h2 id="CardLayout布局管理器"><a href="#CardLayout布局管理器" class="headerlink" title="CardLayout布局管理器"></a>CardLayout布局管理器</h2><p>&emsp;CardLayout布局管理器以时间而非空间来管理它里面的组件，它将加入容器的所有组件看成一叠卡片，每次只有最上面的那个Component才可见。就好像一副扑克牌，它们叠在一起，每次只有最上面的一张扑克牌才可见。</p>
<p>&emsp;CardLayout([int hgap,int vgap])</p>
<p>CardLayout用于控制组件可见的5 个常用方法如下。</p>
<ul>
<li>first(Container target)：显示 target 容器中的第一张卡片。</li>
<li>last(Container target)：显示target容器中的最后一张卡片。</li>
<li>previous(Container target)：显示target容器中的前一张卡片。</li>
<li>next(Container target)：显示target容器中的后一张卡片。</li>
<li>show(Container target,String name)：显示target容器中指定名字</li>
</ul>
<h2 id="绝对定位"><a href="#绝对定位" class="headerlink" title="绝对定位"></a>绝对定位</h2><p>&emsp;也可以指定布局管理器为null，然后使用setBounds或setSize方法来控制具体组件的大小，位置。但是这样不利于跨平台。</p>
<h2 id="BoxLayout布局管理器"><a href="#BoxLayout布局管理器" class="headerlink" title="BoxLayout布局管理器"></a>BoxLayout布局管理器</h2><p>&emsp;BoxLayout布局管理器是Swing新引入的一个布局管理器，相较于GridBagLayout，它比较简单，但是功能却没有减少。BoxLayout只提供了一个很简单的构造器：</p>
<p>&emsp;BoxLayout(Container target, int axis)：指定创建基于target容器的BoxLayout布局管理器，该布 局管理器里的组件按axis方向排列。其中axisBoxLayout.X_AXIS(横向)和BoxLayout.Y_AXIS (纵向)两个方向。</p>
<p>&emsp;BoxLayout布局管理器通常和Box容器结合使用，它属于Swing。它有点像Panel，默认使用BoxLayout。Box提供了如下两个静态方法来创建Box对象：</p>
<ul>
<li>createHorizontalBox(): 创建一个水平排列组件的Box容器。</li>
</ul>
<p>&emsp;* createHorizontalBox(): 创建一个水平排列组件的Box容器。</p>
<ul>
<li>createVerticalBox(): 创建一个垂直排列组件的Box容器。</li>
</ul>
<p>&emsp;但是这两个方法创建的容器是不具有间距的，全部挤在一起。对于间距，Box提供了相关的方法来创建:</p>
<ul>
<li>createHorizontalGlue（）: 创建一条水平Glue （可在两个方向上同时拉伸的间距）</li>
<li>createVerticalGlue（）: 创建一条垂直Glue （可在两个方向上同时拉伸的间距）</li>
<li>createHorizontalStrut（int width）：创建一条指定宽度的水平Strut （可在垂直方向上拉伸的间距）</li>
<li>createVerticalStrut（int height）：创建一条指定高度的垂直Strut （可在水平方向上拉伸的间距）</li>
<li>createRigidArea（Dimension d）：创建指定宽度、高度的RigidArea （不可拉伸的间距）</li>
</ul>
<p>&emsp;这意味着在Box中空白间距也是一种组件。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/17 14:54</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BoxSpaceTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试窗口?"</span>);</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * horizontal Box</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> Box horizontal = Box.createHorizontalBox();</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * vertical Box</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> Box vertical = Box.createVerticalBox();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        horizontal.add(<span class="keyword">new</span> Button(<span class="string">"水平按钮1"</span>));</span><br><span class="line">        horizontal.add(Box.createHorizontalGlue());</span><br><span class="line">        horizontal.add(<span class="keyword">new</span> Button(<span class="string">"水平按钮2"</span>));</span><br><span class="line">        horizontal.add(Box.createHorizontalStrut(<span class="number">10</span>));</span><br><span class="line">        horizontal.add(<span class="keyword">new</span> Button(<span class="string">"水平按钮3"</span>));</span><br><span class="line">        vertical.add(<span class="keyword">new</span> Button(<span class="string">"垂直按钮1"</span>));</span><br><span class="line">        vertical.add(Box.createVerticalGlue());</span><br><span class="line">        vertical.add(<span class="keyword">new</span> Button(<span class="string">"垂直按钮2"</span>));</span><br><span class="line">        vertical.add(Box.createVerticalStrut(<span class="number">10</span>));</span><br><span class="line">        vertical.add(<span class="keyword">new</span> Button(<span class="string">"垂直按钮3"</span>));</span><br><span class="line">        f.add(horizontal, BorderLayout.NORTH);</span><br><span class="line">        f.add(vertical);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> BoxSpaceTest().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;注意，这里的按钮用的是JButton，窗口拉伸后按钮大小不变，而用Button时会自动填充。</p>
<h1 id="AWT常用组件"><a href="#AWT常用组件" class="headerlink" title="AWT常用组件"></a>AWT常用组件</h1><h2 id="基本组件"><a href="#基本组件" class="headerlink" title="基本组件"></a>基本组件</h2><p>&emsp;AWT提供了如下基本组件。</p>
<ul>
<li>Button：按钮，可接受单击操作。</li>
<li>Canvas：用于绘图的画布。</li>
<li>Checkbox：复选框组件（也可变成单选框组件）。</li>
<li>CheckboxGroup：用于将多个Checkbox组件组合成一组，一组Checkbox组件将只有一个可以<br>被选中，即全部变成单选框组件。</li>
<li>Choice：下拉式选择框组件。</li>
<li>Frame：窗口，在 GUI程序里通过该类创建窗口。</li>
<li>Label：标签类，用于放置提示性文本。 </li>
<li>List：列表框组件，可以添加多项条目。</li>
<li>Panel：不能单独存在基本容器类，必须放到其他容器中。</li>
<li>Scrollbar：滑动条组件。如果需要用户输入位于某个范围的值，就可以使用滑动条组件，比如调 色板中设置RGB的三个值所用的滑动条。当创建一个滑动条时，必须指定它的方向、初始值、 滑块的大小、最小值和最大值。</li>
<li>ScrollPane：带水平及垂直滚动条的容器组件。</li>
<li>TextArea：多行文本域。</li>
<li>TextField：单行文本框。</li>
</ul>
<h2 id="对话框"><a href="#对话框" class="headerlink" title="对话框"></a>对话框</h2><p>&emsp;Dialog是Window类的子类，是一个容器类，属于特殊组件。对话框是可以独立存在的顶级窗口， 因此用法与普通窗口的用法几乎完全一样。但对话框有如下两点需要注意。</p>
<ul>
<li>对话框通常依赖于其他窗口，就是通常有一个parent窗口。</li>
<li>对话框有非模式（non-modal）和模式（modal）两种，当某个模式对话框被打开之后，该模式对话框总是位于它依赖的窗口之上；在模式对话框被关闭之前，它依赖的窗口无法获得焦点，</li>
</ul>
<p>&emsp;对话框有多个重载的构造器，它的构造器可能有如下三个参数:</p>
<ul>
<li>owner：指定该对话框所依赖的窗口，既可以是窗口，也可以是对话框。 </li>
<li>title：指定该对话框的窗口标题。</li>
<li>modal：指定该对话框是否是模式的，可以是true或false。</li>
</ul>
<p>&emsp;下面的代码创建了模式对话框和非模式对话框：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/17 16:52</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DialogTest</span> </span>&#123;</span><br><span class="line">    Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试窗口"</span>);</span><br><span class="line">    Dialog d1 = <span class="keyword">new</span> Dialog(f, <span class="string">"模式对话框"</span>, <span class="keyword">true</span>);</span><br><span class="line">    Dialog d2 = <span class="keyword">new</span> Dialog(f, <span class="string">"非模式对话框"</span>, <span class="keyword">false</span>);</span><br><span class="line">    JButton b1 = <span class="keyword">new</span> JButton(<span class="string">"打开模式对话框"</span>);</span><br><span class="line">    JButton b2 = <span class="keyword">new</span> JButton(<span class="string">"打开非模式对话框"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        d1.setBounds(<span class="number">20</span>, <span class="number">30</span>, <span class="number">400</span>, <span class="number">400</span>);</span><br><span class="line">        d2.setBounds(<span class="number">20</span>, <span class="number">30</span>, <span class="number">400</span>, <span class="number">400</span>);</span><br><span class="line">        b1.addActionListener(e -&gt; d1.setVisible(<span class="keyword">true</span>));</span><br><span class="line">        b2.addActionListener(e -&gt; d2.setVisible(<span class="keyword">true</span>));</span><br><span class="line">        f.add(b1);</span><br><span class="line">        f.add(b2, BorderLayout.SOUTH);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> DialogTest().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;从动作监听器那里可以看出，两种窗口不是在点击按钮时创建，而是在那时显示出来。</p>
<blockquote>
<p>不管是模式对话框还是非模式对话框，打开后都无法关闭它们，因为程序没有为这两个对话框编写事件监听器。还有，如果主程序需要对话框里接收的输入值，则应该把该对话框设置成模式对话框，因为模式对话框会阻塞该程序；如果把对话框设置成非模式对话框，则可能造成对话框被打开了 ，但用户并没有操作该对话框，也没有向对话框里进行输 入，这就会引起主程序的异常。</p>
</blockquote>
<p>&emsp;Dialog类还有一个子类：FileDialog，它代表一个文件对话框，用于打开或者保存文件。FileDialog<br>也提供了几个构造器，可分别支持parent、title和 mode三个构造参数，其中parent、title指定文件对话框的所属父窗口和标题；而mode指定该窗口用于打开文件或保存文件，该参数支持如下两个参数值： FileDialog.LOAD、FileDialog.SAVE。FileDialog不能指定是模式对话框还是非模式对话框，这依赖于运行平台。FileDialog还提供了两个方法分别用于获取被打开/保存的文件的绝对路径和文件名：getDirectory(),getFile()。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/17 22:02</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileDialogTest</span> </span>&#123;</span><br><span class="line">    Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试窗口"</span>);</span><br><span class="line">    FileDialog d1 = <span class="keyword">new</span> FileDialog(f, <span class="string">"选择需要打开的文件"</span>, FileDialog.LOAD);</span><br><span class="line">    FileDialog d2 = <span class="keyword">new</span> FileDialog(f, <span class="string">"选择需要保存的文件"</span>, FileDialog.SAVE);</span><br><span class="line">    JButton b1 = <span class="keyword">new</span> JButton(<span class="string">"打开文件"</span>);</span><br><span class="line">    JButton b2 = <span class="keyword">new</span> JButton(<span class="string">"保存文件"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        b1.addActionListener(e -&gt; &#123;</span><br><span class="line">            d1.setVisible(<span class="keyword">true</span>);</span><br><span class="line">            System.out.println(d1.getDirectory() + <span class="string">"\n"</span> + d1.getFile());</span><br><span class="line">        &#125;);</span><br><span class="line">        b2.addActionListener(e -&gt; &#123;</span><br><span class="line">            d1.setVisible(<span class="keyword">true</span>);</span><br><span class="line">            System.out.println(d1.getDirectory() + <span class="string">"\n"</span> + d1.getFile());</span><br><span class="line">        &#125;);</span><br><span class="line">        f.add(b1, BorderLayout.NORTH);</span><br><span class="line">        f.add(b2, BorderLayout.SOUTH);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> FileDialogTest().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>AWT菜单</title>
    <url>/2018/07/26/AWT%E8%8F%9C%E5%8D%95/</url>
    <content><![CDATA[<p>&emsp;前面介绍了创建GUI界面的方式：将AWT组件按某种布局摆放在容器内即可。创建AWT菜单的<br>方式与此完全类似：将菜单条、菜单、菜单项组合在一起即可。</p>
<a id="more"></a>

<p>AWT中的菜单由如下几个类组合而成。</p>
<ul>
<li>MenuBar：菜单条，菜单的容器。</li>
<li>Menu：菜单组件，菜单项的容器。它也是Menultem的子类，所以可作为菜单项使用。</li>
<li>PopupMenu：上下文菜单组件(右键菜单组件)。</li>
<li>Menultem：菜单项组件，真正被点击的选项。</li>
<li>CheckboxMenuItem: 复选框菜单项组件。</li>
<li>MenuShortcut：菜单快捷键组件。</li>
</ul>
<p>下面是它们的继承，组合关系：</p>
<p><img src="/2018/07/26/AWT%E8%8F%9C%E5%8D%95/%E5%85%B3%E7%B3%BB.png" alt="关系"></p>
<p>&emsp;这里面的Menu实现了菜单容器接口：<code>public class Menu extends MenuItem implements MenuContainer, Accessible</code>，所以可以用来装Menultem，如图上Menultem组合成Menu。而MenuBar也实现了菜单容器接口，可以装Menu，而Menu组合成MenuBar。</p>
<p><img src="/2018/07/26/AWT%E8%8F%9C%E5%8D%95/%E8%8F%9C%E5%8D%95.png" alt="菜单"></p>
<p>&emsp;上面的途中，红色框是MenuBar，File，即绿色框是一个Menu，而IDE Setting Sync，即紫色框，也是一个Menu，最终的Disable Sync才是MenuItem。</p>
<p>&emsp;Menu、Menultem的构造器都可接收一个字符串参数，该字符串作为其对应菜单、菜单项上的标签文本。除此之外,Menultem还可以接收一个MenuShortcut对象，该对象用于指定该菜单的快捷键。MenuShortcut类使用虚拟键代码(而不是字符)来创建快捷键。例如，Ctrl+A (通常都以Ctrl键作为快捷键的辅助键) 快捷方式通过以下代码创建。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">MenuShortcut ms = <span class="keyword">new</span> MenuShortcut(KeyEvent.VK_A,[<span class="keyword">true</span>]);</span><br></pre></td></tr></table></figure>

<p>&emsp;true表示要使用shift键。</p>
<p>&emsp;若想要分割菜单，有两种方式：</p>
<ul>
<li>调用Menu对象的addSeparator()方法来添加菜单分隔线。</li>
<li>使用添加new Menultem(“-“)的方式来添加菜单分隔线。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.ActionListener;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.KeyEvent;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.WindowAdapter;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.WindowEvent;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/26 20:16</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleMenu</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试"</span>);</span><br><span class="line">    <span class="keyword">private</span> MenuBar mb = <span class="keyword">new</span> MenuBar();</span><br><span class="line">    Menu file = <span class="keyword">new</span> Menu(<span class="string">"file"</span>);</span><br><span class="line">    Menu edit = <span class="keyword">new</span> Menu(<span class="string">"edit"</span>);</span><br><span class="line">    MenuItem newItem = <span class="keyword">new</span> MenuItem(<span class="string">"new"</span>);</span><br><span class="line">    MenuItem saveItem = <span class="keyword">new</span> MenuItem(<span class="string">"save"</span>);</span><br><span class="line">    MenuItem exitItem = <span class="keyword">new</span> MenuItem(<span class="string">"exit"</span>, <span class="keyword">new</span> MenuShortcut(KeyEvent.VK_X));</span><br><span class="line">    CheckboxMenuItem autoWrap = <span class="keyword">new</span> CheckboxMenuItem(<span class="string">"auto wrap"</span>);</span><br><span class="line">    MenuItem copyItem = <span class="keyword">new</span> MenuItem(<span class="string">"copy"</span>);</span><br><span class="line">    MenuItem pasteItem = <span class="keyword">new</span> MenuItem(<span class="string">"paste"</span>);</span><br><span class="line">    Menu format = <span class="keyword">new</span> Menu(<span class="string">"format"</span>);</span><br><span class="line">    MenuItem commentItem = <span class="keyword">new</span> MenuItem(<span class="string">"comment"</span>, <span class="keyword">new</span> MenuShortcut(KeyEvent.VK_SLASH, <span class="keyword">true</span>));</span><br><span class="line">    MenuItem cancelItem = <span class="keyword">new</span> MenuItem(<span class="string">"cancel comment"</span>);</span><br><span class="line">    <span class="keyword">private</span> TextArea ta = <span class="keyword">new</span> TextArea(<span class="number">6</span>, <span class="number">40</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        ActionListener menuListener = e -&gt; &#123;</span><br><span class="line">            String cmd = e.getActionCommand();</span><br><span class="line">            ta.append(<span class="string">"单击 "</span> + cmd + <span class="string">" 菜单"</span> + <span class="string">"\n"</span>);</span><br><span class="line">            <span class="keyword">if</span> (cmd.equals(<span class="string">"exit"</span>)) &#123;</span><br><span class="line">                System.exit(<span class="number">0</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        commentItem.addActionListener(menuListener);</span><br><span class="line">        exitItem.addActionListener(menuListener);</span><br><span class="line"></span><br><span class="line">        file.add(newItem);</span><br><span class="line">        file.add(saveItem);</span><br><span class="line">        file.add(exitItem);</span><br><span class="line"></span><br><span class="line">        edit.add(autoWrap);</span><br><span class="line">        edit.addSeparator();</span><br><span class="line">        edit.add(copyItem);</span><br><span class="line">        edit.add(pasteItem);</span><br><span class="line"></span><br><span class="line">        format.add(commentItem);</span><br><span class="line">        format.add(cancelItem);</span><br><span class="line"></span><br><span class="line">        edit.add(<span class="keyword">new</span> MenuItem(<span class="string">"-"</span>));</span><br><span class="line">        edit.add(format);</span><br><span class="line"></span><br><span class="line">        mb.add(file);</span><br><span class="line">        mb.add(edit);</span><br><span class="line"></span><br><span class="line"><span class="comment">//        为 f窗口设置菜单条</span></span><br><span class="line">        f.setMenuBar(mb);</span><br><span class="line"></span><br><span class="line">        f.addWindowListener(<span class="keyword">new</span> WindowAdapter() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowClosing</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">                System.exit(<span class="number">0</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        f.add(ta);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> SimpleMenu().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;创建一个右键菜单也很容易,创建多个MenuItem,然后加入PopupMenu中,再将PopupMenu加入相关组件即可.</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Cpp Lambda表达式</title>
    <url>/2018/07/29/Cpp-Lambda%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
    <content><![CDATA[<p>Lambda表达式是C++11的新特性,了解一下.</p>
<a id="more"></a>>

<p>如下图，lambda表达式由下面几个部分构成：  </p>
<p><img src="/2018/07/29/Cpp-Lambda%E8%A1%A8%E8%BE%BE%E5%BC%8F/20161213155101488.png" alt></p>
<p>lambda表达式的语法归纳如下：</p>
<p>[ caputrue ] ( params ) opt -&gt; ret { body; };</p>
<p>1).capture是捕获列表；</p>
<p>2).params是参数表；(选填)</p>
<p>3&amp;4).opt是函数选项；可以填mutable,exception,attribute（选填）</p>
<p>mutable说明lambda表达式体内的代码可以修改被捕获的变量，并且可以访问被捕获的对象的non-const方法。</p>
<p>exception说明lambda表达式是否抛出异常以及何种异常。</p>
<p>attribute用来声明属性。</p>
<p>5).ret是返回值类型。(选填)</p>
<p>6).body是函数体。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmp</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span>  a &lt; b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; myvec&#123; <span class="number">3</span>, <span class="number">2</span>, <span class="number">5</span>, <span class="number">7</span>, <span class="number">3</span>, <span class="number">2</span> &#125;;</span><br><span class="line">    <span class="function"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; <span class="title">lbvec</span><span class="params">(myvec)</span></span>;</span><br><span class="line"></span><br><span class="line">    sort(myvec.begin(), myvec.end(), cmp); <span class="comment">// 旧式做法</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"predicate function:"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> it : myvec)</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; it &lt;&lt; <span class="string">' '</span>;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    sort(lbvec.begin(), lbvec.end(), [](<span class="keyword">int</span> a, <span class="keyword">int</span> b) -&gt; <span class="keyword">bool</span> &#123; <span class="keyword">return</span> a &lt; b; &#125;);   <span class="comment">// Lambda表达式</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"lambda expression:"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> it : lbvec)</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; it &lt;&lt; <span class="string">' '</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在C++11之前，我们使用STL的sort函数，需要提供一个谓词函数。如果使用C++11的Lambda表达式，我们只需要传入一个匿名函数即可，方便简洁，而且代码的可读性也比旧式的做法好多了。</p>
<p>下面，我们就重点介绍一下Lambda表达式各项的具体用法。</p>
<h2 id="捕获外部变量"><a href="#捕获外部变量" class="headerlink" title="捕获外部变量"></a><strong>捕获外部变量</strong></h2><p>Lambda表达式可以使用其可见范围内的外部变量，但必须明确声明（明确声明哪些外部变量可以被该Lambda表达式使用）。那么，在哪里指定这些外部变量呢？Lambda表达式通过在最前面的方括号[]来明确指明其内部可以访问的外部变量，这一过程也称过Lambda表达式“捕获”了外部变量。</p>
<p>我们通过一个例子来直观地说明一下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">123</span>;</span><br><span class="line">    <span class="keyword">auto</span> f = [a] &#123; <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>; &#125;; </span><br><span class="line">    f(); <span class="comment">// 输出：123</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//或通过“函数体”后面的‘()’传入参数</span></span><br><span class="line">    <span class="keyword">auto</span> x = [](<span class="keyword">int</span> a)&#123;<span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>;&#125;(<span class="number">123</span>); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面这个例子先声明了一个整型变量a，然后再创建Lambda表达式，该表达式“捕获”了a变量，这样在Lambda表达式函数体中就可以获得该变量的值。</p>
<p>类似参数传递方式（值传递、引入传递、指针传递），在Lambda表达式中，外部变量的捕获方式也有值捕获、引用捕获、隐式捕获。</p>
<h4 id="1、值捕获"><a href="#1、值捕获" class="headerlink" title="1、值捕获"></a><strong>1、值捕获</strong></h4><p>值捕获和参数传递中的值传递类似，被捕获的变量的值在Lambda表达式创建时通过值拷贝的方式传入，因此随后对该变量的修改不会影响影响Lambda表达式中的值。</p>
<p>示例如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">123</span>;</span><br><span class="line">    <span class="keyword">auto</span> f = [a] &#123; <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>; &#125;; </span><br><span class="line">    a = <span class="number">321</span>;</span><br><span class="line">    f(); <span class="comment">// 输出：123</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里需要注意的是，如果以传值方式捕获外部变量，则在Lambda表达式函数体中不能修改该外部变量的值(函数内部改值不可变,即传入的是const的),除非使用mutable.</p>
<h4 id="2、引用捕获"><a href="#2、引用捕获" class="headerlink" title="2、引用捕获"></a><strong>2、引用捕获</strong></h4><p>使用引用捕获一个外部变量，只需要在捕获列表变量前面加上一个引用说明符&amp;。如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">123</span>;</span><br><span class="line">    <span class="keyword">auto</span> f = [&amp;a] &#123; <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>; &#125;; </span><br><span class="line">    a = <span class="number">321</span>;</span><br><span class="line">    f(); <span class="comment">// 输出：321</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从示例中可以看出，引用捕获的变量使用的实际上就是该引用所绑定的对象。</p>
<p><strong>关于capture 中值[=]和引用[&amp;]的区别</strong> </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;         </span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;       </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>                 </span></span><br><span class="line"><span class="function"></span>&#123;                          </span><br><span class="line">    <span class="keyword">int</span> j = <span class="number">10</span>;            </span><br><span class="line">    <span class="keyword">auto</span> by_val_lambda = [=]&#123; <span class="keyword">return</span> j + <span class="number">1</span>; &#125;;</span><br><span class="line">    <span class="keyword">auto</span> by_ref_lambda = [&amp;]&#123; <span class="keyword">return</span> j + <span class="number">1</span>; &#125;;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"by_val_lambda: "</span>&lt;&lt;by_val_lambda()&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// 11</span></span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"by_ref_lambda: "</span>&lt;&lt;by_ref_lambda()&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// 11</span></span><br><span class="line"></span><br><span class="line">    ++j;                   </span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"by_val_lambda: "</span>&lt;&lt;by_val_lambda()&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// 11</span></span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"by_ref_lambda: "</span>&lt;&lt;by_ref_lambda()&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// 12</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;              </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里要小心,第三个是11.值捕获是不会反应外部变量的更新的,因为值传递产生的是拷贝值,而Lambda中有事const的,所以不变.</p>
<p><img src="/2018/07/29/Cpp-Lambda%E8%A1%A8%E8%BE%BE%E5%BC%8F/Snipaste_2018-07-29_22-59-52.png" alt></p>
<h4 id="3、隐式捕获"><a href="#3、隐式捕获" class="headerlink" title="3、隐式捕获"></a><strong>3、隐式捕获</strong></h4><p>上面的值捕获和引用捕获都需要我们在捕获列表中显示列出Lambda表达式中使用的外部变量。除此之外，我们还可以让编译器根据函数体中的代码来推断需要捕获哪些变量，这种方式称之为隐式捕获。隐式捕获有两种方式，分别是[=]和[&amp;]。[=]表示以值捕获的方式捕获外部变量，[&amp;]表示以引用捕获的方式捕获外部变量。</p>
<p>隐式值捕获示例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">123</span>;</span><br><span class="line">    <span class="keyword">auto</span> f = [=] &#123; <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>; &#125;;    <span class="comment">// 值捕获</span></span><br><span class="line">    f(); <span class="comment">// 输出：123</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>隐式引用捕获示例：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">123</span>;</span><br><span class="line">    <span class="keyword">auto</span> f = [&amp;] &#123; <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>; &#125;;    <span class="comment">// 引用捕获</span></span><br><span class="line">    a = <span class="number">321</span>;</span><br><span class="line">    f(); <span class="comment">// 输出：321</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="4、混合方式"><a href="#4、混合方式" class="headerlink" title="4、混合方式"></a><strong>4、混合方式</strong></h4><p>上面的例子，要么是值捕获，要么是引用捕获，Lambda表达式还支持混合的方式捕获外部变量，这种方式主要是以上几种捕获方式的组合使用。</p>
<p>到这里，我们来总结一下：C++11中的Lambda表达式捕获外部变量主要有以下形式：</p>
<table>
<thead>
<tr>
<th>捕获形式</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>[]</td>
<td>不捕获任何外部变量</td>
</tr>
<tr>
<td>[变量名, …]</td>
<td>默认以值得形式捕获指定的多个外部变量（用逗号分隔），如果引用捕获，需要显示声明（使用&amp;说明符）</td>
</tr>
<tr>
<td>[this]</td>
<td>以值的形式捕获this指针</td>
</tr>
<tr>
<td>[=]</td>
<td>以值的形式捕获所有外部变量</td>
</tr>
<tr>
<td>[&amp;]</td>
<td>以引用形式捕获所有外部变量</td>
</tr>
<tr>
<td>[=, &amp;x]</td>
<td>变量x以引用形式捕获，其余变量以传值形式捕获</td>
</tr>
<tr>
<td>[&amp;, x]</td>
<td>变量x以值的形式捕获，其余变量以引用形式捕获</td>
</tr>
</tbody></table>
<h2 id="修改捕获变量"><a href="#修改捕获变量" class="headerlink" title="修改捕获变量"></a><strong>修改捕获变量</strong></h2><p>前面我们提到过，在Lambda表达式中，如果以传值方式捕获外部变量，则函数体中不能修改该外部变量，否则会引发编译错误。那么有没有办法可以修改值捕获的外部变量呢？这是就需要使用mutable关键字，该关键字用以说明表达式体内的代码可以修改值捕获的变量，示例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">123</span>;</span><br><span class="line">    <span class="keyword">auto</span> f = [a]()<span class="keyword">mutable</span> &#123; <span class="built_in">cout</span> &lt;&lt; ++a; &#125;; <span class="comment">// 不会报错</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>; <span class="comment">// 输出：123</span></span><br><span class="line">    f(); <span class="comment">// 输出：124</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><em>需要注意：被mutable修饰的lambda表达式就算没有参数也要写明参数列表。</em></p>
<p>原因：lambda表达式可以说是就地定义仿函数闭包的“语法糖”。它的捕获列表捕获住的任何外部变量，最终均会变为闭包类型的成员变量。按照C++标准，lambda表达式的operator()默认是const的，一个const成员函数是无法修改成员变量的值的。而mutable的作用，就在于取消operator()的const。 </p>
<h2 id="Lambda表达式的参数"><a href="#Lambda表达式的参数" class="headerlink" title="Lambda表达式的参数"></a><strong>Lambda表达式的参数</strong></h2><p>Lambda表达式的参数和普通函数的参数类似，那么这里为什么还要拿出来说一下呢？原因是在Lambda表达式中传递参数还有一些限制，主要有以下几点：</p>
<ol>
<li>参数列表中不能有默认参数</li>
<li>不支持可变参数</li>
<li>所有参数必须有参数名</li>
</ol>
<p>常用举例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">　　　　 <span class="keyword">int</span> m = [](<span class="keyword">int</span> x) &#123; <span class="keyword">return</span> [](<span class="keyword">int</span> y) &#123; <span class="keyword">return</span> y * <span class="number">2</span>; &#125;(x)+<span class="number">6</span>; &#125;(<span class="number">5</span>);</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"m:"</span> &lt;&lt; m &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;            　　<span class="comment">//输出m:16</span></span><br><span class="line"></span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"n:"</span> &lt;&lt; [](<span class="keyword">int</span> x, <span class="keyword">int</span> y) &#123; <span class="keyword">return</span> x + y; &#125;(<span class="number">5</span>, <span class="number">4</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;            <span class="comment">//输出n:9</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">auto</span> gFunc = [](<span class="keyword">int</span> x) -&gt; function&lt;<span class="keyword">int</span>(<span class="keyword">int</span>)&gt; &#123; <span class="keyword">return</span> [=](<span class="keyword">int</span> y) &#123; <span class="keyword">return</span> x + y; &#125;; &#125;;</span><br><span class="line">        <span class="keyword">auto</span> lFunc = gFunc(<span class="number">4</span>);</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; lFunc(<span class="number">5</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">auto</span> hFunc = [](<span class="keyword">const</span> function&lt;<span class="keyword">int</span>(<span class="keyword">int</span>)&gt;&amp; f, <span class="keyword">int</span> z) &#123; <span class="keyword">return</span> f(z) + <span class="number">1</span>; &#125;;</span><br><span class="line">        <span class="keyword">auto</span> a = hFunc(gFunc(<span class="number">7</span>), <span class="number">8</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> a = <span class="number">111</span>, b = <span class="number">222</span>;</span><br><span class="line">        <span class="keyword">auto</span> func = [=, &amp;b]()<span class="keyword">mutable</span> &#123; a = <span class="number">22</span>; b = <span class="number">333</span>; <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"a:"</span> &lt;&lt; a &lt;&lt; <span class="string">" b:"</span> &lt;&lt; b &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>; &#125;;</span><br><span class="line"></span><br><span class="line">        func();</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"a:"</span> &lt;&lt; a &lt;&lt; <span class="string">" b:"</span> &lt;&lt; b &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">        a = <span class="number">333</span>;</span><br><span class="line">        <span class="keyword">auto</span> func2 = [=, &amp;a] &#123; a = <span class="number">444</span>; <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"a:"</span> &lt;&lt; a &lt;&lt; <span class="string">" b:"</span> &lt;&lt; b &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>; &#125;;</span><br><span class="line">        func2();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">auto</span> func3 = [](<span class="keyword">int</span> x) -&gt;function&lt;<span class="keyword">int</span>(<span class="keyword">int</span>)&gt; &#123; <span class="keyword">return</span> [=](<span class="keyword">int</span> y) &#123; <span class="keyword">return</span> x + y; &#125;; &#125;;</span><br><span class="line"></span><br><span class="line">    　　</span><br><span class="line">　　　　 <span class="built_in">std</span>::function&lt;<span class="keyword">void</span>(<span class="keyword">int</span> x)&gt; f_display_42 = [](<span class="keyword">int</span> x) &#123; print_num(x); &#125;;</span><br><span class="line">	f_display_42(<span class="number">44</span>);</span><br><span class="line">　　&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>Clion的配置</title>
    <url>/2018/04/15/Clion%E7%9A%84%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<p>刚才看到安装已久的Clion想点开试试，结果发现在配置上有点问题。</p>
<a id="more"></a>
<p>&emsp;首先要安装Mingw，这里的安装我就不介绍了，主要是把gcc和g++安上。Clion还支持用VS的编译器和调试器，还支持Cygwin和WSL(Windows内置Linux)。但是后面几个我还没空折腾。<br><br>&emsp;安装Clion后需要配置编译器和调试器的位置，先看下图：<br><img src="/2018/04/15/Clion%E7%9A%84%E9%85%8D%E7%BD%AE/%E9%85%8D%E7%BD%AE.png" alt="配置"><br>Detect表示检测，Bundled表示捆绑的。Debugger和Cmake可以用内置的，也可以选择自己的，但是Mingw的Debugger不在支持版本内，Cmake的版本也是有要求的。C和C++的编译器填Mingw内的相关程序就行。<br>还有非常坑的一点：project的名字绝对不能用test！</p>
]]></content>
      <tags>
        <tag>杂谈</tag>
      </tags>
  </entry>
  <entry>
    <title>C++ 中的类型限定符</title>
    <url>/2018/07/29/Cpp%E4%B8%AD%E7%9A%84%E7%B1%BB%E5%9E%8B%E9%99%90%E5%AE%9A%E7%AC%A6/</url>
    <content><![CDATA[<p>C++中有许多限定符,修饰符,这里列了几个.</p>
<a id="more"></a>

<h2 id="C-中的类型限定符"><a href="#C-中的类型限定符" class="headerlink" title="C++ 中的类型限定符"></a>C++ 中的类型限定符</h2><p>类型限定符提供了变量的额外信息。</p>
<table>
<thead>
<tr>
<th>限定符</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>const</td>
<td><strong>const</strong> 类型的对象在程序执行期间不能被修改改变。</td>
</tr>
<tr>
<td>volatile</td>
<td>修饰符 <strong>volatile</strong> 告诉编译器，变量的值可能以程序未明确指定的方式被改变。</td>
</tr>
<tr>
<td>restrict</td>
<td>由 <strong>restrict</strong> 修饰的指针是唯一一种访问它所指向的对象的方式。只有 C99 增加了新的类型限定符 restrict。</td>
</tr>
</tbody></table>
<p><strong>volatile 往往会用于多线程的修饰，比如：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">volatile</span> boolean isNext = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">Thread A（） &#123;</span><br><span class="line">    <span class="comment">// 第一个工作</span></span><br><span class="line">    <span class="comment">// isNext = true;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Thread B （）&#123;</span><br><span class="line">    <span class="keyword">if</span> (isNext) &#123;</span><br><span class="line">        <span class="comment">// 第二个工作</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里volatile 就是从来标记isNext， 以确保线程B每次都重新从内存中读取isNext的值，第二个工作一定在第一个工作之后进行。</p>
<p>但是要注意，这里无法保证顺序性，因为编译器编译的时候会重新打乱两个语句的先后顺序，因此做第一个工作和赋值给isNext不一定会按照你代码顺序正常执行。</p>
<h1 id="explicit"><a href="#explicit" class="headerlink" title="explicit"></a>explicit</h1><p>C++提供了关键字explicit，可以阻止不应该允许的经过转换构造函数进行的隐式转换的发生。声明为explicit的构造函数不能在隐式转换中使用。</p>
<p>C++中， 一个参数的构造函数(或者除了第一个参数外其余参数都有默认值的多参构造函数)， 承担了两个角色。 1 是个构造器 ，2 是个默认且隐含的类型转换操作符。</p>
<p>所以， 有时候在我们写下如 AAA = XXX， 这样的代码， 且恰好XXX的类型正好是AAA单参数构造器的参数类型， 这时候编译器就自动调用这个构造器， 创建一个AAA的对象。</p>
<p>这样看起来好象很酷， 很方便。 但在某些情况下（见下面权威的例子）， 却违背了我们（程序员）的本意。 这时候就要在这个构造器前面加上explicit修饰， 指定这个构造器只能被明确的调用/使用， 不能作为类型转换操作符被隐含的使用。</p>
<p>explicit构造函数的作用</p>
<p>解析：</p>
<p>explicit构造函数是用来防止隐式转换的。请看下面的代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test1</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Test1(<span class="keyword">int</span> n)</span><br><span class="line">    &#123;</span><br><span class="line">        num=n;</span><br><span class="line">    &#125;<span class="comment">//普通构造函数</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> num;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test2</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">Test2</span><span class="params">(<span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        num=n;</span><br><span class="line">    &#125;<span class="comment">//explicit(显式)构造函数</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> num;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Test1 t1=<span class="number">12</span>;<span class="comment">//隐式调用其构造函数,成功</span></span><br><span class="line">    Test2 t2=<span class="number">12</span>;<span class="comment">//编译错误,不能隐式调用其构造函数</span></span><br><span class="line">    <span class="function">Test2 <span class="title">t2</span><span class="params">(<span class="number">12</span>)</span></span>;<span class="comment">//显式调用成功</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Test1的构造函数带一个int型的参数，代码23行会隐式转换成调用Test1的这个构造函数。而Test2的构造函数被声明为explicit（显式），这表示不能通过隐式转换来调用这个构造函数，因此代码24行会出现编译错误。</p>
<p>普通构造函数能够被隐式调用。而explicit构造函数只能被显式调用。</p>
<h1 id="C-修饰符类型"><a href="#C-修饰符类型" class="headerlink" title="C++ 修饰符类型"></a>C++ 修饰符类型</h1><p>C++ 允许在 <strong>char、int 和 double</strong> 数据类型前放置修饰符。修饰符用于改变基本类型的含义，所以它更能满足各种情境的需求。</p>
<p>下面列出了数据类型修饰符：</p>
<ul>
<li>signed</li>
<li>unsigned</li>
<li>long</li>
<li>short</li>
</ul>
<p>修饰符 <strong>signed、unsigned、long 和 short</strong> 可应用于整型，<strong>signed</strong> 和 <strong>unsigned</strong> 可应用于字符型，<strong>long</strong> 可应用于双精度型。</p>
<p>修饰符 <strong>signed</strong> 和 <strong>unsigned</strong> 也可以作为 <strong>long</strong> 或 <strong>short</strong> 修饰符的前缀。例如：<strong>unsigned long int</strong>。</p>
<p>C++ 允许使用速记符号来声明<strong>无符号短整数</strong>或<strong>无符号长整数</strong>。您可以不写 int，只写单词 <strong>unsigned、short</strong> 或 <strong>unsigned、long</strong>，int 是隐含的。例如，下面的两个语句都声明了无符号整型变量。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> x;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> y;</span><br></pre></td></tr></table></figure>

<p>为了理解 C++ 解释有符号整数和无符号整数修饰符之间的差别，我们来运行一下下面这个短程序：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * 这个程序演示了有符号整数和无符号整数之间的差别</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   short <span class="keyword">int</span> i;           <span class="comment">// 有符号短整数</span></span><br><span class="line">   short <span class="keyword">unsigned</span> <span class="keyword">int</span> j;  <span class="comment">// 无符号短整数</span></span><br><span class="line"> </span><br><span class="line">   j = <span class="number">50000</span>;</span><br><span class="line"> </span><br><span class="line">   i = j;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; i &lt;&lt; <span class="string">" "</span> &lt;&lt; j;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>当上面的程序运行时，会输出下列结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-15536 50000</span><br></pre></td></tr></table></figure>

<p>上述结果中，无符号短整数 50,000 的位模式被解释为有符号短整数 -15,536。</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>Cpp位域</title>
    <url>/2018/08/02/Cpp%E4%BD%8D%E5%9F%9F/</url>
    <content><![CDATA[<p>内存是以字节为单位进行编址的，编程语言的基本类型中，最小类型的长度一般也就是1个字节。然而，在解决某些问题时，必须要有二进制层面的表达手段，又或者某些情形下根本用不着1个字节，作为强大到令人窒息的C/C++，难道没有解决方法？其提供的完美解决方法就是位域（位段）结构，本文将从定义、说明、内存布局和使用这四个方面对它进行详细的介绍。</p>
<a id="more"></a>

<h3 id="1-位域定义"><a href="#1-位域定义" class="headerlink" title="1. 位域定义"></a>1. 位域定义</h3><p>首先，让我们看一下位域的定义。从定义中可以看出位域本质上其实就是结构体，只不过其成员都是按照特定长度的二进制位进行分配而已。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> 位域结构体名</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    位域列表；</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">其中，位域列表为： 类型说明符 位域名： 长度;  组成</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitField</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> a : <span class="number">2</span>; <span class="comment">// 第一个位域，2位</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> b : <span class="number">4</span>; <span class="comment">// 第二个，4位</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> c : <span class="number">2</span>; <span class="comment">// 第三个，2位</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="2-位域说明"><a href="#2-位域说明" class="headerlink" title="2. 位域说明"></a>2. 位域说明</h3><p>其次，我们来看一下位域的一些说明:</p>
<p>\1. 位域可以没有名字的，这时它只用来作填充或调整位置。无名的位域是不能使用的</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitFiled_1</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">unsigned</span> a : <span class="number">4</span>;        </span><br><span class="line">    <span class="keyword">unsigned</span> :   <span class="number">0</span>; <span class="comment">// 无名位域， 无法使用</span></span><br><span class="line">    <span class="keyword">unsigned</span> b : <span class="number">4</span>;       </span><br><span class="line">    <span class="keyword">unsigned</span> c : <span class="number">4</span>;       </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>\2. 宽度为 0 的一个未命名位域强制下一位域对齐到其下一type位域的边界</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitFiled_1</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">unsigned</span> a : <span class="number">4</span>; <span class="comment">// 第一个unsigned int,占4Byte</span></span><br><span class="line">    <span class="keyword">unsigned</span> :   <span class="number">0</span>; <span class="comment">// 未命名位域</span></span><br><span class="line">    <span class="keyword">unsigned</span> b : <span class="number">4</span>; <span class="comment">// 从第二个unsigned int的4Byte开始存放，占4位</span></span><br><span class="line">    <span class="keyword">unsigned</span> c : <span class="number">4</span>; <span class="comment">// 还是第二个unsigned int中的4位</span></span><br><span class="line">    <span class="comment">// 该位域结构总共 8 Byte</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>位域的长度不能大于其类型说明符中指定类型的固有长度，比如说int类型的位域长度不能超过32（bit），char的位域长度不能超过8（bit）</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitField_2</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span>  a : <span class="number">33</span>; <span class="comment">// 编译错误，C2034，BitField_2::a位域类型的位数太小, &gt;32</span></span><br><span class="line">    <span class="keyword">char</span> b : <span class="number">10</span>; <span class="comment">// 编译错误，C2034，BitField_2::b位域类型的位数太小, &gt;8</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="3-位域内存布局"><a href="#3-位域内存布局" class="headerlink" title="3. 位域内存布局"></a>3. 位域内存布局</h3><p>位域有一个非常重要的用途就是压缩存储，即：能够用1个比特解决的问题，绝不用2个比特。因此，我们非常有必要研究一下其内存布局，这样才能对其压缩存储特性有深入的了解。</p>
<ol>
<li><p>整个位域结构体的总大小为<strong>最宽基本类型成员大小的整数倍</strong>，这一点与常规结构体类型是一致的，从这里也可看出，位域本质上就是结构体；</p>
</li>
<li><p>如果<strong>相邻位域字段</strong>的类型相同，且其声明的位宽长度之和小于类型的大小（sizeof获取的大小），则后面的位域字段将紧邻前一个字段存储，直到不能容纳为止；</p>
</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 假如 BitField_3::a = 0x11,(0001 0001 B); BitField_3::b = 0x2,(10 B); </span></span><br><span class="line"><span class="comment">//     BitField_3::c = 0x35,(11 0101 B), 则有：</span></span><br><span class="line"><span class="comment">// 第一个4字节为：0001 0001 0000 0010 0000 0000 0000 0000（B）</span></span><br><span class="line"><span class="comment">// 第二个4字节为：0011 0101 0000 0000 0000 0000 0000 0000（B）</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitField_3</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span>   a: <span class="number">8</span>;    <span class="comment">// 第一个4Byte中的开始8bit</span></span><br><span class="line">    <span class="keyword">int</span>   b: <span class="number">2</span>;    <span class="comment">// 由于相邻两个位域的类型相同，总大小10 &lt; 32(int类型大小）</span></span><br><span class="line">                   <span class="comment">// 所以这里存储是挨着第一个4Byte，紧接着的2bit</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">char</span>  c: <span class="number">6</span>;    <span class="comment">// 由于相邻的两个位域类型不同</span></span><br><span class="line">                   <span class="comment">// 所以这里是第二个4Byte</span></span><br><span class="line">    <span class="comment">// 总共8Byte</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>如果<strong>相邻位域字段</strong>的类型相同，但其位宽之和大于类型的sizeof大小，则后面的位域字段将从新的存储单元开始，其起始偏移量为类型大小的整数倍；</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 假如 BitField_4::a = 0x1,(0001 B); BitField_4::b = 0x08676665</span></span><br><span class="line"><span class="comment">// 则有：第一个4字节：0000 0001 0000 0000 0000 0000 0000 0000（B）</span></span><br><span class="line"><span class="comment">// 第二个4字节：0x65 66 67 08（小端模式，高高低低，高字节存放在高地址）</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitField_4</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> a: <span class="number">4</span>;   <span class="comment">// 第一个4字节</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> b: <span class="number">29</span>;  <span class="comment">// 虽然相邻位域类型相同</span></span><br><span class="line">                <span class="comment">// 但是，4 + 29 &gt; 32</span></span><br><span class="line">                <span class="comment">// 因此b位域从第二个4字节开始存储</span></span><br><span class="line">    <span class="comment">// 总共8字节</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>如果相邻位域字段的<strong>类型不同</strong>，则各编译器的具体实现有差异，<strong>VC采取不压缩方式</strong></li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 假如 BitField_5::a = 0x4, (0100 B); BitField_5::b = 0x5, (0101 B), 则有：</span></span><br><span class="line"><span class="comment">// 第一个4字节：0000 0100 0000 0000 0000 0000 0000 0000（B）</span></span><br><span class="line"><span class="comment">// 第二个4字节：0000 0101 0000 0000 0000 0000 0000 0000（B）</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitField_5</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span>  a : <span class="number">4</span>;  <span class="comment">// 占据int的4字节中的4个bit</span></span><br><span class="line">    <span class="keyword">char</span> b : <span class="number">4</span>;  <span class="comment">// 相邻字段类型不同，就算类型是char，也另起一个4字节</span></span><br><span class="line">    <span class="comment">// 总共8Byte，可见不但没有压缩，还浪费空间</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ol start="5">
<li>如果位域字段之间穿插着非位域字段，则不进行压缩；</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 假如 BitField_6::a = 0x4,(0100 B); BitField_6::b = 0x65, (0110 0101 B)</span></span><br><span class="line"><span class="comment">// BitField_6::c = 0x3,(0011 B), 则有：</span></span><br><span class="line"><span class="comment">// 第一个字节： 0000 0100 （B）</span></span><br><span class="line"><span class="comment">// 第二个字节： 0110 0101 （B）</span></span><br><span class="line"><span class="comment">// 第三个字节： 0000 0011 （B）</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitField_6</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">char</span> a: <span class="number">4</span>;  <span class="comment">// 第1个字节</span></span><br><span class="line">    <span class="keyword">char</span> b;     <span class="comment">// 第2个字节，非位域</span></span><br><span class="line">    <span class="keyword">char</span> c: <span class="number">4</span>;  <span class="comment">// 第3个字节</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>注意：如果不是位域字段之间穿插着非位域字段，如下面这种情况，是进行压缩的：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 以下为两个字节，可见进行了压缩存储</span></span><br><span class="line"><span class="comment">// 如果把 char a; 放到最后，只要不在中间，也是会压缩存储的</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitField_7</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">char</span> a;       <span class="comment">// 第1个字节</span></span><br><span class="line">    <span class="keyword">char</span> b: <span class="number">4</span>;    <span class="comment">// 第2个字节中的4位</span></span><br><span class="line">    <span class="keyword">char</span> c: <span class="number">4</span>;    <span class="comment">// 第2个字节紧挨着的4位</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>\6. 当使用有符号类型来定义位域，并且无意中使用到了正负（有意或者无意）特性时，就有问题了。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitField_8</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">char</span> a : <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">char</span> b : <span class="number">3</span>;</span><br><span class="line">    <span class="keyword">char</span> c : <span class="number">3</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitField_8</span> <span class="title">BF8</span>;</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">// 位域赋值</span></span><br><span class="line">BF8.a = <span class="number">0x3</span>;   <span class="comment">// 11</span></span><br><span class="line">BF8.b = <span class="number">0x5</span>;   <span class="comment">// 101</span></span><br><span class="line">BF8.c = <span class="number">0x2</span>;   <span class="comment">// 010</span></span><br><span class="line">    </span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"%d,%d,%d\n"</span>, BF8.a, BF8.b, BF8.c);</span><br><span class="line"></span><br><span class="line"><span class="comment">// OUTPUT:  -1（0xff, 1111 1111）, -3（0xfd, 1111 1101）, 2（0x02, 0000 0110）</span></span><br><span class="line"><span class="comment">// 可见，当为域的最高位是1的时候，会进行符号扩展，而且这也取决于编译器的实现</span></span><br><span class="line"><span class="comment">// 因此，为避免此类问题，最好使用无符号类型定义位域</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果把BitField_8中的char换成unsigned char就没有问题了，输出是3, 5, 2，</span></span><br></pre></td></tr></table></figure>

<hr>
<p>从其内存布局可以看出，使用位域的最佳实践是：<strong>第一，位域的类型要使用无符号类型，并且在整个结构体内部要保持一致；第二，位域的总长度尽量与类型的长度保持一致；第三，不要在两个位域中间穿插非位域字段；</strong>如下代码所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitFieldDemo</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> a : <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> b : <span class="number">3</span>;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> c : <span class="number">3</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="4-位域使用"><a href="#4-位域使用" class="headerlink" title="4. 位域使用"></a>4. 位域使用</h3><p>使用以下代码，再结合调试器的内存查看功能，即可清晰的验证本文 <strong>位域内存布局</strong> 一节所阐述的内容。本文使用编译器和调试器是Windows下的Visual Studio。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取位域大小，字节单位</span></span><br><span class="line"><span class="keyword">int</span> nsize = <span class="keyword">sizeof</span>(struct BitFieldDemo);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 位域定义及其赋值</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitFieldDemo</span> <span class="title">BFD</span>;</span>    <span class="comment">/* or = &#123;0x3, 0x5, 0x2&#125;*/</span></span><br><span class="line">BFD.a = <span class="number">0x3</span>;</span><br><span class="line">BFD.b = <span class="number">0x5</span>;</span><br><span class="line">BFD.c = <span class="number">0x2</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"BFD.a = %d, BFD.b = %d, BFD.c = %d \n"</span>, BFD.a, BFD.b, BFD.c);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 内存拷贝</span></span><br><span class="line"><span class="keyword">char</span> szBuffer[<span class="number">100</span>] = <span class="string">"abcdefghijklmnopqrstuvwxyz0123456789"</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BitFieldDemo</span> *<span class="title">pBFD</span> = <span class="title">NULL</span>;</span></span><br><span class="line">pBFD = (struct BitFieldDemo *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(struct BitFieldDemo));</span><br><span class="line"><span class="keyword">if</span> (pBFD != <span class="literal">NULL</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">memcpy</span>(pBFD, szBuffer, <span class="keyword">sizeof</span>(struct BitFieldDemo));</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"a = %d, b = %d, c = %d \n"</span>, pBFD-&gt;a, pBFD-&gt;b, pBFD-&gt;c);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">free</span>(pBFD);</span><br><span class="line">    pBFD = <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>Cpp中引用到底占不占空间</title>
    <url>/2018/07/30/Cpp%E4%B8%AD%E5%BC%95%E7%94%A8%E5%88%B0%E5%BA%95%E5%8D%A0%E4%B8%8D%E5%8D%A0%E7%A9%BA%E9%97%B4/</url>
    <content><![CDATA[<p>先放结论:对于C++标准来说,不占,确切说,未指定;对于程序来说,使用&amp;来查看引用的地址,发现指向引用对象的地址,可以看做不占;对于编译器来说,是占用的,因为要实现前面这种效果,不可能凭空而来.</p>
<a id="more"></a>

<p>首先,对于C++标准,它是这么说的:</p>
<p>8.3.2<br>4 It is unspecified whether or not a reference requires storage (3.7).</p>
<p> 也就是,标准并不关心引用是如何实现的(事实上,标准对于很多东西只规定了要什么样的,没规定具体怎么做).</p>
<p>目前,引用的实现方式就是使用常量指针.</p>
<p><a href="https://blog.csdn.net/feigegegegegegegeg/article/details/52613627" target="_blank" rel="noopener">https://blog.csdn.net/feigegegegegegegeg/article/details/52613627</a></p>
<p><a href="http://www.cnblogs.com/rollenholt/articles/1907408.html" target="_blank" rel="noopener">http://www.cnblogs.com/rollenholt/articles/1907408.html</a></p>
<p>下面举个例子：<br>int a = 10;<br>int &amp;b = a;</p>
<p>这里，显然，b是一个引用。<br>语句“int a = 10;”，在内存中申请了一个int型变量，32位机中占4个字节，那么变量名a放哪里了呢，有些人可能会有这个疑问。<br>其实a代表的是一个内存地址，you know，计算机访问内存中的数据，全部是通过地址进行访问（这个结论你可以从《汇编》的寻址方式中得出）。在编译器将你的代码生成目标文件时，它会用这个地址替代所有的a，因此目标文件中也就没有变量名a了，所以，变量名a不会占用内存也就理所当然了。<br>那么，引用 b会不会占内存空间呢？由于受大学课本的影响（引用b只是a的别名，b其实就是a本身），我很自然的会想到，b跟a一样，在编译后，所有的b也都用a的这个地址去替代，所以b也不会占用任何内存空间。<br>前段时间，这个问题在快速入门群和高级专业群中各位朋友讨论了一下，大家也众说纷纭。在网上也搜到很多帖子，说引用只是一个别名，它不会占用空间。<br>BUT，与各位高手的交流，查看众多网站，最后得出的结论是：   <strong>引用的本质是指针，而且是常量指针，占用4个字节的空间</strong>也就是说，“int &amp;b = a;”会开辟一块4个字节的空间，用于存放那个地址，当然,变量名b不会占用空间。<br>如果你听糊涂了，没关系，记住一条：引用b，本质就是一个常量指针（当然使用时不同）</p>
<p>探索c++的底层机制</p>
<p>在看这篇文章之前，请你先要明白一点：那就是c++为我们所提供的各种存取控制仅仅是在编译阶段给我们的限制，也就是说是编译器确保了你在完成任务之前的正确行为，如果你的行为不正确，那么你休想构造出任何可执行程序来。但如果真正到了产生可执行代码阶段，无论是c，c++,还是pascal，大家都一样，你认为c和c++编译器产生的机器代码会有所不同吗，你认为c++产生的机器代码会有访问限制吗？那么你错了。什么const，private，统统没有（const变量或许会放入只读数据段），它不会再给你任何的限制，你可以利用一切内存修改工具或者是自己写一个程序对某一进程空间的某一变量进行修改，不管它在你的印象中是private，还是public，对于此时的你来说都一样，想怎样便怎样。另外，你也不要为c++所提供的什么晚期捆绑等机制大呼神奇，它也仅仅是在所产生的代码中多加了几条而已，它远没有你想象的那么智能，所有的工作都是编译器帮你完成，真正到了执行的时候，计算机会完全按照编译器产生的代码一丝不苟的执行。你明白我在说什么吗？对了，如果你从前接触过汇编，只要你反汇编一段c++代码，你就会说：原来是这么回事呀，c++只不过是把我们的问题进行了更高层次的抽象，但只要你解开面纱，回到问题的本源，一切都将变得不再神秘……<br>（以下的反汇编代码均来自visial c++ 7.0）<br>一．让我们从变量开始—–并非你想象的那么简单<br>变量是什么，变量就是一个在程序执行过程中可以改变的量。换一个角度，变量是一块内存区域的名字，它就代表这块内存区域，当我们对变量进行修改的时候，会引起内存区域中内容的改变。但是你若是学习过汇编或是计算机组成原理，那么你就会清楚对于一块内存区域来说，根本就不存在什么名字，它所仅有的标志就是他的地址，因此我们若想修改一块内存区域的内容，只有知道他的地址方能实现。看来所谓的变量一说只不过是编译器给我们进行的一种抽象，让我们不必去了解更多的细节，降低我们的思维跨度而已。例如下面这条语句：<br>int a=10;<br>按照我们的思维习惯来讲，就是“存在一个变量a，它的值是10”，一切都显得那么的自然。我们不必去在乎什么所谓的地址以及其他的一些细节。然而在这条语句的底层实现中，a已经不能算是一个变量了，它仅仅是一个标记，代表一个地址的标记：<br>mov dword ptr[a],0Ah;<br>怎么样，这条语句不像上面那条易于接受吧，因为它需要了解更多的细节，你几乎不能得到编译器的任何帮助，一切思维上的跨越必须由你自己完成。这条语句应该解释为“把10写入以a为地址的内存区域”。你说什么？a有些像指针？对，的确像，但还不是，只不过他们的过程似乎是类似的。这里所说的跨越实际上就是从一个现实问题到具体地址以及内存区域的跨越。<br>二．引用：你可以拥有引用，但编译器仅拥有指针（地址）<br>看过了第一条，你一定对编译器的工作有了一定的了解，实际上编译器就是程序员与底层之间的一个转换层，它把一个高级语言代码转换为低级语言代码，一个编译器完成的转换跨度越大，那么它也就会越复杂，因为程序员的工作都由他代为完成了。C++编译器必然比汇编编译器复杂就是这个道理。如果我问你引用和指针是一样的吗？你或许会说当然不一样了，指针容易产生不安全的因素，引用却不会，真的不会吗？我们来看下面这段代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> *e=<span class="keyword">new</span> <span class="keyword">int</span>(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> &amp;f=*e;</span><br><span class="line"></span><br><span class="line"><span class="keyword">delete</span> e;</span><br><span class="line"></span><br><span class="line">f=<span class="number">30</span>;</span><br></pre></td></tr></table></figure>

<p>你认为上面这段代码怎么样，我感觉就不很安全，它和指针有相同的隐患。因为它所引用的内存区域就不合法。<br>我个人认为，所谓的引用其实就是一种指针，只不过二者的接口并不相同，引用的接口有一定的限制。指针可以一对多，而引用却只能一对一，即&amp;refer不能被改变，但却并不能说一对一就是安全的，只不过危险的系数降低罢了。引用比指针更容易控制。<br>Ok,下面来说说指针，曾经有过汇编经验的人一定会说，恩，指针的某些地方有些像汇编，尤其是那个“*”，怎么就那么像汇编中的“[]”啊。呵呵，的确，它也涵盖了一个寻址的过程。看来指针的确是个比较低级的东西。然而引用却并不那么直接，虽然程序员用起来方便安全了许多。但是你要清楚，只有你可以拥有引用，编译器可没有这个工具，计算机并不认识这个东西。因此，它的底层机制实际上是和指针一样的。不要相信只有一块内存拷贝，不要认为引用可以为你节省一个指针的空间，因为这一切不会发生，编译器还是会把引用解释为指针。不管你相不相信，请看下面这段代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int&amp; b&#x3D;a;</span><br><span class="line"></span><br><span class="line">lea eax,[a];</span><br><span class="line"></span><br><span class="line">mov dword ptr[b],eax;把a的地址赋给地址为b的一块内存</span><br><span class="line"></span><br><span class="line">b&#x3D;50;</span><br><span class="line"></span><br><span class="line">mov eax,dword ptr[b];</span><br><span class="line"></span><br><span class="line">mov dword ptr[eax],32h;</span><br><span class="line"></span><br><span class="line">int *d&#x3D;&amp;a;</span><br><span class="line"></span><br><span class="line">lea eax,[a];</span><br><span class="line"></span><br><span class="line">mov dword ptr[d],eax</span><br><span class="line"></span><br><span class="line">*d&#x3D;60;</span><br><span class="line"></span><br><span class="line">mov eax,dword ptr[d]</span><br><span class="line"></span><br><span class="line">mov dword ptr[eax],3ch;</span><br></pre></td></tr></table></figure>



<p>以上的代码均来自具体的编译器，怎么样，相信了吧，好，让我再来做一个或许不怎么恰当的比拟，你一定编过有关线性表和栈的程序吧，线性表是一个非常灵活的数据结构，在他上面有许多的操作，然而栈呢，它是一个限制性操作的线性表，它的底层操作实际上是由线性表操作实现的。就好比stack与vector的关系，因此指针和引用的关系就好比线性表和栈的关系，引用也就是受限的指针，它对外的接口和指针虽然并不一样，但底层是相同的。<br>下面再来看看引用的一个重要用途，作为函数的参数传递的时候是怎样的情形：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">void swapr(int &amp;a, int &amp;b)；</span><br><span class="line"></span><br><span class="line">void swapr(int* a, int *b)；</span><br><span class="line"></span><br><span class="line">int a&#x3D;10;</span><br><span class="line"></span><br><span class="line">int b&#x3D;20;</span><br><span class="line"></span><br><span class="line">swapr(a, b);</span><br><span class="line"></span><br><span class="line">lea eax,[a]; </span><br><span class="line"></span><br><span class="line">push eax; &#x2F;&#x2F;把a的地址压入堆栈</span><br><span class="line"></span><br><span class="line">lea ecx,[b];</span><br><span class="line"></span><br><span class="line">push ecx;</span><br><span class="line"></span><br><span class="line">call swapr;</span><br><span class="line"></span><br><span class="line">swapr(&amp;a, &amp;b);</span><br><span class="line"></span><br><span class="line">lea eax,[a];</span><br><span class="line"></span><br><span class="line">push eax;</span><br><span class="line"></span><br><span class="line">lea ecx,[b];</span><br><span class="line"></span><br><span class="line">push ecx;</span><br><span class="line"></span><br><span class="line">call swapr;</span><br></pre></td></tr></table></figure>



<p>怎么样，用引用和指针传递参数无论是在效率上还是在空间上都是完全一样的，如果妄想不传入地址就修改实参的值，简直就是天方夜谭，这就说明引用的本质就是指针。毕竟它们的行为都太相似了，如果不是这样，你还有什么方法去实现引用吗？记住，引用只不过是编译器为你提供的一个有用且安全的工具，对于机器代码可无法表示它，它把指针一对多的缺点去除，禁止了你的不安全的操作。但回到问题的本源，他们没有任何区别。</p>
<p>都史的实例：</p>
<p>#include &lt;iostream.h&gt;</p>
<p>class A {<br>double *m_pDb;<br>};</p>
<p>class B {<br>double &m_db;<br>public:<br>B(double db = 0) : m_db(db) {}<br>};</p>
<p>int main() {<br>cout &lt;&lt; “size of class A:”&lt;&lt;sizeof(A) &lt;&lt; endl;<br>cout &lt;&lt; “size of class B:”&lt;&lt;sizeof(B) &lt;&lt; endl;<br>return 0;<br>}</p>
<p>结果：</p>
<p>size of class A:4</p>
<p>size of class B:4</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>C++函数</title>
    <url>/2018/07/29/Cpp%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p>C++函数的内容的基础.</p>
<a id="more"></a>

<h1 id="C-函数"><a href="#C-函数" class="headerlink" title="C++ 函数"></a>C++ 函数</h1><p>函数是一组一起执行一个任务的语句。每个 C++ 程序都至少有一个函数，即主函数 <strong>main()</strong> ，所有简单的程序都可以定义其他额外的函数。</p>
<p>您可以把代码划分到不同的函数中。如何划分代码到不同的函数中是由您来决定的，但在逻辑上，划分通常是根据每个函数执行一个特定的任务来进行的。</p>
<p>函数<strong>声明</strong>告诉编译器函数的名称、返回类型和参数。函数<strong>定义</strong>提供了函数的实际主体。</p>
<p>C++ 标准库提供了大量的程序可以调用的内置函数。例如，函数 <strong>strcat()</strong> 用来连接两个字符串，函数 <strong>memcpy()</strong> 用来复制内存到另一个位置。</p>
<p>函数还有很多叫法，比如方法、子例程或程序，等等。</p>
<h2 id="定义函数"><a href="#定义函数" class="headerlink" title="定义函数"></a>定义函数</h2><p>C++ 中的函数定义的一般形式如下：</p>
<p>return_type function_name( parameter list ) {    body of the function }</p>
<p>在 C++ 中，函数由一个函数头和一个函数主体组成。下面列出一个函数的所有组成部分：</p>
<ul>
<li><strong>返回类型：</strong>一个函数可以返回一个值。<strong>return_type</strong> 是函数返回的值的数据类型。有些函数执行所需的操作而不返回值，在这种情况下，return_type 是关键字 <strong>void</strong>。</li>
<li><strong>函数名称：</strong>这是函数的实际名称。函数名和参数列表一起构成了函数签名。</li>
<li><strong>参数：</strong>参数就像是占位符。当函数被调用时，您向参数传递一个值，这个值被称为实际参数。参数列表包括函数参数的类型、顺序、数量。参数是可选的，也就是说，函数可能不包含参数。</li>
<li><strong>函数主体：</strong>函数主体包含一组定义函数执行任务的语句。</li>
</ul>
<h2 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h2><p>以下是 <strong>max()</strong> 函数的源代码。该函数有两个参数 num1 和 num2，会返回这两个数中较大的那个数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数返回两个数中较大的那个数</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max</span><span class="params">(<span class="keyword">int</span> num1, <span class="keyword">int</span> num2)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="comment">// 局部变量声明</span></span><br><span class="line">   <span class="keyword">int</span> result;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">if</span> (num1 &gt; num2)</span><br><span class="line">      result = num1;</span><br><span class="line">   <span class="keyword">else</span></span><br><span class="line">      result = num2;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> result; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="函数声明"><a href="#函数声明" class="headerlink" title="函数声明"></a>函数声明</h2><p>函数<strong>声明</strong>会告诉编译器函数名称及如何调用函数。函数的实际主体可以单独定义。</p>
<p>函数声明包括以下几个部分：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">return_type <span class="title">function_name</span><span class="params">( parameter <span class="built_in">list</span> )</span></span>;</span><br></pre></td></tr></table></figure>

<p>针对上面定义的函数 max()，以下是函数声明：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max</span><span class="params">(<span class="keyword">int</span> num1, <span class="keyword">int</span> num2)</span></span>;</span><br></pre></td></tr></table></figure>

<p>在函数声明中，参数的名称并不重要，只有参数的类型是必需的，因此下面也是有效的声明：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max</span><span class="params">(<span class="keyword">int</span>, <span class="keyword">int</span>)</span></span>;</span><br></pre></td></tr></table></figure>

<p>当您在一个源文件中定义函数且在另一个文件中调用函数时，函数声明是必需的。在这种情况下，您应该在调用函数的文件顶部声明函数。</p>
<h2 id="调用函数"><a href="#调用函数" class="headerlink" title="调用函数"></a>调用函数</h2><p>创建 C++ 函数时，会定义函数做什么，然后通过调用函数来完成已定义的任务。</p>
<p>当程序调用函数时，程序控制权会转移给被调用的函数。被调用的函数执行已定义的任务，当函数的返回语句被执行时，或到达函数的结束括号时，会把程序控制权交还给主程序。</p>
<p>调用函数时，传递所需参数，如果函数返回一个值，则可以存储返回值。例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 函数声明</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max</span><span class="params">(<span class="keyword">int</span> num1, <span class="keyword">int</span> num2)</span></span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="comment">// 局部变量声明</span></span><br><span class="line">   <span class="keyword">int</span> a = <span class="number">100</span>;</span><br><span class="line">   <span class="keyword">int</span> b = <span class="number">200</span>;</span><br><span class="line">   <span class="keyword">int</span> ret;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 调用函数来获取最大值</span></span><br><span class="line">   ret = max(a, b);</span><br><span class="line"> </span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Max value is : "</span> &lt;&lt; ret &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 函数返回两个数中较大的那个数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max</span><span class="params">(<span class="keyword">int</span> num1, <span class="keyword">int</span> num2)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="comment">// 局部变量声明</span></span><br><span class="line">   <span class="keyword">int</span> result;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">if</span> (num1 &gt; num2)</span><br><span class="line">      result = num1;</span><br><span class="line">   <span class="keyword">else</span></span><br><span class="line">      result = num2;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> result; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="函数参数"><a href="#函数参数" class="headerlink" title="函数参数"></a>函数参数</h2><p>如果函数要使用参数，则必须声明接受参数值的变量。这些变量称为函数的<strong>形式参数</strong>。</p>
<p>形式参数就像函数内的其他局部变量，在进入函数时被创建，退出函数时被销毁。</p>
<p>当调用函数时，有两种向函数传递参数的方式：</p>
<table>
<thead>
<tr>
<th>调用类型</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td><a href="http://www.runoob.com/cplusplus/cpp-function-call-by-value.html" target="_blank" rel="noopener">传值调用</a></td>
<td>该方法把参数的实际值复制给函数的形式参数。在这种情况下，修改函数内的形式参数对实际参数没有影响。</td>
</tr>
<tr>
<td><a href="http://www.runoob.com/cplusplus/cpp-function-call-by-pointer.html" target="_blank" rel="noopener">指针调用</a></td>
<td>该方法把参数的地址复制给形式参数。在函数内，该地址用于访问调用中要用到的实际参数。这意味着，修改形式参数会影响实际参数。</td>
</tr>
<tr>
<td><a href="http://www.runoob.com/cplusplus/cpp-function-call-by-reference.html" target="_blank" rel="noopener">引用调用</a></td>
<td>该方法把参数的引用复制给形式参数。在函数内，该引用用于访问调用中要用到的实际参数。这意味着，修改形式参数会影响实际参数。</td>
</tr>
</tbody></table>
<p>默认情况下，C++ 使用<strong>传值调用</strong>来传递参数。一般来说，这意味着函数内的代码不能改变用于调用函数的参数。之前提到的实例，调用 max() 函数时，使用了相同的方法。</p>
<h2 id="参数的默认值"><a href="#参数的默认值" class="headerlink" title="参数的默认值"></a>参数的默认值</h2><p>当您定义一个函数，您可以为参数列表中后边的每一个参数指定默认值。当调用函数时，如果实际参数的值留空，则使用这个默认值。</p>
<p>这是通过在函数定义中使用赋值运算符来为参数赋值的。调用函数时，如果未传递参数的值，则会使用默认值，如果指定了值，则会忽略默认值，使用传递的值。请看下面的实例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sum</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b=<span class="number">20</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">int</span> result;</span><br><span class="line"> </span><br><span class="line">  result = a + b;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">return</span> (result);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="comment">// 局部变量声明</span></span><br><span class="line">   <span class="keyword">int</span> a = <span class="number">100</span>;</span><br><span class="line">   <span class="keyword">int</span> b = <span class="number">200</span>;</span><br><span class="line">   <span class="keyword">int</span> result;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 调用函数来添加值</span></span><br><span class="line">   result = sum(a, b);</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Total value is :"</span> &lt;&lt; result &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 再次调用函数</span></span><br><span class="line">   result = sum(a);</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Total value is :"</span> &lt;&lt; result &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>CPU的结构和寄存器</title>
    <url>/2018/01/24/CPU%E7%9A%84%E7%BB%93%E6%9E%84%E5%92%8C%E5%AF%84%E5%AD%98%E5%99%A8/</url>
    <content><![CDATA[<p>一个典型的CPU（此处讨论的不是某一具体的CPU）由运算器、控制器、寄存器（CPU工作原理）等器件构成，这些器件靠内部总线相连。前一章所说的总线，相对于CPU内部来说是外部总线。</p>
<a id="more"></a>

<p>内部总线实现CPU内部各个器件之间的联系，外部总线实现CPU和主板上其他器件的联系。简单地说，在 CPU中：</p>
<p>运算器进行信息处理；</p>
<p>寄存器进行信息存储；</p>
<p> 控制器控制各种器件进行工作；</p>
<p> 内部总线连接各种器件，在它们之间进行数据的传送。</p>
<p>《深入》一书中的结构如下图:</p>
<p>![新文档 2018-01-24_1](CPU的结构和寄存器/新文档 2018-01-24_1.jpg)</p>
<p>PC是指程序计数器，书中原话是“处理器的核心是一个字长的存储设备（或寄存器），称为程序计数器（PC）”，“寄存器文件是一个小的存储设备，由一些1字长的寄存器组成，每个寄存器都有唯一的名字”，“ALU计算新的数据和地址值”。？？？emm,恕我愚钝，看不懂，不知是翻译的锅，还是原作的锅，这段狗屁不通。</p>
<p>对于一个汇编程序员来说，CPU中的主要部件是寄存器。寄存器是CPU中程序员可以用指令读写的部件。程序员通过改变各种寄存器中的内容来实现对CPU的控制。不同的CPU，寄存器的个数、结构是不相同的。8086CPU有 14个寄存器，每个寄存器有一个名称。这些寄存器是：AX、BX、CX、DX、SI、DI、SP、BP、IP、CS、SS、DS、ES、PSW。</p>
<h1 id="通用寄存器"><a href="#通用寄存器" class="headerlink" title="通用寄存器"></a>通用寄存器</h1><p>8086CPU的所有寄存器都是16位的，可以存放两个字节。AX、BX、CX、DX这4个寄存器通常用来存放一般性的数据，被称为通用寄存器。<br>以AX为例，寄存器的逻辑结构如图所示。</p>
<p><img src="/2018/01/24/CPU%E7%9A%84%E7%BB%93%E6%9E%84%E5%92%8C%E5%AF%84%E5%AD%98%E5%99%A8/AX.png" alt="AX寄存器"></p>
<p>其存放的最大值是2^16。</p>
<p>8086CPU的上一代CPU中的寄存器都是8位的，为了保证兼容，8086CPU的AX，BX，CX，DX这四个寄存器都可以分为两个独立使用的8位寄存器（高位字节，低位字节）来使用：</p>
<ul>
<li>AX可分为AH和AL;</li>
</ul>
<ul>
<li>BX可分为BH和BL;</li>
</ul>
<ul>
<li>CX可分为CH和CL;</li>
</ul>
<ul>
<li>DX可分为DH和DL。</li>
</ul>
<p>以AX表示20000（D）为例：</p>
<p><img src="/2018/01/24/CPU%E7%9A%84%E7%BB%93%E6%9E%84%E5%92%8C%E5%AF%84%E5%AD%98%E5%99%A8/AX%E6%8B%86%E5%88%86.png" alt="AX拆分"></p>
<h1 id="字在寄存器中的存储"><a href="#字在寄存器中的存储" class="headerlink" title="字在寄存器中的存储"></a>字在寄存器中的存储</h1><p>出于兼容性考虑，8086CPU可以一次性处理两种尺寸的数据。</p>
<ul>
<li>字节：记为byte, —个字节由8 个 bit组成，可以存在8 位寄存器中。</li>
</ul>
<ul>
<li>字：记 为 word, 一个字由两个字节组成，这两个字节分别称为这个字的高位字节和低位字节。</li>
</ul>
<p>字：16bit；字节：8bit。在上图中，20000（D）表示为01001100 01000000，共16位，如果分开的话分别表示78和32。</p>
<h1 id="几条汇编指令"><a href="#几条汇编指令" class="headerlink" title="几条汇编指令"></a>几条汇编指令</h1><p><img src="/2018/01/24/CPU%E7%9A%84%E7%BB%93%E6%9E%84%E5%92%8C%E5%AF%84%E5%AD%98%E5%99%A8/%E6%B1%87%E7%BC%96%E6%8C%87%E4%BB%A4.png" alt="汇编指令"></p>
<p>简单来说：指令内容 目标 内容或位置（不区分大小写）</p>
<p>下表是练习1（H表示十六进制）</p>
<table>
<thead>
<tr>
<th>程序段中的指令</th>
<th align="center">指令执行后AX中的数据</th>
<th align="center">指令执行后BX中的数据</th>
</tr>
</thead>
<tbody><tr>
<td>mov ax,4E20H</td>
<td align="center">4E20H</td>
<td align="center">0000H</td>
</tr>
<tr>
<td>add ax,1406H</td>
<td align="center">6226H</td>
<td align="center">0000H</td>
</tr>
<tr>
<td>mov bx,2000H</td>
<td align="center">6226H</td>
<td align="center">2000H</td>
</tr>
<tr>
<td>add ax,bx</td>
<td align="center">8226H</td>
<td align="center">8226H</td>
</tr>
<tr>
<td>mov bx,ax</td>
<td align="center">8226H</td>
<td align="center">8226H</td>
</tr>
<tr>
<td>add ax,bx</td>
<td align="center"><strong>044CH</strong></td>
<td align="center">8226H</td>
</tr>
</tbody></table>
<p>最后一行的044CH是因为8226H+8226H=1044CH，但是寄存器里放不下1。</p>
<p>练习2：</p>
<table>
<thead>
<tr>
<th>程序段中的指令</th>
<th align="center">指令执行后AX中的数据</th>
<th align="center">指令执行后BX中的数据</th>
</tr>
</thead>
<tbody><tr>
<td>mov ax,001AH</td>
<td align="center">001AH</td>
<td align="center">0000H</td>
</tr>
<tr>
<td>mov bx,0026H</td>
<td align="center">001AH</td>
<td align="center">0026H</td>
</tr>
<tr>
<td>add al,bl</td>
<td align="center">0040H</td>
<td align="center">0026H</td>
</tr>
<tr>
<td>add ah,bl</td>
<td align="center">2640H</td>
<td align="center">0026H</td>
</tr>
<tr>
<td>add bh,al</td>
<td align="center">2640H</td>
<td align="center">4026H</td>
</tr>
<tr>
<td>mov ah,0</td>
<td align="center">0040H</td>
<td align="center">4026H</td>
</tr>
<tr>
<td>add al,85H</td>
<td align="center">00C5H</td>
<td align="center">4026H</td>
</tr>
<tr>
<td>add al,93H</td>
<td align="center"><strong>0058H</strong></td>
<td align="center">4026H</td>
</tr>
</tbody></table>
<p>同理，C5+85=158，只保留了58。然而，舍弃的数值不是真的丢了。这里如果改为add ax,93H,则结果为0158H。</p>
<p>注意，al是作为一个独立的8 位寄存器来使用的，和 ah没有关系，CPU在执行 这条指令时认为ah和 al是两个不相关的寄存器。不要错误地认为，诸如add al,93H的指 令产生的进位会存储在ah中，add al,93H进行的是8 位运算。</p>
<p>注意，在进行运算或传送时，一定要保证两个操作对象的位数是一致的。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mov ax,bl	（在 8位寄存器和16位寄存器之间传送数据）</span><br><span class="line">mov bh,ax	（在 16位寄存器和8位寄存器之间传送数据）</span><br><span class="line">mov al,20000 	（8位寄存器最大可存放值为255的数据）</span><br><span class="line">add al,100H		 (将一个高于8位的数据加到一个8位寄存器中）</span><br></pre></td></tr></table></figure>



<h1 id="练习"><a href="#练习" class="headerlink" title="练习"></a>练习</h1><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mov ax, 62627	  AX&#x3D;F4A3H</span><br><span class="line"></span><br><span class="line">mov ah,31H	     AX&#x3D;31A3H</span><br><span class="line"></span><br><span class="line">mov al,23H		 AX&#x3D;3123H</span><br><span class="line"></span><br><span class="line">add ax,ax		  AX&#x3D;6246H</span><br><span class="line"></span><br><span class="line">mov bx,826CH	   BX&#x3D;826CH</span><br><span class="line"></span><br><span class="line">mov cx,ax		  CX&#x3D;6246H</span><br><span class="line"></span><br><span class="line">mov ax,bx		  AX&#x3D;826CH</span><br><span class="line"></span><br><span class="line">add ax,bx	 	 AX&#x3D;04D8H</span><br><span class="line"></span><br><span class="line">mov al,bh	 	 AX&#x3D;0482H</span><br><span class="line"></span><br><span class="line">mov ah,bl	 	 AX&#x3D;6C82H</span><br><span class="line"></span><br><span class="line">add ah,ah	 	 AX&#x3D;D882H</span><br><span class="line"></span><br><span class="line">add al, 6		  AX&#x3D;D888H</span><br><span class="line"></span><br><span class="line">add al,al		  AX&#x3D;D810H</span><br><span class="line"></span><br><span class="line">mov ax,cx		  AX&#x3D;6246H</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#编程计算2^4</span><br><span class="line">mov ax,2</span><br><span class="line">add ax,ax	#ax&#x3D;2+2</span><br><span class="line">add ax,ax	#ax&#x3D;4+4</span><br><span class="line">add ax,ax	#ax&#x3D;8+8</span><br></pre></td></tr></table></figure>



]]></content>
      <tags>
        <tag>汇编</tag>
      </tags>
  </entry>
  <entry>
    <title>C++常量</title>
    <url>/2018/07/29/Cpp%E5%B8%B8%E9%87%8F/</url>
    <content><![CDATA[<p>C++的常量.</p>
<a id="more"></a>

<h1 id="C-常量"><a href="#C-常量" class="headerlink" title="C++ 常量"></a>C++ 常量</h1><p>常量是固定值，在程序执行期间不会改变。这些固定的值，又叫做<strong>字面量</strong>。</p>
<p>常量可以是任何的基本数据类型，可分为整型数字、浮点数字、字符、字符串和布尔值。</p>
<p>常量就像是常规的变量，只不过常量的值在定义后不能进行修改。</p>
<h2 id="整数常量"><a href="#整数常量" class="headerlink" title="整数常量"></a>整数常量</h2><p>整数常量可以是十进制、八进制或十六进制的常量。前缀指定基数：0x 或 0X 表示十六进制，0 表示八进制，不带前缀则默认表示十进制。</p>
<p>整数常量也可以带一个后缀，后缀是 U 和 L 的组合，U 表示无符号整数（unsigned），L 表示长整数（long）。后缀可以是大写，也可以是小写，U 和 L 的顺序任意。</p>
<p>下面列举几个整数常量的实例：</p>
<p>212         // 合法的 215u        // 合法的 0xFeeL      // 合法的 078         // 非法的：8 不是八进制的数字 032UU       // 非法的：不能重复后缀</p>
<p>以下是各种类型的整数常量的实例：</p>
<p>85         // 十进制 0213       // 八进制  0x4b       // 十六进制  30         // 整数  30u        // 无符号整数  30l        // 长整数  30ul       // 无符号长整数</p>
<h2 id="浮点常量"><a href="#浮点常量" class="headerlink" title="浮点常量"></a>浮点常量</h2><p>浮点常量由整数部分、小数点、小数部分和指数部分组成。您可以使用小数形式或者指数形式来表示浮点常量。</p>
<p>当使用小数形式表示时，必须包含整数部分、小数部分，或同时包含两者。当使用指数形式表示时， 必须包含小数点、指数，或同时包含两者。带符号的指数是用 e 或 E 引入的。</p>
<p>下面列举几个浮点常量的实例：</p>
<p>3.14159       // 合法的  314159E-5L    // 合法的  510E          // 非法的：不完整的指数 210f          // 非法的：没有小数或指数 .e55          // 非法的：缺少整数或分数</p>
<h2 id="布尔常量"><a href="#布尔常量" class="headerlink" title="布尔常量"></a>布尔常量</h2><p>布尔常量共有两个，它们都是标准的 C++ 关键字：</p>
<ul>
<li><strong>true</strong> 值代表真。</li>
<li><strong>false</strong> 值代表假。</li>
</ul>
<p>我们不应把 true 的值看成 1，把 false 的值看成 0。</p>
<h2 id="字符常量"><a href="#字符常量" class="headerlink" title="字符常量"></a>字符常量</h2><p>字符常量是括在单引号中。如果常量以 L（仅当大写时）开头，则表示它是一个宽字符常量（例如 L’x’），此时它必须存储在 <strong>wchar_t</strong> 类型的变量中。否则，它就是一个窄字符常量（例如 ‘x’），此时它可以存储在 <strong>char</strong> 类型的简单变量中。</p>
<p>字符常量可以是一个普通的字符（例如 ‘x’）、一个转义序列（例如 ‘\t’），或一个通用的字符（例如 ‘\u02C0’）。</p>
<p>在 C++ 中，有一些特定的字符，当它们前面有反斜杠时，它们就具有特殊的含义，被用来表示如换行符（\n）或制表符（\t）等。下表列出了一些这样的转义序列码：</p>
<table>
<thead>
<tr>
<th>转义序列</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>\</td>
<td>\ 字符</td>
</tr>
<tr>
<td>&#39;</td>
<td>‘ 字符</td>
</tr>
<tr>
<td>&quot;</td>
<td>“ 字符</td>
</tr>
<tr>
<td>?</td>
<td>? 字符</td>
</tr>
<tr>
<td>\a</td>
<td>警报铃声</td>
</tr>
<tr>
<td>\b</td>
<td>退格键</td>
</tr>
<tr>
<td>\f</td>
<td>换页符</td>
</tr>
<tr>
<td>\n</td>
<td>换行符</td>
</tr>
<tr>
<td>\r</td>
<td>回车</td>
</tr>
<tr>
<td>\t</td>
<td>水平制表符</td>
</tr>
<tr>
<td>\v</td>
<td>垂直制表符</td>
</tr>
<tr>
<td>\ooo</td>
<td>一到三位的八进制数</td>
</tr>
<tr>
<td>\xhh . . .</td>
<td>一个或多个数字的十六进制数</td>
</tr>
</tbody></table>
<h2 id="字符串常量"><a href="#字符串常量" class="headerlink" title="字符串常量"></a>字符串常量</h2><p>字符串字面值或常量是括在双引号 “” 中的。一个字符串包含类似于字符常量的字符：普通的字符、转义序列和通用的字符。</p>
<p>您可以使用空格做分隔符，把一个很长的字符串常量进行分行。</p>
<p>下面的实例显示了一些字符串常量。下面这三种形式所显示的字符串是相同的。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;hello, dear&quot;</span><br><span class="line"></span><br><span class="line">&quot;hello, \</span><br><span class="line"></span><br><span class="line">dear&quot;</span><br><span class="line"></span><br><span class="line">&quot;hello, &quot; &quot;d&quot; &quot;ear&quot;</span><br></pre></td></tr></table></figure>

<h2 id="定义常量"><a href="#定义常量" class="headerlink" title="定义常量"></a>定义常量</h2><p>在 C++ 中，有两种简单的定义常量的方式：</p>
<ul>
<li>使用 <strong>#define</strong> 预处理器。</li>
<li>使用 <strong>const</strong> 关键字。</li>
</ul>
<h2 id="define-预处理器"><a href="#define-预处理器" class="headerlink" title="#define 预处理器"></a>#define 预处理器</h2><p>下面是使用 #define 预处理器定义常量的形式：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> identifier value</span></span><br></pre></td></tr></table></figure>

<p>具体请看下面的实例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LENGTH 10   </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> WIDTH  5</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NEWLINE <span class="meta-string">'\n'</span></span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">int</span> area;  </span><br><span class="line">   </span><br><span class="line">   area = LENGTH * WIDTH;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; area;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; NEWLINE;</span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>宏定义是可以取消的</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">定义： #define    N    21</span><br><span class="line">取消： #undef    N    12</span><br></pre></td></tr></table></figure>

<p>define定义表达式时要注意“边缘效应”，例如如下定义：</p>
<p>#define N 2+3 //我们预想的N值是5，我们这样使用N，int a = N/2; //我们预想的a的值是2，可实际上a的值是3。原因在于在预处理阶段，编译器将 a = N/2处理成了 a = 2+3/2；这就是宏定义的字符串替换的“边缘效应”因此要如下定义：#define N (2+3)。const定义的表达式则没有上述问题。const定义的常量叫做常变量原因有二：const定义常量像变量一样检查类型；const可以在任何地方定义常量，编译器对它的处理过程与变量相似，只是分配内存的地方不同.</p>
<h2 id="const-关键字"><a href="#const-关键字" class="headerlink" title="const 关键字"></a>const 关键字</h2><p>您可以使用 <strong>const</strong> 前缀声明指定类型的常量，如下所示：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const type variable &#x3D; value;</span><br></pre></td></tr></table></figure>

<p>具体请看下面的实例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="keyword">const</span> <span class="keyword">int</span>  LENGTH = <span class="number">10</span>;</span><br><span class="line">   <span class="keyword">const</span> <span class="keyword">int</span>  WIDTH  = <span class="number">5</span>;</span><br><span class="line">   <span class="keyword">const</span> <span class="keyword">char</span> NEWLINE = <span class="string">'\n'</span>;</span><br><span class="line">   <span class="keyword">int</span> area;  </span><br><span class="line">   </span><br><span class="line">   area = LENGTH * WIDTH;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; area;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; NEWLINE;</span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>const限定符定以后是不可以改变的，所以在定义时必须赋初始值，要不然是错误的，除非这个变量是用extern修饰的外部变量。 例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> A=<span class="number">10</span>;       <span class="comment">//正确。</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> A;          <span class="comment">//错误，没有赋初始值。</span></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">const</span> <span class="keyword">int</span> A;   <span class="comment">//正确，使用extern的外部变量。</span></span><br></pre></td></tr></table></figure>

<p><strong>const关键字作用</strong></p>
<ul>
<li>为给读你代码的人传达非常有用的信息，声明一个参数为常量是为了告诉用户这个参数的应用目的；</li>
<li>通过给优化器一些附加信息，使关键字const也许能产生更紧凑的代码；</li>
<li>合理使用关键字const可以使编译器很自然的保护那些不希望被修改的参数，防止无意的代码修改，可以减少bug的出现；</li>
</ul>
<p><strong>const关键字应用</strong></p>
<ul>
<li>欲阻止一个变量被改变，可使用const，在定义该const变量时，需先初始化，以后就没有机会改变他了；</li>
<li>对指针而言，可以指定指针本身为const，也可以指定指针所指的数据为const，或二者同时指定为const；</li>
<li>在一个函数声明中，const可以修饰形参表明他是一个输入参数，在函数内部不可以改变其值；</li>
<li>对于类的成员函数，有时候必须指定其为const类型，表明其是一个常函数，不能修改类的成员变量；</li>
<li>对于类的成员函数，有时候必须指定其返回值为const类型，以使得其返回值不为“左值”。</li>
</ul>
<h2 id="宏定义-define-和常量-const-的区别"><a href="#宏定义-define-和常量-const-的区别" class="headerlink" title="宏定义 #define 和常量 const 的区别"></a>宏定义 #define 和常量 const 的区别</h2><p><strong>类型和安全检查不同</strong></p>
<p>宏定义是字符替换，没有数据类型的区别，同时这种替换没有类型安全检查，可能产生边际效应等错误；</p>
<p>const常量是常量的声明，有类型区别，需要在编译阶段进行类型检查</p>
<p><strong>编译器处理不同</strong></p>
<p>宏定义是一个”编译时”概念，在预处理阶段展开，不能对宏定义进行调试，生命周期结束与编译时期；</p>
<p>const常量是一个”运行时”概念，在程序运行使用，类似于一个只读行数据</p>
<p><strong>存储方式不同</strong></p>
<p>宏定义是直接替换，不会分配内存，存储与程序的代码段中；</p>
<p>const常量需要进行内存分配，存储与程序的数据段中</p>
<p><strong>定义域不同</strong></p>
<p><strong>#define：</strong></p>
<p>不管是在某个函数内，还是在所有函数之外，作用域都是从定义开始直到整个文件结尾。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f1</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="meta">#<span class="meta-keyword">define</span> N 12</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> n <span class="number">12</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f2</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;N &lt;&lt;<span class="built_in">endl</span>; <span class="comment">//正确，N已经定义过，不受定义域限制</span></span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;n &lt;&lt;<span class="built_in">endl</span>; <span class="comment">//错误，n定义域只在f1函数中</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>定义后能否取消</strong></p>
<p>宏定义可以通过#undef来使之前的宏定义失效</p>
<p>const常量定义后将在定义域内永久有效</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f1</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="meta">#<span class="meta-keyword">define</span> N 12</span></span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">int</span> n = <span class="number">12</span>;</span><br><span class="line"></span><br><span class="line">  <span class="meta">#<span class="meta-keyword">undef</span> N <span class="comment">//取消宏定义后，即使在f1函数中，N也无效了</span></span></span><br><span class="line">  <span class="meta">#<span class="meta-keyword">define</span> N 21<span class="comment">//取消后可以重新定义</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>是否可以做函数参数</strong></p>
<p>宏定义不能作为参数传递给函数</p>
<p>const常量可以在函数的参数列表中出现</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>C++变量类型</title>
    <url>/2018/07/29/Cpp%E5%8F%98%E9%87%8F%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<p>C++变量类型.</p>
<a id="more"></a>

<h1 id="C-变量类型"><a href="#C-变量类型" class="headerlink" title="C++ 变量类型"></a>C++ 变量类型</h1><p>变量其实只不过是程序可操作的存储区的名称。C++ 中每个变量都有指定的类型，类型决定了变量存储的大小和布局，该范围内的值都可以存储在内存中，运算符可应用于变量上。</p>
<p>变量的名称可以由字母、数字和下划线字符组成。它必须以字母或下划线开头。大写字母和小写字母是不同的，因为 C++ 是大小写敏感的。</p>
<p>基于前一章讲解的基本类型，有以下几种基本的变量类型，将在下一章中进行讲解：</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>bool</td>
<td>存储值 true 或 false。</td>
</tr>
<tr>
<td>char</td>
<td>通常是一个八位字节（一个字符）。这是一个整数类型。</td>
</tr>
<tr>
<td>int</td>
<td>对机器而言，整数的最自然的大小。</td>
</tr>
<tr>
<td>float</td>
<td>单精度浮点值。单精度是这样的格式，1位符号，8位指数，23位小数。<img src="http://www.runoob.com/wp-content/uploads/2014/09/v2-749cc641eb4d5dafd085e8c23f8826aa_hd.png" alt="img"></td>
</tr>
<tr>
<td>double</td>
<td>双精度浮点值。双精度是1位符号，11位指数，52位小数。<img src="http://www.runoob.com/wp-content/uploads/2014/09/v2-48240f0e1e0dd33ec89100cbe2d30707_hd.png" alt="img"></td>
</tr>
<tr>
<td>void</td>
<td>表示类型的缺失。</td>
</tr>
<tr>
<td>wchar_t</td>
<td>宽字符类型。</td>
</tr>
</tbody></table>
<p>C++ 也允许定义各种其他类型的变量，比如<strong>枚举、指针、数组、引用、数据结构、类</strong>等等，这将会在后续的章节中进行讲解。</p>
<p>下面我们将讲解如何定义、声明和使用各种类型的变量。</p>
<h2 id="C-中的变量定义"><a href="#C-中的变量定义" class="headerlink" title="C++ 中的变量定义"></a>C++ 中的变量定义</h2><p>变量定义就是告诉编译器在何处创建变量的存储，以及如何创建变量的存储。变量定义指定一个数据类型，并包含了该类型的一个或多个变量的列表，如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">type variable_list;</span><br></pre></td></tr></table></figure>

<p>在这里，<strong>type</strong> 必须是一个有效的 C++ 数据类型，可以是 char、wchar_t、int、float、double、bool 或任何用户自定义的对象，<strong>variable_list</strong>可以由一个或多个标识符名称组成，多个标识符之间用逗号分隔。下面列出几个有效的声明：</p>
<p>int    i, j, k; char   c, ch; float  f, salary; double d;</p>
<p>行 <strong>int i, j, k;</strong> 声明并定义了变量 i、j 和 k，这指示编译器创建类型为 int 的名为 i、j、k 的变量。</p>
<p>变量可以在声明的时候被初始化（指定一个初始值）。初始化器由一个等号，后跟一个常量表达式组成，如下所示：</p>
<p>type variable_name = value;</p>
<p>下面列举几个实例：</p>
<p>extern int d = 3, f = 5;    // d 和 f 的声明  int d = 3, f = 5;           // 定义并初始化 d 和 f byte z = 22;                // 定义并初始化 z char x = ‘x’;               // 变量 x 的值为 ‘x’</p>
<p>不带初始化的定义：带有静态存储持续时间的变量会被隐式初始化为 NULL（所有字节的值都是 0），其他所有变量的初始值是未定义的。</p>
<h2 id="C-中的变量声明"><a href="#C-中的变量声明" class="headerlink" title="C++ 中的变量声明"></a>C++ 中的变量声明</h2><p>变量声明向编译器保证变量以给定的类型和名称存在，这样编译器在不需要知道变量完整细节的情况下也能继续进一步的编译。变量声明只在编译时有它的意义，在程序连接时编译器需要实际的变量声明。</p>
<p>当您使用多个文件且只在其中一个文件中定义变量时（定义变量的文件在程序连接时是可用的），变量声明就显得非常有用。您可以使用 <strong>extern</strong> 关键字在任何地方声明一个变量。虽然您可以在 C++ 程序中多次声明一个变量，但变量只能在某个文件、函数或代码块中被定义一次。(和作用域有关)</p>
<h2 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h2><p>尝试下面的实例，其中，变量在头部就已经被声明，但它们是在主函数内被定义和初始化的：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 变量声明</span></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> a, b;</span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> c;</span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">float</span> f;</span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="comment">// 变量定义</span></span><br><span class="line">  <span class="keyword">int</span> a, b;</span><br><span class="line">  <span class="keyword">int</span> c;</span><br><span class="line">  <span class="keyword">float</span> f;</span><br><span class="line"> </span><br><span class="line">  <span class="comment">// 实际初始化</span></span><br><span class="line">  a = <span class="number">10</span>;</span><br><span class="line">  b = <span class="number">20</span>;</span><br><span class="line">  c = a + b;</span><br><span class="line"> </span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; c &lt;&lt; <span class="built_in">endl</span> ;</span><br><span class="line"> </span><br><span class="line">  f = <span class="number">70.0</span>/<span class="number">3.0</span>;</span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; f &lt;&lt; <span class="built_in">endl</span> ;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当上面的代码被编译和执行时，它会产生下列结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">30</span><br><span class="line">23.3333</span><br></pre></td></tr></table></figure>

<p>同样的，在函数声明时，提供一个函数名，而函数的实际定义则可以在任何地方进行。例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数声明</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">func</span><span class="params">()</span></span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 函数调用</span></span><br><span class="line">    <span class="keyword">int</span> i = func();</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 函数定义</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">func</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="C-中的左值（Lvalues）和右值（Rvalues）"><a href="#C-中的左值（Lvalues）和右值（Rvalues）" class="headerlink" title="C++ 中的左值（Lvalues）和右值（Rvalues）"></a>C++ 中的左值（Lvalues）和右值（Rvalues）</h2><p>C++ 中有两种类型的表达式：</p>
<ul>
<li><strong>左值（lvalue）：</strong>指向内存位置的表达式被称为左值（lvalue）表达式。左值可以出现在赋值号的左边或右边。</li>
<li><strong>右值（rvalue）：</strong>术语右值（rvalue）指的是存储在内存中某些地址的数值。右值是不能对其进行赋值的表达式，也就是说，右值可以出现在赋值号的右边，但不能出现在赋值号的左边。</li>
</ul>
<p>变量是左值，因此可以出现在赋值号的左边。数值型的字面值是右值，因此不能被赋值，不能出现在赋值号的左边。下面是一个有效的语句：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int g &#x3D; 20;</span><br></pre></td></tr></table></figure>

<p>但是下面这个就不是一个有效的语句，会生成编译时错误：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">10 &#x3D; 20;</span><br></pre></td></tr></table></figure>

<p>变量的类型间是可以互相转换的，转换又分为自动转换和强制转换。</p>
<p><strong>自动转换规则：</strong></p>
<ul>
<li>1、若参与运算量的类型不同，则先转换成同一类型，然后进行运算。</li>
<li>2、转换按数据长度增加的方向进行，以保证精度不降低。如int型和long型运算时，先把int量转成long型后再进行运算。     a、若两种类型的字节数不同，转换成字节数高的类型     b、若两种类型的字节数相同，且一种有符号，一种无符号，则转换成无符号类型</li>
<li>3、所有的浮点运算都是以双精度进行的，即使仅含float单精度量运算的表达式，也要先转换成double型，再作运算。</li>
<li>4、char型和short型参与运算时，必须先转换成int型。</li>
<li>5、在赋值运算中，赋值号两边量的数据类型不同时，赋值号右边量的类型将转换为左边量的类型。如果右边量的数据类型长度比左边长时，将丢失一部分数据，这样会降低精度:</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a=<span class="number">1</span>;</span><br><span class="line"><span class="keyword">double</span> b=<span class="number">2.5</span>;</span><br><span class="line">a=b;</span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; a; <span class="comment">//输出为 2，丢失小数部分</span></span><br></pre></td></tr></table></figure>

<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">double</span> b = <span class="number">2.1</span>;</span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; <span class="string">"a + b = "</span> &lt;&lt; a + b &lt;&lt; <span class="built_in">endl</span>;  <span class="comment">//输出为a + b = 3.1</span></span><br></pre></td></tr></table></figure>

<p><strong>强制转换规则：</strong></p>
<p>强制类型转换是通过类型转换运算来实现的。其一般形式为：（类型说明符）（表达式）其功能是把表达式的运算结果强制转换成类型说明符所表示的类型</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">double</span> b = <span class="number">2.1</span>;</span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; <span class="string">"a + b = "</span> &lt;&lt; a + (<span class="keyword">int</span>)b &lt;&lt; <span class="built_in">endl</span>;  <span class="comment">//输出为a + b = 3</span></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>C++变量作用域</title>
    <url>/2018/07/29/Cpp%E5%8F%98%E9%87%8F%E4%BD%9C%E7%94%A8%E5%9F%9F/</url>
    <content><![CDATA[<p>C++变量作用域.</p>
<a id="more"></a>

<h1 id="C-变量作用域"><a href="#C-变量作用域" class="headerlink" title="C++ 变量作用域"></a>C++ 变量作用域</h1><p>作用域是程序的一个区域，一般来说有三个地方可以定义变量：</p>
<ul>
<li>在函数或一个代码块内部声明的变量，称为局部变量。</li>
<li>在函数参数的定义中声明的变量，称为形式参数。</li>
<li>在所有函数外部声明的变量，称为全局变量。</li>
</ul>
<p>我们将在后续的章节中学习什么是函数和参数。本章我们先来讲解什么是局部变量和全局变量。</p>
<h2 id="局部变量"><a href="#局部变量" class="headerlink" title="局部变量"></a>局部变量</h2><p>在函数或一个代码块内部声明的变量，称为局部变量。它们只能被函数内部或者代码块内部的语句使用。下面的实例使用了局部变量：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="comment">// 局部变量声明</span></span><br><span class="line">  <span class="keyword">int</span> a, b;</span><br><span class="line">  <span class="keyword">int</span> c;</span><br><span class="line"> </span><br><span class="line">  <span class="comment">// 实际初始化</span></span><br><span class="line">  a = <span class="number">10</span>;</span><br><span class="line">  b = <span class="number">20</span>;</span><br><span class="line">  c = a + b;</span><br><span class="line"> </span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; c;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="全局变量"><a href="#全局变量" class="headerlink" title="全局变量"></a>全局变量</h2><p>在所有函数外部定义的变量（通常是在程序的头部），称为全局变量。全局变量的值在程序的整个生命周期内都是有效的。</p>
<p>全局变量可以被任何函数访问。也就是说，全局变量一旦声明，在整个程序中都是可用的。下面的实例使用了全局变量和局部变量：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 全局变量声明</span></span><br><span class="line"><span class="keyword">int</span> g;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="comment">// 局部变量声明</span></span><br><span class="line">  <span class="keyword">int</span> a, b;</span><br><span class="line"> </span><br><span class="line">  <span class="comment">// 实际初始化</span></span><br><span class="line">  a = <span class="number">10</span>;</span><br><span class="line">  b = <span class="number">20</span>;</span><br><span class="line">  g = a + b;</span><br><span class="line"> </span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; g;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="初始化局部变量和全局变量"><a href="#初始化局部变量和全局变量" class="headerlink" title="初始化局部变量和全局变量"></a>初始化局部变量和全局变量</h2><p>当局部变量被定义时，系统不会对其初始化，您必须自行对其初始化。定义全局变量时，系统会自动初始化为下列值：</p>
<table>
<thead>
<tr>
<th>数据类型</th>
<th>初始化默认值</th>
</tr>
</thead>
<tbody><tr>
<td>int</td>
<td>0</td>
</tr>
<tr>
<td>char</td>
<td>‘\0’</td>
</tr>
<tr>
<td>float</td>
<td>0</td>
</tr>
<tr>
<td>double</td>
<td>0</td>
</tr>
<tr>
<td>pointer</td>
<td>NULL</td>
</tr>
</tbody></table>
<p>正确地初始化变量是一个良好的编程习惯，否则有时候程序可能会产生意想不到的结果。</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>Cpp数字</title>
    <url>/2018/07/30/Cpp%E6%95%B0%E5%AD%97/</url>
    <content><![CDATA[<p>数学运算和生成随机数.</p>
<a id="more"></a>

<h2 id="C-数学运算"><a href="#C-数学运算" class="headerlink" title="C++ 数学运算"></a>C++ 数学运算</h2><p>在 C++ 中，除了可以创建各种函数，还包含了各种有用的函数供您使用。这些函数写在标准 C 和 C++ 库中，叫做<strong>内置</strong>函数。您可以在程序中引用这些函数。</p>
<p>C++ 内置了丰富的数学函数，可对各种数字进行运算。下表列出了 C++ 中一些有用的内置的数学函数。</p>
<p>为了利用这些函数，您需要引用数学头文件 <strong><cmath></cmath></strong>。</p>
<table>
<thead>
<tr>
<th>序号</th>
<th>函数 &amp; 描述</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><strong>double cos(double);</strong> 该函数返回弧度角（double 型）的余弦。</td>
</tr>
<tr>
<td>2</td>
<td><strong>double sin(double);</strong> 该函数返回弧度角（double 型）的正弦。</td>
</tr>
<tr>
<td>3</td>
<td><strong>double tan(double);</strong> 该函数返回弧度角（double 型）的正切。</td>
</tr>
<tr>
<td>4</td>
<td><strong>double log(double);</strong> 该函数返回参数的自然对数。</td>
</tr>
<tr>
<td>5</td>
<td><strong>double pow(double, double);</strong> 假设第一个参数为 x，第二个参数为 y，则该函数返回 x 的 y 次方。</td>
</tr>
<tr>
<td>6</td>
<td><strong>double hypot(double, double);</strong> 该函数返回两个参数的平方总和的平方根，也就是说，参数为一个直角三角形的两个直角边，函数会返回斜边的长度。</td>
</tr>
<tr>
<td>7</td>
<td><strong>double sqrt(double);</strong> 该函数返回参数的平方根。</td>
</tr>
<tr>
<td>8</td>
<td><strong>int abs(int);</strong> 该函数返回整数的绝对值。</td>
</tr>
<tr>
<td>9</td>
<td><strong>double fabs(double);</strong> 该函数返回任意一个十进制数的绝对值。</td>
</tr>
<tr>
<td>10</td>
<td><strong>double floor(double);</strong> 该函数返回一个小于或等于传入参数的最大整数。</td>
</tr>
</tbody></table>
<p>下面是一个关于数学运算的简单实例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="comment">// 数字定义</span></span><br><span class="line">   short  s = <span class="number">10</span>;</span><br><span class="line">   <span class="keyword">int</span>    i = <span class="number">-1000</span>;</span><br><span class="line">   <span class="keyword">long</span>   l = <span class="number">100000</span>;</span><br><span class="line">   <span class="keyword">float</span>  f = <span class="number">230.47</span>;</span><br><span class="line">   <span class="keyword">double</span> d = <span class="number">200.374</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 数学运算</span></span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"sin(d) :"</span> &lt;&lt; <span class="built_in">sin</span>(d) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"abs(i)  :"</span> &lt;&lt; <span class="built_in">abs</span>(i) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"floor(d) :"</span> &lt;&lt; <span class="built_in">floor</span>(d) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"sqrt(f) :"</span> &lt;&lt; <span class="built_in">sqrt</span>(f) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"pow( d, 2) :"</span> &lt;&lt; <span class="built_in">pow</span>(d, <span class="number">2</span>) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="C-随机数"><a href="#C-随机数" class="headerlink" title="C++ 随机数"></a>C++ 随机数</h2><p>在许多情况下，需要生成随机数。关于随机数生成器，有两个相关的函数。一个是 <strong>rand()</strong>，该函数只返回一个伪随机数。生成随机数之前必须先调用 <strong>srand()</strong> 函数。</p>
<p>下面是一个关于生成随机数的简单实例。实例中使用了 <strong>time()</strong> 函数来获取系统时间的秒数，通过调用 rand() 函数来生成随机数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;ctime&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstdlib&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="keyword">int</span> i,j;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 设置种子</span></span><br><span class="line">   srand( (<span class="keyword">unsigned</span>)time( <span class="literal">NULL</span> ) );</span><br><span class="line"> </span><br><span class="line">   <span class="comment">/* 生成 10 个随机数 */</span></span><br><span class="line">   <span class="keyword">for</span>( i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ )</span><br><span class="line">   &#123;</span><br><span class="line">      <span class="comment">// 生成实际的随机数</span></span><br><span class="line">      j= rand();</span><br><span class="line">      <span class="built_in">cout</span> &lt;&lt;<span class="string">"随机数： "</span> &lt;&lt; j &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>一、rand()</strong></p>
<p>函数名:   rand  </p>
<p>功   能:   随机数发生器</p>
<p>用   法:   int rand(void); </p>
<p>所在头文件: stdlib.h</p>
<p><strong>函数说明 ：</strong></p>
<p>rand()的内部实现是用线性同余法做的，它不是真的随机数，因其周期特别长，故在一定</p>
<p>的范围里可看成是随机的。</p>
<p>rand()返回一随机数值的范围在0至RAND_MAX 间。RAND_MAX的范围最少是在32767之间(int)。用</p>
<p>unsigned int 双字节是65535，四字节是4294967295的整数范围。0~RAND_MAX每个数字被选中</p>
<p>的机率是相同的。     </p>
<p>用户未设定随机数种子时，系统默认的随机数种子为1。</p>
<p>rand()产生的是伪随机数字，每次执行时是相同的;若要不同,用函数srand()初始化它。</p>
<p><strong>实例如下：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt; </span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>; </span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span>  <span class="meta-string">&lt;stdlib.h&gt;   </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span>  <span class="meta-string">&lt;time.h&gt;  </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MIN 1  <span class="comment">//随机数产生的范围   </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX 10 </span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>   </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">  <span class="keyword">int</span> i;  </span><br><span class="line">  srand((<span class="keyword">unsigned</span>)time(<span class="number">0</span>)); </span><br><span class="line">  <span class="built_in">cout</span>&lt;&lt;<span class="string">"Ten random numbers from "</span>&lt;&lt;MIN&lt;&lt; </span><br><span class="line">     <span class="string">" to "</span>&lt;&lt;MAX&lt;&lt;<span class="string">" :/n"</span>&lt;&lt;<span class="built_in">endl</span>;   </span><br><span class="line">  <span class="keyword">for</span>(i=<span class="number">0</span>; i&lt;<span class="number">10</span>; i++)     <span class="comment">//产生随机数 </span></span><br><span class="line">  &#123; </span><br><span class="line">    cout&lt;&lt;MIN + (int)MAX * rand() / (RAND_MAX + 1)&lt;&lt;"/t";  </span><br><span class="line">  &#125; </span><br><span class="line">  <span class="built_in">cout</span>&lt;&lt;<span class="built_in">endl</span>; </span><br><span class="line">  <span class="keyword">return</span>  <span class="number">0</span>;   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>二、srand()</strong></p>
<p>函数名:   srand  </p>
<p>功   能:   初始化随机数发生器  </p>
<p>用   法:   void srand(unsigned  int  seed); </p>
<p>所在头文件： stdlib.h </p>
<p><strong>函数说明：</strong></p>
<p>srand()用来设置rand()产生随机数时的随机数种子。</p>
<p>参数seed必须是个整数，通常可以利用time(0)的返回值或NULL来当做seed。</p>
<p>如果每次seed都设相同值，rand()所产生的随机数值每次就会一样。</p>
<p><strong>实例如下：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt; </span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>; </span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span>  <span class="meta-string">&lt;stdlib.h&gt;   </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span>  <span class="meta-string">&lt;time.h&gt;  </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MIN 0  <span class="comment">//随机数产生的范围   </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX 99 </span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>   </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">  <span class="keyword">int</span> i;  </span><br><span class="line">  srand((<span class="keyword">unsigned</span>)time(<span class="literal">NULL</span>)); </span><br><span class="line">  <span class="built_in">cout</span>&lt;&lt;<span class="string">"Ten random numbers from "</span>&lt;&lt;MIN&lt;&lt; </span><br><span class="line">     <span class="string">" to "</span>&lt;&lt;MAX&lt;&lt;<span class="string">" :/n"</span>&lt;&lt;<span class="built_in">endl</span>;   </span><br><span class="line">  <span class="keyword">for</span>(i=<span class="number">0</span>; i&lt;<span class="number">10</span>; i++)     <span class="comment">//产生随机数 </span></span><br><span class="line">  &#123; </span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;MIN + rand() % (MAX + MIN - <span class="number">1</span>)&lt;&lt;<span class="string">"/t"</span>;  </span><br><span class="line">  &#125; </span><br><span class="line">  <span class="built_in">cout</span>&lt;&lt;<span class="built_in">endl</span>; </span><br><span class="line">  <span class="keyword">return</span>  <span class="number">0</span>;   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>三、rand()和srand()的关系</strong></p>
<p>rand()和srand()要一起使用，其中srand()用来初始化随机数种子,rand()用来产生随机数。</p>
<p>因为默认情况下随机数种子为1，而相同的随机数种子产生的随机数是一样的,失去了随机性的意义，所以为使每次得到的随机数不一样，用函数srand()初始化随机数种子。srand()的参数，用time函数值（即当前时间），因为两次调用rand()函数的时间通常是不同的，这样就可以保证随机性了。</p>
<p><strong>四、产生一定范围随机数的通用表示公式</strong></p>
<p>要取得[a,b)的随机整数，使用(rand() % (b-a))+ a （结果值含a不含b）。</p>
<p>要取得[a,b]的随机整数，使用(rand() % (b-a+1))+ a （结果值含a和b）。</p>
<p>要取得(a,b]的随机整数，使用(rand() % (b-a))+ a + 1 （结果值不含a含b）。</p>
<p>（总的来说，通用公式：a + rand() % n ；其中的a是起始值，n是整数的范围）</p>
<p>要取得a到b之间的随机整数，另一种表示：a + (int)b * rand() / (RAND_MAX + 1)。</p>
<p>要取得0～1之间的浮点数，可以使用rand() / double(RAND_MAX)。</p>
<p><strong>五、产生相同的随机数的原因</strong></p>
<p>计算机的随机数都是由伪随机数，即是由小M多项式序列生成的，其中产生每个小序列都有一个初始值，即随机种子。（注意： 小M多项式序列的周期是65535，即每次利用一个随机种子生成的随机数的周期是65535，当你取得65535个随机数后它们又重复出现了。） </p>
<p><strong>实例如下：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt; </span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>; </span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt; </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt; </span></span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">  <span class="keyword">int</span> i; </span><br><span class="line">  <span class="keyword">for</span> (i=<span class="number">0</span>; i&lt;<span class="number">10</span>; i++)   <span class="comment">//产生10个随机数 </span></span><br><span class="line">  &#123; </span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;rand()&lt;&lt;<span class="string">"/t"</span>; </span><br><span class="line">  &#125; </span><br><span class="line">  <span class="built_in">cout</span>&lt;&lt;<span class="built_in">endl</span>; </span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>每次运行得到相同的随机序列：</strong></p>
<p>41 18467 6334  26500 19169 15724 11478                        </p>
<p>41 18467 6334 26500 19169 15724 11478  29358 26962 24464</p>
<p>为得到不同的随机数序列，则需改变这个种子的值。方法：在开始产生随机数前，调用一次srand(time(NULL))（注意：srand()一定要放在循环外面或者是循环调用的外面，否则的话得到的是相同的随机数）。</p>
<p><strong>程序示例：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt; </span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>; </span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt; </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt; </span></span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">  <span class="keyword">int</span> i; </span><br><span class="line">  srand((<span class="keyword">unsigned</span>)time(<span class="literal">NULL</span>)); <span class="comment">//初始化随机数种子 </span></span><br><span class="line">  <span class="keyword">for</span> (i=<span class="number">0</span>; i&lt;<span class="number">10</span>; i++)     <span class="comment">//产生10个随机数 </span></span><br><span class="line">  &#123; </span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;rand()&lt;&lt;<span class="string">"/t"</span>; </span><br><span class="line">  &#125; </span><br><span class="line">  <span class="built_in">cout</span>&lt;&lt;<span class="built_in">endl</span>; </span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>每次运行得到不同的随机序列：</strong></p>
<p>1294 18562 14141 18165 11910 29784 11070 13225 131 24405</p>
<p>1774 25714 18734 16528 20825 17189 9848 8899 2503 5375</p>
<h2 id="time-的用法"><a href="#time-的用法" class="headerlink" title="time()的用法"></a>time()的用法</h2><p>该函数的声明为</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">time_t</span> <span class="title">time</span><span class="params">(<span class="keyword">time_t</span> *t)</span></span></span><br></pre></td></tr></table></figure>

<p>也就是说,传入一个指向time_t变量的指针,获得秒数后存储在这个指针指向的位置.上面的代码中全是time(NULL),意思是不保存这个秒数.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt; /*用到了time函数，所以要有这个头文件*/</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX 10</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">time_t</span> now_time;</span><br><span class="line">    <span class="keyword">int</span> number[MAX] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    srand((<span class="keyword">unsigned</span>) time(&amp;now_time)); <span class="comment">/*播种子*/</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; MAX; i++) &#123;</span><br><span class="line">        number[i] = rand() % <span class="number">100</span>; <span class="comment">/*产生100以内的随机整数*/</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"%d "</span>, number[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;now_time;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"\n"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>C++存储类</title>
    <url>/2018/07/29/Cpp%E5%AD%98%E5%82%A8%E7%B1%BB/</url>
    <content><![CDATA[<p>C++的变量/函数的生命周期.</p>
<a id="more"></a>

<h1 id="C-存储类"><a href="#C-存储类" class="headerlink" title="C++ 存储类"></a>C++ 存储类</h1><p>存储类定义 C++ 程序中变量/函数的范围（可见性）和生命周期。这些说明符放置在它们所修饰的类型之前。下面列出 C++ 程序中可用的存储类：</p>
<ul>
<li>auto</li>
<li>register</li>
<li>static</li>
<li>extern</li>
<li>mutable</li>
<li>thread_local (C++11)</li>
</ul>
<p>从 C++ 11 开始，auto 关键字不再是 C++ 存储类说明符，且 register 关键字被弃用</p>
<h2 id="auto-存储类"><a href="#auto-存储类" class="headerlink" title="auto 存储类"></a>auto 存储类</h2><p>自 C++ 11 以来，<strong>auto</strong> 关键字用于两种情况：声明变量时根据初始化表达式自动推断该变量的类型、声明函数时函数返回值的占位符。</p>
<p>C++98标准中auto关键字用于自动变量的声明，但由于使用极少且多余，在C++11中已删除这一用法。</p>
<p>根据初始化表达式自动推断被声明的变量的类型，如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> f=<span class="number">3.14</span>;      <span class="comment">//double</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">s</span><span class="params">(<span class="string">"hello"</span>)</span></span>;  <span class="comment">//const char*</span></span><br><span class="line"><span class="keyword">auto</span> z = <span class="keyword">new</span> <span class="keyword">auto</span>(<span class="number">9</span>); <span class="comment">// int*</span></span><br><span class="line"><span class="keyword">auto</span> x1 = <span class="number">5</span>, x2 = <span class="number">5.0</span>, x3=<span class="string">'r'</span>;<span class="comment">//错误，必须是初始化为同一类型</span></span><br></pre></td></tr></table></figure>

<h2 id="register-存储类"><a href="#register-存储类" class="headerlink" title="register 存储类"></a>register 存储类</h2><p><strong>register</strong> 存储类用于定义存储在寄存器中而不是 RAM 中的局部变量。这意味着变量的最大尺寸等于寄存器的大小（通常是一个词），且不能对它应用一元的 ‘&amp;’ 运算符（因为它没有内存位置）。</p>
<p>寄存器只用于需要快速访问的变量，比如计数器。还应注意的是，定义 ‘register’ 并不意味着变量将被存储在寄存器中，它意味着变量可能存储在寄存器中，这取决于硬件和实现的限制。</p>
<h2 id="static-存储类"><a href="#static-存储类" class="headerlink" title="static 存储类"></a>static 存储类</h2><p><strong>static</strong> 存储类指示编译器在程序的生命周期内保持局部变量的存在，而不需要在每次它进入和离开作用域时进行创建和销毁。因此，使用 static 修饰局部变量可以在函数调用之间保持局部变量的值。</p>
<p>static 修饰符也可以应用于全局变量。当 static 修饰全局变量时，会使变量的作用域限制在声明它的文件内。</p>
<p>在 C++ 中，当 static 用在类数据成员上时，会导致仅有<strong>一个该成员的副本被类的所有对象共享</strong>。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// 函数声明 </span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> count = <span class="number">10</span>; <span class="comment">/* 全局变量 */</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span>(count--)</span><br><span class="line">    &#123;</span><br><span class="line">       func();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 函数定义</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func</span><span class="params">( <span class="keyword">void</span> )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> i = <span class="number">5</span>; <span class="comment">// 局部静态变量</span></span><br><span class="line">    i++;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"变量 i 为 "</span> &lt;&lt; i ;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">" , 变量 count 为 "</span> &lt;&lt; count &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="extern-存储类"><a href="#extern-存储类" class="headerlink" title="extern 存储类"></a>extern 存储类</h2><p><strong>extern</strong> 存储类用于提供一个全局变量的引用，全局变量对所有的程序文件都是可见的。当您使用 ‘extern’ 时，对于无法初始化的变量，会把变量名指向一个之前定义过的存储位置。</p>
<p>当您有多个文件且定义了一个可以在其他文件中使用的全局变量或函数时，可以在其他文件中使用 <em>extern</em> 来得到已定义的变量或函数的引用。可以这么理解，<em>extern</em> 是用来在另一个文件中声明一个<strong>全局</strong>变量或函数。</p>
<p>extern 修饰符通常用于当有两个或多个文件共享相同的全局变量或函数的时候，如下所示：</p>
<p>第一个文件：main.cpp</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">int</span> count ;</span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">void</span> <span class="title">write_extern</span><span class="params">()</span></span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   count = <span class="number">5</span>;</span><br><span class="line">   write_extern();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第二个文件：support.cpp </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> count;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">write_extern</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Count is "</span> &lt;&lt; count &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在这里，第二个文件中的 <em>extern</em> 关键字用于声明已经在第一个文件 main.cpp 中定义的 count。现在 ，编译这两个文件，如下所示：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> g++ main.cpp support.cpp -o write</span></span><br></pre></td></tr></table></figure>

<h2 id="mutable-存储类"><a href="#mutable-存储类" class="headerlink" title="mutable 存储类"></a>mutable 存储类</h2><p><strong>mutable</strong> 说明符仅适用于类的对象，这将在本教程的最后进行讲解。它允许对象的成员替代常量。也就是说，mutable 成员可以通过 const 成员函数修改。</p>
<h2 id="thread-local-存储类"><a href="#thread-local-存储类" class="headerlink" title="thread_local 存储类"></a>thread_local 存储类</h2><p>使用 thread_local 说明符声明的变量仅可在它在其上创建的线程上访问。 变量在创建线程时创建，并在销毁线程时销毁。 每个线程都有其自己的变量副本。</p>
<p>thread_local 说明符可以与 static 或 extern 合并。</p>
<p>可以将 thread_local 仅应用于数据声明和定义，thread_local 不能用于函数声明或定义。</p>
<p>以下演示了可以被声明为 thread_local 的变量：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">thread_local</span> <span class="keyword">int</span> x;  <span class="comment">// 命名空间下的全局变量</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">X</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">thread_local</span> <span class="built_in">std</span>::<span class="built_in">string</span> s; <span class="comment">// 类的static成员变量</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">thread_local</span> <span class="built_in">std</span>::<span class="built_in">string</span> X::s;  <span class="comment">// X::s 是需要定义的</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">thread_local</span> <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; v;  <span class="comment">// 本地变量</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>Cpp引用</title>
    <url>/2018/07/29/Cpp%E5%BC%95%E7%94%A8/</url>
    <content><![CDATA[<p>对于引用和指针有点搞不清楚.</p>
<a id="more"></a>

<p>说到引用，先来讲一个故事：我们生活的院子里有这样一个小盆友，他的名字叫罗XX，我们喜欢给他起一个外号，看着他营养不太好，脑袋比较大、身子比较小，于是我们就给他起了一个叫”萝卜头”的外号。那么罗XX就是他的真实姓名，萝卜头就是他的一个别名，那么什么是引用呢？？</p>
<p><strong>定义：</strong>引用：就是变量的一个别名。</p>
<p><img src="/2018/07/29/Cpp%E5%BC%95%E7%94%A8/20160506194946525.png" alt></p>
<p><strong>注：</strong>作为别名来说，一个变量不能只有别名，而没有自己的真实姓名。</p>
<h3 id="基本数据类型的引用"><a href="#基本数据类型的引用" class="headerlink" title="基本数据类型的引用"></a>基本数据类型的引用</h3><p>如下面的一段代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">3</span>;</span><br><span class="line">    <span class="keyword">int</span> &amp;b = a;  <span class="comment">//b就是a的引用，即b是a的一个别别名。</span></span><br><span class="line">                 <span class="comment">//引用必须初始化，否则编译会报错</span></span><br><span class="line">    b = <span class="number">10</span>;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt; a &lt;&lt; <span class="built_in">endl</span>; <span class="comment">//此时a 的值，已由原来的3变成了10.因为我们无论对别名做什么操作，其实都是对变量的本身做操作。就像我们去交萝卜头去干什么，罗XX本身就去干什么。</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="‪结构体类型的引用"><a href="#‪结构体类型的引用" class="headerlink" title="‪结构体类型的引用"></a>‪结构体类型的引用</h3><p>如下面的一段代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="comment">//定义一个名叫Coor的结构体，这是一个坐标。</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> x;</span><br><span class="line">    <span class="keyword">int</span> y;</span><br><span class="line">&#125;Coor;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Coor c1;    <span class="comment">//定义一个结构体变量，叫c1.</span></span><br><span class="line">    Coor &amp;c = c1;    <span class="comment">//给c1起了一个别名叫c.</span></span><br><span class="line">    c.x = <span class="number">10</span>;    <span class="comment">//通过引用给结构体变量的数据成员赋值</span></span><br><span class="line">    c.y = <span class="number">20</span>;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt; c1.x &lt;&lt; c2.y;    <span class="comment">//输出的结果:10 20</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="指针类型的引用"><a href="#指针类型的引用" class="headerlink" title="指针类型的引用"></a>指针类型的引用</h3><p>指针类型的引用是引用当中最为复杂，最难理解的一部分，同时在写法形式上也有着与众不同的地方。</p>
<p>定义：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">类型 *&amp;指针引用名 &#x3D; 指针;</span><br></pre></td></tr></table></figure>

<p>看个例子： </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">10</span>;    <span class="comment">//定义一个整型的a变量，a的值为10。</span></span><br><span class="line">    <span class="keyword">int</span> *p = &amp;a; <span class="comment">//定义一个指向a变量的指针</span></span><br><span class="line">    <span class="keyword">int</span> *&amp;q = p;    <span class="comment">//定义一个指针的引用，即q为p的别名。 定义方法：类型 *&amp;指针引用名 = 指针；</span></span><br><span class="line">    *q = <span class="number">20</span>;    <span class="comment">//把20赋给*q,相当于是把20赋值给*p，也就相当于把20赋值给a.</span></span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt; a &lt;&lt; <span class="built_in">endl</span>;    <span class="comment">//输出a 的值为20.</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我个人觉得下面这种写法更符合理解:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">10</span>;    <span class="comment">//定义一个整型的a变量，a的值为10。</span></span><br><span class="line">    <span class="keyword">int</span>* p = &amp;a; <span class="comment">//定义一个指向a变量的指针</span></span><br><span class="line">    <span class="keyword">int</span>* &amp;q = p;    <span class="comment">//定义一个指针的引用，即q为p的别名。 定义方法：类型 *&amp;指针引用名 = 指针；</span></span><br><span class="line">    *q = <span class="number">20</span>;    <span class="comment">//把20赋给*q,相当于是把20赋值给*p，也就相当于把20赋值给a.</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>;    <span class="comment">//输出a 的值为20.</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>int*</code>可以视为一种新的变量类型,虽然这种写法不符合规范.<code>int* p</code>表示p是指针,<code>int* &amp;p</code>表示&amp;p是一个指针类型的引用.</p>
<h2 id="C-引用-vs-指针"><a href="#C-引用-vs-指针" class="headerlink" title="C++ 引用 vs 指针"></a>C++ 引用 vs 指针</h2><p>引用很容易与指针混淆，它们之间有三个主要的不同：</p>
<ul>
<li>不存在空引用。引用必须连接到一块合法的内存。</li>
<li>一旦引用被初始化为一个对象，就不能被指向到另一个对象。指针可以在任何时候指向到另一个对象。</li>
<li>引用必须在创建时被初始化。指针可以在任何时间被初始化。</li>
</ul>
<p><strong>特别注意：</strong></p>
<p>1.&amp;不是求地址运算符，<strong>而是起标志作用</strong></p>
<p>2.引用的类型必须和其所绑定的变量的类型相同</p>
<p>3.声明引用的同时<strong>必须对其初始化</strong>，否则系统会报错 </p>
<p>4.引用相当于变量或对象的别名，因此<strong>不能再将已有的引用名作为其他变量或对象的名字或别名</strong></p>
<p>5.引用不是定义一个新的变量或对象，因此<strong>内存不会为引用开辟新的空间存储这个引用</strong>关于引用到底占不占内存空间,我另写一篇.</p>
<h2 id="把引用作为返回值"><a href="#把引用作为返回值" class="headerlink" title="把引用作为返回值"></a>把引用作为返回值</h2><p>通过使用引用来替代指针，会使 C++ 程序更容易阅读和维护。C++ 函数可以返回一个引用，方式与返回一个指针类似。</p>
<p>当函数返回一个引用时，则返回一个指向返回值的隐式指针。这样，函数就可以放在赋值语句的左边。例如，请看下面这个简单的程序：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">double</span> vals[] = &#123;<span class="number">10.1</span>, <span class="number">12.6</span>, <span class="number">33.1</span>, <span class="number">24.1</span>, <span class="number">50.0</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">double</span> &amp;<span class="title">setValues</span><span class="params">(<span class="keyword">int</span> i)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> vals[i];   <span class="comment">// 返回第 i 个元素的引用</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 要调用上面定义函数的主函数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"改变前的值"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="string">"vals["</span> &lt;&lt; i &lt;&lt; <span class="string">"] = "</span>;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; vals[i] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    setValues(<span class="number">1</span>) = <span class="number">20.23</span>; <span class="comment">// 改变第 2 个元素</span></span><br><span class="line">    setValues(<span class="number">3</span>) = <span class="number">70.8</span>;  <span class="comment">// 改变第 4 个元素</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"改变后的值"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="string">"vals["</span> &lt;&lt; i &lt;&lt; <span class="string">"] = "</span>;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; vals[i] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>（1）<strong>以引用返回函数值，定义函数时需要在函数名前加 &amp;</strong></p>
<p>（2）用引用返回一个函数值的最大好处是，在内存中不产生被返回值的副本。</p>
<p><strong>引用作为返回值，必须遵守以下规则：</strong></p>
<ul>
<li>（1）不能返回局部变量的引用。主要原因是局部变量会在函数返回后被销毁，因此被返回的引用就成为了”无所指”的引用，程序会进入未知状态。</li>
<li>（2）不能返回函数内部new分配的内存的引用。虽然不存在局部变量的被动销毁问题，可对于这种情况（返回函数内部new分配内存的引用），又面临其它尴尬局面。例如，被函数返回的引用只是作为一 个临时变量出现，而没有被赋予一个实际的变量，那么这个引用所指向的空间（由new分配）就无法释放，造成memory leak。</li>
<li>（3）可以返回类成员的引用，但最好是const。主要原因是当对象的属性是与某种业务规则（business rule）相关联的时候，其赋值常常与某些其它属性或者对象的状态有关，因此有必要将赋值操作封装在一个业务规则当中。如果其它对象可以获得该属性的非常 量引用（或指针），那么对该属性的单纯赋值就会破坏业务规则的完整性。</li>
</ul>
<p><strong>引用虽方便，使用须谨慎：</strong></p>
<p><strong>（1）&amp;在这里不是求地址运算，而是起标识作用。</strong></p>
<p><strong>（2）类型标识符是指目标变量的类型。</strong></p>
<p><strong>（3）声明引用时，必须同时对其进行初始化。</strong></p>
<p><strong>（4）引用声明完毕后，相当于目标变量名有两个名称，即该目标原名称和引用名，且不能再把该引用名作为其他变量名的别名。</strong></p>
<p><strong>（5）对引用求地址，就是对目标变量求地址。即引用名是目标变量名的一个别名。引用在定义上是说引用不占据任何内存空间，但是编译器在一般将其实现为const指针，即指向位置不可变的指针，所以引用实际上与一般指针同样占用内存。</strong></p>
<p><strong>（6）不能建立引用的数组。因为数组是一个由若干个元素所组成的集合，所以无法建立一个由引用组成的集合，但是可以建立数组的引用。</strong></p>
<p><strong>（7）引用常见的使用用途：作为函数的参数、函数的返回值。</strong></p>
<p><strong>总结：</strong></p>
<ol>
<li>不要返回一个临时变量的引用。</li>
<li>如果返回对象出了当前函数的作用域依旧存在，则最好使用引用返回，因为这样更高效。</li>
</ol>
<p>* 引用和指针的区别和联系（笔试热点）</p>
<ol>
<li><p>引用只能在定义时初始化一次，之后不能改变指向其它变量（从一而终）；指针变量的值可变。</p>
</li>
<li><p>引用必须指向有效的变量，指针可以为空。</p>
</li>
<li><p>sizeof指针对象和引用对象的意义不一样。sizeof引用得到的是所指向的变量的大小，而sizeof指针是对象地址的大小。</p>
</li>
<li><p>指针和引用自增(++)自减(–)意义不一样。</p>
</li>
<li><p>相对而言，引用比指针更安全。</p>
<p><strong>Ⅰ.引用和指针的区别和联系：</strong></p>
</li>
</ol>
<p><strong>★不同点</strong>：</p>
<ol>
<li>指针是一个实体，而引用仅是个别名；</li>
<li>引用使用时无需解引用(*)，指针需要解引用；</li>
<li>引用只能在定义时被初始化一次，之后不可变；指针可变；</li>
<li>引用没有 const，指针有 const；const修饰的指针不可变；</li>
<li>引用不能为空，指针可以为空；</li>
<li>“sizeof 引用”得到的是所指向的变量(对象)的大小，而“sizeof 指针”得到的是指针本身(所指向的变量或对象的地址)的大小；</li>
<li>指针和引用的自增(++)运算意义不一样；8.从内存分配上看：程序为指针变量分配内存区域，而引用不需要分配内存区域。</li>
</ol>
<p><strong>★相同点</strong>：两者都是地址的概念，指针指向一块儿内存，其内容为所指内存的地址；引用是某块儿内存的别名。</p>
<p> <strong>Ⅱ.const在C和C++中的含义（笔试热点）：</strong></p>
<p><strong>⑴C中的const，功能比较单一，较容易理解</strong>：<br><strong>作用：</strong>被修饰的内容不可更改。<br><strong>使用场合：</strong>修饰变量，函数参数，返回值等。（c++中应用场合要丰富的多）<br><strong>特点：</strong> 是运行时const，因此不能取代#define用于成为数组长度等需要编译时常量的情况。同时因为是运行时const，可以只定义而不初始化,而在运行时初始化。如 const int iConst;。 另外，在c中，const变量默认是外部链接，因此在不同的编译单元中如果有同名const变量，会引发命名冲突，编译时报错。<br><strong>⑵c++中的const：</strong></p>
<p><strong>a、非类成员const：</strong></p>
<p>①const变量默认是内部连接的，因此在不同的编译单元中可以有同名的const 变量定义。</p>
<p>②编译时常量，因此可以像#define一样使用，而且因为上面一点，可以在头文件中定义const变量，包含的不同的cpp文件（编译</p>
<p>单元）中使用而不引起命名冲突。</p>
<p>③编译器默认不为const变量分配内存，除非：1. 使用 extern 申明， 2：程序中有引用const 变量的地址。</p>
<p>④<strong>c++中临时对象/内置变量默认具有const属性。</strong></p>
<p><strong>b、类中的const：</strong></p>
<p>①与c语言中的const一样，只是运行时常量，不能作为数组维数使用，即不能取代#define。在类中使用下面两种方式取代#define： 1：static const… </p>
<p>2 : enum{….}//enum 不占存储空间。</p>
<p>②类中的const 变量占用存储空间。</p>
<p>③类中的const成员变量需要在构造函数初始化列表中初始化。</p>
<p>④const 对象：在该对象生命周期内，必须保证没有任何成员变量被改变。const对象只能调用const成员函数。</p>
<p>⑤const成员函数： void fun() const … 不仅能被const对象调用，也能被非const对象调用，因此，如果确认一个任何成员函数不改</p>
<p>变任何成员变量，应该习惯性将该函数定义成const类型。</p>
<p>⑥如果一个对象被定义成const，那么该const对象“可能”会被放入到ROM当中，这在嵌入式开发当中有时非常重要。</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>Cpp构造函数和析构函数</title>
    <url>/2018/08/02/Cpp%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E5%92%8C%E6%9E%90%E6%9E%84%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p>C++中的构造函数与java的差不多,析构函数则有点点像java中的finalize().</p>
<a id="more"></a>

<h1 id="C-类构造函数-amp-析构函数"><a href="#C-类构造函数-amp-析构函数" class="headerlink" title="C++ 类构造函数 &amp; 析构函数"></a>C++ 类构造函数 &amp; 析构函数</h1><h2 id="类的构造函数"><a href="#类的构造函数" class="headerlink" title="类的构造函数"></a>类的构造函数</h2><p>类的<strong>构造函数</strong>是类的一种特殊的成员函数，它会在每次创建类的新对象时执行。</p>
<p>构造函数的名称与类的名称是完全相同的，并且不会返回任何类型，也不会返回 void。构造函数可用于为某些成员变量设置初始值。</p>
<p>下面的实例有助于更好地理解构造函数的概念：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Line</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setLength</span><span class="params">( <span class="keyword">double</span> len )</span></span>;</span><br><span class="line">      <span class="function"><span class="keyword">double</span> <span class="title">getLength</span><span class="params">( <span class="keyword">void</span> )</span></span>;</span><br><span class="line">      Line();  <span class="comment">// 这是构造函数</span></span><br><span class="line"> </span><br><span class="line">   <span class="keyword">private</span>:</span><br><span class="line">      <span class="keyword">double</span> length;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 成员函数定义，包括构造函数</span></span><br><span class="line">Line::Line(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"Object is being created"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Line::setLength</span><span class="params">( <span class="keyword">double</span> len )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    length = len;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">Line::getLength</span><span class="params">( <span class="keyword">void</span> )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> length;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 程序的主函数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   Line line;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 设置长度</span></span><br><span class="line">   line.setLength(<span class="number">6.0</span>); </span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of line : "</span> &lt;&lt; line.getLength() &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="带参数的构造函数"><a href="#带参数的构造函数" class="headerlink" title="带参数的构造函数"></a>带参数的构造函数</h2><p>默认的构造函数没有任何参数，但如果需要，构造函数也可以带有参数。这样在创建对象时就会给对象赋初始值，如下面的例子所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Line</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setLength</span><span class="params">( <span class="keyword">double</span> len )</span></span>;</span><br><span class="line">      <span class="function"><span class="keyword">double</span> <span class="title">getLength</span><span class="params">( <span class="keyword">void</span> )</span></span>;</span><br><span class="line">      Line(<span class="keyword">double</span> len);  <span class="comment">// 这是构造函数</span></span><br><span class="line"> </span><br><span class="line">   <span class="keyword">private</span>:</span><br><span class="line">      <span class="keyword">double</span> length;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 成员函数定义，包括构造函数</span></span><br><span class="line">Line::Line( <span class="keyword">double</span> len)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"Object is being created, length = "</span> &lt;&lt; len &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    length = len;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Line::setLength</span><span class="params">( <span class="keyword">double</span> len )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    length = len;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">Line::getLength</span><span class="params">( <span class="keyword">void</span> )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> length;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 程序的主函数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="function">Line <span class="title">line</span><span class="params">(<span class="number">10.0</span>)</span></span>;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 获取默认设置的长度</span></span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of line : "</span> &lt;&lt; line.getLength() &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">   <span class="comment">// 再次设置长度</span></span><br><span class="line">   line.setLength(<span class="number">6.0</span>); </span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of line : "</span> &lt;&lt; line.getLength() &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="使用初始化列表来初始化字段"><a href="#使用初始化列表来初始化字段" class="headerlink" title="使用初始化列表来初始化字段"></a>使用初始化列表来初始化字段</h2><p>使用初始化列表来初始化字段：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">Line::Line( <span class="keyword">double</span> len): length(len)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"Object is being created, length = "</span> &lt;&lt; len &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>上面的语法等同于如下语法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">Line::Line( <span class="keyword">double</span> len)</span><br><span class="line">&#123;</span><br><span class="line">    length = len;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"Object is being created, length = "</span> &lt;&lt; len &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>假设有一个类 C，具有多个字段 X、Y、Z 等需要进行初始化，同理地，您可以使用上面的语法，只需要在不同的字段使用逗号进行分隔，如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">C::C( <span class="keyword">double</span> a, <span class="keyword">double</span> b, <span class="keyword">double</span> c): X(a), Y(b), Z(c)</span><br><span class="line">&#123;</span><br><span class="line">  ....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>冒号的使用目前出现了几种,初始化列表,类的继承,访问修饰,还有一种是位域的定义,另一篇会讲到.</p>
<h2 id="类的析构函数"><a href="#类的析构函数" class="headerlink" title="类的析构函数"></a>类的析构函数</h2><p>类的<strong>析构函数</strong>是类的一种特殊的成员函数，它会在每次删除所创建的对象时执行。</p>
<p>析构函数的名称与类的名称是完全相同的，只是在前面加了个波浪号（~）作为前缀，它不会返回任何值，也不能带有任何参数。析构函数有助于在跳出程序（比如关闭文件、释放内存等）前释放资源。</p>
<p>下面的实例有助于更好地理解析构函数的概念：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Line</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setLength</span><span class="params">( <span class="keyword">double</span> len )</span></span>;</span><br><span class="line">      <span class="function"><span class="keyword">double</span> <span class="title">getLength</span><span class="params">( <span class="keyword">void</span> )</span></span>;</span><br><span class="line">      Line();   <span class="comment">// 这是构造函数声明</span></span><br><span class="line">      ~Line();  <span class="comment">// 这是析构函数声明</span></span><br><span class="line"> </span><br><span class="line">   <span class="keyword">private</span>:</span><br><span class="line">      <span class="keyword">double</span> length;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 成员函数定义，包括构造函数</span></span><br><span class="line">Line::Line(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"Object is being created"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line">Line::~Line(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"Object is being deleted"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Line::setLength</span><span class="params">( <span class="keyword">double</span> len )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    length = len;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">Line::getLength</span><span class="params">( <span class="keyword">void</span> )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> length;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 程序的主函数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   Line line;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 设置长度</span></span><br><span class="line">   line.setLength(<span class="number">6.0</span>); </span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of line : "</span> &lt;&lt; line.getLength() &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>C++的析构函数用来做一些必要的工作，例如释放掉指针成员所指向的对象所占的内存，因为C++没有java的垃圾回收器，所有new出来的对象，都要显式地delete掉，避免内存泄漏。《Effective C++》中提及，基类需要将析构函数声明为virtual函数，这是为了可以通过子类对象指针正确地释放掉基类的资源。总的来说，<strong>在C++中，析构函数和资源的释放息息相关，能不能正确处理析构函数，关乎能否正确回收对象内存资源。</strong> 但是在Java中因为垃圾回收机制,finalize()的作用与之差别明显.</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>C++数据类型</title>
    <url>/2018/07/29/Cpp%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<p>C++的一些常见的数据类型.</p>
<a id="more"></a>

<h2 id="基本的内置类型"><a href="#基本的内置类型" class="headerlink" title="基本的内置类型"></a>基本的内置类型</h2><p>C++ 为程序员提供了种类丰富的内置数据类型和用户自定义的数据类型。下表列出了七种基本的 C++ 数据类型：</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>关键字</th>
</tr>
</thead>
<tbody><tr>
<td>布尔型</td>
<td>bool</td>
</tr>
<tr>
<td>字符型</td>
<td>char</td>
</tr>
<tr>
<td>整型</td>
<td>int</td>
</tr>
<tr>
<td>浮点型</td>
<td>float</td>
</tr>
<tr>
<td>双浮点型</td>
<td>double</td>
</tr>
<tr>
<td>无类型</td>
<td>void</td>
</tr>
<tr>
<td>宽字符型</td>
<td>wchar_t</td>
</tr>
</tbody></table>
<p>一些基本类型可以使用一个或多个类型修饰符进行修饰：</p>
<ul>
<li><p>signed</p>
</li>
<li><p>unsigned</p>
</li>
<li><p>short</p>
</li>
<li><p>long</p>
<p><img src="/2018/07/29/Cpp%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/1-121224205055155.gif" alt="数据类型"></p>
</li>
</ul>
<h2 id="typedef-声明"><a href="#typedef-声明" class="headerlink" title="typedef 声明"></a>typedef 声明</h2><p>您可以使用 <strong>typedef</strong> 为一个已有的类型取一个新的名字。下面是使用 typedef 定义一个新类型的语法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> type newname;</span><br></pre></td></tr></table></figure>

<p>例如，下面的语句会告诉编译器，feet 是 int 的另一个名称：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> feet;</span><br></pre></td></tr></table></figure>

<p>现在，下面的声明是完全合法的，它创建了一个整型变量 distance：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">feet distance;</span><br></pre></td></tr></table></figure>

<p><strong>关于 typedef 的几点说明：</strong></p>
<ol>
<li>typedef 可以声明各种类型名，但不能用来定义变量。用 typedef 可以声明数组类型、字符串类型，使用比较方便。</li>
<li>用typedef只是对已经存在的类型增加一个类型名，而没有创造新的类型。</li>
<li>当在不同源文件中用到同一类型数据（尤其是像数组、指针、结构体、共用体等类型数据）时，常用 typedef 声明一些数据类型，把它们单独放在一个头文件中，然后在需要用到它们的文件中用 ＃include 命令把它们包含进来，以提高编程效率。</li>
<li>使用 typedef 有利于程序的通用与移植。有时程序会依赖于硬件特性，用 typedef 便于移植。</li>
</ol>
<p><strong>typedef：</strong></p>
<p>如果放在所有函数之外，它的作用域就是从它定义开始直到文件尾；</p>
<p>如果放在某个函数内，定义域就是从定义开始直到该函数结尾；</p>
<hr>
<p>size_t 在 C 语言中就有了。</p>
<p>它是一种 整型 类型，里面保存的是一个整数，就像 int, long 那样。这种整数用来记录一个大小(size)。size_t 的全称应该是 size type，就是说 一种用来记录大小的数据类型。</p>
<p>通常我们用 sizeof(XXX) 操作，这个操作所得到的结果就是 size_t 类型。</p>
<p>因为 size_t 类型的数据其实是保存了一个整数，所以它也可以做加减乘除，也可以转化为 int 并赋值给 int 类型的变量。</p>
<p>类似的还有 <strong>wchar_t, ptrdiff_t</strong>。</p>
<p><strong>wchar_t</strong> 就是 wide char type， 一种用来记录一个宽字符的数据类型 。</p>
<p>ptrdiff_t 就是 pointer difference type， 一种用来记录两个指针之间的距离的数据类型 。</p>
<p>通常，size_t 和 ptrdiff_t 都是用 typedef 来实现的。你可能在某个头文件里面找到类似的语句：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="keyword">size_t</span>;</span><br></pre></td></tr></table></figure>

<p>而 wchar_t 则稍有不同。在一些旧的编译器中，wchar_t 也可能是用 typedef 来实现，但是新的标准中 wchar_t 已经是 C/C++ 语言的关键字，wchar_t 类型的地位已经和 char, int 的地位等同了。</p>
<p>在标准 C/C++ 的语法中，只有 </p>
<p>int float char bool</p>
<p> 等基本的数据类型，至于 size_t, 或 size_type 都是以后的编程人员为了方便记忆所定义的一些便于理解的由基本数据类型的变体类型。</p>
<p>例如：typedef int size_t; 定义了 <strong>size_t</strong> 为整型。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> i;                   <span class="comment">// 定义一个 int 类型的变量 i</span></span><br><span class="line"><span class="keyword">size_t</span> size=<span class="keyword">sizeof</span>(i);   <span class="comment">// 用 sizeof 操作得到变量i的类型的大小</span></span><br><span class="line"><span class="comment">// 这是一个size_t类型的值</span></span><br><span class="line"><span class="comment">// 可以用来对一个size_t类型的变量做初始化</span></span><br><span class="line"></span><br><span class="line">i=(<span class="keyword">int</span>)size;             <span class="comment">// size_t 类型的值可以转化为 int 类型的值</span></span><br><span class="line"><span class="keyword">char</span> c=<span class="string">'a'</span>;              <span class="comment">// c 保存了字符 a，占一个字节</span></span><br><span class="line"><span class="keyword">wchar_t</span> wc=<span class="string">L'a'</span>;         <span class="comment">// wc 保存了宽字符 a，占两个字节</span></span><br><span class="line"><span class="comment">// 注意 'a' 表示字符 a，L'a' 表示宽字符 a</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> arr[]=&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;;   <span class="comment">// 定义一个数组</span></span><br><span class="line"><span class="keyword">int</span> *p1=&amp;arr[<span class="number">0</span>];         <span class="comment">// 取得数组中元素的地址，赋值给指针</span></span><br><span class="line"><span class="keyword">int</span> *p2=&amp;arr[<span class="number">3</span>];</span><br><span class="line"><span class="keyword">ptrdiff_t</span> diff=p2-p1;    <span class="comment">// 指针的减法可以计算两个指针之间相隔的元素个数</span></span><br><span class="line"><span class="comment">// 所得结果是一个 ptrdiff_t 类型</span></span><br><span class="line"></span><br><span class="line">i=(<span class="keyword">int</span>)diff;             <span class="comment">// ptrdiff_t 类型的值可以转化为 int 类型的值</span></span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>Cpp类访问修饰符</title>
    <url>/2018/08/02/Cpp%E7%B1%BB%E8%AE%BF%E9%97%AE%E4%BF%AE%E9%A5%B0%E7%AC%A6/</url>
    <content><![CDATA[<p>C++的类访问修饰符和Java的有一点很大的区别,那就是不可以用来单独修饰某个类,修饰符本身用于成员和继承.</p>
<a id="more"></a>

<h1 id="C-类访问修饰符"><a href="#C-类访问修饰符" class="headerlink" title="C++ 类访问修饰符"></a>C++ 类访问修饰符</h1><p>数据封装是面向对象编程的一个重要特点，它防止函数直接访问类类型的内部成员。类成员的访问限制是通过在类主体内部对各个区域标记 public、private、protected 来指定的。关键字 public、private、protected 称为访问修饰符。</p>
<p>一个类可以有多个 public、protected 或 private 标记区域。每个标记区域在下一个标记区域开始之前或者在遇到类主体结束右括号之前都是有效的。成员和类的默认访问修饰符是 private。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base</span> &#123;</span></span><br><span class="line"> </span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line"> </span><br><span class="line">  <span class="comment">// 公有成员</span></span><br><span class="line"> </span><br><span class="line">   <span class="keyword">protected</span>:</span><br><span class="line"> </span><br><span class="line">  <span class="comment">// 受保护成员</span></span><br><span class="line"> </span><br><span class="line">   <span class="keyword">private</span>:</span><br><span class="line"> </span><br><span class="line">  <span class="comment">// 私有成员</span></span><br><span class="line"> </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="公有（public）成员"><a href="#公有（public）成员" class="headerlink" title="公有（public）成员"></a>公有（public）成员</h2><p><strong>公有</strong>成员在程序中类的外部是可访问的。您可以不使用任何成员函数来设置和获取公有变量的值，如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Line</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="keyword">double</span> length;</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setLength</span><span class="params">( <span class="keyword">double</span> len )</span></span>;</span><br><span class="line">      <span class="function"><span class="keyword">double</span> <span class="title">getLength</span><span class="params">( <span class="keyword">void</span> )</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 成员函数定义</span></span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">Line::getLength</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> length ;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Line::setLength</span><span class="params">( <span class="keyword">double</span> len )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    length = len;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 程序的主函数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   Line line;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 设置长度</span></span><br><span class="line">   line.setLength(<span class="number">6.0</span>); </span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of line : "</span> &lt;&lt; line.getLength() &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 不使用成员函数设置长度</span></span><br><span class="line">   line.length = <span class="number">10.0</span>; <span class="comment">// OK: 因为 length 是公有的</span></span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of line : "</span> &lt;&lt; line.length &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="私有（private）成员"><a href="#私有（private）成员" class="headerlink" title="私有（private）成员"></a>私有（private）成员</h2><p><strong>私有</strong>成员变量或函数在类的外部是不可访问的，甚至是不可查看的。只有<strong>类和友元函数可以访问私有成员</strong>。</p>
<p><strong>默认情况下，类的所有成员都是私有的</strong>。例如在下面的类中，<strong>width</strong> 是一个私有成员，这意味着，如果您没有使用任何访问修饰符，类的成员将被假定为私有成员：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Box</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">double</span> width;</span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="keyword">double</span> length;</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setWidth</span><span class="params">( <span class="keyword">double</span> wid )</span></span>;</span><br><span class="line">      <span class="function"><span class="keyword">double</span> <span class="title">getWidth</span><span class="params">( <span class="keyword">void</span> )</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>实际操作中，我们一般会在私有区域定义数据，在公有区域定义相关的函数，以便在类的外部也可以调用这些函数，如下所示： </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Box</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="keyword">double</span> length;</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setWidth</span><span class="params">( <span class="keyword">double</span> wid )</span></span>;</span><br><span class="line">      <span class="function"><span class="keyword">double</span> <span class="title">getWidth</span><span class="params">( <span class="keyword">void</span> )</span></span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">private</span>:</span><br><span class="line">      <span class="keyword">double</span> width;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 成员函数定义</span></span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">Box::getWidth</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> width ;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Box::setWidth</span><span class="params">( <span class="keyword">double</span> wid )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    width = wid;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 程序的主函数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   Box box;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 不使用成员函数设置长度</span></span><br><span class="line">   box.length = <span class="number">10.0</span>; <span class="comment">// OK: 因为 length 是公有的</span></span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of box : "</span> &lt;&lt; box.length &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 不使用成员函数设置宽度</span></span><br><span class="line">   <span class="comment">// box.width = 10.0; // Error: 因为 width 是私有的</span></span><br><span class="line">   box.setWidth(<span class="number">10.0</span>);  <span class="comment">// 使用成员函数设置宽度</span></span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Width of box : "</span> &lt;&lt; box.getWidth() &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="保护（protected）成员"><a href="#保护（protected）成员" class="headerlink" title="保护（protected）成员"></a>保护（protected）成员</h2><p><strong>保护</strong>成员变量或函数与私有成员十分相似，但有一点不同，保护成员在派生类（即子类）中是可访问的。</p>
<p>现在您可以看到下面的实例中，我们从父类 <strong>Box</strong> 派生了一个子类 <strong>smallBox</strong>。</p>
<p>下面的实例与前面的实例类似，在这里 <strong>width</strong> 成员可被派生类 smallBox 的任何成员函数访问。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Box</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">protected</span>:</span><br><span class="line">      <span class="keyword">double</span> width;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SmallBox</span>:</span>Box <span class="comment">// SmallBox 是派生类</span></span><br><span class="line">&#123;</span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setSmallWidth</span><span class="params">( <span class="keyword">double</span> wid )</span></span>;</span><br><span class="line">      <span class="function"><span class="keyword">double</span> <span class="title">getSmallWidth</span><span class="params">( <span class="keyword">void</span> )</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 子类的成员函数</span></span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">SmallBox::getSmallWidth</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> width ;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">SmallBox::setSmallWidth</span><span class="params">( <span class="keyword">double</span> wid )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    width = wid;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 程序的主函数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   SmallBox box;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 使用成员函数设置宽度</span></span><br><span class="line">   box.setSmallWidth(<span class="number">5.0</span>);</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Width of box : "</span>&lt;&lt; box.getSmallWidth() &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="继承中的特点"><a href="#继承中的特点" class="headerlink" title="继承中的特点"></a>继承中的特点</h2><p>有public, protected, private三种继承方式，它们相应地改变了基类成员的访问属性。</p>
<ul>
<li>1.<strong>public 继承：</strong>基类 public 成员，protected 成员，private 成员的访问属性在派生类中分别变成：public, protected, private</li>
<li>2.<strong>protected 继承：</strong>基类 public 成员，protected 成员，private 成员的访问属性在派生类中分别变成：protected, protected, private</li>
<li>3.<strong>private 继承：</strong>基类 public 成员，protected 成员，private 成员的访问属性在派生类中分别变成：private, private, private</li>
</ul>
<p>但无论哪种继承方式，上面两点都没有改变：</p>
<ul>
<li>1.private 成员只能被本类成员（类内）和友元访问，不能被派生类访问；</li>
<li>2.protected 成员可以被派生类访问。</li>
</ul>
<h3 id="public-继承"><a href="#public-继承" class="headerlink" title="public 继承"></a>public 继承</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span>&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">int</span> a;</span><br><span class="line">  A()&#123;</span><br><span class="line">    a1 = <span class="number">1</span>;</span><br><span class="line">    a2 = <span class="number">2</span>;</span><br><span class="line">    a3 = <span class="number">3</span>;</span><br><span class="line">    a = <span class="number">4</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>;    <span class="comment">//正确</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a1 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//正确</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a2 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//正确</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a3 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//正确</span></span><br><span class="line">  &#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">int</span> a1;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">  <span class="keyword">int</span> a2;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  <span class="keyword">int</span> a3;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> :</span> <span class="keyword">public</span> A&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">int</span> a;</span><br><span class="line">  B(<span class="keyword">int</span> i)&#123;</span><br><span class="line">    A();</span><br><span class="line">    a = i;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确，public成员</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a1 &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确，基类的public成员，在派生类中仍是public成员。</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a2 &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确，基类的protected成员，在派生类中仍是protected可以被派生类访问。</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a3 &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//错误，基类的private成员不能被派生类访问。</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">  <span class="function">B <span class="title">b</span><span class="params">(<span class="number">10</span>)</span></span>;</span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a1 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//正确</span></span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a2 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//错误，类外不能访问protected成员</span></span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a3 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//错误，类外不能访问private成员</span></span><br><span class="line">  system(<span class="string">"pause"</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="protected-继承"><a href="#protected-继承" class="headerlink" title="protected 继承"></a>protected 继承</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span>&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">int</span> a;</span><br><span class="line">  A()&#123;</span><br><span class="line">    a1 = <span class="number">1</span>;</span><br><span class="line">    a2 = <span class="number">2</span>;</span><br><span class="line">    a3 = <span class="number">3</span>;</span><br><span class="line">    a = <span class="number">4</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>;    <span class="comment">//正确</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a1 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//正确</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a2 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//正确</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a3 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//正确</span></span><br><span class="line">  &#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">int</span> a1;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">  <span class="keyword">int</span> a2;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  <span class="keyword">int</span> a3;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> :</span> <span class="keyword">protected</span> A&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">int</span> a;</span><br><span class="line">  B(<span class="keyword">int</span> i)&#123;</span><br><span class="line">    A();</span><br><span class="line">    a = i;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确，public成员。</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a1 &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确，基类的public成员，在派生类中变成了protected，可以被派生类访问。</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a2 &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确，基类的protected成员，在派生类中还是protected，可以被派生类访问。</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a3 &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//错误，基类的private成员不能被派生类访问。</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">  <span class="function">B <span class="title">b</span><span class="params">(<span class="number">10</span>)</span></span>;</span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确。public成员</span></span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a1 &lt;&lt; <span class="built_in">endl</span>;      <span class="comment">//错误，protected成员不能在类外访问。</span></span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a2 &lt;&lt; <span class="built_in">endl</span>;      <span class="comment">//错误，protected成员不能在类外访问。</span></span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a3 &lt;&lt; <span class="built_in">endl</span>;      <span class="comment">//错误，private成员不能在类外访问。</span></span><br><span class="line">  system(<span class="string">"pause"</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="private-继承"><a href="#private-继承" class="headerlink" title="private 继承"></a>private 继承</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span>&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">int</span> a;</span><br><span class="line">  A()&#123;</span><br><span class="line">    a1 = <span class="number">1</span>;</span><br><span class="line">    a2 = <span class="number">2</span>;</span><br><span class="line">    a3 = <span class="number">3</span>;</span><br><span class="line">    a = <span class="number">4</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>;    <span class="comment">//正确</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a1 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//正确</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a2 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//正确</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a3 &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//正确</span></span><br><span class="line">  &#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">int</span> a1;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">  <span class="keyword">int</span> a2;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  <span class="keyword">int</span> a3;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> :</span> <span class="keyword">private</span> A&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">int</span> a;</span><br><span class="line">  B(<span class="keyword">int</span> i)&#123;</span><br><span class="line">    A();</span><br><span class="line">    a = i;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确，public成员。</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a1 &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确，基类public成员,在派生类中变成了private,可以被派生类访问。</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a2 &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确，基类的protected成员，在派生类中变成了private,可以被派生类访问。</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a3 &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//错误，基类的private成员不能被派生类访问。</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">  <span class="function">B <span class="title">b</span><span class="params">(<span class="number">10</span>)</span></span>;</span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a &lt;&lt; <span class="built_in">endl</span>;       <span class="comment">//正确。public成员</span></span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a1 &lt;&lt; <span class="built_in">endl</span>;      <span class="comment">//错误，private成员不能在类外访问。</span></span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a2 &lt;&lt; <span class="built_in">endl</span>;      <span class="comment">//错误, private成员不能在类外访问。</span></span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; b.a3 &lt;&lt; <span class="built_in">endl</span>;      <span class="comment">//错误，private成员不能在类外访问。</span></span><br><span class="line">  system(<span class="string">"pause"</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>C++类和对象</title>
    <url>/2018/07/29/Cpp%E7%B1%BB%E5%92%8C%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<p>简要介绍C++的类与对象.</p>
<a id="more"></a>

<h1 id="C-类-amp-对象"><a href="#C-类-amp-对象" class="headerlink" title="C++ 类 &amp; 对象"></a>C++ 类 &amp; 对象</h1><p>C++ 在 C 语言的基础上增加了面向对象编程，C++ 支持面向对象程序设计。类是 C++ 的核心特性，通常被称为用户定义的类型。类用于指定对象的形式，它包含了数据表示法和用于处理数据的方法。类中的数据和方法称为类的成员。函数在一个类中被称为类的成员。</p>
<h2 id="C-类定义"><a href="#C-类定义" class="headerlink" title="C++ 类定义"></a>C++ 类定义</h2><p>定义一个类，本质上是定义一个数据类型的蓝图。这实际上并没有定义任何数据，但它定义了类的名称意味着什么，也就是说，它定义了类的对象包括了什么，以及可以在这个对象上执行哪些操作。</p>
<p>类定义是以关键字 <strong>class</strong> 开头，后跟类的名称。类的主体是包含在一对花括号中。类定义后必须跟着一个分号或一个声明列表。例如，我们使用关键字 <strong>class</strong> 定义 Box 数据类型，如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Box</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="keyword">double</span> length;   <span class="comment">// 盒子的长度</span></span><br><span class="line">      <span class="keyword">double</span> breadth;  <span class="comment">// 盒子的宽度</span></span><br><span class="line">      <span class="keyword">double</span> height;   <span class="comment">// 盒子的高度</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>关键字 <strong>public</strong> 确定了类成员的访问属性。在类对象作用域内，公共成员在类的外部是可访问的。您也可以指定类的成员为 <strong>private</strong> 或 <strong>protected</strong>，这个我们稍后会进行讲解。 </p>
<h2 id="定义-C-对象"><a href="#定义-C-对象" class="headerlink" title="定义 C++ 对象"></a>定义 C++ 对象</h2><p>类提供了对象的蓝图，所以基本上，对象是根据类来创建的。声明类的对象，就像声明基本类型的变量一样。下面的语句声明了类 Box 的两个对象：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">Box Box1;          <span class="comment">// 声明 Box1，类型为 Box</span></span><br><span class="line">Box Box2;          <span class="comment">// 声明 Box2，类型为 Box</span></span><br></pre></td></tr></table></figure>

<h2 id="访问数据成员"><a href="#访问数据成员" class="headerlink" title="访问数据成员"></a>访问数据成员</h2><p>类的对象的公共数据成员可以使用直接成员访问运算符 (.) 来访问。为了更好地理解这些概念，让我们尝试一下下面的实例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Box</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="keyword">double</span> length;   <span class="comment">// 长度</span></span><br><span class="line">      <span class="keyword">double</span> breadth;  <span class="comment">// 宽度</span></span><br><span class="line">      <span class="keyword">double</span> height;   <span class="comment">// 高度</span></span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   Box Box1;        <span class="comment">// 声明 Box1，类型为 Box</span></span><br><span class="line">   Box Box2;        <span class="comment">// 声明 Box2，类型为 Box</span></span><br><span class="line">   <span class="keyword">double</span> volume = <span class="number">0.0</span>;     <span class="comment">// 用于存储体积</span></span><br><span class="line"> </span><br><span class="line">   <span class="comment">// box 1 详述</span></span><br><span class="line">   Box1.height = <span class="number">5.0</span>; </span><br><span class="line">   Box1.length = <span class="number">6.0</span>; </span><br><span class="line">   Box1.breadth = <span class="number">7.0</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// box 2 详述</span></span><br><span class="line">   Box2.height = <span class="number">10.0</span>;</span><br><span class="line">   Box2.length = <span class="number">12.0</span>;</span><br><span class="line">   Box2.breadth = <span class="number">13.0</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// box 1 的体积</span></span><br><span class="line">   volume = Box1.height * Box1.length * Box1.breadth;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Box1 的体积："</span> &lt;&lt; volume &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// box 2 的体积</span></span><br><span class="line">   volume = Box2.height * Box2.length * Box2.breadth;</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Box2 的体积："</span> &lt;&lt; volume &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>需要注意的是，私有的成员和受保护的成员不能使用直接成员访问运算符 (.) 来直接访问。 </p>
<h1 id="C-类成员函数"><a href="#C-类成员函数" class="headerlink" title="C++ 类成员函数"></a>C++ 类成员函数</h1><p>类的成员函数是指那些把<strong>定义和原型写在类定义内部</strong>的函数，就像类定义中的其他变量一样。类成员函数是类的一个成员，它可以操作类的任意对象，可以访问对象中的所有成员。</p>
<p>让我们看看之前定义的类 Box，现在我们要使用成员函数来访问类的成员，而不是直接访问这些类的成员：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Box</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="keyword">double</span> length;         <span class="comment">// 长度</span></span><br><span class="line">      <span class="keyword">double</span> breadth;        <span class="comment">// 宽度</span></span><br><span class="line">      <span class="keyword">double</span> height;         <span class="comment">// 高度</span></span><br><span class="line">      <span class="function"><span class="keyword">double</span> <span class="title">getVolume</span><span class="params">(<span class="keyword">void</span>)</span></span>;<span class="comment">// 返回体积</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>成员函数可以定义在类定义内部，或者单独使用<strong>范围解析运算符 ::</strong> 来定义。在类定义中定义的成员函数把函数声明为<strong>内联</strong>的，即便没有使用 inline 标识符。所以您可以按照如下方式定义 <strong>Volume()</strong> 函数： </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Box</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="keyword">double</span> length;      <span class="comment">// 长度</span></span><br><span class="line">      <span class="keyword">double</span> breadth;     <span class="comment">// 宽度</span></span><br><span class="line">      <span class="keyword">double</span> height;      <span class="comment">// 高度</span></span><br><span class="line">   </span><br><span class="line">      <span class="function"><span class="keyword">double</span> <span class="title">getVolume</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function">      </span>&#123;</span><br><span class="line">         <span class="keyword">return</span> length * breadth * height;</span><br><span class="line">      &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>您也可以在类的外部使用<strong>范围解析运算符 ::</strong> 定义该函数，如下所示： </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">Box::getVolume</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> length * breadth * height;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Box</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="keyword">double</span> length;         <span class="comment">// 长度</span></span><br><span class="line">      <span class="keyword">double</span> breadth;        <span class="comment">// 宽度</span></span><br><span class="line">      <span class="keyword">double</span> height;         <span class="comment">// 高度</span></span><br><span class="line"> </span><br><span class="line">      <span class="comment">// 成员函数声明</span></span><br><span class="line">      <span class="function"><span class="keyword">double</span> <span class="title">getVolume</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setLength</span><span class="params">( <span class="keyword">double</span> len )</span></span>;</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setBreadth</span><span class="params">( <span class="keyword">double</span> bre )</span></span>;</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setHeight</span><span class="params">( <span class="keyword">double</span> hei )</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 成员函数定义</span></span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">Box::getVolume</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> length * breadth * height;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Box::setLength</span><span class="params">( <span class="keyword">double</span> len )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    length = len;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Box::setBreadth</span><span class="params">( <span class="keyword">double</span> bre )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    breadth = bre;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Box::setHeight</span><span class="params">( <span class="keyword">double</span> hei )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    height = hei;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 程序的主函数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   Box Box1;                <span class="comment">// 声明 Box1，类型为 Box</span></span><br><span class="line">   Box Box2;                <span class="comment">// 声明 Box2，类型为 Box</span></span><br><span class="line">   <span class="keyword">double</span> volume = <span class="number">0.0</span>;     <span class="comment">// 用于存储体积</span></span><br><span class="line"> </span><br><span class="line">   <span class="comment">// box 1 详述</span></span><br><span class="line">   Box1.setLength(<span class="number">6.0</span>); </span><br><span class="line">   Box1.setBreadth(<span class="number">7.0</span>); </span><br><span class="line">   Box1.setHeight(<span class="number">5.0</span>);</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// box 2 详述</span></span><br><span class="line">   Box2.setLength(<span class="number">12.0</span>); </span><br><span class="line">   Box2.setBreadth(<span class="number">13.0</span>); </span><br><span class="line">   Box2.setHeight(<span class="number">10.0</span>);</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// box 1 的体积</span></span><br><span class="line">   volume = Box1.getVolume();</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Box1 的体积："</span> &lt;&lt; volume &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// box 2 的体积</span></span><br><span class="line">   volume = Box2.getVolume();</span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Box2 的体积："</span> &lt;&lt; volume &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>:: 叫作用域区分符，指明一个函数属于哪个类或一个数据属于哪个类。</p>
<p>:: 可以不跟类名，表示全局数据或全局函数（即非成员函数）。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> month;<span class="comment">//全局变量</span></span><br><span class="line"><span class="keyword">int</span> day;</span><br><span class="line"><span class="keyword">int</span> year;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Set</span><span class="params">(<span class="keyword">int</span> m,<span class="keyword">int</span> d,<span class="keyword">int</span> y)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ::year=y; <span class="comment">//给全局变量赋值，此处可省略</span></span><br><span class="line">    ::day=d;</span><br><span class="line">    ::month=m;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Class Tdate</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">        <span class="function"><span class="keyword">void</span> <span class="title">Set</span><span class="params">(<span class="keyword">int</span> m,<span class="keyword">int</span> d,<span class="keyword">int</span> y)</span> <span class="comment">//成员函数</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            ::Set(m,d,y); <span class="comment">//非成员函数</span></span><br><span class="line">        &#125;</span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">        <span class="keyword">int</span> month;</span><br><span class="line">        <span class="keyword">int</span> day;</span><br><span class="line">        <span class="keyword">int</span> year;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>类中的成员函数与 inline</strong></p>
<p>定义在类中的成员函数缺省都是内联的，如果在类定义时就在类内给出函数定义，那当然最好。如果在类中未给出成员函数定义，而又想内联该函数的话，那在类外要加上 inline，否则就认为不是内联的。例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">public</span>:<span class="function"><span class="keyword">void</span> <span class="title">Foo</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span> </span>&#123;  &#125; <span class="comment">// 自动地成为内联函数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>将成员函数的定义体放在类声明之中虽然能带来书写上的方便，但不是一种良好的编程风格，上例应该改成：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 头文件</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Foo</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 定义文件</span></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">A::Foo</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span></span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<p><strong>inline 是一种用于实现的关键字</strong></p>
<p>关键字 inline 必须与函数定义体放在一起才能使函数成为内联，仅将inline 放在函数声明前面不起任何作用。</p>
<p>如下风格的函数 <strong>Foo</strong> 不能成为内联函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">Foo</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span></span>; <span class="comment">// inline 仅与函数声明放在一起</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Foo</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span></span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<p>而如下风格的函数Foo 则成为内联函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Foo</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">Foo</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span> </span>&#123;&#125;  <span class="comment">// inline 与函数定义体放在一起</span></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>IO流（复习7）</title>
    <url>/2020/06/01/IO%E6%B5%81%EF%BC%88%E5%A4%8D%E4%B9%A07%EF%BC%89/</url>
    <content><![CDATA[<p>IO学习.</p>
<a id="more"></a>

<h1 id="File类"><a href="#File类" class="headerlink" title="File类"></a>File类</h1><p>File类表示文件或者文件目录。File一般用于IO流的构造器，代表文件操作的起点和终点。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.File;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        File file = <span class="keyword">new</span> File(<span class="string">"hello.txt"</span>);<span class="comment">//相对路径，以module的路径为根</span></span><br><span class="line"></span><br><span class="line">        File file1 = <span class="keyword">new</span> File(<span class="string">"D:\\source"</span>);<span class="comment">//child可以是文件也可以是文件目录</span></span><br><span class="line"></span><br><span class="line">        File file2 = <span class="keyword">new</span> File(file1, <span class="string">"test.txt"</span>);</span><br><span class="line"></span><br><span class="line">        System.out.println(file.getAbsolutePath());<span class="comment">//绝对路径</span></span><br><span class="line">        System.out.println(file.getPath());</span><br><span class="line">        System.out.println(file.getName());</span><br><span class="line">        System.out.println(file.getParent());<span class="comment">//写的相对路径，所以parent路径为null</span></span><br><span class="line">        System.out.println(file.length());<span class="comment">//内容长度</span></span><br><span class="line">        System.out.println(file.lastModified());<span class="comment">//最后修改时间</span></span><br><span class="line"></span><br><span class="line">        String[] list = file1.list();<span class="comment">//获取file1路径下的所有文件或文件目录</span></span><br><span class="line">        File[] files = file1.listFiles();<span class="comment">//同上，返回值类型不同，这两个方法还可以传入过滤器</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">boolean</span> b = file.renameTo(file2);<span class="comment">//把file重命名到指定的file2，要求file存在，file2不存在，若成功，file会消失</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">boolean</span> b1 = file.isFile();</span><br><span class="line">        <span class="keyword">boolean</span> b2 = file.isDirectory();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">boolean</span> exists = file.exists();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">boolean</span> b3 = file.canRead();</span><br><span class="line">        <span class="keyword">boolean</span> b4 = file.canWrite();</span><br><span class="line">        <span class="keyword">boolean</span> b5 = file.isHidden();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        File file = <span class="keyword">new</span> File(<span class="string">"hi.txt"</span>);</span><br><span class="line">        <span class="keyword">if</span> (!file.exists()) &#123;<span class="comment">//创建文件</span></span><br><span class="line">            file.createNewFile();</span><br><span class="line">            System.out.println(<span class="string">"create"</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            file.delete();<span class="comment">//删除</span></span><br><span class="line">            System.out.println(<span class="string">"delete"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test3</span><span class="params">()</span></span>&#123;</span><br><span class="line">        File file = <span class="keyword">new</span> File(<span class="string">"D:\\io\\io1"</span>);</span><br><span class="line">        <span class="keyword">boolean</span> mkdir = file.mkdir();<span class="comment">//上一层目录存在时，mkdir和mkdirs没有区别，但是但上一层不存在时，只有mkdirs能创建成功</span></span><br><span class="line">        <span class="keyword">if</span> (mkdir) &#123;</span><br><span class="line">            System.out.println(<span class="string">"create"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="IO流"><a href="#IO流" class="headerlink" title="IO流"></a>IO流</h1><h2 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h2><p>根据数据单位：字节流(8bit)，处理图片等非文本文件，字符流(16bit)，处理文本文件</p>
<p>根据流向：输入流，输出流</p>
<p>根据角色：节点流（直接处理文件），处理流（对流进行处理）</p>
<p>四大基类：</p>
<table>
<thead>
<tr>
<th>分类</th>
<th>字节流</th>
<th>字符流</th>
</tr>
</thead>
<tbody><tr>
<td>输入流</td>
<td>InputStream</td>
<td>Reader</td>
</tr>
<tr>
<td>输出流</td>
<td>OutputStream</td>
<td>Writer</td>
</tr>
</tbody></table>
<p><img src="/2020/06/01/IO%E6%B5%81%EF%BC%88%E5%A4%8D%E4%B9%A07%EF%BC%89/%E4%BD%93%E7%B3%BB.png" alt></p>
<blockquote>
<p>注意表中的粗体字标出的类代表节点流，斜体字标出的类代表抽象基类</p>
</blockquote>
<p>先选出一部分：</p>
<table>
<thead>
<tr>
<th>抽象基类</th>
<th>节点流（又叫文件流）</th>
<th>缓冲流（处理流的一种）</th>
</tr>
</thead>
<tbody><tr>
<td>InputStream</td>
<td>FileInputStream</td>
<td>BufferedInputStream</td>
</tr>
<tr>
<td>OutputStream</td>
<td>FileOutputStream</td>
<td>BufferedOutputStream</td>
</tr>
<tr>
<td>Reader</td>
<td>FileReader</td>
<td>BufferedReader</td>
</tr>
<tr>
<td>Writer</td>
<td>FileWriter</td>
<td>BufferedWriter</td>
</tr>
</tbody></table>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test4</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    FileReader fileReader = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        File file = <span class="keyword">new</span> File(<span class="string">"hello.txt"</span>);</span><br><span class="line">        fileReader = <span class="keyword">new</span> FileReader(file);</span><br><span class="line">        <span class="keyword">int</span> read = fileReader.read();<span class="comment">//读入的字符,如果到达文件末尾，返回-1</span></span><br><span class="line">        <span class="keyword">while</span> (read != -<span class="number">1</span>) &#123;</span><br><span class="line">            System.out.print((<span class="keyword">char</span>) read);</span><br><span class="line">            read = fileReader.read();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (fileReader != <span class="keyword">null</span>) &#123;</span><br><span class="line">                fileReader.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test5</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    FileReader fileReader = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">//1.File类的实例化</span></span><br><span class="line">        File file = <span class="keyword">new</span> File(<span class="string">"hello.txt"</span>);</span><br><span class="line">        <span class="comment">//2.流的实例化</span></span><br><span class="line">        fileReader = <span class="keyword">new</span> FileReader(file);</span><br><span class="line">        <span class="comment">//3.读入的操作</span></span><br><span class="line">        <span class="keyword">char</span>[] cbuf = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">5</span>];</span><br><span class="line">        <span class="comment">//返回每次读入数组的个数，文件末尾返回-1</span></span><br><span class="line">        <span class="keyword">int</span> len;</span><br><span class="line">        <span class="keyword">while</span> ((len = fileReader.read(cbuf)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; cbuf.length; i++) &#123;</span><br><span class="line">                System.out.print(cbuf[i]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="comment">//4.资源关闭</span></span><br><span class="line">        <span class="keyword">if</span> (fileReader != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                fileReader.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码中读取helloworld.txt，里面是helloworld十个字母，刚好读完。如果是helloworld123，那么结果就是helloworld123ld,最后一次读入时只读了三个，因此上一轮读取的数组前三个被替换，后两个还是上一轮的。所以上面的写法不合适，for循环里面终止条件应该改为<code>i&lt;len</code>,每次读几个就遍历几个。<br>用这种写法也是Ok的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">while</span> ((len = fileReader.read(cbuf)) != -<span class="number">1</span>) &#123;</span><br><span class="line">    String str = <span class="keyword">new</span> String(cbuf, <span class="number">0</span>, len);</span><br><span class="line">    System.out.print(str);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面都是节点流（文件流）读入文件，下面写个节点流写入文件。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test6</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    File file = <span class="keyword">new</span> File(<span class="string">"hello.txt"</span>);</span><br><span class="line"></span><br><span class="line">    FileWriter fileWriter = <span class="keyword">new</span> FileWriter(file);</span><br><span class="line"></span><br><span class="line">    fileWriter.write(<span class="string">"I have a dream.\n"</span>);</span><br><span class="line">    fileWriter.write(<span class="string">"You have a dream too."</span>);</span><br><span class="line">    </span><br><span class="line">    fileWriter.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当hello.txt不存在时，会先创建这个文件。如果第二次执行上述代码，即文件存在，就会覆盖原有文件内容。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">FileWriter fileWriter = <span class="keyword">new</span> FileWriter(file,<span class="keyword">true</span>);<span class="comment">//如果添加了append参数且值为true，那么就是在原文件内容后面追加，而不是覆盖。构造器一个参数时等同于改参数为false。</span></span><br></pre></td></tr></table></figure>

<p>读取写入结合起来：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test7</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    FileReader fileReader = <span class="keyword">null</span>;</span><br><span class="line">    FileWriter fileWriter = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        File srcfile = <span class="keyword">new</span> File(<span class="string">"hello.txt"</span>);</span><br><span class="line">        File destfile = <span class="keyword">new</span> File(<span class="string">"out.txt"</span>);</span><br><span class="line"></span><br><span class="line">        fileReader = <span class="keyword">new</span> FileReader(srcfile);</span><br><span class="line">        fileWriter = <span class="keyword">new</span> FileWriter(destfile);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">char</span>[] cbuf = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">5</span>];</span><br><span class="line">        <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> ((len = fileReader.read(cbuf)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            fileWriter.write(cbuf, <span class="number">0</span>, len);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (fileReader != <span class="keyword">null</span>) &#123;</span><br><span class="line">                fileReader.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (fileWriter != <span class="keyword">null</span>) &#123;</span><br><span class="line">                fileWriter.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>用字节流读取图片并复制：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test7</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    FileInputStream fis = <span class="keyword">null</span>;</span><br><span class="line">    FileOutputStream fos = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        File srcfile = <span class="keyword">new</span> File(<span class="string">"关系.png"</span>);</span><br><span class="line">        File destfile = <span class="keyword">new</span> File(<span class="string">"关系copy.png"</span>);</span><br><span class="line"></span><br><span class="line">        fis = <span class="keyword">new</span> FileInputStream(srcfile);</span><br><span class="line">        fos = <span class="keyword">new</span> FileOutputStream(destfile);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">byte</span>[] cbuf = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">5</span>];</span><br><span class="line">        <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> ((len = fis.read(cbuf)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            fos.write(cbuf, <span class="number">0</span>, len);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (fis != <span class="keyword">null</span>) &#123;</span><br><span class="line">                fis.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (fos != <span class="keyword">null</span>) &#123;</span><br><span class="line">                fos.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果使用字节流读取文本文件，其实也是可以的，但是只有英文字母这样一个字母占一个字节的才能正确显示，要是中文的，以utf-8编码为例，用三个字节存的，那么就会出现乱码。</p>
<p>上面是用节点流处理文件，下面用处理流的缓冲流来处理节点流。缓冲流可以加快文件读取速度。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test8</span><span class="params">()</span></span>&#123;</span><br><span class="line">    File srcFile = <span class="keyword">new</span> File(<span class="string">"关系.png"</span>);</span><br><span class="line">    File destFile = <span class="keyword">new</span> File(<span class="string">"关系.png"</span>);</span><br><span class="line">    copyFileWithBuffer(srcFile,destFile);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">copyFileWithBuffer</span><span class="params">(File srcFile,File destFile)</span> </span>&#123;</span><br><span class="line">FileInputStream fis;</span><br><span class="line">FileOutputStream fos;</span><br><span class="line">BufferedInputStream bis = <span class="keyword">null</span>;</span><br><span class="line">BufferedOutputStream bos = <span class="keyword">null</span>;</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    fis = <span class="keyword">new</span> FileInputStream(srcFile);</span><br><span class="line">    fos = <span class="keyword">new</span> FileOutputStream(destFile);</span><br><span class="line"></span><br><span class="line">    bis = <span class="keyword">new</span> BufferedInputStream(fis);</span><br><span class="line">    bos = <span class="keyword">new</span> BufferedOutputStream(fos);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">byte</span>[] buffer = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">10</span>];</span><br><span class="line">    <span class="keyword">int</span> len;</span><br><span class="line">    <span class="keyword">while</span> ((len=bis.read(buffer))!=-<span class="number">1</span>)&#123;</span><br><span class="line">        bos.write(buffer, <span class="number">0</span>, len);</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;<span class="comment">//先关闭外层的流，再关闭内层的流</span></span><br><span class="line">    <span class="keyword">if</span> (bis != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            bis.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (bos != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            bos.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//关闭外层流的同时内层流也会自动关闭</span></span><br><span class="line">    <span class="comment">//fis.close();</span></span><br><span class="line">    <span class="comment">//fos.close();</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面就是用缓冲流的代码，下面测试了不用缓冲流和用缓冲流的速度差异：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test9</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> start1=System.currentTimeMillis();</span><br><span class="line">    copyFile(<span class="keyword">new</span> File(<span class="string">"C:\\Users\\YLxia\\Downloads\\1.caj"</span>), <span class="keyword">new</span> File(<span class="string">"C:\\Users\\YLxia\\Downloads\\1copy.caj"</span>));</span><br><span class="line">    <span class="keyword">long</span> end1=System.currentTimeMillis();</span><br><span class="line">    System.out.println(end1 - start1);</span><br><span class="line">    System.out.println(<span class="string">"====="</span>);</span><br><span class="line">    <span class="keyword">long</span> start2=System.currentTimeMillis();</span><br><span class="line">    copyFileWithBuffer(<span class="keyword">new</span> File(<span class="string">"C:\\Users\\YLxia\\Desktop\\1.caj"</span>), <span class="keyword">new</span> File(<span class="string">"C:\\Users\\YLxia\\Desktop\\1copy.caj"</span>));</span><br><span class="line">    <span class="keyword">long</span> end2=System.currentTimeMillis();</span><br><span class="line">    System.out.println(end2 - start2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">14660</span><br><span class="line">&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;</span><br><span class="line">96</span><br></pre></td></tr></table></figure>

<p>可见差异巨大。</p>
<p><u>处理流不仅可以套接节点流，也可以套接处理流。</u></p>
<p>java还提供了转换流用于字节流和字符流之间的转换。</p>
<p>InputStreamReader：将一个字节的输入流转换为字符的输入流，用于将字节，字节数组解码为字符，字符串</p>
<p>OutputStreamWriter：将一个字符的输出流转换为字节的输出流，用于字符数组，字符串编码为字节，字节数组</p>
<p>编码解码就涉及到字符集的问题。</p>
<h2 id="其它流："><a href="#其它流：" class="headerlink" title="其它流："></a>其它流：</h2><p>标准输入输出流，打印流。</p>
<p>数据流，用于读取和写入Java的基本数据类型和String类型。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test12</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    DataOutputStream dos = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        dos = <span class="keyword">new</span> DataOutputStream(<span class="keyword">new</span> FileOutputStream(<span class="string">"hello.txt"</span>));</span><br><span class="line"></span><br><span class="line">        dos.writeUTF(<span class="string">"你好"</span>);</span><br><span class="line">        dos.flush();</span><br><span class="line">        dos.writeInt(<span class="number">22</span>);</span><br><span class="line">        dos.flush();</span><br><span class="line">        dos.writeBoolean(<span class="keyword">true</span>);</span><br><span class="line">        dos.flush();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (dos != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                dos.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对象流：存储和读取基本数据类型和<strong>对象</strong>的数据流。</p>
<p>序列化：用ObjectOutputStream类保存基本数据或对象的机制</p>
<p>反序列化：用ObjectInputStream类读取基本数据类型或对象的机制</p>
<p><strong>这两个类不能序列化static和transient修饰的成员变量</strong></p>
<p>要想对象是可序列化的需要满足以下要求：</p>
<p>1.实现Serializable接口</p>
<p>2.需要有一个serialVersionUID</p>
<p>3.内部所有属性都要是可序列化的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span> <span class="keyword">implements</span> <span class="title">Serializable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = -<span class="number">1987345788358137936L</span>;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAge</span><span class="params">(<span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Person</span><span class="params">(String name, <span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test13</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ObjectOutputStream oos = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        oos = <span class="keyword">new</span> ObjectOutputStream(<span class="keyword">new</span> FileOutputStream(<span class="string">"class.dat"</span>));</span><br><span class="line"></span><br><span class="line">        oos.writeObject(<span class="keyword">new</span> Person(<span class="string">"zhangsan"</span>, <span class="number">66</span>));</span><br><span class="line">        oos.flush();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (oos != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                oos.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test14</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ObjectInputStream ois = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        ois = <span class="keyword">new</span> ObjectInputStream(<span class="keyword">new</span> FileInputStream(<span class="string">"class.dat"</span>));</span><br><span class="line">        Object o = ois.readObject();</span><br><span class="line">        System.out.println(o.getClass());</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (ois != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                ois.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>RandomAccessFile:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test15</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    RandomAccessFile raf1 = <span class="keyword">null</span>;</span><br><span class="line">    RandomAccessFile raf2 = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">//RandomAccessFile流</span></span><br><span class="line">        <span class="comment">//直接继承于Object，实现了DataInput和DataOutput，既可以做输入流也可以做输出流（同一个类，但不是同一个对象）</span></span><br><span class="line">        <span class="comment">//RandomAccessFile的构造器第二个参数mode可以指定对文件的访问方式</span></span><br><span class="line">        <span class="comment">//r:只读，rw:可读可写，rwd:多一个同步文件内容的更新，rws:多一个同步文件内容和元数据更新</span></span><br><span class="line">        <span class="comment">//rw模式数据不会立刻写到硬盘，rwd则是。若写数据时发生异常，rw的数据则会全部丢失，rwd写入的不会</span></span><br><span class="line">        raf1 = <span class="keyword">new</span> RandomAccessFile(<span class="keyword">new</span> File(<span class="string">"关系.png"</span>), <span class="string">"r"</span>);</span><br><span class="line">        <span class="comment">//若文件不存在则新建文件</span></span><br><span class="line">        <span class="comment">//若已存在，写入数据时是从文件头开始覆盖，而不是整个文件覆盖，当新内容少于旧内容时会出现新旧混合的情况</span></span><br><span class="line">        raf2 = <span class="keyword">new</span> RandomAccessFile(<span class="keyword">new</span> File(<span class="string">"gx.png"</span>), <span class="string">"rw"</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">byte</span>[] buffer = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">int</span> len;</span><br><span class="line">        <span class="keyword">while</span> ((len = raf1.read(buffer)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            <span class="comment">//对于非空文件，write方法实际就是覆盖操作，默认是从下标0开始</span></span><br><span class="line">            raf2.seek(<span class="keyword">new</span> File(<span class="string">"关系.png"</span>).length());<span class="comment">//设定覆盖起始位置为原文件末尾，实现追加内容功能</span></span><br><span class="line">            raf2.write(buffer, <span class="number">0</span>, len);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (raf1 != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                raf1.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (raf2 != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                raf2.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>JDBC</title>
    <url>/2020/06/22/JDBC/</url>
    <content><![CDATA[<p>JDBC学习.</p>
<a id="more"></a>

<p>driver是一种接口,规范了如何操作数据库.而具体如何实现还要看各大数据库厂商的实现.</p>
<p>下面是几种获得数据库连接的方式:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.InputStream;</span><br><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.Driver;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.SQLException;</span><br><span class="line"><span class="keyword">import</span> java.util.Properties;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YLxia</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConnectionTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span><span class="comment">//方式一</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">ConnectionTest1</span><span class="params">()</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">        <span class="comment">//获取driver的实现类对象</span></span><br><span class="line">        Driver driver = <span class="keyword">new</span> com.mysql.jdbc.Driver();</span><br><span class="line">        String url = <span class="string">"jdbc:mysql://localhost:3306/test?serverTimezone=GMT"</span>;</span><br><span class="line">        Properties info = <span class="keyword">new</span> Properties();</span><br><span class="line">        info.setProperty(<span class="string">"user"</span>, <span class="string">"root"</span>);</span><br><span class="line">        info.setProperty(<span class="string">"password"</span>, <span class="string">"645866233"</span>);</span><br><span class="line">        Connection connect = driver.connect(url, info);</span><br><span class="line">        System.out.println(connect);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span><span class="comment">//方式二</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">ConnectionTest2</span><span class="params">()</span> <span class="keyword">throws</span> ClassNotFoundException, IllegalAccessException, InstantiationException, SQLException </span>&#123;</span><br><span class="line">        Class driverclass = Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);<span class="comment">//获取class实例四种方式之一</span></span><br><span class="line">        Driver driver = (Driver) driverclass.newInstance();</span><br><span class="line">        String url = <span class="string">"jdbc:mysql://localhost:3306/test?serverTimezone=GMT"</span>;</span><br><span class="line">        Properties info = <span class="keyword">new</span> Properties();</span><br><span class="line">        info.setProperty(<span class="string">"user"</span>, <span class="string">"root"</span>);</span><br><span class="line">        info.setProperty(<span class="string">"password"</span>, <span class="string">"645866233"</span>);</span><br><span class="line">        Connection connect = driver.connect(url, info);</span><br><span class="line">        System.out.println(connect);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span><span class="comment">//方式三,用DriverManager替换driver</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">ConnectionTest3</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        Class driverclass = Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);<span class="comment">//获取class实例四种方式之一</span></span><br><span class="line">        Driver driver = (Driver) driverclass.newInstance();</span><br><span class="line">        String url = <span class="string">"jdbc:mysql://localhost:3306/test?serverTimezone=GMT"</span>;</span><br><span class="line">        String user = <span class="string">"root"</span>;</span><br><span class="line">        String password = <span class="string">"645866233"</span>;</span><br><span class="line">        DriverManager.registerDriver(driver);</span><br><span class="line">        DriverManager.getConnection(url, user, password);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span><span class="comment">//方式四</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">ConnectionTest4</span><span class="params">()</span> <span class="keyword">throws</span> SQLException, ClassNotFoundException </span>&#123;</span><br><span class="line">        Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);<span class="comment">//加载驱动到内存,这句也可以省略,但不推荐</span></span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        MySQL8中的com.mysql.jdbc.Driver继承于com.mysql.cj.jdbc.Driver,而在后者中有个静态代码块,当驱动类加载后静态代码块就执行:</span></span><br><span class="line"><span class="comment">        try &#123;</span></span><br><span class="line"><span class="comment">            java.sql.DriverManager.registerDriver(new Driver());//注册驱动</span></span><br><span class="line"><span class="comment">        &#125; catch (SQLException E) &#123;</span></span><br><span class="line"><span class="comment">            throw new RuntimeException("Can't register driver!");</span></span><br><span class="line"><span class="comment">        &#125;</span></span><br><span class="line"><span class="comment">        综上,本质和第三种没区别</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line"></span><br><span class="line">        String url = <span class="string">"jdbc:mysql://localhost:3306/test?serverTimezone=GMT"</span>;</span><br><span class="line">        String user = <span class="string">"root"</span>;</span><br><span class="line">        String password = <span class="string">"645866233"</span>;</span><br><span class="line">        DriverManager.getConnection(url, user, password);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span><span class="comment">//方式五</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">Connection5</span><span class="params">()</span> <span class="keyword">throws</span> IOException, ClassNotFoundException, SQLException </span>&#123;</span><br><span class="line">        <span class="comment">//将连接数据库需要的四个信息写在配置文件中</span></span><br><span class="line">        <span class="comment">//读取配置文件信息</span></span><br><span class="line">        InputStream is = ConnectionTest.class.getClassLoader().getResourceAsStream("jdbc.properties");</span><br><span class="line">        Properties pros = <span class="keyword">new</span> Properties();</span><br><span class="line">        pros.load(is);</span><br><span class="line"></span><br><span class="line">        String user = pros.getProperty(<span class="string">"user"</span>);</span><br><span class="line">        String password = pros.getProperty(<span class="string">"password"</span>);</span><br><span class="line">        String url = pros.getProperty(<span class="string">"url"</span>);</span><br><span class="line">        String driverClass = pros.getProperty(<span class="string">"driverClass"</span>);<span class="comment">//这里改用新版的com.mysql.cj.jdbc.Driver</span></span><br><span class="line"></span><br><span class="line">        Class.forName(driverClass);</span><br><span class="line">        Connection connection = DriverManager.getConnection(url, user, password);</span><br><span class="line">        System.out.println(connection);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">Propertiestest</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        Properties properties = <span class="keyword">new</span> Properties();</span><br><span class="line"><span class="comment">//        FileInputStream fis = new FileInputStream("jdbc.properties");</span></span><br><span class="line"><span class="comment">//        properties.load(fis);</span></span><br><span class="line"></span><br><span class="line">        ClassLoader classLoader = ConnectionTest<span class="class">.<span class="keyword">class</span>.<span class="title">getClassLoader</span>()</span>;<span class="comment">//获取系统类加载器</span></span><br><span class="line">        InputStream is = classLoader.getResourceAsStream(<span class="string">"jdbc.properties"</span>);</span><br><span class="line">        properties.load(is);</span><br><span class="line"></span><br><span class="line">        String user = properties.getProperty(<span class="string">"user"</span>);</span><br><span class="line">        String password = properties.getProperty(<span class="string">"password"</span>);</span><br><span class="line">        System.out.println(user + <span class="string">'+'</span> + password);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>执行SQL语句共有三种方式:</p>
<p>Sataement:执行静态SQL语句并返回结果对象</p>
<p>PreparedStatement:SQL语句被预编译存储在该对象中</p>
<p>CallableStatement:用于执行存储过程</p>
<p>下面代码演示了如何连接并使用预编译SQL语句:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.InputStream;</span><br><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.PreparedStatement;</span><br><span class="line"><span class="keyword">import</span> java.sql.SQLException;</span><br><span class="line"><span class="keyword">import</span> java.text.ParseException;</span><br><span class="line"><span class="keyword">import</span> java.text.SimpleDateFormat;</span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"><span class="keyword">import</span> java.util.Properties;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PreParedStatementTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insertTest</span><span class="params">()</span> <span class="keyword">throws</span> ClassNotFoundException, SQLException, IOException, ParseException </span>&#123;</span><br><span class="line">        Connection connection = <span class="keyword">null</span>;</span><br><span class="line">        PreparedStatement ps = <span class="keyword">null</span>;<span class="comment">//预编译SQL语句,返回preparedstatement的实例</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            InputStream is = ConnectionTest.class.getClassLoader().getResourceAsStream("jdbc.properties");</span><br><span class="line">            Properties pros = <span class="keyword">new</span> Properties();</span><br><span class="line">            pros.load(is);</span><br><span class="line"></span><br><span class="line">            String user = pros.getProperty(<span class="string">"user"</span>);</span><br><span class="line">            String password = pros.getProperty(<span class="string">"password"</span>);</span><br><span class="line">            String url = pros.getProperty(<span class="string">"url"</span>);</span><br><span class="line">            String driverClass = pros.getProperty(<span class="string">"driverClass"</span>);<span class="comment">//这里改用新版的com.mysql.cj.jdbc.Driver</span></span><br><span class="line">            <span class="comment">//加载驱动</span></span><br><span class="line">            Class.forName(driverClass);</span><br><span class="line">            <span class="comment">//获取连接</span></span><br><span class="line">            connection = DriverManager.getConnection(url, user, password);</span><br><span class="line">            <span class="comment">//预编译SQL语句</span></span><br><span class="line">            String sql = <span class="string">"insert into test.customers(name,email,birth) values(?,?,?)"</span>;</span><br><span class="line">            ps = connection.prepareStatement(sql);</span><br><span class="line">            <span class="comment">//占位符填充</span></span><br><span class="line">            ps.setString(<span class="number">1</span>, <span class="string">"哪吒"</span>);</span><br><span class="line">            ps.setString(<span class="number">2</span>, <span class="string">"yl@gmail.com"</span>);</span><br><span class="line">            SimpleDateFormat sdf = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy-MM-dd"</span>);</span><br><span class="line">            Date date = sdf.parse(<span class="string">"1000-1-1"</span>);</span><br><span class="line">            ps.setDate(<span class="number">3</span>, <span class="keyword">new</span> java.sql.Date(date.getTime()));</span><br><span class="line"></span><br><span class="line">            <span class="comment">//执行,如果是查询操作有返回值则是true,如果是增删改操作则是false</span></span><br><span class="line">            ps.execute();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException | ParseException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">//资源关闭</span></span><br><span class="line">            <span class="keyword">if</span> (ps != <span class="keyword">null</span>) &#123;</span><br><span class="line">                ps.close();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (connection != <span class="keyword">null</span>) &#123;</span><br><span class="line">                connection.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">updateTest</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Connection connection = <span class="keyword">null</span>;</span><br><span class="line">        PreparedStatement ps = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">//获取连接</span></span><br><span class="line">            connection = JDBCUtils.getConnection();</span><br><span class="line">            <span class="comment">//预编译SQL语句</span></span><br><span class="line">            String sql = <span class="string">"update test.customers set name = ? where  id= ? "</span>;</span><br><span class="line">            ps = connection.prepareStatement(sql);</span><br><span class="line">            <span class="comment">//填充占位符</span></span><br><span class="line">            ps.setString(<span class="number">1</span>, <span class="string">"哪吒"</span>);</span><br><span class="line">            ps.setInt(<span class="number">2</span>, <span class="number">19</span>);</span><br><span class="line">            <span class="comment">//执行</span></span><br><span class="line">            ps.execute();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">//资源关闭</span></span><br><span class="line">            JDBCUtils.closeResource(connection, ps);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">update</span><span class="params">(String sql, Object... args)</span> </span>&#123;</span><br><span class="line">        Connection connection = <span class="keyword">null</span>;</span><br><span class="line">        PreparedStatement ps = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            connection = JDBCUtils.getConnection();</span><br><span class="line">            ps = connection.prepareStatement(sql);</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; args.length; i++) &#123;</span><br><span class="line">                ps.setObject(i + <span class="number">1</span>, args[i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> ps.executeUpdate();<span class="comment">//返回DML所影响的行数,或者0表示没有任何返回(例如insert操作,返回结果为0,那么表示插入失败)</span></span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            JDBCUtils.closeResource(connection, ps);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后使用可变参数实现增删改的通用写法,其中IDBCUtils工具类的实现如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.InputStream;</span><br><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.PreparedStatement;</span><br><span class="line"><span class="keyword">import</span> java.sql.SQLException;</span><br><span class="line"><span class="keyword">import</span> java.util.Properties;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JDBCUtils</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Connection <span class="title">getConnection</span><span class="params">()</span> <span class="keyword">throws</span> IOException, ClassNotFoundException, SQLException </span>&#123;</span><br><span class="line">        InputStream is = ClassLoader.getSystemClassLoader().getResourceAsStream(<span class="string">"jdbc.properties"</span>);</span><br><span class="line">        Properties pros = <span class="keyword">new</span> Properties();</span><br><span class="line">        pros.load(is);</span><br><span class="line"></span><br><span class="line">        String user = pros.getProperty(<span class="string">"user"</span>);</span><br><span class="line">        String password = pros.getProperty(<span class="string">"password"</span>);</span><br><span class="line">        String url = pros.getProperty(<span class="string">"url"</span>);</span><br><span class="line">        String driverClass = pros.getProperty(<span class="string">"driverClass"</span>);<span class="comment">//这里改用新版的com.mysql.cj.jdbc.Driver</span></span><br><span class="line"></span><br><span class="line">        Class.forName(driverClass);</span><br><span class="line">        <span class="keyword">return</span> DriverManager.getConnection(url, user, password);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">closeResource</span><span class="params">(Connection connection, PreparedStatement ps)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (ps != <span class="keyword">null</span>) &#123;</span><br><span class="line">                ps.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (connection != <span class="keyword">null</span>) &#123;</span><br><span class="line">                connection.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>查询与增删改不一样,需要处理查询的返回值.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testQuery1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Connection connection = <span class="keyword">null</span>;</span><br><span class="line">    PreparedStatement ps = <span class="keyword">null</span>;</span><br><span class="line">    ResultSet resultSet = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        connection = JDBCUtils.getConnection();</span><br><span class="line">        String sql = <span class="string">"select id,name,email,birth from test.customers where id=?"</span>;</span><br><span class="line">        ps = connection.prepareStatement(sql);</span><br><span class="line">        ps.setInt(<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">        resultSet = ps.executeQuery();</span><br><span class="line">        <span class="keyword">if</span> (resultSet.next()) &#123;<span class="comment">//判断结果集下一条是否有数据,如果true,指针下移</span></span><br><span class="line">            <span class="comment">//处理当前数据的各个字段</span></span><br><span class="line">            <span class="keyword">int</span> id = resultSet.getInt(<span class="number">1</span>);</span><br><span class="line">            String name = resultSet.getString(<span class="number">2</span>);</span><br><span class="line">            String email = resultSet.getString(<span class="number">3</span>);</span><br><span class="line">            java.sql.Date birth = resultSet.getDate(<span class="number">4</span>);</span><br><span class="line">            Customer customer = <span class="keyword">new</span> Customer(id, name, email, birth);</span><br><span class="line">            System.out.println(customer);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JDBCUtils.closeResource(connection, ps, resultSet);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>针对一个表的通用操作:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Customer <span class="title">query</span><span class="params">(String sql, Object... args)</span> </span>&#123;</span><br><span class="line">    Connection connection = <span class="keyword">null</span>;</span><br><span class="line">    PreparedStatement ps = <span class="keyword">null</span>;</span><br><span class="line">    ResultSet resultSet = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        connection = JDBCUtils.getConnection();</span><br><span class="line">        ps = connection.prepareStatement(sql);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; args.length; i++) &#123;</span><br><span class="line">            ps.setObject(i + <span class="number">1</span>, args[i]);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        resultSet = ps.executeQuery();</span><br><span class="line">        ResultSetMetaData metaData = resultSet.getMetaData();<span class="comment">//获取结果集的元数据</span></span><br><span class="line">        <span class="keyword">int</span> columnCount = metaData.getColumnCount();</span><br><span class="line">        <span class="keyword">if</span> (resultSet.next()) &#123;</span><br><span class="line">            Customer customer = <span class="keyword">new</span> Customer();</span><br><span class="line">            <span class="comment">//处理结果集中一行数据的每一列</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; columnCount; i++) &#123;</span><br><span class="line">                Object columnVal = resultSet.getObject(i + <span class="number">1</span>);</span><br><span class="line">                <span class="comment">//获取列名</span></span><br><span class="line">                String columnName = metaData.getColumnName(i + <span class="number">1</span>);</span><br><span class="line">                <span class="comment">//给指定customer对象的对应属性赋值,通过反射,由列明找属性</span></span><br><span class="line">                Field field = Customer<span class="class">.<span class="keyword">class</span>.<span class="title">getDeclaredField</span>(<span class="title">columnName</span>)</span>;</span><br><span class="line">                field.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">                field.set(customer, columnVal);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> customer;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException | NoSuchFieldException | IllegalAccessException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JDBCUtils.closeResource(connection, ps, resultSet);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testQuery</span><span class="params">()</span></span>&#123;</span><br><span class="line">    String sql = <span class="string">"select id,name,birth,email from test.customers where id=?"</span>;</span><br><span class="line">    Customer customer = query(sql, <span class="number">1</span>);</span><br><span class="line">    System.out.println(customer);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中将结果写入一个customer对象,先用结果集元数据确定每一行有多少列,然后挨个处理,根据结果集元数据拿到了列名,然后用反射根据这个列名获取对应属性并赋值.但是当bean对象的属性名和列名不一致时就要为列起别名:在SQL语句中的每个字段后加别名.此外从元数据获取的也不能是列名了,而是别名,即getColumnLabel()方法.若是没有别名,该方法则会获取列名,故不管哪种情况都推荐使用getColumnLabel()方法.</p>
<p>下面演示了针对不同表的通用写法,主要是使用了泛型和反射:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> &lt;T&gt; <span class="function">T <span class="title">getInstance</span><span class="params">(Class&lt;T&gt; clazz, String sql, Object... args)</span> </span>&#123;</span><br><span class="line">   Connection connection = <span class="keyword">null</span>;</span><br><span class="line">   PreparedStatement ps = <span class="keyword">null</span>;</span><br><span class="line">   ResultSet resultSet = <span class="keyword">null</span>;</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">       connection = JDBCUtils.getConnection();</span><br><span class="line">       ps = connection.prepareStatement(sql);</span><br><span class="line">       <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; args.length; i++) &#123;</span><br><span class="line">           ps.setObject(i + <span class="number">1</span>, args[i]);</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       resultSet = ps.executeQuery();</span><br><span class="line">       ResultSetMetaData metaData = resultSet.getMetaData();<span class="comment">//获取结果集的元数据</span></span><br><span class="line">       <span class="keyword">int</span> columnCount = metaData.getColumnCount();</span><br><span class="line">       <span class="keyword">if</span> (resultSet.next()) &#123;</span><br><span class="line">           T t = clazz.getDeclaredConstructor().newInstance();</span><br><span class="line">           <span class="comment">//处理结果集中一行数据的每一列</span></span><br><span class="line">           <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; columnCount; i++) &#123;</span><br><span class="line">               Object columnVal = resultSet.getObject(i + <span class="number">1</span>);</span><br><span class="line">               <span class="comment">//获取列名</span></span><br><span class="line">               String columnName = metaData.getColumnLabel(i + <span class="number">1</span>);</span><br><span class="line">               <span class="comment">//给指定customer对象的对应属性赋值,通过反射,由列明找属性</span></span><br><span class="line">               Field field = clazz.getDeclaredField(columnName);</span><br><span class="line">               field.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">               field.set(t, columnVal);</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="keyword">return</span> t;</span><br><span class="line">       &#125;</span><br><span class="line">   &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException | NoSuchFieldException | IllegalAccessException | NoSuchMethodException | InstantiationException | InvocationTargetException e) &#123;</span><br><span class="line">       e.printStackTrace();</span><br><span class="line">   &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">       JDBCUtils.closeResource(connection, ps, resultSet);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>PreparedStatement比普通的Statement的优势不仅在于可以防止SQL注入,前者还可以操作BLOB数据(二进制数据,例如图片)和进行批量操作.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//像数据表插入blob数据</span></span><br><span class="line">   <span class="meta">@Test</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testInsert</span><span class="params">()</span> <span class="keyword">throws</span> SQLException, IOException, ClassNotFoundException </span>&#123;</span><br><span class="line">       Connection connection = JDBCUtils.getConnection();</span><br><span class="line">       String sql = <span class="string">"insert into test.customers(name, email, birth, photo) VALUES (?,?,?,?)"</span>;</span><br><span class="line">       PreparedStatement ps = connection.prepareStatement(sql);</span><br><span class="line"></span><br><span class="line">       ps.setString(<span class="number">1</span>, <span class="string">"my"</span>);</span><br><span class="line">       ps.setString(<span class="number">2</span>, <span class="string">"yl@outlook.com"</span>);</span><br><span class="line">       ps.setString(<span class="number">3</span>, <span class="string">"1998-7-14"</span>);</span><br><span class="line">       FileInputStream fis = <span class="keyword">new</span> FileInputStream(<span class="keyword">new</span> File(<span class="string">"F:\\迷路帖.png"</span>));</span><br><span class="line">       ps.setBlob(<span class="number">4</span>, fis);</span><br><span class="line"></span><br><span class="line">       ps.executeUpdate();</span><br><span class="line">       JDBCUtils.closeResource(connection, ps);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Test</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testQuery2</span><span class="params">()</span> <span class="keyword">throws</span> SQLException, IOException, ClassNotFoundException </span>&#123;</span><br><span class="line">       Connection connection = <span class="keyword">null</span>;</span><br><span class="line">       PreparedStatement ps = <span class="keyword">null</span>;</span><br><span class="line">       ResultSet resultSet = <span class="keyword">null</span>;</span><br><span class="line">       <span class="keyword">try</span> &#123;</span><br><span class="line">           connection = JDBCUtils.getConnection();</span><br><span class="line">           String sql = <span class="string">"select * from test.customers WHERE name=?;"</span>;</span><br><span class="line">           ps = connection.prepareStatement(sql);</span><br><span class="line">           ps.setString(<span class="number">1</span>, <span class="string">"my"</span>);</span><br><span class="line"></span><br><span class="line">           resultSet = ps.executeQuery();</span><br><span class="line">           <span class="keyword">if</span> (resultSet.next()) &#123;</span><br><span class="line">               <span class="keyword">int</span> id = resultSet.getInt(<span class="number">1</span>);</span><br><span class="line">               String name = resultSet.getString(<span class="number">2</span>);</span><br><span class="line">               String email = resultSet.getString(<span class="number">3</span>);</span><br><span class="line">               java.sql.Date birth = resultSet.getDate(<span class="string">"birth"</span>);<span class="comment">//还可以用列别名来获取值</span></span><br><span class="line"></span><br><span class="line">               Customer customer = <span class="keyword">new</span> Customer(id, name, email, birth);</span><br><span class="line">               System.out.println(customer);</span><br><span class="line">               Blob photo = resultSet.getBlob(<span class="string">"photo"</span>);</span><br><span class="line">               InputStream is = photo.getBinaryStream();</span><br><span class="line">               FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(<span class="string">"1.png"</span>);</span><br><span class="line">               <span class="keyword">byte</span>[] buffer = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">               <span class="keyword">int</span> len;</span><br><span class="line">               <span class="keyword">while</span> ((len = is.read(buffer)) != -<span class="number">1</span>) &#123;</span><br><span class="line">                   fos.write(buffer, <span class="number">0</span>, len);</span><br><span class="line">               &#125;</span><br><span class="line">               is.close();</span><br><span class="line">               fos.close();</span><br><span class="line">           &#125;</span><br><span class="line">       &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException e) &#123;</span><br><span class="line">           e.printStackTrace();</span><br><span class="line">       &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">           JDBCUtils.closeResource(connection, ps, resultSet);</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>对于blob文件,MySQL8默认的最大值是4M:<code>max_allowed_packet=4M</code>.</p>
<p>PreparedStatement性能也比普通语句高:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//对于删改查,本身就能批量处理数据,但是要批量增加数据就比较麻烦,除非使用存储过程</span></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Connection connection = <span class="keyword">null</span>;</span><br><span class="line">    PreparedStatement ps = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        connection = JDBCUtils.getConnection();</span><br><span class="line">        String sql = <span class="string">"insert into test.goods(NAME) values (?)"</span>;<span class="comment">//如果是使用Statement,SQL语句就要写在循环里,这样就会产生非常多的SQL对象</span></span><br><span class="line">        <span class="comment">//预编译语句会被缓存下来,每次调用重复的,只要填充占位符即可,而普通语句每次都要编译</span></span><br><span class="line">        ps = connection.prepareStatement(sql);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; <span class="number">2000</span>; i++) &#123;</span><br><span class="line">            ps.setString(<span class="number">1</span>, <span class="string">"name_"</span> + i);</span><br><span class="line">            ps.executeUpdate();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JDBCUtils.closeResource(connection, ps);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是这种方式还不够快,下面还有改进方法,使用批处理,这种方法尤其要注意插入语句的values不能写成value,前者比后者快几十倍.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//另一种批量操作方式</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Connection connection = <span class="keyword">null</span>;</span><br><span class="line">        PreparedStatement ps = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">long</span> start = System.currentTimeMillis();</span><br><span class="line">            connection = JDBCUtils.getConnection();</span><br><span class="line">            String sql = <span class="string">"insert into test.goods(NAME) values (?)"</span>;<span class="comment">//如果是使用Statement,SQL语句就要写在循环里,这样就会产生非常多的SQL对象</span></span><br><span class="line">            <span class="comment">//预编译语句会被缓存下来,每次调用重复的,只要填充占位符即可,而普通语句每次都要编译</span></span><br><span class="line">            ps = connection.prepareStatement(sql);</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">8000</span>; i++) &#123;</span><br><span class="line">                ps.setString(<span class="number">1</span>, <span class="string">"name_"</span> + i);</span><br><span class="line">                <span class="comment">//默认Mysql是不支持批处理的,需要在url加上rewriteBatchedStatements=true</span></span><br><span class="line">                <span class="comment">//"攒"SQL</span></span><br><span class="line">                ps.addBatch();</span><br><span class="line">                <span class="keyword">if</span> (i % <span class="number">100</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="comment">//执行batch</span></span><br><span class="line">                    ps.executeBatch();</span><br><span class="line">                    <span class="comment">//清空batch</span></span><br><span class="line">                    ps.clearBatch();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">long</span> end = System.currentTimeMillis();</span><br><span class="line">            System.out.println(end - start);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            JDBCUtils.closeResource(connection, ps);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>在此基础上,关闭自动提交,避免每次executeBatch都向MySQL写入数据.让它最后一次性提交:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//更快的方式</span></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test3</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Connection connection = <span class="keyword">null</span>;</span><br><span class="line">    PreparedStatement ps = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">long</span> start = System.currentTimeMillis();</span><br><span class="line">        connection = JDBCUtils.getConnection();</span><br><span class="line">        <span class="comment">//设置不允许自动提交</span></span><br><span class="line">        connection.setAutoCommit(<span class="keyword">false</span>);</span><br><span class="line">        String sql = <span class="string">"insert into test.goods(NAME) values (?)"</span>;<span class="comment">//如果是使用Statement,SQL语句就要写在循环里,这样就会产生非常多的SQL对象</span></span><br><span class="line">        <span class="comment">//预编译语句会被缓存下来,每次调用重复的,只要填充占位符即可,而普通语句每次都要编译</span></span><br><span class="line">        ps = connection.prepareStatement(sql);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">8000</span>; i++) &#123;</span><br><span class="line">            ps.setString(<span class="number">1</span>, <span class="string">"name_"</span> + i);</span><br><span class="line">            <span class="comment">//默认Mysql是不支持批处理的,需要在url加上rewriteBatchedStatements=true</span></span><br><span class="line">            <span class="comment">//"攒"SQL</span></span><br><span class="line">            ps.addBatch();</span><br><span class="line">            <span class="keyword">if</span> (i % <span class="number">100</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">//执行batch</span></span><br><span class="line">                ps.executeBatch();</span><br><span class="line">                <span class="comment">//清空batch</span></span><br><span class="line">                ps.clearBatch();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        connection.commit();</span><br><span class="line">        <span class="keyword">long</span> end = System.currentTimeMillis();</span><br><span class="line">        System.out.println(end - start);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JDBCUtils.closeResource(connection, ps);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关闭自动提交还用于MySQL的事务:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">updateWithTransaction</span><span class="params">(Connection connection, String sql, Object @NotNull ... args)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//将连接作为参数传进来,是的多个DML语句共用一个连接</span></span><br><span class="line">    PreparedStatement ps = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        ps = connection.prepareStatement(sql);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; args.length; i++) &#123;</span><br><span class="line">            ps.setObject(i + <span class="number">1</span>, args[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        ps.execute();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JDBCUtils.closeResource(<span class="keyword">null</span>, ps);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testUpdateWithTx</span><span class="params">()</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">    Connection connection = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        connection = JDBCUtils.getConnection();</span><br><span class="line">        connection.setAutoCommit(<span class="keyword">false</span>);</span><br><span class="line">        String sql1 = <span class="string">"update test.user_table set balance = balance - 100 where user = ?"</span>;</span><br><span class="line">        updateWithTransaction(connection, sql1, <span class="string">"AA"</span>);</span><br><span class="line">        <span class="comment">//中间出现异常</span></span><br><span class="line">        System.out.println(<span class="number">10</span> / <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">        String sql2 = <span class="string">"update test.user_table set balance = balance + 100 where user = ?"</span>;</span><br><span class="line">        updateWithTransaction(connection, sql2, <span class="string">"BB"</span>);</span><br><span class="line">        System.out.println(<span class="string">"提交成功"</span>);</span><br><span class="line">        connection.commit();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException | SQLException e) &#123;</span><br><span class="line">        <span class="comment">//回滚数据</span></span><br><span class="line">        <span class="keyword">assert</span> connection != <span class="keyword">null</span>;</span><br><span class="line">        connection.rollback();</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">assert</span> connection != <span class="keyword">null</span>;</span><br><span class="line">        connection.setAutoCommit(<span class="keyword">true</span>);<span class="comment">//将自动提交改回默认状态,在数据库连接池中可能会影响别人</span></span><br><span class="line">        JDBCUtils.closeResource(connection, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在MySQL中可以设置隔离级别,应对脏读,不可重复读,幻读问题:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">set</span> <span class="keyword">session</span> <span class="keyword">transaction</span> <span class="keyword">isolation</span> <span class="keyword">level</span> <span class="keyword">serializable</span>;<span class="comment">#设置当前会话的隔离级别</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">set</span> <span class="keyword">global</span> <span class="keyword">transaction</span> <span class="keyword">isolation</span> <span class="keyword">level</span> <span class="keyword">read</span> committed;<span class="comment">#设置全局隔离级别</span></span><br></pre></td></tr></table></figure>

<p>在Java中也可以设置:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> transactionIsolation = connection.getTransactionIsolation();<span class="comment">//获取隔离级别</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>int TRANSACTION_NONE             = 0;//不支持事务<br>int TRANSACTION_READ_UNCOMMITTED = 1;//允许读未提交数据<br>int TRANSACTION_READ_COMMITTED   = 2;//允许事务读取已经提交的变更,可避免脏读<br>int TRANSACTION_REPEATABLE_READ  = 4;//REPEATABLE READ:确保事务多次从同一个字段读取出相同的值,在这个事务期间禁止其他事务修改该字段,可以避免脏读和不可重复读(默认级别)<br>int TRANSACTION_SERIALIZABLE     = 8;//类似于Java中的加锁,可以避免所有并发问题,但性能较低</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">connection.setTransactionIsolation(Connection.TRANSACTION_SERIALIZABLE);<span class="comment">//设置隔离级别</span></span><br></pre></td></tr></table></figure>

<p>对于数据库操作,一般是以下逻辑:</p>
<blockquote>
<p>xxxBean.java//定义数据对象<br>BaseDAO.java//定义基本的通用的增删改查数据库操作<br>xxxDAO.java//该接口针对特定的bean定义数据库操作规范<br>xxxDaoImpl.java//实现xxxDAO接口,并继承BaseDAO,利用通用的增删改查操作实现对特定对象的数据库操作</p>
</blockquote>
<p>BaseDAO中提供了增删改查等操作的关键代码,但是真正进行增删改查操作的是在xxxDaoImpl中,在这里传入具体的SQL语句给继承来的insert,delete等方法.</p>
<p>数据库连接池技术:</p>
<p>C3P0连接池:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">    ComboPooledDataSource cpds = <span class="keyword">new</span> ComboPooledDataSource(<span class="string">"hello"</span>);<span class="comment">//通过配置文件获得连接池</span></span><br><span class="line">    Connection connection = cpds.getConnection();<span class="comment">//通过连接池获得一个连接</span></span><br><span class="line">    System.out.println(connection);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>需要一个c3p0的xml配置文件,命名为c3p0-config.xml,其中<code>&lt;named-config name=&quot;hello&quot;&gt;</code>表明配置名称.</p>
<p>dbcp连接池:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    Properties properties = <span class="keyword">new</span> Properties();</span><br><span class="line">    InputStream is = ClassLoader.getSystemClassLoader().getResourceAsStream(<span class="string">"dbcp.properties"</span>);</span><br><span class="line">    properties.load(is);</span><br><span class="line">    BasicDataSource source = BasicDataSourceFactory.createDataSource(properties);</span><br><span class="line">    Connection connection = source.getConnection();</span><br><span class="line">    System.out.println(connection);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>druid连接池:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">getConnection</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    Properties properties = <span class="keyword">new</span> Properties();</span><br><span class="line">    InputStream is = ClassLoader.getSystemClassLoader().getResourceAsStream(<span class="string">"druid.properties"</span>);</span><br><span class="line">    properties.load(is);</span><br><span class="line">    DataSource source = DruidDataSourceFactory.createDataSource(properties);</span><br><span class="line">    Connection connection = source.getConnection();</span><br><span class="line">    System.out.println(connection);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于一些常用的增删改查操作,其实也有第三方包实现,下面使用Apache-DBUtils:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testInsert</span><span class="params">()</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">    Connection connection = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        QueryRunner runner = <span class="keyword">new</span> QueryRunner();</span><br><span class="line">        connection = JDBCUtils.getConnection3();</span><br><span class="line">        String sql = <span class="string">"insert into test.customers(name,email,birth) values(?,?,?)"</span>;</span><br><span class="line">        <span class="keyword">int</span> count = runner.update(connection, sql, <span class="string">"蔡徐坤"</span>, <span class="string">"cxk@gmail.com"</span>, <span class="string">"1997-08-14"</span>);</span><br><span class="line">        System.out.println(count);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JDBCUtils.closeResource(connection, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>插入数据比较简单,但是对于查询数据,需要做一些特别的处理:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">QueryTest1</span><span class="params">()</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">    Connection connection = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        QueryRunner runner = <span class="keyword">new</span> QueryRunner();</span><br><span class="line">        connection = JDBCUtils.getConnection3();</span><br><span class="line">        String sql = <span class="string">"select id,name,email,birth from test.customers where id = ?"</span>;</span><br><span class="line">        BeanHandler&lt;Customer&gt; handler = <span class="keyword">new</span> BeanHandler&lt;Customer&gt;(Customer<span class="class">.<span class="keyword">class</span>)</span>;<span class="comment">//BeanHandler是ResultSetHandler的实现类,该接口有多种实现类,</span></span><br><span class="line">        <span class="comment">//用于对应处理各种类型的结果集.BeanHandler用于封装一条记录</span></span><br><span class="line">        Customer customer = runner.query(connection, sql, handler, <span class="number">10</span>);</span><br><span class="line">        System.out.println(customer);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JDBCUtils.closeResource(connection, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">QueryTest2</span><span class="params">()</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">    Connection connection = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        QueryRunner runner = <span class="keyword">new</span> QueryRunner();</span><br><span class="line">        connection = JDBCUtils.getConnection3();</span><br><span class="line">        String sql = <span class="string">"select id,name,email,birth from test.customers where id &lt; ?"</span>;</span><br><span class="line">        BeanListHandler&lt;Customer&gt; handler = <span class="keyword">new</span> BeanListHandler&lt;Customer&gt;(Customer<span class="class">.<span class="keyword">class</span>)</span>;<span class="comment">//BeanHandler是ResultSetHandler的实现类,该接口有多种实现类,</span></span><br><span class="line">        <span class="comment">//用于对应处理各种类型的结果集.BeanHandler用于封装一条记录</span></span><br><span class="line">        List&lt;Customer&gt; customers = runner.query(connection, sql, handler, <span class="number">10</span>);</span><br><span class="line">        customers.forEach(System.out::println);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JDBCUtils.closeResource(connection, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">QueryTest3</span><span class="params">()</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">    Connection connection = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        QueryRunner runner = <span class="keyword">new</span> QueryRunner();</span><br><span class="line">        connection = JDBCUtils.getConnection3();</span><br><span class="line">        String sql = <span class="string">"select max(birth) from test.customers "</span>;</span><br><span class="line">        ScalarHandler&lt;Date&gt; handler = <span class="keyword">new</span> ScalarHandler&lt;&gt;();<span class="comment">//用ScalaHandler处理返回的特殊值</span></span><br><span class="line">        Date birth = runner.query(connection, sql, handler);</span><br><span class="line">        System.out.println(birth);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JDBCUtils.closeResource(connection, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>主要是要注意ResultSetHandler的选择.还可以自定义Handler:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span><span class="comment">//自定义ResultSetHandler实现类</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">QueryTest4</span><span class="params">()</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">    Connection connection = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        QueryRunner runner = <span class="keyword">new</span> QueryRunner();</span><br><span class="line">        connection = JDBCUtils.getConnection3();</span><br><span class="line">        String sql = <span class="string">"select id,name,email,birth from test.customers where id = ?"</span>;</span><br><span class="line">        ResultSetHandler&lt;Customer&gt; handler = <span class="keyword">new</span> ResultSetHandler&lt;&gt;() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> Customer <span class="title">handle</span><span class="params">(ResultSet resultSet)</span> <span class="keyword">throws</span> SQLException </span>&#123;<span class="comment">//传入了结果集</span></span><br><span class="line">                <span class="keyword">if</span> (resultSet.next()) &#123;</span><br><span class="line">                    <span class="keyword">int</span> id = resultSet.getInt(<span class="string">"id"</span>);</span><br><span class="line">                    String name = resultSet.getString(<span class="string">"name"</span>);</span><br><span class="line">                    String email = resultSet.getString(<span class="string">"email"</span>);</span><br><span class="line">                    java.sql.Date birth = resultSet.getDate(<span class="string">"birth"</span>);</span><br><span class="line">                    <span class="keyword">return</span> <span class="keyword">new</span> Customer(id, name, email, birth);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        Customer customer = runner.query(connection, sql, handler, <span class="number">23</span>);</span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JDBCUtils.closeResource(connection, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Cpp结构体</title>
    <url>/2018/08/02/Cpp%E7%BB%93%E6%9E%84%E4%BD%93/</url>
    <content><![CDATA[<p>补充一些结构体的知识.</p>
<a id="more"></a>

<h1 id="1-结构体-struct"><a href="#1-结构体-struct" class="headerlink" title="1. 结构体(struct)"></a>1. 结构体(struct)</h1><h3 id="1-1-结构体的概念"><a href="#1-1-结构体的概念" class="headerlink" title="1.1 结构体的概念"></a>1.1 结构体的概念</h3><ul>
<li><strong>结构体(struct)：</strong>是由一系列具有相同类型或不同类型的数据构成的数据集合，叫做结构。</li>
<li><strong>结构体(struct)：</strong>是一种复合数据类型，结构类型。<br><strong>注：“结构”是一种构造类型，它是由若干“成员”组成的。 每一个成员可以是一个基本数据类型或者又是一个构造类型。 结构即是一种“构造”而成的数据类型， 那么在说明和使用之前必须先定义它，也就是构造它。如同在说明和调用函数之前要先定义一样。</strong></li>
</ul>
<h3 id="1-2-C语言中的结构体"><a href="#1-2-C语言中的结构体" class="headerlink" title="1.2 C语言中的结构体"></a>1.2 C语言中的结构体</h3><ul>
<li><p><strong>说明：</strong>在C语言中，结构体(struct)是复合数据类型的一种。同时也是一些元素的集合，这些元素称为结构体的成员，且这些成员可以为不同的类型，成员一般用名字访问。结构体可以被声明为变量、指针或数组等，用以实现较复杂的数据结构。<br><strong>注：在C语言中，结构体不能包含函数。</strong></p>
</li>
<li><p><strong>定义与声明：</strong><br>(1)示例代码一：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">tag</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    member-<span class="built_in">list</span></span><br><span class="line">&#125;variable-<span class="built_in">list</span>;</span><br><span class="line">注：<span class="class"><span class="keyword">struct</span>为结构体关键字；</span></span><br><span class="line"><span class="class">   <span class="title">tag</span>为结构体的标志；</span></span><br><span class="line"><span class="class">   <span class="title">member</span>-<span class="title">list</span>为结构体成员变量列表，其必须列出其所有成员；</span></span><br><span class="line"><span class="class">   <span class="title">variable</span>-<span class="title">list</span>为此结构体声明的变量；</span></span><br></pre></td></tr></table></figure>

<p>(2)示例代码二：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//此声明声明了拥有3个成员的结构体，分别为整型的a，字符型的b和双精度的c，但没有标明其标签，声明了结构体变量s1</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> a;</span><br><span class="line">    <span class="keyword">char</span> b;</span><br><span class="line">    <span class="keyword">double</span> c;</span><br><span class="line">&#125; s1;</span><br><span class="line"></span><br><span class="line"><span class="comment">//此声明声明了拥有3个成员的结构体，分别为整型的a，字符型的b和双精度的c，结构体的标签被命名为SIMPLE，用SIMPLE标签的结构体，另外声明了变量t1, t2[20], *t3</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">SIMPLE</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> a;</span><br><span class="line">    <span class="keyword">char</span> b;</span><br><span class="line">    <span class="keyword">double</span> c;</span><br><span class="line">&#125;;</span><br><span class="line">SIMPLE t1, t2[<span class="number">20</span>], *t3; </span><br><span class="line"></span><br><span class="line"><span class="comment">//可以用typedef创建新类型，此声明声明了拥有3个成员的结构体，分别为整型的a，字符型的b和双精度的c，结构体的标签被命名为Simple2，用Simple2作为类型声明新的结构体变量u1, u2[20], *u3</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> a;</span><br><span class="line">    <span class="keyword">char</span> b;</span><br><span class="line">    <span class="keyword">double</span> c; </span><br><span class="line">&#125; Simple2;</span><br><span class="line">Simple2 u1, u2[<span class="number">20</span>], *u3;<span class="comment">//若去掉typedef则编译报错，error C2371: “Simple2”: 重定义；不同的基类型</span></span><br><span class="line"></span><br><span class="line">注：在上面的声明中，第一个和第二声明被编译器当作两个完全不同的类型，即使他们的成员列表是一样的，如果令t3=&amp;s1，则是非法的。</span><br></pre></td></tr></table></figure>

<p><strong>注：在一般情况下，tag、member-list、variable-list这3部分至少要出现2个。</strong></p>
</li>
</ul>
<h3 id="1-3-C-中的结构体"><a href="#1-3-C-中的结构体" class="headerlink" title="1.3 C++中的结构体"></a>1.3 C++中的结构体</h3><ul>
<li><p><strong>说明：</strong>在C语言中，结构体不能包含函数。在面向对象的程序设计中，对象具有状态（属性）和行为，状态保存在成员变量中，行为通过成员方法（函数）来实现。C语言中的结构体只能描述一个对象的状态，不能描述一个对象的行为。在C++中，考虑到C语言到C++语言过渡的连续性，对结构体进行了扩展，C++的结构体可以包含函数，这样，C++的结构体也具有类的功能，与class不同的是，结构体包含的函数默认为public，而不是private。<br><strong>注：在C++中，结构体可以包含函数。</strong></p>
</li>
<li><p><strong>定义与声明：</strong><br>(1)示例代码一：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">tag</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    member-<span class="built_in">list</span></span><br><span class="line">&#125;variable-<span class="built_in">list</span>;</span><br><span class="line">注：<span class="class"><span class="keyword">struct</span>为结构体关键字；</span></span><br><span class="line"><span class="class">   <span class="title">tag</span>为结构体的标志；</span></span><br><span class="line"><span class="class">   <span class="title">member</span>-<span class="title">list</span>为结构体成员变量及成员函数列表，其必须列出其所有成员；</span></span><br><span class="line"><span class="class">   <span class="title">variable</span>-<span class="title">list</span>为此结构体声明的变量；</span></span><br></pre></td></tr></table></figure>

<p>(2)示例代码二：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt; </span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">SAMPLE</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> x;</span><br><span class="line">    <span class="keyword">int</span> y;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">add</span><span class="params">()</span> </span>&#123;<span class="keyword">return</span> x+y;&#125;</span><br><span class="line">&#125;s1;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"没初始化成员变量的情况下："</span>&lt;&lt;s1.add()&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">    s1.x = <span class="number">3</span>;</span><br><span class="line">    s1.y = <span class="number">4</span>;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"初始化成员变量的情况下："</span>&lt;&lt;s1.add()&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">    system(<span class="string">"pause"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line">=&gt;没初始化成员变量的情况下：<span class="number">0</span></span><br><span class="line">  初始化成员变量的情况下：<span class="number">7</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>C++中的结构体与类的区别：</strong><br><strong>(1)class中默认的成员访问权限是private的，而struct中则是public的。</strong><br><strong>(2)class继承默认是private继承，而从struct继承默认是public继承。</strong></p>
</li>
</ul>
<h3 id="1-4-结构体的作用"><a href="#1-4-结构体的作用" class="headerlink" title="1.4 结构体的作用"></a>1.4 结构体的作用</h3><ul>
<li>在实际项目中，结构体是大量存在的。研发人员常使用结构体来封装一些属性来组成新的类型。由于C语言内部程序比较简单，研发人员通常使用结构体创造新的“属性”，其目的是简化运算。</li>
<li>结构体在函数中的作用不是简便，<strong>最主要的作用就是封装。</strong>封装的好处就是可以再次利用。让使用者不必关心这个是什么，只要根据定义使用就可以了。</li>
</ul>
<h3 id="1-5-结构体的大小与内存对齐"><a href="#1-5-结构体的大小与内存对齐" class="headerlink" title="1.5 结构体的大小与内存对齐"></a>1.5 结构体的大小与内存对齐</h3><ul>
<li><p><strong>默认的对齐方式：</strong>各成员变量在存放的时候根据在结构中出现的顺序依次申请空间，同时按照上面的对齐方式调整位置，空缺的字节VC会自动填充。同时VC为了确保结构的大小为结构的字节边界数（即该结构中占用最大空间的类型所占用的字节数）的倍数，所以在为最后一个成员变量申请空间后，还会根据需要自动填充空缺的字节。<br><strong>注：VC对变量存储的一个特殊处理。为了提高CPU的存储速度，VC对一些变量的起始地址做了“对齐”处理。在默认情况下，VC规定各成员变量存放的起始地址相对于结构的起始地址的偏移量必须为该变量的类型所占用的字节数的倍数。</strong><br>(1)示例代码一：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">MyStruct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">double</span> dda1;</span><br><span class="line">    <span class="keyword">char</span> dda;</span><br><span class="line">    <span class="keyword">int</span> type;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//错：sizeof(MyStruct)=sizeof(double)+sizeof(char)+sizeof(int)=13。</span></span><br><span class="line"><span class="comment">//对：当在VC中测试上面结构的大小时，你会发现sizeof(MyStruct)为16。</span></span><br></pre></td></tr></table></figure>

<p><strong>注：为上面的结构分配空间的时候，VC根据成员变量出现的顺序和对齐方式。</strong><br><strong>(1)先为第一个成员dda1分配空间，其起始地址跟结构的起始地址相同（刚好偏移量0刚好为sizeof(double)的倍数），该成员变量占用sizeof(double)=8个字节；</strong><br><strong>(2)接下来为第二个成员dda分配空间，这时下一个可以分配的地址对于结构的起始地址的偏移量为8，是sizeof(char)的倍数，所以把dda存放在偏移量为8的地方满足对齐方式，该成员变量占用sizeof(char)=1个字节；</strong><br><strong>(3)接下来为第三个成员type分配空间，这时下一个可以分配的地址对于结构的起始地址的偏移量为9，不是sizeof(int)=4的倍数，为了满足对齐方式对偏移量的约束问题，VC自动填充3个字节（这三个字节没有放什么东西），这时下一个可以分配的地址对于结构的起始地址的偏移量为12，刚好是sizeof(int)=4的倍数，所以把type存放在偏移量为12的地方，该成员变量占用sizeof(int)=4个字节；</strong><br><strong>这时整个结构的成员变量已经都分配了空间，总的占用的空间大小为：8+1+3+4=16，刚好为结构的字节边界数（即结构中占用最大空间的类型所占用的字节数sizeof(double)=8）的倍数，所以没有空缺的字节需要填充。所以整个结构的大小为：sizeof(MyStruct)=8+1+3+4=16，其中有3个字节是VC自动填充的，没有放任何有意义的东西。</strong><br>(2)示例代码二：交换一下上述例子中MyStruct的成员变量的位置</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">MyStruct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">char</span> dda;</span><br><span class="line">    <span class="keyword">double</span> dda1;</span><br><span class="line">    <span class="keyword">int</span> type;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//错：sizeof(MyStruct)=sizeof(double)+sizeof(char)+sizeof(int)=13。</span></span><br><span class="line"><span class="comment">//对：当在VC中测试上面结构的大小时，你会发现sizeof(MyStruct)为24。</span></span><br></pre></td></tr></table></figure>

<p><strong>注：为上面的结构分配空间的时候，VC根据成员变量出现的顺序和对齐方式。</strong><br><strong>(1)先为第一个成员dda分配空间，其起始地址跟结构的起始地址相同（刚好偏移量0刚好为sizeof(char)的倍数），该成员变量占用sizeof(char)=1个字节；</strong><br><strong>(2)接下来为第二个成员dda1分配空间，这时下一个可以分配的地址对于结构的起始地址的偏移量为1，不是sizeof(double)=8的倍数，需要补足7个字节才能使偏移量变为8（满足对齐方式），因此VC自动填充7个字节，dda1存放在偏移量为8的地址上，它占用8个字节；</strong><br><strong>(3)接下来为第三个成员type分配空间，这时下一个可以分配的地址对于结构的起始地址的偏移量为16，是sizeof(int)=4的倍数，满足int的对齐方式，所以不需要VC自动填充，type存放在偏移量为16的地址上，该成员变量占用sizeof(int)=4个字节；</strong><br><strong>这时整个结构的成员变量已经都分配了空间，总的占用的空间大小为：1+7+8+4=20，不是结构的节边界数（即结构中占用最大空间的类型所占用的字节数sizeof(double)=8）的倍数，所以需要填充4个字节，以满足结构的大小为sizeof(double)=8的倍数。所以该结构总的大小为：sizeof(MyStruct)为1+7+8+4+4=24。其中总的有7+4=11个字节是VC自动填充的，没有放任何有意义的东西。</strong></p>
</li>
<li><p><strong>字节的对齐方式：</strong>在VC中提供了#pragmapack(n)来设定变量以n字节对齐方式。n字节对齐就是说变量存放的起始地址的偏移量有两种情况：第一，如果n大于等于该变量所占用的字节数，那么偏移量必须满足默认的对齐方式；第二，如果n小于该变量的类型所占用的字节数，那么偏移量为n的倍数，不用满足默认的对齐方式。结构的总大小也有个约束条件，分下面两种情况：如果n大于所有成员变量类型所占用的字节数，那么结构的总大小必须为占用空间最大的变量占用的空间数的倍数；否则必须为n的倍数。<br><strong>注：VC对结构的存储的特殊处理确实提高了CPU存储变量的速度，但有时也会带来一些麻烦，我们也可以屏蔽掉变量默认的对齐方式，自己来设定变量的对齐方式。</strong><br>(1)示例代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#pragmapack(push)<span class="comment">//保存对齐状态</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">#pragmapack(4)<span class="comment">//设定为4字节对齐</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">test</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">char</span> m1;</span><br><span class="line">    <span class="keyword">double</span> m4;</span><br><span class="line">    <span class="keyword">int</span> m3;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#pragmapack(pop)<span class="comment">//恢复对齐状态</span></span></span><br></pre></td></tr></table></figure>

<p><strong>注：以上结构的大小为16，下面分析其存储情况。</strong><br><strong>(1)首先为m1分配空间，其偏移量为0，满足我们自己设定的对齐方式（4字节对齐），m1占用1个字节；</strong><br><strong>(2)接着开始为m4分配空间，这时其偏移量为1，需要补足3个字节，这样使偏移量满足为n=4的倍数（因为sizeof(double)大于n）,m4占用8个字节；</strong><br><strong>(3)接着为m3分配空间，这时其偏移量为12，满足为4的倍数，m3占用4个字节；</strong><br><strong>这时已经为所有成员变量分配了空间，共分配了16个字节，满足为n的倍数。如果把上面的#pragmapack(4)改为#pragma pack(8)，那么我们可以得到结构的大小为24。</strong></p>
<p><strong>结构指针</strong></p>
</li>
</ul>
<p>结构指针是指向结构的指针。它由一个加在结构变量名前的* 操作符来定义, 例如用前面已说明的结构定义一个结构指针如下:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">string</span></span></span><br><span class="line"><span class="class">&#123;</span> </span><br><span class="line">    <span class="keyword">char</span> name[<span class="number">8</span>]; </span><br><span class="line">    <span class="keyword">char</span> sex[<span class="number">4</span>]; </span><br><span class="line">    <span class="keyword">int</span> age; </span><br><span class="line">    <span class="keyword">char</span> addr[<span class="number">40</span>]; </span><br><span class="line">&#125;*student;</span><br></pre></td></tr></table></figure>

<p>也可省略结构指针名只作结构说明, 然后再用下面的语句定义结构指针。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">string</span> *<span class="title">student</span>;</span></span><br></pre></td></tr></table></figure>

<p>使用结构指针对结构成员的访问，与结构变量对结构成员的访问在表达方式上有所不同。结构指针对结构成员的访问表示为:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">结构指针名-&gt;结构成员</span><br></pre></td></tr></table></figure>

<p>其中 -&gt; 是两个符号 - 和 &gt; 的组合，好象一个箭头指向结构成员。例如要给上面定义的结构中 name 和 age 赋值，可以用下面语句:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">strcpy</span>(student-&gt;name, Lu G.C); </span><br><span class="line">student-&gt;age=<span class="number">18</span>;</span><br></pre></td></tr></table></figure>

<p>实际上，<strong>student-&gt;name</strong> 就是 <strong>(*student).name</strong> 的缩写形式。</p>
<p>需要指出的是结构指针是指向结构的一个指针，即结构中第一个成员的首地址，因此在使用之前应该对结构指针初始化，即分配整个结构长度的字节空间，这可用下面函数完成， 仍以上例来说明如下:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">student=(struct <span class="built_in">string</span>*)<span class="built_in">malloc</span>(size of (struct <span class="built_in">string</span>));</span><br></pre></td></tr></table></figure>

<p><strong>size of (struct string)</strong> 自动求取 string 结构的字节长度， malloc() 函数定义了一个大小为结构长度的内存区域，然后将其诈地址作为结构指针返回。</p>
<p><strong>注意:</strong></p>
<ul>
<li>结构作为一种数据类型，因此定义的结构变量或结构指针变量同样有局部变量和全程变量，视定义的位置而定。</li>
<li>结构变量名不是指向该结构的地址，这与数组名的含义不同，因此若需要求结构中第一个成员的首地址应该是 &amp;[结构变量名]。</li>
<li>结构的复杂形式：嵌套结构</li>
</ul>
<p>嵌套结构是指在一个结构成员中可以包括其它一个结构，Turbo C 允许这种嵌套。</p>
<p>例如: 下面是一个有嵌套的结构</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">string</span></span></span><br><span class="line"><span class="class">&#123;</span> </span><br><span class="line">    <span class="keyword">char</span> name[<span class="number">8</span>]; </span><br><span class="line">    <span class="keyword">int</span> age; </span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addr</span> <span class="title">address</span>;</span> </span><br><span class="line">&#125; student;</span><br></pre></td></tr></table></figure>

<p>其中: addr 为另一个结构的结构名, 必须要先进行, 说明, 即</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addr</span></span></span><br><span class="line"><span class="class">&#123;</span> </span><br><span class="line">    <span class="keyword">char</span> city[<span class="number">20</span>]; </span><br><span class="line">    <span class="keyword">unsigned</span> lon zipcode; </span><br><span class="line">    <span class="keyword">char</span> tel[<span class="number">14</span>]; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果要给 student 结构中成员 address 结构中的 zipcode 赋值, 则可写成:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">student.address.zipcode=<span class="number">200001</span>;</span><br></pre></td></tr></table></figure>

<p>每个结构成员名从最外层直到最内层逐个被列出,即嵌套式结构成员的表达方式是:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">结构变量名.嵌套结构变量名.结构成员名</span><br></pre></td></tr></table></figure>

<p>其中嵌套结构可以有很多，结构成员名为最内层结构中不是结构的成员名。</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>Cpp继承</title>
    <url>/2018/08/02/Cpp%E7%BB%A7%E6%89%BF/</url>
    <content><![CDATA[<p>看看C++中的继承有什么与java不一样的.</p>
<a id="more"></a>

<h1 id="C-继承"><a href="#C-继承" class="headerlink" title="C++ 继承"></a>C++ 继承</h1><p>面向对象程序设计中最重要的一个概念是继承。继承允许我们依据另一个类来定义一个类，这使得创建和维护一个应用程序变得更容易。这样做，也达到了重用代码功能和提高执行时间的效果。</p>
<p>当创建一个类时，您不需要重新编写新的数据成员和成员函数，只需指定新建的类继承了一个已有的类的成员即可。这个已有的类称为<strong>基类</strong>，新建的类称为<strong>派生类</strong>。</p>
<p>继承代表了 <strong>is a</strong> 关系。例如，哺乳动物是动物，狗是哺乳动物，因此，狗是动物，等等。</p>
<h2 id="基类-amp-派生类"><a href="#基类-amp-派生类" class="headerlink" title="基类 &amp; 派生类"></a>基类 &amp; 派生类</h2><p>一个类可以派生自多个类，这意味着，它可以从多个基类继承数据和函数。定义一个派生类，我们使用一个类派生列表来指定基类。类派生列表以一个或多个基类命名，形式如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">derived</span>-<span class="title">class</span>:</span> access-specifier base-<span class="class"><span class="keyword">class</span></span></span><br></pre></td></tr></table></figure>

<p>其中，访问修饰符 access-specifier 是 <strong>public、protected</strong> 或 <strong>private</strong> 其中的一个，base-class 是之前定义过的某个类的名称。如果未使用访问修饰符 access-specifier，则默认为 private。</p>
<p>假设有一个基类 <strong>Shape</strong>，<strong>Rectangle</strong> 是它的派生类，如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 基类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Shape</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setWidth</span><span class="params">(<span class="keyword">int</span> w)</span></span></span><br><span class="line"><span class="function">      </span>&#123;</span><br><span class="line">         width = w;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setHeight</span><span class="params">(<span class="keyword">int</span> h)</span></span></span><br><span class="line"><span class="function">      </span>&#123;</span><br><span class="line">         height = h;</span><br><span class="line">      &#125;</span><br><span class="line">   <span class="keyword">protected</span>:</span><br><span class="line">      <span class="keyword">int</span> width;</span><br><span class="line">      <span class="keyword">int</span> height;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 派生类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rectangle</span>:</span> <span class="keyword">public</span> Shape</span><br><span class="line">&#123;</span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="function"><span class="keyword">int</span> <span class="title">getArea</span><span class="params">()</span></span></span><br><span class="line"><span class="function">      </span>&#123; </span><br><span class="line">         <span class="keyword">return</span> (width * height); </span><br><span class="line">      &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   Rectangle Rect;</span><br><span class="line"> </span><br><span class="line">   Rect.setWidth(<span class="number">5</span>);</span><br><span class="line">   Rect.setHeight(<span class="number">7</span>);</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 输出对象的面积</span></span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Total area: "</span> &lt;&lt; Rect.getArea() &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="访问控制和继承"><a href="#访问控制和继承" class="headerlink" title="访问控制和继承"></a>访问控制和继承</h2><p>派生类可以访问基类中所有的非私有成员。因此基类成员如果不想被派生类的成员函数访问，则应在基类中声明为 private。</p>
<p>我们可以根据访问权限总结出不同的访问类型，如下所示：</p>
<table>
<thead>
<tr>
<th>访问</th>
<th>public</th>
<th>protected</th>
<th>private</th>
</tr>
</thead>
<tbody><tr>
<td>同一个类</td>
<td>yes</td>
<td>yes</td>
<td>yes</td>
</tr>
<tr>
<td>派生类</td>
<td>yes</td>
<td>yes</td>
<td>no</td>
</tr>
<tr>
<td>外部的类</td>
<td>yes</td>
<td>no</td>
<td>no</td>
</tr>
</tbody></table>
<p>一个派生类继承了所有的基类方法，但下列情况除外：</p>
<ul>
<li>基类的构造函数、析构函数和拷贝构造函数。</li>
<li>基类的重载运算符。</li>
<li>基类的友元函数。</li>
</ul>
<h2 id="继承类型"><a href="#继承类型" class="headerlink" title="继承类型"></a>继承类型</h2><p>当一个类派生自基类，该基类可以被继承为 <strong>public、protected</strong> 或 <strong>private</strong> 几种类型。继承类型是通过上面讲解的访问修饰符 access-specifier 来指定的。</p>
<p>我们几乎不使用 <strong>protected</strong> 或 <strong>private</strong> 继承，通常使用 <strong>public</strong> 继承。当使用不同类型的继承时，遵循以下几个规则：</p>
<ul>
<li><strong>公有继承（public）：</strong>当一个类派生自<strong>公有</strong>基类时，基类的<strong>公有</strong>成员也是派生类的<strong>公有</strong>成员，基类的<strong>保护</strong>成员也是派生类的<strong>保护</strong>成员，基类的<strong>私有</strong>成员不能直接被派生类访问，但是可以通过调用基类的<strong>公有</strong>和<strong>保护</strong>成员来访问。</li>
<li><strong>保护继承（protected）：</strong> 当一个类派生自<strong>保护</strong>基类时，基类的<strong>公有</strong>和<strong>保护</strong>成员将成为派生类的<strong>保护</strong>成员。</li>
<li><strong>私有继承（private）：</strong>当一个类派生自<strong>私有</strong>基类时，基类的<strong>公有</strong>和<strong>保护</strong>成员将成为派生类的<strong>私有</strong>成员。</li>
</ul>
<h2 id="多继承"><a href="#多继承" class="headerlink" title="多继承"></a>多继承</h2><p>多继承即一个子类可以有多个父类，它继承了多个父类的特性。</p>
<p>C++ 类可以从多个类继承成员，语法如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> &lt;派生类名&gt;:</span>&lt;继承方式<span class="number">1</span>&gt;&lt;基类名<span class="number">1</span>&gt;,&lt;继承方式<span class="number">2</span>&gt;&lt;基类名<span class="number">2</span>&gt;,…</span><br><span class="line">&#123;</span><br><span class="line">&lt;派生类类体&gt;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>其中，访问修饰符继承方式是 <strong>public、protected</strong> 或 <strong>private</strong> 其中的一个，用来修饰每个基类，各个基类之间用逗号分隔，如上所示。现在让我们一起看看下面的实例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 基类 Shape</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Shape</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setWidth</span><span class="params">(<span class="keyword">int</span> w)</span></span></span><br><span class="line"><span class="function">      </span>&#123;</span><br><span class="line">         width = w;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="function"><span class="keyword">void</span> <span class="title">setHeight</span><span class="params">(<span class="keyword">int</span> h)</span></span></span><br><span class="line"><span class="function">      </span>&#123;</span><br><span class="line">         height = h;</span><br><span class="line">      &#125;</span><br><span class="line">   <span class="keyword">protected</span>:</span><br><span class="line">      <span class="keyword">int</span> width;</span><br><span class="line">      <span class="keyword">int</span> height;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 基类 PaintCost</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PaintCost</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="function"><span class="keyword">int</span> <span class="title">getCost</span><span class="params">(<span class="keyword">int</span> area)</span></span></span><br><span class="line"><span class="function">      </span>&#123;</span><br><span class="line">         <span class="keyword">return</span> area * <span class="number">70</span>;</span><br><span class="line">      &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 派生类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rectangle</span>:</span> <span class="keyword">public</span> Shape, <span class="keyword">public</span> PaintCost</span><br><span class="line">&#123;</span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">      <span class="function"><span class="keyword">int</span> <span class="title">getArea</span><span class="params">()</span></span></span><br><span class="line"><span class="function">      </span>&#123; </span><br><span class="line">         <span class="keyword">return</span> (width * height); </span><br><span class="line">      &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   Rectangle Rect;</span><br><span class="line">   <span class="keyword">int</span> area;</span><br><span class="line"> </span><br><span class="line">   Rect.setWidth(<span class="number">5</span>);</span><br><span class="line">   Rect.setHeight(<span class="number">7</span>);</span><br><span class="line"> </span><br><span class="line">   area = Rect.getArea();</span><br><span class="line">   </span><br><span class="line">   <span class="comment">// 输出对象的面积</span></span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Total area: "</span> &lt;&lt; Rect.getArea() &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="comment">// 输出总花费</span></span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; <span class="string">"Total paint cost: $"</span> &lt;&lt; Rect.getCost(area) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM内存区域划分</title>
    <url>/2018/08/13/JVM%E5%86%85%E5%AD%98%E5%8C%BA%E5%9F%9F%E5%88%92%E5%88%86/</url>
    <content><![CDATA[<p>本篇转载于<a href="https://www.cnblogs.com/dolphin0520/p/3613043.html" target="_blank" rel="noopener">https://www.cnblogs.com/dolphin0520/p/3613043.html</a></p>
<p>先了解了解,以后有空在钻研虚拟机问题.</p>
<a id="more"></a>

<p>学过C语言的朋友都知道C编译器在划分内存区域的时候经常将管理的区域划分为数据段和代码段，数据段包括堆、栈以及静态数据区。那么在Java语言当中，内存又是如何划分的呢？</p>
<p>由于Java程序是交由JVM执行的，所以我们在谈Java内存区域划分的时候事实上是指JVM内存区域划分。在讨论JVM内存区域划分之前，先来看一下Java程序具体执行的过程：</p>
<p><img src="/2018/08/13/JVM%E5%86%85%E5%AD%98%E5%8C%BA%E5%9F%9F%E5%88%92%E5%88%86/pic01.jpg" alt></p>
<p>　如上图所示，首先Java源代码文件(.java后缀)会被Java编译器编译为字节码文件(.class后缀)，然后由JVM中的类加载器加载各个类的字节码文件，加载完毕之后，交由JVM执行引擎执行。在整个程序执行过程中，JVM会用一段空间来存储程序执行期间需要用到的数据和相关信息，这段空间一般被称作为Runtime Data Area（运行时数据区），也就是我们常说的JVM内存。因此，在Java中我们常常说到的内存管理就是针对这段空间进行管理（如何分配和回收内存空间）。</p>
<p>　　在知道了JVM内存是什么东西之后，下面我们就来讨论一下这段空间具体是如何划分区域的，是不是也像C语言中一样也存在栈和堆呢？</p>
<h1 id="一-运行时数据区包括哪几部分？"><a href="#一-运行时数据区包括哪几部分？" class="headerlink" title="一.运行时数据区包括哪几部分？"></a>一.运行时数据区包括哪几部分？</h1><p>　　根据《Java虚拟机规范》的规定，运行时数据区通常包括这几个部分：程序计数器(Program Counter Register)、Java栈(VM Stack)、本地方法栈(Native Method Stack)、方法区(Method Area)、堆(Heap)。</p>
<p><img src="/2018/08/13/JVM%E5%86%85%E5%AD%98%E5%8C%BA%E5%9F%9F%E5%88%92%E5%88%86/pic02.jpg" alt></p>
<p>如上图所示，JVM中的运行时数据区应该包括这些部分。在JVM规范中虽然规定了程序在执行期间运行时数据区应该包括这几部分，但是至于具体如何实现并没有做出规定，不同的虚拟机厂商可以有不同的实现方式。</p>
<h1 id="二-运行时数据区的每部分到底存储了哪些数据？"><a href="#二-运行时数据区的每部分到底存储了哪些数据？" class="headerlink" title="二.运行时数据区的每部分到底存储了哪些数据？"></a>二.运行时数据区的每部分到底存储了哪些数据？</h1><p>　　下面我们来了解一下运行时数据区的每部分具体用来存储程序执行过程中的哪些数据。</p>
<h2 id="1-程序计数器"><a href="#1-程序计数器" class="headerlink" title="1.程序计数器"></a>1.程序计数器</h2><p>　　程序计数器（Program Counter Register），也有称作为PC寄存器。想必学过汇编语言的朋友对程序计数器这个概念并不陌生，在汇编语言中，程序计数器是指CPU中的寄存器，它保存的是程序当前执行的指令的地址（也可以说保存下一条指令的所在存储单元的地址），当CPU需要执行指令时，需要从程序计数器中得到当前需要执行的指令所在存储单元的地址，然后根据得到的地址获取到指令，在得到指令之后，程序计数器便自动加1或者根据转移指针得到下一条指令的地址，如此循环，直至执行完所有的指令。</p>
<p>　　虽然JVM中的程序计数器并不像汇编语言中的程序计数器一样是物理概念上的CPU寄存器，但是JVM中的程序计数器的功能跟汇编语言中的程序计数器的功能在逻辑上是等同的，也就是说是用来指示 执行哪条指令的。</p>
<p>　　由于在JVM中，多线程是通过线程轮流切换来获得CPU执行时间的，因此，在任一具体时刻，一个CPU的内核只会执行一条线程中的指令，因此，为了能够使得每个线程都在线程切换后能够恢复在切换之前的程序执行位置，每个线程都需要有自己独立的程序计数器，并且不能互相被干扰，否则就会影响到程序的正常执行次序。因此，可以这么说，程序计数器是每个线程所私有的。</p>
<p>　　在JVM规范中规定，如果线程执行的是非native方法，则程序计数器中保存的是当前需要执行的指令的地址；如果线程执行的是native方法，则程序计数器中的值是undefined。</p>
<p>　　由于程序计数器中存储的数据所占空间的大小不会随程序的执行而发生改变，因此，对于程序计数器是不会发生内存溢出现象(OutOfMemory)的。</p>
<h2 id="2-Java栈"><a href="#2-Java栈" class="headerlink" title="2.Java栈"></a>2.Java栈</h2><p>　　Java栈也称作虚拟机栈（Java Vitual Machine Stack），也就是我们常常所说的栈，跟C语言的数据段中的栈类似。事实上，Java栈是Java方法执行的内存模型。为什么这么说呢？下面就来解释一下其中的原因。</p>
<p>　　Java栈中存放的是一个个的栈帧，每个栈帧对应一个被调用的方法，在栈帧中包括局部变量表(Local Variables)、操作数栈(Operand Stack)、指向当前方法所属的类的运行时常量池（运行时常量池的概念在方法区部分会谈到）的引用(Reference to runtime constant pool)、方法返回地址(Return Address)和一些额外的附加信息。当线程执行一个方法时，就会随之创建一个对应的栈帧，并将建立的栈帧压栈。当方法执行完毕之后，便会将栈帧出栈。因此可知，线程当前执行的方法所对应的栈帧必定位于Java栈的顶部。讲到这里，大家就应该会明白为什么 在 使用 递归方法的时候容易导致栈内存溢出的现象了以及为什么栈区的空间不用程序员去管理了（当然在Java中，程序员基本不用关系到内存分配和释放的事情，因为Java有自己的垃圾回收机制），这部分空间的分配和释放都是由系统自动实施的。对于所有的程序设计语言来说，栈这部分空间对程序员来说是不透明的。下图表示了一个Java栈的模型：</p>
<p><img src="/2018/08/13/JVM%E5%86%85%E5%AD%98%E5%8C%BA%E5%9F%9F%E5%88%92%E5%88%86/pic03.jpg" alt></p>
<p>局部变量表，顾名思义，想必不用解释大家应该明白它的作用了吧。就是用来存储方法中的局部变量（包括在方法中声明的非静态变量以及函数形参）。对于基本数据类型的变量，则直接存储它的值，对于引用类型的变量，则存的是指向对象的引用。局部变量表的大小在编译器就可以确定其大小了，因此在程序执行期间局部变量表的大小是不会改变的。</p>
<p>　　操作数栈，想必学过数据结构中的栈的朋友想必对表达式求值问题不会陌生，栈最典型的一个应用就是用来对表达式求值。想想一个线程执行方法的过程中，实际上就是不断执行语句的过程，而归根到底就是进行计算的过程。因此可以这么说，程序中的所有计算过程都是在借助于操作数栈来完成的。</p>
<p>　　指向运行时常量池的引用，因为在方法执行的过程中有可能需要用到类中的常量，所以必须要有一个引用指向运行时常量。</p>
<p>　　方法返回地址，当一个方法执行完毕之后，要返回之前调用它的地方，因此在栈帧中必须保存一个方法返回地址。</p>
<p>　　由于每个线程正在执行的方法可能不同，因此每个线程都会有一个自己的Java栈，互不干扰。</p>
<h2 id="3-本地方法栈"><a href="#3-本地方法栈" class="headerlink" title="3.本地方法栈"></a>3.本地方法栈</h2><p>　　本地方法栈与Java栈的作用和原理非常相似。区别只不过是Java栈是为执行Java方法服务的，而本地方法栈则是为执行本地方法（Native Method）服务的。在JVM规范中，并没有对本地方发展的具体实现方法以及数据结构作强制规定，虚拟机可以自由实现它。在HotSopt虚拟机中直接就把本地方法栈和Java栈合二为一。</p>
<h2 id="4-堆"><a href="#4-堆" class="headerlink" title="4.堆"></a>4.堆</h2><p>　　在C语言中，堆这部分空间是唯一一个程序员可以管理的内存区域。程序员可以通过malloc函数和free函数在堆上申请和释放空间。那么在Java中是怎么样的呢？</p>
<p>　　Java中的堆是用来存储对象本身的以及数组（当然，数组引用是存放在Java栈中的）。只不过和C语言中的不同，在Java中，程序员基本不用去关心空间释放的问题，Java的垃圾回收机制会自动进行处理。因此这部分空间也是Java垃圾收集器管理的主要区域。另外，堆是被所有线程共享的，在JVM中只有一个堆。</p>
<h2 id="5-方法区"><a href="#5-方法区" class="headerlink" title="5.方法区"></a>5.方法区</h2><p>　　方法区在JVM中也是一个非常重要的区域，它与堆一样，是被线程共享的区域。在方法区中，存储了每个类的信息（包括类的名称、方法信息、字段信息）、静态变量、常量以及编译器编译后的代码等。</p>
<p>　　在Class文件中除了类的字段、方法、接口等描述信息外，还有一项信息是常量池，用来存储编译期间生成的字面量和符号引用。</p>
<p>　　在方法区中有一个非常重要的部分就是运行时常量池，它是每一个类或接口的常量池的运行时表示形式，在类和接口被加载到JVM后，对应的运行时常量池就被创建出来。当然并非Class文件常量池中的内容才能进入运行时常量池，在运行期间也可将新的常量放入运行时常量池中，比如String的intern方法。</p>
<p>　　在JVM规范中，没有强制要求方法区必须实现垃圾回收。很多人习惯将方法区称为“永久代”，是因为HotSpot虚拟机以永久代来实现方法区，从而JVM的垃圾收集器可以像管理堆区一样管理这部分区域，从而不需要专门为这部分设计垃圾回收机制。不过自从JDK7之后，Hotspot虚拟机便将运行时常量池从永久代移除了。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java日期时间</title>
    <url>/2018/08/07/Java%E6%97%A5%E6%9C%9F%E6%97%B6%E9%97%B4/</url>
    <content><![CDATA[<p>日期类型,从1970年计,每过一毫秒就+1.不是+1s,所以也没法用膜法续命.</p>
<a id="more"></a>

<p>所有的数据类型，无论是整数，布尔，浮点数还是字符串，最后都需要以数字的形式表现出来。  日期类型也不例外，换句话说，一个日期，比如2020年10月1日，在计算机里，会用一个数字来代替。  那么最特殊的一个数字，就是零. 零这个数字，就代表Java中的时间原点，其对应的日期是1970年1月1日 8点0分0秒 。 (为什么是8点，因为中国的太平洋时区是UTC-8，刚好和格林威治时间差8个小时)  为什么对应1970年呢？ 因为1969年发布了第一个 UNIX 版本：AT&amp;T，综合考虑，当时就把1970年当做了时间原点。  所有的日期，都是以为这个0点为基准，每过一毫秒，就+1。 </p>
<h1 id="Java-日期时间"><a href="#Java-日期时间" class="headerlink" title="Java 日期时间"></a>Java 日期时间</h1><p>java.util 包提供了 Date 类来封装当前的日期和时间。 Date 类提供两个构造函数来实例化 Date 对象。</p>
<p>第一个构造函数使用当前日期和时间来初始化对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Date( )</span><br></pre></td></tr></table></figure>

<p>第二个构造函数接收一个参数，该参数是从1970年1月1日起的毫秒数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Date(<span class="keyword">long</span> millisec)</span><br></pre></td></tr></table></figure>

<p>Date对象创建以后，可以调用下面的方法。</p>
<table>
<thead>
<tr>
<th>序号</th>
<th>方法和描述</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><strong>boolean after(Date date)</strong> 若当调用此方法的Date对象在指定日期之后返回true,否则返回false。</td>
</tr>
<tr>
<td>2</td>
<td><strong>boolean before(Date date)</strong> 若当调用此方法的Date对象在指定日期之前返回true,否则返回false。</td>
</tr>
<tr>
<td>3</td>
<td><strong>Object clone( )</strong> 返回此对象的副本。</td>
</tr>
<tr>
<td>4</td>
<td><strong>int compareTo(Date date)</strong> 比较当调用此方法的Date对象和指定日期。两者相等时候返回0。调用对象在指定日期之前则返回负数。调用对象在指定日期之后则返回正数。</td>
</tr>
<tr>
<td>5</td>
<td><strong>int compareTo(Object obj)</strong> 若obj是Date类型则操作等同于compareTo(Date) 。否则它抛出ClassCastException。</td>
</tr>
<tr>
<td>6</td>
<td><strong>boolean equals(Object date)</strong> 当调用此方法的Date对象和指定日期相等时候返回true,否则返回false。</td>
</tr>
<tr>
<td>7</td>
<td><strong>long getTime( )</strong> 返回自 1970 年 1 月 1 日 00:00:00 GMT 以来此 Date 对象表示的毫秒数。</td>
</tr>
<tr>
<td>8</td>
<td><strong>int hashCode( )</strong>  返回此对象的哈希码值。</td>
</tr>
<tr>
<td>9</td>
<td><strong>void setTime(long time)</strong>   用自1970年1月1日00:00:00 GMT以后time毫秒数设置时间和日期。</td>
</tr>
<tr>
<td>10</td>
<td><strong>String toString( )</strong> 把此 Date 对象转换为以下形式的 String： dow mon dd hh:mm:ss zzz yyyy 其中： dow 是一周中的某一天 (Sun, Mon, Tue, Wed, Thu, Fri, Sat)。</td>
</tr>
</tbody></table>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> date;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestDate</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">// 当前时间</span></span><br><span class="line">        Date d1 = <span class="keyword">new</span> Date();</span><br><span class="line">        System.out.println(<span class="string">"当前时间:"</span>);</span><br><span class="line">        System.out.println(d1);</span><br><span class="line">        System.out.println();</span><br><span class="line">        <span class="comment">// 从1970年1月1日 早上8点0分0秒 开始经历的毫秒数</span></span><br><span class="line">        Date d2 = <span class="keyword">new</span> Date(<span class="number">5000</span>);</span><br><span class="line">        System.out.println(<span class="string">"从1970年1月1日 早上8点0分0秒 开始经历了5秒的时间"</span>);</span><br><span class="line">        System.out.println(d2);</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="日期比较"><a href="#日期比较" class="headerlink" title="日期比较"></a>日期比较</h2><p>Java使用以下三种方法来比较两个日期：</p>
<ul>
<li>使用 getTime() 方法获取两个日期（自1970年1月1日经历的毫秒数值），然后比较这两个值。</li>
<li>使用方法 before()，after() 和 equals()。例如，一个月的12号比18号早，则 new Date(99, 2, 12).before(new Date (99, 2, 18)) 返回true。</li>
<li>使用 compareTo() 方法，它是由 Comparable 接口定义的，Date 类实现了这个接口。</li>
</ul>
<h3 id="创建一个从1995-1-1-00-00-00-到-1995-12-31-23-59-59-之间的随机日期"><a href="#创建一个从1995-1-1-00-00-00-到-1995-12-31-23-59-59-之间的随机日期" class="headerlink" title="创建一个从1995.1.1 00:00:00 到 1995.12.31 23:59:59 之间的随机日期"></a>创建一个从1995.1.1 00:00:00 到 1995.12.31 23:59:59 之间的随机日期</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"><span class="keyword">import</span> java.text.SimpleDateFormat;</span><br><span class="line"><span class="keyword">import</span> java.text.ParseException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestDate</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String start = <span class="string">"1995/1/1 0:0:0"</span>;</span><br><span class="line">        String end = <span class="string">"1995/12/31 23:59:59"</span>;</span><br><span class="line">        <span class="keyword">long</span> sd = stringToDateLong(start);</span><br><span class="line">        <span class="keyword">long</span> se = stringToDateLong(end);</span><br><span class="line">        <span class="keyword">long</span> rangeRandom = genRandomDate(se, sd);</span><br><span class="line">        Date ranDate = <span class="keyword">new</span> Date(rangeRandom);</span><br><span class="line">        System.out.println(ranDate);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">long</span> <span class="title">stringToDateLong</span><span class="params">(String d)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">long</span> dlong = <span class="number">0</span>;</span><br><span class="line">        SimpleDateFormat sdf = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy/MM/dd HH:mm:ss"</span>);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Date dd = sdf.parse(d);</span><br><span class="line">            <span class="keyword">long</span> dLong = dd.getTime();</span><br><span class="line">            <span class="keyword">return</span> dLong;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ParseException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> dlong;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">long</span> <span class="title">genRandomDate</span><span class="params">(<span class="keyword">long</span> max, <span class="keyword">long</span> min)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">long</span> ranLong = min + (<span class="keyword">long</span>) (Math.random() * (max - min));</span><br><span class="line">        <span class="keyword">return</span> ranLong;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="使用1970年-2000年之间的随机日期初始化该数组"><a href="#使用1970年-2000年之间的随机日期初始化该数组" class="headerlink" title="使用1970年-2000年之间的随机日期初始化该数组"></a>使用1970年-2000年之间的随机日期初始化该数组</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"><span class="keyword">import</span> java.text.SimpleDateFormat;</span><br><span class="line"><span class="keyword">import</span> java.text.ParseException;</span><br><span class="line"><span class="keyword">import</span> java.util.TreeSet;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestDate</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> String start = <span class="string">"1997/1/1 0:0:0"</span>;</span><br><span class="line">    <span class="keyword">static</span> String end = <span class="string">"2000/12/31 23:59:59"</span>;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">long</span> sd = stringToDateLong(start);</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">long</span> se = stringToDateLong(end);</span><br><span class="line">    <span class="keyword">static</span> TreeSet&lt;Date&gt; dateSet = <span class="keyword">new</span> TreeSet&lt;&gt;() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            String out = <span class="string">""</span>;</span><br><span class="line">            <span class="keyword">for</span> (Date date : dateSet</span><br><span class="line">            ) &#123;</span><br><span class="line">                SimpleDateFormat sdf = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy-MM-dd HH:mm:ss"</span>);</span><br><span class="line">                String str = sdf.format(date) + <span class="string">"\n"</span>;</span><br><span class="line">                out += str;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        init();</span><br><span class="line">        System.out.println(dateSet.toString());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">9</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">long</span> rangeRandom = genRandomDate(se, sd);</span><br><span class="line">            dateSet.add(<span class="keyword">new</span> Date(rangeRandom));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">long</span> <span class="title">stringToDateLong</span><span class="params">(String d)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">long</span> dlong = <span class="number">0</span>;</span><br><span class="line">        SimpleDateFormat sdf = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy/MM/dd HH:mm:ss"</span>);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Date dd = sdf.parse(d);</span><br><span class="line">            <span class="keyword">long</span> dLong = dd.getTime();</span><br><span class="line">            <span class="keyword">return</span> dLong;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ParseException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> dlong;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">long</span> <span class="title">genRandomDate</span><span class="params">(<span class="keyword">long</span> max, <span class="keyword">long</span> min)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">long</span> ranLong = min + (<span class="keyword">long</span>) (Math.random() * (max - min));</span><br><span class="line">        <span class="keyword">return</span> ranLong;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="将日期转为字符串"><a href="#将日期转为字符串" class="headerlink" title="将日期转为字符串"></a>将日期转为字符串</h1><h2 id="使用-SimpleDateFormat-格式化日期"><a href="#使用-SimpleDateFormat-格式化日期" class="headerlink" title="使用 SimpleDateFormat 格式化日期"></a>使用 SimpleDateFormat 格式化日期</h2><p>SimpleDateFormat 是一个以语言环境敏感的方式来格式化和分析日期的类。SimpleDateFormat 允许你选择任何用户自定义日期时间格式来运行。例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.*;</span><br><span class="line"><span class="keyword">import</span> java.text.*;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DateDemo</span> </span>&#123;</span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String args[])</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">      Date dNow = <span class="keyword">new</span> Date( );</span><br><span class="line">      SimpleDateFormat ft = <span class="keyword">new</span> SimpleDateFormat (<span class="string">"E yyyy.MM.dd 'at' hh:mm:ss a zzz"</span>);</span><br><span class="line"> </span><br><span class="line">      System.out.println(<span class="string">"Current Date: "</span> + ft.format(dNow));</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关键字是SimpleDateFormat对象的format方法.</p>
<h2 id="日期和时间的格式化编码"><a href="#日期和时间的格式化编码" class="headerlink" title="日期和时间的格式化编码"></a>日期和时间的格式化编码</h2><p>时间模式字符串用来指定时间格式。在此模式中，所有的 ASCII 字母被保留为模式字母，定义如下：</p>
<table>
<thead>
<tr>
<th><strong>字母</strong></th>
<th><strong>描述</strong></th>
<th><strong>示例</strong></th>
</tr>
</thead>
<tbody><tr>
<td>G</td>
<td>纪元标记</td>
<td>AD</td>
</tr>
<tr>
<td>y</td>
<td>四位年份</td>
<td>2001</td>
</tr>
<tr>
<td>M</td>
<td>月份</td>
<td>July or 07</td>
</tr>
<tr>
<td>d</td>
<td>一个月的日期</td>
<td>10</td>
</tr>
<tr>
<td>h</td>
<td>A.M./P.M. (1~12)格式小时</td>
<td>12</td>
</tr>
<tr>
<td>H</td>
<td>一天中的小时 (0~23)</td>
<td>22</td>
</tr>
<tr>
<td>m</td>
<td>分钟数</td>
<td>30</td>
</tr>
<tr>
<td>s</td>
<td>秒数</td>
<td>55</td>
</tr>
<tr>
<td>S</td>
<td>毫秒数</td>
<td>234</td>
</tr>
<tr>
<td>E</td>
<td>星期几</td>
<td>Tuesday</td>
</tr>
<tr>
<td>D</td>
<td>一年中的日子</td>
<td>360</td>
</tr>
<tr>
<td>F</td>
<td>一个月中第几周的周几</td>
<td>2 (second Wed. in July)</td>
</tr>
<tr>
<td>w</td>
<td>一年中第几周</td>
<td>40</td>
</tr>
<tr>
<td>W</td>
<td>一个月中第几周</td>
<td>1</td>
</tr>
<tr>
<td>a</td>
<td>A.M./P.M. 标记</td>
<td>PM</td>
</tr>
<tr>
<td>k</td>
<td>一天中的小时(1~24)</td>
<td>24</td>
</tr>
<tr>
<td>K</td>
<td>A.M./P.M. (0~11)格式小时</td>
<td>10</td>
</tr>
<tr>
<td>z</td>
<td>时区</td>
<td>Eastern Standard Time</td>
</tr>
<tr>
<td>‘</td>
<td>文字定界符</td>
<td>Delimiter</td>
</tr>
<tr>
<td>“</td>
<td>单引号</td>
<td>`</td>
</tr>
</tbody></table>
<h2 id="使用printf格式化日期"><a href="#使用printf格式化日期" class="headerlink" title="使用printf格式化日期"></a>使用printf格式化日期</h2><p>printf 方法可以很轻松地格式化时间和日期。使用两个字母格式，它以 %t 开头并且以下面表格中的一个字母结尾。</p>
<table>
<thead>
<tr>
<th>转  换  符</th>
<th>说    明</th>
<th>示    例</th>
</tr>
</thead>
<tbody><tr>
<td>c</td>
<td>包括全部日期和时间信息</td>
<td>星期六 十月 27 14:21:20 CST 2007</td>
</tr>
<tr>
<td>F</td>
<td>“年-月-日”格式</td>
<td>2007-10-27</td>
</tr>
<tr>
<td>D</td>
<td>“月/日/年”格式</td>
<td>10/27/07</td>
</tr>
<tr>
<td>r</td>
<td>“HH:MM:SS PM”格式（12时制）</td>
<td>02:25:51 下午</td>
</tr>
<tr>
<td>T</td>
<td>“HH:MM:SS”格式（24时制）</td>
<td>14:28:16</td>
</tr>
<tr>
<td>R</td>
<td>“HH:MM”格式（24时制）</td>
<td>14:28</td>
</tr>
</tbody></table>
<h3 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DateDemo</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String args[])</span> </span>&#123;</span><br><span class="line">     <span class="comment">// 初始化 Date 对象</span></span><br><span class="line">     Date date = <span class="keyword">new</span> Date();</span><br><span class="line"> </span><br><span class="line">     <span class="comment">//c的使用  </span></span><br><span class="line">    System.out.printf(<span class="string">"全部日期和时间信息：%tc%n"</span>,date);          </span><br><span class="line">    <span class="comment">//f的使用  </span></span><br><span class="line">    System.out.printf(<span class="string">"年-月-日格式：%tF%n"</span>,date);  </span><br><span class="line">    <span class="comment">//d的使用  </span></span><br><span class="line">    System.out.printf(<span class="string">"月/日/年格式：%tD%n"</span>,date);  </span><br><span class="line">    <span class="comment">//r的使用  </span></span><br><span class="line">    System.out.printf(<span class="string">"HH:MM:SS PM格式（12时制）：%tr%n"</span>,date);  </span><br><span class="line">    <span class="comment">//t的使用  </span></span><br><span class="line">    System.out.printf(<span class="string">"HH:MM:SS格式（24时制）：%tT%n"</span>,date);  </span><br><span class="line">    <span class="comment">//R的使用  </span></span><br><span class="line">    System.out.printf(<span class="string">"HH:MM格式（24时制）：%tR"</span>,date);  </span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="将字符串转为日期"><a href="#将字符串转为日期" class="headerlink" title="将字符串转为日期"></a>将字符串转为日期</h1><p>关键字为parse,模式（yyyy/MM/dd HH:mm:ss）需要和字符串格式保持一致，如果不一样就会抛出解析异常ParseException.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> date;</span><br><span class="line">  </span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">import</span> java.text.ParseException;</span><br><span class="line"><span class="keyword">import</span> java.text.SimpleDateFormat;</span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestDate</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SimpleDateFormat sdf =<span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy/MM/dd HH:mm:ss"</span> );</span><br><span class="line">  </span><br><span class="line">        String str = <span class="string">"2016/1/5 12:12:12"</span>;</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Date d = sdf.parse(str);</span><br><span class="line">            System.out.printf(<span class="string">"字符串 %s 通过格式  yyyy/MM/dd HH:mm:ss %n转换为日期对象: %s"</span>,str,d.toString());</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ParseException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">          </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Calendar类"><a href="#Calendar类" class="headerlink" title="Calendar类"></a>Calendar类</h1><p>Calendar类用于设置和获取日期数据的特定部分等,常用于进行“翻日历”，比如下个月的今天是多久.Calendar类是一个抽象类,不能实例化,需要使用getInstance()方法来获取.而且该方法还是用的<strong>单例模式</strong>.主要方法:add方法，在原日期上增加年/月/日和set方法，直接设置年/月/日 </p>
<h3 id="Calendar类对象字段类型"><a href="#Calendar类对象字段类型" class="headerlink" title="Calendar类对象字段类型"></a>Calendar类对象字段类型</h3><p>Calendar类中用以下这些常量表示不同的意义，jdk内的很多类其实都是采用的这种思想</p>
<table>
<thead>
<tr>
<th>常量</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>Calendar.YEAR</td>
<td>年份</td>
</tr>
<tr>
<td>Calendar.MONTH</td>
<td>月份</td>
</tr>
<tr>
<td>Calendar.DATE</td>
<td>日期</td>
</tr>
<tr>
<td>Calendar.DAY_OF_MONTH</td>
<td>日期，和上面的字段意义完全相同</td>
</tr>
<tr>
<td>Calendar.HOUR</td>
<td>12小时制的小时</td>
</tr>
<tr>
<td>Calendar.HOUR_OF_DAY</td>
<td>24小时制的小时</td>
</tr>
<tr>
<td>Calendar.MINUTE</td>
<td>分钟</td>
</tr>
<tr>
<td>Calendar.SECOND</td>
<td>秒</td>
</tr>
<tr>
<td>Calendar.DAY_OF_WEEK</td>
<td>星期几</td>
</tr>
</tbody></table>
<h3 id="获得相关数据"><a href="#获得相关数据" class="headerlink" title="获得相关数据:"></a>获得相关数据:</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Calendar c1 = Calendar.getInstance();</span><br><span class="line"><span class="comment">// 获得年份</span></span><br><span class="line"><span class="keyword">int</span> year = c1.get(Calendar.YEAR);</span><br><span class="line"><span class="comment">// 获得月份</span></span><br><span class="line"><span class="keyword">int</span> month = c1.get(Calendar.MONTH) + <span class="number">1</span>;</span><br><span class="line"><span class="comment">// 获得日期</span></span><br><span class="line"><span class="keyword">int</span> date = c1.get(Calendar.DATE);</span><br><span class="line"><span class="comment">// 获得小时</span></span><br><span class="line"><span class="keyword">int</span> hour = c1.get(Calendar.HOUR_OF_DAY);</span><br><span class="line"><span class="comment">// 获得分钟</span></span><br><span class="line"><span class="keyword">int</span> minute = c1.get(Calendar.MINUTE);</span><br><span class="line"><span class="comment">// 获得秒</span></span><br><span class="line"><span class="keyword">int</span> second = c1.get(Calendar.SECOND);</span><br><span class="line"><span class="comment">// 获得星期几（注意（这个与Date类是不同的）：1代表星期日、2代表星期1、3代表星期二，以此类推）</span></span><br><span class="line"><span class="keyword">int</span> day = c1.get(Calendar.DAY_OF_WEEK);</span><br></pre></td></tr></table></figure>

<h2 id="修改相关数据"><a href="#修改相关数据" class="headerlink" title="修改相关数据"></a>修改相关数据</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.text.SimpleDateFormat;</span><br><span class="line"><span class="keyword">import</span> java.util.Calendar;</span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CalendarTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> SimpleDateFormat sdf = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy-MM-dd HH:mm:ss"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Calendar c = Calendar.getInstance();</span><br><span class="line">        Date now = c.getTime();</span><br><span class="line">        <span class="comment">// 当前日期</span></span><br><span class="line">        System.out.println(<span class="string">"当前日期：\t"</span> + sdf.format(c.getTime()));</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 下个月的今天</span></span><br><span class="line">        c.setTime(now);</span><br><span class="line">        c.add(Calendar.MONTH, <span class="number">1</span>);</span><br><span class="line">        System.out.println(<span class="string">"下个月的今天:\t"</span> + sdf.format(c.getTime()));</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 去年的今天</span></span><br><span class="line">        c.setTime(now);</span><br><span class="line">        c.add(Calendar.YEAR, -<span class="number">1</span>);</span><br><span class="line">        System.out.println(<span class="string">"去年的今天:\t"</span> + sdf.format(c.getTime()));</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 上个月的第三天</span></span><br><span class="line">        c.setTime(now);</span><br><span class="line">        c.add(Calendar.MONTH, -<span class="number">1</span>);</span><br><span class="line">        c.set(Calendar.DATE, <span class="number">3</span>);</span><br><span class="line">        System.out.println(<span class="string">"上个月的第三天:\t"</span> + sdf.format(c.getTime()));</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>利用字段类型设置</p>
<p><strong>Set设置</strong></p>
<p>如果只设定某个字段，例如日期的值，则可以使用如下set方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(<span class="keyword">int</span> field,<span class="keyword">int</span> value)</span></span></span><br></pre></td></tr></table></figure>

<p>把 c1对象代表的日期设置为10号，其它所有的数值会被重新计算</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">c1.set(Calendar.DATE,<span class="number">10</span>);</span><br></pre></td></tr></table></figure>

<p>把c1对象代表的年份设置为2008年，其他的所有数值会被重新计算</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">c1.set(Calendar.YEAR,<span class="number">2008</span>);</span><br></pre></td></tr></table></figure>

<p>其他字段属性set的意义以此类推</p>
<p>也可以在set方法中直接指定具体数值</p>
<p><strong>void set(int year, int month, int date, int hour, int minute, int second)</strong>  //设置年、月、日、小时、分钟、秒的值。 </p>
<p><strong>Add设置</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Calendar c1 = Calendar.getInstance();</span><br></pre></td></tr></table></figure>

<p>把c1对象的日期加上10，也就是c1也就表示为10天后的日期，其它所有的数值会被重新计算</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">c1.add(Calendar.DATE, <span class="number">10</span>);</span><br></pre></td></tr></table></figure>

<p>把c1对象的日期减去10，也就是c1也就表示为10天前的日期，其它所有的数值会被重新计算</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">c1.add(Calendar.DATE, -<span class="number">10</span>);</span><br></pre></td></tr></table></figure>

<p>其他字段属性的add的意义以此类推</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java泛型</title>
    <url>/2018/08/13/Java%E6%B3%9B%E5%9E%8B/</url>
    <content><![CDATA[<p>在集合中,集合是不知道向里面添加的是什么的,为了通用性,全都作为Object,导致取出来后要进行强制类型转换.而泛型允许程序在创建集合时指定集合元素的类型.</p>
<a id="more"></a>

<h1 id="泛型入门"><a href="#泛型入门" class="headerlink" title="泛型入门"></a>泛型入门</h1><h2 id="使用泛型"><a href="#使用泛型" class="headerlink" title="使用泛型"></a>使用泛型</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestGeneric</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ArrayList&lt;String&gt; strList = <span class="keyword">new</span> ArrayList&lt;String&gt;();</span><br><span class="line">        strList.add(<span class="string">"你好"</span>);</span><br><span class="line">        strList.add(<span class="string">"再见"</span>);</span><br><span class="line">        strList.forEach(str -&gt; System.out.println(str.length()));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面程序成功创建了一个特殊的List集合:strList,这个List集合只能保存字符串对象，不能保存其他类型的对象。创建这种特殊集合的方法是：在集合接口、类后增加尖括号，尖括号里放一个数据类型，即表明这个集合接口、集合类只能保存特定类型的对象.</p>
<p>在上面的代码中我指定了集合所能装入的类型:String.这样就可以避免一不小心加入类型错误的元素,而且程序 更加简洁，集合自动记住所有集合元素的数据类型，从而无须对集合元素进行强制类型转换.泛型从JDK5开始支持.</p>
<h2 id="JDK7中的变化"><a href="#JDK7中的变化" class="headerlink" title="JDK7中的变化"></a>JDK7中的变化</h2><p>在jdk7中,不需要这么写了<code>ArrayList&lt;String&gt; strList = new ArrayList&lt;String&gt;();</code>,可以把后面一个尖括号里的内容省略:<code>ArrayList&lt;String&gt; strList = new ArrayList&lt;&gt;();</code>.</p>
<h1 id="深入泛型"><a href="#深入泛型" class="headerlink" title="深入泛型"></a>深入泛型</h1><p>所谓泛型，就是允许在定义类、接口、方法时使用类型形参，这个类型形参将在声明变量、创建对象、调用方法时动态地指定(即传入实际的类型参数，也可称为类型实参).</p>
<h2 id="定义泛型接口-类"><a href="#定义泛型接口-类" class="headerlink" title="定义泛型接口,类"></a>定义泛型接口,类</h2><p>下面是JDK5改写之后的List接口,Iterator接口,Map接口:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">List</span>&lt;<span class="title">E</span>&gt;</span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">add</span><span class="params">(E x)</span></span>;</span><br><span class="line">	<span class="function">Iterator&lt;E&gt; <span class="title">Iterator</span><span class="params">()</span></span>;</span><br><span class="line">	...</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Iterator</span>&lt;<span class="title">E</span>&gt;</span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">	<span class="function">E <span class="title">next</span><span class="params">()</span></span>;</span><br><span class="line">	<span class="function"><span class="keyword">boolean</span> <span class="title">hasNext</span><span class="params">()</span></span>;</span><br><span class="line">	...</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Map</span>&lt;<span class="title">K</span> , <span class="title">V</span>&gt;</span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="function">Set&lt;K&gt; <span class="title">keyset</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function">V <span class="title">put</span><span class="params">(K key, V value)</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>观察发现:泛型允许在定义接口、类时声明类型形参，类型形参在整个接口、类体内可当成类型使用，几乎所有可使用普通类型的地方都可以使用这种类型形参。</p>
<p>我们可以将<code>ArrayList&lt;String&gt;</code>理解为ArrayList的一个子类,但仅仅是这么理解,实际上并没有产生这种子类.我倾向于把它解释为一种”标记”.</p>
<p>泛型不仅仅只是用于集合,任何类,接口都可以增加泛型声明.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Apple</span>&lt;<span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">	<span class="keyword">private</span> T info;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">Apple</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">Apple</span><span class="params">(T info)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">    	<span class="keyword">this</span>.info = info;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setlnfo</span><span class="params">(T info)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">    	<span class="keyword">this</span>.info = info;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">getlnfo</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">    	<span class="keyword">return</span> <span class="keyword">this</span>.info;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        / / 由于传给T 形参的是String,所以构造器参数只能是String</span><br><span class="line">        Apple&lt;String&gt; al = <span class="keyword">new</span> Apple&lt;&gt;(<span class="string">"苹果"</span>) ；</span><br><span class="line">        System.out.println(al.getlnfo());</span><br><span class="line">        / / 由于传给T 形参的是Double,所以构造器参数只能是Double或<span class="keyword">double</span></span><br><span class="line">        Apple&lt;Double&gt; a2 = <span class="keyword">new</span> Apple&lt;&gt;(<span class="number">5.67</span>);</span><br><span class="line">        System.out.println(a2.getlnfo());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意:构造器名仍是Apple,不是Apple&lt;T&gt;.</p>
<h2 id="从泛型类派生普通子类"><a href="#从泛型类派生普通子类" class="headerlink" title="从泛型类派生普通子类"></a>从泛型类派生普通子类</h2><blockquote>
<p>从泛型派生子类,泛型类型需具体化</p>
</blockquote>
<p>也就是说这种写法是错误的:<code>public class A extends Apple&lt;T&gt;{ }</code></p>
<p>如果需要这样做有两种方法:</p>
<ol>
<li>指定泛型类型(传入实际的类型参数)</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">A</span> <span class="keyword">extends</span> <span class="title">Apple</span>&lt;<span class="title">String</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这种写法会使得Apple类中的T全部替换为String,A继承了这个具体的Apple类.</p>
<ol start="2">
<li>不指定泛型类型(不给T传入实际的类型参数)</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">A</span> <span class="keyword">extends</span> <span class="title">Apple</span></span></span><br></pre></td></tr></table></figure>

<p>这样子做会使得T形参被当做Object处理.</p>
<h2 id="并不存在泛型类"><a href="#并不存在泛型类" class="headerlink" title="并不存在泛型类"></a>并不存在泛型类</h2><p>前面提到可以把ArrayList&lt;String&gt;类当成ArrayList的子类，事实上，ArrayList&lt;String&gt;类也确实像一种特殊的ArrayList类：该 ArrayList&lt;String&gt;对象只能添加String对象作为集合元素。但实际上，系统并没有为ArrayList&lt;String&gt;生成新的class文件，而且也不会把ArrayList&lt;Strin&gt;当成新类来处理。</p>
<p>如果用<code>==</code>来比较ArrayList&lt;String&gt;和ArrayList&lt;Integer&gt;,结果是true.并且对于泛型类的实例,不能用instanceof来检测它是否是ArrayList&lt;T&gt;的实例.</p>
<p>从本质上说,泛型只是为编译器提供了一种编译前的静态检查,在编译时不管你指定的泛型是什么,都会被视作Object,这是为了兼容JDK5之前的代码,这种叫做类型擦除,后文还会再讲.</p>
<h2 id="类型通配符"><a href="#类型通配符" class="headerlink" title="类型通配符"></a>类型通配符</h2><p>如果Foo是Bar的一个子类型(子类或者子接口)，而 G是具有泛型声明的类或接口，G&lt;Foo&gt;并不是G&lt;Bar&gt;的子类型！这一点非常值得注意，因为它与大部分人的习惯认为是不同的。</p>
<p>数组和泛型有所不同 ，假设Foo是Bar的一个子类型(子类或者子接口)，那么Foo[]依然是Bar[]的子类型；但 G&lt;Foo&gt;不是G&lt;Bar&gt;的子类型。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer[] ia = <span class="keyword">new</span> Integer[<span class="number">5</span>];</span><br><span class="line">Number[] na = ia;<span class="comment">//编译通过,但是na[0]=0.5会出错</span></span><br><span class="line">List&lt;Integer&gt; iList = <span class="keyword">new</span> ArrayList();</span><br><span class="line">List&lt;Number&gt; nList = iList;<span class="comment">//编译错误</span></span><br></pre></td></tr></table></figure>

<h3 id="使用类型通配符"><a href="#使用类型通配符" class="headerlink" title="使用类型通配符"></a>使用类型通配符</h3><p>既然不能向上面那样表示父类,那就换个东西表示,这就是<code>?</code>.类型通配符是一个问号(?)，将一个问号作为类型实参传给List集合，写作：List&lt;?&gt; (意思是元素类型未知的List)。这个问号(?) 被称为通配符, 它的元素类型可以匹配任何类型。所以<code>?</code>其实不是代表父类,而是代表<strong>任意</strong>,只是效果相同.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;Integer&gt; iList = <span class="keyword">new</span> ArrayList();</span><br><span class="line">List&lt;?&gt; nList = iList;</span><br><span class="line">nList.add(<span class="keyword">new</span> Integer(<span class="number">5</span>));<span class="comment">//错误</span></span><br></pre></td></tr></table></figure>

<p>但是<code>?</code>仅仅是用来表示是父类,但是不能向里面添加任何东西(除了null).我们只能以Object的形式将集合里的元素取出来,却不能加进去.因为它有可能List&lt;Object&gt;,有可能是List&lt;String&gt;等等,而泛型的核心思想是:<strong>把一个集合中的内容限制为一个特定的数据类型.</strong>只要违背了这个原则就会引起编译错误.</p>
<h2 id="泛型方法"><a href="#泛型方法" class="headerlink" title="泛型方法"></a>泛型方法</h2><p>在java中,泛型类和接口的定义非常简单，但是泛型方法就比较复杂了。 </p>
<p><strong>泛型类，是在实例化类的时候指明泛型的具体类型；泛型方法，是在调用方法的时候指明泛型的具体类型</strong> 。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 泛型方法的基本介绍</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> tClass 传入的泛型实参</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> T 返回值为T类型</span></span><br><span class="line"><span class="comment"> * 说明：</span></span><br><span class="line"><span class="comment"> *     1）public 与 返回值中间&lt;T&gt;非常重要，可以理解为声明此方法为泛型方法。</span></span><br><span class="line"><span class="comment"> *     2）只有声明了&lt;T&gt;的方法才是泛型方法，泛型类中的使用了泛型的成员方法并不是泛型方法。</span></span><br><span class="line"><span class="comment"> *     3）&lt;T&gt;表明该方法将使用泛型类型T，此时才可以在方法中使用泛型类型T。</span></span><br><span class="line"><span class="comment"> *     4）与泛型类的定义一样，此处T可以随便写为任意标识，常见的如T、E、K、V等形式的参数常用于表示泛型。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> &lt;T&gt; <span class="function">T <span class="title">genericMethod</span><span class="params">(Class&lt;T&gt; tClass)</span><span class="keyword">throws</span> InstantiationException ,</span></span><br><span class="line"><span class="function">  IllegalAccessException</span>&#123;</span><br><span class="line">        T instance = tClass.newInstance();</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请注意第二条.</p>
<h3 id="泛型方法的基本用法"><a href="#泛型方法的基本用法" class="headerlink" title="泛型方法的基本用法"></a>泛型方法的基本用法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> &lt;T&gt; <span class="function">T <span class="title">genericMethod</span><span class="params">(Class&lt;T&gt; tClass)</span><span class="keyword">throws</span> InstantiationException ,</span></span><br><span class="line"><span class="function">  IllegalAccessException</span>&#123;</span><br><span class="line">        T instance = tClass.newInstance();</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意&lt;T&gt;,只有在方法中声明了泛型,这个方法才叫做泛型方法.除此之外的使用泛型类的标识的方法其实不是泛型方法.</p>
<p>也可以在一个泛型类中使用泛型方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Hero</span>&lt;<span class="title">T</span>&gt; <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">1L</span>;</span><br><span class="line">    T name;</span><br><span class="line">    T hp;</span><br><span class="line"></span><br><span class="line">    Hero(T name, T hp) &#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.hp = hp;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">getInfo</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> &lt;E&gt; <span class="function">E <span class="title">Info</span><span class="params">(T nmae)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (E)name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="静态方法与泛型"><a href="#静态方法与泛型" class="headerlink" title="静态方法与泛型"></a>静态方法与泛型</h3><p>静态方法有一种情况需要注意一下，那就是在类中的静态方法使用泛型：<strong>静态方法无法访问类上定义的泛型；如果静态方法操作的引用数据类型不确定的时候，必须要将泛型定义在方法上。</strong></p>
<p>即：<strong>如果静态方法要使用泛型的话，必须将静态方法也定义成泛型方法</strong> 。</p>
<p>因为静态方法是随着类加载就初始化的,而泛型是在实例化时才传入具体的泛型类型:</p>
<p><code>public static void show(T t){..}</code>这样是错误的.</p>
<h2 id="泛型上下边界"><a href="#泛型上下边界" class="headerlink" title="泛型上下边界"></a>泛型上下边界</h2><p>使用<code>?</code>带来的问题是,它太宽泛了.</p>
<h3 id="extends"><a href="#extends" class="headerlink" title="? extends"></a>? extends</h3><p>ArrayList heroList&lt;? extends Hero&gt; 表示这是一个Hero泛型或者其子类泛型</p>
<p>heroList的泛型可能是Hero</p>
<p>heroList 的泛型可能是APHero</p>
<p>heroList 的泛型可能是ADHero </p>
<p>所以,可以确凿的是，从heroList取出来的对象，一定是可以转型成Hero的.但是，不能往里面放东西，因为放APHero就不满足&lt;ADHero&gt;,放ADHero又不满足&lt;APHero&gt;.即使只存在Hero和APHero也不行,因为ArrayList&lt;Hero&gt;和ArrayList&lt;APHero&gt;仍然不是一样东西.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> generic;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> charactor.ADHero;</span><br><span class="line"><span class="keyword">import</span> charactor.APHero;</span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestGeneric</span> </span>&#123;</span><br><span class="line">   </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">          </span><br><span class="line">        ArrayList&lt;APHero&gt; apHeroList = <span class="keyword">new</span> ArrayList&lt;APHero&gt;();</span><br><span class="line">        apHeroList.add(<span class="keyword">new</span> APHero());</span><br><span class="line">         </span><br><span class="line">        ArrayList&lt;? extends Hero&gt; heroList = apHeroList;</span><br><span class="line">          </span><br><span class="line">        <span class="comment">//? extends Hero 表示这是一个Hero泛型的子类泛型</span></span><br><span class="line">          </span><br><span class="line">        <span class="comment">//heroList 的泛型可以是Hero</span></span><br><span class="line">        <span class="comment">//heroList 的泛型可以使APHero</span></span><br><span class="line">        <span class="comment">//heroList 的泛型可以使ADHero</span></span><br><span class="line">          </span><br><span class="line">        <span class="comment">//可以确凿的是，从heroList取出来的对象，一定是可以转型成Hero的</span></span><br><span class="line">          </span><br><span class="line">        Hero h= heroList.get(<span class="number">0</span>);</span><br><span class="line">          </span><br><span class="line">        <span class="comment">//但是，不能往里面放东西</span></span><br><span class="line">        heroList.add(<span class="keyword">new</span> ADHero()); <span class="comment">//编译错误，因为heroList的泛型 有可能是APHero</span></span><br><span class="line">          </span><br><span class="line">    &#125;</span><br><span class="line">      </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>当然也可以不用这种方式:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;Hero&gt; Heros = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">Heros.add(<span class="keyword">new</span> APHero());</span><br></pre></td></tr></table></figure>

<p>这样子也能添加,但取出来的是Hero.</p>
<h3 id="super"><a href="#super" class="headerlink" title="? super"></a>? super</h3><p>ArrayList heroList&lt;? super Hero&gt;表示这是一个Hero泛型或者其父类泛型</p>
<p>heroList的泛型可能是Hero</p>
<p>heroList的泛型可能是Object.</p>
<p>可以往里面插入Hero以及Hero的子类,但是取出来有风险，因为不确定取出来是Hero还是Object.这和上面的代码效果一样.</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li>如果希望只取出，不插入，就使用? extends Hero</li>
<li>如果希望只插入，不取出，就使用? super Hero</li>
<li>如果希望，又能插入，又能取出，就不要用通配符？ </li>
</ul>
<p>阅读过一些Java集合类的源码，可以发现通常我们会将两者结合起来一起用，比如像下面这样：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Collections</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">copy</span><span class="params">(List&lt;? <span class="keyword">super</span> T&gt; dest, List&lt;? extends T&gt; src)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;src.size(); i++)</span><br><span class="line">            dest.set(i, src.get(i));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="有限制泛型"><a href="#有限制泛型" class="headerlink" title="有限制泛型"></a>有限制泛型</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">T</span> <span class="keyword">extends</span> <span class="title">Comparable</span>&lt;<span class="title">T</span>&gt;&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> T data;</span><br><span class="line">    <span class="keyword">private</span> Node&lt;T&gt; next;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Node</span><span class="params">(T data, Node&lt;T&gt; next)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.data = data;</span><br><span class="line">        <span class="keyword">this</span>.next = next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">getData</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> data; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>类型参数T是有限制的,要求T必须继承Comparable接口,并且这个接口也是被泛型化的.</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java正则表达式</title>
    <url>/2018/08/09/Java%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
    <content><![CDATA[<p>测试内容<br>测试内容<br>测试内容<br>测试内容<br>测试内容</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java的JDBC</title>
    <url>/2018/08/18/Java%E7%9A%84JDBC/</url>
    <content><![CDATA[<p>用Java操作数据库.</p>
<a id="more"></a>

<h1 id="前提工作"><a href="#前提工作" class="headerlink" title="前提工作"></a>前提工作</h1><h2 id="为当前项目添加驱动"><a href="#为当前项目添加驱动" class="headerlink" title="为当前项目添加驱动"></a>为当前项目添加驱动</h2><p>IDEA有内置的数据库工具(貌似ultimate版才有),可以直接在IDEA中像用SSMS,workbench,Navicat等工具一样操作数据库,这个工具也有独立版,叫DataGrip.使用这个工具时,只要注意密码用户名,端口号,而且它会提示下载驱动.</p>
<p><img src="/2018/08/18/Java%E7%9A%84JDBC/%E5%B7%A5%E5%85%B7.png" alt></p>
<p>然后把这个下下来的驱动包加入当前项目,File&gt;Project Structure&gt;Dependencies,点加号加进去.</p>
<h2 id="注册驱动-建立连接"><a href="#注册驱动-建立连接" class="headerlink" title="注册驱动,建立连接"></a>注册驱动,建立连接</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.SQLException;</span><br><span class="line"><span class="comment">//注意不要导错包  </span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestJDBC</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">//向DriverManager注册驱动</span></span><br><span class="line">            Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">  </span><br><span class="line">            <span class="comment">// 建立与数据库的Connection连接</span></span><br><span class="line">            <span class="comment">// 这里需要提供：</span></span><br><span class="line">            <span class="comment">// 数据库所处于的ip:127.0.0.1 (本机)</span></span><br><span class="line">            <span class="comment">// 数据库的端口号： 3306 （mysql专用端口号）</span></span><br><span class="line">            <span class="comment">// 数据库名称 how2java</span></span><br><span class="line">            <span class="comment">// 编码方式 UTF-8</span></span><br><span class="line">            <span class="comment">// 账号 root</span></span><br><span class="line">            <span class="comment">// 密码 admin</span></span><br><span class="line">  </span><br><span class="line">            Connection c = DriverManager</span><br><span class="line">                    .getConnection(</span><br><span class="line">                            <span class="string">"jdbc:mysql://127.0.0.1:3306/how2java?useSSL=false"</span>,</span><br><span class="line">                            <span class="string">"root"</span>, <span class="string">"admin"</span>);</span><br><span class="line">  </span><br><span class="line">            System.out.println(<span class="string">"连接成功，获取连接对象： "</span> + c);</span><br><span class="line">  </span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">  </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>useSSL=false</code>这里我没有设置语言,因为我的MySQL默认就是UTF-8,而设置SSL为false是避免等会的警告.</p>
<h1 id="一些SQL的知识"><a href="#一些SQL的知识" class="headerlink" title="一些SQL的知识"></a>一些SQL的知识</h1><h2 id="存储机制"><a href="#存储机制" class="headerlink" title="存储机制"></a>存储机制</h2><p>MySQL数据库通常支持如下两种存储机制。 </p>
<ul>
<li>MylSAM：这是MySQL早期默认的存储机制，对事务支持不够好。</li>
<li>InnoDB：InnoDB提供事务安全的存储机制。InnoDB通过建立行级锁来保证事务完整性，并以Oracle风格的共享锁来处理Select语句。系统默认启动InnoDB存储机制，如果不想使用InnoDB表，则可以使用skip-innodb选项。</li>
</ul>
<p>对比两种存储机制，不难发现InnoDB比MylSAM多了事务支持的功能，而事务支持是Java EE最重要的特性，因此通常推荐使用InnoDB存储机制。如果使用5.0以上版本的MySQL数据库系统，通 常无须指定数据表的存储机制，因为系统默认使用InnoDB存储机制。如果需要在建表时显式指定存储机制，则可在标准建表语法的后面添加下面任意一句。</p>
<p>ENGINE=MyISAM—— 强制使用MylSAM存储机制。</p>
<p>ENGFNE=InnoDB—— 强制使用InnoDB存储机制。</p>
<h2 id="SQL语句基础"><a href="#SQL语句基础" class="headerlink" title="SQL语句基础"></a>SQL语句基础</h2><p>标准的SQL语句通常可分为如下几种类型。</p>
<ul>
<li>查询语句：主要由select关键字完成，查询语句是SQL语句中最复杂、功能最丰富的语句。</li>
<li>DML （Data Manipulation Language,数据操作语言）语句：主要由 insert、update 和 delete 三个<br>关键字完成。</li>
<li>DDL （Data Definition Language,数据定义语言）语句：主要由 create、alter、drop 和 truncate<br>四个关键字完成。</li>
<li>DCL （Data Control L anguage,数据控制语言）语句：主要由grant和 revoke两个关键字完成。</li>
<li>事务控制语句：主要由commit、rollback和 savepoint三个关键字完成。</li>
</ul>
<p>在 SQL命令中也可能需要使用标识符，标识符可用于定义表名、列名，也可用于定义变量等。这 些标识符的命名规则如下。</p>
<ul>
<li><p>标识符通常必须以字母开头。</p>
</li>
<li><p>标识符包括字母、数字和三个特殊字符（#_$）。</p>
</li>
<li><p>不要使用当前数据库系统的关键字、保留字，通常建议使用多个单词连缀而成，单词之间以_<br>分隔。</p>
</li>
<li><p>同一个模式下的对象不应该同名，这里的模式指的是外模式。</p>
<p>什么是外模式?</p>
<blockquote>
<p><img src="/2018/08/18/Java%E7%9A%84JDBC/%E6%A8%A1%E5%BC%8F.png" alt></p>
<p>作者：刘慰</p>
<p>链接：<a href="https://www.zhihu.com/question/38737183/answer/93294527" target="_blank" rel="noopener">https://www.zhihu.com/question/38737183/answer/93294527</a></p>
<p>来源：知乎</p>
<p>著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。</p>
<p>这张图表示数据库管理系统（DBMS）从三个层次来管理数据：外部层次（External Level）、概念层次（Conceptual Level）和内部层次（Internal Level）。其中外部层次的数据是用户所看到的数据，所以又叫『用户』层次；概念层次的数据是DBMS中全局管理（可以简单地理解为所有的）数据，及数据之间的约束，所以又叫『逻辑』层次；内部层次的数据是存储在介质上的数据，包括存储路径、存储方式、索引方式等，所以又叫『物理』层次。</p>
<p>而『模式』，就是『对于数据的结构性描述』，所以对于不同层次的数据，就对应着不同层次的模式，分别是：外模式（用户模式）、模式（概念模式/逻辑模式）与内模式（物理模式）。</p>
<p>在一个数据库系统中，『模式』与『内模式』都只能有一个，但『外模式』可以有很多个。原因是『模式』是对DBMS中所有数据的全局性的结构描述，所以只能有一种形式。举个例子：一个学校的数据库中要存放所有学生、课程与选课记录的信息。我们可以用三张表：学生信息表、课程信息表与选课记录表来存放所有的数据。但是存放学生的数据的时候，我们只能够选取某一种特定的结构对学生的信息进行描述，而不能多种不同的结构共存（比如两张不同结构的学生表，存放的都是所有学生的信息）。所以『模式』只能有一个。同样，我们在介质上存放数据的时候，也只能选取某一种特定的存储路径（数据文件在哪个文件夹下）、存储方式（二进制/文本）、索引方式（聚簇索引，普通索引）等等，可以理解为一种数据结构，和具体的计算机语言没什么关系，也不能够像程序一样执行。</p>
<p>至于『外模式』，是以『视图』的形式展现给用户的，所以我猜你对于外模式理解错误的原因，就是理解错误了『视图』的概念。这里的『视图』不是我们平时说的 UI ，而是指数据库中的视图，又叫『虚表』，是相对于『基本表』来说的。这里的『基本表』指的就是我们在建立数据库的时候需要创建的那些数据表（用 CREATE TABLE 创建出来的），而视图，是对这些基本表中的数据的某种展现形式，是这些数据的一个子集，但不限于某张表中的数据的子集，也可以是多张表联合起来进行显示的一组数据。比如在一个学校的教务系统中，我们希望每个学生只能够看到自己的成绩，普通老师可以看到他/她教的学生的成绩，系主任可以看到这个系中所有学生的成绩，校长可以看到学校中每个学生的成绩。那么我们可以基于所有学生的成绩数据，针对于不同权限的用户选取不同的子集，这个子集就是所谓的『视图』。但需要注意的是，『视图』本身并不『实际』存放数据，而只是对于如何选取数据的一种描述，这也是所谓的『虚』。当全局数据改变的时候，视图中的数据也会随之改变。通过外模式，可以让不同的用户（包括应用程序）看到他们希望看到的（比如可以同时看到学生本人的信息及他/她的所有成绩与对应的课程的信息，而这些数据实际上是分别存在于不同的数据表中的），或是我们希望他们看到的数据（通过不同的子集来实现）。</p>
<p>总而言之，三种层次的模式都是对于数据的描述，只不过描述的层次不同。</p>
</blockquote>
</li>
</ul>
<h3 id="DDL语句"><a href="#DDL语句" class="headerlink" title="DDL语句"></a>DDL语句</h3><p>DDL语句是操作数据库对象的语句，包括创建(create),删除(drop)和修改(alter)数据库对象。最基本的数据库对象是数据表，数据表是存储数据的逻辑单元。但数据库里绝不仅包括数据表，数据库里可包含如下表所示的几种常见的数据库对象。</p>
<p><img src="/2018/08/18/Java%E7%9A%84JDBC/DDL.png" alt></p>
<p>1.创建表的语法</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> 表名</span><br><span class="line">(</span><br><span class="line">	columnName datatype,</span><br><span class="line">    ...</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>2.修改表的语法</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">--增加列定义</span></span><br><span class="line"><span class="keyword">alter</span> <span class="keyword">table</span> 表名</span><br><span class="line"><span class="keyword">add</span></span><br><span class="line">(</span><br><span class="line">	columnName datatype,</span><br><span class="line">    ...</span><br><span class="line">);</span><br><span class="line"><span class="comment">--修改列定义</span></span><br><span class="line"><span class="keyword">alter</span> <span class="keyword">table</span> 表名</span><br><span class="line"><span class="keyword">modify</span> columnName datatype;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">add的必须是原表不存在的,modify的必须是原表存在的</span></span><br><span class="line"><span class="comment">MySQL的modify不支持一次修改多个表,Oracle的可以,只要像add一样用括号将多个字段包起来</span></span><br><span class="line"><span class="comment">MySQL的想要修改多个就要多个modify</span></span><br><span class="line"><span class="comment">modify时注意修改后的字段属性不能和已存在的数据冲突</span></span><br><span class="line"><span class="comment">修改默认值只会修改新增记录的默认值,原来的没有影响</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="comment">--删除列</span></span><br><span class="line"><span class="keyword">alter</span> <span class="keyword">table</span> 表名</span><br><span class="line"><span class="keyword">drop</span> columnName;</span><br><span class="line"><span class="comment">--MySQL支持的重命名表</span></span><br><span class="line"><span class="keyword">alter</span> <span class="keyword">table</span> 表名</span><br><span class="line"><span class="keyword">rename</span> <span class="keyword">to</span> 新表名;</span><br><span class="line"><span class="comment">--MySQL支持的重命名列名</span></span><br><span class="line"><span class="keyword">alter</span> <span class="keyword">table</span> 表名</span><br><span class="line"><span class="keyword">change</span> old_column_name new_column_name datatype;</span><br></pre></td></tr></table></figure>

<p>3.删除表的语法</p>
<p><code>drop table 表名</code></p>
<p>4.truncate表</p>
<p><code>truncate 表名</code>可以清空表的数据,但是保留了表的结构.</p>
<p><em>在information_scheme数据库中的tables表保存了数据库实例中的所有数据表,可以查询该表来获取表信息.</em></p>
<h3 id="数据库约束"><a href="#数据库约束" class="headerlink" title="数据库约束"></a>数据库约束</h3><p>大部分数据库支持下面5种完整性约束。</p>
<ul>
<li><p>NOT NULL：非空约束，指定某列不能为空。</p>
</li>
<li><p>UNIQUE：唯一约束，指定某列或者几列组合不能重复。</p>
</li>
<li><p>PRIMARY KEY：主键，指定该列的值可以唯一地标识该条记录。</p>
</li>
<li><p>FOREIGN KEY：外键，指定该行记录从属于主表中的一条记录，主要用于保证参照完整性。</p>
</li>
<li><p>CHECK：检查，指定一个布尔表达式，用于指定对应列的值必须满足该表达式。(MySQL不支持)</p>
<p><em>可以在information_schema数据库的TABLE_CONSTRAINTS表查看约束信息</em></p>
</li>
</ul>
<h3 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h3><p>索引通过快速路径访问方法来快速定位数据,加速查询.索引从属于数据表,和数据表一样属于数据库对象,不能独立存在,必须属于某个表.</p>
<p>创建索引有两种方式。</p>
<ul>
<li>自动：当在表上定义主键约束、唯一约束和外键约束时，系统会为该数据列自动创建对应的索<br>引。</li>
<li>手动：用户可以通过create index…语句来创建索引。</li>
</ul>
<p>删除索引也有两种方式。</p>
<ul>
<li>自动：数据表被删除时，该表上的索引自动被删除。</li>
<li>手动：用户可以通过drop index…语句来删除指定数据表上的指定索引。</li>
</ul>
<p>索引的作用类似于书的目录，几乎没有一本书没有目录，因此几乎没有一个表没有索引。一个表中可以有多个索引列，每个索引都可用于加速该列的查询速度。<br>正如书的目录总是根据书的知识点来建立一样,因为读者经常要根据知识点来查阅一本书。类似的，通常为经常需要查询的数据列建立索引，可以在一列或者多列上创建索引。创建索引的语法格式如下：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">index</span> index_name</span><br><span class="line"><span class="keyword">on</span> table_name (<span class="keyword">column</span>[,<span class="keyword">column</span>]...);</span><br></pre></td></tr></table></figure>

<p>索引也会带来两点坏处,1.每当数据表发生变化时,数据库系统需要维护索引,因此会有一定开销,2.索引本身也要占地方</p>
<h3 id="视图"><a href="#视图" class="headerlink" title="视图"></a>视图</h3><p>视图其实更像是向外展示数据的手段,它是将数据库中的一些数据查询出来给人看.是一个便利的展示数据集的手段.</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">or</span> <span class="keyword">replace</span> <span class="keyword">view</span> 视图名</span><br><span class="line"><span class="keyword">as</span></span><br><span class="line">subquery;</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">or</span> <span class="keyword">replace</span> <span class="keyword">view</span> view_test</span><br><span class="line"><span class="keyword">as</span></span><br><span class="line"><span class="keyword">select</span> tescher_name,class_name <span class="keyword">from</span> school_table</span><br><span class="line"><span class="keyword">with</span> <span class="keyword">check</span> <span class="keyword">option</span>;<span class="comment">--强制只读视图数据</span></span><br></pre></td></tr></table></figure>

<h3 id="DML语句"><a href="#DML语句" class="headerlink" title="DML语句"></a>DML语句</h3><p>DML语句由insert into、update 和delete from三个命令组成。</p>
<p>比较简单,略过.</p>
<h3 id="单表查询"><a href="#单表查询" class="headerlink" title="单表查询"></a>单表查询</h3><p><em>不少内容都在数据库课讲过,下面只简明的提一下</em></p>
<p>1.concat用于将查询出来的结果拼接起来,as用于给结果的列起个别名(或者用空格).对于MySQL来说,如果在算数表达式中使用null,整个表达式结果就是null</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">	<span class="keyword">CONCAT</span>( <span class="string">`name`</span>, hp ) <span class="keyword">AS</span> <span class="keyword">DATA</span> </span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">	<span class="string">`jdbctest`</span></span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> teacher_id MY_ID , <span class="keyword">concat</span> (teacher_name , teacher_id) teacher</span><br></pre></td></tr></table></figure>

<p>也可以给表起别名. </p>
<p>2.like和下划线(一个字符),百分号(任意个字符)实现模糊查询,还有正则模式</p>
<p>3.order by和desc降序排列</p>
<h3 id="数据库函数"><a href="#数据库函数" class="headerlink" title="数据库函数"></a>数据库函数</h3><p>函数被分为单行函数和多行函数，单行函数对每行输入值单独计算，每行得 到一个计算结果返回给用户；多行函数对多行输入值整体计算，最后只会得到一个结果。</p>
<h4 id="单行函数"><a href="#单行函数" class="headerlink" title="单行函数"></a>单行函数</h4><ul>
<li>单行函数的参数可以是变量、常量或数据列。单行函数可以接收多个参数，但<strong>只返回一个值</strong>。</li>
<li>单行函数会对每行单独起作用，每行(可能包含多个参数)返回一个结果。</li>
<li>使用单行函数可以改变参数的数据类型。单行函数支持嵌套使用，即内层函数的返回值是外层函数的参数。</li>
</ul>
<p>大致分为以下几类:</p>
<ul>
<li>日期时间函数</li>
<li>数值函数</li>
<li>字符函数</li>
<li>转换函数</li>
<li>其他函数<ul>
<li>位函数</li>
<li>流程控制函数</li>
<li>加密解密函数</li>
<li>信息函数</li>
</ul>
</li>
</ul>
<p>一些例子:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">--选出teacher_table表中teacher_name列的字符长度</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">char_length</span>(teacher_name) <span class="keyword">from</span> teacher_table;</span><br><span class="line"><span class="comment">--计算teacher_name列的字符长度的sin值</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">sin</span>(<span class="keyword">char_length</span>(teacher_name))</span><br><span class="line"><span class="keyword">from</span> teacher_table;</span><br><span class="line"><span class="comment">--计算1.57的Sin值，约等于1</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">sin</span>(<span class="number">1.57</span>);</span><br><span class="line"><span class="comment">--为指定日期添加一定的时间</span></span><br><span class="line"><span class="comment">--在这种用法下interval是关键字，需要一个数值，还需要一个单位</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">DATE_ADD</span>(<span class="string">'1998-01-02'</span>, <span class="built_in">interval</span> <span class="number">2</span> <span class="keyword">MONTH</span>);</span><br><span class="line"><span class="comment">--这种用法更简单</span></span><br><span class="line"><span class="keyword">select</span> ADDDATE(<span class="string">'1998-01-02'</span>, <span class="number">3</span>);</span><br><span class="line"><span class="comment">--获取当前日期</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">CURDATE</span>();</span><br><span class="line"><span class="comment">--获取当前时间</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">curtime</span>();</span><br><span class="line"><span class="comment">--下面的MD5是MD5加密函数</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">MD5</span>(<span class="string">'testing'</span>);</span><br></pre></td></tr></table></figure>

<p>SQL提供了如下几个处理null的函数。</p>
<ul>
<li>ifnull(exprl，expr2 ):如果exprl为null,则返回expr2, 否则返回exprl。</li>
<li>nullif(exprl，expr2):如果erprl和expr2相等，则返回null，否则返回exprl。</li>
<li>if(exprl,expr2,expr3)：有点类似于？:三目运算符，如果exprl为true ,不等于0，且不等于null, 则返回expr2, 否则返回expr3。</li>
<li>isnull(exprl)：判断exprl是否为null,如果为null则返回true ,否则返回false。</li>
</ul>
<p>另一个case函数:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">case value </span><br><span class="line">when compare_valuel then resultl </span><br><span class="line">when compare_value2 then result2</span><br><span class="line">...</span><br><span class="line">else result <span class="keyword">end</span></span><br></pre></td></tr></table></figure>

<p>case函数用value和后面的compare_valuel、compare_value2、…依次进行比较，如果value和指定的 compare valuel相等，则返回对应的result1, 否则返回else后的result。例如如下SQL语句：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> student_name , <span class="keyword">case</span> java_teacher </span><br><span class="line"><span class="keyword">when</span> <span class="number">1</span> <span class="keyword">then</span> <span class="string">'Java 老师'</span> </span><br><span class="line"><span class="keyword">when</span> <span class="number">2</span> <span class="keyword">then</span> <span class="string">'Ruby 老师'</span> </span><br><span class="line"><span class="keyword">else</span> <span class="string">'其他老师’</span></span><br><span class="line"><span class="string">end from student_table;</span></span><br></pre></td></tr></table></figure>

<p>在第二种用法中，condition1, condition2都是一个返回boolean值的条件表达式，因此这种用法更<br>加灵活。例如如下SQL语句：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">	*,</span><br><span class="line"><span class="keyword">CASE</span></span><br><span class="line">	<span class="keyword">WHEN</span> hp &lt; <span class="number">410</span> <span class="keyword">THEN</span></span><br><span class="line">	<span class="string">'low_hp'</span> <span class="keyword">ELSE</span> <span class="string">'high_hp'</span> </span><br><span class="line"><span class="keyword">END</span> <span class="keyword">AS</span> <span class="keyword">LEVEL</span> </span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">	<span class="string">`jdbctest`</span>;</span><br></pre></td></tr></table></figure>

<h4 id="分组和组函数-多行函数"><a href="#分组和组函数-多行函数" class="headerlink" title="分组和组函数(多行函数)"></a>分组和组函数(多行函数)</h4><p>组函数也就是前面提到的多行函数，组函数将一组记录作为整体计算，每组记录返回一个结果，而 不是每条记录返回一个结果.常用的组函数有如下5个。</p>
<ul>
<li><p>avg([distinct|all]expr)：计算多行expr的平均值，其中，expr可以是变量、常量或数据列，但其<br>数据类型必须是数值型。还可以在变量、列前使用distinct或 all关键字，如果使用distinct,则表明不计算重复值；all用和不用的效果完全一样，表明需要计算重复值.</p>
</li>
<li><p>count({ *|[distinct|all]expr})：计算多行expr的总条数，其中，expr可以是变量、常量或数据列，<br>其数据类型可以是任意类型；用星号(*)表示统计该表内的记录行数；distinct表示不计算重复值。</p>
</li>
<li><p>max(expr)：计算多行expr的最大值，其中expr可以是变量、常量或数据列，其数据类型可以 是任意类型。</p>
</li>
<li><p>min(expr)：计算多行expr的最小值，其中expr可以是变量、常量或数据列，其数据类型可以是 任意类型。</p>
</li>
<li><p>sum([distinct|all]expr): 计算多行expr的总和，其中，expr可以是变量、常量或数据列，但其数据类型必须是数值型；distinct表示不计算重复值。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span></span><br><span class="line">	<span class="keyword">avg</span>( <span class="keyword">ifnull</span>( info, <span class="number">0</span> ))<span class="comment">--mysql中null会导致表达式结果为null</span></span><br><span class="line"><span class="keyword">from</span></span><br><span class="line">	jdbctest</span><br><span class="line"><span class="keyword">where</span></span><br><span class="line">	<span class="keyword">id</span>&lt;<span class="number">6</span>;</span><br><span class="line"><span class="keyword">select</span></span><br><span class="line">	hp,<span class="keyword">count</span>(*)<span class="comment">--查询hp字段,统计出表格的行数,按照hp分组,且升序排列</span></span><br><span class="line"><span class="keyword">from</span></span><br><span class="line">	jdbctest</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span></span><br><span class="line">	hp <span class="keyword">asc</span>;</span><br></pre></td></tr></table></figure>

<p>对分组的结果进行过滤:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span></span><br><span class="line">	hp,</span><br><span class="line">	<span class="keyword">count</span>( hp )</span><br><span class="line"><span class="keyword">from</span></span><br><span class="line">	jdbctest</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span></span><br><span class="line">	hp <span class="keyword">asc</span></span><br><span class="line"><span class="keyword">having</span></span><br><span class="line">	<span class="keyword">count</span>( hp )&gt;= <span class="number">2</span>;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="多表连接查询"><a href="#多表连接查询" class="headerlink" title="多表连接查询"></a>多表连接查询</h4><p>需要选择的数据并不是来自一个表，而是来自多个数据表，这就需要使用多表连接查询。</p>
<p>多表连接查询有两种规范，较早的SQL92规范支持如下几种多表连接查询。</p>
<ul>
<li>等值连接。</li>
<li>非等值连接。</li>
<li>外连接。</li>
<li>广义笛卡儿积。</li>
</ul>
<p>SQL99规范提供了可读性更好的多表连接语法，并提供了更多类型的连接查询。SQL99支持如下几种多表连接查询。</p>
<ul>
<li>交叉连接。</li>
<li>自然连接。</li>
<li>使用using子句的连接。</li>
<li>使用on子句的连接。</li>
<li>全外连接或者左、右外连接。</li>
</ul>
<ol>
<li><p>SQL92的连接查询<br>SQL92的多表连接语法比较简洁，这种语法把多个数据表都放在from之后，多个表之间以逗号隔 开；连接条件放在where之后，与查询条件之间用and逻辑运算符连接。如果连接条件要求两列值相等， 则称为等值连接，否则称为非等值连接；如果没有任何连接条件，则称为广义笛卡儿积。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span></span><br><span class="line">	jdbctest.*,</span><br><span class="line">	jdbctest1.name</span><br><span class="line"><span class="keyword">from</span></span><br><span class="line">	jdbctest,</span><br><span class="line">	jdbctest1</span><br><span class="line"><span class="keyword">where</span></span><br><span class="line">	jdbctest.id = jdbctest1.id;</span><br></pre></td></tr></table></figure>

<p>大致的流程是:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> t in jdbctest</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">for</span> s in jdbctest1</span><br><span class="line">    &#123;</span><br><span class="line">    	<span class="keyword">if</span> (jdbctest.id = jdbctest1.id)</span><br><span class="line">    	sout s+t</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果没有where后面的条件,得到的就是广义笛卡尔积.</p>
</li>
<li><p>SQL99的连接查询</p>
<p>交叉连接(cross join )：交叉连接效果就是SQL92中的广义笛卡儿积，所以交叉连接无须任何连接条件.</p>
<p>自然连接(natural join ) : 自然连接表面上看起来也无须指定连接条件，但自然连接是有连接条件的，自然连接会以两个表中的同名列作为连接条件；如果两个表中没有同名列，则自然连接与交叉连接效果完全一样—因为没有连接条件。</p>
<p>using子句连接：using子句可以指定一列或多列，用于显式指定两个表中的同名列作为连接条件。假设两个表中有超过一列的同名列，如果使用natural join ,则会把所有的同名列当成连接条件；使用using子句，就可显式指定使用哪些同名列作为连接条件。</p>
<p>on子句连接：这是最常用的连接方式，SQL 99语法的连接条件放在on子句中指定，而且每个 on子句只指定一个连接条件。这意味着：如果需要进行N表连接，则需要有N-1个join…on对。</p>
<p>剩下的左外连接,右外连接,全连接</p>
<ul>
<li><p>LEFT JOIN关键字会从左表那里返回所有的行，即使在右表中没有匹配的行。</p>
</li>
<li><p>RIGHT JOIN 关键字会右表那里返回所有的行，即使在左表中没有匹配的行。</p>
</li>
<li><p>只要其中某个表存在匹配，FULL JOIN 关键字就会返回行。</p>
</li>
</ul>
</li>
</ol>
<h4 id="子查询"><a href="#子查询" class="headerlink" title="子查询"></a>子查询</h4><p>子查询就是指在查询语句中嵌套另一个査询，子查询可以支持多层嵌套。对于一个普通的查询语句而言，子查询可以出现在两个位置。</p>
<ul>
<li>出现在from语句后当成数据表，这种用法也被称为行内视图，因为该子查询的实质就是一个临时视图。</li>
<li>出现在where条件后作为过滤条件的值。</li>
</ul>
<h3 id="集合运算"><a href="#集合运算" class="headerlink" title="集合运算"></a>集合运算</h3><p>select语句查询的结果是一个包含多条数据的结果集，类似于数学里的集合，可以进行交(intersect)、 并 (union)和差 (minus)运算，select查询得到的结果集也可能需要进行这三种运算。 为了对两个结果集进行集合运算，这两个结果集必须满足如下条件。</p>
<ul>
<li>两个结果集所包含的数据列的数量必须相等。</li>
<li>两个结果集所包含的数据列的数据类型也必须一一对应</li>
</ul>
<p>实际上MySQL只支持union运算,其他两种都只能用子查询或多表连接的方式实现.</p>
<h1 id="创建语句"><a href="#创建语句" class="headerlink" title="创建语句"></a>创建语句</h1><p>在连接后创建相应的SQL语句:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Statement s = c.createStatement();</span><br><span class="line">String sql = <span class="string">"insert into jdbctest values(2,'ye',15,'C1')"</span>;</span><br><span class="line">s.execute(sql);</span><br></pre></td></tr></table></figure>

<h1 id="关闭连接"><a href="#关闭连接" class="headerlink" title="关闭连接"></a>关闭连接</h1><p>数据库的连接是有限资源，相关操作结束后，养成关闭数据库的好习惯</p>
<p>先关闭Statement</p>
<p>后关闭Connection </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.SQLException;</span><br><span class="line"><span class="keyword">import</span> java.sql.Statement;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestJDBC</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Connection c = <span class="keyword">null</span>;</span><br><span class="line">        Statement s = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line"></span><br><span class="line">            c = DriverManager</span><br><span class="line">                    .getConnection(</span><br><span class="line">                            <span class="string">"jdbc:mysql://127.0.0.1:3306/dailytest?useSSL=false"</span>,</span><br><span class="line">                            <span class="string">"root"</span>, <span class="string">"******"</span>);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 注意：使用的是 java.sql.Statement</span></span><br><span class="line">            <span class="comment">// 不要不小心使用到： com.mysql.jdbc.Statement;</span></span><br><span class="line">            s = c.createStatement();</span><br><span class="line"></span><br><span class="line">            String sql = <span class="string">"insert into jdbctest values(2,'ye',15,'C1')"</span>;</span><br><span class="line">            s.execute(sql);</span><br><span class="line"></span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException | SQLException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (s != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    s.close();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (c != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    c.close();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>或者用try(){}风格:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.SQLException;</span><br><span class="line"><span class="keyword">import</span> java.sql.Statement;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestJDBC</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> (Connection c = DriverManager</span><br><span class="line">                .getConnection(</span><br><span class="line">                        <span class="string">"jdbc:mysql://127.0.0.1:3306/dailytest?useSSL=false"</span>,</span><br><span class="line">                        <span class="string">"root"</span>, <span class="string">"******"</span>);</span><br><span class="line">             Statement s = c.createStatement();) &#123;</span><br><span class="line">            String sql = <span class="string">"insert into jdbctest values(2,'ye',15,'C1')"</span>;</span><br><span class="line">            s.execute(sql);</span><br><span class="line"></span><br><span class="line">        &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="练习"><a href="#练习" class="headerlink" title="练习"></a>练习</h1><p>向DailyTest数据库的JDBCtest表插入一百个hero记录.</p>
<p><code>truncate table dailytest.jdbctest</code>使用这条命令可以清空jdbctest表的数据,便于测试.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.SQLException;</span><br><span class="line"><span class="keyword">import</span> java.sql.Statement;</span><br><span class="line"><span class="keyword">import</span> java.util.Random;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestJDBC</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> (Connection c = DriverManager</span><br><span class="line">                .getConnection(</span><br><span class="line">                        <span class="string">"jdbc:mysql://127.0.0.1:3306/dailytest?useSSL=false"</span>,</span><br><span class="line">                        <span class="string">"root"</span>, <span class="string">"******"</span>);</span><br><span class="line">             Statement s = c.createStatement()) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">                <span class="keyword">int</span> maxh = <span class="number">450</span>;</span><br><span class="line">                <span class="keyword">int</span> minh = <span class="number">350</span>;</span><br><span class="line">                <span class="keyword">int</span> maxd = <span class="number">80</span>;</span><br><span class="line">                <span class="keyword">int</span> mind = <span class="number">30</span>;</span><br><span class="line">                String name = <span class="string">"英雄"</span> + i;</span><br><span class="line">                <span class="keyword">int</span> hp = <span class="keyword">new</span> Random().nextInt(maxh) % (maxh - minh + <span class="number">1</span>) + minh;</span><br><span class="line">                <span class="keyword">int</span> damage = <span class="keyword">new</span> Random().nextInt(maxd) % (maxd - mind + <span class="number">1</span>) + minh;</span><br><span class="line">                s.execute(<span class="string">"insert into jdbctest values(null,'"</span> + name + <span class="string">"','"</span> + hp + <span class="string">"','"</span> + damage + <span class="string">"')"</span>);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意:在SQL语句中使用Java变量要这样写:<code>&#39;&quot; + name + &quot;&#39;</code>.</p>
<h2 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h2><p>查询不能使用Statement的execute方法,因为查询需要返回查询的结果.使用Statement的executeQuery方法,将结果返回给一个ResultSet对象.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestRetrieve</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> (Connection c = DriverManager</span><br><span class="line">                .getConnection(</span><br><span class="line">                        <span class="string">"jdbc:mysql://127.0.0.1:3306/dailytest?useSSL=false"</span>,</span><br><span class="line">                        <span class="string">"root"</span>, <span class="string">"******"</span>);</span><br><span class="line">             Statement s = c.createStatement()) &#123;</span><br><span class="line">            ResultSet rs = s.executeQuery(<span class="string">"select * from jdbctest where hp regexp '.[345].'"</span>);</span><br><span class="line">            <span class="keyword">while</span> (rs.next()) &#123;</span><br><span class="line">                <span class="keyword">int</span> id = rs.getInt(<span class="string">"ID"</span>);</span><br><span class="line">                String name = rs.getString(<span class="number">2</span>);</span><br><span class="line">                <span class="keyword">int</span> hp = rs.getInt(<span class="number">3</span>);</span><br><span class="line">                <span class="keyword">int</span> damage = rs.getInt(<span class="number">4</span>);</span><br><span class="line">                System.out.printf(<span class="string">"%d\t%s\t%d\t%d%n"</span>, id, name, hp, damage);</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println(<span class="string">"exit"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>&quot;select * from jdbctest where hp regexp &#39;.[345].&#39;&quot;</code>,这里我是用了MySQL的正则模式,表示查出hp为三位数,且中间数字是3,4,5的字段的全部信息.</p>
<p><strong>注意</strong>:这里<code>rs.getXxx</code>是从1开始算的.</p>
<h3 id="判断密码是否正确"><a href="#判断密码是否正确" class="headerlink" title="判断密码是否正确"></a>判断密码是否正确</h3><p>判断密码问题应该是在某张表中找是否有符合的记录,而不是将数据读出来比较.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestJDBC</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> (Connection c = DriverManager</span><br><span class="line">                .getConnection(</span><br><span class="line">                        <span class="string">"jdbc:mysql://127.0.0.1:3306/dailytest?useSSL=false"</span>,</span><br><span class="line">                        <span class="string">"root"</span>, <span class="string">"******"</span>);</span><br><span class="line">             Statement s = c.createStatement()) &#123;</span><br><span class="line">            String name = <span class="string">"aaa"</span>;</span><br><span class="line">            String password = <span class="string">"password"</span>;</span><br><span class="line">            ResultSet rs = s.executeQuery(<span class="string">"select * from jdbctest1 where name='"</span> + name + <span class="string">"'and password='"</span> + password + <span class="string">"'"</span>);</span><br><span class="line">            <span class="keyword">if</span> (rs.next()) &#123;</span><br><span class="line">                System.out.println(<span class="string">"right"</span>);</span><br><span class="line">            &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">"wrong"</span>);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="使用预编译语句"><a href="#使用预编译语句" class="headerlink" title="使用预编译语句"></a>使用预编译语句</h1><p>当遇到有些需要重复使用某些结构相同的查询语句时,重复的编译会浪费大量时间,因此可以使用PreparedStatement,具体后面解释.PreparedStatement还有其他优势.</p>
<ul>
<li><p>避免了字符串的拼接</p>
<p>在前面的例子中我们可以看出,写SQL语句有个非常大的问题就是各种双引号,单引号套在一起很容易出错.而PreparedStatement可以使用占位符来替换.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String sql=<span class="string">"insert into hero values(null,?,?,?)"</span>;</span><br><span class="line"><span class="keyword">try</span>(Connection c=...</span><br><span class="line">   PreparedStatement ps=c.prepareStatement(sql))&#123;</span><br><span class="line">    ps.setInt(<span class="number">1</span>,<span class="string">"bbb"</span>);</span><br><span class="line">    ps.setString(<span class="number">2</span>,<span class="string">"password"</span>);</span><br><span class="line">    ps.execute();</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//这里设置参数是从1开始计算的,如果第一个字段是自增长的ID则不算做第一个,我这就是如此,那个null表示自增长ID</span></span><br></pre></td></tr></table></figure>


</li>
</ul>
<ul>
<li><p>性能优势</p>
<p>在前面的例子中:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">                <span class="keyword">int</span> maxh = <span class="number">450</span>;</span><br><span class="line">                <span class="keyword">int</span> minh = <span class="number">350</span>;</span><br><span class="line">                <span class="keyword">int</span> maxd = <span class="number">80</span>;</span><br><span class="line">                <span class="keyword">int</span> mind = <span class="number">30</span>;</span><br><span class="line">                String name = <span class="string">"英雄"</span> + i;</span><br><span class="line">                <span class="keyword">int</span> hp = <span class="keyword">new</span> Random().nextInt(maxh) % (maxh - minh + <span class="number">1</span>) + minh;</span><br><span class="line">                <span class="keyword">int</span> damage = <span class="keyword">new</span> Random().nextInt(maxd) % (maxd - mind + <span class="number">1</span>) + minh;</span><br><span class="line">                s.execute(<span class="string">"insert into jdbctest values(null,'"</span> + name + <span class="string">"','"</span> + hp + <span class="string">"','"</span> + damage + <span class="string">"')"</span>);</span><br><span class="line">            &#125;</span><br></pre></td></tr></table></figure>

<p>同样结构的语句每次都要编译处理一次,如果使用预编译语句,则在固定部分只要编译一次,每次只要将参数传给数据库,无需编译,网络传输量也更小.</p>
</li>
<li><p>防止SQL注入式攻击</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.*;</span><br><span class="line"><span class="keyword">import</span> java.util.Scanner;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestJDBC</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        String name;</span><br><span class="line">        String password;</span><br><span class="line">        Scanner scanner = <span class="keyword">new</span> Scanner(System.in);</span><br><span class="line">        name = scanner.nextLine();</span><br><span class="line">        password = scanner.nextLine();</span><br><span class="line">        <span class="keyword">try</span> (Connection c = DriverManager</span><br><span class="line">                .getConnection(</span><br><span class="line">                        <span class="string">"jdbc:mysql://127.0.0.1:3306/dailytest?useSSL=false"</span>,</span><br><span class="line">                        <span class="string">"root"</span>, <span class="string">"******"</span>);</span><br><span class="line">             Statement s = c.createStatement()) &#123;</span><br><span class="line">            ResultSet rs = s.executeQuery(<span class="string">"select * from jdbctest1 where name='"</span> + name + <span class="string">"'and password='"</span> + password + <span class="string">"'"</span>);</span><br><span class="line">            <span class="keyword">if</span> (rs.next()) &#123;</span><br><span class="line">                System.out.println(<span class="string">"right"</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">"wrong"</span>);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中,如果我输入<code>&#39;or true or&#39;</code>,SQL语句拼接后就是<code>select * from jdbctest1 where name=&#39;&#39; or trur or&#39;&#39; and password=&#39;&#39;</code>,这就是产生了一个必真条件,导致结果为right.</p>
</li>
</ul>
<h1 id="execute与executeUpdate的区别"><a href="#execute与executeUpdate的区别" class="headerlink" title="execute与executeUpdate的区别"></a>execute与executeUpdate的区别</h1><p>同:</p>
<p>execute与executeUpdate的相同点：都可以执行增加，删除，修改</p>
<p>异:</p>
<p> 不同1：<br>execute可以执行查询语句<br>然后通过getResultSet，把结果集取出来<br>executeUpdate不能执行查询语句<br> 不同2:<br>execute返回boolean类型，true表示执行的是查询语句，false表示执行的是insert,delete,update等等<br>executeUpdate返回的是int，表示有多少条数据受到了影响</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java中静态代码块、构造代码块、构造函数、普通代码块</title>
    <url>/2018/08/17/Java%E4%B8%AD%E9%9D%99%E6%80%81%E4%BB%A3%E7%A0%81%E5%9D%97%E3%80%81%E6%9E%84%E9%80%A0%E4%BB%A3%E7%A0%81%E5%9D%97%E3%80%81%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E3%80%81%E6%99%AE%E9%80%9A%E4%BB%A3%E7%A0%81%E5%9D%97/</url>
    <content><![CDATA[<p><strong>转载自：<a href="http://www.cnblogs.com/ysocean/p/8194428.html" target="_blank" rel="noopener">http://www.cnblogs.com/ysocean/p/8194428.html</a></strong> </p>
<p>在Java中，静态代码块、构造代码块、构造函数、普通代码块的执行顺序是一个笔试的考点，通过这篇文章希望大家能彻底了解它们之间的执行顺序。 </p>
<a id="more"></a>

<h1 id="1、静态代码块"><a href="#1、静态代码块" class="headerlink" title="1、静态代码块"></a>1、静态代码块</h1><h2 id="①、格式"><a href="#①、格式" class="headerlink" title="　　①、格式"></a>　　①、格式</h2><p>　　在java类中（方法中不能存在静态代码块）使用static关键字和{}声明的代码块：</p>
<h2 id="②、执行时机"><a href="#②、执行时机" class="headerlink" title="　　②、执行时机"></a>　　②、执行时机</h2><p>　　静态代码块在类被加载的时候就运行了，而且只运行一次，并且优先于各种代码块以及构造函数。如果一个类中有多个静态代码块，会按照书写顺序依次执行。后面在比较的时候会通过具体实例来证明。</p>
<h2 id="③、静态代码块的作用"><a href="#③、静态代码块的作用" class="headerlink" title="　　③、静态代码块的作用"></a>　　③、静态代码块的作用</h2><p>　　一般情况下，如果有些代码需要在项目启动的时候就执行，这时候就需要静态代码块。比如一个项目启动需要加载的很多配置文件等资源，我们就可以都放入静态代码块中。</p>
<h2 id="④、静态代码块不能存在任何方法体中"><a href="#④、静态代码块不能存在任何方法体中" class="headerlink" title="　　④、静态代码块不能存在任何方法体中"></a>　　④、静态代码块不能存在任何方法体中</h2><p>　　这个应该很好理解，首先我们要明确静态代码块是在类加载的时候就要运行了。我们分情况讨论：</p>
<p>　　对于普通方法，由于普通方法是通过加载类，然后new出实例化对象，通过对象才能运行这个方法，而静态代码块只需要加载类之后就能运行了。</p>
<p>　　对于静态方法，在类加载的时候，静态方法也已经加载了，但是我们必须要通过类名或者对象名才能访问，也就是说相比于静态代码块，静态代码块是主动运行的，而静态方法是被动运行的。</p>
<p>　　不管是哪种方法，我们需要明确静态代码块的存在在类加载的时候就自动运行了，而放在不管是普通方法还是静态方法中，都是不能自动运行的。</p>
<h2 id="⑤、静态代码块不能访问普通变量"><a href="#⑤、静态代码块不能访问普通变量" class="headerlink" title="　　⑤、静态代码块不能访问普通变量"></a>　　⑤、静态代码块不能访问普通变量</h2><p>　　这个理解思维同上，普通变量只能通过对象来调用，是不能放在静态代码块中的。</p>
<h1 id="2、构造代码块"><a href="#2、构造代码块" class="headerlink" title="2、构造代码块"></a>2、构造代码块</h1><h2 id="①、格式-1"><a href="#①、格式-1" class="headerlink" title="　　①、格式"></a>　　①、格式</h2><p>　　在java类中使用{}声明的代码块（和静态代码块的区别是少了static关键字）:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CodeBlock</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"静态代码块"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(<span class="string">"构造代码块"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="②、执行时机-1"><a href="#②、执行时机-1" class="headerlink" title="　　②、执行时机"></a>　　②、执行时机</h2><p>构造代码块在创建对象时被调用，每次创建对象都会调用一次，但是优先于构造函数执行。需要注意的是，听名字我们就知道，构造代码块不是优先于构造函数执行，而是依托于构造函数，也就是说，如果你不实例化对象，构造代码块是不会执行的。怎么理解呢？我们看看下面这段代码： </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CodeBlock</span> </span>&#123;</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(<span class="string">"构造代码块"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CodeBlock</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"无参构造函数"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CodeBlock</span><span class="params">(String str)</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"有参构造函数"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们反编译生成的class文件： </p>
<p><img src="/2018/08/17/Java%E4%B8%AD%E9%9D%99%E6%80%81%E4%BB%A3%E7%A0%81%E5%9D%97%E3%80%81%E6%9E%84%E9%80%A0%E4%BB%A3%E7%A0%81%E5%9D%97%E3%80%81%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E3%80%81%E6%99%AE%E9%80%9A%E4%BB%A3%E7%A0%81%E5%9D%97/%E5%8F%8D%E7%BC%96%E8%AF%91.png" alt></p>
<p>　如果存在多个构造代码块，则执行顺序按照书写顺序依次执行。 </p>
<h2 id="③、构造代码块的作用"><a href="#③、构造代码块的作用" class="headerlink" title="③、构造代码块的作用"></a>③、构造代码块的作用</h2><p> 　　和构造函数的作用类似，都能对对象进行初始化，并且只要创建一个对象，构造代码块都会执行一次。但是反过来，构造函数则不一定每个对象建立时都执行（多个构造函数情况下，建立对象时传入的参数不同则初始化使用对应的构造函数）。</p>
<p>　　利用每次创建对象的时候都会提前调用一次构造代码块特性，我们可以做诸如统计创建对象的次数等功能。</p>
<h1 id="3、构造函数"><a href="#3、构造函数" class="headerlink" title="3、构造函数"></a>3、构造函数</h1><p>　　1.构造函数的命名必须和类名完全相同。在java中普通函数可以和构造函数同名，但是必须带有返回值；</p>
<p>　　2.构造函数的功能主要用于在类的对象创建时定义初始化的状态。它没有返回值，也不能用void来修饰。这就保证了它不仅什么也不用自动返回，而且根本不能有任何选择。而其他方法都有返回值，即使是void返回值。尽管方法体本身不会自动返回什么，但仍然可以让它返回一些东西，而这些东西可能是不安全的；</p>
<p>　　3.构造函数不能被直接调用，必须通过new运算符在创建对象时才会自动调用；而一般的方法是在程序执行到它的时候被调用的；</p>
<p>　　4.当定义一个类的时候，通常情况下都会显示该类的构造函数，并在函数中指定初始化的工作也可省略，不过Java编译器会提供一个默认的构造函数.此默认构造函数是不带参数的。而一般的方法不存在这一特点；</p>
<h1 id="4、普通代码块"><a href="#4、普通代码块" class="headerlink" title="4、普通代码块"></a>4、普通代码块</h1><p>　　普通代码块和构造代码块的区别是，构造代码块是在类中定义的，而普通代码块是在方法体中定义的。且普通代码块的执行顺序和书写顺序一致。</p>
<h1 id="5、执行顺序"><a href="#5、执行顺序" class="headerlink" title="5、执行顺序"></a>5、执行顺序</h1><ul>
<li>　<strong>静态代码块&gt;构造代码块&gt;构造函数&gt;普通代码块</strong>　</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CodeBlock</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"静态代码块"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(<span class="string">"构造代码块"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CodeBlock</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"无参构造函数"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sayHello</span><span class="params">()</span></span>&#123;</span><br><span class="line">        &#123;</span><br><span class="line">            System.out.println(<span class="string">"普通代码块"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"执行了main方法"</span>);</span><br><span class="line">         </span><br><span class="line">        <span class="keyword">new</span> CodeBlock().sayHello();;</span><br><span class="line">        System.out.println(<span class="string">"---------------"</span>);</span><br><span class="line">        <span class="keyword">new</span> CodeBlock().sayHello();;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>反编译生成的class文件：</strong></p>
<p><img src="/2018/08/17/Java%E4%B8%AD%E9%9D%99%E6%80%81%E4%BB%A3%E7%A0%81%E5%9D%97%E3%80%81%E6%9E%84%E9%80%A0%E4%BB%A3%E7%A0%81%E5%9D%97%E3%80%81%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E3%80%81%E6%99%AE%E9%80%9A%E4%BB%A3%E7%A0%81%E5%9D%97/%E5%8F%8D%E7%BC%96%E8%AF%912.png" alt></p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java方法引用</title>
    <url>/2018/08/11/Java%E6%96%B9%E6%B3%95%E5%BC%95%E7%94%A8/</url>
    <content><![CDATA[<p>了解下Lambda的兄弟:方法引用.我是真的觉得这些新特新一点每增加代码可读性,反而越来越难以理解了.</p>
<a id="more"></a>

<h1 id="Java8之方法引用"><a href="#Java8之方法引用" class="headerlink" title="Java8之方法引用"></a>Java8之方法引用</h1><h2 id="一、概述"><a href="#一、概述" class="headerlink" title="一、概述"></a>一、概述</h2><p>在学习lambda表达式之后，我们通常使用lambda表达式来创建匿名方法。然而，有时候我们仅仅是调用了一个已存在的方法。如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Arrays.sort(stringsArray,(s1,s2)-&gt;s1.compareToIgnoreCase(s2));</span><br></pre></td></tr></table></figure>

<p> 在Java8中，我们可以直接通过方法引用来<strong>简写</strong>lambda表达式中已经存在的方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Arrays.sort(stringsArray, String::compareToIgnoreCase);</span><br></pre></td></tr></table></figure>

<p>这种特性就叫做方法引用(Method Reference)。</p>
<h2 id="二、什么是方法引用"><a href="#二、什么是方法引用" class="headerlink" title="二、什么是方法引用"></a>二、什么是方法引用</h2><p>​     <strong>方法引用</strong>是用来直接访问类或者实例的已经存在的方法或者构造方法。方法引用提供了一种引用而不执行方法的方式，它需要由兼容的函数式接口构成的目标类型上下文。计算时，方法引用会创建函数式接口的一个实例。</p>
<p>当Lambda表达式中只是执行一个方法调用时，不用Lambda表达式，直接通过方法引用的形式可读性更高一些。方法引用是一种更简洁易懂的Lambda表达式。</p>
<p>注意方法引用是一个Lambda表达式，其中方法引用的操作符是双冒号”::”。</p>
<hr>
<p>​      简单地说，就是一个Lambda表达式。在Java 8中，我们会使用Lambda表达式创建匿名方法，但是有时候，我们的Lambda表达式可能仅仅调用一个已存在的方法，而不做任何其它事，对于这种情况，通过一个方法名字来引用这个已存在的方法会更加清晰，Java 8的方法引用允许我们这样做。方法引用是一个更加紧凑，易读的Lambda表达式，注意方法引用是一个Lambda表达式，其中方法引用的操作符是双冒号”::”。</p>
<h2 id="三、方法引用例子"><a href="#三、方法引用例子" class="headerlink" title="三、方法引用例子"></a>三、方法引用例子</h2><p>先看一个例子，首先定义一个Person类，如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo.model;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.time.LocalDate;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Person</span><span class="params">(String name, LocalDate birthday)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.birthday = birthday;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    String name;</span><br><span class="line">    LocalDate birthday;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> LocalDate <span class="title">getBirthday</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> birthday;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">compareByAge</span><span class="params">(Person a, Person b)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a.birthday.compareTo(b.birthday);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>假设我们有一个Person数组，并且想对它进行排序，这时候，我们可能会这样写：</p>
<p><strong>原始写法，使用匿名类：</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.time.LocalDate;</span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"><span class="keyword">import</span> java.util.Comparator;</span><br><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"><span class="keyword">import</span> com.demo.model.Person;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">testMethodReference</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Person[] pArr = <span class="keyword">new</span> Person[]&#123;</span><br><span class="line">                <span class="keyword">new</span> Person(<span class="string">"003"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">9</span>,<span class="number">1</span>)),</span><br><span class="line">                <span class="keyword">new</span> Person(<span class="string">"001"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">2</span>,<span class="number">1</span>)),</span><br><span class="line">                <span class="keyword">new</span> Person(<span class="string">"002"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">3</span>,<span class="number">1</span>)),</span><br><span class="line">                <span class="keyword">new</span> Person(<span class="string">"004"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">12</span>,<span class="number">1</span>))&#125;;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用匿名类</span></span><br><span class="line">        Arrays.sort(pArr, <span class="keyword">new</span> Comparator&lt;Person&gt;() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(Person a, Person b)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> a.getBirthday().compareTo(b.getBirthday());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        </span><br><span class="line">        System.out.println(Arrays.asList(pArr));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中，Arrays类的sort方法定义如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">sort</span><span class="params">(T[] a, Comparator&lt;? <span class="keyword">super</span> T&gt; c)</span></span></span><br></pre></td></tr></table></figure>

<p>这里，我们首先要注意<code>Comparator</code>接口是一个函数式接口，因此我们可以使用Lambda表达式，而不需要定义一个实现<code>Comparator</code>接口的类，并创建它的实例对象，传给sort方法。</p>
<p>使用Lambda表达式，我们可以这样写：</p>
<h3 id="改进一，使用Lambda表达式，没有调用已存在的方法"><a href="#改进一，使用Lambda表达式，没有调用已存在的方法" class="headerlink" title="改进一，使用Lambda表达式，没有调用已存在的方法"></a>改进一，使用Lambda表达式，没有调用已存在的方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Person[] pArr = <span class="keyword">new</span> Person[]&#123;</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"003"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">9</span>,<span class="number">1</span>)),</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"001"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">2</span>,<span class="number">1</span>)),</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"002"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">3</span>,<span class="number">1</span>)),</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"004"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">12</span>,<span class="number">1</span>))&#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//使用lambda表达式</span></span><br><span class="line">    Arrays.sort(pArr, (Person a, Person b) -&gt; &#123;</span><br><span class="line">        <span class="keyword">return</span> a.getBirthday().compareTo(b.getBirthday());</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    System.out.println(Arrays.asList(pArr));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然而，在以上代码中，关于两个人生日的比较方法在Person类中已经定义了，因此，我们可以直接使用已存在的Person.compareByAge方法。</p>
<h3 id="改进二，使用Lambda表达式，调用已存在的方法"><a href="#改进二，使用Lambda表达式，调用已存在的方法" class="headerlink" title="改进二，使用Lambda表达式，调用已存在的方法"></a>改进二，使用Lambda表达式，调用已存在的方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Person[] pArr = <span class="keyword">new</span> Person[]&#123;</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"003"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">9</span>,<span class="number">1</span>)),</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"001"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">2</span>,<span class="number">1</span>)),</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"002"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">3</span>,<span class="number">1</span>)),</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"004"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">12</span>,<span class="number">1</span>))&#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//使用lambda表达式和类的静态方法</span></span><br><span class="line">    Arrays.sort(pArr, (a ,b) -&gt; Person.compareByAge(a, b));</span><br><span class="line">    </span><br><span class="line">    System.out.println(Arrays.asList(pArr));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因为这个Lambda表达式调用了一个已存在的方法，因此，我们可以直接使用方法引用来替代这个Lambda表达式。</p>
<h3 id="改进三，使用方法引用"><a href="#改进三，使用方法引用" class="headerlink" title="改进三，使用方法引用"></a>改进三，使用方法引用</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test3</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Person[] pArr = <span class="keyword">new</span> Person[]&#123;</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"003"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">9</span>,<span class="number">1</span>)),</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"001"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">2</span>,<span class="number">1</span>)),</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"002"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">3</span>,<span class="number">1</span>)),</span><br><span class="line">            <span class="keyword">new</span> Person(<span class="string">"004"</span>, LocalDate.of(<span class="number">2016</span>,<span class="number">12</span>,<span class="number">1</span>))&#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//使用方法引用，引用的是类的静态方法</span></span><br><span class="line">    Arrays.sort(pArr, Person::compareByAge);</span><br><span class="line">    </span><br><span class="line">    System.out.println(Arrays.asList(pArr));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[001, 002, 003, 004]</span><br></pre></td></tr></table></figure>

<p>在以上代码中，方法引用Person::compareByAge在语义上与Lambda表达式 (a, b) -&gt; Person.compareByAge(a, b) 是等同的，都有如下特性：</p>
<ul>
<li>真实的参数是拷贝自Comparator<Person>.compare方法，即(Person, Person)；</Person></li>
<li>表达式体调用Person.compareByAge方法。</li>
</ul>
<h2 id="四、四种方法引用类型"><a href="#四、四种方法引用类型" class="headerlink" title="四、四种方法引用类型"></a>四、四种方法引用类型</h2><p>方法引用的标准形式是：<code>类名::方法名</code>。（<strong>注意：只需要写方法名，不需要写括号</strong>）</p>
<p>有以下四种形式的方法引用：</p>
<table>
<thead>
<tr>
<th><strong>类型</strong></th>
<th><strong>示例</strong></th>
</tr>
</thead>
<tbody><tr>
<td>引用静态方法</td>
<td>ContainingClass::staticMethodName</td>
</tr>
<tr>
<td>引用某个对象的实例方法</td>
<td>containingObject::instanceMethodName</td>
</tr>
<tr>
<td>引用某个类型的任意对象的实例方法</td>
<td>ContainingType::methodName</td>
</tr>
<tr>
<td>引用构造方法</td>
<td>ClassName::new</td>
</tr>
</tbody></table>
<p>下面我们通过一个小Demo来分别学习这几种形式的方法引用：</p>
<h3 id="1、静态方法引用"><a href="#1、静态方法引用" class="headerlink" title="1、静态方法引用"></a>1、静态方法引用</h3><p><strong>组成语法格式：ClassName::staticMethodName</strong></p>
<p>我们前面举的例子Person::compareByAge就是一个静态方法引用。</p>
<p>注意：</p>
<ul>
<li>静态方法引用比较容易理解，和静态方法调用相比，只是把 <strong>.</strong> 换为 <strong>::</strong></li>
<li>在目标类型兼容的任何地方，都可以使用静态方法引用。</li>
</ul>
<p>例子：</p>
<p>　　String::valueOf   等价于lambda表达式 (s) -&gt; String.valueOf(s)</p>
<p>　　Math::pow       等价于lambda表达式  (x, y) -&gt; Math.pow(x, y);</p>
<p><strong>字符串反转的例子：</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 函数式接口</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">StringFunc</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="function">String <span class="title">func</span><span class="params">(String n)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyStringOps</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//静态方法： 反转字符串</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">strReverse</span><span class="params">(String str)</span> </span>&#123;</span><br><span class="line">        String result = <span class="string">""</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = str.length() - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">            result += str.charAt(i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MethodRefDemo</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">stringOp</span><span class="params">(StringFunc sf, String s)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sf.func(s);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String inStr = <span class="string">"lambda add power to Java"</span>;</span><br><span class="line">        <span class="comment">//MyStringOps::strReverse 相当于实现了接口方法func() </span></span><br><span class="line">        <span class="comment">// 并在接口方法func()中作了MyStringOps.strReverse()操作</span></span><br><span class="line">         String outStr = stringOp(MyStringOps::strReverse, inStr);</span><br><span class="line">        System.out.println(<span class="string">"Original string: "</span> + inStr);</span><br><span class="line">        System.out.println(<span class="string">"String reserved: "</span> + outStr);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Original string: lambda add power to Java</span><br><span class="line">String reserved: avaJ ot rewop dda adbmal</span><br></pre></td></tr></table></figure>

<p>表达式MyStringOps::strReverse的计算结果为对象引用，其中，strReverse提供了StringFunc的func()方法的实现。</p>
<p><strong>找到列表中具有最大值的对象</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyClass</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> val;</span><br><span class="line"></span><br><span class="line">    MyClass(<span class="keyword">int</span> v) &#123;</span><br><span class="line">        val = v;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getValue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> val;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.Collections;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UseMethodRef</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">     <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">compareMC</span><span class="params">(MyClass a, MyClass b)</span> </span>&#123;</span><br><span class="line">         <span class="keyword">return</span> a.getValue() - b.getValue();</span><br><span class="line">     &#125;</span><br><span class="line">     </span><br><span class="line">     <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">         ArrayList&lt;MyClass&gt; a1 = <span class="keyword">new</span> ArrayList&lt;MyClass&gt;();</span><br><span class="line">         a1.add(<span class="keyword">new</span> MyClass(<span class="number">1</span>));</span><br><span class="line">         a1.add(<span class="keyword">new</span> MyClass(<span class="number">4</span>));</span><br><span class="line">         a1.add(<span class="keyword">new</span> MyClass(<span class="number">2</span>));</span><br><span class="line">         a1.add(<span class="keyword">new</span> MyClass(<span class="number">9</span>));</span><br><span class="line">         a1.add(<span class="keyword">new</span> MyClass(<span class="number">3</span>));</span><br><span class="line">         a1.add(<span class="keyword">new</span> MyClass(<span class="number">7</span>));</span><br><span class="line">         <span class="comment">//UseMethodRef::compareMC生成了抽象接口Comparator定义的compare()方法的实例。</span></span><br><span class="line">         MyClass maxValObj = Collections.max(a1, UseMethodRef::compareMC);</span><br><span class="line">         System.out.println(<span class="string">"Maximum value is: "</span> + maxValObj.getValue());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Maximum value is: 9</span><br></pre></td></tr></table></figure>

<p>UseMethodRef定义了静态方法compareMC()，它与Comparator定义的compare()方法兼容。因此，没有必要显式的实现Comparator接口并创建其实例。</p>
<h3 id="2、特定实例对象的方法引用"><a href="#2、特定实例对象的方法引用" class="headerlink" title="2、特定实例对象的方法引用"></a>2、特定实例对象的方法引用</h3><p><strong>这种语法与用于静态方法的语法类似，只不过这里使用对象引用而不是类名。实例方法引用又分以下三种类型：</strong></p>
<h4 id="a-实例上的实例方法引用"><a href="#a-实例上的实例方法引用" class="headerlink" title="a.实例上的实例方法引用"></a>a.实例上的实例方法引用</h4><p><strong>组成语法格式：instanceReference::methodName</strong></p>
<p>如下示例，引用的方法是myComparisonProvider 对象的compareByName方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ComparisonProvider</span></span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareByName</span><span class="params">(Person a, Person b)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a.getName().compareTo(b.getName());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareByAge</span><span class="params">(Person a, Person b)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a.getBirthday().compareTo(b.getBirthday());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ComparisonProvider myComparisonProvider = <span class="keyword">new</span> ComparisonProvider();</span><br><span class="line">Arrays.sort(rosterAsArray, myComparisonProvider::compareByName);</span><br></pre></td></tr></table></figure>

<p><strong>例子：反转字符串</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 函数式接口</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">StringFunc</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="function">String <span class="title">func</span><span class="params">(String n)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyStringOps</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//普通方法： 反转字符串</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">strReverse1</span><span class="params">(String str)</span> </span>&#123;</span><br><span class="line">        String result = <span class="string">""</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = str.length() - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">            result += str.charAt(i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MethodRefDemo2</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">stringOp</span><span class="params">(StringFunc sf, String s)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sf.func(s);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String inStr = <span class="string">"lambda add power to Java"</span>;</span><br><span class="line">        MyStringOps strOps = <span class="keyword">new</span> MyStringOps();<span class="comment">//实例对象</span></span><br><span class="line">        <span class="comment">//strOps::strReverse1 相当于实现了接口方法func() </span></span><br><span class="line">        String outStr = stringOp(strOps::strReverse1, inStr);</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">"Original string: "</span> + inStr);</span><br><span class="line">        System.out.println(<span class="string">"String reserved: "</span> + outStr);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Original string: lambda add power to Java</span><br><span class="line">String reserved: avaJ ot rewop dda adbmal</span><br></pre></td></tr></table></figure>

<h4 id="b-超类上的实例方法引用"><a href="#b-超类上的实例方法引用" class="headerlink" title="b.超类上的实例方法引用"></a>b.超类上的实例方法引用</h4><p><strong>组成语法格式：super::methodName</strong></p>
<p>方法的名称由methodName指定，通过使用<strong>super</strong>，可以引用方法的超类版本。</p>
<p>例子：</p>
<p>还可以捕获this 指针，this :: equals  等价于lambda表达式  x -&gt; this.equals(x);</p>
<h4 id="c-类型上的实例方法引用"><a href="#c-类型上的实例方法引用" class="headerlink" title="c.类型上的实例方法引用"></a>c.类型上的实例方法引用</h4><p><strong>组成语法格式：</strong>ClassName<strong>::</strong>methodName</p>
<p>注意：</p>
<p>若类型的实例方法是泛型的，就需要在::分隔符前提供类型参数，或者（多数情况下）利用目标类型推导出其类型。</p>
<p>静态方法引用和类型上的实例方法引用拥有一样的语法。编译器会根据实际情况做出决定。一般我们不需要指定方法引用中的参数类型，因为编译器往往可以推导出结果，但如果需要我们也可以显式在::分隔符之前提供参数类型信息。</p>
<p>例子：</p>
<p>String::toString 等价于lambda表达式 (s) -&gt; s.toString()</p>
<p>这里不太容易理解，实例方法要通过对象来调用，方法引用对应Lambda，Lambda的第一个参数会成为调用实例方法的对象。</p>
<p><strong>在泛型类或泛型方法中，也可以使用方法引用。</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyFunc</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">func</span><span class="params">(T[] als, T v)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyArrayOps</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">     <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">int</span> <span class="title">countMatching</span><span class="params">(T[] vals, T v)</span> </span>&#123;</span><br><span class="line">         <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">         <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; vals.length; i++) &#123;</span><br><span class="line">             <span class="keyword">if</span> (vals[i] == v) count++;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">return</span> count;</span><br><span class="line">     &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GenericMethodRefDemo</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">int</span> <span class="title">myOp</span><span class="params">(MyFunc&lt;T&gt; f, T[] vals, T v)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> f.func(vals, v);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        Integer[] vals = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">4</span>, <span class="number">5</span>&#125;;</span><br><span class="line">        String[] strs = &#123;<span class="string">"One"</span>, <span class="string">"Two"</span>, <span class="string">"Three"</span>, <span class="string">"Two"</span>&#125;;</span><br><span class="line">        <span class="keyword">int</span> count;</span><br><span class="line">        count=myOp(MyArrayOps::&lt;Integer&gt;countMatching, vals, <span class="number">4</span>);</span><br><span class="line">        System.out.println(<span class="string">"vals contains "</span>+count+<span class="string">" 4s"</span>);</span><br><span class="line">        count=myOp(MyArrayOps::&lt;String&gt;countMatching, strs, <span class="string">"Two"</span>);</span><br><span class="line">        System.out.println(<span class="string">"strs contains "</span>+count+<span class="string">" Twos"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vals contains 3 4s</span><br><span class="line">strs contains 2 Twos</span><br></pre></td></tr></table></figure>

<p>分析：</p>
<p>​      在程序中，MyArrayOps是非泛型类，包含泛型方法countMatching()。该方法返回数组中与指定值匹配的元素的个数。注意这里如何指定泛型类型参数。例如，在main()方法中，对countMatching()方法的第一次调用如下所示：count = myOp(MyArrayOps::<Integer>countMatching,vals,4); 这里传递了类型参数Integer。</Integer></p>
<p>​      注意，参数传递发生在::的后面。这种语法可以推广。当把泛型方法指定为方法引用时，类型参数出现在::之后、方法名之前。但是，需要指出的是，在这种情况(和其它许多情况)下，并非必须显示指定类型参数，因为类型参数会被自动推断得出。对于指定泛型类的情况，类型参数位于类名的后面::的前面。</p>
<h3 id="3、任意对象（属于同一个类）的实例方法引用"><a href="#3、任意对象（属于同一个类）的实例方法引用" class="headerlink" title="3、任意对象（属于同一个类）的实例方法引用"></a>3、任意对象（属于同一个类）的实例方法引用</h3><p>如下示例，这里引用的是字符串数组中任意一个对象的compareToIgnoreCase方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String[] stringArray = &#123; <span class="string">"Barbara"</span>, <span class="string">"James"</span>, <span class="string">"Mary"</span>, <span class="string">"John"</span>, <span class="string">"Patricia"</span>, <span class="string">"Robert"</span>, <span class="string">"Michael"</span>, <span class="string">"Linda"</span> &#125;;</span><br><span class="line">Arrays.sort(stringArray, String::compareToIgnoreCase);</span><br></pre></td></tr></table></figure>

<h3 id="4、构造方法引用"><a href="#4、构造方法引用" class="headerlink" title="4、构造方法引用"></a>4、构造方法引用</h3><p>构造方法引用又分构造方法引用和数组构造方法引用。</p>
<h4 id="a-构造方法引用（也可以称作构造器引用）"><a href="#a-构造方法引用（也可以称作构造器引用）" class="headerlink" title="a.构造方法引用（也可以称作构造器引用）"></a>a.构造方法引用（也可以称作构造器引用）</h4><p><strong>组成语法格式：Class::new</strong></p>
<p>构造函数本质上是静态方法，只是方法名字比较特殊，使用的是<strong>new 关键字</strong>。</p>
<p>例子：</p>
<p>String::new， 等价于lambda表达式 () -&gt; new String() </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyFunc1</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function">MyClass <span class="title">func</span><span class="params">(<span class="keyword">int</span> n)</span></span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyClass</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> val;</span><br><span class="line"></span><br><span class="line">    MyClass(<span class="keyword">int</span> v) &#123;</span><br><span class="line">        val = v;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    MyClass()&#123;</span><br><span class="line">        val = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getValue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> val;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConstructorRefDemo</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        MyFunc1 myClassCons = MyClass :: <span class="keyword">new</span>;</span><br><span class="line">        MyClass mc = myClassCons.func(<span class="number">100</span>);</span><br><span class="line">        System.out.println(<span class="string">"val in mc is: "</span> + mc.getValue());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">val in mc is: 100</span><br></pre></td></tr></table></figure>

<h4 id="b-数组构造方法引用"><a href="#b-数组构造方法引用" class="headerlink" title="b.数组构造方法引用"></a>b.数组构造方法引用</h4><p><strong>组成语法格式：TypeName[]::new</strong></p>
<p>例子：</p>
<p>int[]::new 是一个含有一个参数的构造器引用，这个参数就是数组的长度。等价于lambda表达式  x -&gt; new int[x]。</p>
<p>假想存在一个接收int参数的数组构造方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">IntFunction&lt;<span class="keyword">int</span>[]&gt; arrayMaker = <span class="keyword">int</span>[]::<span class="keyword">new</span>;</span><br><span class="line"><span class="keyword">int</span>[] array = arrayMaker.apply(<span class="number">10</span>) <span class="comment">// 创建数组 int[10]</span></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记2</title>
    <url>/2018/04/02/Java%E7%AC%94%E8%AE%B02/</url>
    <content><![CDATA[<p>一周过去了，又有些收获。</p>
<a id="more"></a>

<p>1.基本类型打包器的一点内幕</p>
<p>编译程序会以下代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer number = <span class="number">100</span>;</span><br></pre></td></tr></table></figure>

<p>转换为</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer localInteger = Integer.valueOf(<span class="number">100</span>);</span><br></pre></td></tr></table></figure>

<p>查看src.zip文件夹中的java/lang中的Integer.java,有关valueOf()的操作内容如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Integer <span class="title">valueOf</span><span class="params">(<span class="keyword">int</span> i)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(i &gt;= IntegerCache.low &amp;&amp; i &lt;= IntegerCache.high)</span><br><span class="line">        <span class="keyword">return</span> IntegerCache.cache[i + (-IntegerCache.low)];</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> Integer(i);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p><code>IntegerCache.low</code>和<code>IntegerCache.high</code>的默认值是-128和127.当<code>i</code>的值在这中间时就会检查前面是否有相同的值，如果有就直接返回这个实例。所以</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer i1=<span class="number">100</span>;</span><br><span class="line">Integer i2=<span class="number">100</span>;</span><br><span class="line">System.out.println(i1==i2);</span><br></pre></td></tr></table></figure>

<p>结果是true，但是换成200就是false。</p>
<hr>
<p>2.数组</p>
<p>首先，数组是对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>[] scores = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">2</span>];</span><br></pre></td></tr></table></figure>

<p>可以将<code>int[]</code>看作类名，这个类是由JVM动态产生的，而<code>scores</code>则是参考名称。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>[][] scores = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">2</span>][<span class="number">2</span>];</span><br></pre></td></tr></table></figure>

<p>应该理解为一个<code>int[]</code>的一维数组。如图</p>
<p><img src="/2018/04/02/Java%E7%AC%94%E8%AE%B02/%E7%A4%BA%E6%84%8F%E5%9B%BE.png" alt="示意图"></p>
<p>我的理解是：</p>
<ol>
<li><p>有一个参考，是<code>int[][]</code>类型的，名字是<code>scores</code>,</p>
</li>
<li><p><code>scores</code>指向一个数组，数组长度为二，分别装了一个<code>int[]</code>类型的参考，</p>
</li>
<li><p>这两个参考有指向了两个连续的内存空间，分别里面是0，因为数组默认值是0。</p>
<p>默认值的情况：</p>
</li>
</ol>
<table>
<thead>
<tr>
<th>数据类型</th>
<th>初始值</th>
</tr>
</thead>
<tbody><tr>
<td>byte</td>
<td>0</td>
</tr>
<tr>
<td>short</td>
<td>0</td>
</tr>
<tr>
<td>int</td>
<td>0</td>
</tr>
<tr>
<td>long</td>
<td>0L</td>
</tr>
<tr>
<td>float</td>
<td>0.0F</td>
</tr>
<tr>
<td>double</td>
<td>0.0D</td>
</tr>
<tr>
<td>char</td>
<td>\u0000</td>
</tr>
<tr>
<td>boolean</td>
<td>false</td>
</tr>
<tr>
<td>类</td>
<td>null</td>
</tr>
</tbody></table>
<p>看的出来，第一层的数组不影响第二层的，所以：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>[][] arr = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">2</span>][];</span><br><span class="line">arr[<span class="number">0</span>] = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;;</span><br><span class="line">arr[<span class="number">1</span>] = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;;</span><br><span class="line"><span class="comment">//------------------------------------</span></span><br><span class="line"><span class="keyword">int</span>[][] arr1 = &#123;</span><br><span class="line">    &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;,</span><br><span class="line">    (<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer[] scores = <span class="keyword">new</span> Integer;</span><br></pre></td></tr></table></figure>

<p>这段代码建立了0个Integer数组，因为类的默认值是null。</p>
<hr>
<p>3.数组复制</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>[] scores1 = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>&#125;;</span><br><span class="line"><span class="keyword">int</span>[] scores2 =<span class="keyword">new</span> <span class="keyword">int</span>[scores1.lenght];</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;scores1.length;++i)&#123;</span><br><span class="line">    scores2[i] = scores1[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种复制要先创建一个新数组，再将值一个个填进去。</p>
<p>也有简单方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>[] scores1 = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>&#125;;</span><br><span class="line"><span class="keyword">int</span>[] scores2 =<span class="keyword">new</span> <span class="keyword">int</span>[scores1.lenght];</span><br><span class="line">System.arraycopy(scores1,<span class="number">0</span>,scores2,<span class="number">0</span>,scores1.length);</span><br></pre></td></tr></table></figure>

<p>五个参数是：来源数组，来源起始索引，目的数组，目的起始索引，复制长度。</p>
<p>在JDK6以上，有Arrays.copyOf()方法。不需要手动建立新数组。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>[] scores1 = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>&#125;;</span><br><span class="line"><span class="keyword">int</span>[] scores2 = Arrays.copyOf(scores1,scores1.length);</span><br></pre></td></tr></table></figure>

<p>以上都是基本数据类型的复制，遇到对象时会不一样。</p>
<p>首先建立类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Clothes</span></span>&#123;</span><br><span class="line">    String color;</span><br><span class="line">    <span class="keyword">char</span> size;</span><br><span class="line">    Clothes(String color,<span class="keyword">char</span> size)&#123;</span><br><span class="line">        <span class="keyword">this</span>.color = color;</span><br><span class="line">        <span class="keyword">this</span>.size = size;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ShallowCopy</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="keyword">static</span> <span class="title">mian</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        Clothes[] Carray1 = &#123;<span class="keyword">new</span> Clothes(<span class="string">"red"</span>,<span class="string">'L'</span>),<span class="keyword">new</span> Clothes(<span class="string">"blue"</span>,<span class="string">'M'</span>)&#125;;</span><br><span class="line">        Clothes[] Carray2 = <span class="keyword">new</span> Clothes[Carray1.length];</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;Carray1.length;++i)&#123;</span><br><span class="line">            Carray2[i]=Carry1[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>问题就出在这句<code>Carray2[i]=Carry1[i]</code>。对于基本数据类型，假设a=3,a=b,则b=3。但是对于对象来说，<code>=</code>代表了参考的相同，比如有个数组对象，参考名称是array1，如果，array2=array1，意思是这两个参考指向了同一个对象。从始至终，这里只会有一个对象，而我复制数组，显然是要两个对象（尽管他们一模一样）。</p>
<p>这种不成功的复制叫浅层复制，如图：</p>
<p><img src="/2018/04/02/Java%E7%AC%94%E8%AE%B02/%E7%A4%BA%E6%84%8F%E5%9B%BE2.png" alt="示意图"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeepCopy</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="keyword">static</span> <span class="title">mian</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        Clothes[] Carray1 = &#123;<span class="keyword">new</span> Clothes(<span class="string">"red"</span>,<span class="string">'L'</span>),<span class="keyword">new</span> Clothes(<span class="string">"blue"</span>,<span class="string">'M'</span>)&#125;;</span><br><span class="line">        Clothes[] Carray2 = <span class="keyword">new</span> Clothes[Carray1.length];</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;Carray1.length;++i)&#123;</span><br><span class="line">            Clothes c = <span class="keyword">new</span> Clotnes(Carray1[i].color,Carray1[i].size);</span><br><span class="line">            Carray2[i] = c;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>每次都新建一个对象，从Carray1那把相应的属性赋给它，再将这个对象的参考放入数组。</p>
<p>4.字符串</p>
<p>（1）将字符串转换为数字</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Scanner;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Sum</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(Stirng[] args)</span></span>&#123;</span><br><span class="line">        Scanner scanner = <span class="keyword">new</span> Scanner(System.in);</span><br><span class="line">        <span class="keyword">long</span> sum = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">long</span> number = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">do</span>&#123;</span><br><span class="line">            System.out.print(<span class="string">"输入数字："</span>);</span><br><span class="line">            number = Long.parseLong(scanner.nextLine());</span><br><span class="line">            sum += number;</span><br><span class="line">        &#125;<span class="keyword">while</span>(number != <span class="number">0</span>);</span><br><span class="line">        System.out.println(<span class="string">"总和："</span>+sum);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>number = Long.parseLong(scanner.nextLine());</code>可以将传入的字符串转化为long类型。其他转换方式与此类似：<code>Byte.parseByte(number)</code>。</p>
<p>（2）字符串常量与字符串池</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> name = &#123;<span class="string">'k'</span>,<span class="string">'a'</span>,<span class="string">'t'</span>,<span class="string">'e'</span>&#125;;</span><br><span class="line">String name1 = <span class="keyword">new</span> String(name);</span><br><span class="line">String name2 = <span class="keyword">new</span> String(name);</span><br><span class="line">System.out.println(name1==name2);</span><br></pre></td></tr></table></figure>

<p>显然是false，因为new了两个对象，他们的内容是一样的，但是斌不是同一个对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String name1 = <span class="string">"kate"</span>;</span><br><span class="line">String name2 = <span class="string">"kate"</span>;</span><br><span class="line">System.out.println(name1==name2);</span><br></pre></td></tr></table></figure>

<p>是什么呢？是true。</p>
<p>为了效率考虑，以””包含的字符串，只要内容相同，无论在程序中出现了多少次，JVM都只会建立一个String实例。</p>
<p>注意：<strong>new一定是建立了新对象。</strong></p>
<p>（3）不可变动字符串</p>
<p>在Java中，字符串对象一旦建立，就无法再次更改，对象中没有任何方法可以更改。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String name1 = <span class="string">"Java"</span>;</span><br><span class="line">String name2 = name1 + <span class="string">"world"</span>;</span><br><span class="line">System.out.println(name2);</span><br></pre></td></tr></table></figure>

<p>会显示Javaworld，显然并不是更改了name1的内容。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String s = <span class="string">"Java"</span>;</span><br><span class="line">String s1 = (<span class="keyword">new</span> StringBuilder()).append(s).append(<span class="string">"world"</span>).toString();</span><br></pre></td></tr></table></figure>

<p>大意是会变成建立java.lang.StringBuilder对象，使用其append()方法把s和”world”拼接，转换为toString()返回。</p>
<p>简单说，用+拼接会产生新的String实例。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记4</title>
    <url>/2018/04/08/Java%E7%AC%94%E8%AE%B04/</url>
    <content><![CDATA[<p>&emsp;这篇的主要内容是查漏补缺。原因是今天想继续下一部分：继承和多态，结果发现林的书有许多东西讲的太精简，甚至有失偏颇。所以下面我将结合李的书和博客多前面的内容进行回顾。</p>
<a id="more"></a>

<h1 id="1-数组"><a href="#1-数组" class="headerlink" title="1.数组"></a>1.数组</h1><h2 id="内存中的数组"><a href="#内存中的数组" class="headerlink" title="内存中的数组"></a>内存中的数组</h2><p>&emsp;数组引用变量只是一个引用，类似于指针，它存放了对象的真是地址，但是这只是类比理解，实际上还是有差别的。实际的数组对象存放在堆中，而引用变量如果是一个局部变量就是在栈内存中。（是不是又像c++）同样的，当一个方法执行时，每个方法都会建立内存栈，然后将变量依次放入栈中，当使用完时也就被销毁了。堆中的对象是不会这样的，只要有引用指向它，它就会存在，当没有任何引用指向它时，它就会被垃圾管理器回收。这和C++不一样，C++中的需要自己手动创建，手动释放。<br>&emsp;引用的指定并不是唯一的，不是我被指定给了某个对象就不能再被指定给别人，只要类型相符就没有问题。</p>
<h3 id="基本类型数组的初始化"><a href="#基本类型数组的初始化" class="headerlink" title="基本类型数组的初始化"></a>基本类型数组的初始化</h3><p>&emsp;对于基本类型数组，数组元素的值是直接存在对应的数组元素中的，因此，初始化数组时先为该数组分配内存空间，然后将数组的值放入里面就行。注意细节：分配空间后，里面并不是空的，而是默认值，关于默认值前面的笔记提到过。</p>
<h3 id="引用类型数组的初始化"><a href="#引用类型数组的初始化" class="headerlink" title="引用类型数组的初始化"></a>引用类型数组的初始化</h3><p>先给出代码：<br>定义类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">double</span> height;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">info</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"我的年龄是："</span>+age+<span class="string">"我的身高是："</span>+height)；</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>测试类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ReferenceArrayTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Person[] students = <span class="keyword">new</span> Person[<span class="number">2</span>];</span><br><span class="line">        Person zhang = <span class="keyword">new</span> Person();</span><br><span class="line">        Person lee = <span class="keyword">new</span> Person();</span><br><span class="line">        zhang.age = <span class="number">15</span>;</span><br><span class="line">        zhang.height = <span class="number">158</span>;</span><br><span class="line">        lee.height = <span class="number">161</span>;</span><br><span class="line">        students[<span class="number">0</span>]=zhang;</span><br><span class="line">        students[<span class="number">1</span>]=lee;</span><br><span class="line">        lee.info();</span><br><span class="line">        student[<span class="number">1</span>].info();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果非常简单。问题在于下面这张图：</p>
<p><img src="/2018/04/08/Java%E7%AC%94%E8%AE%B04/%E6%95%B0%E7%BB%84.png" alt="数组"></p>
<p>&emsp;注意图的左边部分，是不是觉得很变扭，我总觉得这里不对。后来查到，引用变量是放在栈中的。关于内存空间分配，参看链接（我还没研究过相关内容，不保证文章的正确）：<br><br>[Java的内存分配]:<a href="https://www.cnblogs.com/SaraMoring/p/5687466.html" target="_blank" rel="noopener">https://www.cnblogs.com/SaraMoring/p/5687466.html</a></p>
<h1 id="this的使用"><a href="#this的使用" class="headerlink" title="this的使用"></a>this的使用</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Dog</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">jump</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"正在执行jump方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.jump();</span><br><span class="line">        System.out.println(<span class="string">"正在执行run方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;谁在调用这个方法，this就代表谁。<br>&emsp;在上面的代码中，假如我创建了一个<code>dog</code>对象，<code>this.jump()</code>代表调用dog的jump()方法，这种在同一个对象中的两个方法之间的依赖，在用的时候可以省略this。</p>
<blockquote>
<p>&emsp;省略this前缀只是一种假象，虽然程序员省略了调用jump()方法之前的this ,但实际上这个this依然是存在的。根据汉语语法习惯：完整的语句至少包括主语、谓语、宾语，在面向对象的世界里，主、谓、宾的结构完全成立，例如 “猪八戒吃西瓜”是一条汉语语句，转换为面向对象的语法，就可以写成“猪八戒.吃(西瓜);”，因此本书常常把调用成员变量、方法的对象称为“主调(主语调用者的简称)”。对于Java语言来说，调用成员变量、方法时，主调是必不可少的，即使代码中省略了主调，但实际的主调依然存在。一般来说，如果调用static修饰的成员(包括方法、成员变量)时省略了前面的主调，那么默认使用该类作为主调；如果调用没有static修饰的成员(包括方法、成员变量)时省略了 前面的主调，那么默认使用this作为主调。</p>
</blockquote>
<h1 id="关于静态与非静态"><a href="#关于静态与非静态" class="headerlink" title="关于静态与非静态"></a>关于静态与非静态</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StaticAccessNoStatic</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"调用info()方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        info();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>main()方法是静态的，它是属于类的，info()是属于对象的方法，必须使用对象来调用。类比上一节的代码，main方法调用info方法时，相当于调用<code>this.info()</code>方法，但是再三强调过了，static是类的，不属于对象，也就无从调用。而且就算允许了，那么，this是没法引用有效的对象的。<br>但是，但是，最不合理的地方就是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Dog dog = <span class="keyword">new</span> Dog();</span><br></pre></td></tr></table></figure>

<p>假定run()方法是static的，你是可以通过对象dog来调用的。但是，必须杜绝这种写法，正确的做法是：<code>Dog.run;</code>。<br><br>还有一些特殊情况，例如：当类的成员变量和类的方法的局部变量同名时，就需要用到this。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Dog</span></span>&#123;</span><br><span class="line">    String aa;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">func</span><span class="params">()</span></span>&#123;</span><br><span class="line">        String aa;</span><br><span class="line">        System.out.println(<span class="keyword">this</span>.aa+aa);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>所以说，在构造函数（构造器）中的this就是这么来的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Dog</span> </span>&#123;</span><br><span class="line">    String name;</span><br><span class="line">    <span class="keyword">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Dog</span><span class="params">(String name, <span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在构造中，this是指new出来的新对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CashCard</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String number;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> balance;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> bonus;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CashCard</span><span class="params">(String number, <span class="keyword">int</span> balance, <span class="keyword">int</span> bonus)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.number = number;</span><br><span class="line">        <span class="keyword">this</span>.balance = balance;</span><br><span class="line">        <span class="keyword">this</span>.bonus = bonus;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>构造器是被new调用的，所以this指代的是这个新对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CashCardTest</span></span>&#123;</span><br><span class="line">    CashCard card = <span class="keyword">new</span> CashCard(<span class="string">"500"</span>,<span class="number">20</span>,<span class="number">2</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以得出推测，谁在调用含有this的方法，this就代表了谁。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ReturnThis</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ReturnThis <span class="title">grow</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        age++;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ReturnThis rt = <span class="keyword">new</span> ReturnThis();</span><br><span class="line">        rt.grow().grow().grow();</span><br><span class="line">        System.out.println(<span class="string">"rt的成员变量值是："</span>+rt.age);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;从grow()方法的返回值类型也可以看出，this是指代的调用它的rt。当执行了第一次调用后，原本的<code>rt.grow().grow().grow();</code>意思就是<code>rt.grow().grow();</code>，只不过现在的rt中的成员变量值已经改变。因为返回的是一个对象，所以可以连续调用。</p>
<h1 id="关于方法的一些思考"><a href="#关于方法的一些思考" class="headerlink" title="关于方法的一些思考"></a>关于方法的一些思考</h1><p>&emsp;方法很像函数，在C++中函数是老大，一切功能都是依靠函数，但在Java中类是老大，一切方法都必须在类或对象中。方法的调用是通过“类.方法”或“对象.方法”,必须通过方法和类。即使是调用同一个类中的不同方法，如果是static的，是依靠类，如果是普通的，是依靠this。</p>
<h2 id="关于值传递"><a href="#关于值传递" class="headerlink" title="关于值传递"></a>关于值传递</h2><p>&emsp;在笔记3中我贴了两篇有关的博客，说的非常清楚了。值传递传递的是值的拷贝，对于基本类型，这会造成你修改了传递过去的拷贝值并不会影响原来的值，但是对于引用对象来说，确实是传递了值——引用，但并没有拷贝对象（没有new）,所以修改拷贝值指向的对象是会影响到原来的对象的。</p>
<h1 id="成员变量与局部变量"><a href="#成员变量与局部变量" class="headerlink" title="成员变量与局部变量"></a>成员变量与局部变量</h1><p>&emsp;一个是类中的变量，另一个是方法中的变量。成员变量也可以加上static，就成了类变量，同样的具有类方法的相关表现。还是上面说过的坚决杜绝通过实例对象访问类的静态的东西，不论方法还是成员。</p>
<h1 id="封装与隐藏"><a href="#封装与隐藏" class="headerlink" title="封装与隐藏"></a>封装与隐藏</h1><h2 id="访问控制符"><a href="#访问控制符" class="headerlink" title="访问控制符"></a>访问控制符</h2><p>&emsp;private，protect，public三个，初次之外其实还有个default。<br><br>&emsp;private:如果类中的成员，包括成员变量，方法，构造器等，使用这个关键字，则这个成员只能在当前类中使用。用来修饰成员变量最合适。<br><br>&emsp;default:如果类中的成员，包括成员变量，方法，构造器等或者一个外部类不使用任何访问控制符，它则是包访问权限，这个成员只能在当前包中使用。说的有点绕，其实就是没有任何控制符的成员只能在同一个包中使用。<br><br>&emsp;protected:如果类中的成员，包括成员变量，方法，构造器等，使用这个关键字，则这个成员既可以被同一个包中的其他类使用，也可以被不同包中的<strong>子类</strong>使用。<br><br>&emsp;public:最宽松的访问控制，不论是否在同一个包中，是否具有父子继承关系，不论在哪都可以使用。<br></p>
<table>
<thead>
<tr>
<th align="center"></th>
<th align="center">private</th>
<th align="center">default</th>
<th align="center">protected</th>
<th align="center">public</th>
</tr>
</thead>
<tbody><tr>
<td align="center">同一个类中</td>
<td align="center">Y</td>
<td align="center">Y</td>
<td align="center">Y</td>
<td align="center">Y</td>
</tr>
<tr>
<td align="center">同一个包中</td>
<td align="center"></td>
<td align="center">Y</td>
<td align="center">Y</td>
<td align="center">Y</td>
</tr>
<tr>
<td align="center">子类中</td>
<td align="center"></td>
<td align="center"></td>
<td align="center">Y</td>
<td align="center">Y</td>
</tr>
<tr>
<td align="center">全局范围内</td>
<td align="center"></td>
<td align="center"></td>
<td align="center"></td>
<td align="center">Y</td>
</tr>
</tbody></table>
<p>*一个Java源文件中如果有一个public类，那么文件名必须是这个类名，不然，在别的地方要怎么调用这个类呢。<br><br>关于访问控制符有以下几个原则：<br><br>1.类里的绝大部分成员变量都应该使用private，只有一些static修饰的，类似全局变量的成员变量，才考虑用public。还有一些辅助类的其他方法的方法（工具方法）也应该是private。<br><br>2.如果某个类主要用于做为父类，类里的大部分方法只被用来让子类重写，而不像被外界调用，则应该用protected。</p>
<h2 id="关于package"><a href="#关于package" class="headerlink" title="关于package"></a>关于package</h2><p>&emsp;在没有import package时，在一个父包中使用子包中的类，也必须写完整的包路径和类名。父包和子包确实有内在的逻辑关系，通常子包是父包的一个模块，但这并不代表在用法上有任何关系，使用时必须用完整的名字。<br><br>&emsp;可以使用import导入包，<code>import java.util.*</code>表示导入这个包下面的所有类，不包括下面的子包。<br><br>&emsp;但不是全部导入就行，因为可能存在不同的包中有相同的类名，还是需要在使用时显式的指定。</p>
<h1 id="再论构造器"><a href="#再论构造器" class="headerlink" title="再论构造器"></a>再论构造器</h1><p>&emsp;构造器是构建Java对象的重要手段，但是，要强调的是，不是构造器新建了Java对象。根本的还是new关键字。<code>ReturnThis rt = new ReturnThis();</code>可以看出，最初额内存空间是有new创建的，而构造器进一步对这个空间内的东西进行创建。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记3</title>
    <url>/2018/04/06/Java%E7%AC%94%E8%AE%B03/</url>
    <content><![CDATA[<p>气温骤降，头疼的厉害。</p>
<p>今天发生了一些难过的事，心情真的不好，状态糟糕透了，但是这件事也激励我要努力学习，加油！</p>
<p>继续学习Java。</p>
<a id="more"></a>

<p>1.构造函数</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CashCard</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​    String number;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">int</span> balance;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">int</span> bonus;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="title">CashCard</span><span class="params">(String number, <span class="keyword">int</span> balance, <span class="keyword">int</span> bonus)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.number = number;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.balance = balance;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.bonus = bonus;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>构造函数与类名称同名，不用声明返回类型。构造函数支持函数重载。若没有写构造函数，编译程序会自动加入一个内容为空，参数为空的构造函数。所以新建对象的语法就是这么来的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">CashCard card = <span class="keyword">new</span> CashCard();</span><br></pre></td></tr></table></figure>

<p>在使用方法重载时要注意自动装箱和拆箱，会依照以下顺序处理：</p>
<p>（1）还没有装箱前可符合变量个数和类型的方法</p>
<p>（2）装箱后才符合的</p>
<p>（3）尝试有不定长度变量并可符合自变量类型的方法</p>
<p>（4）找不到合适的，报错</p>
<hr>
<p>2.封装对象的方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CashCard</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​    String number;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">int</span> balance;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">int</span> bonus;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="title">CashCard</span><span class="params">(String number, <span class="keyword">int</span> balance, <span class="keyword">int</span> bonus)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.number = number;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.balance = balance;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.bonus = bonus;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">void</span> <span class="title">store</span><span class="params">(<span class="keyword">int</span> money)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">if</span> (money &gt; <span class="number">0</span>) &#123;</span><br><span class="line"></span><br><span class="line">​            <span class="keyword">this</span>.balance += money;</span><br><span class="line"></span><br><span class="line">​            <span class="keyword">if</span> (money &gt;= <span class="number">1000</span>) &#123;</span><br><span class="line"></span><br><span class="line">​                <span class="keyword">this</span>.bonus++;</span><br><span class="line"></span><br><span class="line">​            &#125;</span><br><span class="line"></span><br><span class="line">​        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line"></span><br><span class="line">​            System.out.print(<span class="string">"储值是负的"</span>);</span><br><span class="line"></span><br><span class="line">​        &#125;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在这个对象中我写了一个方法在里面，所以每次存钱的时候我就只管调用<code>store</code>方法，而不需要自己操作数据成员。方法名称要小写，也支持重载。</p>
<hr>
<p>3.getters和setters</p>
<p>对于上面的代码还有个问题是用户可以直接访问数据成员，这显然是不安全的。我们应该将他们改为<code>private</code>，但是也要提供一个可靠的方法供用户使用修改数据。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CashCard</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">private</span> String number;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">private</span> <span class="keyword">int</span> balance;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">private</span> <span class="keyword">int</span> bonus;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="title">CashCard</span><span class="params">(String number, <span class="keyword">int</span> balance, <span class="keyword">int</span> bonus)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.number = number;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.balance = balance;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.bonus = bonus;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> String <span class="title">getNumber</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">return</span> number;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setNumber</span><span class="params">(String number)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.number = number;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getBalance</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">return</span> balance;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setBalance</span><span class="params">(<span class="keyword">int</span> balance)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.balance = balance;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getBonus</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">return</span> bonus;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setBonus</span><span class="params">(<span class="keyword">int</span> bonus)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.bonus = bonus;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br></pre></td></tr></table></figure>

<p>所以有get和set方法，这种写法是固定的。在eclipse中，在空白处单击右键-&gt;source-&gt;generate Getters and Setters可自动生成。</p>
<p>构造函数的功能其实和get,set方法有一定重复，他们都可以用来赋值，但是用途是不一样的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">test</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        CashCard card = <span class="keyword">new</span> CashCard(<span class="string">"a1"</span>,<span class="number">1000</span>,<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">​        card.setNumber(<span class="string">"a2"</span>);</span><br><span class="line"></span><br><span class="line">​        card.setBalance(<span class="number">500</span>);</span><br><span class="line"></span><br><span class="line">​        card.setBonus(<span class="number">20</span>);</span><br><span class="line"></span><br><span class="line">​        System.out.print(card.getNumber()+card.getBalance()+card.getBonus());</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看出，构造函数在一次性赋全部值时比较方便，而set方法则适合对单个值的修改。当然，事无绝对，可以对构造函数进行重载，条条大路通罗马，方法不止一个。</p>
<hr>
<p>4.一些关键字</p>
<p>（1）<code>private</code>表示的成员为类私有，用户无法在其他类的代码中<em>直接</em>使用。如果没有声明权限修饰的成员，只有在相同包的类程序代码中才可以直接使用，也就是包范围权限。</p>
<p>（2）如果想在其他的类程序代码中使用包的类或对象成员，则该类或对象必须是公开的，要加<code>public</code>声明。</p>
<p>（3）使用this</p>
<p>除了被声明为static的地方外，this可以出现在类中的任何地方，<em>在对象建立后，this为这个对象的别名（参考名称）</em>。例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">CashCard card = <span class="keyword">new</span> CashCard(<span class="string">"a1"</span>,<span class="number">500</span>,<span class="number">20</span>);</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">CashCard</span><span class="params">(String number, <span class="keyword">int</span> balance, <span class="keyword">int</span> bonus)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">this</span>.number = number;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">this</span>.balance = balance;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">this</span>.bonus = bonus;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在这里，构造函数中的this指代了新对象card，将传入的参数给了card。</p>
<p>在上面我写到，构造函数可以重载，this在这里也有应用。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Some</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">private</span> <span class="keyword">int</span> a = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">private</span> String text = <span class="string">"n.a."</span>;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="title">Some</span><span class="params">(<span class="keyword">int</span> a)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">if</span> (a &gt; <span class="number">0</span>) &#123;</span><br><span class="line"></span><br><span class="line">​            <span class="keyword">this</span>.a = a;</span><br><span class="line"></span><br><span class="line">​        &#125;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="title">Some</span><span class="params">(<span class="keyword">int</span> a, String text)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">if</span> (a &gt; <span class="number">0</span>) &#123;</span><br><span class="line"></span><br><span class="line">​            <span class="keyword">this</span>.a = a;</span><br><span class="line"></span><br><span class="line">​        &#125;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">if</span> (text != <span class="keyword">null</span>) &#123;</span><br><span class="line"></span><br><span class="line">​            <span class="keyword">this</span>.text = text;</span><br><span class="line"></span><br><span class="line">​        &#125;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>两个构造函数中有部分重复的，这不是一个好的代码。所以我们可以在一个构造函数中再次调用另一个构造函数。写成：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Some</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">private</span> <span class="keyword">int</span> a = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">private</span> String text = <span class="string">"n.a."</span>;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="title">Some</span><span class="params">(<span class="keyword">int</span> a)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">if</span> (a &gt; <span class="number">0</span>) &#123;</span><br><span class="line"></span><br><span class="line">​            <span class="keyword">this</span>.a = a;</span><br><span class="line"></span><br><span class="line">​        &#125;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="title">Some</span><span class="params">(<span class="keyword">int</span> a, String text)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>(a);</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">if</span> (text != <span class="keyword">null</span>) &#123;</span><br><span class="line"></span><br><span class="line">​            <span class="keyword">this</span>.text = text;</span><br><span class="line"></span><br><span class="line">​        &#125;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面说过this是对象的参考名称，this(a)可以看成是Some(a)，这样一来，意义就明确了，就是调用了<code>public Some(int a)</code>这个构造函数。注意，this()调用只能出现在构造函数的第一行。</p>
<p>有点小细节：</p>
<p>如果在<em>创建对象之后和调用构造函数之前</em>有想要执行的流程可以这么写：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">other</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    &#123;</span><br><span class="line"></span><br><span class="line">​        System.out.println(<span class="string">"对象初始区块"</span>)；</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    other()&#123;</span><br><span class="line"></span><br><span class="line">​        System.out.println(<span class="string">"调用other()构造函数"</span>)；</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    other(<span class="keyword">int</span> o)&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>();</span><br><span class="line"></span><br><span class="line">​        System.out.println(<span class="string">"调用other(int o)构造函数"</span>)；</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Aa</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">new</span> other(<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在这里是先执行对象初始区块后在执行指定的构造函数。</p>
<p>(4)final关键字</p>
<p>一旦使用了final就不能更改。有点意思的是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Something</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">final</span> <span class="keyword">int</span> x;</span><br><span class="line"></span><br><span class="line">​    <span class="comment">//....</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对对象成员使用final且不给赋值表示的是延迟对象成员值的指定。所以在构造函数中一定要有对改数据成员指定值的动作。而且，有构造函数重载时，要注意做到每个构造函数都有指定值或默认值，不能有一个没有，因为有可能调用到那个没有指定值或默认值的构造函数。</p>
<p>(5)static</p>
<p>对于一个类，可以根据他建立多个对象，每个对象都具有类中规定的数据成员。例如我建立两个CashCard对象，那么就有六个数据成员。但是有的时候，类中的数据是个常数，例如Pi,这样的数据显然不需要每个都拥有。那么可以这么写：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Ball</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">double</span> radius;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">double</span> PI = <span class="number">3.1415</span>;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">static</span> <span class="keyword">double</span> <span class="title">toRadians</span><span class="params">(<span class="keyword">double</span> angdeg)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">return</span> angdeg * (Ball.PI/<span class="number">180</span>);</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>被声明为static的成员，是将类名称作为名称空间，不会让个别对象拥有，而是属于类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">System.out.println(Ball.PI);</span><br><span class="line"></span><br><span class="line">System.out.println(Ball.toRadians(<span class="number">100</span>));</span><br></pre></td></tr></table></figure>

<p>类比到<code>System.out.println</code>，out是System类的，查看API文档发现out是java.io.PrintStream类型。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Ball</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">double</span> radius;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">doSome</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">double</span> r=<span class="keyword">this</span>.radius;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>由于static是属于类的，所以在static中使用this是不对的。就是说，在静态区访问非静态区是不对的，最简单的理解就是this是在建立对象后的别名，但是在上面的代码中，我压根就没建立对象。</p>
<p>下面这种也是不对的;</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Ball</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="keyword">double</span> radius;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">doSome</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">double</span> r=radius;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>虽然没写this，但实际隐含了this。什么意思？假如我新建两个对象，例如ball1和ball2，在类中调用doSome,没法确定用哪个radius。静态的对非静态的调用就是有这种问题：调用的东西可能根本还没建立或者建立了多个。this有关的东西不止这点，后面还会再出现。</p>
<p>用import可以导入包，而使用import static则可以把静态成员导入。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="keyword">static</span> java.lang.System.in;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="keyword">static</span> java.lang.System.out;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="keyword">static</span> java.util.Scanner;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ImportStatic</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​        Scanner scanner = <span class="keyword">new</span> Scanner(in);</span><br><span class="line"></span><br><span class="line">​        out.print(<span class="string">"name?"</span>);</span><br><span class="line"></span><br><span class="line">​        out.printf(<span class="string">"%s hello! %n"</span>,scannner.nextline());</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<p>5.不定长度变量</p>
<p>观察<code>System.out.printf(&quot;%d&quot;,10);</code>，printf方法的变量是不定个数的。在自己写方法时也可以做到这样。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MathTool</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">sum</span><span class="params">(<span class="keyword">int</span>... number)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">for</span>(<span class="keyword">int</span> number : numbers)&#123;</span><br><span class="line"></span><br><span class="line">​            sum+=number;</span><br><span class="line"></span><br><span class="line">​        &#125;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">return</span> sum;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当使用这种方法后，就可以这样调用<code>System.out.print(MathTool.sum(1,2,3));</code>这种写法的本质是将参数改为一个数组，前面的调用相当于<code>MathTool.sum(new int[] {1,2,3})</code>。在参数不只有不定长度变量而还有其它的变量时，不定长度参数要放在最后一个，也不允许声明两个以上不定长度变量。</p>
<hr>
<p>6.内部类</p>
<p>简单说，就是在类里面再定义一个类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Some</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="class"><span class="keyword">class</span> <span class="title">Othes</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>要使用Some中的Other类，则必须先建立Some的实例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">Some s = <span class="keyword">new</span> Some();</span><br><span class="line"></span><br><span class="line">Some.Other o = s.<span class="keyword">new</span> Other();</span><br></pre></td></tr></table></figure>

<p>内部类也可以使用public，protected或private声明。一个被声明为static的内部类，通常是将外部类作为名称空间，所以可以这么写：<code>Some.Other o = new Some.Other();</code>内部static类可以存取外部static类，外部非static不可以。</p>
<p>方法中也可以声明类，通常用于方法中的计算，外部无法使用。</p>
<p>这部分我没怎么理解透彻。</p>
<p>7.传值调用</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CallByValue</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​        Customer c1 = <span class="keyword">new</span> Customer(<span class="string">"Justin"</span>);</span><br><span class="line"></span><br><span class="line">​        some(c1);</span><br><span class="line"></span><br><span class="line">​        System.out.println(c1.name);</span><br><span class="line"></span><br><span class="line">​        Customer c2 = <span class="keyword">new</span> Customer(<span class="string">"Justin"</span>);</span><br><span class="line"></span><br><span class="line">​        some(c2);</span><br><span class="line"></span><br><span class="line">​        System.out.println(c2.name);</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">some</span><span class="params">(customer c)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​        c,name = <span class="string">"John"</span>;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">​    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">other</span><span class="params">(Customer c)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​        c = <span class="keyword">new</span> Customer(<span class="string">"Bill"</span>);</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Customer</span></span>&#123;</span><br><span class="line"></span><br><span class="line">​    String name;</span><br><span class="line"></span><br><span class="line">​    Customer(String name)&#123;</span><br><span class="line"></span><br><span class="line">​        <span class="keyword">this</span>.name = name;</span><br><span class="line"></span><br><span class="line">​    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果是:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">John</span><br><span class="line"></span><br><span class="line">Justin</span><br></pre></td></tr></table></figure>

<p>首先建立两个对象，c1&amp;c2,name都是Justin。some(c1)将c1(参考而不是整个对象)传递给some()方法的参数c，也就是说现在是c和c1一起指向一个Customer对象。接着，”John”被要求给c参考的对象。</p>
<p>但是在other()方法中，原本c和c1一起指向一个Customer对象，但是c又被要求指向一个新的name是”Bill”的对象，而c1并没有被更改什么。</p>
<p>说实话，上面这顿是Java学习笔记中的讲法，很不清楚，再贴两个链接，写的清晰明了。</p>
<p> <a href="https://www.cnblogs.com/chen-kh/p/6696303.html" target="_blank" rel="noopener">https://www.cnblogs.com/chen-kh/p/6696303.html</a></p>
<p> <a href="https://blog.csdn.net/zzp_403184692/article/details/8184751" target="_blank" rel="noopener">https://blog.csdn.net/zzp_403184692/article/details/8184751</a></p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记11</title>
    <url>/2018/05/20/Java%E7%AC%94%E8%AE%B011/</url>
    <content><![CDATA[<p>&emsp;这篇将会讲Java的IO。</p>
<a id="more"></a>
<p>&emsp;Java的IO通过java.io包下的类和接口来支持，在java.io包下主要包括输入、输出两种IO流，每种输入、输出流又可分为字节流和字符流两大类。其中字节流以字节为单位来处理输入、输出操作，而字符流则以字符来处理输入、输出操作。除此之外，Java的IO流使用了一种装饰器设计模式，它将IO流分成底层节点流和上层处理流，其中节点流用于和底层的物理存储节点直接关联—不同的物理节点获取节点流的方式可能存在一定的差异，但程序可以把不同的物理节点流包装成统一的处理流，从而允许程序使用统一的输入、输出代码来读取不同的物理存储节点的资源。<br><br>&emsp;Java7在java.nio及其子包下提供了一系列全新的API,这些API是对原有新IO的升级，因此也被称为NIO2,通过这些NIO2,程序可以更高效地进行输入、输出操作。<br><br>&emsp;除此之外，本文还会介绍Java对象的序列化机制，使用序列化机制可以把内存中的Java对象转换成二进制字节流，这样就可以把Java对象存储到磁盘里，或者在网络上传输Java对象。这也是Java提供分布式编程的重要基础。<br></p>
<h1 id="File类"><a href="#File类" class="headerlink" title="File类"></a>File类</h1><p>&emsp;File类是java.io包下在程序中操作文件和目录的工具。File类能新建，删除，重命名文件和目录，而不能访问文件本身。如果需要访问文件本省，则需要使用输入\输出流。</p>
<h2 id="访问文件和目录"><a href="#访问文件和目录" class="headerlink" title="访问文件和目录"></a>访问文件和目录</h2><p>&emsp;File类可以使用文件路径字符串来创建File实例，该文件路径字符串既可以是绝对路径，也可以是相对路径。相对路径一般是在JVM运行时所在的路径，IDEA中是在相对应的项目文件夹中。也就是说，File类的事例代表的是一个路径或者一个文件。<br></p>
<h3 id="访问文件名相关的方法"><a href="#访问文件名相关的方法" class="headerlink" title="访问文件名相关的方法"></a>访问文件名相关的方法</h3><ul>
<li>String getName()：返回此File对象所表示的文件名或路径名（如果是路径，则返回最后一级子 路径名）。</li>
<li>String getPath()：返回此File对象所对应的路径名。</li>
<li>File getAbsoluteFile()：返回此File对象的绝对路径。</li>
<li>String getAbsolutePath(): 返回此File对象所对应的绝对路径名。</li>
<li>String getParent()：返回此File对象所对应目录（最后一级子目录）的父目录名。</li>
<li>boolean renameTo(File newName)：重命名此File对象所对应的文件或目录，如果重命名成功， 则返回true；否则返回false。</li>
</ul>
<h3 id="文件检测相关的方法"><a href="#文件检测相关的方法" class="headerlink" title="文件检测相关的方法"></a>文件检测相关的方法</h3><ul>
<li>boolean exists()：判断File对象所对应的文件或目录是否存在。</li>
<li>boolean canWrite()：判断File对象所对应的文件和目录是否可写。</li>
<li>boolean canRead()：判断File对象所对应的文件和目录是否可读。</li>
<li>boolean isFile()：判断File对象所对应的是否是文件，而不是目录。</li>
<li>boolean isDirectory()：判断F*ile对象所对应的是否是目录，而不是文件。</li>
<li>boolean isAbsolute()：判断File对象所对应的文件或目录是否是绝对路径。该方法消除了不同平台的差异，可以直接判断File对象是否为绝对路径。在 UNIX/Linux/BSD等系统上，如果路径名开头是一条斜线（/)，则表明该n ie对象对应一个绝对路径；在 Windows等系统上，如果路 径开头是盘符，则说明它是一个绝对路径。</li>
</ul>
<h3 id="获取常规文件信息"><a href="#获取常规文件信息" class="headerlink" title="获取常规文件信息"></a>获取常规文件信息</h3><ul>
<li>long lastModified()：返回文件的最后修改时间。</li>
<li>long length()：返回文件内容的长度。</li>
</ul>
<h3 id="文件操作相关的方法"><a href="#文件操作相关的方法" class="headerlink" title="文件操作相关的方法"></a>文件操作相关的方法</h3><ul>
<li>boolean createNewFile()：当此File对象所对应的文件不存在时，该方法将新建一个该File对象所指定的新文件，如果创建成功则返回true；否则返回false。</li>
<li>boolean delete()：删除File对象所对应的文件或路径。</li>
<li>static File createTempFile(String prefix, String suffix)：在默认的临时文件目录中创建一个临时的空文件，使用给定前缀、系统生成的随机数和给定后缀作为文件名。这是一个静态方法，可以直接通过File类来调用。prefix参数必须至少是3字节长。建议前缀使用一个短的、有意义的字符串，比如 “hjb”或”mail”。suffix参数可以为null,在这种情况下，将使用默认的后缀“ .tmp”。</li>
<li>static File createTempFile(String prefix, String suffix, File directory): 在 directory所指定的目录中创建一个临时的空文件，使用给定前缀、系统生成的随机数和给定后缀作为文件名。这是一个静态方法，可以直接通过File类来调用。</li>
<li>void deleteOnExit()：注册一个删除钩子，指定当Java虚拟机退出时，删除File对象所对应的文件和目录。</li>
</ul>
<h3 id="目录操作相关的方法"><a href="#目录操作相关的方法" class="headerlink" title="目录操作相关的方法"></a>目录操作相关的方法</h3><ul>
<li>boolean mkdir()：试图创建一个File对象所对应的目录，如果创建成功，则返回true;否则返回false。调用该方法时File对象必须对应一个路径，而不是一个文件。</li>
<li>String[] list()：列出File对象的所有子文件名和路径名，返回String数组。</li>
<li>File[] listFiles()：列出File对象的所有子文件和路径，返回File数组。</li>
<li>static File[] listRoots()：列出系统所有的根路径。这是一个静态方法，可以直接通过File类来调用。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="comment">//"."表示当前路径,".."表示上一级路径，当前路径为项目路径</span></span><br><span class="line">        File file = <span class="keyword">new</span> File(<span class="string">"."</span>);</span><br><span class="line">        <span class="comment">//获取文件名</span></span><br><span class="line">        System.out.println(file.getName());</span><br><span class="line">        <span class="comment">//获取相对路径的上一级路径，返回null</span></span><br><span class="line">        System.out.println(file.getParent());</span><br><span class="line">        <span class="comment">//获取绝对路径</span></span><br><span class="line">        System.out.println(file.getAbsoluteFile());</span><br><span class="line">        <span class="comment">//获取绝对路径的上一级路径</span></span><br><span class="line">        System.out.println(file.getAbsoluteFile().getParent());</span><br><span class="line">        <span class="comment">//创建临时文件，file代表“.”这个路径</span></span><br><span class="line">        File tmpFile = File.createTempFile(<span class="string">"aaa"</span>, <span class="string">".txt"</span>, file);</span><br><span class="line">        tmpFile.deleteOnExit();</span><br><span class="line">        <span class="comment">//该构造方法与第七行是一个构造函数，因为currentTimeMillis()返回的是long型，所以加双引号转为字符</span></span><br><span class="line">        File newFile = <span class="keyword">new</span> File(System.currentTimeMillis() + <span class="string">""</span>);</span><br><span class="line">        <span class="comment">//判断newFile所对应的目录或文件是否存在，结果是不存在</span></span><br><span class="line">        System.out.println(<span class="string">"newFile对象是否存在："</span> + newFile.exists());</span><br><span class="line">        <span class="comment">//因为不存在，所以创建一个newFile指定的新文件</span></span><br><span class="line">        newFile.createNewFile();</span><br><span class="line">        <span class="comment">//因为newFile已经对应了一个文件，不是一个路径，所以创建失败</span></span><br><span class="line">        newFile.mkdir();</span><br><span class="line">        String[] fileList = file.list();</span><br><span class="line">        System.out.println(<span class="string">"当前路径下所有文件和路径如下："</span>);</span><br><span class="line">        <span class="keyword">for</span> (String fileName :</span><br><span class="line">                fileList) &#123;</span><br><span class="line">            System.out.println(fileName);</span><br><span class="line">        &#125;</span><br><span class="line">        File[] roots = File.listRoots();</span><br><span class="line">        System.out.println(<span class="string">"系统所有根路径如下"</span>);</span><br><span class="line">        <span class="keyword">for</span> (File root :</span><br><span class="line">                roots) &#123;</span><br><span class="line">            System.out.println(root);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="文件过滤器"><a href="#文件过滤器" class="headerlink" title="文件过滤器"></a>文件过滤器</h2><p>&emsp;在File类中list()方法可以接受一个FilenameFilter参数，通过该参数只列出符合条件的文件。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> String[] list(FilenameFilter filter) &#123;</span><br><span class="line">    String names[] = list();</span><br><span class="line">    <span class="keyword">if</span> ((names == <span class="keyword">null</span>) || (filter == <span class="keyword">null</span>)) &#123;</span><br><span class="line">        <span class="keyword">return</span> names;</span><br><span class="line">    &#125;</span><br><span class="line">    List&lt;String&gt; v = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; names.length ; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (filter.accept(<span class="keyword">this</span>, names[i])) &#123;</span><br><span class="line">            v.add(names[i]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> v.toArray(<span class="keyword">new</span> String[v.size()]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;进一步查看源码，发现FilenameFilter是一个接口（函数式接口）。接口中包含了一个accept(File dir,String name)方法，该方法将依次对指定File的所有子目录或文件进行迭代。如果该方法返回true，那么list()方法将会列出该子目录或者文件。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FilenameFilterTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File file = <span class="keyword">new</span> File(<span class="string">"."</span>);</span><br><span class="line">        String[] nameList = file.list((dir, name) -&gt; name.endsWith(<span class="string">".java"</span>) || <span class="keyword">new</span> File(name).isDirectory());</span><br><span class="line">        <span class="keyword">for</span> (String name : nameList) &#123;</span><br><span class="line">            System.out.println(name);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这里在对这个Lambda表达式解释一下，<code>(dir, name)</code>的目标是FilenameFilter接口，而这个接口是一个函数式接口，accept()抽象方法的返回值是boolean型的，所以经过Lambda表达式的转换，最终一个boolean类型的值传入到list()方法中参与执行。</p>
<h1 id="理解Java的IO流"><a href="#理解Java的IO流" class="headerlink" title="理解Java的IO流"></a>理解Java的IO流</h1><p>&emsp;Java的IO流是实现输入输出的基础，它可以方便的实现数据的输入输出。在Java中把不同的输入输出源抽象表示为“流（stream）”,通过流的方式允许Java以相同的方式来访问不同的输入输出源。stream是从起源(source)到(sink)的有序数据。</p>
<h2 id="流的分类"><a href="#流的分类" class="headerlink" title="流的分类"></a>流的分类</h2><h3 id="输入流和输出流"><a href="#输入流和输出流" class="headerlink" title="输入流和输出流"></a>输入流和输出流</h3><p>&emsp;按照方向来分，从内存向硬盘中写入数据，对于硬盘来说这是输入流，对于内存来说，这是输出流。但是，程序运行是在内存的，所以划分输入输出的角度还是要从内存来考虑。Java的输入流主要有InputStream和Reader作为基类，输出流有OutputStream和Writer作为基类。它们都是抽象类。</p>
<h3 id="字节流和字符流"><a href="#字节流和字符流" class="headerlink" title="字节流和字符流"></a>字节流和字符流</h3><p>&emsp;字节流和字符流的用法几乎一样，字节流操作的数据单元是8bit，一个字节，而字符流是16bit，两个字节。前者主要是由InputStream和OutputStream作为基类，后者主要是由Reader和Writer作为基类。</p>
<h3 id="节点流和处理流"><a href="#节点流和处理流" class="headerlink" title="节点流和处理流"></a>节点流和处理流</h3><p>&emsp;可以从/向一个特定的IO设备中读/写数据的流称为节点流也被成为低级流。当程序使用节点流输入输出时，程序直接连接到实际的数据源，和实际的输入输出节点链接。<br><br>&emsp;处理流则用于对一个已存在的流进行连接或封装，通过封装后的流来实现数据读写的功能，也被成为高级流。处理流不连接实际的数据，没有和实际的输入输出节点连接。它是面向节点流的，这是一种典型的装饰器设计模式。处理流来包装处理节点流，而程序则不用对不同的节点流做出调整，只要处理流将节点流进行包装，消除了“差异”。这样可以用一套代码访问多种数据源。节点流因此也被成为包装流。</p>
<h2 id="流的概念模型"><a href="#流的概念模型" class="headerlink" title="流的概念模型"></a>流的概念模型</h2><p>&emsp;Java的IO流共涉及40多个类，这些类都是从前面提到过的四个基类派生出来的。<br></p>
<ul>
<li>InputStream/Reader:所有输入流的基类，前者是字节流，后者是字符流</li>
<li>OutputStream/Writer:所有输出流的基类，前者是字节流，后者是字符流</li>
</ul>
<p>&emsp;对于InputStream和Reader而言，它们把输入设备想象成一个水管，每一个字节或字符就是一个水滴，同时有个隐式的指针表示当前正准备读取哪个水滴。每当程序取出一个或多个水滴时，指针就会相应的移动。同时，这两个类中也提供方法来控制指针。而OutputStream和Writer于此类似，只不过水管的下一部分是空的，即即将放入水滴的地方是空地，而指针记录的就是水滴即将放的地方。<br></p>
<p><img src="/2018/05/20/Java%E7%AC%94%E8%AE%B011/%E6%88%AA%E5%9B%BE1.png" alt="输入"><br><img src="/2018/05/20/Java%E7%AC%94%E8%AE%B011/%E6%88%AA%E5%9B%BE2.png" alt="输出"><br>&emsp;处理流模型的功能体现在一下两个方面：</p>
<ul>
<li>性能的提高，主要以增加缓冲的方式来提高输入输出的效率</li>
<li>操作的便捷，处理流可能提供了方法来一次输出输入大量“水滴”</li>
</ul>
<p>&emsp;处理流可以嫁接在任何已存在的流的基础之上，将不同的节点流包装成处理流，这就允许Java应用使用相同的代码来读写不同的输入输出设备的数据。<br><img src="/2018/05/20/Java%E7%AC%94%E8%AE%B011/%E6%88%AA%E5%9B%BE3.png" alt="处理流"></p>
<h1 id="探讨字节流和字符流"><a href="#探讨字节流和字符流" class="headerlink" title="探讨字节流和字符流"></a>探讨字节流和字符流</h1><h2 id="InputStream和Reader"><a href="#InputStream和Reader" class="headerlink" title="InputStream和Reader"></a>InputStream和Reader</h2><p>&emsp;InputStream和Reader是所有输入流的抽象基类，本身并不能创建实例来执行输入。<br>&emsp;InputStream里包含如下三个方法:</p>
<ul>
<li>int read():从输入流中读取一个字节（一滴水），返回读取的字节数据</li>
<li>int read(byte[] b):从输入流中最多读取b.length个字节的数据，并将其存储在字节数组b中，返回实际读取的字节数</li>
<li>int read(byte[] b,int off,int len)：从输入流中最多读取len个字符的数据，并将其存储在数组b中，且放入位置是off指定的位置</li>
</ul>
<p>&emsp;Reader里包含如下三个方法：</p>
<ul>
<li>int read():从输入流中读取一个字节（一滴水），返回读取的字节数据</li>
<li>int read(char[] cbuf):从输入流中最多读取cbuf.length个字符的数据，并将其存储在字节数组cbuf中，返回实际读取的字符数</li>
<li>int read(char[] cbuf,int off,int len):从输入流中最多读取len个字符的数据，并将其存在字符数组cbuf中，存储位置是off，返回实际读取的字符数</li>
</ul>
<p>&emsp;当read(char[] cbuf)或read(byte[] b)方法返回-1,即表明到了输入流的结束点。<br><br>&emsp;InoutStream和Reader都是抽象类，它们分别有一个用于读取文件的输入流：FileInputStream和FileReader，它们都是节点流，直接和文件关联。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileInputStreamTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        FileInputStream fis = <span class="keyword">new</span> FileInputStream(<span class="string">".\\src\\FileInputStreamTest.java"</span>);  <span class="comment">//注意指定好读取文件的位置</span></span><br><span class="line">        <span class="keyword">byte</span>[] bbuf = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">int</span> hasRead = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> ((hasRead = fis.read(bbuf)) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            System.out.println(<span class="keyword">new</span> String(bbuf, <span class="number">0</span>, hasRead));</span><br><span class="line">        &#125;</span><br><span class="line">        fis.close();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;在最后，使用<code>fis.close</code>语句关闭资源，因为IO资源不属于内存中的资源。也可以使用在异常一节提到的Java7改进的try语句自动关闭IO流。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileReaderTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> (FileReader fr = <span class="keyword">new</span> FileReader(<span class="string">".\\src\\FileReaderTest.java"</span>)) &#123;</span><br><span class="line">            <span class="keyword">char</span>[] cbuf = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">32</span>];</span><br><span class="line">            <span class="keyword">int</span> hasRead = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">while</span> ((hasRead = fr.read(cbuf)) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                System.out.println(<span class="keyword">new</span> String(cbuf, <span class="number">0</span>, hasRead));</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ex) &#123;</span><br><span class="line">            ex.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;除此之外，InputStream和 Reader还支持如下几个方法来移动记录指针。</p>
<ul>
<li>void mark(int readAheadLimit)：在记录指针当前位置记录一个标记(mark)。</li>
<li>boolean markSupported()：判断此输入流是否支持mark()操作，即是否支持记录标记。</li>
<li>voidreset()：将此流的记录指针重新定位到上一次记录标记(mark)的位置。</li>
<li>long skip(long n)：记录指针向前移动n个字节/字符。</li>
</ul>
<h2 id="OutputStream和Writer"><a href="#OutputStream和Writer" class="headerlink" title="OutputStream和Writer"></a>OutputStream和Writer</h2><p>&emsp;OutputStream和Writer与上面的非常相似，两个流都提供了如下的方法：</p>
<ul>
<li>void write(int c)：将指定的字节/字符输出到输出流中，其中c既可以代表字节，也可以代表字符。</li>
<li>void write(byte[]/char[] buf)：将字节数组/字符数组中的数据输出到指定输出流中。</li>
<li>void write(byte[]/char[] buf, int off, int len)：将字节数组/字符数组中从off位置开始，长度为len的字节/字符输出到输出流中。</li>
</ul>
<p>&emsp;因为字符流直接以字符作为操作的单位，所以Writer可以用字符串来代替字符数组：</p>
<ul>
<li>void write(String str)：将str字符串里包含的字符输出到指定输出流中。</li>
<li>void write(String str,int off,int len):将str字符串里从off位置开始，长度为len的字符输出到指定输出流中。</li>
</ul>
<p>&emsp;下面的代码使用FileInputStream来执行输入，用FileOutputSteam执行输出，实现复制文件内容的功能：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileOutputStreamTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> (FileInputStream fis = <span class="keyword">new</span> FileInputStream(<span class="string">"..\\Lambda\\src\\LambdaTest.java"</span>);</span><br><span class="line">             FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(<span class="string">"newFile.txt"</span>)) &#123;</span><br><span class="line">            <span class="keyword">byte</span>[] bbuf = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">32</span>];</span><br><span class="line">            <span class="keyword">int</span> hasread = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">while</span> ((hasread = fis.read(bbuf)) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                fos.write(bbuf, <span class="number">0</span>, hasread);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ioe) &#123;</span><br><span class="line">            ioe.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;使用Java的IO流执行输出时，不能忘记关闭输出流，关闭输出流，这样除了可以保证流的物理资源被回收之外，还可以将输出缓冲区的数据flush到物理节点去（因为在执行close()方法之前自动执行flush()方法）。<br><br>&emsp;如果希望直接输出字符串内容，则使用Writer效果更好：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileWriter;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileWriterTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> (FileWriter fw = <span class="keyword">new</span> FileWriter(<span class="string">"poem.txt"</span>)) &#123;</span><br><span class="line">            fw.write(<span class="string">"蛤 - 真正的粉丝\r\n"</span>);</span><br><span class="line">            fw.write(<span class="string">"苟利国家生死以\r\n"</span>);</span><br><span class="line">            fw.write(<span class="string">"岂因祸福避趋之\r\n"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ioe) &#123;</span><br><span class="line">            ioe.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="输入-输出流体系"><a href="#输入-输出流体系" class="headerlink" title="输入/输出流体系"></a>输入/输出流体系</h1><p>&emsp;上面介绍的四种都是节点流，都稍显繁琐，所以下面使用处理流简化代码。</p>
<h2 id="处理流的用法"><a href="#处理流的用法" class="headerlink" title="处理流的用法"></a>处理流的用法</h2><p>&emsp;先直接看代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.PrintStream;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PrintStreamTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> (FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(<span class="string">"poem.txt"</span>);</span><br><span class="line">             PrintStream ps = <span class="keyword">new</span> PrintStream(fos)) &#123;</span><br><span class="line">            ps.println(<span class="string">"普通字符串"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ioe) &#123;</span><br><span class="line">            ioe.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;使用处理流操作更简单，执行效率更高。只要流的构造器参数不是一个物理节点，而是已经存在的流，那么这个流就是处理流。上面代码的try语句中，先定义了一个节点输出流FileOutputStream，然后使用PrintStream包装了这个流。关于PrintStream的相关用法参见连接<a href="https://www.cnblogs.com/skywang12345/p/io_16.html" target="_blank" rel="noopener">https://www.cnblogs.com/skywang12345/p/io_16.html</a><br><br>&emsp;在关闭IO资源时，只要关闭最上层的处理流即可。<br></p>
<h2 id="关于输入-输出流体系"><a href="#关于输入-输出流体系" class="headerlink" title="关于输入/输出流体系"></a>关于输入/输出流体系</h2><p>&emsp;Java的输入输出流体系提供了将近40个类，但是这中间是有着明显的体系结构的，看下图:<br><img src="/2018/05/20/Java%E7%AC%94%E8%AE%B011/%E4%BD%93%E7%B3%BB.png" alt="体系结构"></p>
<blockquote>
<p>注意表中的粗体字标出的类代表节点流，斜体字标出的类代表抽象基类</p>
</blockquote>
<p>&emsp;表中还列出了一种以数组为物理节点的节点流，字节流以字节数组为节点，字符流以字符数组为节点；这种以数组为物理节点的节点流除了在创建节点流对象时需要传入一个字节数组或者字符数组之外，用法上与文件节点流完全相似。与此类似的是，字符流还可以使用字符串作为物理节点，用于实现从字符串读取内容，或将内容写入字符串(用StringBuffer充当字符串)的功能。下面程序示范了使用字符串作为物理节点的字符输入/输出流的用法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.StringReader;</span><br><span class="line"><span class="keyword">import</span> java.io.StringWriter;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StringNodeTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String src = <span class="string">"苟利国家生死以\n"</span> + <span class="string">"岂因祸福避趋之"</span>;</span><br><span class="line">        <span class="keyword">char</span>[] buffer = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">32</span>];</span><br><span class="line">        <span class="keyword">int</span> hasRead = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">try</span> (StringReader sr = <span class="keyword">new</span> StringReader(src)) &#123;</span><br><span class="line">            <span class="keyword">while</span> ((hasRead = sr.read(buffer)) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                System.out.println(<span class="keyword">new</span> String(buffer, <span class="number">0</span>, hasRead));</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ioe) &#123;</span><br><span class="line">            ioe.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//创建StringWriter时，实际上以一个StringBuffer作为输出节点</span></span><br><span class="line">        <span class="comment">//下面指定的20是StringBuffer的初始长度</span></span><br><span class="line">        <span class="keyword">try</span> (StringWriter sw = <span class="keyword">new</span> StringWriter(<span class="number">20</span>)) &#123;</span><br><span class="line">            sw.write(<span class="string">"垂死病中惊坐起\n"</span>);</span><br><span class="line">            sw.write(<span class="string">"谈笑风生续你命\n"</span>);</span><br><span class="line">            System.out.println(<span class="string">"下面是sw字符串节点中的内容"</span>);</span><br><span class="line">            System.out.println(sw.toString());</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ex) &#123;</span><br><span class="line">            ex.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="转换流"><a href="#转换流" class="headerlink" title="转换流"></a>转换流</h2><p>&emsp;输入输出流体系中还提供了两个转换流，InputStreamReader和OutputStreamWriter用于将字节流转换为字符流。<br><br>&emsp;以获取键盘输入为例。System.in代表标准输入，即键盘输入，但是这个标准输入流是抽象字节流基类InputStream的实例，使用不太方便，而且由于键盘输入的一般是字符，所以可以使用InputStreamReader将其转换为字符流。然后将普通的Reader再次包装为BufferedReader，利用BufferedReader的readLine()方法一次读取一行内容。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.BufferedReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.InputStreamReader;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">KeyinTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> (InputStreamReader reader = <span class="keyword">new</span> InputStreamReader(System.in);</span><br><span class="line">             BufferedReader br = <span class="keyword">new</span> BufferedReader(reader)) &#123;</span><br><span class="line">            String line = <span class="keyword">null</span>;</span><br><span class="line">            <span class="keyword">while</span> ((line = br.readLine()) != <span class="keyword">null</span>)&#123;</span><br><span class="line">                <span class="keyword">if</span> (line.equals(<span class="string">"exit"</span>)) &#123;</span><br><span class="line">                    System.exit(<span class="number">1</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(<span class="string">"输入内容："</span> + line);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ioe) &#123;</span><br><span class="line">            ioe.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;将System.in包装成BufferedReader，BufferedReader流具有缓冲功能,它可以一次读取一行文本—以换行符为标志，如果它没有读到换行符，则程序阻塞，等到读到换行符为止。</p>
<h2 id="推回输入流"><a href="#推回输入流" class="headerlink" title="推回输入流"></a>推回输入流</h2><p>&emsp;在输入输出流体系中有两个特殊的流，就是PushbackInputStream和PushbackReader。它们都提供下面三个方法：</p>
<ul>
<li>void unread(byte[]/char[] buf)：将一个字节/字符数组内容推回到推回缓冲区里，从而允许重复读取刚刚读取的内容。</li>
<li>void unread(byte[]/char[] b, int off, int len ):将一个字节/字符数组里从off开始，长度为len字节/字符的内容推回到推回缓冲区里，从而允许重复读取刚刚读取的内容。</li>
<li>void unread(int b)：将一个字节/字符(b)推回到推回缓冲区里，从而允许重复读取刚刚读取的内容。</li>
</ul>
<p>&emsp;可以看出这里的三个方法与InputStream和Reader的方法相对应。<br><br>&emsp;这两个推回输入流都带有一个推回缓冲区，当程序调用这两个推回输入流的unread()方法时，系统将会把指定数组的内容推回到该缓冲区里，而推回输入流每次调用read()方法时总是先从推回缓冲区读取，只有完全读取了推回缓冲区的内容后，但还没有装满read()所需的数组时才会从原输入流中读取。<br><br>&emsp;当程序创建一个PushbacklnputStream和PushbackReader时需要指定推回缓冲区的大小，默认的推回缓冲区的长度为1。如果程序中推回到推回缓冲区的内容超出了推回缓冲区的大小，将会引发Pushback buffer overflow的IOException异常。<br><img src="/2018/05/20/Java%E7%AC%94%E8%AE%B011/%E6%8E%A8%E5%9B%9E.png" alt="推回"><br>&emsp;下面的代码师徒找出程序中的”new PushbackReader”字符串，找到后只打印目标字符串之前的内容。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.PushbackReader;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PushbackTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//64为推回缓冲区的长度</span></span><br><span class="line">        <span class="keyword">try</span> (PushbackReader pr = <span class="keyword">new</span> PushbackReader(<span class="keyword">new</span> FileReader(<span class="string">".\\src\\PushbackTest.java"</span>), <span class="number">64</span>)) &#123;</span><br><span class="line">            <span class="keyword">char</span>[] buf = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">32</span>];</span><br><span class="line">            <span class="comment">//上一次读取的字符串</span></span><br><span class="line">            String lastContent = <span class="string">""</span>;</span><br><span class="line">            <span class="keyword">int</span> hasRead = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">while</span> ((hasRead = pr.read(buf)) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                String content = <span class="keyword">new</span> String(buf, <span class="number">0</span>, hasRead);<span class="comment">//将数组转化为字符串</span></span><br><span class="line">                <span class="keyword">int</span> targetIndex = <span class="number">0</span>;</span><br><span class="line">                <span class="comment">//将已经读取的字符串和新读取的字符串拼接在一起，再检测目标字符串的位置，避免前一次和现在的读取将字符串分割，没有就返回-1</span></span><br><span class="line">                <span class="keyword">if</span> ((targetIndex = (lastContent + content).indexOf(<span class="string">"new PushbackReader"</span>)) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="comment">//将字符串再转换为字符数组并推回</span></span><br><span class="line">                    pr.unread((lastContent + content).toCharArray());</span><br><span class="line">                    <span class="comment">//如果推回的字符串大于32，原来的打水的“篮子“就装不下现在的字符串，换个大的</span></span><br><span class="line">                    <span class="keyword">if</span> (targetIndex &gt; <span class="number">32</span>) &#123;</span><br><span class="line">                        buf = <span class="keyword">new</span> <span class="keyword">char</span>[targetIndex];</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">//再次读取（从缓冲区开始）</span></span><br><span class="line">                    pr.read(buf, <span class="number">0</span>, targetIndex);</span><br><span class="line">                    System.out.println(<span class="keyword">new</span> String(buf, <span class="number">0</span>, targetIndex));</span><br><span class="line">                    System.exit(<span class="number">0</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    System.out.println(lastContent);</span><br><span class="line">                    lastContent = content;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ioe) &#123;</span><br><span class="line">            ioe.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;阅读代码发现，pr冒出来一个read()方法，也就是说，这两个推回输入流不是仅仅有上面三个方法。进一步查看源码可知，这个read()方法实际还是用Reader基类的代码，只不过中间经过一层FilterReader类，加了点料。这个代码暗含一个问题，如果，缓冲区长度不是“篮子”长度的两倍，那么有可能产生Pushback buffer overflow异常。</p>
<h1 id="重定向标准输入-输出"><a href="#重定向标准输入-输出" class="headerlink" title="重定向标准输入/输出"></a>重定向标准输入/输出</h1><p>&emsp;java的标准输入输出分别通过System.in和System.out实现，前者是从键盘读取输入，后者是从屏幕打印输出。System类中提供了三个重定向标准输入/输出的方法。</p>
<ul>
<li>static void setErr(PrintStream err)：重定向“标准”错误输出流。</li>
<li>static void setIn(InputStream in):重定向“标准”输入流。</li>
<li>static void setOut(PrintStream out)：重定向“标准”输出流。</li>
</ul>
<p>&emsp;下面的程序通过重定向标准输入输出流。将System.out的输出重定向到文件输出，而不是在屏幕上。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.PrintStream;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedirectOut</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> (PrintStream ps = <span class="keyword">new</span> PrintStream(<span class="keyword">new</span> FileOutputStream(<span class="string">"out.txt"</span>))) &#123;</span><br><span class="line">            System.setOut(ps);</span><br><span class="line">            System.out.println(<span class="string">"普通字符串"</span>);</span><br><span class="line">            System.out.println(<span class="keyword">new</span> RedirectOut());</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ex) &#123;</span><br><span class="line">            ex.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;下面程序重定向标准输入，从而可以将System.in重定向到指定文件，而不是键盘输入。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.util.Scanner;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedirectIn</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> (FileInputStream fis = <span class="keyword">new</span> FileInputStream(<span class="string">"./src/RedirectIn.java"</span>)) &#123;</span><br><span class="line">            System.setIn(fis);</span><br><span class="line">            Scanner sc = <span class="keyword">new</span> Scanner(System.in);</span><br><span class="line">            sc.useDelimiter(<span class="string">"\n"</span>);</span><br><span class="line">            <span class="keyword">while</span> (sc.hasNext()) &#123;</span><br><span class="line">                System.out.println(<span class="string">"输入的内容是:"</span> + sc.next());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Java虚拟机读写其他进程的数据"><a href="#Java虚拟机读写其他进程的数据" class="headerlink" title="Java虚拟机读写其他进程的数据"></a>Java虚拟机读写其他进程的数据</h1><p>&emsp;使用Runtime对象的exec()方法可以运行平台上的其他程序，该方法产生一 个Process对象，Process对象代表由该Java程序启动的子进程。Process类提供了如下三个方法，用于 让程序和其子进程进行通信。</p>
<ul>
<li>InputStream getErrorStream()：获取子进程的错误流。</li>
<li>InputStream getInputStream(): 获取子进程的输入流。</li>
<li>OutputStream getOutputStream()：获取子进程的输出流。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.BufferedReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.InputStreamReader;</span><br><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.PrintStream;</span><br><span class="line"><span class="keyword">import</span> java.util.Scanner;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ReadFromProcess</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        Process p = Runtime.getRuntime().exec(<span class="string">"javac ReadStandard"</span>);</span><br><span class="line">        <span class="keyword">try</span> (BufferedReader br = <span class="keyword">new</span> BufferedReader(<span class="keyword">new</span> InputStreamReader(p.getErrorStream()))) &#123;</span><br><span class="line">            String buff = <span class="keyword">null</span>;</span><br><span class="line">            <span class="keyword">while</span> ((buff = br.readLine()) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                System.out.println(buff);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ReadStandard</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span></span><br><span class="line"><span class="function">        <span class="title">try</span> <span class="params">(Scanner sc = new Scanner(System.in)</span></span>;</span><br><span class="line">             PrintStream ps = <span class="keyword">new</span> PrintStream(<span class="keyword">new</span> FileOutputStream(<span class="string">"out.txt"</span>))) &#123;</span><br><span class="line">            sc.useDelimiter(<span class="string">"\n"</span>);</span><br><span class="line">            <span class="keyword">while</span> (sc.hasNext()) &#123;</span><br><span class="line">                ps.println(<span class="string">"输入内容是:"</span> + sc.next());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.util.Scanner;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedirectIn</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> (FileInputStream fis = <span class="keyword">new</span> FileInputStream(<span class="string">"./src/RedirectIn.java"</span>)) &#123;</span><br><span class="line">            System.setIn(fis);</span><br><span class="line">            Scanner sc = <span class="keyword">new</span> Scanner(System.in);</span><br><span class="line">            sc.useDelimiter(<span class="string">"\n"</span>);</span><br><span class="line">            <span class="keyword">while</span> (sc.hasNext()) &#123;</span><br><span class="line">                System.out.println(<span class="string">"输入的内容是:"</span> + sc.next());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记6</title>
    <url>/2018/04/19/Java%E7%AC%94%E8%AE%B06/</url>
    <content><![CDATA[<p>&emsp;在上一篇中，虽然写到了继承，但是并没有详细深入。这篇来看看相关问题。</p>
<a id="more"></a>

<h1 id="继承共同行为"><a href="#继承共同行为" class="headerlink" title="继承共同行为"></a>继承共同行为</h1><p>&emsp;先做出如下假定：有个游戏有多个角色，包括剑士，魔法师等，每个角色都有血量，名字，等级，攻击方式等。<br>&emsp;观察发现，每个角色都有名字，血量，等级三个共同特点，那么就可以将这部份提升为一个父类role（我觉得基类可能是个更好的名字）。然后剑士，魔法师这些子类（派生类）继承这些共有的属性，扩展自己的方法（攻击方式）。</p>
<h1 id="重新定义行为"><a href="#重新定义行为" class="headerlink" title="重新定义行为"></a>重新定义行为</h1><p>&emsp;进一步观察，发现每个角色都要攻击，那么就可以把“攻击”这个方法（<code>fight()</code>）提取到父类中。然后在子类中重写(Override)这个方法。父类中的fight()方法实际是空的，具体的攻击行为留到子类中重写。我这里写一个调用<code>fight()方法</code>的方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">drawFight</span><span class="params">(Role role)</span></span>&#123;</span><br><span class="line">    System.out.println(role.getName);</span><br><span class="line">    role.fight();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;我将传入的参数定义为Role类型，这样不管我传入的时剑士还是魔法师类型都可以。这就是多态的表现形式。剑士是（is-a）Role类型。在重写这个方法时，如果一不小心写错了，例如fight()写成了Fight()，那就不是重写了。可以在重写的上一行添加标注<code>@Override</code>。它会检查该方法是否重新定义了某方法。</p>
<h1 id="抽象方法和抽象类"><a href="#抽象方法和抽象类" class="headerlink" title="抽象方法和抽象类"></a>抽象方法和抽象类</h1><p>&emsp;在上面的例子中，父类中的fight()方法中是空的，问题是如果不使用子类的fight()方法就会出现没有攻击行为的情况。这种情况下可以把fight()理解为一个抽象方法，在前面加上<code>abstract</code>关键字。这表示这个方法是不完整的，同时，这个类也是不完整的，因此类也要加上<code>abstract</code>。当一个子类继承抽象类时，要么自己也保持抽象类，要么就是将这个抽象方法重写完整。这里只是简单提一下，具体细节在下篇讨论。</p>
<h1 id="protected成员"><a href="#protected成员" class="headerlink" title="protected成员"></a>protected成员</h1><p>&emsp;对于继承来说，protected是非常重要的。protected的成员相较于默认的多了个子类范围，即可以在同一个类中，同一个包中，同一个或不同个包的子类中访问。</p>
<h1 id="重写的一些小细节"><a href="#重写的一些小细节" class="headerlink" title="重写的一些小细节"></a>重写的一些小细节</h1><p>&emsp;前面写过，可以用super来调用父类的东西，但是要注意的是不能调用private的父类。同时，重写还要注意“两同两小一大”的规则，权限只能扩大不能缩小。在JDK5之前，返回类型是必须相同的，但在5之后可以相同也可以更小。即可以返回一个父类对象或一个子类对象。<br><br>&emsp;在林的书中说：static的方法属于类所有，在子类中定义了一个相同的static方法,这个方法属于子类，而不是重写。我觉得这句话有错，重写的方法本身就不是父类的，它就是子类的方法，父类的是父类的，两者不产生任何影响，只是说子类的会将父类的“盖”住，导致调用的时候会用到子类的。</p>
<h1 id="能否继承private"><a href="#能否继承private" class="headerlink" title="能否继承private"></a>能否继承private</h1><p>参看链接<a href="https://blog.csdn.net/zhousenshan/article/details/51222908" target="_blank" rel="noopener">https://blog.csdn.net/zhousenshan/article/details/51222908</a></p>
<h1 id="再论-和equals"><a href="#再论-和equals" class="headerlink" title="再论==和equals"></a>再论==和equals</h1><h2 id><a href="#" class="headerlink" title="=="></a>==</h2><p>&emsp;当比较的数据是基本数据类型，==在两个数据大小一致时返回true，即使两个数据的类型并不一致。<br><br>&emsp;当比较的是引用对象时，==只有在两个引用变量指向同一个对象时才返回true。同时，==不可以用于比较类型上没有父子关系的两个对象。<br></p>
<h2 id="equals"><a href="#equals" class="headerlink" title="equals"></a>equals</h2><p>&emsp;本以为equals就是用来比较的是对象的值，但实际上还是有点区别的。首先，在Object中，equals和==的效果是一样的，都是比较两个是否是同一个对象（比较内存地址）<br>参考链接：<a href="https://blog.csdn.net/snowqoo/article/details/2455605" target="_blank" rel="noopener">https://blog.csdn.net/snowqoo/article/details/2455605</a>，<a href="https://www.cnblogs.com/www123----/p/7857298.html" target="_blank" rel="noopener">https://www.cnblogs.com/www123----/p/7857298.html</a>,<a href="https://www.cnblogs.com/yangzhilong/p/6391548.html" target="_blank" rel="noopener">https://www.cnblogs.com/yangzhilong/p/6391548.html</a></p>
<h1 id="toString方法"><a href="#toString方法" class="headerlink" title="toString方法"></a>toString方法</h1><p>&emsp;有如下代码（不完整）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Person P = <span class="keyword">new</span> Person(<span class="string">"孙悟空"</span>);</span><br><span class="line">System.out.println(p);</span><br></pre></td></tr></table></figure>

<p>&emsp;当答应一个对象时，会返回“类名+@+hashCode”值，这是调用了toString()方法。它是一个特殊的方法，用于在打印对象时输出“自我描述”信息。<code>System.out.println(p);</code>相当于<code>System.out.println(p.toString());</code>。但是原本的返回值的信息太简略了，可以重载Object类的toString()方法。</p>
<h1 id="类成员"><a href="#类成员" class="headerlink" title="类成员"></a>类成员</h1><p>&emsp;在Java类中只能包含成员变量，方法，构造器，初始化块，内部类（包括接口，枚举）五种成员，static可以修饰除构造器之外的四种，用static修饰的成员属于类，而不属于某个对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">NullAccessStatic</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"static修饰的类方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        NullAccessStatic nas=<span class="keyword">null</span>;</span><br><span class="line">        nas.test();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="单例-Singleton-类"><a href="#单例-Singleton-类" class="headerlink" title="单例(Singleton)类"></a>单例(Singleton)类</h1><p>&emsp;在某些时候，不需要根据类创建多个对象，只需要一个。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Singleton instance;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getinstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (instance == <span class="keyword">null</span>) &#123;</span><br><span class="line">            instance = <span class="keyword">new</span> Singleton();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Singleton s1 = Singleton.getinstance();</span><br><span class="line">        Singleton s2 = Singleton.getinstance();</span><br><span class="line">        System.out.println(s1 == s2);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;首先需要将原来的构造函数隐藏起来，防止自由创建对象。在类中提供一个类变量存储对象的缓存，然后提供一个静态的方法用来在缓存为空时生成对象，如果缓存不为空，就将这个缓存返回。</p>
<h1 id="final修饰符"><a href="#final修饰符" class="headerlink" title="final修饰符"></a>final修饰符</h1><p>&emsp;final关键字可用于修饰类，变量，方法，表示它们不可改变。final的变量获得了初始值后就不可改变，不是不可赋值。变量类型可以是类变量，实例变量，局部变量，形参。</p>
<h2 id="final成员变量"><a href="#final成员变量" class="headerlink" title="final成员变量"></a>final成员变量</h2><p>&emsp;成员变量是随着类初始化或对象初始化而初始化的，因为final的变量一旦赋值后就不能改变，所以必须在初始化时指定初始值，不然初始化结束后里面就是0，“\u0000”，false，null等初始值，完全没有意义。<br>&emsp;对于它们的使用，最关键的就两点：可以在哪指定初始值以及指定后就不能再改变。</p>
<ul>
<li>类变量：必须在静态初始化块或声明该变量时指定</li>
<li>实例变量：必须在非静态初始化块，声明该变量或构造器中指定。</li>
</ul>
<h2 id="final局部变量"><a href="#final局部变量" class="headerlink" title="final局部变量"></a>final局部变量</h2><p>&emsp;并没有什么特别的，要注意，作为形参时，其值应由传入的参数决定。</p>
<h2 id="final修饰基本类型和引用类型"><a href="#final修饰基本类型和引用类型" class="headerlink" title="final修饰基本类型和引用类型"></a>final修饰基本类型和引用类型</h2><p>&emsp;当修饰引用类型时，它仅仅强调引用的地址，即指向的对象不变，而对象本身内的改变与之无关。</p>
<h2 id="可执行宏替换的final变量"><a href="#可执行宏替换的final变量" class="headerlink" title="可执行宏替换的final变量"></a>可执行宏替换的final变量</h2><ul>
<li>使用final修饰符</li>
<li>定义final变量初始值时指定了初始值</li>
<li>该初始值可以在编译时就确定下来</li>
</ul>
<p>&emsp;满足以上条件的变量将变成一个直接辆，比如<code>final int a=5</code>后，再使用a时，会将a直接替换为5。</p>
<h2 id="final方法"><a href="#final方法" class="headerlink" title="final方法"></a>final方法</h2><p>&emsp;final修饰的方法时不能被重写。但有种特殊的情况：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PrivateFinalMethodTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Sub</span> <span class="keyword">extends</span> <span class="title">PrivateFinalMethodTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;private修饰的父类方法，对于子类来说是不可见的，因此也就不存在重写父类方法。<br>对于final修饰的方法仅仅是不能被重写，但是可以重载。</p>
<h2 id="final类"><a href="#final类" class="headerlink" title="final类"></a>final类</h2><p>&emsp;对于final类，它不能被继承。</p>
<h2 id="不可变类-immutable"><a href="#不可变类-immutable" class="headerlink" title="不可变类(immutable)"></a>不可变类(immutable)</h2><p>&emsp;不可变类的意思是该类的实例变量被创建后就不可改变。Java的八个包装类和String类都是不可变类。要创建自己的不可变类要遵循如下规则：</p>
<ul>
<li>使用private和final来修饰该类的成员变量</li>
<li>提供带参数构造器，用于根据传入参数来初始化类里的成员变量</li>
<li>仅提供getter方法，普通方法是无法修改final的变量的</li>
<li>如有必要，重写hashCode()和equals()方法。equals()方法根据关键成员变量来作为两个人对象是否相等的标准，除此之外，还要保证两个用equals()方法判断相等的对象的hashCode()也相等。</li>
</ul>
<p>&emsp;例如String这个类，它是根据String对象里的字符序列来作为相等的标准，其hashCode()方法也是根据字符序列计算得到的。下面给出例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Address</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String detail;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String postCode;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Address</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.detail = <span class="string">""</span>;</span><br><span class="line">        <span class="keyword">this</span>.postCode = <span class="string">""</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Address</span><span class="params">(String detail, String postCode)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.detail = detail;</span><br><span class="line">        <span class="keyword">this</span>.postCode = postCode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getDetail</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.detail;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getPostCode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.postCode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object obj)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span> == obj) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (obj != <span class="keyword">null</span> &amp;&amp; obj.getClass() == Address<span class="class">.<span class="keyword">class</span>) </span>&#123;</span><br><span class="line">            Address ad = (Address) obj;</span><br><span class="line">            <span class="keyword">if</span> (<span class="keyword">this</span>.getDetail().equals(ad.getDetail()) &amp;&amp; <span class="keyword">this</span>.getPostCode().equals(ad.getPostCode())) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> detail.hashCode() + postCode.hashCode() * <span class="number">31</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;前面写过，final仅仅能表示修饰的引用变量的指向不变，其对象还是可以任意改变的。如果，在一个不可变类中定义了一个final的成员变量是引用类型的，那么这个不可变类就失败了，它可变。例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Name</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String firstName;</span><br><span class="line">    <span class="keyword">private</span> String lastName;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Name</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Name</span><span class="params">(String firstName, String lastName)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="keyword">this</span>.firstName = firstName;</span><br><span class="line">        <span class="keyword">this</span>.lastName = lastName;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getFirstName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> firstName;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setFirstName</span><span class="params">(String firstName)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.firstName = firstName;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getLastName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> lastName;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setLastName</span><span class="params">(String lastName)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.lastName = lastName;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Name name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Person</span><span class="params">(Name name)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Name <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// TODO Auto-generated method stub</span></span><br><span class="line">        Name n=<span class="keyword">new</span> Name(<span class="string">"aa"</span>,<span class="string">"bb"</span>);</span><br><span class="line">        Person p=<span class="keyword">new</span> Person(n);</span><br><span class="line">        System.out.println(p.getName().getFirstName());</span><br><span class="line">        n.setFirstName(<span class="string">"cc"</span>);</span><br><span class="line">        System.out.println(p.getName().getFirstName());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;按上面的代码我可以随便修改Person对象p的值。请看下面修改的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> test1;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Name name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Person</span><span class="params">(Name name)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// this.name=name;</span></span><br><span class="line">        <span class="keyword">this</span>.name = <span class="keyword">new</span> Name(name.getFirstName(), name.getLastName());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Name <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Name(name.getFirstName(), name.getLastName());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// TODO Auto-generated method stub</span></span><br><span class="line">        Name n = <span class="keyword">new</span> Name(<span class="string">"aa"</span>, <span class="string">"bb"</span>);</span><br><span class="line">        Person p = <span class="keyword">new</span> Person(n);</span><br><span class="line">        System.out.println(p.getName().getFirstName());</span><br><span class="line">        n.setFirstName(<span class="string">"cc"</span>);</span><br><span class="line">        System.out.println(p.getName().getFirstName());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;现在就无法修改p的值了，原理在于，原来p的name就是n的值，当修改n时，p的name也被改变。现在，在创建p时，调用构造函数后会创建一个临时Name对象，把传进来的name的值（即具体内容，而不是将引用/地址）给这个临时对象，然后将临时对象赋给p的name。在以后修改n时，不会对p有任何影响，因为n和临时变量只有数值上的传递。</p>
<h2 id="缓存实例的不可变类"><a href="#缓存实例的不可变类" class="headerlink" title="缓存实例的不可变类"></a>缓存实例的不可变类</h2><p>&emsp;不可变类由于一旦创建就不可更改，所以可以方便的进行共享，应该考虑缓存这个实例。缓存的实现很多，这里先考虑用数组实现。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> cacheImmutale;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CacheImmutale</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> MAX_SIZE = <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> CacheImmutale[] cache = <span class="keyword">new</span> CacheImmutale[MAX_SIZE];</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> pos = <span class="number">0</span>;<span class="comment">// 记录实例在缓存中的位置，cache[pos-1]是最新缓存的实例</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">CacheImmutale</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> CacheImmutale <span class="title">valueOf</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; MAX_SIZE; i++) &#123;<span class="comment">//遍历是否有相同的</span></span><br><span class="line">            <span class="keyword">if</span> (cache[i] != <span class="keyword">null</span> &amp;&amp; cache[i].getName().equals(name)) &#123;</span><br><span class="line">                <span class="keyword">return</span> cache[i];</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (pos == MAX_SIZE) &#123;<span class="comment">//如果满了就把第一个替换</span></span><br><span class="line">            cache[<span class="number">0</span>] = <span class="keyword">new</span> CacheImmutale(name);</span><br><span class="line">            pos = <span class="number">1</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            cache[pos++] = <span class="keyword">new</span> CacheImmutale(name);<span class="comment">//新建一个CacheImmutale对象，将这个对象的name赋值，并保存在数组中</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> cache[pos - <span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object obj)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span> == obj) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (obj != <span class="keyword">null</span> &amp;&amp; obj.getClass() == CacheImmutale<span class="class">.<span class="keyword">class</span>) </span>&#123;</span><br><span class="line">            CacheImmutale ci = (CacheImmutale) obj;</span><br><span class="line">            <span class="keyword">return</span> name.equals(ci.getName());</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name.hashCode();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> cacheImmutale;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CacheImmutaleTest</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// TODO Auto-generated method stub</span></span><br><span class="line">        CacheImmutale c1 = CacheImmutale.valueOf(<span class="string">"hello"</span>);</span><br><span class="line">        CacheImmutale c2 = CacheImmutale.valueOf(<span class="string">"hello"</span>);</span><br><span class="line">        System.out.println(c1 == c2);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;分析上面的代码，根据<code>CacheImmutale</code>类创建的对象是一个不可更改的String字符串name，这个类有一个数组，用于保存输入的name。有两个细节要注意，第一：<code>cache[pos++] = new CacheImmutale(name);</code>pos++是先把pos的值传出去在自己加1，所以相当于<code>cache[pos] = new CacheImmutale(name);pos=pos+1;</code>注意不是将name的值赋给第一个。第二：为什么valueOf要返回cache数组中的一个？因为cache数组是CacheImmutale[]类型的，里面都是CacheImmutale类的对象，要把这个对象指向c1和c2，如果name相同，也就没有新建对象，c1和c2指向的是同一个对象。<br><br>&emsp;Integer类采用的就是类似的策略，如果用new，则每次都返回全新的对象，如果是valueOf()方法，则会启用缓存策略，但只缓存-128~127之间的。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记5</title>
    <url>/2018/04/10/Java%E7%AC%94%E8%AE%B05/</url>
    <content><![CDATA[<p>上一篇主要回顾了一些细枝末节，这篇讲继承与多态。</p>
<a id="more"></a>

<h1 id="类的继承"><a href="#类的继承" class="headerlink" title="类的继承"></a>类的继承</h1><p>&emsp;<code>class SubClass extends SuperClass</code>中的extends表示SubClass继承于SuperClass，但是extends本意是扩展的意思。因为翻译问题，子类和父类有各种名字。<br>子类继承父类的全部成员变量和方法（有关于private的有点小例外），但是构造器不算。<br><br>&emsp;一个子类只能有一个父类，是单继承关系，但是可以有多层继承关系。所有的类都是java.lang.Object类的子类或间接子类。<br>&emsp;子类扩展（extends）了父类，父类派生（derive）了子类。<br>&emsp;方法的重写要遵循“两同两小一大”的规则，“两同”即方法名，形参列表相同；“两小”即子类方法返回值类型应比父类方法返回值类型更小或相等，子类方法声明抛出的异常类应比父类方法声明抛出的异常类更小或相等；“一大”指子类方法的访问权限要比父类的更大或相等。而且，字类和父类的静态与非静态要对应。<br><br>&emsp;当子类覆盖了父类后，在子类中就不能直接访问父类中的方法，但是可以通过super（实例方法）和父类类名（类方法，静态的）调用。对于有private的父类方法，子类是不可见的，即使满足上面的规则也不是重写。<strong>还有一点要注意的是，当参数列表不同时，就不是重写，而是父类与子类方法的重载</strong>。</p>
<h2 id="super的使用"><a href="#super的使用" class="headerlink" title="super的使用"></a>super的使用</h2><p>&emsp;前面写到，如果要在子类中调用父类中被覆盖的实例方法，则要使用super。像this一样，super也不能在static中使用，因为它是用来调用实例方法的。<br><br>&emsp;如果在构造器中使用super的话，则表示限定改构造器初始化的对象是从父类继承得到的实例变量，该类自己拓展的变量将不会使用。<br><br>&emsp;上面还写到，当遵循了“两同两小一大”原则时发生了重写。类似的，当实例变量发生覆盖时，也可以使用super来调用父类的实例变量。但是如果没有显式的指定super，那就会根据下面的顺序检查实例变量：<br></p>
<ol>
<li>查找该方法中是否有局部变量a;</li>
<li>查找当前类中是否有成员变量a;</li>
<li>查找a的直接父类中是否有成员变量a,一次上溯到a的所有父类，直到java.lang.object，若还没有就出现编译错误。</li>
</ol>
<p>&emsp;当程序创建一个子类对象时，不仅会为该类中定义的实例变量创建内存空间，其父类包括间接父类的实例变量也会被创建，即使使用的是同一个名字。存在一种下面的特殊情况:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String tag = <span class="string">"hello"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Derived</span> <span class="keyword">extends</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String tag = <span class="string">"hi"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HideTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Derived d = <span class="keyword">new</span> Derived();</span><br><span class="line">        <span class="comment">//System.out.println(d.tag);</span></span><br><span class="line">        System.out.println(((Parent) d).tag);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;首先这是在main方法中，显然不能用super。这里的写法是将<code>d</code>强制转化为Parent类型。解决方法多的是，我也试着写了一个：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String tag = <span class="string">"hello"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Derived</span> <span class="keyword">extends</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String tag = <span class="string">"hi"</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">method</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="keyword">super</span>.tag);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HideTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Derived d = <span class="keyword">new</span> Derived();</span><br><span class="line">        method();</span><br><span class="line">        <span class="comment">// System.out.println(super.tag);</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="使用父类的构造器"><a href="#使用父类的构造器" class="headerlink" title="使用父类的构造器"></a>使用父类的构造器</h2><p>与子类无法获取父类的private的成员变量一样，子类是无法获取父类的构造器的，但是一样可以调用父类的初始化代码，类似于前面讲过的一个构造器调用另一个重载的构造器。在一个构造器中调用别的构造器使用this，在子类中调用父类的构造器则用super。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">double</span> size;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Base</span><span class="params">(<span class="keyword">double</span> size,String name)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.size=size;</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Sub</span> <span class="keyword">extends</span> <span class="title">Base</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String color;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Sub</span><span class="params">(<span class="keyword">double</span> size,String name,String color)</span></span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(size,name);</span><br><span class="line">        <span class="keyword">this</span>.color=color;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Sub s=<span class="keyword">new</span> Sub(<span class="number">5.6</span>,<span class="string">"测试对象"</span>,<span class="string">"红色"</span>);</span><br><span class="line">        System.out.println(s.size+<span class="string">"--"</span>+s.name+<span class="string">"--"</span>+s.color);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;从上面的代码中可以看出，super和this的行为非常相似。同时，super也必须在第一行，this和super也不能同时出现。<br>&emsp;有意思的是不管是否用super调用来执行父类构造器的代码，子类构造器总会调用父类构造器一次。子类构造器调用父类构造器分如下几种情况:</p>
<ol>
<li>子类构造器执行体的第一行使用super显式调用父类构造器，系统将根据super调用里的参数调用对应的父类构造器。</li>
<li>子类构造器执行体的第一行代码使用this显式调用本类中重载的构造器，系统将根据this的参数选择合适的本类的构造器。执行本类的另一个构造器前会调用父类的构造器。</li>
<li>子类构造器执行体既没有super也没有this，系统将会在调用子类构造器之前隐式调用父类构造器。</li>
</ol>
<p>&emsp;不管是哪种情况，父类构造器都要在子类构造器之前调用，而且不止一层，要直至<code>java.lang.object</code>。</p>
<p>&emsp;如果使用eclipse等IDE写代码，可以自动生成构造函数。但是你会发现，生成出来的都是这样的:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person2</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Person2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Person2</span><span class="params">(<span class="keyword">int</span> age, String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;一个不带参数，另一个带参数，但是都有一个<code>super()</code>。为什么要有<code>super()</code>调用父类的构造函数？很简单，前面说过，子类是父类的扩充，如果使用构造函数生成子类时，不用<code>super()</code>将父类一并生成，那么子类要从哪里来呢？这就是上面说所有的调用构造器最终都会调用<code>java.lang.object</code>的原因。就像是人一样，生长时通过细胞分化，长出各种不同的细胞，但是最根本的还是离不了干细胞。下面举个例子加深印象:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Creature</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Creature</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Creature无参数的构造器"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Animal</span> <span class="keyword">extends</span> <span class="title">Creature</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Animal</span><span class="params">(String name)</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Animal带一个参数的构造器"</span>+<span class="string">"该动物的name为"</span>+name);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Animal</span><span class="params">(String name,<span class="keyword">int</span> age)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name;</span><br><span class="line">        System.out.println(<span class="string">"Animal带两个参数的构造器"</span>+<span class="string">"该动物的age为"</span>+age);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Wolf</span> <span class="keyword">extends</span> <span class="title">Animal</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Wolf</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(<span class="string">"灰太狼"</span>,<span class="number">3</span>);</span><br><span class="line">        System.out.println(<span class="string">"Wolf无参数的构造器"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> Wolf();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BaseClass</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> book = <span class="number">6</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">base</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"父类的普通方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"父类的被覆盖的方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SubClass</span> <span class="keyword">extends</span> <span class="title">BaseClass</span> </span>&#123;</span><br><span class="line">    <span class="comment">//重新定义一个book实例变量来隐藏父类的book实例变量</span></span><br><span class="line">    <span class="keyword">public</span> String book = <span class="string">"阿拉伯的劳伦斯"</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"子类的覆盖父类的方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sub</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"子类的普通方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//下面编译时类型和运行时类型时一样,不存在多态</span></span><br><span class="line">        BaseClass bc = <span class="keyword">new</span> BaseClass();</span><br><span class="line">        <span class="comment">//输出6</span></span><br><span class="line">        System.out.println(bc.book);</span><br><span class="line">        <span class="comment">//执行BaseClass的方法</span></span><br><span class="line">        bc.base();</span><br><span class="line">        bc.test();</span><br><span class="line">        <span class="comment">//下面编译时类型和运行时类型时一样,不存在多态</span></span><br><span class="line">        SubClass sc = <span class="keyword">new</span> SubClass();</span><br><span class="line">        <span class="comment">//输出"阿拉伯的劳伦斯"</span></span><br><span class="line">        System.out.println(sc.book);</span><br><span class="line">        <span class="comment">//执行BaseClass的方法</span></span><br><span class="line">        sc.base();</span><br><span class="line">        <span class="comment">//执行SubClass的方法</span></span><br><span class="line">        sc.test();</span><br><span class="line">        <span class="comment">//下面编译时类型与运行时类型不一样,多态发生</span></span><br><span class="line">        BaseClass ploymophicBc = <span class="keyword">new</span> SubClass();</span><br><span class="line">        <span class="comment">//输出6---表明访问的是父类对象的实例变量</span></span><br><span class="line">        System.out.println(ploymophicBc.book);</span><br><span class="line">        <span class="comment">//调用从父类继承的base()方法</span></span><br><span class="line">        ploymophicBc.base();</span><br><span class="line">        <span class="comment">//调用当前类的test()方法</span></span><br><span class="line">        ploymophicBc.test();</span><br><span class="line">        <span class="comment">//因为ploymophicBc的编译时类型是BaseClass</span></span><br><span class="line">        <span class="comment">//BaseClass没有sub()方法,所以下面的方法会出现错误</span></span><br><span class="line">        <span class="comment">//ploymophicBc.sub();</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">6</span></span><br><span class="line">父类的普通方法</span><br><span class="line">父类的被覆盖的方法</span><br><span class="line">阿拉伯的劳伦斯</span><br><span class="line">父类的普通方法</span><br><span class="line">子类的覆盖父类的方法</span><br><span class="line"><span class="number">6</span></span><br><span class="line">父类的普通方法</span><br><span class="line">子类的覆盖父类的方法</span><br></pre></td></tr></table></figure>

<p>&emsp;先不管什么是多态,上面的代码还有点问题,既然ploumophicBc是BaseClass类型,不能用sub方法,那么为什么能用SubClass的test方法?李书讲的很迷啊.对于多态的解释也非常奇怪:当编译时类型与运行时类型不一样时就有可能发生多态.<code>BaseClass ploymophicBc = new SubClass();</code>什么叫ploymophicBc的编译时类型是BaseClass,运行时类型是SubClass???至于林书,emmm,更迷了.例如:当一个方法可以接受多种类型的参数就是多态.这TM都什么解释啊,两个人说的八杆子打不着…</p>
<h2 id="继承与组合"><a href="#继承与组合" class="headerlink" title="继承与组合"></a>继承与组合</h2><p>&emsp;先不论多态到底是什么，回到上文，继承还有一些要注意的地方。<br>&emsp;以前写过，类应该保证良好的封装性，但是父类无疑破坏了这种封装，而且如果子类对父类进行重写等操作无疑会提高耦合性。这是非常危险的。所以，父类需要遵循一定的原则保证封装性：</p>
<ul>
<li>尽量隐藏父类的内部数据，成员变量应尽量设置为private的。</li>
<li>不要让父类可以随意直接访问，修改父类的方法。一些辅助性的方法要设置为private的。如果父类的方法需要被外部类使用，则设为public，但是又不要子类修改，则要设为final。而希望子类重写又不要别的类自由访问的方法则设置为protected。</li>
<li>尽量不要在父类构造器中调用要被子类重写额方法，具体原因看下面的代码：</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Base</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        test();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"将被子类重写的方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Sub</span> <span class="keyword">extends</span> <span class="title">Base</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"子类重写父类的方法，"</span>+<span class="string">"name的长度为"</span>+name.length());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Sub s = <span class="keyword">new</span> Sub();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这段代码会引发空指针异常。当创建Sub对象时，会先上溯到Base类，调用Base类的构造器，而在这个构造器中又调用了test()方法，但是这里事调用了子类中的test()方法。为什么会这样，还需要再强调一下什么是“重写（Overriding）”，请看链接：<a href="http://www.runoob.com/java/java-override-overload.html" target="_blank" rel="noopener">http://www.runoob.com/java/java-override-overload.html</a>。里面有张图，看了后就明白了，“重写”其实相当于一种<strong>覆盖</strong>，在上面的代码中父类的test()方法已经是不可见的了，除非是用super调用。再来看空指针异常，异常的原因不是表面上我没有给name赋值，而是，name是一个成员变量，这个时候还在类初始化期，s这个对象还没出来。所以对于一个空的东西是没法调用length()方法的。<br><br>&emsp;这里还要提到一点“初始化”的顺序，这比较复杂。给出链接：<a href="https://www.zhihu.com/question/49196023" target="_blank" rel="noopener">https://www.zhihu.com/question/49196023</a></p>
<blockquote>
<p>a. 加载类<br><br>1 为父类静态属性分配内存并赋值 / 执行父类静态代码段 （按代码顺序）<br><br>2 为子类静态属性分配内存并赋值 / 执行子类静态代码段 （按代码顺序）<br><br>b. 创建对象<br><br>1 为父类实例属性分配内存并赋值 / 执行父类非静态代码段 （按代码顺序）<br><br>2 执行父类构造器<br><br>3 为子类实例属性分配内存并赋值 / 执行子类非静态代码段 （按代码顺序）<br><br>4 执行子类构造器<br><br>&emsp;注意，静态属性就是类变量，静态代码段就是静态初始化块。<br>&emsp;按照这个顺序，注意这个顺序是初始化类的顺序，这个时候还没有对象出来。由于并没有静态属性和静态代码块等东西，所以直接进入b的2，调用了子类的test()方法，但是问题是4还没执行，不存在成员变量，所以出现了空指针异常。<br>&emsp;这个话题暂时打住，类的加载比较复杂，以后会专门说。<br>&emsp;回到前文，不仅要考虑继承时的规则，也要考虑什么时候什么时候应该派生子类，什么时候没有必要派生。用简单两句话可以概括：子类需要增加额外的属性，子类要增加自己独有的行为方式。<br>&emsp;还有另外一种实现复用的方式：组合。先来看用继承实现的代码：</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Animal</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">beat</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"心脏跳动..."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">breath</span><span class="params">()</span></span>&#123;</span><br><span class="line">        beat();</span><br><span class="line">        System.out.println(<span class="string">"吸气，吐气..."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bird</span> <span class="keyword">extends</span> <span class="title">Animal</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fly</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"在天空飞翔"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Wolf</span> <span class="keyword">extends</span> <span class="title">Animal</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"在陆地奔跑"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">InheritTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Bird b=<span class="keyword">new</span> Bird();</span><br><span class="line">        b.breath();</span><br><span class="line">        b.fly();</span><br><span class="line">        Wolf w=<span class="keyword">new</span> Wolf();</span><br><span class="line">        w.breath();</span><br><span class="line">        w.run();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>再给出用组合的方式实现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Animal</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">beat</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"心脏跳动..."</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">breath</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        beat();</span><br><span class="line">        System.out.println(<span class="string">"吸气，吐气..."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bird</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Animal a;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Bird</span><span class="params">(Animal a)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.a = a;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">breath</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        a.breath();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fly</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"在天空飞翔"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Wolf</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Animal a;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Wolf</span><span class="params">(Animal a)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.a = a;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">breath</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        a.breath();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"在陆地奔跑"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">InheritTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Animal a1 = <span class="keyword">new</span> Animal();</span><br><span class="line">        Bird b = <span class="keyword">new</span> Bird(a1);</span><br><span class="line">        b.breath();</span><br><span class="line">        b.fly();</span><br><span class="line">        Animal a2 = <span class="keyword">new</span> Animal();</span><br><span class="line">        Wolf w = <span class="keyword">new</span> Wolf(a2);</span><br><span class="line">        w.breath();</span><br><span class="line">        w.run();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;组合的方式显然是在原来的子类中包含另外的原来的父类。继承和组合都是实现复用，但是对于上面的代码来说，继承更合适。而多余Person类和Arm类更加适合用组合。继承表达的是“是(is-a)”的关系，组合表达的是“有(has-a)”的关系。</p>
<h2 id="初始化块"><a href="#初始化块" class="headerlink" title="初始化块"></a>初始化块</h2><p>&emsp;关于初始化块，我的疑问非常多，求助了许多地方都没有得到解决，无奈只能放一放，先往下进行。<br><br>&emsp;一个类中可以有多个初始化块，但是最好还是放在一起。初始化块只在创建对象时隐式的执行，且在构造器之前执行。请看下面的代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">InstanceInitTest</span> </span>&#123;</span><br><span class="line">    &#123;</span><br><span class="line">        a=<span class="number">6</span>;</span><br><span class="line">   &#125;</span><br><span class="line">    <span class="keyword">int</span> a=<span class="number">9</span>;</span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span></span><br><span class="line"><span class="function">   </span>&#123;</span><br><span class="line">       System.out.println(<span class="keyword">new</span> InstanceInitTest().a);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>a. 加载类<br><br>1 为父类静态属性分配内存并赋值 / 执行父类静态代码段 （按代码顺序）<br><br>2 为子类静态属性分配内存并赋值 / 执行子类静态代码段 （按代码顺序）<br><br>b. 创建对象<br><br>1 为父类实例属性分配内存并赋值 / 执行父类非静态代码段 （按代码顺序）<br><br>2 执行父类构造器<br><br>3 为子类实例属性分配内存并赋值 / 执行子类非静态代码段 （按代码顺序）<br><br>4 执行子类构造器<br></p>
</blockquote>
<p>&emsp;按照上面的顺序，<code>a=6</code>比<code>int a=9</code>先执行，结果出来也确实是9.但是，我的问题是，在执行<code>a=6</code>时，a还未声明，它是怎么执行的？注意：<code>a=6</code>是在{}中的，所以如果在那里声明变量的话，a就是个局部变量，现在a没有在里面声明，说明a在{}之外已被声明过了，但是<code>int a=9</code>又在后面，到底是怎么回事呢？<br><br>&emsp;初始化块和构造器有点类似，都是对<strong>对象</strong>进行初始化操作。实际上，初始化块可以看成把几个重载的构造器中共同的部分提取出来，减少代码量。同时，在编译后初始化块会被还原到构造器中，且位于最前面。<br><br>&emsp;再回到上面的问题，根据李刚本人的回答，<code>int a=9</code>被拆成两截，<code>int a</code>和<code>a=9</code>，a=9是被还原到构造器中的，而int a没有，不然就是两次声明同一个变量。我猜测，应该是在类加载时，将这种声明提前了。查看相应的字节码文件：<br><img src="/2018/04/10/Java%E7%AC%94%E8%AE%B05/javap.png" alt="javap"><br><br>&emsp;可以看到，int a在构造器之前。更详细的内容：<br><img src="/2018/04/10/Java%E7%AC%94%E8%AE%B05/javap-c.png" alt="javap"><br><br>&emsp;当把代码改为下面的后：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StaticInitTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        a = <span class="number">6</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> a = <span class="number">9</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="keyword">new</span> StaticInitTest().a);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/04/10/Java%E7%AC%94%E8%AE%B05/javapc.png" alt="javap"><br><br>&emsp;在这几张图片中出现了&lt;init&gt;这个东西，具体是什么请看链接<a href="https://blog.csdn.net/u013309870/article/details/72975536" target="_blank" rel="noopener">https://blog.csdn.net/u013309870/article/details/72975536</a>。按链接所说，这个东西会对非静态的代码合并，&lt;clinit&gt;会对静态的合并，看第二个代码的图，合并的非常明显。但是图里没有出现clinit,是因为某种“美化”，实际上是有的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// class version 53.0 (53)</span></span><br><span class="line"><span class="comment">// access flags 0x21</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">test3</span>/<span class="title">StaticInitTest</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// compiled from: StaticInitTest.java</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// access flags 0x8</span></span><br><span class="line">  <span class="keyword">static</span> I a</span><br><span class="line"></span><br><span class="line">  <span class="comment">// access flags 0x8</span></span><br><span class="line">  <span class="keyword">static</span> &lt;clinit&gt;()V</span><br><span class="line">   L0</span><br><span class="line">    LINENUMBER <span class="number">5</span> L0</span><br><span class="line">    BIPUSH <span class="number">6</span></span><br><span class="line">    PUTSTATIC test3/StaticInitTest.a : I</span><br><span class="line">   L1</span><br><span class="line">    LINENUMBER <span class="number">7</span> L1</span><br><span class="line">    BIPUSH <span class="number">9</span></span><br><span class="line">    PUTSTATIC test3/StaticInitTest.a : I</span><br><span class="line">    RETURN</span><br><span class="line">    MAXSTACK = <span class="number">1</span></span><br><span class="line">    MAXLOCALS = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// access flags 0x1</span></span><br><span class="line">  <span class="keyword">public</span> &lt;init&gt;()V</span><br><span class="line">   L0</span><br><span class="line">    LINENUMBER <span class="number">3</span> L0</span><br><span class="line">    ALOAD <span class="number">0</span></span><br><span class="line">    INVOKESPECIAL java/lang/Object.&lt;init&gt;()V</span><br><span class="line">    RETURN</span><br><span class="line">   L1</span><br><span class="line">    LOCALVARIABLE <span class="keyword">this</span> Ltest3/StaticInitTest; L0 L1 <span class="number">0</span></span><br><span class="line">    MAXSTACK = <span class="number">1</span></span><br><span class="line">    MAXLOCALS = <span class="number">1</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// access flags 0x9</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="title">main</span><span class="params">([Ljava/lang/String;)</span>V</span></span><br><span class="line"><span class="function">   L0</span></span><br><span class="line"><span class="function">    LINENUMBER 10 L0</span></span><br><span class="line"><span class="function">    GETSTATIC java/lang/System.out : Ljava/io/PrintStream</span>;</span><br><span class="line">    NEW test3/StaticInitTest</span><br><span class="line">    INVOKESPECIAL test3/StaticInitTest.&lt;init&gt;()V</span><br><span class="line">    GETSTATIC test3/StaticInitTest.a : I</span><br><span class="line">    INVOKEVIRTUAL java/io/PrintStream.println(I)V</span><br><span class="line">   L1</span><br><span class="line">    LINENUMBER <span class="number">11</span> L1</span><br><span class="line">    RETURN</span><br><span class="line">   L2</span><br><span class="line">    LOCALVARIABLE args [Ljava/lang/String; L0 L2 <span class="number">0</span></span><br><span class="line">    MAXSTACK = <span class="number">2</span></span><br><span class="line">    MAXLOCALS = <span class="number">1</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;忽略这些不相关的，a=6为什么可以，是因为类加载时将a的声明放在了前面。但是还是有着疑问，如果是这样的话，我将a=6放在代码块外为什么就不行？这个问题还未结束。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记7</title>
    <url>/2018/04/24/Java%E7%AC%94%E8%AE%B07/</url>
    <content><![CDATA[<p>&emsp;这章主要写接口，内部类和Lambda表达式。</p>
<a id="more"></a>

<h1 id="抽象类"><a href="#抽象类" class="headerlink" title="抽象类"></a>抽象类</h1><p>&emsp;假定有个Shape类，需要一个calPerimeter()方法计算各种图形的周长，但是在这个类中显然不可能将所有图形的计算周长的方法全都实现，具体的应该交给各个子类自己实现。在父类中就需要一个抽象方法，具体的方法留待子类实现。<br><br>&emsp;在抽象类中可以没有抽象方法，但是抽象方法必须在抽象类中。具体遵循下面的规则：</p>
<ul>
<li><p>抽象方法和抽象类必须使用<code>abstract</code>修饰</p>
</li>
<li><p>抽象类不能被实例化，即使里面并没有抽象方法</p>
</li>
<li><p>抽象类中可以包括普通类的各种成分（包括构造器，即使它不能被实例化，它的构造器主要是用于被子类继承）</p>
</li>
<li><p>含有抽象方法的类必须被定义成抽象类，具体包括：直接定义了一个抽象方法的类；继承了一个抽象父类但是在这个子类中并没有完全实现父类的抽象方法（即还剩抽象方法）；或实现了一个接口但没有完全实现接口包含的抽象方法。</p>
</li>
</ul>
<p>&emsp;具体代码我这里偷个懒。<br><br>&emsp;关于abstract和其他关键字的配合使用要注意，final永远不能和其一起，因为final表示不能被重写。abstract也不能用于修饰成员变量，局部变量和构造器。在和static配合使用时，不可以同时修饰某个方法，调用一个类中的没有方法体的方法肯定会出错；但是在调用一个抽象类中的静态方法是可以的；它们两个可以同时修饰内部类。private和abstract的使用也是，不能同时修饰某个方法。<br><br>&emsp;不论如何搭配使用，最简单的规则在于不能违反抽象的初衷，不能妨碍子类的重写，就比如private修饰的方法如果加上abstract，子类无法访问到也就无法进行重写，显然是不合适的。<br>&emsp;关于抽象还有一个参考链接，写的不错：<a href="https://blog.csdn.net/wei_zhi/article/details/52736350" target="_blank" rel="noopener">https://blog.csdn.net/wei_zhi/article/details/52736350</a></p>
<h1 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h1><p>&emsp;抽象类是从多个类中抽象出的模板，对抽象类更彻底的抽象就是“接口”(interface)，接口里的方法全是抽象方法，而在Java8中允许在接口中定义默认方法。<br><br>&emsp;关于什么是接口，接口的作用我也说不好，贴出个知乎链接，前几个回答写的都很好;<a href="https://www.zhihu.com/question/20111251" target="_blank" rel="noopener">https://www.zhihu.com/question/20111251</a><br><br>&emsp;接口的定义方式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">[修饰符] <span class="class"><span class="keyword">interface</span> 接口名 <span class="keyword">extends</span> 父接口1,父接口2...</span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    零到多个常量定义</span><br><span class="line">    零到多个抽象方法定义</span><br><span class="line">    零到多个内部类，内部接口，内部枚举</span><br><span class="line">    零到多个默认方法或类方法定义(这里用类方法这个名字貌似不太合适，其实就是静态方法)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;修饰符可以是public或者默认的包访问控制权限，接口的命名与类的命名类似，一个源文件中只能有一个public的接口，且名字应和源文件名字相同。<br><br>&emsp;由于接口定义的是一种规范，是完全抽象出来的东西，因此不能有构造器和初始化块。作为规范，接口是公共的，所以接口里的东西都是公共的，不论是否加上了public修饰符。<br><br>&emsp;对于接口中常量，系统会自动加上public static final这三个修饰符，同时，这也意味着只能在定义是指定默认值。<br><br>&emsp;接口中定义的抽象方法在没有关键字abstract时也会自动加上，类方法和默认方法必须要有方法体。接口中的内部类，内部接口，内部枚举都采用public static修饰。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Output</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> MAX_CACHE_LINE = <span class="number">50</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">out</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">getData</span><span class="params">(String msg)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">default</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">(String... msgs)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (String msg : msgs) &#123;</span><br><span class="line">            System.out.println(msg);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">default</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"默认的test()方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">static</span> String <span class="title">staticTest</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"接口里的类方法"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;对于默认方法，必须使用default修饰，不能使用static修饰，相对的接口中的类方法只能用static修饰，不能用default修饰。同样，他俩都会被加上public。</p>
<h2 id="接口的继承"><a href="#接口的继承" class="headerlink" title="接口的继承"></a>接口的继承</h2><p>&emsp;接口的继承不同与父类的继承，它支持一个接口继承多个直接父接口，通过extends关键字，之间用逗号隔开。</p>
<h2 id="使用接口"><a href="#使用接口" class="headerlink" title="使用接口"></a>使用接口</h2><p>&emsp;接口不可以用来创建实例，但是可以用来声明引用类型变量，但是这个引用类型变量必须引用到接口的实现类的实现，意思是：假定用接口A声明引用类型变量C，那么这个C引用的对象必须是根据A实现的类B的对象。接口的主要用途还是被实现类来实现。用途：</p>
<ul>
<li>定义变量或者用于强制类型转换</li>
<li>调用接口中的常量</li>
<li>被其他类实现</li>
</ul>
<p>&emsp;一个类可以实现一个或多个接口。子类继承父类使用extends关键字，而实现接口用implements关键字，implements必须放在extends后。一个类使用了implements关键字后，必须实现这些接口中定义的全部抽象方法，否则该类将保留继承到的抽象方法，所以要定义为抽象类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Product</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">getProduceTime</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Printer</span> <span class="keyword">implements</span> <span class="title">Output</span>,<span class="title">Product</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String[] printData=<span class="keyword">new</span> String[MAX_CACHE_LINE];</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> dataNum=<span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">out</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (dataNum&gt;<span class="number">0</span>)&#123;</span><br><span class="line">            System.out.println(<span class="string">"打印机打印："</span>+printData[<span class="number">0</span>]);</span><br><span class="line">            System.arraycopy(printData, <span class="number">1</span>, printData, <span class="number">0</span>, --dataNum);<span class="comment">//从指定数组的指定位置开始复制到指定数组的指定位置，length为复制长度</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">getData</span><span class="params">(String msg)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(dataNum&gt;=MAX_CACHE_LINE)&#123;</span><br><span class="line">            System.out.println(<span class="string">"输出队列已满，添加失败"</span>);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="comment">//把打印数据添加到队列中，以保存的数据量加一</span></span><br><span class="line">            printData[dataNum++]=msg;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getProduceTime</span><span class="params">()</span></span>&#123;<span class="comment">//实现接口中的方法</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">45</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//创建一个Printer对象，当成Output使用</span></span><br><span class="line">        Output o=<span class="keyword">new</span> Printer();</span><br><span class="line">        o.getData(<span class="string">"天国之秋"</span>);</span><br><span class="line">        o.getData(<span class="string">"世界文明史"</span>);</span><br><span class="line">        o.out();</span><br><span class="line">        o.getData(<span class="string">"文明的冲突与世界历史的重建"</span>);</span><br><span class="line">        o.getData(<span class="string">"21世纪资本论"</span>);</span><br><span class="line">        o.out();</span><br><span class="line">        o.print(<span class="string">"孙悟空"</span>,<span class="string">"猪八戒"</span>,<span class="string">"沙僧"</span>);</span><br><span class="line">        o.test();</span><br><span class="line">        <span class="comment">//创建一个Printer对象，当成Product使用</span></span><br><span class="line">        Product p=<span class="keyword">new</span> Printer();</span><br><span class="line">        System.out.println(p.getProduceTime());</span><br><span class="line">        Object obj=p;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;从上面的程序可以看出，<code>Product p=new Printer();</code>Product接口修饰了其实现类的对象，同理，Object也是如此。对象o也可以直接使用接口中的默认方法。<br><br>&emsp;由于接口里的方法都是public的，所以在实现类中的方法也必须是public的。</p>
<h2 id="接口和抽象类"><a href="#接口和抽象类" class="headerlink" title="接口和抽象类"></a>接口和抽象类</h2><p>&emsp;接口和抽象类都具有一下特征：</p>
<ul>
<li>接口和抽象类都不能被实例化，用于被其他类实现和继承</li>
<li>接口和抽象类都包含抽象方法，实现接口或继承抽象类的普通子类都必须实现这些抽象方法</li>
</ul>
<p>&emsp;但是接口和抽象类有着非常大的区别：<br>&emsp;接口是一种规范，最简单来说，它充当在多个模块中间的耦合标准。例如，接口A规定了需要实现哪些方法和具有的常量，那么实现类B就会根据接口实现，而另一个类C参照接口就知道可以调用B中的哪些方法。如果是系统与外界交互，那么接口就能告诉系统先实现什么方法，也告诉用户可以使用哪些方法。总之，接口的作用就是告诉一边要做什么，另一边能做什么。而抽象类则体现了模板的思想，算是一种中间产品。在这种根本思想的差别外还在用法上存在差别：</p>
<ul>
<li>接口只能包含抽象方法和默认方法，抽象类则可以包含普通方法</li>
<li>接口不能定义静态方法(JDK8之前)，抽象类可以。</li>
<li>接口中只能有静态常量，不能有普通成员变量，抽象类则两种都可以</li>
<li>接口中不能有构造器，抽象类则有，虽然不能用</li>
<li>接口中不能有初始化块，抽象类则可以</li>
</ul>
<h2 id="面向接口编程"><a href="#面向接口编程" class="headerlink" title="面向接口编程"></a>面向接口编程</h2><h3 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h3><p>&emsp;假定程序中有个Computer类需要一个输出的设备，一般可以直接组合（即包含）一个Printer类(上面的代码)在里面，修改Computer类就可以，但是如果有多个类组合了Printer类的话，一旦Printer类要被替代，那就要修改所有类中的Printer类，非常麻烦。<br><br>&emsp;但是假如在Computer中组合一个Output（接口）的类，那就会很方便。因为它需要的是一个符合Output接口规范的类，所以不论Printer类还是修改后的BatterPrinter类，只要符合规范就可以直接被使用。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Computer</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Output out;<span class="comment">//用接口修饰成员变量</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Computer</span><span class="params">(Output out)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.out = out;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">keyIn</span><span class="params">(String msg)</span></span>&#123;</span><br><span class="line">        out.getData(msg);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">()</span></span>&#123;</span><br><span class="line">        out.out();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;再贴出Output的代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Output</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> MAX_CACHE_LINE = <span class="number">50</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">out</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">getData</span><span class="params">(String msg)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">default</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">(String... msgs)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (String msg : msgs) &#123;</span><br><span class="line">            System.out.println(msg);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;同时，在Computer类中也不负责具体的Output接口的实现类的对象产生，这部分工作交给了一个Output工厂。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OutputFactory</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Output <span class="title">getOutput</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Printer();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        OutputFactory of = <span class="keyword">new</span> OutputFactory();</span><br><span class="line">        Computer c = <span class="keyword">new</span> Computer(of.getOutput());</span><br><span class="line">        c.keyIn(<span class="string">"天国之秋"</span>);</span><br><span class="line">        c.keyIn(<span class="string">"阿拉伯的劳伦斯"</span>);</span><br><span class="line">        c.print();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;在OutputFactory这个类中，getOutput的返回值是符合Output接口的Printer类。这个类被传入computer的构造函数，现在Computer类的对象c中就具有了Printer()对象。c.keyIn则调用Printer()对象的getData方法，将文字存储进去。c.print()则调用了out()方法。<br><br>&emsp;如果需要改进Printer()方法，则只要将getOutput的返回值改为改进后的类的对象。不需要担心在调用c.keyIn方法时不兼容，因为getOutput的返回类型是Output的，computer的构造器接受的也是Output的，只要符合Output接口规范的类在里面都可以使用。</p>
<h3 id="命令模式"><a href="#命令模式" class="headerlink" title="命令模式"></a>命令模式</h3><p>&emsp;接口还有一个妙用，请看代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Command</span></span>&#123;<span class="comment">//Command接口，要求接受一个数组</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">process</span><span class="params">(<span class="keyword">int</span>[] target)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ProcessArray</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">process</span><span class="params">(<span class="keyword">int</span>[] target , Command cmd)</span></span>&#123;<span class="comment">//注意这里传入了Command接口的实现对象</span></span><br><span class="line">        cmd.process(target);<span class="comment">//这里调用了传入cmd的process方法</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommandTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        ProcessArray pa = <span class="keyword">new</span> ProcessArray();</span><br><span class="line">        <span class="keyword">int</span>[] target = &#123;<span class="number">3</span>,-<span class="number">4</span>,<span class="number">6</span>,<span class="number">4</span>&#125;;</span><br><span class="line">        pa.process(target,<span class="keyword">new</span> PrintCommand());</span><br><span class="line">        pa.process(target,<span class="keyword">new</span> AddCommand());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;假定根据Command接口实现了PrintCommand和AddCommand类，一个具有打印数组的方法，一个具有累加数组的方法。根据ProcessArray类中的方法，就可以实现不同的方法。这样子就实现了ProcessArray类中的process方法与真正的处理方法的分离。</p>
<h1 id="内部类"><a href="#内部类" class="headerlink" title="内部类"></a>内部类</h1><p>&emsp;内部类主要有以下作用和区别：</p>
<ul>
<li>当一个类脱离另一个类后就完全没有意义时，则可以作为内部类，这样有更好的封装</li>
<li>内部类可以直接访问外部类的成员，因为它自己也是外部类的成员，但是不能倒过类访问</li>
<li>匿名内部类更适合创建一次性的类</li>
<li>因为是外部类的成员，所以可以用private，protected,static修饰</li>
<li>非静态内部类不能拥有静态成员</li>
</ul>
<h2 id="非静态内部类"><a href="#非静态内部类" class="headerlink" title="非静态内部类"></a>非静态内部类</h2><p>&emsp;内部类可以放在类内任何地方，包括方法体中（局部内部类）。成员内部类是一种与成员变量类似的类成员，但局部内部类和匿名内部类则不是。<br><br>&emsp;当编译含有内部类的外部类时，会产生两个class文件，一个外部类的，还有个内部类的，名字是外部类和内部类的名字用<code>$</code>连起来，不管是静态还是非静态的内部类。<br><br>&emsp;内部类的对象可以访问外部类的私有数据，这是因为内部类的对象保存了它所寄生的外部类的对象的引用，调用非静态内部类的实例方法时，必须有一个非静态内部类实例，而非静态内部类必须寄生在外部类实例中。<br><br>&emsp;当非静态内部类的某个方法访问某个变量时，会先在改方法中查找变量，然后是内部类，最后是外部类。如果外部类和内部类的成员变量存在重名情况，访问外部类则要通过OuterClass.this.membername使用，内部类则是this.membername。<br><br>&emsp;外部类是不能直接访问内部类的。因为内部类的存在是依附于外部类，但是反过来不一定，内部类外一定有外部类，但是外部类中不一定有内部类。所以，当要访问内部类时，要显式的创建内部类。<br><br>&emsp;根据静态成员不能访问非静态成员的规则，外部类的静态成员不能访问非静态内部类的成员。非静态内部类中也不允许定义静态成员。<br><br>&emsp;为什么非静态内部类中不可定义静态成员，是因为，对于外部类来说，内部类也就是个成员，和其他的成员变量没有区别。在前面说过，内部类和外部类的class文件是分开的，当加载类时也是分开加载的，先加载外部类，如果要实例化外部类的话再加载内部类。因此，如果要有内部类（可以当成一个普通的外部类方法），没有外部类实例是不行的。但是，JVM要求所有的静态变量要在对象创建之前加载这就是矛盾所在。也有从另一个角度看问题的解释：<a href="https://segmentfault.com/q/1010000000304968" target="_blank" rel="noopener">https://segmentfault.com/q/1010000000304968</a>。</p>
<h2 id="静态内部类"><a href="#静态内部类" class="headerlink" title="静态内部类"></a>静态内部类</h2><p>&emsp;用static修饰的内部类变成了属于外部类所有，不再和外部类的实例绑在一起。<br><br>&emsp;静态内部类可以包含静态成员。静态内部类可以访问外部的静态成员，但是外部的非静态成员还是不能访问。因为，静态内部类是寄生在外部类中的，并不和某个外部类对象产生关联，没有持有他的引用，也就找不到相关的成员变量。<br><br>&emsp;在接口中也是可以定义内部类的，但只能是static的。</p>
<h2 id="使用内部类"><a href="#使用内部类" class="headerlink" title="使用内部类"></a>使用内部类</h2><h3 id="在外部类中使用内部类"><a href="#在外部类中使用内部类" class="headerlink" title="在外部类中使用内部类"></a>在外部类中使用内部类</h3><p>&emsp;在外部类中使用内部类没有什么特别的地方，直接在外部类中声明并使用内部类就行。</p>
<h3 id="在外部类以外的地方"><a href="#在外部类以外的地方" class="headerlink" title="在外部类以外的地方"></a>在外部类以外的地方</h3><p>&emsp;如果希望在外部类使用内部类（包括静态和非静态），则内部类不能有private。具体语法格式：<code>OuterClass.InnerClass varName=new OuterInatance.new InnerConstructor()</code>，首先需要用内部类来声明一个内部类的引用变量，然后要在外部类实例中再new一个内部类实例。非静态内部类的构造器必须要在外部类的实例中调用。<br><br>&emsp;如果需要创建内部类的子类，则尤其要注意上面一点。因为子类在调用自生构造器之前会调用父类的构造器，所以必须保证子类可以访问到父类的构造器，这就要求必须存在一个外部类对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SubClass</span> <span class="keyword">extends</span> <span class="title">Out</span>.<span class="title">In</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">SubClass</span><span class="params">(Out out)</span></span>&#123;</span><br><span class="line">        out.<span class="keyword">super</span>(<span class="string">"hello"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;上面代码的意思就是，在调用SubClass类的父类In类的构造器时，需要一个外部类的实例。非静态内部类的子类不一定是内部类，但是同样要保留一个引用，该引用指向父类的外部类的对象。当内部类的子类的对象存在时，也一定存在外部类的对象。</p>
<h3 id="在外部类以外使用静态类"><a href="#在外部类以外使用静态类" class="headerlink" title="在外部类以外使用静态类"></a>在外部类以外使用静态类</h3><p>&emsp;创建静态内部类实例的语法和非静态内部类的语法是类似的：<code>OuterClass.InnerClass varName=new OuterClass.InnerConstructor()</code>，最明显的区别就是不需要通过外部类的实例调用内部类的构造器了。同样，内部类的子类的使用也更简单，无需外部类实例。</p>
<h3 id="一点小疑问"><a href="#一点小疑问" class="headerlink" title="一点小疑问"></a>一点小疑问</h3><p>&emsp;内部类是外部类的一个成员，那么能否像重写方法一样在外部类的子类中重写内部类呢？是不能的。因为前面写过，内部类的类名不只是它的名字，而是外部类和内部类用’$’连起来的，父类的内部类名字是OuterA$InnerB.class，子类的名字就可能是OuterC$InnerB.class，它们实际不重名。</p>
<h2 id="局部内部类"><a href="#局部内部类" class="headerlink" title="局部内部类"></a>局部内部类</h2><p>&emsp;如果把内部类放在方法里，那就是局部内部类。它的上一级单元是方法，所以用static修饰这种内部类是没有意义的。与局部变量类似，局部内部类也不能用static和public等修饰。关于局部内部类的所有操作都和局部变量类似，只能在方法内部有用。请看代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> test5;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">outer</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="class"><span class="keyword">class</span> <span class="title">InnerBase</span></span>&#123;</span><br><span class="line">            <span class="keyword">int</span> a;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="class"><span class="keyword">class</span> <span class="title">InnerSub</span> <span class="keyword">extends</span> <span class="title">InnerBase</span></span>&#123;</span><br><span class="line">            <span class="keyword">int</span> b;</span><br><span class="line">        &#125;</span><br><span class="line">        InnerSub is=<span class="keyword">new</span> InnerSub();</span><br><span class="line">        is.a=<span class="number">5</span>;</span><br><span class="line">        is.b=<span class="number">5</span>;</span><br><span class="line">        System.out.println(<span class="string">"InnerSub的对象的a和b的实例变量是："</span>+is.a+<span class="string">","</span>+is.b);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;有意思的是生成的class文件<img src="/2018/04/24/Java%E7%AC%94%E8%AE%B07/class.png" alt="class">，针对局部内部类和其子类，它们的生成class中间都有个数字，这是因为一个类中可能存在多个方法语句，每个语句中有种同名的局部内部类，所以需要加以区分。</p>
<h2 id="匿名内部类"><a href="#匿名内部类" class="headerlink" title="匿名内部类"></a>匿名内部类</h2><p>&emsp;定义匿名内部类的格式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> 实现接口() | 父类构造器(实参列表)&#123;</span><br><span class="line">    <span class="comment">//类体部分</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;直接给出代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Product</span></span><br><span class="line"><span class="comment"> */</span><span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">getPrice</span><span class="params">()</span></span>;</span><br><span class="line"> <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AnonymousTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">(Product P)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"购买了一个"</span> + P.getName() + <span class="string">",花掉了"</span> + P.getPrice());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        AnonymousTest ta = <span class="keyword">new</span> AnonymousTest();</span><br><span class="line">        ta.test(<span class="keyword">new</span> Product() &#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">getPrice</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="number">567.8</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="string">"配件"</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;是不是觉得奇怪，怎么和上面的语法格式不太一样？匿名内部类因为使用了new关键字，所以会创建对象，只不过是没有引用变量显式的指向它，而且，匿名内部类是实现接口或调用父类构造器的，然而这里却没有出现接口的实现类和子类的名字，所以是匿名的。可以简单理解为，在不实现接口或继承父类的情况下，创建基于它们的对象。<br><br>&emsp;再来看上面的代码，test()方法需要一个Product接口实现类的对象，但是因为内容都很简单，为此实现一个类出来不划算，所以采用了匿名内部类。ta.test()接受了匿名内部类的对象。<br><br>&emsp;关于匿名内部类有些注意点：</p>
<ul>
<li>匿名内部类因为必须创建对象，所以不能是抽象的，即必须完全实现接口或父类的抽象方法。只能在接口和父类中二选一，也不支持多个实现或继承。</li>
<li>支持对父类的非抽象方法进行重写</li>
<li>匿名内部类不能定义构造器，但是可以有初始化块。同时，因为不能定义构造器，所以只有默认的无参数构造器，所以在<code>new Product()</code>中没有参数。但是，如果是父类的匿名内部类，那么就会有与父类相似的构造器，具有构造器的形参列表。</li>
</ul>
<p>&emsp;在Java8之前，局部内部类，匿名内部类访问的局部变量必须是final修饰的，Java8之后不需要这样了。在Java8中，这种变量可以使用final修饰，也可以不使用，但是都是按照final的方式来用的。因此被匿名内部类和局部内部类访问的局部变量是不能重新赋值的。</p>
<h1 id="Lambda表达式"><a href="#Lambda表达式" class="headerlink" title="Lambda表达式"></a>Lambda表达式</h1><p>&emsp;Lam表达式支持将代码块作为方法参数，允许使用更简洁的代码来创建只有一个抽象方法的接口（函数式接口）。<br><br>&emsp;先再看下命令模式中的代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Command</span></span>&#123;<span class="comment">//Command接口，要求接受一个数组</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">process</span><span class="params">(<span class="keyword">int</span>[] target)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ProcessArray</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">process</span><span class="params">(<span class="keyword">int</span>[] target , Command cmd)</span></span>&#123;<span class="comment">//注意这里传入了Command接口的实现对象</span></span><br><span class="line">        cmd.process(target);<span class="comment">//这里调用了传入cmd的process方法</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommandTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        ProcessArray pa = <span class="keyword">new</span> ProcessArray();</span><br><span class="line">        <span class="keyword">int</span>[] target = &#123;<span class="number">3</span>,-<span class="number">4</span>,<span class="number">6</span>,<span class="number">4</span>&#125;;</span><br><span class="line">        pa.process(target,<span class="keyword">new</span> PrintCommand());</span><br><span class="line">        pa.process(target,<span class="keyword">new</span> AddCommand());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AddCommand</span> <span class="keyword">implements</span> <span class="title">Command</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">proocess</span><span class="params">(<span class="keyword">int</span>[] target)</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> tmp : target)&#123;</span><br><span class="line">            sum=sum+tmp;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"数组元素的总和："</span>+sum);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;但是上面的代码需要我分别实现各个操作过程。接着试着用匿名内部类来改写：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommandTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span> <span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        ProcessArray pa = <span class="keyword">new</span> ProcessArray();</span><br><span class="line">        <span class="keyword">int</span>[] target = &#123;<span class="number">3</span>,-<span class="number">4</span>,<span class="number">6</span>,<span class="number">4</span>&#125;;</span><br><span class="line">        pa.procsee(target,<span class="keyword">new</span> Command&#123;            <span class="comment">//使用匿名内部类</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">process</span><span class="params">(<span class="keyword">int</span>[] target)</span></span>&#123;</span><br><span class="line">                <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">                <span class="keyword">for</span>(<span class="keyword">int</span> tmp : target)&#123;</span><br><span class="line">                    sum=sum+tmp;</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(<span class="string">"数组元素的总和："</span>+sum);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;注意有两个process，ProcessArray和接口中的，是不一样的。ProcessArray的对象的process方法接受数组和Command接口的实现类的对象，然后调用这个对象的process方法处理数组。而用匿名内部类时，直接根据接口创建一个对象。这样可以避免不必要的接口实现。<br><br>&emsp;而使用Lambda可以将匿名内部类进一步简化：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommandTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ProcessArray pa = <span class="keyword">new</span> ProcessArray();</span><br><span class="line">        <span class="keyword">int</span>[] array = &#123;<span class="number">3</span>, -<span class="number">4</span>, <span class="number">6</span>, <span class="number">4</span>&#125;;</span><br><span class="line">        pa.process(array, (<span class="keyword">int</span>[] target) -&gt; &#123;</span><br><span class="line">            <span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> tmp : target) &#123;</span><br><span class="line">                sum = sum + tmp;</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println(<span class="string">"数组元素的总和："</span> + sum);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;从上面的代码来看，Lambda表达式仍然是创建了一个对象，只是省略了非常多的东西，不需要new Xxx(){}，也不需要指出重写的方法的名字和返回值类型（上面的代码没用到），只要括号和括号中的参数列表。<br><br>&emsp;形参列表允许省略参数类型，如果只有一个参数，连括号也可以省略。代码块如果只要一条语句，花括号也可以省略。Lambda代码块只有一条return语句。如果，代码块中只有一条省略了return的语句，那就会直接返回这条语句的值。<br><br>&emsp;先看代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Eatable</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">taste</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Flyable</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">fly</span><span class="params">(String weather)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Addable</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">add</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LambdaTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">eat</span><span class="params">(Eatable e)</span> </span>&#123;</span><br><span class="line">        System.out.println(e);</span><br><span class="line">        e.taste();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">drive</span><span class="params">(Flyable f)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"我正在驾驶："</span> + f);</span><br><span class="line">        f.fly(<span class="string">"【碧空如洗的晴日】"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">(Addable add)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"5和3的和为："</span> + add.add(<span class="number">5</span>, <span class="number">3</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        LambdaTest lq = <span class="keyword">new</span> LambdaTest();</span><br><span class="line">        lq.eat(() -&gt; System.out.println(<span class="string">"苹果的味道不错"</span>));</span><br><span class="line">        lq.drive(weather -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">"今天天气是："</span> + weather);</span><br><span class="line">            System.out.println(<span class="string">"直升机平稳飞行"</span>);</span><br><span class="line">        &#125;);</span><br><span class="line">        lq.test((a, b) -&gt; &#123;</span><br><span class="line">            <span class="keyword">return</span> a + b;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;先来看看<code>lq.eat(() -&gt; System.out.println(&quot;苹果的味道不错&quot;));</code>这句的意思，在LambdaQs的类体中查看eat()方法，这个方法需要一个Eatable接口的实现类的对象，下面的也是。Lambda表达式实际上会被当成一个任意类型的对象，具体是何种类型取决于运行环境的需要。但是这里面具体是如何运行的呢？</p>
<h2 id="Lambda表达式与函数式接口"><a href="#Lambda表达式与函数式接口" class="headerlink" title="Lambda表达式与函数式接口"></a>Lambda表达式与函数式接口</h2><p>&emsp;Lambda表达式的类型（也被叫“目标类型”），必须是“函数式接口”，这种接口只有一个抽象方法，但是可以包含多个默认方法，类方法。Java8还提供了@FunctionalInterface注解。<br><br>&emsp;以Java本身提供的Runnable接口为例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Runable接口中只包含一个无参数的方法</span></span><br><span class="line"><span class="comment">//Lambda表达式代表的匿名方法实现了Runnable接口中唯一的，无参数的方法</span></span><br><span class="line"><span class="comment">//因此下面的Lambda表达式创建了一个对象</span></span><br><span class="line">Runnable r = () -&gt; &#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">100</span>;i++)&#123;</span><br><span class="line">        System.out.println();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>&emsp;Lambda表达式实现的是<strong>特定函数式接口中的唯一方法</strong>。注意这句话的意思。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Object obj = () -&gt; &#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">100</span>;i++)&#123;</span><br><span class="line">        System.out.println();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>&emsp;如果这样写，就会得到错误，因为Object不是一个函数式接口。如果要保证有一个明确的函数式接口，有三种常见方式：</p>
<ul>
<li>将Lambda表达式赋值给函数式接口类型的变量</li>
<li>将Lambda表达式作为函数式接口的参数传给某个方法</li>
<li>使用函数式接口对Lambda表达式进行强制类型转换</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Object obj = (Runnable)() -&gt; &#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">100</span>;i++)&#123;</span><br><span class="line">        System.out.println();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>&emsp;接着回过头再来看一下Lambda表达式到底是如何来的。</p>
<h3 id="Lambda的演变"><a href="#Lambda的演变" class="headerlink" title="Lambda的演变"></a>Lambda的演变</h3><h4 id="最初的命令模式"><a href="#最初的命令模式" class="headerlink" title="最初的命令模式"></a>最初的命令模式</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Command</span></span>&#123;<span class="comment">//Command接口，要求接受一个数组</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">process</span><span class="params">(<span class="keyword">int</span>[] target)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ProcessArray</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">process</span><span class="params">(<span class="keyword">int</span>[] target , Command cmd)</span></span>&#123;<span class="comment">//注意这里传入了Command接口的实现对象</span></span><br><span class="line">        cmd.process(target);<span class="comment">//这里调用了传入cmd的process方法</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommandTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        ProcessArray pa = <span class="keyword">new</span> ProcessArray();</span><br><span class="line">        <span class="keyword">int</span>[] target = &#123;<span class="number">3</span>,-<span class="number">4</span>,<span class="number">6</span>,<span class="number">4</span>&#125;;</span><br><span class="line">        pa.process(target,<span class="keyword">new</span> PrintCommand());</span><br><span class="line">        pa.process(target,<span class="keyword">new</span> AddCommand());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AddCommand</span> <span class="keyword">implements</span> <span class="title">Command</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">proocess</span><span class="params">(<span class="keyword">int</span>[] target)</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> tmp : target)&#123;</span><br><span class="line">            sum=sum+tmp;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"数组元素的总和："</span>+sum);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="使用匿名内部类"><a href="#使用匿名内部类" class="headerlink" title="使用匿名内部类"></a>使用匿名内部类</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommandTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span> <span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        ProcessArray pa = <span class="keyword">new</span> ProcessArray();</span><br><span class="line">        <span class="keyword">int</span>[] target = &#123;<span class="number">3</span>,-<span class="number">4</span>,<span class="number">6</span>,<span class="number">4</span>&#125;;</span><br><span class="line">        pa.procsee(target,<span class="keyword">new</span> Command&#123;            <span class="comment">//使用匿名内部类</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">process</span><span class="params">(<span class="keyword">int</span>[] target)</span></span>&#123;</span><br><span class="line">                <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">                <span class="keyword">for</span>(<span class="keyword">int</span> tmp : target)&#123;</span><br><span class="line">                    sum=sum+tmp;</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(<span class="string">"数组元素的总和："</span>+sum);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;匿名内部类不过是把接口中的抽象方法实现，但是没有具体的类而已。</p>
<h4 id="Lambda表达式的演变"><a href="#Lambda表达式的演变" class="headerlink" title="Lambda表达式的演变"></a>Lambda表达式的演变</h4><ol>
<li>把外面的壳子去掉</li>
</ol>
<p>&emsp;只保留参数列表和方法题。参数和方法题之间用‘-&gt;’链接。为什么可以去掉？因为<strong>特定函数式接口中的唯一方法</strong>这句话，只有一个方法，当然可以知道省略的是哪个。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommandTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span> <span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        ProcessArray pa = <span class="keyword">new</span> ProcessArray();</span><br><span class="line">        <span class="keyword">int</span>[] target = &#123;<span class="number">3</span>,-<span class="number">4</span>,<span class="number">6</span>,<span class="number">4</span>&#125;;</span><br><span class="line">        pa.procsee(target,<span class="keyword">new</span> Command&#123;            <span class="comment">//使用匿名内部类</span></span><br><span class="line">            (<span class="keyword">int</span>[] target)-&gt;&#123;</span><br><span class="line">                <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">                <span class="keyword">for</span>(<span class="keyword">int</span> tmp : target)&#123;</span><br><span class="line">                    sum=sum+tmp;</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(<span class="string">"数组元素的总和："</span>+sum);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>把return和{}去掉</li>
</ol>
<p>&emsp;这里不能去，跳过</p>
<ol start="3">
<li>把参数类型和圆括号去掉</li>
</ol>
<p>&emsp;只有一个参数时可以去掉，类似int[] target这样的，括号也不能去。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommandTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span> <span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        ProcessArray pa = <span class="keyword">new</span> ProcessArray();</span><br><span class="line">        <span class="keyword">int</span>[] target = &#123;<span class="number">3</span>,-<span class="number">4</span>,<span class="number">6</span>,<span class="number">4</span>&#125;;</span><br><span class="line">        pa.procsee(target,<span class="keyword">new</span> Command&#123;            <span class="comment">//使用匿名内部类</span></span><br><span class="line">                target-&gt;&#123;</span><br><span class="line">                <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">                <span class="keyword">for</span>(<span class="keyword">int</span> tmp : target)&#123;</span><br><span class="line">                    sum=sum+tmp;</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(<span class="string">"数组元素的总和："</span>+sum);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>直接把表达式传递进去或者作为参数传递进去</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommandTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span> <span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        ProcessArray pa = <span class="keyword">new</span> ProcessArray();</span><br><span class="line">        <span class="keyword">int</span>[] target = &#123;<span class="number">3</span>,-<span class="number">4</span>,<span class="number">6</span>,<span class="number">4</span>&#125;;</span><br><span class="line">        pa.procsee(target, target-&gt;&#123;</span><br><span class="line">                <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">                <span class="keyword">for</span>(<span class="keyword">int</span> tmp : target)&#123;</span><br><span class="line">                    sum=sum+tmp;</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(<span class="string">"数组元素的总和："</span>+sum);</span><br><span class="line">            &#125;</span><br><span class="line">        );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;再把变量名字改一下就和上面的一样了（上面的没省略变量类型）。这种演变分的方法请看链接<a href="http://how2j.cn/k/lambda/lambda-lamdba-tutorials/697.html#nowhere" target="_blank" rel="noopener">http://how2j.cn/k/lambda/lambda-lamdba-tutorials/697.html#nowhere</a>，非常不错的教程。<br><br>&emsp;仔细观察发现，要想使用Lambda表达式，最重要的就是Lambda实现的匿名方法的参数列表要和函数式接口中唯一的抽象方法的接口一样。这意味这在前面提到过的Runnable强制转换可以用来改变Lambda表达式的用处，只要参数列表是一样的，就可以通过强制转换通用。&lt;/br?<br>&emsp;Lambda表达式的本质是用简洁的语法来创建函数式接口的实例。</p>
<h2 id="方法引用和构造器"><a href="#方法引用和构造器" class="headerlink" title="方法引用和构造器"></a>方法引用和构造器</h2><p>&emsp;当Lambda表达式只有一条语句时还可以使用方法引用和构造器引用。</p>
<h3 id="引用类方法"><a href="#引用类方法" class="headerlink" title="引用类方法"></a>引用类方法</h3><p>&emsp;定义了如下函数式接口：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Converter</span></span>&#123;</span><br><span class="line">    <span class="function">Integer <span class="title">convert</span><span class="params">(String from)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;该接口的要求是传入一个字符串并将其转化为Integer类型。分析后发现，不需要单独将实现类写出来，因为可以直接使用<code>Integer.valueOf</code>方法转化。下面的代码使用Lambda表达式创建一个Converter接口的对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Converter converter1 = from -&gt; Integer.valueOf(from);</span><br></pre></td></tr></table></figure>

<p>&emsp;接下来使用这个对象：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer val = converter1.convert(<span class="string">"99"</span>);</span><br><span class="line">System.out.println(val);</span><br></pre></td></tr></table></figure>

<p>&emsp;由于Lambda表达式创建了一个Converter对象，这个对象具有Lambda表达式代表的匿名方法，所以可以将参数传进去调用这个对象的方法。<br><br>&emsp;对于这里的Lambda表达式，引用了Integer类的valueOf()类方法，所以还可以换种方式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Converter converter1 = Integer::valueOf;</span><br></pre></td></tr></table></figure>

<p>&emsp;乍看肯定会说卧槽，这是怎么变得。我觉得要是直接从接口写出来这种有点转不过弯，还是一步步来吧。上面的类引用方法简单来说就是使用某个类的类方法来实现接口中的抽象方法。</p>
<h3 id="引用特定对象的实例方法"><a href="#引用特定对象的实例方法" class="headerlink" title="引用特定对象的实例方法"></a>引用特定对象的实例方法</h3><p>&emsp;现在还是用上面的那个接口，只不过要求变了，要求输入一个字符串，然后返回在给定字符串中的索引值（Integer）。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Converter converter2 = from -&gt; <span class="string">"hellojava"</span>.indexOf(from);  <span class="comment">//使用现成的indexOf方法</span></span><br></pre></td></tr></table></figure>

<p>&emsp;原本indexOf方法返回的是int类型，这里自动装箱为Interger。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer val = converter2.convert(<span class="string">"ll"</span>);</span><br><span class="line">System.out.println(val);</span><br></pre></td></tr></table></figure>

<p>&emsp;结果为2，也可以采用下面的简写：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Converter converter1 = <span class="string">"hellojava"</span>::indexOf;</span><br></pre></td></tr></table></figure>

<p>&emsp;上面的实例引用方法就是使用具体的实例的方法来实现接口中的抽象方法。</p>
<h3 id="引用某类对象的实例方法"><a href="#引用某类对象的实例方法" class="headerlink" title="引用某类对象的实例方法"></a>引用某类对象的实例方法</h3><p>&emsp;这回换一个接口：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">MyTest</span></span>&#123;</span><br><span class="line">    <span class="function">String <span class="title">test</span><span class="params">(String a,<span class="keyword">int</span> b,<span class="keyword">int</span> c)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;根据test()抽象方法，使用Lambda表达式创建一个MyTest对象：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">MyTest mt = (a,b,c) -&gt; a.substring(b,c);  <span class="comment">//截取a字符串，从b开始，到c结束，不包括c</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String str = mt.test(<span class="string">"hello java"</span>,<span class="number">2</span>,<span class="number">4</span>);</span><br><span class="line">System.out.println(str);</span><br></pre></td></tr></table></figure>

<p>&emsp;该代码的Lambda部分可以改写为：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">MyTest mt = String::substring;</span><br><span class="line">String str = mt.test(<span class="string">"hello java"</span>,<span class="number">2</span>,<span class="number">4</span>);</span><br><span class="line">System.out.println(str);</span><br></pre></td></tr></table></figure>

<p>&emsp;这里与上面类似，只不过不是具体的哪个对象的方法，而是某类对象的实例方法。注意和引用类方法的区别，从<code>a.substring(b,c)</code>可以看出，它是实例方法。传入的三个参数，第一个作为调用者，后面两个作为substring的参数。</p>
<h3 id="引用构造器"><a href="#引用构造器" class="headerlink" title="引用构造器"></a>引用构造器</h3><p>&emsp;再换一个接口</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">YourTest</span></span>&#123;</span><br><span class="line">    <span class="function">JFrame <span class="title">win</span><span class="params">(String title)</span></span>;  <span class="comment">//JFrame是一个类</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;该抽象方法要求根据String参数返回一个JFrame对象，Lambda代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Yourtest yt = (String a) -&gt; <span class="keyword">new</span> JFrame(a);</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">JFrame jf = yt.win(<span class="string">"我的窗口"</span>);</span><br><span class="line">System.out.println(jf);</span><br></pre></td></tr></table></figure>

<p>&emsp;进一步简略：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Yourtest yt = JFrame::<span class="keyword">new</span>;</span><br></pre></td></tr></table></figure>

<p>&emsp;不需要写明调用哪个构造器，就如同正常调用构造器一样，是根据传入的参数来决定用哪个构造器。</p>
<h2 id="Lambda表达式和匿名内部类的联系和区别"><a href="#Lambda表达式和匿名内部类的联系和区别" class="headerlink" title="Lambda表达式和匿名内部类的联系和区别"></a>Lambda表达式和匿名内部类的联系和区别</h2><p>&emsp;通过上面一系列的使用，可以发现匿名内部类和Lambda表达式非常像，Lambda可以说是匿名内部类的简化版。<br><br>&emsp;存在如下相同点：</p>
<ul>
<li>都可以直接访问局部变量（之后不能改变）和外部类的成员变量</li>
<li>都可以直接调用从接口继承的默认方法</li>
</ul>
<p>&emsp;也有很多区别：</p>
<ul>
<li>匿名内部类可以为任意接口创建实例，只要实现全部抽象方法即可；Lambda表达式只能为函数式接口创建实例</li>
<li>匿名内部类可以为抽象类或者普通类创建实例，但是Lambda表达式都不能</li>
<li>匿名内部类实现的抽象方法允许调用接口中的抽象方法，但是Lambda表达式不允许</li>
</ul>
<h1 id="一点补充知识"><a href="#一点补充知识" class="headerlink" title="一点补充知识"></a>一点补充知识</h1><blockquote>
<p>&emsp;在Java8之前，局部内部类，匿名内部类访问的局部变量必须是final修饰的，Java8之后不需要这样了。在Java8中，这种变量可以使用final修饰，也可以不使用，但是都是按照final的方式来用的。因此被匿名内部类和局部内部类访问的局部变量是不能重新赋值的。</p>
</blockquote>
<blockquote>
<p>&emsp;存在如下相同点：</p>
<ul>
<li>都可以直接访问局部变量（之后不能改变）和外部类的成员变量</li>
<li>都可以直接调用从接口继承的默认方法</li>
</ul>
</blockquote>
<p>&emsp;这两个地方都写到，局部内部类，匿名内部类，Lambda表达式访问<strong>外面</strong>的局部变量时，这些变量要是final的，即使没有这么写，之后也不能更改。大概的原因是，外部的局部变量会进行值传递，如果内部修改或外部修改值就会造成数据混乱。具体请看链接：<br><a href="https://www.zhihu.com/question/21395848" target="_blank" rel="noopener">https://www.zhihu.com/question/21395848</a><br><a href="http://cuipengfei.me/blog/2013/06/22/why-does-it-have-to-be-final/" target="_blank" rel="noopener">http://cuipengfei.me/blog/2013/06/22/why-does-it-have-to-be-final/</a><br><a href="https://www.cnblogs.com/hapjin/p/5744478.html" target="_blank" rel="noopener">https://www.cnblogs.com/hapjin/p/5744478.html</a><br>还有一篇关于闭包的：<br><a href="https://www.zhihu.com/question/24084277/answer/110176733" target="_blank" rel="noopener">https://www.zhihu.com/question/24084277/answer/110176733</a></p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记9</title>
    <url>/2018/05/06/Java%E7%AC%94%E8%AE%B09/</url>
    <content><![CDATA[<p>&emsp;本篇讲讲异常。</p>
<a id="more"></a>

<h1 id="异常处理"><a href="#异常处理" class="headerlink" title="异常处理"></a>异常处理</h1><p>&emsp;程序在运行时 ，我们往往会默认用户会按照我们的设想去做，但事实是，用户往往不知道我们要做什么。例如，我要求成绩需要是字符型，但是用户可能就会输入数字型。所以，我需要在一开始对各种意外作出考虑。例如，输入了数字怎么办，输入了英文怎么办。但是，各种异常是很难穷举完的。那么就反过来作出假设，只分为两种，一种有错，一种没错。即：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(用户输入不正常)&#123;</span><br><span class="line">    <span class="comment">//抛出错误</span></span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="comment">//业务实现代码</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="异常处理机制"><a href="#异常处理机制" class="headerlink" title="异常处理机制"></a>异常处理机制</h2><h3 id="使用try…catch捕获异常"><a href="#使用try…catch捕获异常" class="headerlink" title="使用try…catch捕获异常"></a>使用try…catch捕获异常</h3><p>&emsp;问题是如何判定是否是对的或错的呢？最简单的就是直接把代码跑一遍。当程序出现错误时，系统会自动生成一个Exception对象，称为抛出（throw）。直接上代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Scanner;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Scanner scanner = <span class="keyword">new</span> Scanner(System.in);</span><br><span class="line">        <span class="keyword">int</span> i = scanner.nextInt();</span><br><span class="line">        <span class="keyword">int</span>[] Array = &#123; <span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">8</span>, <span class="number">9</span> &#125;;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(Array[i]);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="comment">// <span class="doctag">TODO:</span> handle exception</span></span><br><span class="line">            System.out.println(<span class="string">"arrayoverflow!"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;当Java运行时环境收到异常对象时，会寻找能处理异常对象的catch块，将这个异常对象交给catch处理，称为捕获（catch）。如果找不到合适的catch块，运行时环境就会终止，程序退出。不论程序代码块是否在try中，甚至在catch中，只要程序出现了异常，系统都会生成一个异常对象，但是只要找不到catch块，就会推出。<br></p>
<h3 id="异常类的继承体系"><a href="#异常类的继承体系" class="headerlink" title="异常类的继承体系"></a>异常类的继承体系</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="comment">// <span class="doctag">TODO:</span> handle exception</span></span><br><span class="line">            System.out.println(<span class="string">"arrayoverflow!"</span>);</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;注意这里的<code>Exception e</code>，这说明每个catch块都是接受特定异常对象的，是处理这个异常类及其子类的。如果存在一个try和多个catch，那么就会根据抛出的异常类型找符合的catch。当异常进入catch块后，异常对象将被传给形参，以便获得具体的异常信息。一个异常对象只会引起catch执行一次。<br><br>&emsp;Java提供了丰富的异常类，这些类有严格的继承关系，看下图：<br><img src="/2018/05/06/Java%E7%AC%94%E8%AE%B09/exception.jpg" alt="exception"><br>&emsp;从图上来看，主要分为两个类，Exception和Error。Error一般是虚拟机相关的问题，如系统崩溃，虚拟机错误，动态链接失败，这种错误无法恢复或不可能捕获，将导致程序中断。因此程序也不应该试图用catch捕获。至于Exception，看具体的名字基本上就能猜出来是什么了，下面看看简答的例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Divtest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">int</span> a = Integer.parseInt(args[<span class="number">0</span>]);</span><br><span class="line">            <span class="keyword">int</span> b = Integer.parseInt(args[<span class="number">1</span>]);</span><br><span class="line">            <span class="keyword">int</span> c = a / b;</span><br><span class="line">            System.out.println(<span class="string">"两数相除结果："</span>+c);  </span><br><span class="line">        &#125; <span class="keyword">catch</span> (IndexOutOfBoundsException ie) &#123;</span><br><span class="line">            System.out.println(<span class="string">"数组越界"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (NumberFormatException ne) &#123;</span><br><span class="line">            System.out.println(<span class="string">"数字格式异常"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ArithmeticException ae) &#123;</span><br><span class="line">            System.out.println(<span class="string">"算术异常"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            System.out.println(<span class="string">"未知异常"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;根据继承关系，Exception异常应该放在最后，如果放在了最前面，程序将直接进入这个异常，而后面的将被忽略。所以，所有的父异常都应该排在子异常的后面。</p>
<h3 id="Java7提供的多异常捕获"><a href="#Java7提供的多异常捕获" class="headerlink" title="Java7提供的多异常捕获"></a>Java7提供的多异常捕获</h3><p>&emsp;从Java7开始，一个catch块可以捕获多个类型的异常。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Divtest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">int</span> a = Integer.parseInt(args[<span class="number">0</span>]);</span><br><span class="line">            <span class="keyword">int</span> b = Integer.parseInt(args[<span class="number">1</span>]);</span><br><span class="line">            <span class="keyword">int</span> c = a / b;</span><br><span class="line">            System.out.println(<span class="string">"两数相除结果："</span>+c);    </span><br><span class="line">        &#125; <span class="keyword">catch</span> (IndexOutOfBoundsException ie) &#123;</span><br><span class="line">            System.out.println(<span class="string">"数组越界"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ArithmeticException | NumberFormatException ae) &#123;</span><br><span class="line">            System.out.println(<span class="string">"算术异常"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            System.out.println(<span class="string">"未知异常"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;捕获多种类型的异常时，异常变量隐式地用final修饰，所以是不能修改的。</p>
<h3 id="访问异常信息"><a href="#访问异常信息" class="headerlink" title="访问异常信息"></a>访问异常信息</h3><p>&emsp;都包含啊了下面几个方法，用于访问异常的详细信息：</p>
<ul>
<li>getMessage():返回该异常的详细描述字符串</li>
<li>printStackTrace():将该异常的跟踪栈信息输出到标准错误输出</li>
<li>printStackTrace(PrintStream ):将该异常的跟踪栈信息输出到指定输出流</li>
<li>getStackTrace():返回该异常的跟踪栈信息</li>
</ul>
<h3 id="使用finally回收资源"><a href="#使用finally回收资源" class="headerlink" title="使用finally回收资源"></a>使用finally回收资源</h3><p>&emsp;在一个程序中，不仅要用try处理异常，还应该回收资源。内存资源由垃圾回收机制管理，而物理资源，例如打开的文件，数据库链接，网络链接等等，都需要显式地回收。但是在哪回收呢，try里和catch里都有可能执行不到，所以有一个新的finally。<br><br>&emsp;异常处理语法结构中，只有try是必须的，catch和finally都是可选的，但是必须出现一个。finally必须位于所有catch之后。finally的作用就是，不论try和catch的执行情况怎样，finally部分都会被执行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Divtest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        FileInputStream fis = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            fis = <span class="keyword">new</span> FileInputStream(<span class="string">"a.txt"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ioe) &#123;</span><br><span class="line">            System.out.println(ioe.getMessage());</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">            <span class="comment">// System.exit(1);</span></span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (fis != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    fis.close();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (IOException ioe) &#123;</span><br><span class="line">                    ioe.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println(<span class="string">"执行资源回收"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;对于方法来说，如果遇到<code>return;</code>就会强制返回，结束方法，虽然这里也是如此，但是根据结果可以看出，仍旧是执行了finally语句。但是如果我把注释去掉，执行<code>System.exit(1)</code>这句，直接强制退出虚拟机，那么finally就没有机会运行了。<br><br>&emsp;对于finally这种特性，有个注意点：应该避免在finally中使用return语句，这会影响到try中的return语句。当执行了finally中的return语句后，方法就直接推出了，try中的是不会执行的。</p>
<h3 id="Java7的自动关闭资源的try语句"><a href="#Java7的自动关闭资源的try语句" class="headerlink" title="Java7的自动关闭资源的try语句"></a>Java7的自动关闭资源的try语句</h3><p>&emsp;在Java7中允许在try关键字后面加上一对圆括号，里面是声明，初始化的一个或多个资源，try结束时将会自动关闭这些资源。为了保证能关闭这些资源，，这些资源实现类必须实现接口AutoCloseable或子接口Closeable，实现这两个接口就必须实现close()方法。AutoCloseable接口中的close方法要求抛出Exception，所以实现类中抛出什么异常都可以，但是Closeable接口的close方法声明的是IOException，因此在实现这个方法时必须抛出IOException类或其子类的异常。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> test6;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.BufferedReader;</span><br><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.PrintStream;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AutoCloseTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> (BufferedReader br = <span class="keyword">new</span> BufferedReader(<span class="keyword">new</span> FileReader(<span class="string">"AutoCloseTest"</span>));</span><br><span class="line">                PrintStream ps = <span class="keyword">new</span> PrintStream(<span class="keyword">new</span> FileOutputStream(<span class="string">"a.txt"</span>))) &#123;</span><br><span class="line">            System.out.println(br.readLine());</span><br><span class="line">            ps.println(<span class="string">"蛤"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Checked异常和Runtime异常体系"><a href="#Checked异常和Runtime异常体系" class="headerlink" title="Checked异常和Runtime异常体系"></a>Checked异常和Runtime异常体系</h2><p>&emsp;Java的异常被分为两大类，Checked和Runtime异常。所有RuntimeException类及其子类的实例都被称作Runtime异常，其余称为Checked异常。Java认为checked异常都是可以被修复的异常，因此必须显式地处理这种异常。对于这种异常有两种处理方式：</p>
<ul>
<li>当前方法知道如何处理异常，用try…catch来捕获该异常，然后在catch中修复，例如，提示输入的不合法并要求重新输入</li>
<li>不知道如何让处理，应该在定义方法时声明抛出该异常，如上面的代码</li>
</ul>
<p>&emsp;总之，对于checked异常要么捕获它并处理它，要么显式声明抛出。</p>
<h3 id="用throws声明抛出异常"><a href="#用throws声明抛出异常" class="headerlink" title="用throws声明抛出异常"></a>用throws声明抛出异常</h3><p>&emsp;使用throws的思路是，当前方法不知道如何处理异常，应该交给上一级处理，如果main方法也不知道如何让处理，则用throws声明抛出异常，交给JVM处理。JVM的处理方式是，打印异常的跟踪栈信息，并终止运行。throws声明抛出只能在方法签名中使用，可以抛出多个异常种类，用逗号隔开。对于继承中的方法重写，要记得”两小“原则，子类方法声明中抛出的异常类型应该是父类方法声明抛出的异常类型的子类或相同，子类抛出的异常声明不能比父类抛出的多。<br><br>&emsp;对于checked异常，处理起来是比较麻烦的，要显式地声明，还要注意父类和子类的限制。所以更加推荐Runtime异常，他不需要声明，一旦发生了自定义错误，程序只管抛出Runtime异常即可，一样也可以用try…catch捕获并处理它们。</p>
<h2 id="用throw抛出异常"><a href="#用throw抛出异常" class="headerlink" title="用throw抛出异常"></a>用throw抛出异常</h2><p>&emsp;在前面讲的东西中，不论有什么区别，异常都是系统自动抛出的，除此之外，Java也允许程序自行抛出异常，这种异常是由throw抛出，与throws不一样。对于JVM来说，age=-100，不是一种异常，但是对于业务逻辑来说，这就是一种异常，所以要用到throw抛出。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThrowTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            throwChecked(-<span class="number">3</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            System.out.println(e.getMessage());</span><br><span class="line">        &#125;</span><br><span class="line">        throwRuntime(<span class="number">3</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">throwChecked</span><span class="params">(<span class="keyword">int</span> a)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (a &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> Exception(<span class="string">"a大于0，不符合要求1"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">throwRuntime</span><span class="params">(<span class="keyword">int</span> a)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (a &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"a大于0，不符合要求2"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这段代码有写非常有意思的地方，首先按上面的代码运行结果是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Exception in thread <span class="string">"main"</span> java.lang.RuntimeException: a大于<span class="number">0</span>，不符合要求<span class="number">2</span></span><br><span class="line">	at test6.ThrowTest.throwRuntime(ThrowTest.java:<span class="number">21</span>)</span><br><span class="line">	at test6.ThrowTest.main(ThrowTest.java:<span class="number">10</span>)</span><br></pre></td></tr></table></figure>

<p>&emsp;对于Runtime异常，无需放在try…catch中，也不要用throw抛出，可以进行捕获处理，也可以不理会它，交给该方法的调用者处理。这里并没有对这种异常的处理，所以交由JVM处理，JVM会打印跟踪栈信息和异常的详细信息。而看到的<code>a大于0，不符合要求2</code>这句就是由JVM调用getMAssage()方法的结果，可以看图：<br><img src="/2018/05/06/Java%E7%AC%94%E8%AE%B09/%E5%BC%82%E5%B8%B8.png" alt="异常"><br>&emsp;上面的代码中3和-3替换一下，结果又不同</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">a大于<span class="number">0</span>，不符合要求<span class="number">1</span></span><br></pre></td></tr></table></figure>

<p>&emsp;就这简单一句，在try…catch中接收到了用户自行抛出的异常，随后用catch来处理，调用了getMassage()方法，所以并没有打印跟踪栈信息。这就能很明显的看出checked和Runtime的区别，明显后者更灵活。<br><br>&emsp;throws和throw的区别，参看链接：<a href="https://www.cnblogs.com/liuyaozhi/p/5812700.html" target="_blank" rel="noopener">https://www.cnblogs.com/liuyaozhi/p/5812700.html</a></p>
<h3 id="自定义异常类"><a href="#自定义异常类" class="headerlink" title="自定义异常类"></a>自定义异常类</h3><p>&emsp;上一节讲了程序员自己决定什么情况抛出异常，现在再看看抛出何种异常。<br><br>&emsp;要自定义异常，需要继承Exception基类。如果是Runtime异常，则要继承RuntimeException基类。需要提供两个构造器，一个无参数，一个参数为字符串，用于getMassage()方法。大部分情况下下面的代码可以用作自定义异常的代码，只要改个名字。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AuctionException</span> <span class="keyword">extends</span> <span class="title">Exception</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">AuctionException</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">AuctionException</span><span class="params">(String msg)</span></span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(msg);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="throw和catch同时使用"><a href="#throw和catch同时使用" class="headerlink" title="throw和catch同时使用"></a>throw和catch同时使用</h3><p>&emsp;前面写的对异常的处理方式，要么在catch中处理，要么用throw或throws抛出，交给上一级处理。但是在实际中，可能更复杂，当前方法的catch完成不了处理，全部交给上一级也处理不了，所以需要同时使用两种方法协作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AuctionTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">double</span> initPrice = <span class="number">30.0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">bid</span><span class="params">(String bidPrice)</span> <span class="keyword">throws</span> AuctionException </span>&#123;</span><br><span class="line">        <span class="keyword">double</span> d = <span class="number">0.0</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            d = Double.parseDouble(bidPrice);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();                            <span class="comment">//1</span></span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> AuctionException(<span class="string">"竞拍价必须是数字"</span>);  <span class="comment">//2</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (initPrice &gt; d) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> AuctionException(<span class="string">"不允许比竞拍价低"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        initPrice = d;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        AuctionTest at = <span class="keyword">new</span> AuctionTest();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            at.bid(<span class="string">"df"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (AuctionException ae) &#123;                     <span class="comment">//3</span></span><br><span class="line">            System.err.println(ae.getMessage());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这段代码中，先打印了跟踪栈信息（1），然后抛出了一个异常对象（2），然后catch捕获这个异常对象（3）。</p>
<h3 id="Java7中增强的throw语句"><a href="#Java7中增强的throw语句" class="headerlink" title="Java7中增强的throw语句"></a>Java7中增强的throw语句</h3><p>&emsp;首先看下面的代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThrowTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">new</span> FileOutputStream(<span class="string">"a.txt"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">            ex.printStackTrace();</span><br><span class="line">            <span class="keyword">throw</span> ex;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这段代码将catch捕获到的异常再次抛出，由于<code>Exception ex</code>这样的声明，所以throws后面跟的是Exception。但是这里面的问题是，原本try中调用了FileOutStream构造器，这种构造器只是抛出FileNotFoundException异常。但是后面这种抛出导致了异常的放大，不利于异常的处理。在Java7中，不在这么”粗暴“，请看代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThrowTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> FileNotFoundException </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">new</span> FileOutputStream(<span class="string">"a.txt"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">            ex.printStackTrace();</span><br><span class="line">            <span class="keyword">throw</span> ex;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;编译器会进行更细致的检查，检查throw语句抛出异常的实际种类，这样在方法签名处只要抛出FileNotFoundException异常就行。</p>
<h3 id="异常链"><a href="#异常链" class="headerlink" title="异常链"></a>异常链</h3><p>&emsp;假设下面的情况，在对数据库进行操作时出现了异常，但是这种异常是不应该返回给用户的，因为对于他们来说这没有任何用处，同时增加了把程序缺陷暴露给恶意用户的风险。正确的做法是，将原始异常记录下来，将异常进行一定的转变再传给用户层。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">calSal</span><span class="params">()</span> <span class="keyword">throws</span> SalException</span>&#123;</span><br><span class="line">    <span class="keyword">try</span>&#123;</span><br><span class="line">        <span class="comment">//实现业务逻辑</span></span><br><span class="line">    &#125;<span class="keyword">catch</span>(SQLException sqle)&#123;</span><br><span class="line">        <span class="comment">//把原始异常记录下来</span></span><br><span class="line">        <span class="comment">//抛出给用户的异常提示</span></span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> SalException(<span class="string">"访问底层数据出现错误"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这种捕获一个异常接着抛出另一个异常，并把原始异常信息保存下来是一种典型的链式处理模式（设计模式：职责链模式）。在Java1.4之前，需要自己写代码将原始异常保存下来，但是在之后，所有的Throwable的子类在构造器中都可以接受一个cause对象作为参数。这个cause就用来表示原始异常：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">calSal</span><span class="params">()</span> <span class="keyword">throws</span> SalException</span>&#123;</span><br><span class="line">    <span class="keyword">try</span>&#123;</span><br><span class="line">        <span class="comment">//实现业务逻辑</span></span><br><span class="line">    &#125;<span class="keyword">catch</span>(SQLException sqle)&#123;</span><br><span class="line">        <span class="comment">//把原始异常记录下来</span></span><br><span class="line">        <span class="comment">//将原始异常作为参数传递出去</span></span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> SalException(sqle)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SalException</span> <span class="keyword">extends</span> <span class="title">Exception</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">SqlException</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">SalException</span><span class="params">(String msg)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(t);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">SqlException</span><span class="params">(Throwable t)</span></span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(t);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="异常处理规则"><a href="#异常处理规则" class="headerlink" title="异常处理规则"></a>异常处理规则</h2><p>&emsp;异常处理应该实现一下四个目标：</p>
<ul>
<li>使程序代码混乱最小化</li>
<li>捕获并保留诊断信息</li>
<li>通知何时的人员</li>
<li>采用何时的方式结束异常活动</li>
</ul>
<h3 id="不要过度使用异常"><a href="#不要过度使用异常" class="headerlink" title="不要过度使用异常"></a>不要过度使用异常</h3><ul>
<li>把异常和普通错误混在一起，不在编写任何处理错误的代码，全部一刀切的用抛出异常处理</li>
<li>使用异常处理来代替流程控制</li>
</ul>
<p>&amp;上面两点应该避免，例如，下棋时，用户重复在一点落子，这种情况是完全能预料到的，直接用个判断语句比抛出异常更加简单合适。</p>
<h3 id="避免过大的try块"><a href="#避免过大的try块" class="headerlink" title="避免过大的try块"></a>避免过大的try块</h3><p>&emsp;try块过大的话，内部的代码过多，产生异常的可能就越多，导致分析异常产生原因时会很难。针对一个try块中的各种异常，还要用多个catch来处理，这种分类也会很复杂。所以，应该将代码拆开，方便寻找异常和处理异常。</p>
<h3 id="避免使用Catch-All语句"><a href="#避免使用Catch-All语句" class="headerlink" title="避免使用Catch All语句"></a>避免使用Catch All语句</h3><p>&emsp;所谓的Catch All语句就是用一个异常种类，例如用Throwable来捕获异常。这样子模糊了异常的种类，不利于做出针对性的处理，在catch中还有用流程控制语句分别处理，得不偿失。</p>
<h3 id="不要忽略异常"><a href="#不要忽略异常" class="headerlink" title="不要忽略异常"></a>不要忽略异常</h3><p>&emsp;对于异常不能视而不见，然catch为空，企图蒙混过关，或者打印跟踪栈信息就了事。应该尽量修复异常，或者将异常转译包装为当前层的异常抛给上一层。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记8</title>
    <url>/2018/05/01/Java%E7%AC%94%E8%AE%B08/</url>
    <content><![CDATA[<p>&emsp;有些特殊的类的对象是由固定个数的，例如四季，只有四个。这就是枚举类。这篇就来讲讲枚举类以及垃圾管理。</p>
<a id="more"></a>

<h1 id="枚举类"><a href="#枚举类" class="headerlink" title="枚举类"></a>枚举类</h1><h2 id="手动实现枚举类"><a href="#手动实现枚举类" class="headerlink" title="手动实现枚举类"></a>手动实现枚举类</h2><p>&emsp;在以前如果想用枚举类，偷懒的办法是用静态常量来表示，四季用四个数字表示，但是存在许多问题，例如类型不安全，意义不明确等。或者用类来实现，那有太麻烦，需要用private将构造器隐藏起来，把四个季节全都用public static final保存，还要提供一些静态方法使用四个季节。这样就比较麻烦。而在JDK1.5之后就有了枚举类。</p>
<h2 id="枚举类入门"><a href="#枚举类入门" class="headerlink" title="枚举类入门"></a>枚举类入门</h2><p>&emsp;enum关键字与class，interface地位相同，用于定义枚举类。（static不能修饰外部类，只有静态内部类，final可以修饰外部类，表示不能继承）enum是一种特殊的类，一样可以有自己的成员变量，方法，也可实现一个或多个接口，也可以定义自己的构造器。一个Java源文件只能由一个public的enum类。但是枚举类和普通了之间还是存在非常多的区别：</p>
<ul>
<li>enum类继承的是java.lang.Enum类，不是Object类，因此不能显示的继承其他父类。</li>
<li>enum实现了java.lang.Serializable和java.lang.Comparable接口（序列化和可比较）</li>
<li>使用enum定义<strong>非抽象</strong>的枚举类会默认使用final修饰，因此不能继承</li>
<li>枚举类的构造器只能是private修饰</li>
<li>枚举类的所有<strong>实例</strong>必须在枚举类的第一行显式的列出，系统会自动添加public static final</li>
<li>枚举类默认提供一个values()方法，用于遍历枚举值</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> SeasonEnum&#123;</span><br><span class="line">    SPRING,SUMMER,FALL,WINTER;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;如果需要使用，则通过EnumClass.variable的形式，如Season.SPRING。下面使用values()方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EnumTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">judge</span><span class="params">(SeasonEnum s)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">switch</span> (s) &#123;</span><br><span class="line">        <span class="keyword">case</span> SPRING:</span><br><span class="line">            System.out.println(<span class="string">"春天"</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> SUMMER:</span><br><span class="line">            System.out.println(<span class="string">"夏天"</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> FALL:</span><br><span class="line">            System.out.println(<span class="string">"秋天"</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> WINTER:</span><br><span class="line">            System.out.println(<span class="string">"冬天"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (SeasonEnum s : SeasonEnum.values()) &#123;</span><br><span class="line">            System.out.println(s);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">new</span> EnumTest().judge(SeasonEnum.SPRING);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;foreach循环中可以接受数组和集合，经过调试，values()方法返回的是数组。<br><br><img src="/2018/05/01/Java%E7%AC%94%E8%AE%B08/array.png" alt="返回值"><br>&emsp;在上面的代码中，switch控制表达式可以是任意枚举类，而且可以直接用枚举值作为case的值。<br><br>&emsp;由于所有的枚举类继承了java.lang.Enum类，所以可以直接使用改类中的方法：</p>
<ul>
<li>int compareTo(E o):与参数比较，若该枚举实例在参数前则返回负整数；若该枚举实例在参数后则返回正整数，否则为0。要求参数必须是与该枚举实例相同的的枚举类实例。</li>
<li>String name():返回枚举实例的名称，名称就是定义枚举类时所列出的所有枚举值之一。</li>
<li>String toString()：与上一个方法类似，优先使用该方法。</li>
<li>int ordinal():返回枚举值的索引值</li>
</ul>
<h2 id="枚举值的成员变量、方法和构造器"><a href="#枚举值的成员变量、方法和构造器" class="headerlink" title="枚举值的成员变量、方法和构造器"></a>枚举值的成员变量、方法和构造器</h2><p>&emsp;枚举值也是一个类，所以可以有自己的成员变量，方法和构造器。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Gender&#123;</span><br><span class="line">    MALE,FEMALE;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GenderTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        Gender g = Enum.valueOf(Gender.class , "FEMALE");</span><br><span class="line">        g.name = <span class="string">"女"</span>;</span><br><span class="line">        System.out.println(g + <span class="string">"代表："</span> + g.name);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;<code>Enum.valueOf(Gender.class , &quot;FEMALE&quot;)</code>这句的意思是将指定名称(“FEMALE”)的指定枚举类型的枚举常量返回给<code>Gender g</code>。简而言之，有“FEMALE”枚举值的这个枚举类实例给了g。但是这里的代码写的很不好，<code>g.name = &quot;女&quot;</code>直接对对象的成员变量修改是不好的，可能出现FEMALE代表了男的情况。应该将name改为private的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Gender &#123;</span><br><span class="line">    MALE, FEMALE;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">switch</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">        <span class="keyword">case</span> MALE:</span><br><span class="line">            <span class="keyword">if</span> (name.equals(<span class="string">"男"</span>))&#123;</span><br><span class="line">                <span class="keyword">this</span>.name = name;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">"参数错误"</span>);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> FEMALE:</span><br><span class="line">            <span class="keyword">if</span> (name.equals(<span class="string">"女"</span>))&#123;</span><br><span class="line">                <span class="keyword">this</span>.name = name;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">"参数错误"</span>);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这样就避免了直接对name操作，也避免了将“男”传给FEMALE这种事。但是这样的代码还是存在问题，枚举类通常应该设计成不可变类，其成员变量值不应该允许改变，因此最好用private final修饰成员变量。联系以前的知识，如果变量都用final修饰，则应在构造器中指定初始值，或者定义成员变量时指定初始值，或者在初始化块中指定初始值。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Gender&#123;</span><br><span class="line">    MALE(<span class="string">"男"</span>),FEMALE(<span class="string">"女"</span>);</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Gender</span><span class="params">(String name)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这段代码相当于下面这段：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> Gender MALE = <span class="keyword">new</span> Gender(<span class="string">"男"</span>);</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> Gender FEMALE = <span class="keyword">new</span> Gender(<span class="string">"女"</span>);</span><br></pre></td></tr></table></figure>

<h2 id="实现接口的枚举类"><a href="#实现接口的枚举类" class="headerlink" title="实现接口的枚举类"></a>实现接口的枚举类</h2><p>&emsp;与普通方法类似，枚举类实现一个或多个接口时，也需要实现该接口所包含的方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">GenderDesc</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">info</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Gender implements GenderDesc&#123;</span><br><span class="line">    MALE(<span class="string">"男"</span>),FEMALE(<span class="string">"女"</span>);</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Gender</span><span class="params">(String name)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">info</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"这是一个定义性别的枚举类"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;但是这样子写有个问题，“男”和“女”的类体是一样的，不论哪个调用info()方法结果都是一样的。因为它们具有同一个方法题，所以需要拆分开。<br></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Gender implements GenderDesc&#123;</span><br><span class="line">    MALE(<span class="string">"男"</span>)&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">info</span><span class="params">()</span></span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"这个枚举值代表男性"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    FEMALE(<span class="string">"女"</span>)&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">info</span><span class="params">()</span></span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"这个枚举值代表女性"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这个代码和匿名内部类语法类似，花括号实际就是类体的一部分。编译上面的程序会产生Gender.class,Gender$1.class,Gender$2.class三个文件，MALE和FEMALE实际是Gender匿名子类的实例，不是Gender类的实例。</p>
<h2 id="包含抽象方法的枚举类"><a href="#包含抽象方法的枚举类" class="headerlink" title="包含抽象方法的枚举类"></a>包含抽象方法的枚举类</h2><p>&emsp;假设有个Operation枚举类，有四个枚举值分别代表加，减，乘，除，再定义一个eval方法完成计算。但是不同的枚举值会有不同的操作，所以可以将eval定义为抽象方法，让四个枚举值分别为eval提供不同的实现。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Operation &#123;</span><br><span class="line">    PLUS &#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">eval</span><span class="params">(<span class="keyword">double</span> x, <span class="keyword">double</span> y)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> x + y;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    MINUS &#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">eval</span><span class="params">(<span class="keyword">double</span> x, <span class="keyword">double</span> y)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> x - y;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    TIMES &#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">eval</span><span class="params">(<span class="keyword">double</span> x, <span class="keyword">double</span> y)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> x * y;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    DIVIDE &#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">eval</span><span class="params">(<span class="keyword">double</span> x, <span class="keyword">double</span> y)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> x / y;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">double</span> <span class="title">eval</span><span class="params">(<span class="keyword">double</span> x,<span class="keyword">double</span> y)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        System.out.println(Operation.PLUS.eval(<span class="number">3</span>,<span class="number">4</span>));</span><br><span class="line">        System.out.println(Operation.MINUS.eval(<span class="number">3</span>,<span class="number">4</span>));</span><br><span class="line">        System.out.println(Operation.TIMES.eval(<span class="number">3</span>,<span class="number">4</span>));</span><br><span class="line">        System.out.println(Operation.DIVIDE.eval(<span class="number">3</span>,<span class="number">4</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;编译上面的代码会产生5个class文件。枚举类里定义抽象方法时不能用abstract将枚举类定义为抽象类，因为系统会自动添加。同时，每个枚举值都要为抽象方法提供实现。<br><br>&emsp;对于理解枚举类，最关键的就是就是将第一行（到’;’为止）的枚举值先剔除不看，把枚举值作为枚举类的实例，当作一个普通类，再来理解上面的代码。</p>
<h1 id="对象与垃圾回收"><a href="#对象与垃圾回收" class="headerlink" title="对象与垃圾回收"></a>对象与垃圾回收</h1><p>&emsp;当程序创建对象，数组等引用实体类型时，系统都会在堆内存分配一块内存区，对象就保存在这里，当这块内存不再有任何引用变量引用时，这块内存就变为垃圾，等待垃圾回收机制回收，垃圾回收机制具有如下机制：</p>
<ul>
<li>垃圾回收机制只负责回收堆内存中的对象</li>
<li>程序无法精确控制垃圾回收的运行</li>
<li>垃圾回收机制回收任何对象之前，总会先调用它的finalize()方法，该方法可能使该对象重新复活（让一个引用变量重新引用该对象），从而导致垃圾回收机制取消回收</li>
</ul>
<h2 id="对象在内存中的状态"><a href="#对象在内存中的状态" class="headerlink" title="对象在内存中的状态"></a>对象在内存中的状态</h2><p>&emsp;根据一个对象被引用变量所引用的状态，可以把它所处的状态分为三个状态：</p>
<ul>
<li>可达状态：当一个对象被创建后，若有一个以上的引用变量引用它，则这个对象处于可达状态，程序可以通过引用变量来使用该对象的实例变量和方法</li>
<li>可恢复状态：如果某个对象不再有引用变量引用它，它就进入了可恢复状态，垃圾回收机制会准备回收内存，在回收之前会先调用<strong>所有</strong>finalize()方法进行资源清理，如果finalize()让一个引用变量引用该对象，则该对象重新进入可达状态，否则进入不可达状态</li>
<li>不可达状态：当finalize()被调用后仍然没有使对象恢复为可达状态，这是就是不可达状态，系统就会回收对象所占用的资源</li>
</ul>
<h2 id="强制垃圾回收"><a href="#强制垃圾回收" class="headerlink" title="强制垃圾回收"></a>强制垃圾回收</h2><p>&emsp;当一个对象失去引用后，系统何时调用finalize()方法，何时回收资源是不可控的，能控制的只有对某个对象不再引用。虽然存在强制回收的方式：调用System类的gc()静态方法System.gc()和Buntime对象的gc()实例方法Runtime.getRuntime.gc()。但是这种方式只是通知系统回收，系统是否进行垃圾回收仍然是不可确定的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Gctest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">4</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">new</span> Gctest();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">finalize</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"系统正在清理"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这里重写了finalize()方法，如果说进行了垃圾回收，那么就会调用到重写的方法，但是什么都没出现，那么代表没有进行回收。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Gctest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">4</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">new</span> Gctest();</span><br><span class="line">            Runtime.getRuntime().gc();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">finalize</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"系统正在清理"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;这样子后发现就进行了回收。而且当在Eclipse的Run As&gt;Run Configurations&gt;Arguments&gt;VM arguments中添加参数<code>-verbose:gc</code>或<code>-XX:+PrintGC</code>可以看到垃圾回收的简略信息,而参数<code>-XX:+PrintGCDetails</code>可以看到详细信息。</p>
<h2 id="finalize方法"><a href="#finalize方法" class="headerlink" title="finalize方法"></a>finalize方法</h2><p>&emsp;finalize()方法的原型为<code>protected void finalize() throws Throwable</code>,是定义在Object类中的实例方法，具有如下特点：</p>
<ul>
<li>永远不要主动调用某个对象的finalize方法，而应交给垃圾回收机制</li>
<li>该方法何时被调用，是否被调用都是不确定的</li>
<li>当该方法遇到异常时，垃圾回收机制不会报告任何异常，程序继续执行</li>
</ul>
<p>&emsp;由于该方法不一定会被执行，所以想清理类里的资源不能用这个方法，有专门的方法做这个。</p>
<h2 id="对象的软，弱和虚引用"><a href="#对象的软，弱和虚引用" class="headerlink" title="对象的软，弱和虚引用"></a>对象的软，弱和虚引用</h2><p>&emsp;对于大部分对象，程序里都会有引用变量引用到该对象。除此之外，java.lang.ref包还提供了三个类：SoftReference,PhantomReference,WeakReference。四种对应了强引用，软引用，虚引用，弱引用。</p>
<h3 id="强引用（StrongReference）"><a href="#强引用（StrongReference）" class="headerlink" title="强引用（StrongReference）"></a>强引用（StrongReference）</h3><p>&emsp;最常见的引用方式，当一个对象有一个或多个引用变量引用到它时，这就是强引用，是不可能被回收的。</p>
<h3 id="软引用（SoftReference）"><a href="#软引用（SoftReference）" class="headerlink" title="软引用（SoftReference）"></a>软引用（SoftReference）</h3><p>&emsp;软引用需要通过SoftReference类实现，当一个对象只有软引用时，有可能被回收，特别是当内存空间不足时。</p>
<h3 id="弱引用（WeakReference）"><a href="#弱引用（WeakReference）" class="headerlink" title="弱引用（WeakReference）"></a>弱引用（WeakReference）</h3><p>&emsp;弱引用需要通过WeakReference类实现比软引用更低的引用，对于只有弱引用的对象，系统总是会回收，不管内存够不够。但是不是立刻回收，具体什么时候还是要看垃圾回收机制。</p>
<h3 id="虚引用（PhantomReference）"><a href="#虚引用（PhantomReference）" class="headerlink" title="虚引用（PhantomReference）"></a>虚引用（PhantomReference）</h3><p>&emsp;虚引用通过PhantomReference类实现，虚引用完全类似没有引用，虚引用主要用于跟踪垃圾回收状态，虚引用不能单独使用，必须和引用队列（ReferenceQueue）联合使用。</p>
<hr>
<p>&emsp;三个引用类都包含了一个get()方法，用于获取被它们引用的对象。<br><br>&emsp;引用队列由java.lang.ref.ReferenceQueue类表示，它用于保存被回收后的对象的引用。当联合使用软引用，弱引用和引用队列时，系统在回收被引用的对象后，将把被回收对象的引用添加到关联的引用队列。而对于虚引用，它实在对象被释放之前就被添加到关联的引用队列中，这使得可以在对象被回收之前采取行动。软引用和弱引用可以单独使用，但是虚引用不能。单独使用虚引用没什么意义，虚引用的作用就是跟踪对象被垃圾回收的状态，程序可以通过检查与虚引用关联的引用队列中是否已经包含了该虚引用，从而了解虚引用所引用的对象是否即将被回收。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.ref.WeakReference;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ReferenceTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        String str = <span class="keyword">new</span> String(<span class="string">"算法导论"</span>);</span><br><span class="line">        WeakReference wr = <span class="keyword">new</span> WeakReference(str);</span><br><span class="line">        str = <span class="keyword">null</span>;</span><br><span class="line">        System.out.println(wr.get());</span><br><span class="line">        System.gc();</span><br><span class="line">        System.runFinalization();</span><br><span class="line">        System.out.println(wr.get());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;上面的代码创建了一个字符串,str强引用到字符串，wr弱引用到字符串，然后将str与对象的引用切断。结果会输出字符串和null。不要使用<code>String str=&quot;算法导论&quot;;</code>的方式，这样产生的字符串将有常量池管理，系统不会回收这个字符串直接量。<br><br>&emsp;如果要使用这种由软引用或弱引用引用的对象，在使用get()方法后要注意是否成功取出了对象，因为有可能已近被释放了。</p>
<h1 id="使用jar文件"><a href="#使用jar文件" class="headerlink" title="使用jar文件"></a>使用jar文件</h1><p>&emsp;jar类似于压缩文件，与zip文件兼容，但是多了一个META-INF/MANIFEST.MF的清单文件。当把jar文件给别人使用时，只用把jar的位置添加到CLASSPATH中，JVM就会自动在内存中解压这个包，把这个包作为CLASSPATH的一部分，可以直接调用里面的文件。</p>
<h2 id="jar命令详解"><a href="#jar命令详解" class="headerlink" title="jar命令详解"></a>jar命令详解</h2><p>&emsp;生成jar的工具是由bin目录下的jar.exe工具来做，会用到lib目录下的tools.jar，这个通常会自动加载。</p>
<h3 id="创建JAR文件"><a href="#创建JAR文件" class="headerlink" title="创建JAR文件"></a>创建JAR文件</h3><p>&emsp;<code>jar cf test.jar test</code>表示将当前文件夹下的test文件夹中所有内容生成test.jar。如果加上参数v:<code>cvf</code>，就会显式详细压缩过程。</p>
<h3 id="不添加清单文件"><a href="#不添加清单文件" class="headerlink" title="不添加清单文件"></a>不添加清单文件</h3><p>&emsp;<code>jar cvfM test.jar test</code>这样写表示不生成清单文件。</p>
<h3 id="自定义清单文件内容"><a href="#自定义清单文件内容" class="headerlink" title="自定义清单文件内容"></a>自定义清单文件内容</h3><p>&emsp;<code>jar cvfm test.jar manifest.mf test</code>注意manifest.mf文件只是意味着需要一个清单文件，不是说非要这个名字，也不是非要这个格式。虽然格式和名字不受限制，但是内容的格式有限制，依照<code>key:&lt;空格&gt;value</code>，有如下要求：</p>
<ul>
<li>每行只能写一个key-value对，且必须顶头写</li>
<li>文件开头不能有空行</li>
<li>文件以一个空行结尾</li>
</ul>
<p>&emsp;保存在当前文件夹，名字随意，用txt格式。假如添加了如下内容，且保存为a.txt。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Manifest-Version: <span class="number">1.0</span>txt</span><br><span class="line">Created-By: <span class="number">9.0</span><span class="number">.4</span> (Oracle Corporation)</span><br></pre></td></tr></table></figure>

<p>&emsp;运行命令<code>jar cvfm test.jar a.txt test</code>，打开jar文件，test.jar&gt;META-INF&gt;MANIFEST.MF，里面写的就是上面的内容。</p>
<h3 id="其他命令"><a href="#其他命令" class="headerlink" title="其他命令"></a>其他命令</h3><ul>
<li><code>jar tf test.jar</code>，查看jar包中的内容。若内容过多，可用<code>jar tf test.jar &gt; b.txt</code>将结果重定向至b.txt中</li>
<li><code>jar tvf test.jar</code>，查看详细内容，包括大小，时间等</li>
<li><code>jar xf test.jar</code>，解压test.jar到当前目录</li>
<li><code>jar xvf test.jar</code>，带提示信息解压</li>
<li><code>jar uf test.jar Hello.class</code>，将Hello.class添加到包中，.java文件也可，都不会添加到test文件夹中，而是在上一层级</li>
<li><code>jar uvf test.jar Hello.class</code>，带提示信息添加</li>
</ul>
<h2 id="创建可执行的jar文件"><a href="#创建可执行的jar文件" class="headerlink" title="创建可执行的jar文件"></a>创建可执行的jar文件</h2><p>&emsp;<code>jar cvfe test.jar test.Test test</code>，用-e参数指定主类，要注意在jar文件中的test文件夹下有Test这个竹类，且要是.class文件。前面，所有的添加操作都是如此，一定要把.java和.class文件都包含进去。<br><br>&emsp;运行这个jar包的命令是<code>java -jar test.jar</code>和<code>javaw test.jar</code>。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java集合</title>
    <url>/2018/08/12/Java%E9%9B%86%E5%90%88/</url>
    <content><![CDATA[<p>本篇是Java笔记10的复习,顺便捎带了点数据结构中的链表和树.</p>
<a id="more"></a>

<h1 id="ArrayList"><a href="#ArrayList" class="headerlink" title="ArrayList"></a>ArrayList</h1><h2 id="与数组的区别"><a href="#与数组的区别" class="headerlink" title="与数组的区别"></a>与数组的区别</h2><p>如果要存放多个对象，可以使用数组，但是数组有局限性.比如:声明长度是10的数组,不用的数组就浪费了,超过10的个数，又放不下.</p>
<p>为了解决数组的局限性，引入容器类的概念。最常见的容器类就是ArrayList容器的容量”capacity”会随着对象的增加，自动增长只需要不断往容器里增加英雄即可，不用担心会出现数组的边界问题。 </p>
<h2 id="常用方法"><a href="#常用方法" class="headerlink" title="常用方法:"></a>常用方法:</h2><ul>
<li><p>add </p>
<p>有两种用法:</p>
<p>第一种是直接add对象，把对象加在最后面</p>
<p><code>heros.add(new Hero(&quot;hero &quot; + i));</code></p>
<p>第二种是在指定位置加对象</p>
<p><code>heros.add(3, specialHero);</code></p>
</li>
<li><p>判断是否存在</p>
<p>通过方法contains判断一个对象是否在容器中</p>
<p><code>contains(Object o)</code></p>
<p>判断标准:是否是同一个对象，而不是name是否相同 </p>
</li>
<li><p>获取指定位置的对象 </p>
<p><code>get(int index)</code></p>
<p>通过get获取指定位置的对象，如果输入的下标越界，一样会报错 </p>
</li>
<li><p>获取对象所处的位置</p>
<p>indexOf用于判断一个对象在ArrayList中所处的位置 与contains一样，判断标准是对象是否相同，而非对象的name值是否相等 </p>
</li>
<li><p>删除 </p>
<p>remove用于把对象从ArrayList中删除</p>
<p>remove可以根据下标删除ArrayList的元素 </p>
<p><code>heros.remove(2);</code></p>
<p><code>heros.remove(specialHero);</code></p>
</li>
<li><p>替换</p>
<p>set用于替换指定位置的元素</p>
<p><code>set(int index, E element)</code></p>
</li>
<li><p>获取大小</p>
<p>size用于获取ArrayList的大小 </p>
</li>
<li><p>转换为数组</p>
<p>toArray可以把一个ArrayList对象转换为数组。 需要注意的是，如果要转换为一个Hero数组，那么需要传递一个Hero数组类型的对象给toArray()，这样toArray方法才知道，你希望转换为哪种类型的数组，否则只能转换为Object数组 </p>
<p><code>Hero hs[] = (Hero[])heros.toArray(new Hero[]{});</code></p>
</li>
<li><p>把另一个容器所有对象都加进来</p>
<p>addAll</p>
</li>
<li><p>清空</p>
<p>clear清空一个ArrayList </p>
</li>
</ul>
<p>其他方法参见我Java笔记10.</p>
<h1 id="遍历"><a href="#遍历" class="headerlink" title="遍历"></a>遍历</h1><ul>
<li>普通for循环</li>
<li>foreach循环</li>
<li>迭代器循环</li>
<li>Lambda表达式</li>
</ul>
<h1 id="LinkedList"><a href="#LinkedList" class="headerlink" title="LinkedList"></a>LinkedList</h1><h2 id="双向链表"><a href="#双向链表" class="headerlink" title="双向链表"></a>双向链表</h2><p>LinkedList除了实现了List接口外，LinkedList还实现了双向链表结构Deque，可以很方便的在头尾插入删除数据 </p>
<p>什么是链表结构: 与数组结构相比较，数组结构，就好像是电影院，每个位置都有标示，每个位置之间的间隔都是一样的。 而链表就相当于佛珠，每个珠子，只连接前一个和后一个，不用关心除此之外的其他佛珠在哪里。 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> collection;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestCollection</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//LinkedList是一个双向链表结构的list</span></span><br><span class="line">        LinkedList&lt;Hero&gt; ll =<span class="keyword">new</span> LinkedList&lt;Hero&gt;();</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//所以可以很方便的在头部和尾部插入数据</span></span><br><span class="line">        <span class="comment">//在最后插入新的英雄</span></span><br><span class="line">        ll.addLast(<span class="keyword">new</span> Hero(<span class="string">"hero1"</span>));</span><br><span class="line">        ll.addLast(<span class="keyword">new</span> Hero(<span class="string">"hero2"</span>));</span><br><span class="line">        ll.addLast(<span class="keyword">new</span> Hero(<span class="string">"hero3"</span>));</span><br><span class="line">        System.out.println(ll);</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//在最前面插入新的英雄</span></span><br><span class="line">        ll.addFirst(<span class="keyword">new</span> Hero(<span class="string">"heroX"</span>));</span><br><span class="line">        System.out.println(ll);</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//查看最前面的英雄</span></span><br><span class="line">        System.out.println(ll.getFirst());</span><br><span class="line">        <span class="comment">//查看最后面的英雄</span></span><br><span class="line">        System.out.println(ll.getLast());</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//查看不会导致英雄被删除</span></span><br><span class="line">        System.out.println(ll);</span><br><span class="line">        <span class="comment">//取出最前面的英雄</span></span><br><span class="line">        System.out.println(ll.removeFirst());</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//取出最后面的英雄</span></span><br><span class="line">        System.out.println(ll.removeLast());</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//取出会导致英雄被删除</span></span><br><span class="line">        System.out.println(ll);</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">      </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="队列"><a href="#队列" class="headerlink" title="队列"></a>队列</h2><p>LinkedList除了实现了List和Deque外，还实现了Queue接口(队列)。Queue是先进先出队列 FIFO，常用方法:</p>
<p>offer在最后添加元素</p>
<p>poll取出第一个元素</p>
<p>peek查看第一个元素 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> collection;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="keyword">import</span> java.util.Queue;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestCollection</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//和ArrayList一样，LinkedList也实现了List接口</span></span><br><span class="line">        List ll =<span class="keyword">new</span> LinkedList&lt;Hero&gt;();</span><br><span class="line">          </span><br><span class="line">        <span class="comment">//所不同的是LinkedList还实现了Deque，进而又实现了Queue这个接口</span></span><br><span class="line">        <span class="comment">//Queue代表FIFO 先进先出的队列</span></span><br><span class="line">        Queue&lt;Hero&gt; q= <span class="keyword">new</span> LinkedList&lt;Hero&gt;();</span><br><span class="line">          </span><br><span class="line">        <span class="comment">//加在队列的最后面</span></span><br><span class="line">        System.out.print(<span class="string">"初始化队列：\t"</span>);</span><br><span class="line">        q.offer(<span class="keyword">new</span> Hero(<span class="string">"Hero1"</span>));</span><br><span class="line">        q.offer(<span class="keyword">new</span> Hero(<span class="string">"Hero2"</span>));</span><br><span class="line">        q.offer(<span class="keyword">new</span> Hero(<span class="string">"Hero3"</span>));</span><br><span class="line">        q.offer(<span class="keyword">new</span> Hero(<span class="string">"Hero4"</span>));</span><br><span class="line">          </span><br><span class="line">        System.out.println(q);</span><br><span class="line">        System.out.print(<span class="string">"把第一个元素取poll()出来:\t"</span>);</span><br><span class="line">        <span class="comment">//取出第一个Hero，FIFO 先进先出</span></span><br><span class="line">        Hero h = q.poll();</span><br><span class="line">        System.out.println(h);</span><br><span class="line">        System.out.print(<span class="string">"取出第一个元素之后的队列:\t"</span>);</span><br><span class="line">        System.out.println(q);</span><br><span class="line">          </span><br><span class="line">        <span class="comment">//把第一个拿出来看一看，但是不取出来</span></span><br><span class="line">        h=q.peek();</span><br><span class="line">        System.out.print(<span class="string">"查看peek()第一个元素:\t"</span>);</span><br><span class="line">        System.out.println(h);</span><br><span class="line">        System.out.print(<span class="string">"查看并不会导致第一个元素被取出来:\t"</span>);</span><br><span class="line">        System.out.println(q);</span><br><span class="line">          </span><br><span class="line">    &#125;</span><br><span class="line">       </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ArrayList和LinkedList的区别"><a href="#ArrayList和LinkedList的区别" class="headerlink" title="ArrayList和LinkedList的区别"></a>ArrayList和LinkedList的区别</h2><p>ArrayList 插入，删除数据慢 </p>
<p>LinkedList，插入，删除数据快</p>
<p>ArrayList是顺序结构，所以定位很快，指哪找哪。 就像电影院位置一样，有了电影票，一下就找到位置了.</p>
<p>LinkedList 是链表结构，就像手里的一串佛珠，要找出第99个佛珠，必须得一个一个的数过去，所以定位慢.</p>
<p>比较 ArrayList和LinkedList 在最后面插入100000条数据，谁快谁慢？为什么？ </p>
<blockquote>
<p>直接调用add方法，就表示在最后插入数据  因为是插入在最后面，所以对于数组而言，并没有一个移动很多数据的需要，所以也非常的快  而链表本身就很快，无论插入在哪里 </p>
</blockquote>
<p>在List的中间位置，插入数据，比较ArrayList快，还是LinkedList快，并解释为什么？ </p>
<blockquote>
<p>在这个位置插入数据 </p>
<p>数组定位很快，插入数据比较慢 </p>
<p>链表定位很慢，插入数据比较快 </p>
<p>最后发现，当总数是10000条的时候，链表定位的总开支要比数组插入的总开支更多，所以最后整体表现，数组会更好。 如果总数是1000条，结果可能就不一样了。 </p>
</blockquote>
<h1 id="二叉树"><a href="#二叉树" class="headerlink" title="二叉树"></a>二叉树</h1><p>二叉树由各种节点组成 二叉树特点： 每个节点都可以有左子节点，右子节点,每一个节点都有一个值.</p>
<p>假设通过二叉树对如下10个随机数进行排序 67,7,30,73,10,0,78,81,10,74 排序的第一个步骤是把数据插入到该二叉树中 插入基本逻辑是，小、相同的放左边，大的放右边 </p>
<ol>
<li><p>67 放在根节点 </p>
</li>
<li><p>7 比 67小，放在67的左节点 </p>
</li>
<li><p>30 比67 小，找到67的左节点7，30比7大，就放在7的右节点 </p>
</li>
<li><p>73 比67大， 放在67得右节点 </p>
</li>
<li><p>10 比 67小，找到67的左节点7，10比7大，找到7的右节点30，10比30小，放在30的左节点。 </p>
<p>… … </p>
</li>
</ol>
<p>   9.10比67小，找到67的左节点7，10比7大，找到7的右节点30，10比30小，找到30的左节点10，10和10一样大，放在左边 </p>
<p><img src="/2018/08/12/Java%E9%9B%86%E5%90%88/blog/source/_posts/Java%E9%9B%86%E5%90%88%5C1009.png" alt></p>
<p>通过上一个步骤的插入行为，实际上，数据就已经排好序了。 </p>
<p>接下来要做的是看，把这些已经排好序的数据，遍历成我们常用的List或者数组的形式  </p>
<p>二叉树的遍历分左序，中序，右序 </p>
<p>左序即： 中间的数遍历后放在左边 </p>
<p>中序即： 中间的数遍历后放在中间 </p>
<p>右序即： 中间的数遍历后放在右边 </p>
<p>我们希望遍历后的结果是从小到大的，所以应该采用中序遍历 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> collection;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 左子节点</span></span><br><span class="line">    <span class="keyword">public</span> Node leftNode;</span><br><span class="line">    <span class="comment">// 右子节点</span></span><br><span class="line">    <span class="keyword">public</span> Node rightNode;</span><br><span class="line">  </span><br><span class="line">    <span class="comment">// 值</span></span><br><span class="line">    <span class="keyword">public</span> Object value;</span><br><span class="line">  </span><br><span class="line">    <span class="comment">// 插入 数据</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(Object v)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 如果当前节点没有值，就把数据放在当前节点上</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">null</span> == value)</span><br><span class="line">            value = v;</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 如果当前节点有值，就进行判断，新增的值与当前值的大小关系</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 新增的值，比当前值小或者相同</span></span><br><span class="line">             </span><br><span class="line">            <span class="keyword">if</span> ((Integer) v -((Integer)value) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (<span class="keyword">null</span> == leftNode)</span><br><span class="line">                    leftNode = <span class="keyword">new</span> Node();</span><br><span class="line">                leftNode.add(v);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 新增的值，比当前值大</span></span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (<span class="keyword">null</span> == rightNode)</span><br><span class="line">                    rightNode = <span class="keyword">new</span> Node();</span><br><span class="line">                rightNode.add(v);</span><br><span class="line">            &#125;</span><br><span class="line">  </span><br><span class="line">        &#125;</span><br><span class="line">  </span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line"> <span class="comment">// 中序遍历所有的节点</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Object&gt; <span class="title">values</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        List&lt;Object&gt; values = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 左节点的遍历结果</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">null</span> != leftNode)</span><br><span class="line">            values.addAll(leftNode.values());</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 当前节点</span></span><br><span class="line">        values.add(value);</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 右节点的遍历结果</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">null</span> != rightNode)</span><br><span class="line">  </span><br><span class="line">            values.addAll(rightNode.values());</span><br><span class="line">  </span><br><span class="line">        <span class="keyword">return</span> values;</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">        <span class="keyword">int</span> randoms[] = <span class="keyword">new</span> <span class="keyword">int</span>[] &#123; <span class="number">67</span>, <span class="number">7</span>, <span class="number">30</span>, <span class="number">73</span>, <span class="number">10</span>, <span class="number">0</span>, <span class="number">78</span>, <span class="number">81</span>, <span class="number">10</span>, <span class="number">74</span> &#125;;</span><br><span class="line">  </span><br><span class="line">        Node roots = <span class="keyword">new</span> Node();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> number : randoms) &#123;</span><br><span class="line">            roots.add(number);</span><br><span class="line">        &#125;</span><br><span class="line">  </span><br><span class="line">        System.out.println(roots.values());</span><br><span class="line">  </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>比较冒泡法，选择法以及二叉树排序的性能区别</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> collection;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SortCompare</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//初始化随机数</span></span><br><span class="line">        <span class="keyword">int</span> total = <span class="number">40000</span>;</span><br><span class="line">        System.out.println(<span class="string">"初始化一个长度是"</span>+total+<span class="string">"的随机数字的数组"</span>);</span><br><span class="line">        <span class="keyword">int</span>[] originalNumbers = <span class="keyword">new</span> <span class="keyword">int</span>[total];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; originalNumbers.length; i++) &#123;</span><br><span class="line">            originalNumbers[i] = (<span class="keyword">int</span>)(Math.random()*total);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"初始化完毕"</span>);</span><br><span class="line">        System.out.println(<span class="string">"接下来分别用3种算法进行排序"</span>);</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//从初始化了的随机数组复制过来，以保证，每一种排序算法的目标数组，都是一样的</span></span><br><span class="line">        <span class="keyword">int</span>[] use4sort;</span><br><span class="line">         </span><br><span class="line">        use4sort= Arrays.copyOf(originalNumbers, originalNumbers.length);</span><br><span class="line">        <span class="keyword">int</span>[] sortedNumbersBySelection= performance(<span class="keyword">new</span> SelectionSort(use4sort),<span class="string">"选择法"</span>);</span><br><span class="line">         </span><br><span class="line">        use4sort= Arrays.copyOf(originalNumbers, originalNumbers.length);</span><br><span class="line">        <span class="keyword">int</span>[] sortedNumbersByBubbling=performance(<span class="keyword">new</span> BubblingSort(use4sort),<span class="string">"冒泡法"</span>);</span><br><span class="line">         </span><br><span class="line">        use4sort= Arrays.copyOf(originalNumbers, originalNumbers.length);</span><br><span class="line">        <span class="keyword">int</span>[] sortedNumbersByTree=performance(<span class="keyword">new</span> TreeSort(use4sort),<span class="string">"二叉树"</span>);</span><br><span class="line">         </span><br><span class="line">        System.out.println(<span class="string">"查看排序结果，是否是不同的数组对象"</span>);</span><br><span class="line">        System.out.println(sortedNumbersBySelection);</span><br><span class="line">        System.out.println(sortedNumbersByBubbling);</span><br><span class="line">        System.out.println(sortedNumbersByTree);</span><br><span class="line">         </span><br><span class="line">        System.out.println(<span class="string">"查看排序结果，内容是否相同"</span>);</span><br><span class="line">        System.out.println(<span class="string">"比较 选择法 和 冒泡法  排序结果："</span>);</span><br><span class="line">        System.out.println(Arrays.equals(sortedNumbersBySelection, sortedNumbersByBubbling));</span><br><span class="line">        System.out.println(<span class="string">"比较 选择法 和 二叉树  排序结果："</span>);</span><br><span class="line">        System.out.println(Arrays.equals(sortedNumbersBySelection, sortedNumbersByTree));</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="class"><span class="keyword">interface</span> <span class="title">Sort</span></span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">void</span> <span class="title">sort</span><span class="params">()</span></span>;</span><br><span class="line">        <span class="keyword">int</span>[] values();</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">SelectionSort</span> <span class="keyword">implements</span> <span class="title">Sort</span></span>&#123;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">int</span> numbers[];</span><br><span class="line">        SelectionSort(<span class="keyword">int</span> [] numbers)&#123;</span><br><span class="line">            <span class="keyword">this</span>.numbers = numbers;</span><br><span class="line">        &#125;</span><br><span class="line">         </span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; numbers.length-<span class="number">1</span>; j++) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> i = j+<span class="number">1</span>; i &lt; numbers.length; i++) &#123;</span><br><span class="line">                    <span class="keyword">if</span>(numbers[i]&lt;numbers[j])&#123;  </span><br><span class="line">                        <span class="keyword">int</span> temp = numbers[j];</span><br><span class="line">                        numbers[j] = numbers[i];</span><br><span class="line">                        numbers[i] = temp;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">int</span>[] values() &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated method stub</span></span><br><span class="line">            <span class="keyword">return</span> numbers;</span><br><span class="line">        &#125;</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">BubblingSort</span> <span class="keyword">implements</span> <span class="title">Sort</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> numbers[];</span><br><span class="line">        BubblingSort(<span class="keyword">int</span> [] numbers)&#123;</span><br><span class="line">            <span class="keyword">this</span>.numbers = numbers;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; numbers.length; j++) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; numbers.length-j-<span class="number">1</span>; i++) &#123;</span><br><span class="line">                    <span class="keyword">if</span>(numbers[i]&gt;numbers[i+<span class="number">1</span>])&#123;  </span><br><span class="line">                        <span class="keyword">int</span> temp = numbers[i];</span><br><span class="line">                        numbers[i] = numbers[i+<span class="number">1</span>];</span><br><span class="line">                        numbers[i+<span class="number">1</span>] = temp;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">int</span>[] values() &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated method stub</span></span><br><span class="line">            <span class="keyword">return</span> numbers;</span><br><span class="line">        &#125;</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">TreeSort</span> <span class="keyword">implements</span> <span class="title">Sort</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> numbers[];</span><br><span class="line">        Node n;</span><br><span class="line">         </span><br><span class="line">        TreeSort(<span class="keyword">int</span> [] numbers)&#123;</span><br><span class="line">            n =<span class="keyword">new</span> Node();</span><br><span class="line">            <span class="keyword">this</span>.numbers = numbers;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">()</span> </span>&#123;</span><br><span class="line">         </span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i : numbers) &#123;</span><br><span class="line">                n.add(i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">int</span>[] values() &#123;</span><br><span class="line">            List&lt;Object&gt; list = n.values();</span><br><span class="line">            <span class="keyword">int</span> sortedNumbers[] = <span class="keyword">new</span> <span class="keyword">int</span>[list.size()];</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; sortedNumbers.length; i++) &#123;</span><br><span class="line">                sortedNumbers[i] = Integer.parseInt(list.get(i).toString());</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> sortedNumbers;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span>[] performance(Sort algorithm, String type) &#123;</span><br><span class="line">        <span class="keyword">long</span> start = System.currentTimeMillis();</span><br><span class="line">        algorithm.sort();</span><br><span class="line">        <span class="keyword">int</span> sortedNumbers[] = algorithm.values();</span><br><span class="line">        <span class="keyword">long</span> end = System.currentTimeMillis();</span><br><span class="line">        System.out.printf(<span class="string">"%s排序，一共耗时 %d 毫秒%n"</span>,type,end-start);</span><br><span class="line">        <span class="keyword">return</span> sortedNumbers;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<p>卒,写着写着发现还没我原来写的好,这篇就到这吧,不写了.</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java聚合操作</title>
    <url>/2018/08/13/Java%E8%81%9A%E5%90%88%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<p>在Java笔记11那篇中提到了聚合操作,但是没讲,这篇来看看.</p>
<a id="more"></a>

<h1 id="传统方式与聚合操作方式遍历数据"><a href="#传统方式与聚合操作方式遍历数据" class="headerlink" title="传统方式与聚合操作方式遍历数据"></a>传统方式与聚合操作方式遍历数据</h1><p>遍历数据的传统方式就是使用for循环，然后条件判断，最后打印出满足条件的数据:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (Hero h : heros) &#123;</span><br><span class="line">   <span class="keyword">if</span> (h.hp &gt; <span class="number">100</span> &amp;&amp; h.damage &lt; <span class="number">50</span>)</span><br><span class="line">      System.out.println(h.name);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用聚合操作方式，画风就发生了变化：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> </span><br><span class="line">heros</span><br><span class="line">	.stream()</span><br><span class="line">	.filter(h -&gt; h.hp &gt; <span class="number">100</span> &amp;&amp; h.damage &lt; <span class="number">50</span>)</span><br><span class="line">	.forEach(h -&gt; System.out.println(h.name));</span><br></pre></td></tr></table></figure>

<h1 id="Stream和管道的概念"><a href="#Stream和管道的概念" class="headerlink" title="Stream和管道的概念"></a>Stream和管道的概念</h1><p>要了解聚合操作，首先要建立Stream和管道的概念</p>
<p>Stream和Collection结构化的数据不一样，Stream是一系列的元素，就像是生产线上的罐头一样，一串串的出来。</p>
<p>管道指的是一系列的聚合操作。 </p>
<p>管道又分3个部分:</p>
<ul>
<li>管道源：在这个例子里，源是一个List</li>
<li>中间操作：每个中间操作，又会返回一个Stream，比如.filter()又返回一个Stream, 中间操作是“懒”操作，并不会真正进行遍历。</li>
<li>结束操作：当这个操作执行后，流就被使用“光”了，无法再被操作。所以这必定是流的最后一个操作。结束操作不会返回Stream，但是会返回int、float、String、 Collection或者像forEach，什么都不返回, 结束操作才进行真正的遍历行为，在遍历的时候，才会去进行中间操作的相关判断.</li>
</ul>
<p>注： 这个Stream和I/O章节的InputStream,OutputStream是不一样的概念。</p>
<h1 id="管道源"><a href="#管道源" class="headerlink" title="管道源"></a>管道源</h1><p>把Collection切换成管道源很简单，调用stream()就行了。</p>
<p><code>heros.stream()</code></p>
<p>但是数组却没有stream()方法，需要使用<code>Arrays.stream(hs)</code>或者<code>Stream.of(hs)</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lambda;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"><span class="keyword">import</span> java.util.HashMap;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="keyword">import</span> java.util.Random;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestAggregate</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Random r = <span class="keyword">new</span> Random();</span><br><span class="line">        List&lt;Hero&gt; heros = <span class="keyword">new</span> ArrayList&lt;Hero&gt;();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">            heros.add(<span class="keyword">new</span> Hero(<span class="string">"hero "</span> + i, r.nextInt(<span class="number">1000</span>), r.nextInt(<span class="number">100</span>)));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//管道源是集合</span></span><br><span class="line">        heros</span><br><span class="line">        .stream()</span><br><span class="line">        .forEach(h-&gt;System.out.println(h.name));</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//管道源是数组</span></span><br><span class="line">        Hero hs[] = heros.toArray(<span class="keyword">new</span> Hero[heros.size()]);</span><br><span class="line">        Arrays.stream(hs)</span><br><span class="line">        .forEach(h-&gt;System.out.println(h.name));</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="中间操作"><a href="#中间操作" class="headerlink" title="中间操作"></a>中间操作</h1><p>每个中间操作，又会返回一个Stream，比如.filter()又返回一个Stream, 中间操作是“懒”操作，并不会真正进行遍历。 </p>
<p>中间操作比较多，主要分两类对元素进行筛选和转换为其他形式的流</p>
<ul>
<li><p>对元素进行筛选：</p>
<ul>
<li>filter 匹配</li>
<li>distinct 去除重复(根据equals判断)</li>
<li>sorted 自然排序</li>
<li>sorted(Comparator<T>) 指定排序</T></li>
<li>limit 保留 </li>
<li>skip 忽略 </li>
</ul>
</li>
<li><p>转换为其他形式的流</p>
<ul>
<li>mapToDouble 转换为double的流</li>
<li>map 转换为任意类型的流 </li>
</ul>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lambda;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="keyword">import</span> java.util.Random;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestAggregate</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Random r = <span class="keyword">new</span> Random();</span><br><span class="line">        List&lt;Hero&gt; heros = <span class="keyword">new</span> ArrayList&lt;Hero&gt;();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">            heros.add(<span class="keyword">new</span> Hero(<span class="string">"hero "</span> + i, r.nextInt(<span class="number">1000</span>), r.nextInt(<span class="number">100</span>)));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//制造一个重复数据</span></span><br><span class="line">        heros.add(heros.get(<span class="number">0</span>));</span><br><span class="line">        System.out.println(<span class="string">"初始化集合后的数据 (最后一个数据重复)："</span>);</span><br><span class="line">        System.out.println(heros);</span><br><span class="line">        System.out.println(<span class="string">"满足条件hp&gt;100&amp;&amp;damage&lt;50的数据"</span>);</span><br><span class="line">          </span><br><span class="line">        heros</span><br><span class="line">            .stream()</span><br><span class="line">            .filter(h-&gt;h.hp&gt;<span class="number">100</span>&amp;&amp;h.damage&lt;<span class="number">50</span>)</span><br><span class="line">            .forEach(h-&gt;System.out.print(h));</span><br><span class="line">          </span><br><span class="line">        System.out.println(<span class="string">"去除重复的数据，去除标准是看equals"</span>);</span><br><span class="line">        heros</span><br><span class="line">            .stream()</span><br><span class="line">            .distinct()</span><br><span class="line">            .forEach(h-&gt;System.out.print(h));</span><br><span class="line">        System.out.println(<span class="string">"按照血量排序"</span>);</span><br><span class="line">        heros</span><br><span class="line">            .stream()</span><br><span class="line">            .sorted((h1,h2)-&gt;h1.hp&gt;=h2.hp?<span class="number">1</span>:-<span class="number">1</span>)</span><br><span class="line">            .forEach(h-&gt;System.out.print(h));</span><br><span class="line">          </span><br><span class="line">        System.out.println(<span class="string">"保留3个"</span>);</span><br><span class="line">        heros</span><br><span class="line">            .stream()</span><br><span class="line">            .limit(<span class="number">3</span>)</span><br><span class="line">            .forEach(h-&gt;System.out.print(h));</span><br><span class="line">          </span><br><span class="line">        System.out.println(<span class="string">"忽略前3个"</span>);</span><br><span class="line">        heros</span><br><span class="line">            .stream()</span><br><span class="line">            .skip(<span class="number">3</span>)</span><br><span class="line">            .forEach(h-&gt;System.out.print(h));</span><br><span class="line">          </span><br><span class="line">        System.out.println(<span class="string">"转换为double的Stream"</span>);</span><br><span class="line">        heros</span><br><span class="line">            .stream()</span><br><span class="line">            .mapToDouble(Hero::getHp)</span><br><span class="line">            .forEach(h-&gt;System.out.println(h));</span><br><span class="line">          </span><br><span class="line">        System.out.println(<span class="string">"转换任意类型的Stream"</span>);</span><br><span class="line">        heros</span><br><span class="line">            .stream()</span><br><span class="line">            .map((h)-&gt; h.name + <span class="string">" - "</span> + h.hp + <span class="string">" - "</span> + h.damage)</span><br><span class="line">            .forEach(h-&gt;System.out.println(h));</span><br><span class="line">          </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中Hero类的代码如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> charactor;</span><br><span class="line">      </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Hero</span> <span class="keyword">implements</span> <span class="title">Comparable</span>&lt;<span class="title">Hero</span>&gt;</span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">float</span> hp;</span><br><span class="line">         </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> damage;</span><br><span class="line">         </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Hero</span><span class="params">()</span></span>&#123;</span><br><span class="line">            </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">float</span> <span class="title">getHp</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> hp;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setHp</span><span class="params">(<span class="keyword">float</span> hp)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.hp = hp;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getDamage</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> damage;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setDamage</span><span class="params">(<span class="keyword">int</span> damage)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.damage = damage;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Hero</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name =name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//初始化name,hp,damage的构造方法</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Hero</span><span class="params">(String name,<span class="keyword">float</span> hp, <span class="keyword">int</span> damage)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name =name;</span><br><span class="line">        <span class="keyword">this</span>.hp = hp;</span><br><span class="line">        <span class="keyword">this</span>.damage = damage;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(Hero anotherHero)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(damage&lt;anotherHero.damage)</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>; </span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Hero [name="</span> + name + <span class="string">", hp="</span> + hp + <span class="string">", damage="</span> + damage + <span class="string">"]\r\n"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">        </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="结束操作"><a href="#结束操作" class="headerlink" title="结束操作"></a>结束操作</h1><p>结束操作才真正进行遍历行为，前面的中间操作也在这个时候，才真正的执行。 常见结束操作如下：</p>
<p>forEach() 遍历每个元素</p>
<p>toArray() 转换为数组</p>
<p>min(Comparator<T>) 取最小的元素</T></p>
<p>max(Comparator<T>) 取最大的元素</T></p>
<p>count() 总数</p>
<p>findFirst() 第一个元素</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lambda;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="keyword">import</span> java.util.Random;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> org.omg.Messaging.SYNC_WITH_TRANSPORT;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestAggregate</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Random r = <span class="keyword">new</span> Random();</span><br><span class="line">        List&lt;Hero&gt; heros = <span class="keyword">new</span> ArrayList&lt;Hero&gt;();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">            heros.add(<span class="keyword">new</span> Hero(<span class="string">"hero "</span> + i, r.nextInt(<span class="number">1000</span>), r.nextInt(<span class="number">100</span>)));</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"遍历集合中的每个数据"</span>);</span><br><span class="line">        heros</span><br><span class="line">            .stream()</span><br><span class="line">            .forEach(h-&gt;System.out.print(h));</span><br><span class="line">        System.out.println(<span class="string">"返回一个数组"</span>);</span><br><span class="line">        Object[] hs= heros</span><br><span class="line">            .stream()</span><br><span class="line">            .toArray();</span><br><span class="line">        System.out.println(Arrays.toString(hs));</span><br><span class="line">        System.out.println(<span class="string">"返回伤害最低的那个英雄"</span>);</span><br><span class="line">        Hero minDamageHero =</span><br><span class="line">        heros</span><br><span class="line">            .stream()</span><br><span class="line">            .min((h1,h2)-&gt;h1.damage-h2.damage)</span><br><span class="line">            .get();</span><br><span class="line">        System.out.print(minDamageHero);</span><br><span class="line">        System.out.println(<span class="string">"返回伤害最高的那个英雄"</span>);</span><br><span class="line"> </span><br><span class="line">        Hero mxnDamageHero =</span><br><span class="line">                heros</span><br><span class="line">                .stream()</span><br><span class="line">                .max((h1,h2)-&gt;h1.damage-h2.damage)</span><br><span class="line">                .get();</span><br><span class="line">        System.out.print(mxnDamageHero);     </span><br><span class="line">         </span><br><span class="line">        System.out.println(<span class="string">"流中数据的总数"</span>);</span><br><span class="line">        <span class="keyword">long</span> count = heros</span><br><span class="line">                .stream()</span><br><span class="line">                .count();</span><br><span class="line">        System.out.println(count);</span><br><span class="line"> </span><br><span class="line">        System.out.println(<span class="string">"第一个英雄"</span>);</span><br><span class="line">        Hero firstHero =</span><br><span class="line">                heros</span><br><span class="line">                .stream()</span><br><span class="line">                .findFirst()</span><br><span class="line">                .get();</span><br><span class="line">         </span><br><span class="line">        System.out.println(firstHero);</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatisPlus</title>
    <url>/2020/07/17/MyBatisPlus/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>MySQL(1)</title>
    <url>/2020/06/12/MySQL-1/</url>
    <content><![CDATA[<p>MySQL使用.</p>
<a id="more"></a>

<h1 id="初步使用MySQL"><a href="#初步使用MySQL" class="headerlink" title="初步使用MySQL"></a>初步使用MySQL</h1><p>登录：<code>mysql -h localhost -P 3306 -u root -p</code></p>
<p>显示数据库列表：<code>show databases;</code></p>
<p>选择数据库：<code>use sakila;</code></p>
<p>显示表列表：<code>show tables;</code></p>
<p>合并起来写：<code>show tables from world;</code>，此时还是在sakila库中</p>
<p>显示现在在哪个库：<code>select database();</code></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> testtable(</span><br><span class="line">    -&gt; <span class="keyword">id</span> <span class="built_in">int</span>,</span><br><span class="line">    -&gt; <span class="keyword">name</span> <span class="built_in">varchar</span>(<span class="number">20</span>));</span><br></pre></td></tr></table></figure>

<p>创建一张表。</p>
<p><code>desc testtable;</code>查看表结构。</p>
<p><code>insert into testtable (id,name) values(1,&quot;aa&quot;);</code>插入一条数据</p>
<p><code>update testtable set name=&quot;cc&quot; where id=1;</code>修改一条数据</p>
<p><code>delete from testtable where id=1;</code>删除一条数据</p>
<p><code>select version();</code>查看MySQL版本</p>
<blockquote>
<p>建议关键字大写，其它小写；命令以分号结尾；<br>注释：单行注释：“#注释”或者“– 注释”<br>      多行注释：/*注释*/</p>
</blockquote>
<h1 id="DQL语言"><a href="#DQL语言" class="headerlink" title="DQL语言"></a>DQL语言</h1><p>第一部分：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 基础查询</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 语法：</span></span><br><span class="line"><span class="comment"> select 查询列表</span></span><br><span class="line"><span class="comment"> from 表明;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> 查询列表可以是：字段，常量值，表达式，函数</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#查询单个字段</span></span><br><span class="line"><span class="keyword">select</span> last_name</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="comment">#查多个字段</span></span><br><span class="line"><span class="keyword">select</span> last_name, salary, email</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="comment">#查所有字段</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="comment">#当字段名和关键字重复时，给字段加上''</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#查询常量值</span></span><br><span class="line"><span class="keyword">select</span> <span class="number">100</span>;</span><br><span class="line"><span class="keyword">select</span> <span class="string">'aa'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#查询表达式</span></span><br><span class="line"><span class="keyword">select</span> <span class="number">5</span> * <span class="number">8</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#查询函数</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">version</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">#起别名</span></span><br><span class="line"><span class="comment">#方式一</span></span><br><span class="line"><span class="keyword">select</span> <span class="number">100</span> % <span class="number">98</span> <span class="keyword">as</span> 结果;</span><br><span class="line"><span class="keyword">select</span> last_name <span class="keyword">as</span> 姓, first_name <span class="keyword">as</span> 名</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="comment">#方式二</span></span><br><span class="line"><span class="keyword">select</span> last_name 姓, first_name 名</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> salary <span class="keyword">as</span> <span class="string">"out put"</span></span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="comment">#去重</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">distinct</span> department_id</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="comment">#加号</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> MySQL 中的加号只有运算功能，若是有字符，则会试着将字符转为数值</span></span><br><span class="line"><span class="comment"> ‘123’转换成功，则继续加法</span></span><br><span class="line"><span class="comment"> ‘aa’转换失败，则将字符转换成0</span></span><br><span class="line"><span class="comment"> 如果有一方是null，则结果是null</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">#将员工名和姓拼在一起，显示为姓名</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">concat</span>(last_name, first_name) <span class="keyword">as</span> 姓名</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 将奖金率字段和其它字段拼接，因为奖金率字段有部分为null,导致部分结果为null</span></span><br><span class="line"><span class="comment"> 可以使用ifnull函数过滤</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">ifnull</span>(commission_pct, <span class="number">0</span>) <span class="keyword">as</span> 奖金率</span><br><span class="line"><span class="keyword">from</span> employees;<span class="comment">#null的地方换成0</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">concat</span>(last_name, <span class="string">','</span>, first_name, <span class="string">','</span>, <span class="keyword">ifnull</span>(commission_pct, <span class="number">0</span>)) <span class="keyword">as</span> <span class="keyword">result</span></span><br><span class="line"><span class="keyword">from</span> employees;</span><br></pre></td></tr></table></figure>

<p>第二部分：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">#条件查询</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> select③</span></span><br><span class="line"><span class="comment">    查询列表</span></span><br><span class="line"><span class="comment"> from①</span></span><br><span class="line"><span class="comment">    表名</span></span><br><span class="line"><span class="comment"> where②</span></span><br><span class="line"><span class="comment">    筛选条件</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> 分类：</span></span><br><span class="line"><span class="comment"> 一、按条件表达式筛选：条件运算符 &gt;&lt;=,不等于是&lt;&gt;，!=;</span></span><br><span class="line"><span class="comment"> 二、按逻辑表达式筛选：&amp;&amp; || !或者and or not;</span></span><br><span class="line"><span class="comment"> 三、模糊查询：like,between and,in,is null;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 按条件表达式筛选</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> salary &gt; <span class="number">12000</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, department_id</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> department_id &lt;&gt; <span class="number">90</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#按逻辑表达式筛选</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> salary &lt;= <span class="number">12000</span></span><br><span class="line">  <span class="keyword">and</span> salary &gt;= <span class="number">5000</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#模糊查询</span></span><br><span class="line"><span class="comment">#和通配符搭配使用</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> last_name <span class="keyword">like</span> <span class="string">'%a%'</span>;<span class="comment">#%表示任意多个字符，包括0个</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, salary</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> last_name <span class="keyword">like</span> <span class="string">'__e_a%'</span>;<span class="comment">#_表示一个字符</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, salary</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> last_name <span class="keyword">like</span> <span class="string">'_\_%'</span>;<span class="comment">#\表示转义</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, salary</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> last_name <span class="keyword">like</span> <span class="string">'_$_%'</span> escape <span class="string">'$'</span>;<span class="comment">#用escape指定转义符号</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> employee_id <span class="keyword">between</span> <span class="number">100</span> <span class="keyword">and</span> <span class="number">120</span>;<span class="comment">#包含临界值,不能颠倒</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, job_id</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="comment">#in列表不能用通配符,因为in等价于=,而不是like</span></span><br><span class="line"><span class="keyword">where</span> job_id <span class="keyword">in</span> (<span class="string">'IT_PROG'</span>, <span class="string">'AD_VP'</span>, <span class="string">'AD_PRES'</span>);<span class="comment">#判断字段中的值是否是某一项,in列表的值必须一致或兼容</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, commission_pct</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> commission_pct <span class="keyword">is</span> <span class="literal">null</span>;</span><br><span class="line"><span class="comment">#不能用等于号来判断null</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#安全等于</span></span><br><span class="line"><span class="keyword">select</span> last_name, commission_pct</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> commission_pct &lt;=&gt; <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, salary</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> salary &lt;=&gt; <span class="number">12000</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#注意点</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees <span class="keyword">where</span> commission_pct <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">null</span> ;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> commission_pct <span class="keyword">like</span> <span class="string">'%'</span>;<span class="comment">#通配符不能表示null</span></span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 排序查询</span></span><br><span class="line"><span class="comment"> select 查询列表 from 表 [where 筛选条件] order by 排序条件 [asc|desc]</span></span><br><span class="line"><span class="comment"> order by子句一般放在最后面,limit子句除外,排序条件可以多个字段,表达式,函数,别名</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> salary <span class="keyword">desc</span>;<span class="comment">#desc降序,asc升序,默认升序</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> department_id &gt;= <span class="number">90</span></span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> hiredate;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> *, salary * <span class="number">12</span> * (<span class="number">1</span> + <span class="keyword">ifnull</span>(commission_pct, <span class="number">0</span>)) <span class="keyword">as</span> 年薪</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> 年薪 <span class="keyword">desc</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">length</span>(last_name) <span class="keyword">as</span> 姓名长度, last_name, salary</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> 姓名长度;</span><br><span class="line"></span><br><span class="line"><span class="comment">#多个字段排序</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> salary, employee_id <span class="keyword">desc</span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 常见函数</span></span><br><span class="line"><span class="comment"> select 函数(实参列表)</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> 单行函数</span></span><br><span class="line"><span class="comment"> concat,length,ifnull</span></span><br><span class="line"><span class="comment"> 分组函数(聚合函数)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#一,字符函数</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">length</span>(<span class="string">'john'</span>);</span><br><span class="line"><span class="keyword">select</span> <span class="keyword">length</span>(<span class="string">'张三丰'</span>);<span class="comment">#utf8字符集汉字占三个字节</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">variables</span> <span class="keyword">like</span> <span class="string">'%char%'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">concat</span>(last_name, <span class="string">'_'</span>, first_name)</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">concat</span>(<span class="keyword">upper</span>(last_name), <span class="keyword">lower</span>(first_name)) 姓名</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">substr</span>(<span class="string">'李莫愁爱上陆湛远'</span>, <span class="number">6</span>);<span class="comment">#第二个参数标明开始截取位置,索引从1开始</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">substr</span>(<span class="string">'李莫愁爱上陆湛远'</span>, <span class="number">1</span>,<span class="number">3</span>);<span class="comment">#第三个参数是截取长度,不是截取结束位置</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">instr</span>(<span class="string">'李莫愁爱上陆湛远'</span>,<span class="string">'爱上'</span>) <span class="keyword">as</span> <span class="string">'out'</span>;<span class="comment">#返回字串第一次出现的索引,如果找不到返回0</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">trim</span>(<span class="string">'  张  翠山  '</span>) <span class="keyword">as</span> <span class="string">'out'</span>;<span class="comment">#去除前后的空格</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">trim</span>(<span class="string">'a'</span> <span class="keyword">from</span> <span class="string">'aaa张aa三  丰aaaa'</span>);<span class="comment">#去除前后的a</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">trim</span>(<span class="string">'aa'</span> <span class="keyword">from</span> <span class="string">'aaa张aa三  丰aaaa'</span>);<span class="comment">#前面第三个a没有匹配到</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">lpad</span>(<span class="string">'殷素素'</span>,<span class="number">10</span>,<span class="string">'*'</span>) <span class="keyword">as</span> <span class="string">'out'</span>;<span class="comment">#用指定字符填充指定长度</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">lpad</span>(<span class="string">'hello'</span>,<span class="number">3</span>,<span class="string">'*'</span>) <span class="keyword">as</span> <span class="string">'out'</span>;<span class="comment">#超出个数会截断</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> rpad(<span class="string">'ad'</span>,<span class="number">12</span>,<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">replace</span>(<span class="string">'hello world world'</span>,<span class="string">'world'</span>,<span class="string">'sql'</span>) <span class="keyword">as</span> <span class="keyword">output</span>;<span class="comment">#替换所有匹配字符</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">#二,数学函数</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">round</span>(<span class="number">1.65</span>);<span class="comment">#四舍五入</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">round</span>(<span class="number">1.658</span>, <span class="number">2</span>);<span class="comment">#保留两位小数</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">ceil</span>(<span class="number">2.6</span>);<span class="comment">#向上取整</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">floor</span>(<span class="number">3.5</span>);<span class="comment">#向下取整</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">truncate</span>(<span class="number">1.6555</span>, <span class="number">2</span>);<span class="comment">#截断</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">mod</span>(<span class="number">10</span>, <span class="number">3</span>);<span class="comment">#取余</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">rand</span>();<span class="comment">#获取0到1之间的随机数</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#三,日期函数</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">now</span>();<span class="comment">#日期加实践</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">curdate</span>();<span class="comment">#日期</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">curtime</span>();<span class="comment">#时间</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">year</span>(<span class="keyword">now</span>()) 年;<span class="comment">#获取指定时间的部分</span></span><br><span class="line"><span class="keyword">select</span> monthname(<span class="keyword">now</span>()) 月;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">str_to_date</span>(<span class="string">'9-13-2020'</span>, <span class="string">'%m-%d-%y'</span>);<span class="comment">#y代表两位的年,Y代表四位的</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> hiredate = <span class="keyword">str_to_date</span>(<span class="string">'4-3 1992'</span>, <span class="string">'%c-%d %Y'</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">date_format</span>(<span class="keyword">now</span>(), <span class="string">'%y年%m月%d日'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">#四,其它函数</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">database</span>();</span><br><span class="line"><span class="keyword">select</span> <span class="keyword">USER</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">#五,流程控制函数</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">if</span>(<span class="number">10</span> &gt; <span class="number">5</span>, <span class="string">'大'</span>, <span class="string">'小'</span>);<span class="comment">#类似于三元运算符</span></span><br><span class="line"><span class="keyword">select</span> last_name, <span class="keyword">ifnull</span>(commission_pct, <span class="number">0</span>), <span class="keyword">if</span>(commission_pct <span class="keyword">is</span> <span class="literal">null</span>, <span class="string">'无'</span>, <span class="string">'有'</span>) <span class="keyword">as</span> 备注</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> case语句:</span></span><br><span class="line"><span class="comment"> case 要判断的表达式</span></span><br><span class="line"><span class="comment"> when 常量值 then 语句或者值;(当是值时,不加分号)</span></span><br><span class="line"><span class="comment"> ...</span></span><br><span class="line"><span class="comment"> else 默认语句</span></span><br><span class="line"><span class="comment"> end</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">select</span> salary  <span class="keyword">as</span> 原始工资,</span><br><span class="line">       department_id,</span><br><span class="line">       <span class="keyword">case</span> department_id</span><br><span class="line">           <span class="keyword">when</span> <span class="number">30</span> <span class="keyword">then</span> salary * <span class="number">1.1</span></span><br><span class="line">           <span class="keyword">when</span> <span class="number">40</span> <span class="keyword">then</span> salary * <span class="number">1.2</span></span><br><span class="line">           <span class="keyword">when</span> <span class="number">50</span> <span class="keyword">then</span> salary * <span class="number">1.3</span></span><br><span class="line">           <span class="keyword">else</span> salary</span><br><span class="line">           <span class="keyword">end</span> <span class="keyword">as</span> 新工资</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> case还可以做多重if判断</span></span><br><span class="line"><span class="comment"> case</span></span><br><span class="line"><span class="comment"> when 条件 then 语句或者值;(当是值时,不加分号)</span></span><br><span class="line"><span class="comment"> ...</span></span><br><span class="line"><span class="comment"> else 默认语句</span></span><br><span class="line"><span class="comment"> end</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">select</span> salary,</span><br><span class="line">       <span class="keyword">case</span></span><br><span class="line">           <span class="keyword">when</span> salary &gt; <span class="number">20000</span> <span class="keyword">then</span> <span class="string">'A'</span></span><br><span class="line">           <span class="keyword">when</span> salary &gt; <span class="number">15000</span> <span class="keyword">then</span> <span class="string">'B'</span></span><br><span class="line">           <span class="keyword">when</span> salary &gt; <span class="number">10000</span> <span class="keyword">then</span> <span class="string">'C'</span></span><br><span class="line">           <span class="keyword">else</span> <span class="string">'D'</span></span><br><span class="line">           <span class="keyword">end</span> <span class="keyword">as</span> 工资级别</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="comment">#分组函数</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">sum</span>(salary)<span class="comment">#忽略null</span></span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="keyword">select</span> <span class="keyword">avg</span>(salary)<span class="comment">#忽略null</span></span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="keyword">select</span> <span class="keyword">min</span>(salary)</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="keyword">select</span> <span class="keyword">max</span>(salary)</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="keyword">select</span> <span class="keyword">count</span>(salary)<span class="comment">#计算非空的个数</span></span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="comment">#和distinct搭配</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">sum</span>(<span class="keyword">distinct</span> salary), <span class="keyword">sum</span>(salary)</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">COUNT</span>(*)</span><br><span class="line"><span class="keyword">from</span> employees;<span class="comment">#统计行数</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">COUNT</span>(<span class="number">1</span>)</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="comment">#相当于加了一列1,统计1的个数</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 和分组函数一起查询的字段必须是group by后的字段</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">datediff</span>(<span class="keyword">max</span>(hiredate), <span class="keyword">min</span>(hiredate))</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">count</span>(*)</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> department_id = <span class="number">90</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#分组查询</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> select 分组函数,列(要求出现在group by的后面)</span></span><br><span class="line"><span class="comment"> from 表</span></span><br><span class="line"><span class="comment"> group by 分组的列表</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">max</span>(salary), job_id</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> job_id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">COUNT</span>(*), location_id</span><br><span class="line"><span class="keyword">from</span> departments</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> location_id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">avg</span>(salary), department_id</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> email <span class="keyword">like</span> <span class="string">'%a%'</span></span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> department_id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">max</span>(salary), manager_id</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> commission_pct <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">null</span></span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> manager_id;</span><br><span class="line"></span><br><span class="line"><span class="comment">#分组后的筛选</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">count</span>(*), department_id</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> department_id</span><br><span class="line"><span class="keyword">having</span> <span class="keyword">count</span>(*) &gt; <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">max</span>(salary), job_id</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> commission_pct <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">null</span></span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> job_id</span><br><span class="line"><span class="keyword">having</span> <span class="keyword">max</span>(salary) &gt; <span class="number">12000</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">min</span>(salary), manager_id</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> manager_id &gt; <span class="number">102</span></span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> manager_id</span><br><span class="line"><span class="keyword">having</span> <span class="keyword">min</span>(salary) &gt; <span class="number">5000</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#按表达式或函数分组</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">COUNT</span>(*) <span class="keyword">as</span> c</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> <span class="keyword">length</span>(last_name)</span><br><span class="line"><span class="keyword">having</span> c &gt; <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#按多个字段分组</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">avg</span>(salary), department_id, job_id</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> department_id <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">null</span></span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> department_id, job_id</span><br><span class="line"><span class="keyword">having</span> <span class="keyword">avg</span>(salary) &gt; <span class="number">10000</span></span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> department_id;</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> girls;</span><br><span class="line"><span class="comment">#连接查询(多表查询)(注意笛卡尔积)</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">name</span>, boyName</span><br><span class="line"><span class="keyword">from</span> boys,</span><br><span class="line">     beauty</span><br><span class="line"><span class="keyword">where</span> beauty.boyfriend_id = boys.id;</span><br><span class="line"></span><br><span class="line"><span class="comment">#一,SQL92标准</span></span><br><span class="line"><span class="comment">#1,等值连接</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 多表等值连接的结果为多表的交集部分</span></span><br><span class="line"><span class="comment">  n表连接至少要n-1个连接条件</span></span><br><span class="line"><span class="comment">  多表的顺序没有要求</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">name</span>, boyName</span><br><span class="line"><span class="keyword">from</span> boys,</span><br><span class="line">     beauty</span><br><span class="line"><span class="keyword">where</span> beauty.boyfriend_id = boys.id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> myemployees;</span><br><span class="line"><span class="keyword">select</span> last_name, department_name</span><br><span class="line"><span class="keyword">from</span> employees,</span><br><span class="line">     departments</span><br><span class="line"><span class="keyword">where</span> employees.department_id = departments.department_id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, e.job_id, job_title</span><br><span class="line"><span class="keyword">from</span> employees <span class="keyword">as</span> e,</span><br><span class="line">     jobs <span class="keyword">as</span> j</span><br><span class="line"><span class="keyword">where</span> e.job_id = j.job_id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, department_name</span><br><span class="line"><span class="keyword">from</span> employees,</span><br><span class="line">     departments</span><br><span class="line"><span class="keyword">where</span> employees.department_id = departments.department_id</span><br><span class="line">  <span class="keyword">and</span> employees.commission_pct <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">null</span>;<span class="comment">#添加筛选</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> department_name, city</span><br><span class="line"><span class="keyword">from</span> departments d,</span><br><span class="line">     locations l</span><br><span class="line"><span class="keyword">where</span> d.location_id = l.location_id</span><br><span class="line">  <span class="keyword">and</span> city <span class="keyword">like</span> <span class="string">'_o%'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> city, <span class="keyword">count</span>(*) <span class="keyword">as</span> 个数</span><br><span class="line"><span class="keyword">from</span> locations l,</span><br><span class="line">     departments d</span><br><span class="line"><span class="keyword">where</span> d.location_id = l.location_id</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> city;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> department_name, d.manager_id, <span class="keyword">min</span>(salary)</span><br><span class="line"><span class="keyword">from</span> departments d,</span><br><span class="line">     employees e</span><br><span class="line"><span class="keyword">where</span> d.department_id = e.department_id</span><br><span class="line">  <span class="keyword">and</span> commission_pct <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">null</span></span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> department_name, d.manager_id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> job_title, <span class="keyword">count</span>(*)</span><br><span class="line"><span class="keyword">from</span> employees e,</span><br><span class="line">     jobs j</span><br><span class="line"><span class="keyword">where</span> j.job_id = e.job_id</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> job_title</span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> <span class="keyword">count</span>(*) <span class="keyword">desc</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, department_name, city</span><br><span class="line"><span class="keyword">from</span> employees e,</span><br><span class="line">     departments d,</span><br><span class="line">     locations l</span><br><span class="line"><span class="keyword">where</span> e.department_id = d.department_id</span><br><span class="line">  <span class="keyword">and</span> d.location_id = l.location_id;</span><br><span class="line"></span><br><span class="line"><span class="comment">#2,非等值连接</span></span><br><span class="line"><span class="comment">#查询员工的工资和工资等级</span></span><br><span class="line"><span class="keyword">select</span> salary, grade_level</span><br><span class="line"><span class="keyword">from</span> employees e,</span><br><span class="line">     job_grades g</span><br><span class="line"><span class="keyword">where</span> salary <span class="keyword">between</span> lowest_sal <span class="keyword">and</span> highest_sal;</span><br><span class="line"></span><br><span class="line"><span class="comment">#3,自连接(查员工的领导名字)</span></span><br><span class="line"><span class="keyword">select</span> e.employee_id, e.last_name, m.manager_id, m.last_name</span><br><span class="line"><span class="keyword">from</span> employees e,</span><br><span class="line">     employees m</span><br><span class="line"><span class="keyword">where</span> e.manager_id = m.employee_id;</span><br><span class="line"></span><br><span class="line"><span class="comment">#二,SQL99标准</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> select 查询列表</span></span><br><span class="line"><span class="comment"> from 表一 别名 [连接类型]</span></span><br><span class="line"><span class="comment"> join 表二 别名</span></span><br><span class="line"><span class="comment"> on 连接条件</span></span><br><span class="line"><span class="comment"> [where 筛选条件]</span></span><br><span class="line"><span class="comment"> 内连接:inner</span></span><br><span class="line"><span class="comment"> 外连接;</span></span><br><span class="line"><span class="comment">    左外:left [outer]</span></span><br><span class="line"><span class="comment">    右外:right [outer]</span></span><br><span class="line"><span class="comment">    全外:full [outer]</span></span><br><span class="line"><span class="comment"> 交叉连接:cross</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#1,内连接</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 内连接又分为等值连接,非等值连接,自连接,内连接是查表的交集</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">#等值连接</span></span><br><span class="line"><span class="keyword">select</span> last_name, department_name</span><br><span class="line"><span class="keyword">from</span> employees e</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> departments d <span class="keyword">on</span> e.department_id = d.department_id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, job_title</span><br><span class="line"><span class="keyword">from</span> employees e</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> jobs j <span class="keyword">on</span> e.job_id = j.job_id</span><br><span class="line"><span class="keyword">where</span> last_name <span class="keyword">like</span> <span class="string">'%e%'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> city, <span class="keyword">count</span>(*)</span><br><span class="line"><span class="keyword">from</span> departments d</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> locations l <span class="keyword">on</span> d.location_id = l.location_id</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> city</span><br><span class="line"><span class="keyword">having</span> <span class="keyword">count</span>(*) &gt; <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">count</span>(*), department_name</span><br><span class="line"><span class="keyword">from</span> departments</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> employees e <span class="keyword">on</span> departments.department_id = e.department_id</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> department_name</span><br><span class="line"><span class="keyword">having</span> <span class="keyword">count</span>(*) &gt; <span class="number">3</span></span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> <span class="keyword">count</span>(*) <span class="keyword">desc</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> last_name, department_name, job_title</span><br><span class="line"><span class="keyword">from</span> employees e</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> departments d <span class="keyword">on</span> e.department_id = d.department_id</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> jobs j <span class="keyword">on</span> e.job_id = j.job_id</span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> department_name <span class="keyword">desc</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#非等值连接</span></span><br><span class="line"><span class="keyword">select</span> salary, grade_level</span><br><span class="line"><span class="keyword">from</span> employees e</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> job_grades g</span><br><span class="line">                    <span class="keyword">on</span> e.salary <span class="keyword">between</span> g.lowest_sal <span class="keyword">and</span> highest_sal;</span><br><span class="line"></span><br><span class="line"><span class="comment">#自连接</span></span><br><span class="line"><span class="keyword">select</span> e.last_name, m.last_name</span><br><span class="line"><span class="keyword">from</span> employees e</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> employees m</span><br><span class="line">                    <span class="keyword">on</span> e.manager_id = m.employee_id</span><br><span class="line"><span class="keyword">where</span> e.last_name <span class="keyword">like</span> <span class="string">'%k%'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#二,外连接,查一个表有,另一个表没有的数据</span></span><br><span class="line"><span class="comment">#外连接查询的结果为主表中的所有记录,如果没有匹配的,就像是null</span></span><br><span class="line"><span class="comment">#外连接结果=内连接+主表有从表没有的记录</span></span><br><span class="line"><span class="comment">#左外连接,left join左边的是主表</span></span><br><span class="line"><span class="comment">#右外连接,right join右边的是主表</span></span><br><span class="line"><span class="comment">#左外和右外交换两表顺序效果一样</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#查询没有男朋友的女生</span></span><br><span class="line"><span class="keyword">use</span> girls;</span><br><span class="line"><span class="keyword">select</span> b.name, bo.*</span><br><span class="line"><span class="keyword">from</span> beauty b</span><br><span class="line">         <span class="keyword">left</span> <span class="keyword">join</span> boys bo</span><br><span class="line">                   <span class="keyword">on</span> boyfriend_id = bo.id</span><br><span class="line"><span class="keyword">where</span> bo.id <span class="keyword">is</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> b.*, bo.*</span><br><span class="line"><span class="keyword">from</span> boys bo</span><br><span class="line">         <span class="keyword">left</span> <span class="keyword">join</span> beauty b</span><br><span class="line">                   <span class="keyword">on</span> b.boyfriend_id = bo.id</span><br><span class="line"><span class="keyword">where</span> bo.id <span class="keyword">is</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> myemployees;</span><br><span class="line"><span class="comment">#查询哪个部门没有员工</span></span><br><span class="line"><span class="keyword">select</span> d.*, e.employee_id</span><br><span class="line"><span class="keyword">from</span> departments d</span><br><span class="line">         <span class="keyword">left</span> <span class="keyword">join</span> employees e <span class="keyword">on</span> d.department_id = e.department_id</span><br><span class="line"><span class="keyword">where</span> e.employee_id <span class="keyword">is</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#交叉连接</span></span><br><span class="line"><span class="keyword">select</span> b.*, bo.*</span><br><span class="line"><span class="keyword">from</span> beauty b</span><br><span class="line">         <span class="keyword">cross</span> <span class="keyword">join</span> boys bo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> b.id, b.name, bo.*</span><br><span class="line"><span class="keyword">from</span> beauty b</span><br><span class="line">         <span class="keyword">left</span> <span class="keyword">join</span> boys bo</span><br><span class="line">                   <span class="keyword">on</span> b.boyfriend_id = bo.id</span><br><span class="line"><span class="keyword">where</span> b.id &gt; <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> myemployees;</span><br><span class="line"><span class="keyword">select</span> city, departments.*</span><br><span class="line"><span class="keyword">from</span> locations</span><br><span class="line">         <span class="keyword">left</span> <span class="keyword">join</span> departments</span><br><span class="line">                   <span class="keyword">on</span> locations.location_id = departments.location_id</span><br><span class="line"><span class="keyword">where</span> departments.location_id <span class="keyword">is</span> <span class="literal">null</span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">#子查询</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 出现在其它语句中的select语句,称为子查询或内查询</span></span><br><span class="line"><span class="comment"> 外部的查询称为主查询</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> 分类:</span></span><br><span class="line"><span class="comment">    select后面:仅支持标量子查询</span></span><br><span class="line"><span class="comment">    from后面:表子查询</span></span><br><span class="line"><span class="comment">    where和having后面:标量子查询⭐,列子查询⭐,行子查询</span></span><br><span class="line"><span class="comment">    exists后面(相关子查询):表子查询</span></span><br><span class="line"><span class="comment"> 按结果集的行列数:</span></span><br><span class="line"><span class="comment">    标量子查询(结果集只有一行一列)</span></span><br><span class="line"><span class="comment">    列子查询(结果集只有一列多行)</span></span><br><span class="line"><span class="comment">    行子查询(结果集只有一行多列)</span></span><br><span class="line"><span class="comment">    表子查询(结果集一般为多行多列)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#一,where和having后面</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 子查询放在小括号内</span></span><br><span class="line"><span class="comment"> 子查询一般放在条件右侧</span></span><br><span class="line"><span class="comment"> 标量子查询一般搭配单行操作符使用</span></span><br><span class="line"><span class="comment"> 列子查询一般搭配多行操作 in,all,any/some</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">#标量子查询</span></span><br><span class="line"><span class="comment">#查谁的工资表Abel高</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> salary &gt; (<span class="keyword">select</span> salary</span><br><span class="line">                <span class="keyword">from</span> employees</span><br><span class="line">                <span class="keyword">where</span> last_name = <span class="string">'Abel'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">#查询job_id与141号员工相同,salary比143号员工多的员工</span></span><br><span class="line"><span class="keyword">select</span> last_name, job_id, salary</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> job_id = (<span class="keyword">select</span> job_id <span class="keyword">from</span> employees <span class="keyword">where</span> employee_id = <span class="number">141</span>)</span><br><span class="line">  <span class="keyword">and</span> salary &gt; (<span class="keyword">select</span> salary <span class="keyword">from</span> employees <span class="keyword">where</span> employee_id = <span class="number">143</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">#列子查询(多行子查询)</span></span><br><span class="line"><span class="comment">#查location_id是14000或1700的部门中的所有员工姓名</span></span><br><span class="line"><span class="keyword">select</span> last_name</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> department_id <span class="keyword">in</span> (</span><br><span class="line">    <span class="keyword">select</span> <span class="keyword">distinct</span> department_id</span><br><span class="line">    <span class="keyword">from</span> departments</span><br><span class="line">    <span class="keyword">where</span> location_id <span class="keyword">in</span> (<span class="number">1400</span>, <span class="number">1700</span>)</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">#行子查询</span></span><br><span class="line"><span class="comment">#查询员工编号最小并且工资最高的员工信息</span></span><br><span class="line"><span class="comment">#写法一</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> employee_id = (<span class="keyword">select</span> <span class="keyword">min</span>(employee_id) <span class="keyword">from</span> employees)</span><br><span class="line">  <span class="keyword">and</span> salary = (<span class="keyword">select</span> <span class="keyword">max</span>(salary) <span class="keyword">from</span> employees);</span><br><span class="line"><span class="comment">#写法二</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> (employee_id, salary) = (<span class="keyword">select</span> <span class="keyword">min</span>(employee_id), <span class="keyword">max</span>(salary) <span class="keyword">from</span> employees);</span><br><span class="line"></span><br><span class="line"><span class="comment">#二,select后面</span></span><br><span class="line"><span class="comment">#查询每个部门的员工个数</span></span><br><span class="line"><span class="keyword">select</span> departments.*, (<span class="keyword">select</span> <span class="keyword">count</span>(*) <span class="keyword">from</span> employees e <span class="keyword">where</span> e.department_id = departments.department_id)</span><br><span class="line"><span class="keyword">from</span> departments;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> d.*, <span class="keyword">count</span>(*) <span class="keyword">as</span> 个数</span><br><span class="line"><span class="keyword">from</span> departments d</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> employees e <span class="keyword">on</span> d.department_id = e.department_id</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> d.department_id;</span><br><span class="line"></span><br><span class="line"><span class="comment">#查询员工号120的部门名</span></span><br><span class="line"><span class="keyword">select</span> (<span class="keyword">select</span> department_name</span><br><span class="line">        <span class="keyword">from</span> departments d</span><br><span class="line">                 <span class="keyword">inner</span> <span class="keyword">join</span> employees e <span class="keyword">on</span> d.department_id = e.department_id</span><br><span class="line">        <span class="keyword">where</span> e.employee_id = <span class="number">102</span>) 名称;</span><br><span class="line"></span><br><span class="line"><span class="comment">#三,from后面</span></span><br><span class="line"><span class="comment">#查询每个部门的平均工资的工资等级</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">avg</span>(salary), department_id</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> department_id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> ag_dep.*, grade_level</span><br><span class="line"><span class="keyword">from</span> (<span class="keyword">select</span> <span class="keyword">avg</span>(salary) ag, department_id</span><br><span class="line">      <span class="keyword">from</span> employees</span><br><span class="line">      <span class="keyword">group</span> <span class="keyword">by</span> department_id) ag_dep</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> job_grades g</span><br><span class="line">                    <span class="keyword">on</span> ag_dep.ag <span class="keyword">between</span> lowest_sal <span class="keyword">and</span> highest_sal;</span><br><span class="line"></span><br><span class="line"><span class="comment">#,四exists后面</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">exists</span>(<span class="keyword">select</span> employee_id <span class="keyword">from</span> employees);</span><br><span class="line"><span class="comment">#exists判断存不存在</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#查询有员工的部门名</span></span><br><span class="line"><span class="keyword">select</span> department_name</span><br><span class="line"><span class="keyword">from</span> departments d</span><br><span class="line"><span class="keyword">where</span> <span class="keyword">exists</span>(</span><br><span class="line">              <span class="keyword">select</span> *</span><br><span class="line">              <span class="keyword">from</span> employees e</span><br><span class="line">              <span class="keyword">where</span> d.department_id = e.department_id</span><br><span class="line">          );</span><br><span class="line"><span class="keyword">select</span> department_name</span><br><span class="line"><span class="keyword">from</span> departments</span><br><span class="line"><span class="keyword">where</span> department_id <span class="keyword">in</span> (<span class="keyword">select</span> department_id <span class="keyword">from</span> employees);</span><br><span class="line"></span><br><span class="line"><span class="comment">#分页查询</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> select ...</span></span><br><span class="line"><span class="comment"> ...</span></span><br><span class="line"><span class="comment"> limit offset(要显示条目的起始索引,从0开始),size(显示条目个数);</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">limit</span> <span class="number">0</span>,<span class="number">5</span>;</span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">limit</span> <span class="number">10</span>,<span class="number">15</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#union联合查询</span></span><br><span class="line"><span class="comment">#将多条查询语句的结果合并成一个</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#查询部门编号&gt;90或邮箱包含a的员工信息</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> email <span class="keyword">like</span> <span class="string">'%a%'</span></span><br><span class="line">   <span class="keyword">or</span> department_id &gt; <span class="number">90</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> email <span class="keyword">like</span> <span class="string">'%a%'</span></span><br><span class="line"><span class="keyword">union</span></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> department_id &gt; <span class="number">90</span>;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 要求联合查询的多条语句的查询列数是一致的</span></span><br><span class="line"><span class="comment"> 另外,语句间查询的字段要一直,类型,顺序</span></span><br><span class="line"><span class="comment"> union默认会去重,不想去重就在union后加一个all</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure>

<h1 id="DML语言"><a href="#DML语言" class="headerlink" title="DML语言"></a>DML语言</h1><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">#一,插入语言</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> insert into 表名(列名...) values(值...)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">insert</span> <span class="keyword">into</span> beauty(<span class="keyword">id</span>, <span class="keyword">name</span>, sex, borndate, phone, photo, boyfriend_id)</span><br><span class="line"><span class="keyword">values</span> (<span class="number">13</span>, <span class="string">'唐艺昕'</span>, <span class="string">'女'</span>, <span class="string">'1990-4-23'</span>, <span class="string">'1895200001'</span>, <span class="literal">null</span>, <span class="number">2</span>);</span><br><span class="line"><span class="comment">#上面的列名和下面的值对应,也可以省略列名,但是值的顺序要和表对应</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">insert</span> <span class="keyword">into</span> beauty</span><br><span class="line"><span class="keyword">set</span> <span class="keyword">id</span>=<span class="number">19</span>,</span><br><span class="line">    <span class="keyword">name</span>=<span class="string">'刘涛'</span>,</span><br><span class="line">    phone=<span class="string">'1596458623'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 第一种可以一次插入多行,中间用逗号隔开</span></span><br><span class="line"><span class="comment"> 第一种还可以加入子查询语句,将select结果作为值插入表</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">insert</span> <span class="keyword">into</span> beauty(<span class="keyword">id</span>, <span class="keyword">name</span>, phone)</span><br><span class="line"><span class="keyword">select</span> <span class="number">26</span>, <span class="string">'宋茜'</span>, <span class="string">'11582222222'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#二,修改语句</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> update 表名</span></span><br><span class="line"><span class="comment"> set 列=新值,列=新值...</span></span><br><span class="line"><span class="comment"> where 筛选条件</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">update</span> beauty</span><br><span class="line"><span class="keyword">set</span> phone=<span class="string">'12345678911'</span></span><br><span class="line"><span class="keyword">where</span> <span class="keyword">name</span> <span class="keyword">like</span> <span class="string">'唐%'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#修改多表</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> update 表一 别名</span></span><br><span class="line"><span class="comment"> inner|left|right join 表二 别名</span></span><br><span class="line"><span class="comment"> on 连接条件</span></span><br><span class="line"><span class="comment"> set 列=值...</span></span><br><span class="line"><span class="comment"> where 筛选条件;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#修改没有男朋友的女神的男朋友都为2号</span></span><br><span class="line"><span class="keyword">use</span> girls;</span><br><span class="line"><span class="keyword">update</span> boys bo</span><br><span class="line">    <span class="keyword">right</span> <span class="keyword">join</span> beauty b <span class="keyword">on</span> bo.id = b.boyfriend_id</span><br><span class="line"><span class="keyword">set</span> b.boyfriend_id=<span class="number">2</span></span><br><span class="line"><span class="keyword">where</span> b.id <span class="keyword">is</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#,三,删除语句</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> delete</span></span><br><span class="line"><span class="comment"> delete from 表名 where 条件</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">#单表删除</span></span><br><span class="line"><span class="keyword">delete</span></span><br><span class="line"><span class="keyword">from</span> beauty</span><br><span class="line"><span class="keyword">where</span> phone <span class="keyword">like</span> <span class="string">'%9'</span>;</span><br><span class="line"><span class="comment">#多表删除</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> delete 表一别名,表二别名</span></span><br><span class="line"><span class="comment"> from 表一 别名</span></span><br><span class="line"><span class="comment"> inner|left|right join 表二 别名 on 连接条件</span></span><br><span class="line"><span class="comment"> where 筛选条件</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">delete</span> b</span><br><span class="line"><span class="keyword">from</span> beauty b</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> boys bo</span><br><span class="line">                    <span class="keyword">on</span> b.boyfriend_id = bo.id</span><br><span class="line"><span class="keyword">where</span> bo.boyName = <span class="string">'张无忌'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#truncate清空表中数据</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 若表中有自增长列,delete删除数据后自增编号仍被占用,二truncate后从一开始</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure>

<h1 id="DDL语言"><a href="#DDL语言" class="headerlink" title="DDL语言"></a>DDL语言</h1><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">数据定义语言</span></span><br><span class="line"><span class="comment">库和表的管理</span></span><br><span class="line"><span class="comment">一、库的管理</span></span><br><span class="line"><span class="comment">创建、修改、删除</span></span><br><span class="line"><span class="comment">二、表的管理</span></span><br><span class="line"><span class="comment">创建、修改、删除</span></span><br><span class="line"><span class="comment">创建： create</span></span><br><span class="line"><span class="comment">修改： alter</span></span><br><span class="line"><span class="comment">删除： drop</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">#一、库的管理</span></span><br><span class="line"><span class="comment">#1、库的创建</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">语法：</span></span><br><span class="line"><span class="comment">    create database  [if not exists]库名;</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#案例：创建库Books</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">DATABASE</span> <span class="keyword">IF</span> <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> books ;</span><br><span class="line"></span><br><span class="line"><span class="comment">#2、库的修改</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">RENAME</span> <span class="keyword">DATABASE</span> books <span class="keyword">TO</span> 新库名;</span><br><span class="line"></span><br><span class="line"><span class="comment">#更改库的字符集</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">DATABASE</span> books <span class="built_in">CHARACTER</span> <span class="keyword">SET</span> gbk;</span><br><span class="line"></span><br><span class="line"><span class="comment">#3、库的删除</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">DATABASE</span> <span class="keyword">IF</span> <span class="keyword">EXISTS</span> books;</span><br><span class="line"></span><br><span class="line"><span class="comment">#二、表的管理</span></span><br><span class="line"><span class="comment">#1.表的创建 ★</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">语法：</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">create table 表名(</span></span><br><span class="line"><span class="comment">	列名 列的类型【(长度) 约束】,</span></span><br><span class="line"><span class="comment">	列名 列的类型【(长度) 约束】,</span></span><br><span class="line"><span class="comment">	列名 列的类型【(长度) 约束】,</span></span><br><span class="line"><span class="comment">	...</span></span><br><span class="line"><span class="comment">	列名 列的类型【(长度) 约束】</span></span><br><span class="line"><span class="comment">)</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="comment">#案例：创建表Book</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> book(</span><br><span class="line">    	<span class="keyword">id</span> <span class="built_in">INT</span>,<span class="comment">#编号</span></span><br><span class="line">    	bName <span class="built_in">VARCHAR</span>(<span class="number">20</span>),<span class="comment">#图书名</span></span><br><span class="line">    	price <span class="keyword">DOUBLE</span>,<span class="comment">#价格</span></span><br><span class="line">    	authorId  <span class="built_in">INT</span>,<span class="comment">#作者编号</span></span><br><span class="line">    	publishDate DATETIME<span class="comment">#出版日期</span></span><br><span class="line">    );</span><br><span class="line">    查看book表： DESC book;</span><br><span class="line"></span><br><span class="line"><span class="comment">#案例：创建表author</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> <span class="keyword">IF</span> <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> author(</span><br><span class="line">	<span class="keyword">id</span> <span class="built_in">INT</span>,</span><br><span class="line">	au_name <span class="built_in">VARCHAR</span>(<span class="number">20</span>),</span><br><span class="line">	nation <span class="built_in">VARCHAR</span>(<span class="number">10</span>)</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">查看作者表： <span class="keyword">DESC</span> author;</span><br><span class="line"></span><br><span class="line"><span class="comment">#2.表的修改</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">语法</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">alter table 表名 add|drop|modify|change column 列名 【列类型 约束】;</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#①修改列名</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> book <span class="keyword">CHANGE</span> <span class="keyword">COLUMN</span> publishdate pubDate DATETIME;</span><br><span class="line"></span><br><span class="line"><span class="comment">#②修改列的类型或约束</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> book <span class="keyword">MODIFY</span> <span class="keyword">COLUMN</span> pubdate <span class="built_in">TIMESTAMP</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#③添加新列</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> author <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> annual <span class="keyword">DOUBLE</span>; </span><br><span class="line"></span><br><span class="line"><span class="comment">#④删除列</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> book_author <span class="keyword">DROP</span> <span class="keyword">COLUMN</span>  annual;</span><br><span class="line"></span><br><span class="line"><span class="comment">#⑤修改表名</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> author <span class="keyword">RENAME</span> <span class="keyword">TO</span> book_author;</span><br><span class="line"></span><br><span class="line">DESC book;</span><br><span class="line"></span><br><span class="line"><span class="comment">#3.表的删除</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> <span class="keyword">IF</span> <span class="keyword">EXISTS</span> book_author;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">TABLES</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#通用的写法：</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">DATABASE</span> <span class="keyword">IF</span> <span class="keyword">EXISTS</span> 旧库名;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">DATABASE</span> 新库名;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> <span class="keyword">IF</span> <span class="keyword">EXISTS</span> 旧表名;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span>  表名();</span><br><span class="line"></span><br><span class="line"><span class="comment">#4.表的复制</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> author <span class="keyword">VALUES</span></span><br><span class="line">(<span class="number">1</span>,<span class="string">'村上春树'</span>,<span class="string">'日本'</span>),</span><br><span class="line">(<span class="number">2</span>,<span class="string">'莫言'</span>,<span class="string">'中国'</span>),</span><br><span class="line">(<span class="number">3</span>,<span class="string">'冯唐'</span>,<span class="string">'中国'</span>),</span><br><span class="line">(<span class="number">4</span>,<span class="string">'金庸'</span>,<span class="string">'中国'</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> Author;</span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> copy2;</span><br><span class="line"><span class="comment">#1.仅仅复制表的结构</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> copy <span class="keyword">LIKE</span> author;</span><br><span class="line"></span><br><span class="line"><span class="comment">#2.复制表的结构+数据</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> copy2 </span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> author;</span><br><span class="line"></span><br><span class="line"><span class="comment">#只复制部分数据</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> copy3</span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">id</span>,au_name</span><br><span class="line"><span class="keyword">FROM</span> author </span><br><span class="line"><span class="keyword">WHERE</span> nation=<span class="string">'中国'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#仅仅复制某些字段</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> copy4 </span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">id</span>,au_name</span><br><span class="line"><span class="keyword">FROM</span> author</span><br><span class="line"><span class="keyword">WHERE</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#1. 创建表dept1</span></span><br><span class="line"></span><br><span class="line">  NAME	NULL?	TYPE</span><br><span class="line">    id		INT(7)</span><br><span class="line">    NAME		VARCHAR(25）；</span><br><span class="line">    <span class="keyword">USE</span> <span class="keyword">test</span>；</span><br><span class="line">    <span class="keyword">CREATE</span> <span class="keyword">TABLE</span> dept1(</span><br><span class="line">    	<span class="keyword">id</span> <span class="built_in">INT</span>(<span class="number">7</span>),</span><br><span class="line">    	<span class="keyword">NAME</span> <span class="built_in">VARCHAR</span>(<span class="number">25</span>)</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line"><span class="comment">#2. 将表departments中的数据插入新表dept2中</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> dept2</span><br><span class="line"><span class="keyword">SELECT</span> department_id,department_name</span><br><span class="line"><span class="keyword">FROM</span> myemployees.departments;</span><br><span class="line"></span><br><span class="line"><span class="comment">#3. 创建表emp5</span></span><br><span class="line"></span><br><span class="line">NAME	NULL?	TYPE</span><br><span class="line">id		INT(7)</span><br><span class="line">First_name	VARCHAR (25)</span><br><span class="line">Last_name	VARCHAR(25)</span><br><span class="line">Dept_id		INT(7)</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> emp5(</span><br><span class="line"><span class="keyword">id</span> <span class="built_in">INT</span>(<span class="number">7</span>),</span><br><span class="line">first_name <span class="built_in">VARCHAR</span>(<span class="number">25</span>),</span><br><span class="line">last_name <span class="built_in">VARCHAR</span>(<span class="number">25</span>),</span><br><span class="line">dept_id <span class="built_in">INT</span>(<span class="number">7</span>)</span><br><span class="line"></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">#4. 将列Last_name的长度增加到50</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> emp5 <span class="keyword">MODIFY</span> <span class="keyword">COLUMN</span> last_name <span class="built_in">VARCHAR</span>(<span class="number">50</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">#5. 根据表employees创建employees2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> employees2 <span class="keyword">LIKE</span> myemployees.employees;</span><br><span class="line"></span><br><span class="line"><span class="comment">#6. 删除表emp5</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> <span class="keyword">IF</span> <span class="keyword">EXISTS</span> emp5;</span><br><span class="line"></span><br><span class="line"><span class="comment">#7. 将表employees2重命名为emp5</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> employees2 <span class="keyword">RENAME</span> <span class="keyword">TO</span> emp5;</span><br><span class="line"></span><br><span class="line"><span class="comment">#8.在表dept和emp5中添加新列test_column，并检查所作的操作</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> emp5 <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> test_column <span class="built_in">INT</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">#9.直接删除表emp5中的列 dept_id</span></span><br><span class="line">DESC emp5;</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> emp5 <span class="keyword">DROP</span> <span class="keyword">COLUMN</span> test_column;</span><br></pre></td></tr></table></figure>

<p>MySql常见约束<br>含义：一种限制，用于限制表中的数据，为了保证表中数据的准确性和可靠性。</p>
<p>分类：六大约束</p>
<p>　　1.NOT NULL ：非空，用于保证该字段的值不能为空。例如学生表的学生姓名及学号等等。</p>
<p>　　2.DEFAULT：默认值，用于保证该字段有默认值。例如学生表的学生性别</p>
<p>　　3.PRIMARY KEY：主键，用于保证该字段的值具有唯一性并且非空。例如学生表的学生学号等。</p>
<p>　　4.UNIQUE：唯一，用于保证该字段的值具有唯一性，可以为空。例如注册用户的手机号，身份证号等。</p>
<p>　　5.CHECK：检查约束（MySql不支持），检查字段的值是否为指定的值。</p>
<p>　　6.FOREIGN KEY：外键，用于限制两个表的关系，用于保证该字段的值必须来自于主表的关联列的值，在从表添加外键约束，用于引用主表中某些的值。例如学生表的专业编号</p>
<p>添加约束的实际：</p>
<p>　　1.创建表时</p>
<p>　　2.修改表时</p>
<p>约束的添加分类：</p>
<p>　　列级约束：六大约束语法上都支持，但外键约束没有效果</p>
<p>　　表级约束：除了非空、默认、其它的都支持。</p>
<p>　　示例：</p>
<p>　　　　①添加列级约束　　　</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> <span class="keyword">if</span> <span class="keyword">not</span> <span class="keyword">exists</span> t_stuinfo</span><br><span class="line">(</span><br><span class="line">    <span class="keyword">id</span>      <span class="built_in">int</span> primary <span class="keyword">key</span>,                              <span class="comment">#主键</span></span><br><span class="line">    stuName <span class="built_in">varchar</span>(<span class="number">20</span>) <span class="keyword">not</span> <span class="literal">null</span>,                         <span class="comment">#非空</span></span><br><span class="line">    gender  <span class="built_in">char</span>(<span class="number">1</span>) <span class="keyword">check</span> (gender = <span class="string">'男'</span> <span class="keyword">or</span> gender = <span class="string">'女'</span>), <span class="comment">#检查约束，MySql没有效果但不报错</span></span><br><span class="line">    seat    <span class="built_in">int</span> <span class="keyword">unique</span>,                                   <span class="comment">#唯一约束</span></span><br><span class="line">    age     <span class="built_in">int</span> <span class="keyword">default</span> <span class="number">18</span>,                               <span class="comment">#默认（值）约束</span></span><br><span class="line">    majorId <span class="built_in">int</span> <span class="keyword">references</span> major (<span class="keyword">id</span>)                     <span class="comment">#外键约束，MySql没有效果，但不报错</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>


<p>　　　　语法：在创建表字段的最下面</p>
<p>　　　　　　    constraint 约束名 约束类型(字段名)</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> <span class="keyword">if</span> <span class="keyword">not</span> <span class="keyword">exists</span> t_stuinfo</span><br><span class="line">(</span><br><span class="line">    <span class="keyword">id</span>      <span class="built_in">int</span>,</span><br><span class="line">    stuName <span class="built_in">varchar</span>(<span class="number">20</span>),</span><br><span class="line">    gender  <span class="built_in">char</span>(<span class="number">1</span>),</span><br><span class="line">    seat    <span class="built_in">int</span>,</span><br><span class="line">    age     <span class="built_in">int</span>,</span><br><span class="line">    majorId <span class="built_in">int</span>,</span><br><span class="line">    <span class="keyword">constraint</span> pk primary <span class="keyword">key</span> (<span class="keyword">id</span>),                                         <span class="comment">#约束名随意，主键不生效，但不报错。</span></span><br><span class="line">    <span class="keyword">constraint</span> uq <span class="keyword">unique</span> (seat),                                            <span class="comment">#唯一约束</span></span><br><span class="line">    <span class="keyword">constraint</span> ck <span class="keyword">check</span> (gender = <span class="string">'男'</span> <span class="keyword">or</span> gender = <span class="string">'女'</span>),                     <span class="comment">#检查约束，MySql不支持此约束，不报错但不生效</span></span><br><span class="line">    <span class="keyword">constraint</span> fk_stuinfo_major <span class="keyword">foreign</span> <span class="keyword">key</span> (majorId) <span class="keyword">references</span> major (<span class="keyword">id</span>) <span class="comment">#外键约束</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>
<p>列级约束支持所有,但外没效果;表级不支持默认和非空,二者都不支持check.列级不可以起名字,表级可以,但是主键没效果.</p>
<p> 主键和唯一的区别</p>
<table>
<thead>
<tr>
<th>约束名称</th>
<th>保证唯一性</th>
<th>是否允许为空</th>
<th>一个表中可以有多少个</th>
<th>是否允许组合</th>
</tr>
</thead>
<tbody><tr>
<td>主键</td>
<td>√</td>
<td>×</td>
<td>最多有1个，可以没有</td>
<td>√（不推荐）</td>
</tr>
<tr>
<td>唯一</td>
<td>√</td>
<td>√</td>
<td>可以有多个</td>
<td>√（不推荐）</td>
</tr>
<tr>
<td>外键：</td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<p>　　1.要求在从表中设置外键关系</p>
<p>　　2.从表的外键列的类型和主表的关联列的类型要求一致或兼容，名称无要求。</p>
<p>　　3.<strong>主表的关联列必须时一个Key（一般为主键或唯一，外键也可以但无意义）</strong></p>
<p>　　4.<strong>插入数据时，先插入主表，再插入从表</strong></p>
<p>　　  <strong>删除数据时，先删除从表，再删除主表</strong></p>
<p>修改表时添加约束</p>
<p>　　1.添加非空约束</p>
<p> <code>alter table 表名称 modify column 列名 列类型 not null;</code></p>
<p>　　2.添加默认约束</p>
<p> <code>alter table 表名称 modify column 列名 列类型 default 默认值;</code></p>
<p>　　3.添加主键</p>
<p>　　　　①列级约束方式</p>
<p> <code>alter table 表名称 modify column 列名 列类型 primary key;</code></p>
<p>　　　　②表级约束方式</p>
<p> <code>alter table 表名称 add primary key(id);</code></p>
<p>　　4.添加唯一</p>
<p>　　　　①列级约束</p>
<p> <code>alter table 表名称 modify column 列名 列类型 unique;</code></p>
<p>　　　　②表级约束</p>
<p> <code>alter table 表名称 add unique(列名称);</code></p>
<p>　　5，添加外键（列级写法可行，但无效果）</p>
<p>　　　　①表级约束</p>
<p><code>alter table 表名称 add constraint fk_stuinfo_magor foreign key(magorId) references major(id);</code></p>
<p>　　6.添加通用写法（语法）</p>
<p>　　　　①列级约束　</p>
<p> <code>alter table 表名称 modify column 列名 列类型 新约束;</code></p>
<p>　　　　②表级约束</p>
<p> <code>alter table 表名称 add [constraint 约束名] 约束类型(列名称) [外键的引用];</code></p>
<p>修改表时删除约束</p>
<p>　　1.删除非空约束</p>
<p> <code>alter table 表名称 modify column 列名 列类型 [null];</code></p>
<p>　　2.删除默认约束</p>
<p> <code>alter table 表名称 modify column 列名 列类型;</code></p>
<p>　　3.删除主键</p>
<p>　　　　①</p>
<p> <code>alter table 表名称 modify column id int;</code></p>
<p>　　　　②</p>
<p> <code>alter table 表名称 drop primary key;</code></p>
<p>　　4.删除唯一</p>
<p><code>alter table 表名称 drop index(索引名) 设置唯一时的名称;</code></p>
<p>　　5.删除外键</p>
<p><code>alter table 表名称 drop foreign key(索引名) 设置外键时的名称;</code></p>
<p>标识列</p>
<p>　　又称为自增长列，可以不用手动的插入值，系统提供默认的序列值</p>
<p>　　特点：</p>
<p>　　　　1.标识列必须和一个Key搭配（Key指主键、唯一、外键….）</p>
<p>　　　　2.一个表最多有一个标识列</p>
<p>　　　　3.标识列的类型只能是数值型</p>
<p>　　　　4.标识列可以通过SET auto_increment_increment = 3;设置步长（全局），可以通过<strong>插入行时手动插入标识列值设置起始值</strong>。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">insert</span> <span class="keyword">into</span> tab_identity (<span class="keyword">id</span>, <span class="keyword">name</span>)<span class="comment">#id是自增长的,为它指定起始值10</span></span><br><span class="line"><span class="keyword">values</span> (<span class="number">10</span>, <span class="string">'john'</span>);</span><br></pre></td></tr></table></figure>

<p>　　1.创建表时设置标识列</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> <span class="keyword">user</span></span><br><span class="line">(</span><br><span class="line">    <span class="keyword">id</span>   <span class="built_in">int</span> primary <span class="keyword">key</span> auto_increment,</span><br><span class="line">    <span class="keyword">name</span> <span class="built_in">varchar</span>(<span class="number">20</span>)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>　　2.修改表时设置标识列</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter</span> <span class="keyword">table</span> 表名称</span><br><span class="line">    <span class="keyword">modify</span> <span class="keyword">column</span> <span class="keyword">id</span> <span class="built_in">int</span> primary <span class="keyword">key</span> auto_increment;</span><br></pre></td></tr></table></figure>

<p>　　3.修改表时删除标识列</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter</span> <span class="keyword">table</span> 表名称</span><br><span class="line">    <span class="keyword">modify</span> <span class="keyword">column</span> <span class="keyword">id</span> <span class="built_in">int</span> primary <span class="keyword">key</span>;</span><br></pre></td></tr></table></figure>

<h1 id="TCL语言"><a href="#TCL语言" class="headerlink" title="TCL语言"></a>TCL语言</h1><p>事务控制语言.</p>
<p>事务:一个或一组SQL语句组成一个执行单元,这个单元要么全部执行,要么全部不执行.</p>
<p>事务的ACID属性:原子性(不可分割),一致性,隔离性(一个事务的执行不受其它事务的影响,还要考虑隔离级别),持久性</p>
<p>事务的创建:</p>
<p>隐式事务:没有明显的开启和结束标记,比如insert,update,delete</p>
<p>显式事务:具有明显的开启和结束标记,需要先禁用自动提交功能</p>
<p>步骤一:开启事务</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set autocommit&#x3D;0;</span><br><span class="line">start transaction;#可选的</span><br></pre></td></tr></table></figure>

<p>步骤二:编写事务中的SQL语句(select update insert delete)</p>
<p>步骤三:结束事务</p>
<p>commit;提交事务</p>
<p>rollback;回滚事务</p>
<p>savepoint;设置保存点</p>
<p>对于同时运行的多个事务,如果是访问同一数据,就要采取必要的隔离机制.</p>
<p>select @@transaction_isolation;#查看隔离级别</p>
<p>MySQL提供了四种隔离级别:</p>
<p>READ UNCOOMMITED:允许事务读取其他事务未提交的变更,脏读,不可重复读,幻读都会出现</p>
<p>READ COMMITED:允许事务读取已经提交的变更,可避免脏读</p>
<p>REPEATABLE READ:确保事务多次从同一个字段读取出相同的值,在这个事务期间禁止其他事务修改该字段,可以避免脏读和不可重复读(默认级别)</p>
<p>SERIALIZABLE:类似于Java中的加锁,可以避免所有并发问题,但性能较低</p>
<p>set session transaction isolation level serializable;#设置当前会话的隔离级别</p>
<p>set global transaction isolation level read committed;#设置全局隔离级别</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">set</span> autocommit = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">start</span> <span class="keyword">transaction</span>;</span><br><span class="line"><span class="keyword">delete</span></span><br><span class="line"><span class="keyword">from</span> beauty</span><br><span class="line"><span class="keyword">where</span> <span class="keyword">id</span> = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">savepoint</span> a;<span class="comment">#设置保存点</span></span><br><span class="line"><span class="keyword">delete</span></span><br><span class="line"><span class="keyword">from</span> beauty</span><br><span class="line"><span class="keyword">where</span> <span class="keyword">id</span> = <span class="number">2</span>;</span><br><span class="line"><span class="keyword">rollback</span> <span class="keyword">to</span> a;</span><br></pre></td></tr></table></figure>

<h1 id="视图"><a href="#视图" class="headerlink" title="视图:"></a>视图:</h1><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">虚拟表,和普通表一样使用,该表不是真实存在的.</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> myemployees;</span><br><span class="line"><span class="keyword">select</span> employee_id, last_name, job_title</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> jobs j <span class="keyword">on</span> employees.job_id = j.job_id</span><br><span class="line"><span class="keyword">where</span> employee_id &lt; <span class="number">140</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">view</span> v1<span class="comment">#创建一个视图</span></span><br><span class="line"><span class="keyword">as</span></span><br><span class="line"><span class="keyword">select</span> employee_id, last_name, job_title</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> jobs j <span class="keyword">on</span> employees.job_id = j.job_id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> v1</span><br><span class="line"><span class="keyword">where</span> employee_id &lt; <span class="number">140</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">or</span> <span class="keyword">replace</span> <span class="keyword">view</span> v1<span class="comment">#修改视图方式一</span></span><br><span class="line"><span class="keyword">as</span></span><br><span class="line"><span class="keyword">select</span> employee_id, last_name, job_title</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line">         <span class="keyword">inner</span> <span class="keyword">join</span> jobs j <span class="keyword">on</span> employees.job_id = j.job_id;</span><br><span class="line"></span><br><span class="line"><span class="keyword">alter</span> <span class="keyword">view</span> myemployees.v1<span class="comment">#方式二</span></span><br><span class="line">    <span class="keyword">as</span></span><br><span class="line">        <span class="keyword">select</span> employee_id, last_name, job_title</span><br><span class="line">        <span class="keyword">from</span> employees</span><br><span class="line">                 <span class="keyword">inner</span> <span class="keyword">join</span> jobs j <span class="keyword">on</span> employees.job_id = j.job_id</span><br><span class="line"></span><br><span class="line"><span class="keyword">drop</span> <span class="keyword">view</span> v1,v2...<span class="comment">#删除视图</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">desc</span> myemployees.v1;</span><br><span class="line"><span class="keyword">show</span> <span class="keyword">create</span> <span class="keyword">view</span> myemployees.v1;</span><br><span class="line"></span><br><span class="line"><span class="keyword">delete</span> <span class="keyword">from</span> v2 <span class="keyword">where</span> employee_id=<span class="number">233</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">insert</span> <span class="keyword">into</span> myemployees.v2 (employee_id, last_name)<span class="comment">#对视图插入数据,原始表也会有相应数据</span></span><br><span class="line"><span class="keyword">values</span> (<span class="string">'99'</span>, <span class="string">'test'</span>);</span><br></pre></td></tr></table></figure>
<p>对视图的修改是非常少的,一般不修改,并且很多视图也不能修改:</p>
<p>sql语句包含分组函数,distinct,group by,having,union,union all</p>
<p>常量视图<code>create view v3 as select &#39;john&#39; name;</code></p>
<p>包含子查询</p>
<p>包含join</p>
<p>视图数据from于一个不能更新的视图</p>
<p>where子句的子查询引用了from子句中的表</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">view</span> v3</span><br><span class="line"><span class="keyword">as</span></span><br><span class="line"><span class="keyword">select</span> <span class="string">'john'</span> <span class="keyword">name</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> v3;</span><br><span class="line"></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">view</span> v4</span><br><span class="line"><span class="keyword">as</span></span><br><span class="line"><span class="keyword">select</span> last_name, email, salary</span><br><span class="line"><span class="keyword">from</span> employees</span><br><span class="line"><span class="keyword">where</span> employee_id <span class="keyword">in</span> (</span><br><span class="line">    <span class="keyword">select</span> manager_id</span><br><span class="line">    <span class="keyword">from</span> employees</span><br><span class="line">    <span class="keyword">where</span> manager_id <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">null</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h1 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h1><p>系统变量:</p>
<ul>
<li>全局变量</li>
<li>会话变量<br>自定义变量</li>
<li>用户变量</li>
<li>局部变量</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">show</span> <span class="keyword">global</span>|[sesion] <span class="keyword">variables</span> <span class="keyword">like</span> <span class="string">'%char'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> @@<span class="keyword">global</span>|[sesion].系统变量名;</span><br><span class="line"></span><br><span class="line"><span class="keyword">set</span> <span class="keyword">global</span>|[<span class="keyword">session</span>] 系统变量名 = 值;</span><br><span class="line"></span><br><span class="line"><span class="keyword">set</span> @@<span class="keyword">global</span>|[<span class="keyword">session</span>].系统变量名 = 值;</span><br><span class="line"></span><br><span class="line"><span class="comment">#自定义变量</span></span><br><span class="line"></span><br><span class="line"><span class="comment">##用户变量,针对当前会话有效,可以放在任意地方</span></span><br><span class="line"><span class="keyword">set</span> @用户变量名 = 值;</span><br><span class="line"><span class="keyword">set</span> @用户变量名 := 值;</span><br><span class="line"><span class="keyword">select</span> @用户变量名 := 值;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> 字段 <span class="keyword">into</span> 变量名</span><br><span class="line"><span class="keyword">from</span> 表;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> @用户变量名;<span class="comment">#使用</span></span><br><span class="line"></span><br><span class="line"><span class="comment">##局部变量,仅仅能在begin end中使用,而且要放在第一句</span></span><br><span class="line"><span class="keyword">declare</span> 变量名 类型[<span class="keyword">default</span> 值];</span><br><span class="line"><span class="keyword">set</span> 局部变量名 = 值;</span><br><span class="line"><span class="keyword">set</span> 局部变量名 := 值;</span><br><span class="line"><span class="keyword">select</span> @局部变量名 := 值;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> 字段 <span class="keyword">into</span> 局部变量名</span><br><span class="line"><span class="keyword">from</span> 表;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> 局部变量名;<span class="comment">#使用</span></span><br></pre></td></tr></table></figure>

<h1 id="存储过程和函数"><a href="#存储过程和函数" class="headerlink" title="存储过程和函数"></a>存储过程和函数</h1><p>存储过程</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> <span class="keyword">name</span>(参数列表)<span class="comment">#参数列表包含参数模式 参数名 参数类型三部分</span></span><br><span class="line"><span class="keyword">begin</span> </span><br><span class="line">    存储过程</span><br><span class="line"><span class="keyword">end</span>;</span><br></pre></td></tr></table></figure>
<p>参数模式:</p>
<p>in:该参数可以作为输入 </p>
<p>out:该参数可以作为返回值 </p>
<p>inout:既需要传入参数,也可以返回值</p>
<p>如果存储过程体只有一句话,begin end可以省略.存储过程的每条语句结尾都要加分号,而存储过程结尾可以使用delimiter重新指定结束符.</p>
<p>调用存储过程:call 存储过程名(参数)</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">delimiter $</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> myp1()</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">    <span class="keyword">insert</span> <span class="keyword">into</span> <span class="keyword">admin</span>(username, <span class="keyword">password</span>) <span class="keyword">VALUES</span> (<span class="string">'john1'</span>,<span class="number">0000</span>),(<span class="string">'ross'</span>,<span class="number">9999</span>);</span><br><span class="line"><span class="keyword">end</span> $</span><br><span class="line"></span><br><span class="line"><span class="keyword">call</span> myp1();</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> girls;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">from</span> <span class="keyword">admin</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> myp2(<span class="keyword">in</span> beautyName <span class="built_in">varchar</span>(<span class="number">20</span>))</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">    <span class="keyword">select</span> bo.*</span><br><span class="line">    <span class="keyword">from</span> boys bo</span><br><span class="line">             <span class="keyword">right</span> <span class="keyword">join</span> beauty b <span class="keyword">on</span> bo.id = b.boyfriend_id</span><br><span class="line">    <span class="keyword">where</span> b.name = beautyName;</span><br><span class="line"><span class="keyword">end</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">call</span> myp2(<span class="string">'周芷若'</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> myp3(<span class="keyword">in</span> username <span class="built_in">varchar</span>(<span class="number">20</span>), <span class="keyword">in</span> <span class="keyword">password</span> <span class="built_in">varchar</span>(<span class="number">20</span>))</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">    <span class="keyword">declare</span> <span class="keyword">result</span> <span class="built_in">int</span> <span class="keyword">default</span> <span class="number">0</span>;<span class="comment">#声明并初始化</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">select</span> <span class="keyword">count</span>(*)</span><br><span class="line">    <span class="keyword">into</span> <span class="keyword">result</span><span class="comment">#赋值</span></span><br><span class="line">    <span class="keyword">from</span> <span class="keyword">admin</span></span><br><span class="line">    <span class="keyword">where</span> admin.username = username</span><br><span class="line">      <span class="keyword">and</span> admin.password = <span class="keyword">password</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">select</span> <span class="keyword">if</span>(<span class="keyword">result</span> &gt; <span class="number">0</span>, <span class="string">'成功'</span>, <span class="string">'失败'</span>);<span class="comment">#使用</span></span><br><span class="line"><span class="keyword">end</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">call</span> myp3(<span class="string">'张飞'</span>, <span class="number">44444</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> myp5(<span class="keyword">in</span> beautyName <span class="built_in">varchar</span>(<span class="number">20</span>), <span class="keyword">out</span> boyName <span class="built_in">varchar</span>(<span class="number">20</span>))</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">    <span class="keyword">select</span> boys.boyName</span><br><span class="line">    <span class="keyword">into</span> boyName</span><br><span class="line">    <span class="keyword">from</span> boys</span><br><span class="line">             <span class="keyword">inner</span> <span class="keyword">join</span> beauty <span class="keyword">on</span> boys.id = boyfriend_id</span><br><span class="line">    <span class="keyword">where</span> beauty.name = beautyName;</span><br><span class="line"><span class="keyword">end</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">call</span> myp5(<span class="string">'热巴'</span>, @<span class="keyword">name</span>);</span><br><span class="line"><span class="keyword">select</span> @<span class="keyword">name</span>;</span><br></pre></td></tr></table></figure>
<p>删除存储过程:<br>drop procedure myp1</p>
<p>函数与存储过程类似,区别在于函数的返回值只能有一个.</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">function</span> 函数名(参数列表) <span class="keyword">returns</span> 返回类型<span class="comment">#参数列表只有参数名 参数类型</span></span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">  函数体</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> myemployees;</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">function</span> myf1(empName <span class="built_in">varchar</span>(<span class="number">20</span>)) <span class="keyword">returns</span> <span class="keyword">double</span></span><br><span class="line">    <span class="keyword">DETERMINISTIC</span></span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">    <span class="keyword">set</span> @sal = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">select</span> salary</span><br><span class="line">    <span class="keyword">into</span> @sal</span><br><span class="line">    <span class="keyword">from</span> employees</span><br><span class="line">    <span class="keyword">where</span> last_name = empName;</span><br><span class="line">    return @sal;</span><br><span class="line"><span class="keyword">end</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> myf1(<span class="string">'kochhar'</span>);</span><br></pre></td></tr></table></figure>

<h1 id="流程控制结构"><a href="#流程控制结构" class="headerlink" title="流程控制结构"></a>流程控制结构</h1><p>if函数</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">if</span>(<span class="number">10</span> &gt; <span class="number">5</span>, <span class="string">'大'</span>, <span class="string">'小'</span>);</span><br></pre></td></tr></table></figure>

<p>case结构 </p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> case语句:</span></span><br><span class="line"><span class="comment"> case 要判断的表达式</span></span><br><span class="line"><span class="comment"> when 常量值 then 语句或者值;(当是值时,不加分号)</span></span><br><span class="line"><span class="comment"> ...</span></span><br><span class="line"><span class="comment"> else 默认语句</span></span><br><span class="line"><span class="comment"> end</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">select</span> salary  <span class="keyword">as</span> 原始工资,</span><br><span class="line">       department_id,</span><br><span class="line">       <span class="keyword">case</span> department_id</span><br><span class="line">           <span class="keyword">when</span> <span class="number">30</span> <span class="keyword">then</span> salary * <span class="number">1.1</span></span><br><span class="line">           <span class="keyword">when</span> <span class="number">40</span> <span class="keyword">then</span> salary * <span class="number">1.2</span></span><br><span class="line">           <span class="keyword">when</span> <span class="number">50</span> <span class="keyword">then</span> salary * <span class="number">1.3</span></span><br><span class="line">           <span class="keyword">else</span> salary</span><br><span class="line">           <span class="keyword">end</span> <span class="keyword">as</span> 新工资</span><br><span class="line"><span class="keyword">from</span> employees;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> case还可以做多重if判断</span></span><br><span class="line"><span class="comment"> case</span></span><br><span class="line"><span class="comment"> when 条件 then 语句或者值;(当是值时,不加分号)</span></span><br><span class="line"><span class="comment"> ...</span></span><br><span class="line"><span class="comment"> else 默认语句</span></span><br><span class="line"><span class="comment"> end</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">select</span> salary,</span><br><span class="line">       <span class="keyword">case</span></span><br><span class="line">           <span class="keyword">when</span> salary &gt; <span class="number">20000</span> <span class="keyword">then</span> <span class="string">'A'</span></span><br><span class="line">           <span class="keyword">when</span> salary &gt; <span class="number">15000</span> <span class="keyword">then</span> <span class="string">'B'</span></span><br><span class="line">           <span class="keyword">when</span> salary &gt; <span class="number">10000</span> <span class="keyword">then</span> <span class="string">'C'</span></span><br><span class="line">           <span class="keyword">else</span> <span class="string">'D'</span></span><br><span class="line">           <span class="keyword">end</span> <span class="keyword">as</span> 工资级别</span><br><span class="line"><span class="keyword">from</span> employees;</span><br></pre></td></tr></table></figure>

<p>上面是作为嵌套的语句可以放在任何地方,但是case要做独立的语句就只能放在begin-end中间.并且作为独立语句时,then后面加的是语句和分号,最后的end处还要加上case和分号.</p>
<p>if结构</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">if 条件一 then 语句一;</span><br><span class="line">elseif 条件二 then 语句二;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">end</span> <span class="keyword">if</span>;</span><br></pre></td></tr></table></figure>
<p>应用在begin-end中.</p>
<p>循环结构:</p>
<p>while,loop,repeat</p>
<p>iterate:类似于continue,leave:类似于break</p>
<p>[标签:]while 循环条件 do<br>  循环体;<br>end while[标签];</p>
<p>[标签:]loop<br>  循环体;<br>end loop[标签];</p>
<p>[标签:]repeat<br>  循环体;<br>until 结束条件<br>end repeat[标签];</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> girls;</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> pro_while1(<span class="keyword">in</span> insertcount <span class="built_in">int</span>)</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">    <span class="keyword">declare</span> i <span class="built_in">int</span> <span class="keyword">default</span> <span class="number">1</span>;</span><br><span class="line">    a:</span><br><span class="line">    while i &lt;= insertcount</span><br><span class="line">        <span class="keyword">do</span></span><br><span class="line">            <span class="keyword">insert</span> <span class="keyword">into</span> <span class="keyword">admin</span>(username, <span class="keyword">password</span>) <span class="keyword">VALUES</span> (<span class="keyword">concat</span>(<span class="string">'rose'</span>, i), <span class="string">'666'</span>);</span><br><span class="line">        <span class="keyword">end</span> <span class="keyword">while</span> a;</span><br><span class="line"><span class="keyword">end</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">call</span> pro_while1(<span class="number">15</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> girls;</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> pro_while1(<span class="keyword">in</span> insertcount <span class="built_in">int</span>)</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">    <span class="keyword">declare</span> i <span class="built_in">int</span> <span class="keyword">default</span> <span class="number">1</span>;</span><br><span class="line">    a:</span><br><span class="line">    while i &lt;= insertcount</span><br><span class="line">        <span class="keyword">do</span></span><br><span class="line">            <span class="keyword">insert</span> <span class="keyword">into</span> <span class="keyword">admin</span>(username, <span class="keyword">password</span>) <span class="keyword">VALUES</span> (<span class="keyword">concat</span>(<span class="string">'rose'</span>, i), <span class="string">'666'</span>);</span><br><span class="line">            if i &gt; 10 then leave a; end if;#加入控制语句</span><br><span class="line">        <span class="keyword">end</span> <span class="keyword">while</span> a;</span><br><span class="line"><span class="keyword">end</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">call</span> pro_while1(<span class="number">15</span>);</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>JdbcTemplate</title>
    <url>/2020/07/02/JdbcTemplate/</url>
    <content><![CDATA[<p>Spring与JDBC的结合.</p>
<a id="more"></a>

<p>spring对jdbc进行了封装,使用JdbcTemplate方便实现对数据库操作.</p>
<p>一些准备工作:</p>
<p>配置数据库连接池,将JdbcTemplate加入Bean管理,开启组件扫描.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/context http://www.springframework.org/schema/beans/spring-context.xsd"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:property-placeholder</span> <span class="attr">location</span>=<span class="string">"classpath:../druid.properties"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"dataSource"</span> <span class="attr">class</span>=<span class="string">"com.alibaba.druid.pool.DruidDataSource"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driverClassName"</span> <span class="attr">value</span>=<span class="string">"$&#123;driverClassName&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span> <span class="attr">value</span>=<span class="string">"$&#123;url&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"$&#123;username&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"$&#123;password&#125;"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--JdbcTemplate对象--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"jdbcTemplate"</span> <span class="attr">class</span>=<span class="string">"org.springframework.jdbc.core.JdbcTemplate"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--注入dataSource,前一个是JdbcTemplate中的属性,set方法注入--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"dataSource"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"dao,service"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>BookService和BookDaoImpl代码;</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BookService</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> BookDao bookDao;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Repository</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BookDaoImpl</span> <span class="keyword">implements</span> <span class="title">BookDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> JdbcTemplate jdbcTemplate;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>具体的add方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(Book book)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//language=MySQL</span></span><br><span class="line">    String sql = <span class="string">"insert into test.book values (?,?)"</span>;</span><br><span class="line">    <span class="keyword">int</span> update = jdbcTemplate.update(sql, <span class="keyword">null</span>, book.getName());</span><br><span class="line">    System.out.println(update);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>增删改的错做都差不多,下面看下查:</p>
<p>返回某个值的查询:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">selectCount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">//language=MySQL</span></span><br><span class="line">    String sql = <span class="string">"select count(*) from test.book"</span>;</span><br><span class="line">    <span class="keyword">return</span> jdbcTemplate.queryForObject(sql, Integer<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>查询返回对象:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Book <span class="title">findBookInfo</span><span class="params">(String id)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//language=MySQL</span></span><br><span class="line">    String sql = <span class="string">"select * from test.book where id=?"</span>;</span><br><span class="line">    <span class="keyword">return</span> jdbcTemplate.queryForObject(sql, <span class="keyword">new</span> BeanPropertyRowMapper&lt;Book&gt;(Book<span class="class">.<span class="keyword">class</span>), <span class="title">id</span>)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>RowMapper接口类似于DBUtils里的handler,针对不同的返回值有不同的实现,这里是返回对象,所以用BeanPropertyRowMapper.</p>
<p>返回一个集合:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> List&lt;Book&gt; <span class="title">findAllBookInfo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">//language=MySQL</span></span><br><span class="line">    String sql = <span class="string">"select * from book;"</span>;</span><br><span class="line">    <span class="keyword">return</span> jdbcTemplate.query(sql, <span class="keyword">new</span> BeanPropertyRowMapper&lt;Book&gt;(Book<span class="class">.<span class="keyword">class</span>))</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>批量添加:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">batchAddBook</span><span class="params">(List&lt;Object[]&gt; batchArgs)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//language=MySQL</span></span><br><span class="line">    String sql = <span class="string">"insert into book values (?,?)"</span>;</span><br><span class="line">    jdbcTemplate.batchUpdate(sql, batchArgs);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>批量修改和删除与此类似.</p>
<p>事务的操作:</p>
<p>在UserDaoImpl中有增加钱和减少钱的方法.而在UserService中有一个利用这两个方法的转账方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">accountMoney</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">try</span>&#123;</span><br><span class="line">        userDAo.reduceMoney();</span><br><span class="line"></span><br><span class="line">        userDao.addMoney();</span><br><span class="line">        <span class="comment">//没有异常提交事务</span></span><br><span class="line">    &#125;<span class="keyword">catch</span>(Exception e)&#123;</span><br><span class="line">        <span class="comment">//有异常,回滚事务</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>事务的添加一般在Service层.用Spring管理事务有编程式和声明式两种.前一种就是上面那样写代码,不常用.后一种有注解和xml两种实现.</p>
<p>Spring中的事务管理提供了一个接口:PlatformTransactionManager,它针对不同的框架实现不同的实现类.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--创建事务管理器--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"transactionManager"</span> <span class="attr">class</span>=<span class="string">"org.springframework.jdbc.datasource.DataSourceTransactionManager"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--注入数据源--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"dataSource"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>在spring配置文件中开启事务注解要引入名称空间tx.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:tx</span>=<span class="string">"http://www.springframework.org/schema/tx"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--开启事务注解--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">tx:annotation-driven</span> <span class="attr">transaction-manager</span>=<span class="string">"transactionManager"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后在service的类或者类里的方法上加上事务的注解.</p>
<p>@Transactional注解可以加到类上,也可以加到方法上.该注解还有几个重要的属性:</p>
<p><strong>propagation</strong>:事务的传播行为,多事务方法之间进行调用,这个过程中事务是如何进行管理的.事务方法指对数据库进行变化的操作.在add方法(加了事务)中调用update方法(没加事务)时,事务如何管理,前者如何影响后者.或者没有事务的调用有事务的,或者两个都有事务之间的调用.</p>
<p>一共有七种行为:</p>
<p><strong>required</strong>:<strong>如果有事务在运行就在这个事务内运行,否则就启动一个新事务,并在自己的事务内运行.add(有事务)调用update(没事务),update在add的事务中运行.若是add和update都没事务那就没事务.若是add没事务,update有事务就在update自己的事务中运行.</strong></p>
<p><strong>required_new</strong>:<strong>当前的方法必须启动新事务,并在自己的事务中运行.如果有事务在运行就把旧事务挂起.</strong></p>
<p><strong>supports</strong>:如果有事务就在事务中运行,否则可以不在事务中运行.</p>
<p><strong>not_supported</strong>:以非事务方式执行操作，如果当前存在事务，就把当前事务挂起</p>
<p><strong>nerver</strong>:当前方法不应该在事务中执行,如果有就抛异常</p>
<p><strong>nested</strong>:如果有事务,当前方法就在该事务中嵌套事务内运行,否则就启动一个新事务在里面运行.</p>
<p><strong>mandatory</strong>:当前方法必须在事务内运行,如果没有事务就抛异常</p>
<p><strong>isolation</strong>:隔离级别</p>
<p><strong>timeout</strong>:超时时间,事务提交的时限,超过时间后不提交就会回滚.默认值为-1,手动设置单位为秒</p>
<p><strong>readOnly</strong>:是否只读</p>
<p><strong>rollbackFor</strong>:回滚,设置出现哪些异常进行回滚</p>
<p><strong>noRollbackFor</strong>:不回滚,设置出现哪些异常不进行回滚</p>
<p>基于xml实现事务管理,仍旧需要用xml配置事务管理器和数据源:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">tx:advice</span> <span class="attr">id</span>=<span class="string">"txadvice"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tx:attributes</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">tx:method</span> <span class="attr">name</span>=<span class="string">"accountMoney"</span> <span class="attr">propagation</span>=<span class="string">"REQUIRED"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tx:attributes</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">tx:advice</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--配置切入点和切面--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">aop:config</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置切入点--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">aop:pointcut</span> <span class="attr">id</span>=<span class="string">"pt"</span> <span class="attr">expression</span>=<span class="string">"execution(* service.*(..))"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">aop:advisor</span> <span class="attr">advice-ref</span>=<span class="string">"txadvice"</span> <span class="attr">pointcut-ref</span>=<span class="string">"pt"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">aop:config</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>完全注解实现:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.alibaba.druid.pool.DruidDataSource;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.ComponentScan;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.jdbc.core.JdbcTemplate;</span><br><span class="line"><span class="keyword">import</span> org.springframework.jdbc.datasource.DataSourceTransactionManager;</span><br><span class="line"><span class="keyword">import</span> org.springframework.transaction.annotation.EnableTransactionManagement;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.sql.DataSource;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/3 14:39</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ComponentScan</span>(basePackages = <span class="string">"service"</span>)</span><br><span class="line"><span class="meta">@EnableTransactionManagement</span><span class="comment">//开启事务</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">config</span> </span>&#123;</span><br><span class="line">    <span class="comment">//创建数据库连接池</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> DruidDataSource <span class="title">getDruidDataSource</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        DruidDataSource dataSource = <span class="keyword">new</span> DruidDataSource();</span><br><span class="line">        dataSource.setDriverClassName(<span class="string">"com.mysql.cj.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setUrl(<span class="string">"jdbc:mysql://127.0.0.1:3306/test?rewriteBatchedStatements=true&amp;serverTimezone=GMT"</span>);</span><br><span class="line">        dataSource.setUsername(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">"root"</span>);</span><br><span class="line">        <span class="keyword">return</span> dataSource;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> JdbcTemplate <span class="title">getJdbcTemplate</span><span class="params">(DataSource dataSource)</span> </span>&#123;</span><br><span class="line">        JdbcTemplate jdbcTemplate = <span class="keyword">new</span> JdbcTemplate();</span><br><span class="line">        jdbcTemplate.setDataSource(dataSource);</span><br><span class="line">        <span class="keyword">return</span> jdbcTemplate;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSourceTransactionManager <span class="title">getDataSourceTransactionManager</span><span class="params">(DataSource dataSource)</span> </span>&#123;</span><br><span class="line">        DataSourceTransactionManager dataSourceTransactionManager = <span class="keyword">new</span> DataSourceTransactionManager();</span><br><span class="line">        dataSourceTransactionManager.setDataSource(dataSource);</span><br><span class="line">        <span class="keyword">return</span> dataSourceTransactionManager;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>补充:</p>
<p>spring中用junit4测试:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"><span class="keyword">import</span> org.junit.runner.RunWith;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.test.context.ContextConfiguration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.test.context.junit4.SpringJUnit4ClassRunner;</span><br><span class="line"><span class="keyword">import</span> service.BookService;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/3 15:45</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@RunWith</span>(SpringJUnit4ClassRunner<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line">@ContextConfiguration("classpath:bean.xml")</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JTest4</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> BookService bookService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        bookService.findCount();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>基于junit5的测试</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.extension.ExtendWith;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.test.context.ContextConfiguration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.test.context.junit.jupiter.SpringExtension;</span><br><span class="line"><span class="keyword">import</span> service.BookService;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/3 16:01</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@ExtendWith</span>(SpringExtension<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line">@ContextConfiguration("classpath:bean.xml")</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JTest5</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> BookService bookService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        bookService.findCount();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>测试类上的两个注解可以简化为一个:<code>@SpringJUnitConfig(locations = &quot;classpath:bean.xml&quot;)</code></p>
]]></content>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL-2</title>
    <url>/2020/07/13/MySQL-2/</url>
    <content><![CDATA[<p>MySQL高级.</p>
<a id="more"></a>

<p>两种Mysql引擎的对比:</p>
<table>
<thead>
<tr>
<th>对比项</th>
<th>MyISAM</th>
<th>InnoDB</th>
</tr>
</thead>
<tbody><tr>
<td>主外键</td>
<td>不支持</td>
<td>支持</td>
</tr>
<tr>
<td>事务</td>
<td>不支持</td>
<td>支持</td>
</tr>
<tr>
<td>行表锁</td>
<td>表锁</td>
<td>行锁</td>
</tr>
<tr>
<td>缓存</td>
<td>只缓存索引,不缓存真实数据</td>
<td>二者都缓存,对内存要求高</td>
</tr>
<tr>
<td>表空间</td>
<td>小</td>
<td>大</td>
</tr>
<tr>
<td>关注点</td>
<td>性能</td>
<td>事务</td>
</tr>
<tr>
<td>默认安装</td>
<td>是</td>
<td>是</td>
</tr>
</tbody></table>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">+--------------------+---------+----------------------------------------------------------------+--------------+------+------------+</span><br><span class="line">| Engine             | Support | Comment                                                        | Transactions | XA   | Savepoints |</span><br><span class="line">+--------------------+---------+----------------------------------------------------------------+--------------+------+------------+</span><br><span class="line">| InnoDB             | DEFAULT | Supports transactions, row-level locking, and foreign keys     | YES          | YES  | YES        |</span><br><span class="line">| MRG_MYISAM         | YES     | Collection of identical MyISAM tables                          | NO           | NO   | NO         |</span><br><span class="line">| MEMORY             | YES     | Hash based, stored in memory, useful for temporary tables      | NO           | NO   | NO         |</span><br><span class="line">| BLACKHOLE          | YES     | &#x2F;dev&#x2F;null storage engine (anything you write to it disappears) | NO           | NO   | NO         |</span><br><span class="line">| MyISAM             | YES     | MyISAM storage engine                                          | NO           | NO   | NO         |</span><br><span class="line">| CSV                | YES     | CSV storage engine                                             | NO           | NO   | NO         |</span><br><span class="line">| ARCHIVE            | YES     | Archive storage engine                                         | NO           | NO   | NO         |</span><br><span class="line">| PERFORMANCE_SCHEMA | YES     | Performance Schema                                             | NO           | NO   | NO         |</span><br><span class="line">| FEDERATED          | NO      | Federated MySQL storage engine                                 | NULL         | NULL | NULL       |</span><br><span class="line">+--------------------+---------+----------------------------------------------------------------+--------------+------+------------+</span><br></pre></td></tr></table></figure>

<p>查询SQL慢的原因:</p>
<ul>
<li>SQL语句写的烂</li>
<li>索引没做好或者没生效</li>
<li>过多的join</li>
<li>服务器本身问题</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">执行顺序:from,on,where,group by,having,select distincat,order by,limit.</span><br></pre></td></tr></table></figure>

<h1 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h1><p>索引是为了更快的查找数据,底层大多数是B+树实现.但是索引会降低更新数据的速度,因为不仅要更改数据表,还要调整更新带来的键值变化后的索引信息.此外,索引也是一张表,保存了主键与索引字段信息,指向实体表记录.</p>
<p>聚簇索引和非聚簇索引:前者是id这样的主键索引,紧挨着排序.后者一般是自定义的索引,实际的数据并不是挨着的.以字典为例,是按照拼音排的,这种拼音索引就是聚簇索引,而笔画索引就不是,因为笔画一样的字并不挨在一起.</p>
<p>查看索引:<code>show index from 表</code></p>
<p>新建索引:<code>create index idx_字段名 on 表名(字段);</code>这是一种单值索引.除了这样单独写一个索引,创建表时也可以创建索引.单值索引只包含单个列,一个表可以有多个单值suoy.唯一索引:索引值必须唯一,但是允许有空值.主键索引:设置主键后会自动创建.复合索引:针对多个索引创建索引.</p>
<p>频繁查询的字段应该建索引;关联的外键应该建一个索引.</p>
<p>多个单值索引和复合索引应该优先使用后者,因为MySQL只会在多个单值索引中找一个最优的.</p>
<p>要排序的字段应该建索引.</p>
<p>查询中统计或者分组的字段要建索引.</p>
<p>对于是否要索引,有这样一把”尺子”来衡量:explain.用法是直接加载SQL语句之前:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">EXPLAIN</span> <span class="keyword">SELECT</span></span><br><span class="line">	d.*,</span><br><span class="line">	e.employee_id </span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">	departments d</span><br><span class="line">	<span class="keyword">LEFT</span> <span class="keyword">JOIN</span> employees e <span class="keyword">ON</span> d.department_id = e.department_id </span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">	e.employee_id <span class="keyword">IS</span> <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure>

<p>结果为:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">id	select_type	table	partitions	type	possible_keys	key			key_len	ref							rows	filtered	Extra</span><br><span class="line">1	SIMPLE		d					ALL																		27		100.00	</span><br><span class="line">1	SIMPLE		e					ref		dept_id_fk		dept_id_fk	5		myemployees.d.department_id	8		10.00		Using where; Not exists; Using index</span><br></pre></td></tr></table></figure>

<p>各字段意义:</p>
<p>id:这里是执行顺序,当id相同,则顺序是从上到下:当id不同(子查询),从id大的开始执行;既有一样的,又有不一样的,先按不一样的从大到小,再从上到下.一个id代表一个独立的查询,一句SQL的查询数越少越好.</p>
<p>select_type:</p>
<ul>
<li>SIMPLE:简单的select查询,不包含子查询或者union.</li>
<li>PRIMARY:查询中包含复杂的子查询,最外层的查询就是PRIMARY的.</li>
<li>DERIVED:在FROM列表中包含的子查询(衍生),Mysql会递归执行它们,把结果放在临时表.</li>
<li>SUBQUERY:在select或where中包含的子查询</li>
<li>DEPENDENT SUBQUERY:在select或where中包含的子查询,子查询位于外层.上面是等号,这里是in.</li>
<li>UNCACHEABLE SUBQUERY:不可缓存的子查询,查系统变量时,由于MySQL认为系统变量有可能被人更改不能缓存,所以这种查询放在子查询里就是不可缓存的子查询,<code>select @@lower_case_table_name from DUAL;</code>(DUAL是虚表,因为环境变量不存在表中)</li>
<li>UNION:若第二个select出现在UNION后则被标记为UNION</li>
<li>UNION RESULT:从UNION中获取结果的select</li>
</ul>
<p><strong>type</strong>:显示查询使用了何种类型.简要介绍六种:它们分别是all,index,range,ref,eq_ref，const。从左到右，它们的效率依次是增强的。撇开sql的具体应用环境以及其他因素，你应当尽量优化你的sql语句，使它的type尽量靠右，但实际运用中还是要综合考虑各个方面的。</p>
<p><strong>all</strong><br>这便是所谓的“全表扫描”，如果是展示一个数据表中的全部数据项，倒是觉得也没什么，如果是在一个查找数据项的sql中出现了all类型，那通常意味着你的sql语句处于一种最原生的状态，有很大的优化空间。</p>
<p>为什么这么说呢？因为all是一种非常暴力和原始的查找方法，非常的耗时而且低效。用all去查找数据就好比这样的一个情形：S学校有俩万人，我告诉你你给我找到小明，然后你怎么做呢！你当然是把全校俩万人挨个找一遍，即使你很幸运第一个人便找到了小明，但是你仍然不能停下，因为你无法确认是否有另外一个小明存在，直到你把俩万人找完为止。所以，基本所有情况，我们都要避免这样类型的查找，除非你不得不这样做。<br>以employee表为例，下面一种情形便是all类型的查找：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; explain select * from employee where &#96;no&#96; &#x3D; &#39;20150001&#39;;</span><br><span class="line">+----+-------------+----------+------+---------------+------+---------+------+------+-------------+</span><br><span class="line">| id | select_type | table    | type | possible_keys | key  | key_len | ref  | rows | Extra       |</span><br><span class="line">+----+-------------+----------+------+---------------+------+---------+------+------+-------------+</span><br><span class="line">|  1 | SIMPLE      | employee | ALL  | NULL          | NULL | NULL    | NULL |    5 | Using where |</span><br><span class="line">+----+-------------+----------+------+---------------+------+---------+------+------+-------------+</span><br></pre></td></tr></table></figure>
<p>这是因为no列既不是主键也不是索引，因此只能采用全表扫描来查找目标no。</p>
<p><strong>index</strong></p>
<p>遍历索引树查询.这种连接类型只是另外一种形式的全表扫描，只不过它的扫描顺序是按照索引的顺序。这种扫描根据索引然后回表取数据，和all相比，他们都是取得了全表的数据.</p>
<p><strong>range</strong></p>
<p>range指的是有范围的索引扫描，相对于index的全索引扫描，它有范围限制，因此要优于index。关于range比较容易理解，需要记住的是出现了range，则一定是基于索引的。同时除了显而易见的between，and以及’&gt;’,’&lt;’外，in和or也是索引范围扫描。</p>
<p><strong>ref</strong></p>
<p>出现该连接类型的条件是： 查找条件列使用了索引而且不为主键和unique。其实，意思就是虽然使用了索引，但该索引列的值并不唯一，有重复。这样即使使用索引快速查找到了第一条数据，仍然不能停止，要进行目标值附近的小范围扫描。但它的好处是它并不需要扫全表，因为索引是有序的，即便有重复值，也是在一个非常小的范围内扫描。下面为了演示这种情形，给employee表中的name列添加一个普通的key(值允许重复)</p>
<p><strong>ref_eq</strong></p>
<p>ref_eq 与 ref相比牛的地方是，它知道这种类型的查找结果集只有一个？什么情况下结果集只有一个呢！那便是使用了主键或者唯一性索引进行查找的情况，比如根据学号查找某一学校的一名同学，在没有查找前我们就知道结果一定只有一个，所以当我们首次查找到这个学号，便立即停止了查询。这种连接类型每次都进行着精确查询，无需过多的扫描，因此查找效率更高，当然列的唯一性是需要根据实际情况决定的。</p>
<p><strong>const</strong></p>
<p>通常情况下，如果将一个主键放置到where后面作为条件查询，mysql优化器就能把这次查询优化转化为一个常量。至于如何转化以及何时转化，这个取决于优化器。</p>
<p>possible_keys:可能用到的索引</p>
<p>key:实际使用的索引</p>
<p>key_len:索引长度(复合索引时,该值越大越好)</p>
<p>rows:实际扫描的行数</p>
<p>extra:</p>
<ul>
<li>NULL: 没有用到额外的附加条件</li>
<li>Using filesort：查询语句中包含了oder by语句，索引无法完成排序，数据量小的时候在内存中完成排序，否则在磁盘中完成排序。</li>
<li>Using temporary：使用到了临时表。</li>
<li>Using Index：使用覆盖索引进行数据返回(Innodb引擎才存在)，不会进行回表查找。</li>
<li>Using Where：查询语句中存在where范围查找，会回表查找数据。</li>
<li>Using Where Using Index：覆盖索引中存在范围查找，但是也不会进行回表，直接在索引上就能拿到数据。</li>
<li>Using Index Condition：跟Using Where Using Index有一些差别，它的查找用到了联合索引，查找的数据有不在索引中的字段，所以会进行回表查找数据。</li>
</ul>
<h2 id="单表索引优化"><a href="#单表索引优化" class="headerlink" title="单表索引优化"></a>单表索引优化</h2><p><code>create index idx_age on emp(age)</code>,为单表中某字段建索引,<code>select * from emp where emp.age=30</code>,性能显著提高.</p>
<p>如果查询时where后面有两个字段条件,可以为这两个字段建个索引,<code>create index idx_age_deptid on emp(age,deptid)</code></p>
<p>对于多个字段构成的复合索引,它的结构类似于技能树,age字段按照值的大小构成一棵树,每个值展开后又是一颗deptid的树.所以,最佳的索引命中策略是按照建索引时的顺序命中,如果没有按照顺序来,索引的性能也会变差.</p>
<p>在写SQL时用函数,计算,类型转换等操作,索引会失效.</p>
<p>对于范围查询字段,需要把该字段放在最后,如果放在前面,则后面的索引字段失效.</p>
<p>此外,is not null也会导致索引失效.</p>
<p>使用like做模糊查询时,例如查名字中间包含字母a这种的,首字母不确定也会导致索引失效.因为字符的索引是按照首字母顺序排的.</p>
<p>当查询名字时条件是123这样的数字,由于类型不一致,有类型转换,索引失效.</p>
<p>一般性建议:</p>
<ul>
<li>对于单键索引,尽量选择过滤性更好的字段</li>
<li>对于组合索引,尽量将过滤性好的字段放前面</li>
<li>选择组合索引时应该尽量选择包含当前query中where后更多字段的索引</li>
<li>若某个字段有范围查询应该放在最后</li>
</ul>
<h2 id="其它查询优化"><a href="#其它查询优化" class="headerlink" title="其它查询优化"></a>其它查询优化</h2><p>如果两个表关联查询,第一个表叫驱动表,第二个表叫被驱动表.第一个无论如何都会是全表扫描的.所以对第二个表的关键字建立索引是有效的.即在被驱动表建立索引是有效的.此外,如果采用inner join,将由MySQL决定哪个是被驱动表.决定依据就是那个表有索引,有索引的哪个会被当做被驱动表.</p>
<p>此外,子查询尽量不要放到被驱动表.因为子查询的虚拟表没法建索引优化.多表之间能直接关联的尽量直接关联,不用子查询.</p>
<p>关于Order by有几句口诀:无过滤,不索引(分组后没有过滤条件就不会使用索引),顺序错,必排序(order by后面的字段顺序和索引中顺序不一致),方向反,必排序(要么都是升序,要么都是降序).</p>
<p>而Group by与Order by类似,只是不遵循第一条原则,没有过滤也能用索引.</p>
<p>还有一种覆盖索引的手段:select到from之间的字段&lt;=索引字段+主键.</p>
<h2 id="查询截取分析"><a href="#查询截取分析" class="headerlink" title="查询截取分析"></a>查询截取分析</h2><h3 id="慢查询日志"><a href="#慢查询日志" class="headerlink" title="慢查询日志"></a>慢查询日志</h3><p><code>set global slow_query_log=1;</code>开启慢查询日志,<code>set global long_query_time=0.1;</code>设置超过0.1的sql语句视为慢查询.</p>
<h3 id="杀进程"><a href="#杀进程" class="headerlink" title="杀进程"></a>杀进程</h3><p><code>show processlist</code>显示查询进程.</p>
<h1 id="主从复制"><a href="#主从复制" class="headerlink" title="主从复制"></a>主从复制</h1><p>主从复制(也称 AB 复制)允许将来自一个MySQL数据库服务器(主服务器)的数据复制到一个或多个MySQL数据库服务器(从服务器)。</p>
<blockquote>
<p>复制是异步的 从站不需要永久连接以接收来自主站的更新。</p>
</blockquote>
<p>根据配置，您可以复制数据库中的所有数据库，所选数据库甚至选定的表。</p>
<p>MySQL中复制的优点包括：</p>
<ul>
<li>横向扩展解决方案 - 在多个从站之间分配负载以提高性能。在此环境中，所有写入和更新都必须在主服务器上进行。但是，读取可以在一个或多个从设备上进行。该模型可以提高写入性能（因为主设备专用于更新），同时显着提高了越来越多的从设备的读取速度。</li>
<li>数据安全性 - 因为数据被复制到从站，并且从站可以暂停复制过程，所以可以在从站上运行备份服务而不会破坏相应的主数据。</li>
<li>分析 - 可以在主服务器上创建实时数据，而信息分析可以在从服务器上进行，而不会影响主服务器的性能。</li>
<li>远程数据分发 - 您可以使用复制为远程站点创建数据的本地副本，而无需永久访问主服务器。</li>
</ul>
<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p><img src="/2020/07/13/MySQL-2/%E5%8E%9F%E7%90%86.png" alt="原理"></p>
<blockquote>
<p>前提是作为主服务器角色的数据库服务器必须开启二进制日志</p>
</blockquote>
<p>主服务器上面的任何修改都会通过自己的 I/O tread(I/O 线程)保存在二进制日志 Binary log 里面。</p>
<p>从服务器上面也启动一个 I/O thread，通过配置好的用户名和密码, 连接到主服务器上面请求读取二进制日志，然后把读取到的二进制日志写到本地的一个Realy log（中继日志）里面。</p>
<p>从服务器上面同时开启一个 SQL thread 定时检查 Realy log(这个文件也是二进制的)，如果发现有更新立即把更新的内容在本机的数据库上面执行一遍。</p>
<p>每个从服务器都会收到主服务器二进制日志的全部内容的副本。</p>
<p>从服务器设备负责决定应该执行二进制日志中的哪些语句。</p>
<p>除非另行指定，否则主从二进制日志中的所有事件都在从站上执行。</p>
<p>如果需要，您可以将从服务器配置为仅处理一些特定数据库或表的事件。</p>
<p>每个从站(从服务器)都会记录二进制日志坐标：</p>
<ul>
<li>文件名</li>
<li>文件中它已经从主站读取和处理的位置。</li>
</ul>
<p>由于每个从服务器都分别记录了自己当前处理二进制日志中的位置，因此可以断开从服务器的连接，重新连接然后恢复继续处理。</p>
<h2 id="一主多从"><a href="#一主多从" class="headerlink" title="一主多从"></a>一主多从</h2><p>如果一主多从的话，这时主库既要负责写又要负责为几个从库提供二进制日志。此时可以稍做调整，将二进制日志只给某一从，这一从再开启二进制日志并将自己的二进制日志再发给其它从。或者是干脆这个从不记录只负责将二进制日志转发给其它从，这样架构起来性能可能要好得多，而且数据之间的延时应该也稍微要好一些。工作原理图如下：<br><img src="/2020/07/13/MySQL-2/%E4%B8%80%E4%B8%BB%E5%A4%9A%E4%BB%8E.png" alt="一主多从"></p>
<h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><p>配置步骤：</p>
<ol>
<li>在主服务器上，您必须启用二进制日志记录并配置唯一的服务器ID。需要重启服务器。<br>编辑主服务器的配置文件my.cnf，添加如下内容:</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">log-bin&#x3D;&#x2F;var&#x2F;log&#x2F;mysql&#x2F;mysql-bin</span><br><span class="line">server-id&#x3D;1</span><br></pre></td></tr></table></figure>

<p>Windows上与之类似.server-id用来指定唯一id,log-bin指定位置和名字,binlog-do-db指定需要复制的数据库.(该数据库要等到主从复制完成后搭建).binlog_format指定格式:statement(默认),row,mixed.</p>
<p>Linux上还需要创建日志目录并赋予权限</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">shell&gt; mkdir &#x2F;var&#x2F;log&#x2F;mysql</span><br><span class="line">shell&gt; chown mysql.mysql &#x2F;var&#x2F;log&#x2F;mysql</span><br></pre></td></tr></table></figure>

<p>然后重启服务.</p>
<p><strong>注意</strong>：</p>
<p>如果省略server-id（或将其显式设置为默认值0），则主服务器拒绝来自从服务器的任何连接。</p>
<p>为了在使用带事务的InnoDB进行复制设置时尽可能提高持久性和一致性，<br>应该在master my.cnf文件中使用以下配置项：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">innodb_flush_log_at_trx_commit &#x3D; 1</span><br><span class="line">sync_binlog &#x3D; 1</span><br></pre></td></tr></table></figure>

<p>确保在主服务器上<code>skip_networking</code>选项处于OFF关闭状态, 这是默认值。<br>如果是启用的，则从站无法与主站通信，并且复制失败。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; show variables like &#39;%skip_networking%&#39;;</span><br><span class="line">+-----------------+-------+</span><br><span class="line">| Variable_name   | Value |</span><br><span class="line">+-----------------+-------+</span><br><span class="line">| skip_networking | OFF   |</span><br><span class="line">+-----------------+-------+</span><br><span class="line">1 row in set (0.00 sec)</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>应该创建一个专门用于复制数据的用户.每个从服务器需要使用MySQL主服务器上的用户名和密码连接到主站。</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">GRANT REPLICATION SLAVE ON *.* TO &#39;zhangsan&#39;@&#39;从机IP&#39; IDENTIFIED BY &#39;123456&#39;</span><br></pre></td></tr></table></figure>
<p><em>.</em>表示所有库所有表,从机IP可以用%,表示所有远程用户.</p>
<ol start="3">
<li>从机配置.在[mysqld]下添加server-id,要和主机不一样.</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">server-id&#x3D;2</span><br><span class="line">relay-log&#x3D;mysql-relay</span><br></pre></td></tr></table></figure>

<p>然后重启服务.</p>
<p>接下来用<code>show master status</code>查看主机状态.注意看Position(接入点)和mysqlbin.后面的数字.此时主机就不要再变动了.</p>
<ol start="4">
<li>从机连接主机</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">CHANGE MASTER TO MASTER_HOST&#x3D;&#39;主机IP&#39;,</span><br><span class="line">MASTER_USER&#x3D;&#39;zhangsan&#39;,</span><br><span class="line">MASTER_PASSWORD&#x3D;&#39;123456&#39;,</span><br><span class="line">MASTER_LOG_FILR&#x3D;&#39;mysqlbin.具体数字&#39;,MASTER_LOG_POS&#x3D;具体值;</span><br></pre></td></tr></table></figure>

<p>在从机的MySQL中执行上述语句.</p>
<ol start="5">
<li>启动</li>
</ol>
<p>在从机中启动</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">start slave;</span><br></pre></td></tr></table></figure>

<p>用<code>show slave status\G;</code>查看从机的执行状态.</p>
<p>如果从机上一步配置出现了问题可以使用<code>stop slave</code>和<code>reset master</code>重新配置.</p>
<h1 id="Mycat"><a href="#Mycat" class="headerlink" title="Mycat"></a>Mycat</h1><p>Mycat是数据库中间件.一端是Java程序,另一端是mycat.mycat可以用来做读写分离,数据分表(分库,分表),多数据源整合.</p>
<p>首先下载压缩吧,然后解压即可.然后修改配置文件:</p>
<ul>
<li>schema.xml,定义逻辑库(即Java程序认为的库,实际的库还是另一头的数据库),表,分片节点等内容</li>
<li>rule.xml,定义分片规则</li>
<li>server.xml,定义用户及系统相关变量,如端口</li>
</ul>
<p>先设置server.xml:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">user</span> <span class="attr">name</span>=<span class="string">"mycat"</span> <span class="attr">defaultAccount</span>=<span class="string">"true"</span>&gt;</span>//用户名</span><br><span class="line">		<span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span>&gt;</span>123456<span class="tag">&lt;/<span class="name">property</span>&gt;</span>//密码</span><br><span class="line">		<span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"schemas"</span>&gt;</span>TESTDB<span class="tag">&lt;/<span class="name">property</span>&gt;</span>//这里选一个schema.xml中的schema</span><br><span class="line">		<span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"defaultSchema"</span>&gt;</span>TESTDB<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!--No MyCAT Database selected 错误前会尝试使用该schema作为schema，不设置则为null,报错 --&gt;</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">&lt;!-- 表级 DML 权限设置 --&gt;</span></span><br><span class="line">		<span class="comment">&lt;!--</span></span><br><span class="line"><span class="comment">		&lt;privileges check="false"&gt;</span></span><br><span class="line"><span class="comment">				&lt;schema name="TESTDB" dml="0110" &gt;</span></span><br><span class="line"><span class="comment">						&lt;table name="tb01" dml="0000"&gt;&lt;/table&gt;</span></span><br><span class="line"><span class="comment">						&lt;table name="tb02" dml="1111"&gt;&lt;/table&gt;</span></span><br><span class="line"><span class="comment">				&lt;/schema&gt;</span></span><br><span class="line"><span class="comment">		&lt;/privileges&gt;</span></span><br><span class="line"><span class="comment">			--&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">user</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>接着配置schema.xml:</p>
<ul>
<li><p>schema标签<br><code>&lt;schema name=&quot;TESTDB&quot; checkSQLschema=&quot;false&quot; sqlMaxLimit=&quot;100&quot;&gt;&lt;/schema&gt;</code>schema 标签用于定义 MyCat 实例中的逻辑库，MyCat 可以有多个逻辑库，每个逻辑库都有自己的相关配置。可以使用 schema 标签来划分这些不同的逻辑库。</p>
</li>
<li><p>table标签</p>
</li>
</ul>
<p><code>&lt;table name=&quot;travelrecord&quot; dataNode=&quot;dn1,dn2,dn3&quot; rule=&quot;auto-sharding-long&quot; &gt;&lt;/table&gt;</code></p>
<p>Table 标签定义了 MyCat 中的逻辑表，所有需要拆分的表都需要在这个标签中定义。</p>
<p>table 标签的相关属性：</p>
<p><img src="/2020/07/13/MySQL-2/table.jpg" alt="table"></p>
<ul>
<li>childTable标签</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">table</span> <span class="attr">name</span>=<span class="string">"customer"</span> <span class="attr">primaryKey</span>=<span class="string">"ID"</span> <span class="attr">dataNode</span>=<span class="string">"dn1,dn2"</span> <span class="attr">rule</span>=<span class="string">"sharding-by-intfile"</span>&gt;</span> </span><br><span class="line">	<span class="tag">&lt;<span class="name">childTable</span> <span class="attr">name</span>=<span class="string">"c_a"</span> <span class="attr">primaryKey</span>=<span class="string">"ID"</span> <span class="attr">joinKey</span>=<span class="string">"customer_id"</span> <span class="attr">parentKey</span>=<span class="string">"id"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>childTable 标签用于定义 E-R 分片的子表。通过标签上的属性与父表进行关联。</p>
<p><img src="/2020/07/13/MySQL-2/childtable.jpg" alt="childtable"></p>
<ul>
<li>dataNode标签</li>
</ul>
<p>dataNode 标签定义了 MyCat 中的数据节点，也就是我们通常说所的数据分片。一个 dataNode 标签就是一个独立的数据分片。</p>
<p><code>&lt;dataNode name=&quot;dNode1&quot;  dataHost=&quot;dHost128&quot;  database=&quot;db1&quot; &gt;&lt;/dataNode&gt;</code></p>
<p>示例中所表述的意思为：使用名字为 dHost128数据库实例上的 db1 物理数据库，这就组成一个数据分片，最后，我们使用名字 dNode1标识这个分片。<br>dataNode 标签的相关属性：</p>
<p><img src="/2020/07/13/MySQL-2/dataNode.jpg" alt="dataNode"></p>
<ul>
<li>dataHost标签</li>
</ul>
<p>该标签定义了具体的数据库实例、读写分离配置和心跳语句。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dataHost</span> <span class="attr">name</span>=<span class="string">"localhost1"</span> <span class="attr">maxCon</span>=<span class="string">"1000"</span> <span class="attr">minCon</span>=<span class="string">"10"</span> <span class="attr">balance</span>=<span class="string">"0"</span> <span class="attr">writeType</span>=<span class="string">"0"</span> <span class="attr">dbType</span>=<span class="string">"mysql"</span> <span class="attr">dbDriver</span>=<span class="string">"native"</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">heartbeat</span>&gt;</span>select user()<span class="tag">&lt;/<span class="name">heartbeat</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">writeHost</span>  <span class="attr">host</span>=<span class="string">"hostM1"</span>  <span class="attr">url</span>=<span class="string">"localhost:3306"</span>  <span class="attr">user</span>=<span class="string">"root"</span> <span class="attr">password</span>=<span class="string">"123456"</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">writeHost</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dataHost</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>dataHost 标签的相关属性：</p>
<p><img src="/2020/07/13/MySQL-2/dataHost.jpg" alt="dataHost"></p>
<p>里面有一个非常重要的属性:balance,设置负载均衡类型.</p>
<ul>
<li>writeHost和readHost标签</li>
</ul>
<p>这两个标签都指定后端数据库的相关配置，用于实例化后端连接池。唯一不同的是，writeHost 指定写实例、readHost 指定读实例。</p>
<p>属性说明：</p>
<p><img src="/2020/07/13/MySQL-2/writeread.jpg" alt="writeread"></p>
<p>下面是一个最简单的schema配置:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">mycat</span>:schema <span class="meta-keyword">SYSTEM</span> <span class="meta-string">"schema.dtd"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mycat:schema</span> <span class="attr">xmlns:mycat</span>=<span class="string">"http://io.mycat/"</span>&gt;</span></span><br><span class="line">           <span class="comment">&lt;!--逻辑库    name名称,   checkSQLschema         sqlMaxLimit 末尾是否要加 limit xxx--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">schema</span> <span class="attr">name</span>=<span class="string">"TESTDB"</span> <span class="attr">checkSQLschema</span>=<span class="string">"false"</span> <span class="attr">sqlMaxLimit</span>=<span class="string">"100"</span> <span class="attr">dataNode</span>=<span class="string">"dn1"</span>&gt;</span> <span class="tag">&lt;/<span class="name">schema</span>&gt;</span></span><br><span class="line">            <span class="comment">&lt;!--逻辑库    name名称,   dataHost 引用的哪个dataHost      database:对应mysql的database--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataNode</span> <span class="attr">name</span>=<span class="string">"dn1"</span> <span class="attr">dataHost</span>=<span class="string">"localhost1"</span> <span class="attr">database</span>=<span class="string">"db1"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataHost</span> <span class="attr">name</span>=<span class="string">"localhost1"</span> <span class="attr">maxCon</span>=<span class="string">"1000"</span> <span class="attr">minCon</span>=<span class="string">"10"</span> <span class="attr">balance</span>=<span class="string">"0"</span></span></span><br><span class="line"><span class="tag">                          <span class="attr">writeType</span>=<span class="string">"0"</span> <span class="attr">dbType</span>=<span class="string">"mysql"</span> <span class="attr">dbDriver</span>=<span class="string">"native"</span> <span class="attr">switchType</span>=<span class="string">"1"</span>  <span class="attr">slaveThreshold</span>=<span class="string">"100"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">heartbeat</span>&gt;</span>select user()<span class="tag">&lt;/<span class="name">heartbeat</span>&gt;</span></span><br><span class="line">                <span class="comment">&lt;!-- can have multi write hosts --&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">writeHost</span> <span class="attr">host</span>=<span class="string">"hostM1"</span> <span class="attr">url</span>=<span class="string">"localhost:3306"</span> <span class="attr">user</span>=<span class="string">"root"</span></span></span><br><span class="line"><span class="tag">                                   <span class="attr">password</span>=<span class="string">"123456"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;/<span class="name">writeHost</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dataHost</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mycat:schema</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>接着是rule.xml配置</p>
<p>rule.xml 里面就定义了我们对表进行拆分所涉及到的规则定义。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">tableRule</span> <span class="attr">name</span>=<span class="string">"date"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">rule</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">columns</span>&gt;</span>indate<span class="tag">&lt;/<span class="name">columns</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">algorithm</span>&gt;</span>sharding-by-date<span class="tag">&lt;/<span class="name">algorithm</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">rule</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">tableRule</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">function</span> <span class="attr">name</span>=<span class="string">"sharding-by-date"</span> <span class="attr">class</span>=<span class="string">"io.mycat.route.function.PartitionByDate"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dateFormat"</span>&gt;</span>yyyy-MM-dd<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"sBeginDate"</span>&gt;</span>2019-01-01<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"sPartionDay"</span>&gt;</span>30<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">function</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>tableRule标签</p>
<p>属性 name指定唯一的名字，用于标识不同的表规则。</p>
<p>内嵌的 rule 标签则指定对物理表中的哪一列进行拆分和使用什么路由算法。</p>
<p><img src="/2020/07/13/MySQL-2/rule.jpg" alt="rule"></p>
<p>function标签</p>
<p>name属性：指定算法名字</p>
<p>class属性：制定路由算法的类名</p>
<p>property标签：具体算法所需要用到的一些属性</p>
<p>配置完成后,使用mysql登录命令分别登录两台机器测试连接情况.</p>
<p>接着启动:控制台启动:在mycat/bin目录下mycat console,后台启动:在该目录mycat start</p>
<p>启动后直接使用MySQL的登录命令登录,账户和密码是server.xml中配置的,另外端口号有所区别.9066是mycat的管理端口,8066是数据使用的窗口.</p>
<p>进入后<code>show databases;</code>发现会只有一个逻辑库.</p>
<h2 id="读写分离"><a href="#读写分离" class="headerlink" title="读写分离"></a>读写分离</h2><p>在schema.xml中设置:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">mycat</span>:schema <span class="meta-keyword">SYSTEM</span> <span class="meta-string">"schema.dtd"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mycat:schema</span> <span class="attr">xmlns:mycat</span>=<span class="string">"http://io.mycat/"</span>&gt;</span></span><br><span class="line"> </span><br><span class="line"><span class="tag">&lt;<span class="name">schema</span> <span class="attr">name</span>=<span class="string">"TESTDB"</span> <span class="attr">checkSQLschema</span>=<span class="string">"false"</span> <span class="attr">sqlMaxLimit</span>=<span class="string">"100"</span> <span class="attr">dataNode</span>=<span class="string">"dn1"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">schema</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataNode</span> <span class="attr">name</span>=<span class="string">"dn1"</span> <span class="attr">dataHost</span>=<span class="string">"host1"</span> <span class="attr">database</span>=<span class="string">"db1"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataHost</span> <span class="attr">name</span>=<span class="string">"host1"</span> <span class="attr">maxCon</span>=<span class="string">"1000"</span> <span class="attr">minCon</span>=<span class="string">"10"</span> <span class="attr">balance</span>=<span class="string">"3"</span></span></span><br><span class="line"><span class="tag">                          <span class="attr">writeType</span>=<span class="string">"0"</span> <span class="attr">dbType</span>=<span class="string">"mysql"</span> <span class="attr">dbDriver</span>=<span class="string">"native"</span> <span class="attr">switchType</span>=<span class="string">"1"</span> <span class="attr">slaveThreshold</span>=<span class="string">"100"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">heartbeat</span>&gt;</span>select user()<span class="tag">&lt;/<span class="name">heartbeat</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">writeHost</span> <span class="attr">host</span>=<span class="string">"hostm1"</span> <span class="attr">url</span>=<span class="string">"192.168.67.1:3306"</span> <span class="attr">user</span>=<span class="string">"root"</span></span></span><br><span class="line"><span class="tag">                                   <span class="attr">password</span>=<span class="string">"123123"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--读库（从库）的配置 --&gt;</span></span><br><span class="line">                       <span class="tag">&lt;<span class="name">readHost</span> <span class="attr">host</span>=<span class="string">"hosts1"</span> <span class="attr">url</span>=<span class="string">"192.168.67.131:3306"</span> <span class="attr">user</span>=<span class="string">"root"</span></span></span><br><span class="line"><span class="tag">                                   <span class="attr">password</span>=<span class="string">"123123"</span>&gt;</span></span><br><span class="line">                       <span class="tag">&lt;/<span class="name">readHost</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;/<span class="name">writeHost</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dataHost</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mycat:schema</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>现在只有一个dataNode,名字为dn1.修改balance.</p>
<p>具体架构如下:<br><img src="/2020/07/13/MySQL-2/%E7%BB%93%E6%9E%84.jpg" alt="结构"></p>
<h2 id="分库"><a href="#分库" class="headerlink" title="分库"></a>分库</h2><p>分库有水平分库和垂直分库两类,下面演示的是垂直分库.</p>
<p>重要原则:要分库的表不能和另外的库里的表有关联.因为在不同机器上的不同库中的表不能join关联.</p>
<p>设置分库:将库中的某一张表分离到另一个库中.</p>
<p>具体配置如下:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">mycat:schema</span> <span class="attr">xmlns:mycat</span>=<span class="string">"http://io.mycat/"</span>&gt;</span></span><br><span class="line"> </span><br><span class="line">        <span class="tag">&lt;<span class="name">schema</span> <span class="attr">name</span>=<span class="string">"TESTDB"</span> <span class="attr">checkSQLschema</span>=<span class="string">"false"</span> <span class="attr">sqlMaxLimit</span>=<span class="string">"100"</span> <span class="attr">dataNode</span>=<span class="string">"dn1"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">table</span> <span class="attr">name</span>=<span class="string">"customer"</span> <span class="attr">dataNode</span>=<span class="string">"dn2"</span> &gt;</span><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">schema</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataNode</span> <span class="attr">name</span>=<span class="string">"dn1"</span> <span class="attr">dataHost</span>=<span class="string">"host1"</span> <span class="attr">database</span>=<span class="string">"test_mc"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataNode</span> <span class="attr">name</span>=<span class="string">"dn2"</span> <span class="attr">dataHost</span>=<span class="string">"host2"</span> <span class="attr">database</span>=<span class="string">"test_sm"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataHost</span> <span class="attr">name</span>=<span class="string">"host1"</span> <span class="attr">maxCon</span>=<span class="string">"1000"</span> <span class="attr">minCon</span>=<span class="string">"10"</span> <span class="attr">balance</span>=<span class="string">"3"</span></span></span><br><span class="line"><span class="tag">                          <span class="attr">writeType</span>=<span class="string">"0"</span> <span class="attr">dbType</span>=<span class="string">"mysql"</span> <span class="attr">dbDriver</span>=<span class="string">"native"</span> <span class="attr">switchType</span>=<span class="string">"1"</span>  <span class="attr">slaveThreshold</span>=<span class="string">"100"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">heartbeat</span>&gt;</span>select user()<span class="tag">&lt;/<span class="name">heartbeat</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">writeHost</span> <span class="attr">host</span>=<span class="string">"hostm1"</span> <span class="attr">url</span>=<span class="string">"192.168.67.1:3306"</span> <span class="attr">user</span>=<span class="string">"root"</span></span></span><br><span class="line"><span class="tag">                                   <span class="attr">password</span>=<span class="string">"123123"</span>&gt;</span></span><br><span class="line">                       <span class="tag">&lt;<span class="name">readHost</span> <span class="attr">host</span>=<span class="string">"hosts1"</span> <span class="attr">url</span>=<span class="string">"192.168.67.131:3306"</span> <span class="attr">user</span>=<span class="string">"root"</span></span></span><br><span class="line"><span class="tag">                                   <span class="attr">password</span>=<span class="string">"123123"</span>&gt;</span></span><br><span class="line">                       <span class="tag">&lt;/<span class="name">readHost</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;/<span class="name">writeHost</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dataHost</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataHost</span> <span class="attr">name</span>=<span class="string">"host2"</span> <span class="attr">maxCon</span>=<span class="string">"1000"</span> <span class="attr">minCon</span>=<span class="string">"10"</span> <span class="attr">balance</span>=<span class="string">"0"</span></span></span><br><span class="line"><span class="tag">                          <span class="attr">writeType</span>=<span class="string">"0"</span> <span class="attr">dbType</span>=<span class="string">"mysql"</span> <span class="attr">dbDriver</span>=<span class="string">"native"</span> <span class="attr">switchType</span>=<span class="string">"1"</span>  <span class="attr">slaveThreshold</span>=<span class="string">"100"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">heartbeat</span>&gt;</span>select user()<span class="tag">&lt;/<span class="name">heartbeat</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">writeHost</span> <span class="attr">host</span>=<span class="string">"hostm2"</span> <span class="attr">url</span>=<span class="string">"192.168.67.1:3306"</span> <span class="attr">user</span>=<span class="string">"root"</span></span></span><br><span class="line"><span class="tag">                                   <span class="attr">password</span>=<span class="string">"123123"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;/<span class="name">writeHost</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dataHost</span>&gt;</span></span><br><span class="line"> </span><br><span class="line"><span class="tag">&lt;/<span class="name">mycat:schema</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>总体逻辑是:将customer表放到另一个库中,即dataNode名字为dn2的地方,该数据节点信息放在host2中.并且dn1节点设置读写分离.</p>
<p><img src="/2020/07/13/MySQL-2/%E5%88%86%E5%BA%93.jpg" alt="分库"></p>
<blockquote>
<p>图中没画dn1的readHost</p>
</blockquote>
<p><strong>当配置完成后再去创建相关的库.</strong>此外,建表的操作要在mycat中进行,mycat会拦截建表操作,发现建的是customer表就会在dn2(test_sm)中创建(假定为Linux),其他表就在dn1(test_mc)中创建.如果绕过mycat直接去查看两个数据库,则会在test_sm中看到只有customer表.<strong>所谓的分库是对于逻辑库来说,一个逻辑上的库的表放在物理上的不同库中</strong></p>
<p>而水平分库则是将一张大表拆分到两个数据库中.</p>
<h2 id="分表"><a href="#分表" class="headerlink" title="分表"></a>分表</h2><p>分表也分为水平分表和垂直分表.</p>
<p>对于order订单表进行分表操作,首先在schema.xml的schema标签的table子标签添加这张表,并且dataNode写明表被分到哪去.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">schema</span> <span class="attr">name</span>=<span class="string">"TESTDB"</span> <span class="attr">checkSQLschema</span>=<span class="string">"false"</span> <span class="attr">sqlMaxLimit</span>=<span class="string">"100"</span> <span class="attr">dataNode</span>=<span class="string">"dn1"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">table</span> <span class="attr">name</span>=<span class="string">"customer"</span> <span class="attr">dataNode</span>=<span class="string">"dn2"</span> &gt;</span><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">table</span> <span class="attr">name</span>=<span class="string">"orders"</span> <span class="attr">dataNode</span>=<span class="string">"dn1,dn2"</span>  <span class="attr">rule</span>=<span class="string">"mod_rule"</span> &gt;</span><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">schema</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>注意还要指定规则.根据customer_id来分,具体是用customer_id对节点数取模,将这张表里的数据放在几个节点内.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"> </span><br><span class="line"><span class="tag">&lt;<span class="name">tableRule</span> <span class="attr">name</span>=<span class="string">"mod_rule"</span>&gt;</span></span><br><span class="line">             <span class="tag">&lt;<span class="name">rule</span>&gt;</span>//根据customer_id分片</span><br><span class="line">				<span class="tag">&lt;<span class="name">columns</span>&gt;</span>customer_id<span class="tag">&lt;/<span class="name">columns</span>&gt;</span></span><br><span class="line">				//分片算法为取模</span><br><span class="line">				<span class="tag">&lt;<span class="name">algorithm</span>&gt;</span>mod-long<span class="tag">&lt;/<span class="name">algorithm</span>&gt;</span></span><br><span class="line">               <span class="tag">&lt;/<span class="name">rule</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;/<span class="name">tableRule</span>&gt;</span></span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="tag">&lt;<span class="name">function</span> <span class="attr">name</span>=<span class="string">"mod-long"</span> <span class="attr">class</span>=<span class="string">"io.mycat.route.function.PartitionByMod"</span>&gt;</span></span><br><span class="line">                <span class="comment">&lt;!-- how many data nodes --&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"count"</span>&gt;</span>2<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">function</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>配置完成后在dn1和dn2中新建表(直接在物理库中,不是mycat中).</p>
<p>插入数据则在mycat中进行.</p>
<p>上面的分表严格来说叫水平分库分表,因为表的数据被拆分后放在不同的数据库中.</p>
<p>现在有一张新表,订单详情表,该表含有order表的id作为外键.订单详情表作为订单表的”小弟”.</p>
<p>如果在dn2中新建这张表并且插入数据,然后查询时join这张表和订单表,结果是出现错误.因为mycat拦截查询后,在dn2中有一部分订单表和全部订单详情表,可以正常查到,而在dn1中只有另一部分订单表,查不到,报错,因此mycat就会返回错误信息.</p>
<p>所有订单详情表也要分表,而且这个表和订单表关系紧密,所以要用childtable标签把该表作为订单表的子表.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">table</span> <span class="attr">name</span>=<span class="string">"orders"</span> <span class="attr">dataNode</span>=<span class="string">"dn1,dn2"</span>  <span class="attr">rule</span>=<span class="string">"mod_rule"</span> &gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">childTable</span> <span class="attr">name</span>=<span class="string">"orders_detail"</span> <span class="attr">primaryKey</span>=<span class="string">"id"</span> <span class="attr">joinKey</span>=<span class="string">"order_id"</span> <span class="attr">parentKey</span>=<span class="string">"id"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>primaryKey是该表主键,joinKey是关联字段,parentKey是父表主键.</p>
<p>然后在dn1中建另一个订单详情表即可.这种有关联的分表叫做ER表.</p>
<p>还有一种全局表.比如订单状态字典表.该表数据量不大,但是每个数据节点都要有一个,不然就会某个节点的订单表无法查询订单状态.设定为全局的表，会直接复制给每个数据库一份，所有写操作也会同步给多个库。所以全局表一般不能是大数据表或者更新频繁的表.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">table</span> <span class="attr">name</span>=<span class="string">"dict_order_type"</span> <span class="attr">dataNode</span>=<span class="string">"dn1,dn2"</span> <span class="attr">type</span>=<span class="string">"global"</span> &gt;</span><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>配置一个global类型即可.</p>
<p>还有一种垂直分表,一般在数据库设计之初就进行,把一些字段分为两部分,不放在一个表里.</p>
<p>总结:分库就是把数据拆分到多个数据库,分表就是把一个表拆为多个表;垂直拆分就是拆开表里的字段分开放或者拆开库里的表分开放,水平拆分就是拆分表里的数据.</p>
<h2 id="全局序列"><a href="#全局序列" class="headerlink" title="全局序列"></a>全局序列</h2><p>id这种应该自增,但是分表后不能自增的字段,需要全局序列.这个序列生成有多种方式,其中本地文件和时间戳方式不推荐.可以使用数据库方式:</p>
<p>利用数据库一个表 来进行计数累加。但是并不是每次生成序列都读写数据库，这样效率太低</p>
<p>mycat会预加载一部分号段到mycat的内存中，这样大部分读写序列都是在内存中完成的。<br>如果内存中的号段用完了 mycat会再向数据库要一次。</p>
<p>问：那如果mycat崩溃了 ，那内存中的序列岂不是都没了？</p>
<p>是的。如果是这样，那么mycat启动后会向数据库申请新的号段，原有号段会弃用。<br>也就是说如果mycat重启，那么损失是当前的号段没用完的号码，但是不会因此出现主键重复。</p>
<p>先在某个数据节点,如db2中建表:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> MYCAT_SEQUENCE (</span><br><span class="line">    <span class="keyword">NAME</span> <span class="built_in">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span>,</span><br><span class="line">    current_value <span class="built_in">INT</span> <span class="keyword">NOT</span> <span class="literal">NULL</span>,</span><br><span class="line">    <span class="keyword">increment</span> <span class="built_in">INT</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="keyword">DEFAULT</span> <span class="number">100</span>,</span><br><span class="line">    PRIMARY <span class="keyword">KEY</span>(<span class="keyword">NAME</span>)</span><br><span class="line">) <span class="keyword">ENGINE</span> = <span class="keyword">INNODB</span>;</span><br></pre></td></tr></table></figure>

<p>然后执行官方提供的三个函数:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">DELIMITER $$</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">FUNCTION</span> mycat_seq_currval(seq_name <span class="built_in">VARCHAR</span>(<span class="number">50</span>)) <span class="keyword">RETURNS</span> <span class="built_in">VARCHAR</span>(<span class="number">64</span>) <span class="keyword">DETERMINISTIC</span> <span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">DECLARE</span> retval <span class="built_in">VARCHAR</span>(<span class="number">64</span>);</span><br><span class="line"><span class="keyword">SET</span> retval = <span class="string">"-999999999,null"</span>;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">CONCAT</span>(</span><br><span class="line">        <span class="keyword">CAST</span>(current_value <span class="keyword">AS</span> <span class="built_in">CHAR</span>),</span><br><span class="line">        <span class="string">","</span>,</span><br><span class="line">        <span class="keyword">CAST</span>(<span class="keyword">increment</span> <span class="keyword">AS</span> <span class="built_in">CHAR</span>)</span><br><span class="line">    ) <span class="keyword">INTO</span> retval</span><br><span class="line"><span class="keyword">FROM</span> MYCAT_SEQUENCE</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">NAME</span> = seq_name;</span><br><span class="line">RETURN retval;</span><br><span class="line"><span class="keyword">END</span> $$ DELIMITER;</span><br><span class="line"></span><br><span class="line">DELIMITER $$ </span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">FUNCTION</span> mycat_seq_setval(seq_name <span class="built_in">VARCHAR</span>(<span class="number">50</span>), <span class="keyword">VALUE</span> <span class="built_in">INTEGER</span>) <span class="keyword">RETURNS</span> <span class="built_in">VARCHAR</span>(<span class="number">64</span>) <span class="keyword">DETERMINISTIC</span> <span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">UPDATE</span> MYCAT_SEQUENCE</span><br><span class="line"><span class="keyword">SET</span> current_value = <span class="keyword">VALUE</span></span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">NAME</span> = seq_name;</span><br><span class="line">RETURN mycat_seq_currval(seq_name);</span><br><span class="line"><span class="keyword">END</span> $$ DELIMITER;</span><br><span class="line"></span><br><span class="line">DELIMITER $$ </span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">FUNCTION</span> mycat_seq_nextval(seq_name <span class="built_in">VARCHAR</span>(<span class="number">50</span>)) <span class="keyword">RETURNS</span> <span class="built_in">VARCHAR</span>(<span class="number">64</span>) <span class="keyword">DETERMINISTIC</span> <span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">UPDATE</span> MYCAT_SEQUENCE</span><br><span class="line"><span class="keyword">SET</span> current_value = current_value + <span class="keyword">increment</span></span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">NAME</span> = seq_name;</span><br><span class="line">RETURN mycat_seq_currval(seq_name);</span><br><span class="line"><span class="keyword">END</span> $$ DELIMITER;</span><br></pre></td></tr></table></figure>

<p>上述操作完成后执行<code>INSERT INTO MYCAT_SEQUENCE(NAME,current_value,increment) VALUES (&#39;ORDERS&#39;,400000,100);</code>,三个值依次代表全局序列名字,起始值,每次给mycat多少.</p>
<p>然后配置mycat,首先配置sequence_db_conf.properties</p>
<p><img src="/2020/07/13/MySQL-2/%E5%BA%8F%E5%88%97.jpg" alt="序列"></p>
<p>Orders=dn1表示这个全局序列在哪个数据节点.</p>
<p>接着在server.xml中告诉mycat选用的是数据库方式.数字1单表数据库方式.</p>
<p><img src="/2020/07/13/MySQL-2/%E6%96%B9%E5%BC%8F.jpg" alt="方式"></p>
<p>接着重启即可.</p>
]]></content>
      <tags>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV初探</title>
    <url>/2018/07/14/OpenCV%E5%88%9D%E6%8E%A2/</url>
    <content><![CDATA[<p>&emsp;暑假开始了，这次申请了留校，所以将有大把时间。初步打算如下：补完java和c++知识，Opencv入门，复习提高算法，加深Python知识，可谓是任务繁重。加油。闲言少叙，今天是OpenCV初步入门。</p>
<a id="more"></a>

<h1 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h1><ul>
<li>下载<blockquote>
<p>使用OpenCV3和VS2017</p>
</blockquote>
</li>
<li>设置环境变量<blockquote>
<p>注意，新版本的build文件夹中不存在网上部分教程里的x86和x64文件夹，只有一个x64，选择里面VC的版本视vs的情况而定，2017应选择vc15。</p>
</blockquote>
</li>
<li>工程包含（include）目录的配置：<blockquote>
<p><del>可以在解决方案资源管理器项目标题处右击或者vs视图-&gt;其它窗口-&gt;属性管理器打开属性相关设置，没有区别。注意不要学网上有的教程那样对于release，debug和win32，x64单独设置（前文说了，新版本只有一个x64的文件夹）。正确的做法是在解决方案资源管理器空白处右击设置整个项目的属性或者在下图中的项目名位置处右击，设置属性。设置包括包含目录，库目录，链接器三部分。注意，链接器在新版本中只需添加两个了（被浓缩了），位于库目录之内，有d的是debug的，没有的是release的，两个都要加，debug在前，即优先支持debug。</del><br></p>
<p>Update：经测试，上述方法会有几率引发错误。稳妥的办法是在属性管理器中针对release和debug的x64版本分别设置属性（不带带d和带d）。</p>
<p><img src="/2018/07/14/OpenCV%E5%88%9D%E6%8E%A2/%E5%B1%9E%E6%80%A7.png" alt="属性设置"></p>
</blockquote>
</li>
</ul>
<blockquote>
<p>Tips:项目属性页设置必须是选择x64，因为目前压根就没有x86的相关文件存在，不要设置错了。运行时也要选择x64。后文将会使用CMake编译处x86文件。</p>
</blockquote>
<p>&emsp;下面是测试代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;opencv2/opencv.hpp&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> cv;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Mat img = imread(<span class="string">"迷路帖.png"</span>);<span class="comment">//Mat为图像存储数据结构类</span></span><br><span class="line">    imshow(<span class="string">"载入的图片"</span>, img);</span><br><span class="line">    waitKey(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;下面的代码进行了“腐蚀”操作，用图片的暗色部分替换掉高亮部分。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;opencv2/highgui/highgui.hpp&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;opencv2/imgproc/imgproc.hpp&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> cv;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Mat srcImage = imread(<span class="string">"1.jpg"</span>);</span><br><span class="line">    imshow(<span class="string">"原图"</span>, srcImage);</span><br><span class="line">    Mat element = getStructuringElement(MORPH_RECT, Size(<span class="number">15</span>, <span class="number">15</span>));<span class="comment">//该函数的返回值为指定形状和尺寸的结构元素</span></span><br><span class="line">    Mat dstImage;</span><br><span class="line">    erode(srcImage, dstImage, element);<span class="comment">//进行腐蚀操作</span></span><br><span class="line">    imshow(<span class="string">"效果图"</span>, dstImage);</span><br><span class="line">    waitKey(<span class="number">0</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;<strong>经测试，这里不能使用png图片。</strong></p>
<p>下面的代码进行了均值滤波操作（blur函数），即模糊操作。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;opencv2/highgui/highgui.hpp&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;opencv2/imgproc/imgproc.hpp&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> cv;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Mat srcImage = imread(<span class="string">"1.jpg"</span>);</span><br><span class="line">    imshow(<span class="string">"原图"</span>, srcImage);</span><br><span class="line">    Mat dstImage;</span><br><span class="line">    blur(srcImage, dstImage, Size(<span class="number">7</span>, <span class="number">7</span>));</span><br><span class="line">    imshow(<span class="string">"效果图"</span>, dstImage);</span><br><span class="line">    waitKey(<span class="number">0</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>&emsp;经测试，这里可以使用png图片。</strong></p>
<p>&emsp;下面的代码进行了边缘检测。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;opencv2/opencv.hpp&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;opencv2/imgproc/imgproc.hpp&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> cv;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Mat srcImage = imread(<span class="string">"迷路帖.png"</span>);</span><br><span class="line">    imshow(<span class="string">"原图"</span>, srcImage);</span><br><span class="line">    Mat edge, grayImage;</span><br><span class="line">    cvtColor(srcImage, grayImage, CV_BGR2GRAY);<span class="comment">//将原图转为灰度图像</span></span><br><span class="line">    blur(grayImage, edge, Size(<span class="number">3</span>, <span class="number">3</span>));<span class="comment">//降噪</span></span><br><span class="line">    imshow(<span class="string">"灰度图"</span>, edge);</span><br><span class="line">    Canny(edge, edge, <span class="number">3</span>, <span class="number">9</span>, <span class="number">3</span>);</span><br><span class="line">    imshow(<span class="string">"效果图"</span>, edge);</span><br><span class="line">    waitKey(<span class="number">0</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;下面的代码将视频的每一帧读取出来,当waitkey较小时，效果就是播放视频。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;opencv2/opencv.hpp&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> cv;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">VideoCapture <span class="title">capture</span><span class="params">(<span class="string">"1.avi"</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        Mat frame;</span><br><span class="line">        capture &gt;&gt; frame;</span><br><span class="line">        imshow(<span class="string">"读取视频"</span>, frame);</span><br><span class="line">        waitKey(<span class="number">30</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>Mybatis</title>
    <url>/2020/07/03/Mybatis/</url>
    <content><![CDATA[<hr>
<p>title:Mybatis<br>date:2020-07-0318:00:41<br>tags:Mybatis</p>
<hr>
<p>Mybatis使用.</p>
<a id="more"></a>

<p>每个基于MyBatis的应用都是以一个SqlSessionFactory的实例为核心的。SqlSessionFactory的实例可以通过SqlSessionFactoryBuilder获得。而SqlSessionFactoryBuilder则可以从XML配置文件或一个预先配置的Configuration实例来构建出SqlSessionFactory实例。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> &#123;<span class="comment">//放在静态代码块,保证先得到一个SqlSessionFactory</span></span><br><span class="line">    <span class="comment">//获取SqlSessionFactory</span></span><br><span class="line">    String resource=<span class="string">"mybatis-config.xml"</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        InputStream inputStream = Resources.getResourceAsStream(resource);</span><br><span class="line">        SqlSessionFactory sqlSessionFactory = <span class="keyword">new</span> SqlSessionFactoryBuilder().build(inputStream);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SqlSession <span class="title">getSqlSession</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> sqlSessionFactory.openSession();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>先准备一个pojo,然后写UserDao接口,在该接口中规定一些方法.原来需要写BaseDao提供基本的curd方法,然后在UserDaoImpl中调用这些方法实现Dao接口中的方法.现在只需要一个UserMapper.xml来写sql语句即可:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">mapper</span></span></span><br><span class="line"><span class="meta">        <span class="meta-keyword">PUBLIC</span> <span class="meta-string">"-//mybatis.org//DTD Mapper 3.0//EN"</span></span></span><br><span class="line"><span class="meta">        <span class="meta-string">"http://mybatis.org/dtd/mybatis-3-mapper.dtd"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--命名空间绑定一个对应的Dao(Mapper接口)--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mapper</span> <span class="attr">namespace</span>=<span class="string">"com.company.dao.UserDao"</span>&gt;</span></span><br><span class="line">   <span class="comment">&lt;!-- id为方法名,这里返回值类型不是集合,而是集合的泛型--&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getUserList"</span> <span class="attr">resultType</span>=<span class="string">"com.company.pojo.User"</span>&gt;</span></span><br><span class="line">       select * from test.user</span><br><span class="line">   <span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mapper</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后回到mybatis-config中注册这个mapper:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">mappers</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mapper</span> <span class="attr">resource</span>=<span class="string">"com/company/dao/UserMapper.xml"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mappers</span>&gt;</span></span><br><span class="line">​```xml</span><br><span class="line"></span><br><span class="line">此外,由于mapper的xml文件是放在Java目录下,maven有着约定大于配置的规则(即将resources文件夹里的内容才导出打包).因此要在pom中设置一下:</span><br><span class="line"></span><br><span class="line">​``` xml</span><br><span class="line"><span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">resources</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">resource</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">directory</span>&gt;</span>src/main/resources<span class="tag">&lt;/<span class="name">directory</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">includes</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">include</span>&gt;</span>**/*.properties<span class="tag">&lt;/<span class="name">include</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">include</span>&gt;</span>**/*.xml<span class="tag">&lt;/<span class="name">include</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">includes</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">filtering</span>&gt;</span>true<span class="tag">&lt;/<span class="name">filtering</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">resource</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">resource</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">directory</span>&gt;</span>src/main/java<span class="tag">&lt;/<span class="name">directory</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">includes</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">include</span>&gt;</span>**/*.properties<span class="tag">&lt;/<span class="name">include</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">include</span>&gt;</span>**/*.xml<span class="tag">&lt;/<span class="name">include</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">includes</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">filtering</span>&gt;</span>true<span class="tag">&lt;/<span class="name">filtering</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">resource</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">resources</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后写一个测试方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">getUserList</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    SqlSession sqlSession = MybatisUtils.getSqlSession();</span><br><span class="line">    <span class="comment">//方式一</span></span><br><span class="line">    <span class="comment">//UserMapper当作UserDao的实现类,通过sqlSession的方法拿到接口,就可以调用接口里的方法</span></span><br><span class="line">    UserDao userDao = sqlSession.getMapper(UserDao<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    List&lt;User&gt; userList = userDao.getUserList();</span><br><span class="line">    userList.forEach(System.out::println);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//方式二</span></span><br><span class="line">    List&lt;User&gt; list = sqlSession.selectList(<span class="string">"com.company.dao.UserDao.getUserList"</span>);</span><br><span class="line">    list.forEach(System.out::println);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于修改或者插入:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">updateUser</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    SqlSession sqlSession = MybatisUtils.getSqlSession();</span><br><span class="line">    UserMapper mapper = sqlSession.getMapper(UserMapper<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    mapper.updateUser(<span class="keyword">new</span> User(<span class="number">8</span>, <span class="string">"刘诗诗"</span>, <span class="string">"56123"</span>, <span class="string">"home"</span>, <span class="string">"123456789"</span>));</span><br><span class="line">    sqlSession.commit();</span><br><span class="line">    sqlSession.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addUser</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    SqlSession sqlSession = MybatisUtils.getSqlSession();</span><br><span class="line">    UserMapper mapper = sqlSession.getMapper(UserMapper<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    mapper.addUser(<span class="keyword">new</span> User(<span class="string">"刘诗诗"</span>, <span class="string">"123"</span>, <span class="string">"home"</span>, <span class="string">"123223"</span>));</span><br><span class="line">    sqlSession.commit();</span><br><span class="line">    sqlSession.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>id是自增的,所以不能传入id,为此就得有另一个构造器.在修改时也不得不把所有属性都写出来.这两种情况下使用new user的方式都不方便,因此可以使用map,只传需要的字段.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addUser2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    SqlSession sqlSession = MybatisUtils.getSqlSession();</span><br><span class="line">    UserMapper mapper = sqlSession.getMapper(UserMapper<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    mapper.addUser2(<span class="keyword">new</span> HashMap&lt;String, Object&gt;() &#123;&#123;</span><br><span class="line">        put(<span class="string">"userid"</span>, <span class="keyword">null</span>);</span><br><span class="line">        put(<span class="string">"username"</span>, <span class="string">"江疏影"</span>);</span><br><span class="line">        put(<span class="string">"userpwd"</span>, <span class="string">"2356"</span>);</span><br><span class="line">        put(<span class="string">"useradd"</span>, <span class="keyword">null</span>);</span><br><span class="line">        put(<span class="string">"userphone"</span>, <span class="string">"5623148"</span>);</span><br><span class="line">    &#125;&#125;);</span><br><span class="line">    sqlSession.commit();</span><br><span class="line">    sqlSession.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">insert</span> <span class="attr">id</span>=<span class="string">"addUser2"</span> <span class="attr">parameterType</span>=<span class="string">"map"</span>&gt;</span></span><br><span class="line">    insert into test.user (id, name, password, address, phone)</span><br><span class="line">    VALUES (#&#123;userid&#125;, #&#123;username&#125;, #&#123;userpwd&#125;, #&#123;useradd&#125;, #&#123;userphone&#125;);</span><br><span class="line"><span class="tag">&lt;/<span class="name">insert</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>下面采取注解方式实现:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--使用注解方式,则要指定接口--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mappers</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mapper</span> <span class="attr">class</span>=<span class="string">"com.company.dao.UserMapper"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mappers</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后在这个Mapper中的方法上写好SQL语句即可:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//查询全部用户</span></span><br><span class="line"><span class="meta">@Select</span>(<span class="string">"select * from user;"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> List&lt;User&gt; <span class="title">getUserList</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
<p>使用注解不能应对一些复杂的情况.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Select</span>(<span class="string">"select * from user where id = #&#123;id&#125;;"</span>)</span><br><span class="line"><span class="function">User <span class="title">getUserByID</span><span class="params">(@Param(<span class="string">"id"</span>)</span> <span class="keyword">int</span> id)</span>;</span><br></pre></td></tr></table></figure>

<p>为参数加上@Param注解,即为括号里的参数取别名,与查询语句里的参数对应,此时的第三个id就是形参,可以不叫id.多个参数,且类型为基本类型和string类型时就必须要加该注解.</p>
<p>总结一下传递参数的方式:</p>
<p>1.用@param注解</p>
<p>2.用map传递</p>
<p>3.JavaBean传参数,这种方式变体很多,例如VO,把要用到的参数封装为一个value object作为参数传递.此外第二种用Map也可以看作此类,引申出来还可以用数组,List等等.</p>
<p>4.按索引位置传,不推荐.</p>
<p>${}不能防止SQL注入,#{}可以,因此用后者.</p>
<ul>
<li>配置文件(配置文件中的各种配置是有顺序的):</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">properties</span> <span class="attr">resource</span>=<span class="string">"druid.properties"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">environments</span> <span class="attr">default</span>=<span class="string">"development"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">environment</span> <span class="attr">id</span>=<span class="string">"development"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">transactionManager</span> <span class="attr">type</span>=<span class="string">"JDBC"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataSource</span> <span class="attr">type</span>=<span class="string">"POOLED"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driver"</span> <span class="attr">value</span>=<span class="string">"$&#123;driverClassName&#125;"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span></span></span><br><span class="line"><span class="tag">                        <span class="attr">value</span>=<span class="string">"$&#123;url&#125;"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"$&#123;username&#125;"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"$&#123;password&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dataSource</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">environment</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>通过properties配置外部数据库连接配置,properties下还可以添加property补充属性.</p>
<ul>
<li>配置别名:</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getUserList"</span> <span class="attr">resultType</span>=<span class="string">"com.company.pojo.User"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from test.user</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以为resultType配置别名,减少冗余.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">typeAliases</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">typeAlias</span> <span class="attr">type</span>=<span class="string">"com.company.pojo.User"</span> <span class="attr">alias</span>=<span class="string">"User"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">typeAliases</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>也可以给包取别名,然后resultType里写bean的名字(首字母大小写都可,推荐小写).如果非要用给包取别名,然后bean也有别名,则在实体类上加@Alias(“user”)注解.此外,一些常用类型直接首字母小写即可,基本类型前面加下划线.</p>
<ul>
<li>设置:</li>
</ul>
<p>mapUnderscoreToCamelCase:开启驼峰命名规则映射.</p>
<p>logImpl:指定mybatis日志实现</p>
<ul>
<li>mappers映射器:</li>
</ul>
<p>在Mappers标签中使用,resource指定xml,class指定xxxMapper,package将包内的接口实现都扫描为映射器.</p>
<h2 id="ResultMap"><a href="#ResultMap" class="headerlink" title="ResultMap"></a>ResultMap</h2><p>实体类的属性和数据库表中的字段名不一致.那么按照原来方式查出来的user的某些属性就会为null.</p>
<p>直接的解决方法:在sql语句中给字段取别名.</p>
<p>使用ResultMap进行结果集映射:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">mapper</span> <span class="attr">namespace</span>=<span class="string">"com.company.dao.UserMapper"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"UserMap"</span> <span class="attr">type</span>=<span class="string">"User"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"id"</span> <span class="attr">property</span>=<span class="string">"id"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"name"</span> <span class="attr">property</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"password"</span> <span class="attr">property</span>=<span class="string">"pwd"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"address"</span> <span class="attr">property</span>=<span class="string">"address"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"phone"</span> <span class="attr">property</span>=<span class="string">"phone"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"findById"</span> <span class="attr">resultMap</span>=<span class="string">"UserMap"</span>&gt;</span></span><br><span class="line">        select *</span><br><span class="line">        from user</span><br><span class="line">        where id = #&#123;id&#125;;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mapper</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>日志工厂:</p>
<p>使用logImpl指定日志实现.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log4j.rootLogger&#x3D;DEBUG,console,file</span><br><span class="line"></span><br><span class="line">#控制台输出的相关设置</span><br><span class="line">log4j.appender.console &#x3D; org.apache.log4j.ConsoleAppender</span><br><span class="line">log4j.appender.console.Target &#x3D; System.out</span><br><span class="line">log4j.appender.console.Threshold&#x3D;DEBUG</span><br><span class="line">log4j.appender.console.layout &#x3D; org.apache.log4j.PatternLayout</span><br><span class="line">log4j.appender.console.layout.ConversionPattern&#x3D;[%c]-%m%n</span><br><span class="line"></span><br><span class="line">#文件输出的相关设置</span><br><span class="line">log4j.appender.file &#x3D; org.apache.log4j.RollingFileAppender</span><br><span class="line">log4j.appender.file.File&#x3D;.&#x2F;log&#x2F;my.log</span><br><span class="line">log4j.appender.file.MaxFileSize&#x3D;10mb</span><br><span class="line">log4j.appender.file.Threshold&#x3D;DEBUG</span><br><span class="line">log4j.appender.file.layout&#x3D;org.apache.log4j.PatternLayout</span><br><span class="line">log4j.appender.file.layout.ConversionPattern&#x3D;[%p][%d&#123;yy-MM-dd&#125;][%c]%m%n</span><br><span class="line"></span><br><span class="line">#日志输出级别</span><br><span class="line">log4j.logger.org.mybatis&#x3D;DEBUG</span><br><span class="line">log4j.logger.java.sql&#x3D;DEBUG</span><br><span class="line">log4j.logger.java.sql.Statement&#x3D;DEBUG</span><br><span class="line">log4j.logger.java.sql.ResultSet&#x3D;DEBUG</span><br><span class="line">log4j.logger.java.sql.PreparedStatement&#x3D;DEBUG</span><br></pre></td></tr></table></figure>

<p>用Mybatis实现分页:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">getUserByLimit</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    SqlSession sqlSession = MybatisUtils.getSqlSession();</span><br><span class="line">    UserMapper mapper = sqlSession.getMapper(UserMapper<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    Map&lt;String, Integer&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">    map.put(<span class="string">"startIndex"</span>, <span class="number">0</span>);</span><br><span class="line">    map.put(<span class="string">"pageSize"</span>, <span class="number">2</span>);</span><br><span class="line">    List&lt;User&gt; userByLimit = mapper.getUserByLimit(map);</span><br><span class="line">    userByLimit.forEach(System.out::println);</span><br><span class="line">    sqlSession.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getUserByLimit"</span> <span class="attr">parameterType</span>=<span class="string">"map"</span> <span class="attr">resultType</span>=<span class="string">"user"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from user</span><br><span class="line">    limit #&#123;startIndex&#125;,#&#123;pageSize&#125;;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果一个实体类中含有另一个实体类,分别对应了两张表,这个被包含的实体类需要用resultMap来特别的映射.例如,student中包含了一个teacher.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getStudent"</span> <span class="attr">resultMap</span>=<span class="string">"StudentTeacher"</span>&gt;</span></span><br><span class="line">    select * from student</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"StudentTeacher"</span> <span class="attr">type</span>=<span class="string">"Student"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">property</span>=<span class="string">"id"</span> <span class="attr">column</span>=<span class="string">"id"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">property</span>=<span class="string">"name"</span> <span class="attr">column</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--对于复杂的属性要单独处理</span></span><br><span class="line"><span class="comment">        对象:association</span></span><br><span class="line"><span class="comment">        集合:collection</span></span><br><span class="line"><span class="comment">     --&gt;</span></span><br><span class="line">     <span class="comment">&lt;!-- column表示表中字段名,property表示属性名,这样对应起来,javaType告诉这个属性的类型 --&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">association</span> <span class="attr">property</span>=<span class="string">"teacher"</span> <span class="attr">column</span>=<span class="string">"tid"</span> <span class="attr">javaType</span>=<span class="string">"Teacher"</span> <span class="attr">select</span>=<span class="string">"getTeacher"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getTeacher"</span> <span class="attr">resultType</span>=<span class="string">"Teacher"</span>&gt;</span></span><br><span class="line">    select * from teacher where id= #&#123;id&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>首先是为了查student的数据,然后为了把student中teacher的信息查出来,又在后面查了teacher,最后用resultMap将teacher的数据与student中teacher属性映射.这是按照查询嵌套处理.还有一种按照结果嵌套处理的写法:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getStudent2"</span> <span class="attr">resultMap</span>=<span class="string">"StudentTeacher2"</span>&gt;</span></span><br><span class="line">    select s.id sid,s.name sname,t.name tname</span><br><span class="line">    from student s,teacher t</span><br><span class="line">    where s.tid=t.id;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"StudentTeacher2"</span> <span class="attr">type</span>=<span class="string">"Student"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">property</span>=<span class="string">"id"</span> <span class="attr">column</span>=<span class="string">"sid"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">property</span>=<span class="string">"name"</span> <span class="attr">column</span>=<span class="string">"sname"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">association</span> <span class="attr">property</span>=<span class="string">"teacher"</span> <span class="attr">javaType</span>=<span class="string">"Teacher"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">property</span>=<span class="string">"name"</span> <span class="attr">column</span>=<span class="string">"tname"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">association</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果反过来,一个老师有多个学生,老师的实体类中含有一个集合.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getTeacher"</span> <span class="attr">resultMap</span>=<span class="string">"TeacherStudent"</span>&gt;</span></span><br><span class="line">    select s.id sid,s.name sanme,t.name tname,t.id tid</span><br><span class="line">    from student s,teacher t</span><br><span class="line">    where s.tid=t.id and t.id= #&#123;tid&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"TeacherStudent"</span> <span class="attr">type</span>=<span class="string">"Teacher"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">property</span>=<span class="string">"id"</span> <span class="attr">column</span>=<span class="string">"tid"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">property</span>=<span class="string">"name"</span> <span class="attr">column</span>=<span class="string">"tname"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--集合里的类型用ofType--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">collection</span> <span class="attr">property</span>=<span class="string">"students"</span> <span class="attr">ofType</span>=<span class="string">"Student"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">property</span>=<span class="string">"id"</span> <span class="attr">column</span>=<span class="string">"sid"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">property</span>=<span class="string">"name"</span> <span class="attr">column</span>=<span class="string">"sname"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">property</span>=<span class="string">"tid"</span> <span class="attr">column</span>=<span class="string">"tid"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">collection</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="动态SQL"><a href="#动态SQL" class="headerlink" title="动态SQL"></a>动态SQL</h2><p>if:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"queryIf"</span> <span class="attr">resultType</span>=<span class="string">"com.company.pojo.Employees"</span> <span class="attr">parameterType</span>=<span class="string">"Map"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from myemployees.employees</span><br><span class="line">            where 1 = 1</span><br><span class="line">    <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"commission_pct != null"</span>&gt;</span></span><br><span class="line">        and commission_pct = #&#123;commission_pct&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"department_id != null"</span>&gt;</span></span><br><span class="line">        and department_id = #&#123;department_id&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>将commission_pct和department_id作为参数放入map中传入,如果两个参数有不为null的,根据条件在第一个SQL语句后追加查询条件.这里要注意的是where 1=1后面不能有分号结束SQL语句,因为这种写法本质是SQL语句的拼接.</p>
<p>上面的语句的问题在于有个where 1=1这种不符合规范的东西.这里应该使用where标签.where标签可以自动将多余的and去掉.</p>
<p>choose:</p>
<p>类似于switch语句,只会根据传入的一个参数去选择查询.满足哪个条件查哪个.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"queryChoose"</span> <span class="attr">resultType</span>=<span class="string">"com.company.pojo.Employees"</span> <span class="attr">parameterType</span>=<span class="string">"map"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from myemployees.employees</span><br><span class="line">    <span class="tag">&lt;<span class="name">where</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">choose</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">when</span> <span class="attr">test</span>=<span class="string">"commission_pct != null"</span>&gt;</span></span><br><span class="line">                commission_pct = #&#123;commission_pct&#125;</span><br><span class="line">            <span class="tag">&lt;/<span class="name">when</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">when</span> <span class="attr">test</span>=<span class="string">"department_id != null"</span>&gt;</span></span><br><span class="line">                and department_id = #&#123;department_id&#125;</span><br><span class="line">            <span class="tag">&lt;/<span class="name">when</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">otherwise</span>&gt;</span></span><br><span class="line">                and manager_id = #&#123;manager_id&#125;</span><br><span class="line">            <span class="tag">&lt;/<span class="name">otherwise</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">choose</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">where</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>而动态更新则用set:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">update</span> <span class="attr">id</span>=<span class="string">"updateAuthorIfNecessary"</span>&gt;</span></span><br><span class="line">  update Author</span><br><span class="line">    <span class="tag">&lt;<span class="name">set</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"username != null"</span>&gt;</span>username=#&#123;username&#125;,<span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"password != null"</span>&gt;</span>password=#&#123;password&#125;,<span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"email != null"</span>&gt;</span>email=#&#123;email&#125;,<span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"bio != null"</span>&gt;</span>bio=#&#123;bio&#125;<span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">set</span>&gt;</span></span><br><span class="line">  where id=#&#123;id&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">update</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>SQL片段:可以使用SQL标签将一段公共的SQL语句抽取出来,并给个id,需要使用时则用include标签加refid引用这个片段.在SQL片段里的语句最好基于单表,也不要有where标签.</p>
<p>foreach标签:</p>
<p>mapper中的代码<code>List&lt;Employees&gt; queryForEach(Map&lt;String, ArrayList&lt;Integer&gt;&gt; map);</code></p>
<p>mapper.xml:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"queryForEach"</span> <span class="attr">resultType</span>=<span class="string">"com.company.pojo.Employees"</span> <span class="attr">parameterType</span>=<span class="string">"map"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from myemployees.employees</span><br><span class="line">    <span class="tag">&lt;<span class="name">where</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">foreach</span> <span class="attr">collection</span>=<span class="string">"ids"</span> <span class="attr">item</span>=<span class="string">"mid"</span> <span class="attr">open</span>=<span class="string">"("</span> <span class="attr">close</span>=<span class="string">")"</span> <span class="attr">separator</span>=<span class="string">"or"</span>&gt;</span></span><br><span class="line">            manager_id = #&#123;mid&#125;</span><br><span class="line">        <span class="tag">&lt;/<span class="name">foreach</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">where</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这里使用一个mao传入参数:map(ids:list),key为ids,value为一个ArrayList数组.foreach的collection根据传入的map参数的key:ids拿到要遍历的数组,item为集合元素的代称,方便下一行把集合元素赋给查询语句.open,close,separator为拼凑SQL语句的字符.例如:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, ArrayList&lt;Integer&gt;&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">ArrayList&lt;Integer&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">list.add(<span class="number">100</span>);</span><br><span class="line">list.add(<span class="number">120</span>);</span><br><span class="line">map.put(<span class="string">"ids"</span>, list);</span><br><span class="line">List&lt;Employees&gt; employees = mapper.queryForEach(map);</span><br></pre></td></tr></table></figure>

<p>集合里元素为100,120.那么拼凑的SQL为<code>select * from myemployees.employees WHERE ( manager_id = 100 or manager_id = 120 )</code>.</p>
<p>另一个例子:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"queryForEach"</span> <span class="attr">resultType</span>=<span class="string">"com.company.pojo.Employees"</span> <span class="attr">parameterType</span>=<span class="string">"map"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from myemployees.employees</span><br><span class="line">            where employees.manager_id in</span><br><span class="line">    <span class="tag">&lt;<span class="name">foreach</span> <span class="attr">collection</span>=<span class="string">"ids"</span> <span class="attr">item</span>=<span class="string">"mid"</span> <span class="attr">open</span>=<span class="string">"("</span> <span class="attr">close</span>=<span class="string">")"</span> <span class="attr">separator</span>=<span class="string">","</span>&gt;</span></span><br><span class="line">        #&#123;mid&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">foreach</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>拼凑的结果为:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> *</span><br><span class="line"><span class="keyword">FROM</span> myemployees.employees</span><br><span class="line"><span class="keyword">WHERE</span> employees.manager_id <span class="keyword">in</span> ( <span class="number">100</span> , <span class="number">120</span> );</span><br></pre></td></tr></table></figure>

<p>这里使用Map将一个ArrayList传进去其实挺麻烦,可以直接把ArrayList作为参数传入.</p>
<p>下面是Mybatis的缓存.</p>
<p>mybatis也提供了对缓存的支持，分为一级缓存和二级缓存。但是在默认的情况下，只开启一级缓存（一级缓存是对同一个SqlSession而言的）。</p>
<p>一级缓存:</p>
<p>同一个SqlSession对象,在参数和SQL完全一样的情况下，只执行一次SQL语句（如果缓存没有过期）,SQL查询的结果是同一个对象.</p>
<p>不是同一个SqlSession对象,在参数和SQL完全一样的情况下,不会缓存.</p>
<p>如果在mapper.xml中设置flushCache=”true”,则会刷新缓存.同一个SqlSession中在参数和SQL完全一样的情况下还是会重新查询.</p>
<p><strong>任何的UPDATE,INSERT,DELETE语句都会刷新缓存(commit)。</strong></p>
<p>二级缓存:</p>
<p><img src="/2020/07/03/Mybatis/%E7%BC%93%E5%AD%98.png" alt="缓存"></p>
<p>首先要手动开启mybatis二级缓存。</p>
<p>在config.xml设置二级缓存开关，还要在具体的mapper.xml开启二级缓存.二级缓存是用来解决一级缓存不能跨会话共享的问题的，范围是namespace 级别的，可以被多个SqlSession 共享（只要是同一个接口里面的相同方法，都可以共享）</p>
<p>1.<settings><br>    <!--开启二级缓存--><br>    <setting name="cacheEnabled" value="true"><br></setting></settings></p>
<p>2.需要将映射的pojo类实现<strong>序列化</strong></p>
<p>class Student implements Serializable{}</p>
<p>3.<!--开启本Mapper的namespace下的二级缓存--><br><cache eviction="LRU" flushinterval="10000"></cache></p>
<p>cache的属性简介:</p>
<p>eviction:代表的是缓存回收策略，目前MyBatis提供以下策略:</p>
<p>LRU:最近最少使用，移除最长时间不被使用的对象。(默认策略)</p>
<p>FIFO:先进先出，按对象进入缓存的顺序来移除对象。</p>
<p>SOFT:软引用，移除基于垃圾回收器状态和软引用规则的对象。</p>
<p>WEAK:弱引用，移除基于垃圾回收器状态和弱引用规则的对象。</p>
<p>flushInterval对应刷新间隔，单位毫秒，默认值不设置，即没有刷新间隔，缓存仅仅在刷新语句时刷新。</p>
<p>如果设定了之后，到了对应时间会过期，再次查询需要从数据库中取数据。</p>
<p>除此之外还有许多设置:</p>
<p>type用于指定缓存的实现类型，默认是PERPETUAL,对应的是mybatis本身的缓存实现类org.apache.ibatis.cache.impl.PerpetualCache。这个实现类本质是基于HashMap的.MyBatis在这个实现类基础上还提供了很多装饰器.<br><img src="/2020/07/03/Mybatis/%E7%B1%BB%E5%9E%8B.png" alt="类型"><br>后续如果我们要实现自己的缓存或者使用第三方的缓存，都需要更改此处。</p>
<p>size对应为引用的数量，即最多的缓存对象数据，默认为1024。</p>
<p>readOnly为只读属性，默认为false</p>
<ul>
<li>false:可读写，在创建对象时，会通过反序列化得到缓存对象的拷贝。因此在速度上会相对慢一点，但重在安全。</li>
<li>true:只读，只读的缓存会给所有调用者返回缓存对象的相同实例。因此性能很好，但如果修改了对象，有可能会导致程序出问题。</li>
</ul>
<p>如果某些查询方法对数据的实时性要求很高，不需要二级缓存，怎么办？我们可以在单个Statement ID上显式关闭二级缓存（默认是true）：<code>&lt;select id=&quot;selectBlog&quot; resultMap=&quot;BaseResultMap&quot; useCache=&quot;false&quot;&gt;</code>.不影响一级缓存.</p>
<p>在mapper的同一个namespace中，如果有其他insert、update、delete操作后都需要执行刷新缓存操作，来避免脏读。这时我们只需要设置statement配置中的flushCache=“true“属性，就会默认刷新缓存，相反如果是false就不会了。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">...</span> <span class="attr">flushCache</span>=<span class="string">"false"</span> <span class="attr">useCache</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">insert</span> <span class="attr">...</span> <span class="attr">flushCache</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">update</span> <span class="attr">...</span> <span class="attr">flushCache</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">delete</span> <span class="attr">...</span> <span class="attr">flushCache</span>=<span class="string">"true"</span>/&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
  </entry>
  <entry>
    <title>Python笔记1</title>
    <url>/2018/04/24/Python%E7%AC%94%E8%AE%B01/</url>
    <content><![CDATA[<p>&emsp;因为要做图像处理，又得把python捡起来的，再加上许多库都是基于python2的，以前学的都是3的，所以还要再回顾下基础知识。</p>
<a id="more"></a>

<h1 id="一些零碎知识点"><a href="#一些零碎知识点" class="headerlink" title="一些零碎知识点"></a>一些零碎知识点</h1><h2 id="除法"><a href="#除法" class="headerlink" title="除法"></a>除法</h2><p>&emsp;在python2中，<code>1/2</code>的结果是0，整数除整数结果就是整数，除非在前面<code>from __future__ import division</code>；对于有小数点的浮点数，这种除法的结果就是小数的，<code>1/2.0=0.5</code>。总结：什么类型的数除出来就是什么类型的。<br><br>&emsp;<code>//</code>用于整除，<code>1.0//2.0=0.0</code></p>
<h2 id="print"><a href="#print" class="headerlink" title="print"></a>print</h2><p>&emsp;<code>print</code>在python2中是不要加括号的，<code>print hello</code></p>
<h2 id="几个函数"><a href="#几个函数" class="headerlink" title="几个函数"></a>几个函数</h2><p>&emsp;round用于四舍五入，floor用于向下取整，ceil用于向上取整。int(32.9)会转换为32，也是向下取整。</p>
<h2 id="模块"><a href="#模块" class="headerlink" title="模块"></a>模块</h2><p>&emsp;import导入模块，然后按照“模块.函数”的方式调用函数。from 模块 import 函数的方式，则可以直接使用函数。还可以用变量来引用函数。导入math模块使用普通的sqrt函数，导入cmath(complex math)模块则可以使用用于复数的sqrt函数。</p>
]]></content>
      <tags>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记10</title>
    <url>/2018/05/10/Java%E7%AC%94%E8%AE%B010/</url>
    <content><![CDATA[<p>&emsp;Java集合大致分为四大类，Set，List，Queue，Map。Set代表不记录插入顺序，不可重复的集合；List代表记录顺序，可重复的集合；Map代表具有映射关系的集合；Queue代表了队列的集合。集合可以存储数量不等的对象，实现常用的数据结构。集合就像是一种容器，在Java5之前，将对象的引用放入后，集合会丢失所有对象的数据类型，都当作Object类处理。Java5之后增加泛型，Java集合可以记住容器中对象的数据类型。</p>
<a id="more"></a>

<h1 id="集合概述"><a href="#集合概述" class="headerlink" title="集合概述"></a>集合概述</h1><p>&emsp;在初期阶段，要保存数据，通常使用数组。但是这种结构的灵活性很差比如，保存数量不确定的数据和具有映射关系的数据。Java的集合类也称为容器类，位于java.util下，java.util.concurrent下还提供了一些多线程支持的集合类。集合中只能保存对象（引用变量）。集合类主要由两个接口派生：Collection和Map。<br><img src="/2018/05/10/Java%E7%AC%94%E8%AE%B010/Collection.jpg" alt="Collection"><br>&emsp;Set和List接口分别代表了不记录插入顺序和记录插入顺序的集合，Queue时Java提供的队列实现，与List相似。<br><img src="/2018/05/10/Java%E7%AC%94%E8%AE%B010/Map.jpg" alt="Map"><br>&emsp;Map接口的实现类非常多，主要的特点都是保存数据要用key-value对，key是数据的唯一标识，不可重复。对于粗线框起来的四个接口，大致可以看成三类，List的可以记住顺序，List长度可变，根据下表访问；Map的访问需要通过key值；Set无法记住顺序，所以是通过元素本身访问，不能重复。Queue有点特殊，不在此列。</p>
<h1 id="Collection和Iterator接口"><a href="#Collection和Iterator接口" class="headerlink" title="Collection和Iterator接口"></a>Collection和Iterator接口</h1><p>&emsp;Collection接口是List，Set和Queue接口的父接口，它自生定义了一些方法：</p>
<ul>
<li>boolean add(Object o):该方法用于向集合添加一个元素，如果集合对象被添加操作改变了，返回true</li>
<li>boolean addAll(Collection c):该方法用于将集合c中的所有元素添加到指定集合，返回值同上</li>
<li>void clear():清除集合内的所有元素，将长度变为0</li>
<li>boolean contains(Object o)：返回集合里是否包含指定元素</li>
<li>boolean containsAll(Collection c):返回集合里是否包含集合c里的所有元素</li>
<li>boolean isEmpty():返回集合是否为空</li>
<li>Iterator iterator()：返回一个Iterator对象（迭代器），用于遍历集合里的元素</li>
<li>boolean remove(Object o):删除集合中的指定元素o，当包含一个或多个元素o时，将删除第一个，返回true</li>
<li>boolean removeAll(Collection c):从集合中删除集合c里包含的所有元素，如果删除了一个或以上的元素，返回true</li>
<li>boolean retain(Collection c):从集合中删除集合c里不包含的元素，如果该操作改变了调用方法的集合，则返回true</li>
<li>int size():返回集合中元素的个数</li>
<li>Object[] toArray()：把集合转换为一个数组</li>
</ul>
<p>&emsp;所有的Collection实现类都重写了toString()方法，<code>System.out.println(c)</code>将把集合中全部元素打印出来。</p>
<h2 id="使用Lambda表达式遍历集合"><a href="#使用Lambda表达式遍历集合" class="headerlink" title="使用Lambda表达式遍历集合"></a>使用Lambda表达式遍历集合</h2><p>&emsp;查看Collection接口的源码，发现它继承了Iterable接口。Java8为Iterable接口增加了一个forEach(Consumer action)的默认方法，该方法所需参数的类型是一个函数式接口。因此Collection集合也可以调用该方法。Iterable接口中的该方法的具体代码如下，Consumer是另一个接口，这里的语法涉及到泛型，先不做解释。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">default</span> <span class="keyword">void</span> <span class="title">forEach</span><span class="params">(Consumer&lt;? <span class="keyword">super</span> T&gt; action)</span> </span>&#123;</span><br><span class="line">        Objects.requireNonNull(action);</span><br><span class="line">        <span class="keyword">for</span> (T t : <span class="keyword">this</span>) &#123;</span><br><span class="line">            action.accept(t);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;Consumer接口中相关代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Consumer</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Performs this operation on the given argument.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> t the input argument</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">accept</span><span class="params">(T t)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Returns a composed &#123;<span class="doctag">@code</span> Consumer&#125; that performs, in sequence, this</span></span><br><span class="line"><span class="comment">     * operation followed by the &#123;<span class="doctag">@code</span> after&#125; operation. If performing either</span></span><br><span class="line"><span class="comment">     * operation throws an exception, it is relayed to the caller of the</span></span><br><span class="line"><span class="comment">     * composed operation.  If performing this operation throws an exception,</span></span><br><span class="line"><span class="comment">     * the &#123;<span class="doctag">@code</span> after&#125; operation will not be performed.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> after the operation to perform after this operation</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> a composed &#123;<span class="doctag">@code</span> Consumer&#125; that performs in sequence this</span></span><br><span class="line"><span class="comment">     * operation followed by the &#123;<span class="doctag">@code</span> after&#125; operation</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> NullPointerException if &#123;<span class="doctag">@code</span> after&#125; is null</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">default</span> Consumer&lt;T&gt; <span class="title">andThen</span><span class="params">(Consumer&lt;? <span class="keyword">super</span> T&gt; after)</span> </span>&#123;</span><br><span class="line">        Objects.requireNonNull(after);</span><br><span class="line">        <span class="keyword">return</span> (T t) -&gt; &#123; accept(t); after.accept(t); &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;当调用forEach(Consumer action)方法时，程序会将集合元素传给Consumer的accept(T t)方法，因为该接口只有这一个抽象方法，所以这是个函数式接口，所以可以用Lambda表达式。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Collection;</span><br><span class="line"><span class="keyword">import</span> java.util.HashSet;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CollectionTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Collection books = <span class="keyword">new</span> HashSet();</span><br><span class="line">        books.add(<span class="string">"天国之秋"</span>);</span><br><span class="line">        books.add(<span class="string">"阿拉伯的劳伦斯"</span>);</span><br><span class="line">        books.add(<span class="string">"全球通史"</span>);</span><br><span class="line">        books.add(<span class="string">"极端年代"</span>);</span><br><span class="line">        books.forEach(obj -&gt; System.out.println(<span class="string">"迭代集合元素"</span>)+obj );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;上面的代码会有warning，因为没用泛型，可以忽略。</p>
<h2 id="使用Java8增强的Iterator遍历元素"><a href="#使用Java8增强的Iterator遍历元素" class="headerlink" title="使用Java8增强的Iterator遍历元素"></a>使用Java8增强的Iterator遍历元素</h2><p>&emsp;Iterator接口也是Java集合框架的成员，但不用与容器，只作为迭代器。注意与Iterable接口的区别。Iterator接口中定义了如下四个方法：</p>
<ul>
<li>boolean hasNext():如果被迭代集合元素还没有被遍历完，则返回true</li>
<li>Object next():返回集合里的下一个元素</li>
<li>void remove():删除集合里上一次next方法返回的元素</li>
<li>void forEachRemaining(Consumer action):Java8新增的默认方法，该方法可以使用Lambda表达式来遍历集合元素</li>
</ul>
<p>&emsp;前面的Iterable接口就是包含了<code>Iterator&lt;T&gt; iterator()</code>方法,所以实现了迭代。那么为什么要这样用一个接口包含迭代相关的方法，再用另一个接口包含呢？参看链接：<a href="https://blog.csdn.net/github_39424631/article/details/75048955" target="_blank" rel="noopener">https://blog.csdn.net/github_39424631/article/details/75048955</a><br></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Iterator;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IteratorTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Iterator it = books.iterator();</span><br><span class="line">        <span class="keyword">while</span> (it.hasNext()) &#123;</span><br><span class="line">            String book = (String) it.next();</span><br><span class="line">            System.out.println(book);</span><br><span class="line">            <span class="keyword">if</span> (book.equals(<span class="string">"天国之秋"</span>))&#123;</span><br><span class="line">                it.remove();</span><br><span class="line">            &#125;</span><br><span class="line">            book = <span class="string">"测试字符串"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(books);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;Iterator对象必须依靠一个集合对象。这段代码由两个地方值得注意，一是<code>book = &quot;测试字符串&quot;;</code>这句并不会修改集合中的值，因为是值传递。二是<code>it.remove();</code>删除的是<strong>上一次</strong>的next返回值。如果改<code>books.remove(book);</code>修改当前的next返回值就会出java.util.ModificationException异常。因为是不允许在迭代过程中集合的，后面的HashSet，ArrayList都是如此。因为如果可以修改可能会产生因为资源共享而引发的潜在问题。而且，Iterator采用的是快速失败（fail-fast）机制，一旦检测到集合修改就立即引发异常。</p>
<h2 id="使用Lambda表达式遍历Iterator"><a href="#使用Lambda表达式遍历Iterator" class="headerlink" title="使用Lambda表达式遍历Iterator"></a>使用Lambda表达式遍历Iterator</h2><p>&emsp;与使用Iterable类似。forEachRemaining(Consumer action)的代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">default</span> <span class="keyword">void</span> <span class="title">forEachRemaining</span><span class="params">(Consumer&lt;? <span class="keyword">super</span> E&gt; action)</span> </span>&#123;</span><br><span class="line">        Objects.requireNonNull(action);</span><br><span class="line">        <span class="keyword">while</span> (hasNext())</span><br><span class="line">            action.accept(next());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="使用foreach循环遍历集合元素"><a href="#使用foreach循环遍历集合元素" class="headerlink" title="使用foreach循环遍历集合元素"></a>使用foreach循环遍历集合元素</h2><p>&emsp;相较于用Iterator接口迭代访问，foreach反而更加方便。使用类似<code>for(Object obj : books)</code>的语句就可以，但是任然不可以在迭代中修改集合的元素。参考链接:<a href="https://segmentfault.com/q/1010000003775035?_ea=362485" target="_blank" rel="noopener">https://segmentfault.com/q/1010000003775035?_ea=362485</a></p>
<h2 id="用Java8新增的Predicate操作集合"><a href="#用Java8新增的Predicate操作集合" class="headerlink" title="用Java8新增的Predicate操作集合"></a>用Java8新增的Predicate操作集合</h2><p>&emsp;Java8为Collection集合新增了一个removeIf(Predicate filter)方法，代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">default</span> <span class="keyword">boolean</span> <span class="title">removeIf</span><span class="params">(Predicate&lt;? <span class="keyword">super</span> E&gt; filter)</span> </span>&#123;</span><br><span class="line">        Objects.requireNonNull(filter);</span><br><span class="line">        <span class="keyword">boolean</span> removed = <span class="keyword">false</span>;</span><br><span class="line">        <span class="keyword">final</span> Iterator&lt;E&gt; each = iterator();</span><br><span class="line">        <span class="keyword">while</span> (each.hasNext()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (filter.test(each.next())) &#123;</span><br><span class="line">                each.remove();</span><br><span class="line">                removed = <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> removed;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">books.removeIf(ele -&gt; ((String)ele).lenght() &lt; <span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<p>&emsp;这句代码的意思是删除集合中符合条件的元素</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.function.Predicate;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PredicateTest2</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        System.out.println(calAll(books, ele -&gt; ((String) ele).contains(<span class="string">"年"</span>)));</span><br><span class="line">        System.out.println(calAll(books, ele -&gt; ((String) ele).contains(<span class="string">"之"</span>)));</span><br><span class="line">        System.out.println(calAll(books, ele -&gt; ((String) ele).length() &gt; <span class="number">10</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">calAll</span><span class="params">(Collection books, Predicate p)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> total = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (Object obj : books) &#123;</span><br><span class="line">            <span class="keyword">if</span> (p.test(obj)) &#123;</span><br><span class="line">                total++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> total;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;<code>ele -&gt; ((String) ele).length() &gt; 10)</code>这句产生了一个Predicate接口的实例，这个对象被传入calAll方法。然后迭代books，将里面的元素传入这个实例的test()方法，具体形参就是ele,test()方法的实现过程就是Lambda表达式的过程。</p>
<h2 id="使用Java8新增的Stream操作集合"><a href="#使用Java8新增的Stream操作集合" class="headerlink" title="使用Java8新增的Stream操作集合"></a>使用Java8新增的Stream操作集合</h2><p>&emsp;关于Stream的详细内容，单开一篇写，书上写的没头没尾非常糟糕。</p>
<h1 id="Set集合"><a href="#Set集合" class="headerlink" title="Set集合"></a>Set集合</h1><p>&emsp;Set集合的最大特点就是不能重复，Set集合通常不能记住元素的添加顺序，Set集合与Collection基本相同，没有提供任何额外的方法。当试图添加一个重复的元素，add()方法会返回false。这些事Set集合的通用知识，HashSet,TreeSet,EnumSet三个实现类又各有不同。</p>
<h2 id="HashSet类"><a href="#HashSet类" class="headerlink" title="HashSet类"></a>HashSet类</h2><p>&emsp;HashSet是Set的典型接口，大多数就是使用这个类HashSet按Hash算法来存储集合中的元素。具有如下特点：</p>
<ul>
<li>不能保证元素的排列顺序</li>
<li>HashSet不是同步的，如果由多个线程同时访问一个HashSet，必须通过代码来保证同步</li>
<li>集合元素值可以是null</li>
</ul>
<p>&emsp;当向HashSet中存入一个元素时，HashSet会调用该对象的hashCode()方法来得到该元素的hashCode值。HashSet判断元素相等的条件是：equals()方法结果为true，hashCode相同。只有这样才算做两个一样的元素。在重写hashCode()方法和equals()方法时，应注意，equals()返回true时，hashCode也应相同。因为如果不同，那么就会添加成功，使得存在两个相同的元素。（这里的相同指按照自定义规则的equals方法的结果，本来Object中要求同一个对象才能返回true，但是重写后规则改变，可能是不同对象内容相同。）<br><br>&emsp;上面说如果equals方法返回true,hashCode却不同会出现两个一样的值；反过来，如果hashCode相同，equals却不同，HashSet会试着将两个元素保存在一个地方，实际就会用一种链式结构（？）来保存多个对象。HashSet访问元素是根据hashCode值快速定位的，如果具有两个相同的hashCode值，就会导致性能下降。<br><br>&emsp;总之不论为什么，重写equals和重写hashCode必须同时进行。<br><br>&emsp;hash被译为哈希，散列。hash算法的价值在于速度，该算法可以直接根据hashCode值算出元素的存储位置，从而快速定位该元素。类似于数组的索引，只是hashCode的值不是连续的。<br><br>&emsp;因为默认的hashCode方法就是返回当前对象的地址。所以一般需要重写，规则如下：</p>
<ul>
<li>程序中多次调用同一个对象的hashCode方法应该返回相同值</li>
<li>当equals返回了true,那么hashCode应该相同（equals返回false,hashCode也可以相同，但是应竭力避免）</li>
<li>对象中用于equals方法作比较标准的实例变量，都应该被用于计算hashCode值</li>
</ul>
<p>&emsp;具体步骤：<br></p>
<p>1.把对象内每个有意义的实例变量（即参与equals()方法比较标准的实例变量）计算出一个int类型的hashCode值，计算方式如下表：</p>
<table>
<thead>
<tr>
<th>实例变量类型</th>
<th>计算方式</th>
</tr>
</thead>
<tbody><tr>
<td>boolean</td>
<td>hashCode=(f?0:1);</td>
</tr>
<tr>
<td>int,char,short,byte</td>
<td>hashCode=(int)f;</td>
</tr>
<tr>
<td>long</td>
<td>hashCode=(int)(f^(f&gt;&gt;&gt;32));</td>
</tr>
<tr>
<td>float</td>
<td>hashCode=Float.floatToIntBits(f);</td>
</tr>
<tr>
<td>double</td>
<td>long L=Double.doubleToLongBits(f); hashCode=(int)(L^(L&gt;&gt;&gt;32));</td>
</tr>
<tr>
<td>引用类型</td>
<td>(null==f?0:hashCode=f.hashCode());</td>
</tr>
</tbody></table>
<p>2.用第一步计算出来的多个hashCode值组成一个hashCode值，代码如下：<br>&emsp;<code>return f1.hashCode() + (int)f2;</code><br><br>&emsp;这句代码的意义不明，Effective Java一书中也不是这么写的，书上的代码是：<code>result 31 * result + c</code>,完整的代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> String passport;</span><br><span class="line">    <span class="comment">//getters and setters, constructor</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object o)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (o == <span class="keyword">this</span>) <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        <span class="keyword">if</span> (!(o <span class="keyword">instanceof</span> User)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        User user = (User) o;</span><br><span class="line">        <span class="keyword">return</span> user.name.equals(name) &amp;&amp;</span><br><span class="line">                user.age == age &amp;&amp;</span><br><span class="line">                user.passport.equals(passport);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//Idea from effective Java : Item 9</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> result = <span class="number">17</span>;</span><br><span class="line">        result = <span class="number">31</span> * result + name.hashCode();</span><br><span class="line">        result = <span class="number">31</span> * result + age;</span><br><span class="line">        result = <span class="number">31</span> * result + passport.hashCode();</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>3.当向集合中添加可变对象时必须十分小心，如果修改了这个对象的内容，有可能会导致算出来的hashCode与其他的一样，这样就会出现无法正确访问到该对象。</p>
<h3 id="LinkedHashSet类"><a href="#LinkedHashSet类" class="headerlink" title="LinkedHashSet类"></a>LinkedHashSet类</h3><p>&emsp;HashSet类还有一个子类LinkedHashSet,与HashSet基本类似，但是它是有顺序的，是通过链表来实现记住添加顺序。在性能上略低于HashSet，但是在迭代时会有很好的性能。</p>
<h2 id="TreeSet"><a href="#TreeSet" class="headerlink" title="TreeSet"></a>TreeSet</h2><p>&emsp;TreeSet是SortedSet接口的实现类，正如名字一样，它可以确保元素处于排序状态。与HashSet相比，该类还提供了其他几个额外方法：</p>
<ul>
<li>Comparator comparator():如果TreeSet采用了定制排序，则返回定制排序使用的comparator，如果是自然排序，则返回null</li>
<li>Object first():返回集合第一个元素</li>
<li>Object last():返回集合最后一个元素</li>
<li>Object lower(Object e):返回指定元素的前一个元素，指定元素可以不是集合中的</li>
<li>Object higher(Object e):返回指定元素的后一个元素，指定元素可以不是集合中的</li>
<li>SortedSet subSet(Object fromElement,Object toElement):返回此Set中的子集，范围是fromElement（包含）到toElement（不包含）</li>
<li>SortedSet headSet(Object toElement):返回子集，由从头到toElement的元素组成</li>
<li>SortSet tailSet(Object fromElement):返回子集，有fromElement到尾组成</li>
</ul>
<p>&emsp;TreeSet的插入顺序又元素大小决定。HashSet采用hash算法决定存储位置，TreeeSet则是采用红黑树的数据结构来存储元素，具体的存储规则有两种。</p>
<h3 id="自然排序"><a href="#自然排序" class="headerlink" title="自然排序"></a>自然排序</h3><p>&emsp;TreeSet会调用集合元素的compareTo(Object obj)方法来比较元素大小，然后按升序排列。这就是自然排序。Java提供了一个Compareable接口，接口里定义了一个compareTo(Object obj)方法，该方法返回一个整数值，0代表相等，正数代表调用者大于被比较者，负数代表小于。一些常用的类已经实现了这个接口：</p>
<ul>
<li>BigDecimal,BigInteger以及所有的数值型对应的包装类：按数据大小比较</li>
<li>Character:按字符的unicode值比较</li>
<li>Boolean:true对应的大于false</li>
<li>String:按字符的unicode值比较</li>
<li>Data,Time:比较日期时间</li>
</ul>
<p>&emsp;当将一个对象添加到这种集合时，必须实现了Comparable()接口，不然无法比较的话，也没法正确的加入元素，会引发ClassCastException异常。但是第一个可以不实现这个接口，因为它没什么可以比较的。不过将这唯一一个元素取出来时，仍就会引发上面的异常。<br><br>&emsp;另一点需要注意的是，只有对象类型相同才有比较的意义。若类型不相同，就会强制转换。也就是说，TreeSet中的元素都是一个类型的，当类型不同时，comparaTo方法没法正确执行。会抛出ClassCastException异常。<br><br>&emsp;对于这种集合来说，判断是否相同的唯一标准是compareTo判断是否相等。如果相等就没法添加到集合中。也就是说，哪怕equals方法返回的是true，只要compareTo返回的不是0，该元素仍旧可以添加进去。所以equals和compareTo方法应该保持一致性，避免这种不合理情况。反过来，equals判断不相等，compareTo判断相等的情况也要避免。<br><br>&emsp;对于添加可变对象也要非常注意。假定集合{1,2,3,4},1被改为4（这里简写，实际集合中加入的必须是对象）。这就导致了有两个一样的元素，对这两个一样的，你都没法删除，只能等待重新索引，例如删掉2之后。然后就会是采用链式结构将两个一样的元素特殊处理。这种情况也要竭力避免。</p>
<h3 id="定制排序"><a href="#定制排序" class="headerlink" title="定制排序"></a>定制排序</h3><p>&emsp;如果需要实现降序排列等特别的排序，则需要借助Comparator接口的int compare(T o1,T o2)抽象方法。代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.TreeSet;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">M</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">M</span><span class="params">(<span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"M[age:"</span> + age + <span class="string">"]"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TreeSetTest4</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//TreeSet ts2=new TreeSet();</span></span><br><span class="line">        TreeSet ts = <span class="keyword">new</span> TreeSet((o1, o2) -&gt; &#123;</span><br><span class="line">            M m1 = (M) o1;</span><br><span class="line">            M m2 = (M) o2;</span><br><span class="line">            <span class="keyword">return</span> m1.age &gt; m2.age ? -<span class="number">1</span> : m1.age &lt; m2.age ? <span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">        ts.add(<span class="keyword">new</span> M(<span class="number">5</span>));</span><br><span class="line">        ts.add(<span class="keyword">new</span> M(<span class="number">8</span>));</span><br><span class="line">        ts.add(<span class="keyword">new</span> M(-<span class="number">1</span>));</span><br><span class="line">        ts.add(<span class="keyword">new</span> M(<span class="number">3</span>));</span><br><span class="line">        ts.add(<span class="keyword">new</span> M(<span class="number">9</span>));</span><br><span class="line">        <span class="comment">//System.out.println(ts);</span></span><br><span class="line">        System.out.println(ts.headSet(<span class="keyword">new</span> M(<span class="number">4</span>)));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;在new一个ts时，调用了另一个与上面方式不同的构造器，代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">TreeSet</span><span class="params">(Comparator&lt;? <span class="keyword">super</span> E&gt; comparator)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(<span class="keyword">new</span> TreeMap&lt;&gt;(comparator));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;该构造器接受一个Comparator接口的实现。而Comparator接口只有一个int compare()方法，所以是一个函数式接口。而且还new了一个TreeMap,再追更溯源到TreeMap中，代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">TreeMap</span><span class="params">(Comparator&lt;? <span class="keyword">super</span> K&gt; comparator)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.comparator = comparator;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;由此可见 compatator这个东西最终被内含在了TreeSet中。但是这里如何实现了add时将add的参数传递过去，我还是不明白。</p>
<h2 id="EnumSet类"><a href="#EnumSet类" class="headerlink" title="EnumSet类"></a>EnumSet类</h2><p>&emsp;看名字就能知道这是什么集合，该集合中的所有元素都是指定枚举类型的枚举值。该枚举类型在创建EnumSet时显式或隐式指定。EnumSet类的顺序由枚举值在枚举类中的定义顺序来决定集合元素的顺序。<br><br>&emsp;EnumSet内部以位向量形式存储，这种形式非常经凑，高效。尤其是在批量操作时。该集合不允许加入null,但是可以查找null和删除null。<br><br>&emsp;该类不提供构造器，需通过类方法创建EnumSet对象。常用方法如下：</p>
<ul>
<li>EnumSet allOf(Class elementType):创建一个包含指定枚举类里所有枚举值的集合</li>
<li>EnumSet complemetOf(EnumSet s)：创建一个枚举类中的所有枚举值与指定集合的元素的差集</li>
<li>EnumSet copyOf(Collection c)：使用一个普通集合创建一个EnumSet集合</li>
<li>EnumSet copyOf(EnumSet s)：复制一个EnumSet集合</li>
<li>EnumSet noneOf(Class elementType)：创建一个元素类型为指定枚举类型的空EnumSet</li>
<li>EnumSet of(E first,E…rest)：创建一个包含一个或多个枚举值的EnumSet集合，传入的多个枚举值必须是同一枚举类</li>
<li>EnumSet range(E from,E to)：创建一个从from到to枚举值范围内所有枚举值的EnumSet集合</li>
</ul>
<h2 id="各Set实现类的性能分析"><a href="#各Set实现类的性能分析" class="headerlink" title="各Set实现类的性能分析"></a>各Set实现类的性能分析</h2><p>&emsp;Hashset的性能要优于TreeSet,特别是常用的添加，查询等，因为TreeSet需要额外的红黑树。Hashset的子类LinkedHahSet有一个链表，所以遍历会很快。EnumSet是性能最好的，但是元素的类型受限。</p>
<h1 id="List集合"><a href="#List集合" class="headerlink" title="List集合"></a>List集合</h1><p>&emsp;List代表有序，可重复的集合，每个元素都有类似于数组下标的索引。</p>
<h2 id="Java8改进的List接口和ListIterator接口"><a href="#Java8改进的List接口和ListIterator接口" class="headerlink" title="Java8改进的List接口和ListIterator接口"></a>Java8改进的List接口和ListIterator接口</h2><p>&emsp;List接口作为Collection接口的子接口，可以使用Collecion中的全部方法，同时，由于是有序集合，所以有一些根据索引操作集合的方法。<br></p>
<ul>
<li>void add(int index,Object element):将元素element插入到List集合的index处</li>
<li>boolean addAll(int index,Collection c):将集合c的所有元素都插入到list的index处</li>
<li>Object get(int index):返回index处的元素</li>
<li>int indexOf(Object o):返回对象o在集合中第一次出现的索引</li>
<li>int lastIndexOf(Object o):同上，返回最后一次出现的索引</li>
<li>Object remove(int index):删除并返回index处的元素</li>
<li>Object set(int index,Object element)：将index处的元素替换成element对象，返回被替换的旧元素</li>
<li>List subList(int fromIndex,int toIndex):返回从fromIndex(包含)到toIndex(不包含)的子集</li>
</ul>
<p>&emsp;除此之外，Java8还为List接口增加了两个默认方法：</p>
<ul>
<li>void replaceAll(UnaryOperator operator)：根据operator指定的计算规则重新设置List集合的所有元素</li>
<li>void sort(Comparator c):根据Comparator参数对集合重新排序</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Listtest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        List books=<span class="keyword">new</span> java.util.ArrayList();</span><br><span class="line">        books.add(<span class="keyword">new</span> String(<span class="string">"天国之秋"</span>));</span><br><span class="line">        books.add(<span class="keyword">new</span> String(<span class="string">"追忆似水年华"</span>));</span><br><span class="line">        books.add(<span class="keyword">new</span> String(<span class="string">"果壳中的宇宙"</span>));</span><br><span class="line">        books.add(<span class="keyword">new</span> String(<span class="string">"罗马人的故事"</span>));</span><br><span class="line">        books.add(<span class="keyword">new</span> String(<span class="string">"天朝的崩溃"</span>));</span><br><span class="line">        System.out.println(books);</span><br><span class="line">        books.add(<span class="number">1</span>,<span class="keyword">new</span> String(<span class="string">"编译原理"</span>));</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; books.size(); i++) &#123;</span><br><span class="line">            System.out.println(books.get(i));</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(books);</span><br><span class="line">        System.out.println(books.indexOf(<span class="keyword">new</span> String(<span class="string">"编译原理"</span>)));   <span class="comment">//(1)</span></span><br><span class="line">        books.set(<span class="number">1</span>,<span class="keyword">new</span> String(<span class="string">"C++标准库"</span>));</span><br><span class="line">        System.out.println(books);</span><br><span class="line">        System.out.println(books.subList(<span class="number">1</span>,<span class="number">2</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">[天国之秋, 追忆似水年华, 果壳中的宇宙, 罗马人的故事, 天朝的崩溃]</span><br><span class="line">天国之秋</span><br><span class="line">编译原理</span><br><span class="line">追忆似水年华</span><br><span class="line">果壳中的宇宙</span><br><span class="line">罗马人的故事</span><br><span class="line">天朝的崩溃</span><br><span class="line">[天国之秋, 编译原理, 追忆似水年华, 果壳中的宇宙, 罗马人的故事, 天朝的崩溃]</span><br><span class="line"><span class="number">1</span></span><br><span class="line">[天国之秋, C++标准库, 追忆似水年华, 果壳中的宇宙, 罗马人的故事, 天朝的崩溃]</span><br><span class="line">[C++标准库]</span><br></pre></td></tr></table></figure>

<p>&emsp;注意在代码(1)处,new了一个新的对象，这个新的“编译原理”与集合中的是不同的，但是从返回值1可以看出：List判断两个对象相等只要通过equals()就行。<br><br>&emsp;Java8新增的两个方法的代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">default</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">(Comparator&lt;? <span class="keyword">super</span> E&gt; c)</span> </span>&#123;</span><br><span class="line">    Object[] a = <span class="keyword">this</span>.toArray();</span><br><span class="line">    Arrays.sort(a, (Comparator) c);</span><br><span class="line">    ListIterator&lt;E&gt; i = <span class="keyword">this</span>.listIterator();</span><br><span class="line">    <span class="keyword">for</span> (Object e : a) &#123;</span><br><span class="line">        i.next();</span><br><span class="line">        i.set((E) e);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">default</span> <span class="keyword">void</span> <span class="title">replaceAll</span><span class="params">(UnaryOperator&lt;E&gt; operator)</span> </span>&#123;</span><br><span class="line">    Objects.requireNonNull(operator);</span><br><span class="line">    <span class="keyword">final</span> ListIterator&lt;E&gt; li = <span class="keyword">this</span>.listIterator();</span><br><span class="line">    <span class="keyword">while</span> (li.hasNext()) &#123;</span><br><span class="line">        li.set(operator.apply(li.next()));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;Comparator和UnaryOperator都是函数式接口。所以都可以使用Lambda表达式。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Listtest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        List books=<span class="keyword">new</span> java.util.ArrayList();</span><br><span class="line">        books.add(<span class="keyword">new</span> String(<span class="string">"天国之秋"</span>));</span><br><span class="line">        books.add(<span class="keyword">new</span> String(<span class="string">"追忆似水年华"</span>));</span><br><span class="line">        books.add(<span class="keyword">new</span> String(<span class="string">"果壳中的宇宙"</span>));</span><br><span class="line">        books.add(<span class="keyword">new</span> String(<span class="string">"罗马人的故事"</span>));</span><br><span class="line">        books.add(<span class="keyword">new</span> String(<span class="string">"天朝的崩溃"</span>));</span><br><span class="line">        books.sort((o1,o2)-&gt;((String)o1).length()-((String)o2).length());</span><br><span class="line">        System.out.println(books);</span><br><span class="line">        books.replaceAll(ele-&gt;((String)ele).length());</span><br><span class="line">        System.out.println(books);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;与Set只有提供了iterator()方法不同，List还提供了一个listIterator()方法，该方法返回一个ListIterator对象，ListIterator接口继承了Iterator接口，提供了专门操作List的方法。ListIterator接口除了继承Iterator接口的方法，还增加了一些方法：</p>
<ul>
<li>boolean hashPrevious():返回该迭代器关联的集合是否还有上一个元素</li>
<li>Object previous():返回该迭代器的上一个元素</li>
<li>void add(Object o):在制定位置插入一个元素</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="keyword">import</span> java.util.ListIterator;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Listtest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String[] books = &#123;<span class="string">"天国之秋"</span>, <span class="string">"算法导论"</span>,<span class="string">"具体数学"</span>,<span class="string">"深入理解Java虚拟机"</span>&#125;;</span><br><span class="line">        List bookList=<span class="keyword">new</span> ArrayList();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; books.length; i++) &#123;</span><br><span class="line">            bookList.add(books[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        ListIterator lit=bookList.listIterator();</span><br><span class="line">        <span class="keyword">while</span>(lit.hasNext())&#123;</span><br><span class="line">            System.out.println(lit.next());</span><br><span class="line">            lit.add(<span class="string">"-------------"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"========开始反向迭代=========="</span>);</span><br><span class="line">        <span class="keyword">while</span> (lit.hasPrevious())&#123;</span><br><span class="line">            System.out.println(lit.previous());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ArrayList和Vector实现类"><a href="#ArrayList和Vector实现类" class="headerlink" title="ArrayList和Vector实现类"></a>ArrayList和Vector实现类</h2><p>&emsp;ArrayList和Vector作为List的两个典型实现，完全支持前面介绍的List接口的全部功能。<br><br>&emsp;ArrayList和Vector是基于数组实现的List类，所以内部封装了一个动态的，允许再分配的Object[]数组。ArrayList和Vector对象使用<code>initialCapacity</code>参数设置数组的长度，当添加元素超过数组长度时，<code>initialCapacity</code>自动增加。<br><br>&emsp;由于这种自动的行为，所以大多数时候程序员不需要管这个<code>initialCapacity</code>的。但是一旦多次重复的添加过大的数组，造成多次修改<code>initialCapacity</code>值，这时就应该使用ensureCapacity(int minCapacity)方法一次性增加<code>initialCapacity</code>值。<br><br>&emsp;<code>initialCapacity</code>可以在创建集合时制定，如果不指定，默认值为10.<br><br>&emsp;除此之外，这两个集合还提供如下两个方法来重新分配Object[]数组：</p>
<ul>
<li>void ensureCapacity(int minCapacity):上面提到的，让数组长度增加大于或等于minCapacity值</li>
<li>void trimToSize():将数组长度调整为当前元素的个数，减少浪费的空间</li>
</ul>
<p>&emsp;ArrayList和Vector的显著区别是，ArrayList是线程不安全的，当多个线程访问ArrayList集合时，如果有修改，必须手动同步。此外，Vector是一个非常古老的类，早在集合之前就有了，在集合出现后，Vector被重写，出现了一些方法上的重复，命名不同但功能一样的情况（可能已经有所改进），不建议使用Vector类，即使它有线程安全的优点。<br><br>&emsp;Vector类还有个Stack子类，但是同样也是非常古老的，所以也不建议使用。要使用栈则应该使用List的实现类ArrayDeque，这个类也实现了Deque接口。</p>
<h2 id="固定长度的List"><a href="#固定长度的List" class="headerlink" title="固定长度的List"></a>固定长度的List</h2><p>&emsp;在以前的文章中曾经提到过Arrays这个类，专门用于对数组的一些操作。其中有一个asList(Object… a)方法，它可以把一个数组或若干个对象转换为一个List集合。这个转换而来的特殊Lsit既不是Vector也不是ArrayList，而是Arrays中的内部类ArrayList实例（与ArrayList类不是一回事）。<br><br>&emsp;对于这种特殊的List，不可以增加或删除，尽管在写代码时可以写List.add()或List.remove()，但是在运行时将产生UnsupportedOperationException异常。</p>
<h1 id="Queue集合"><a href="#Queue集合" class="headerlink" title="Queue集合"></a>Queue集合</h1><p>&emsp;Queue接口中定义了如下几个方法:</p>
<ul>
<li>void add(Object e):将指定元素加入队列的尾部</li>
<li>Object element():获取队列的头部，不删除</li>
<li>boolean offer(Object e):将指定元素加入队列的队尾，当使用容量有限的队列时，此方法比add方法好</li>
<li>Object peek():获取队列的头部元素，若队列为空，返回null</li>
<li>Object poll():返回并删除队列的头部元素，队列为空时，返回null</li>
<li>Object remove():获取队列的头部元素并删除</li>
</ul>
<h2 id="PriorityQueue实现类"><a href="#PriorityQueue实现类" class="headerlink" title="PriorityQueue实现类"></a>PriorityQueue实现类</h2><p>&emsp;PriorityQueue是一个比较标准的队列实现类，其保存顺序是按照元素大小决定的。所以调用peek和poll方法的结果不是先进先出的（FIFO）。从这个意义来说，PriorityQueue已经违反了一个队列最基本的规则。<br><br>&emsp;PriorityQueue队列不允许插入null元素。同时，由于需要排序，与TreeSet类似有自然排序和定制排序两种。</p>
<h2 id="Deque接口和ArrayDeque实现类"><a href="#Deque接口和ArrayDeque实现类" class="headerlink" title="Deque接口和ArrayDeque实现类"></a>Deque接口和ArrayDeque实现类</h2><p>&emsp;Deque接口是Queue接口的子接口，它代表一个双端队列，即两端都可出队或入队。该接口该出了以下方法从两端操作队列(不完全)：</p>
<ul>
<li>void addFirst(Object e):将指定元素插入头部</li>
<li>void addLast(Object e)：将指定元素插入尾部</li>
<li>Iterator descendingIterator()：返回该双端队列对应的迭代器，该迭代器将以逆向顺序来迭代队列中的元素</li>
<li>Object getFirst()：获取但不删除第一个</li>
<li>Object getLast()：获取但不删除最后一个</li>
<li>boolean offerGirst(Object e)：将制定元素插入开头，返回boolean值</li>
<li>boolean offerLast(Object e)：将指定元素插入末尾，返回boolean值</li>
<li>Object peekFirst()：获取但不删除该队列的第一个，如果队列为空，返回null</li>
<li>Object peekLast()：获取但不删除该队列的最后一个，如果队列为空，返回null</li>
<li>Object pollFirst()：获取并删除该队列的第一个，如果队列为空，返回null</li>
<li>Object pollLast()：获取并删除该队列的最后一个，如果队列为空，返回null</li>
<li>Object pop()：（栈方法）pop出该双端队列表示的栈的栈顶元素，相当于removeFirst()</li>
<li>void push(Object e)：（栈方法）将一个元素插入栈顶，相当于addFirst(Object e)</li>
<li>Object removeFirst()：获取并删除该双端队列的第一个元素</li>
<li>Object removeFirstOccurrence(Object o)：删除队列中第一次出现的元素o</li>
<li>Object removeLast()：获取并删除该双端队列的最后一个元素</li>
<li>boolean removeLastOccurrence(Object o)：删除队列中最后一次出现的元素o</li>
</ul>
<p>&emsp;从上面的方法可以看出，Deque接口的方法具有栈的“特质”。具体当做栈来使用则是ArrayDeque实现类。他是一个基于数组实现的双端队列。创建Deque时同样可以指定numElements参数，该参数用于指定数组长度。如果不指定，默认为16。<br><br>&emsp;下面代码示范了把ArrayDeque当做“栈”用：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayDeque;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ArrayDequeStack</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ArrayDeque stack=<span class="keyword">new</span> ArrayDeque();</span><br><span class="line">        stack.push(<span class="string">"Java编程思想"</span>);</span><br><span class="line">        stack.push(<span class="string">"组合数学"</span>);</span><br><span class="line">        stack.push(<span class="string">"现代操作系统"</span>);</span><br><span class="line">        stack.push(<span class="string">"TCP/IP详解"</span>);</span><br><span class="line">        System.out.println(stack);</span><br><span class="line">        System.out.println(stack.peek());</span><br><span class="line">        System.out.println(stack.pop());</span><br><span class="line">        System.out.println(stack);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;当然，ArrayDeque仍然可以作为一个队列使用。</p>
<h2 id="LinkedList实现类"><a href="#LinkedList实现类" class="headerlink" title="LinkedList实现类"></a>LinkedList实现类</h2><p>&emsp;LinkedList实现了Deque接口，也实现了List接口，可以根据索引来访问集合中的元素。而由于实现了Deque接口，所以上面提到的用与双端队列，用于栈，用于队列都是可以的。所有的基于数组的集合，例如ArrayList，ArrayDeque内部都是以数组保存元素的，所以随机访问的性能很好。而LinkedList是用链表实现的，随机访问的效率略差，但插入，删除时性能出色。</p>
<h2 id="各种线性表的性能分析"><a href="#各种线性表的性能分析" class="headerlink" title="各种线性表的性能分析"></a>各种线性表的性能分析</h2><p>&emsp;List就是一个线性表接口,ArrayList,LinkedList有是线性表的两种典型实现：基于数组和基于链表。Queue代表队列，Deque代表双端队列（也可用于栈）。<br><br>&emsp;尽管上一节写到了ArrayList,LinkedList之间有着性能差异，但是它的影响对于初级应用来说是微乎其微的。而且总体来说还是ArrayList更胜一筹，只在插入等操作有劣势，所以大部分时候用ArrayList就可以了。<br><br>&emsp;关于List集合，有如下建议：</p>
<ul>
<li>如果需要遍历集合元素，对于ArrayList，Vector集合，应该采用随机访问方法（get）来遍历集合；对于LinkedList，则应该采用迭代器（Iterator）来遍历集合。</li>
<li>如果需要经常执行插入，删除操作则应考虑用LinkedList集合</li>
</ul>
<h1 id="Java8增强的Map集合"><a href="#Java8增强的Map集合" class="headerlink" title="Java8增强的Map集合"></a>Java8增强的Map集合</h1><p>&emsp;Map用于保存具有映射关系的数据，Map集合里保存着两组值，一组用于保存key，另一组保存了value。key和value可以是任何引用类型的数据。key是不可以重复的。value是可以重复的。<strong>注意，value可以重复不是说一个key对应多个value，key和value是单项一对一关系，总能根据一个key找到唯一的，确定的value。</strong><br><br>&emsp;把key和value分开来看，key构成了一个set,没有顺序，不能重复。实际上Map里面确实包含一个keySet()方法，用于返回key组成的set集合。<br><br>&emsp;Set和Map的关系非常的紧密，key的存储形式和Set里的元素存储形式很像，Map的子类的名字和Set子类的名字也非常相似，例如Set接口下有HashSet,LinkedSet,SortedSet(接口)，TreeSet,EnumSet等子接口和实现类，而Map中也有HashMap,LinkedHashMap,SortedMap(接口)，TreeMap,EnumMap等子接口和实现类。正如名字所暗示的，Map的这些实现类和子接口中的key值的存储形式与对应Set集合中的元素的存储形式完全相同。Map提供了一个Entry内部类来封装key-value对，key和value组成了Entry，多个Entry构成了Map。如果把value当做key的附属，Map可以视为一个Set。<strong>事实上，在Java中实现Set是实现了一个value值全为null的Map。</strong><br><br>&emsp;再看value。把所有的value放在一起非常类似List：元素之间可以重复，每个元素可以根据索引来查找。只是Map中不使用整数值做索引值，而是使用key。<br><br>&emsp;Map可以视作两种集合的映射。<br><br>&emsp;下面列出了Map接口中定义的常用方法:</p>
<ul>
<li>void clear()：删除该Map对象中的所有key-value对</li>
<li>boolean containsKey(Object key)：查询Map中是否包含指定key</li>
<li>boolean containsValue(Object value)：查询Map中是否包含一个或多个指定value</li>
<li>Set entrySet()：返回Map中包含的key-value对组成的Set集合，每个集合元素都是Map.Entry对象</li>
<li>Object get(Object key)：返回指定key对应的value，如果不包含，则返回null</li>
<li>boolean isEmpty()：检查Map是否为空，是则返回true</li>
<li>Set keySet()：返回Map中所有的key组成的Set集合，注意与entrySet区别</li>
<li>Object put(Object key,Object value)：添加一个key-value对，如果已经存在相同的key,新的value会覆盖旧的</li>
<li>void putAll(Map m)：将指定Map中的key-value复制到本Map中</li>
<li>Object remove(Object key)：删除指定key的key-value对，返回指定key的value值</li>
<li>boolean remove(Object key,Object value):Java8新增方法，删除指定key和vaule对应的key-value对。如果删除成功返回true</li>
<li>int size()：返回Map中key-value对的个数</li>
<li>Collection values()：返回Map里所有的value组成的Collection</li>
</ul>
<p>&emsp;Map中包含一个内部类Entry，该类封装了一个key-value对。Entry包含了如下三个方法：</p>
<ul>
<li>Object getKey():返回该Entry里包含的key值</li>
<li>Object getValue()：返回该Entry里包含的value值</li>
<li>Object setValue(V value)：设置该Entry对中的value值，并返回新的value值</li>
</ul>
<p>&emsp;下面的代码示范了一部分方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.HashMap;</span><br><span class="line"><span class="keyword">import</span> java.util.Map;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MapTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Map map = <span class="keyword">new</span> HashMap();</span><br><span class="line">        map.put(<span class="string">"HTTP权威指南"</span>, <span class="number">1</span>);</span><br><span class="line">        map.put(<span class="string">"PHP 编程"</span>, <span class="number">2</span>);</span><br><span class="line">        map.put(<span class="string">"C++ Primer"</span>, <span class="number">3</span>);</span><br><span class="line">        map.put(<span class="string">"Java核心技术"</span>, <span class="number">1</span>);</span><br><span class="line">        System.out.println(map.put(<span class="string">"HTTP权威指南"</span>, <span class="number">4</span>));</span><br><span class="line">        System.out.println(map);</span><br><span class="line">        System.out.println(<span class="string">"是否包含C++ Primer:"</span> + map.containsKey(<span class="string">"C++ Primer"</span>));</span><br><span class="line">        System.out.println(<span class="string">"是否包含value为２的key:"</span> + map.containsValue(<span class="number">2</span>));</span><br><span class="line">        <span class="keyword">for</span> (Object key : map.keySet()) &#123;</span><br><span class="line">            System.out.println(key + <span class="string">"---&gt;"</span> + map.get(key));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Java为Map新增的方法"><a href="#Java为Map新增的方法" class="headerlink" title="Java为Map新增的方法"></a>Java为Map新增的方法</h2><p>&emsp;Java8除了增加了remove（Object key,Object value)默认方法外，还增加了如下方法（只列出方法声明）：</p>
<ul>
<li>Object compute(Object key, Bi Function remappingFunction)：该方法使用remappingFunction根据原key-value对计算一个新value。只要新value不为null，就使用新value覆盖原value;如果原value不为null，但新value为null，则删除原key-value对;如果原value、新value同时为null，那么该方法不改变任何key-value对，直接返回null。</li>
<li>Object computelfAbsent(Object key, Function mappingFunction):如果传给该方法的key参数在Map中对应的value为null,则使用mappingFunction根据key计算一个新的结果，如果计算结果不为null，则用计算结果覆盖原有的value。如果原Map原来不包括该key，那么该方法可能会添加一组key-value对。</li>
<li>Object computeIfPresent(Object key, BiFunction remappingFunction)：如果传给该方法的key参数在Map中对应的value不为null,该方法将使用remappingFunction根据原key、value计算一个新的结果，如果计算结果不为null,则使用该结果覆盖原来的value;如果计算结果为null,则删除原key-value对。</li>
<li>void forEach(BiConsumer action)：该方法是Java8为Map新增的一个遍历key-value对的方法，通过该方法可以更简洁地遍历Map的key-value对。</li>
<li>Object getOrDefault(Object key, V defaultValue)：获取指定key对应的value。如果该key不存在，则返回 defaultValue。</li>
<li>Object merge(Object key, Object value, BiFunction remappingFunction):该方法会先根据key参数获取该Map中对应的value。如果获取的value为null，则直接用传入的value覆盖原有的value(在这种情况下，可能要添加一组key-value对）；如果获取的value不为null，则使用remappingFunction函数根据原value、新value计算一个新的结果，并用得到的结果去覆盖原有的 value。</li>
<li>Object putIfAbsent(Object key, Object value)：该方法会自动检测指定key对应的value是否为null,如果该key对应的value为null,该方法将会用新value代替原来的null值。</li>
<li>Object replace(Object key, Object value):将Map中指定key对应的value替换成新value。与传统put()方法不同的是，该方法不可能添加新的key-value对。如果尝试替换的key在原Map中不存在，该方法不会添加key-value对，而是返回null。</li>
<li>boolean replace(K key, V oldValue, V newValue):将Map中指定key-value对的原value替换成新value。如果在Map中找到指定的key-value对，则执行替换并返回true ,否则返回false。</li>
<li>replaceAll(BiFunction function)：该方法使用BiFunction对原key-value对执行计算，并将计算结果作为该key-value对的value值。</li>
</ul>
<h2 id="Java8改进的HashMap和Hashtable实现类"><a href="#Java8改进的HashMap和Hashtable实现类" class="headerlink" title="Java8改进的HashMap和Hashtable实现类"></a>Java8改进的HashMap和Hashtable实现类</h2><p>&emsp;HashMap和Hashtable实现类是Map接口的典型实现类，它们的关系类似ArrayList和Vector的关系。同样的Hashtable也是非常古老的实现类，有两个繁琐的方法elements()和keys()方法，前者类似于Map接口中定义的values方法，后者类似与Map中的keySet方法。<br><br>&emsp;Java8改进了HashMap的实现，使得在使用HashMap时，出现了key冲突仍就具有较好的性能。HashMap和Hashtable的典型区别在于：</p>
<ul>
<li>Hashtable是线程安全的，HashMap不是，所以后者性能稍好，但是即使是为了做到线程安全，也不应使用Hashtable</li>
<li>Hashtable不允许使用null作为key和value，会引发NullPointerException异常，而HashMap可以，但是因为key是不能重复的，所以只能有一个key为null，value无此限制</li>
</ul>
<p>&emsp;用作key的对象必须实现hashCode()方法和equals()方法。与HashSet方法类似，HashMap和Hashtable不能保证元素的存储顺序。而equals方法和hashCode方法的规则与之类似，equals返回true，hashCode也应返回true。而判断是否包含某个value的方法（containsValue()）是通过equals方法。<br></p>
<h2 id="LinkedHashMap实现类"><a href="#LinkedHashMap实现类" class="headerlink" title="LinkedHashMap实现类"></a>LinkedHashMap实现类</h2><p>&emsp;LinkedHashMap内部使用双向链表来维护key-value的顺序，该顺序即插入顺序。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.LinkedHashMap;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LinkedHashMapTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        LinkedHashMap scores=<span class="keyword">new</span> LinkedHashMap();</span><br><span class="line">        scores.put(<span class="string">"语文"</span>,<span class="number">87</span>);</span><br><span class="line">        scores.put(<span class="string">"数学"</span>,<span class="number">77</span>);</span><br><span class="line">        scores.put(<span class="string">"英语"</span>,<span class="number">85</span>);</span><br><span class="line">        scores.forEach((key,value)-&gt; System.out.println(key+<span class="string">"--&gt;"</span>+value));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="使用Properties读写属性文件"><a href="#使用Properties读写属性文件" class="headerlink" title="使用Properties读写属性文件"></a>使用Properties读写属性文件</h2><p>&emsp;Properties类是Hashtable的子类，用于处理属性文件，例如ini文件。这个类用于将属性文件的“属性名=属性值”对读取到Map中，或者将MapEntry对写入到属性文件。</p>
<ul>
<li>String getProperty(String key):获取属性文件中指定属性名的属性值</li>
<li>String getProperty(String key,String defaultValue)：与上面的相似，如果不存在指定属性值，则指定默认值</li>
<li>Object setProperty(String key,String value)：设置属性值</li>
</ul>
<p>&emsp;除此之外，还有两个读写属性文件的方法：</p>
<ul>
<li>void load(InputStream inStream):从属性文件（以输入流表示）中加载key-value对，把加载的key-value追加到Properties里</li>
<li>void store(OutputStream out,String comments)：上面的反过来</li>
</ul>
<h2 id="SortedMap接口和TreeMap实现类"><a href="#SortedMap接口和TreeMap实现类" class="headerlink" title="SortedMap接口和TreeMap实现类"></a>SortedMap接口和TreeMap实现类</h2><p>&emsp;与Set接口有个子接口SortedSet,SortedSet接口有个TreeSet实现类一样，Map接口也派生出一个SortedMap子接口，SortedMap也有一个TreeMap实现类。<br><br>&emsp;TreeMap就是一个红黑树数据结构，每个key-value对即作为红黑树的一个节点TreeMap存储key-value对时，需要根据key对节点进行排序，TreeMap可以保证所有的key-value处于有序状态。同样也有两种排序方式，自然排序和定制排序：</p>
<ul>
<li>自然排序：所有key必须实现Comparable接口，并且所有key应该是同一类，否则抛出ClassCastException</li>
<li>定制排序：创建TreeMap时，传入一个Comparator对象，负责对key排序，key不要求实现Comparable接口</li>
</ul>
<p>&emsp;类似TreeSet中判断两个元素相等的标准，TreeMap的标准是：两个key通过compareTo()方法判断。equals方法的结果应和compareTo的结果相匹配。<br></p>
<ul>
<li>Map.Entry firstEntry()：返回该Map中最小key所对应的key-value对，如果该Map为空，则返 回 null。</li>
<li>Object firstKey()：返回该Map中的最小key值，如果该Map为空，则返回null。</li>
<li>Map.Entry lastEntry()：返回该Map中最大key所对应的key-value对，如果该Map为空或不存 在这样的key-value对，则都返回null。</li>
<li>Object lastKey()：返回该Map中的最大key值，如果该Map为空或不存在这样的key，则都返 回 null-</li>
<li>Map.Entry higherEntry(Object key)：返回该 Map 中位于 key 后一位的 key-value 对（即大于指定 key的最小key所对应的key-value对）。如果该Map为空，则返回null。</li>
<li>Object higherKey(Object key)：返回该Map中位于key后一位的key值（即大于指定key的最小 key值）。如果该Map为空或不存在这样的key-value对，则都返回null。</li>
<li>Map.Entry lowerEntry(Object key)：返回该 Map 中位于 key 前一位的 key-value 对（即小于指定 key的最大key所对应的key-value对）。如果该Map为空或不存在这样的key-value对，则都返 回 null。</li>
<li>Object lowerKey(Object key)：返回该Map中位于key前一位的key值（即小于指定key的最大 key值）。如果该Map为空或不存在这样的key，则都返回null。</li>
<li>NavigableMap subMap(Object fromKey, boolean fromlnclusive, Object toKey, boolean tolnclusive)： 返回该Map的子Map,其key的范围是从fromKey (是否包括取决于第二个参数）到toKey (是 否包括取决于第四个参数）。</li>
<li>SortedMap subMap(Object fromKey, Object toKey)：返回该 Map 的子 Map，其 key 的范围是从 fromKey (包括）到toKey (不包括）。</li>
<li>SortedMap tailMap(Object fromKey)：返回该 Map 的子 Map，其 key 的范围是大于 fromKey (包 括）的所有key。</li>
<li>NavigableMap tailMap(Object fromKey, boolean inclusive)：返回该 Map 的子 Map，其 key 的范围 是大于fromKey (是否包括取决于第二个参数）的所有key。</li>
<li>SortedMap headMap(Object toKey)：返回该 Map 的子 Map，其 key 的范围是小于 toKey (不包括） 的所有key。</li>
<li>NavigableMap headMap(Object toKey, boolean inclusive)：返回该 Map 的子 Map,其 key 的范围 是小于toKey (是否包括取决于第二个参数）的所有key。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.TreeMap;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">R</span> <span class="keyword">implements</span> <span class="title">Comparable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> count;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">R</span><span class="params">(<span class="keyword">int</span> count)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.count = count;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"R[count:]"</span> + count + <span class="string">"]"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object obj)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span> == obj) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (obj != <span class="keyword">null</span> &amp;&amp; obj.getClass() == R<span class="class">.<span class="keyword">class</span>) </span>&#123;</span><br><span class="line">            R r = (R) obj;</span><br><span class="line">            <span class="keyword">return</span> r.count == <span class="keyword">this</span>.count;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(Object obj)</span> </span>&#123;</span><br><span class="line">        R r = (R) obj;</span><br><span class="line">        <span class="keyword">return</span> count &gt; r.count ? <span class="number">1</span> : count &lt; r.count ? -<span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TreeMapTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        TreeMap tm = <span class="keyword">new</span> TreeMap();</span><br><span class="line">        tm.put(<span class="keyword">new</span> R(<span class="number">3</span>), <span class="string">"高级编译器设计与实现"</span>);</span><br><span class="line">        tm.put(<span class="keyword">new</span> R(-<span class="number">5</span>), <span class="string">"算法图解"</span>);</span><br><span class="line">        tm.put(<span class="keyword">new</span> R(<span class="number">9</span>), <span class="string">"Java并发编程的艺术"</span>);</span><br><span class="line">        System.out.println(tm);</span><br><span class="line">        System.out.println(tm.firstEntry());</span><br><span class="line">        System.out.println(tm.lastEntry());</span><br><span class="line">        System.out.println(tm.higherKey(<span class="keyword">new</span> R(<span class="number">2</span>)));</span><br><span class="line">        System.out.println(tm.lowerEntry(<span class="keyword">new</span> R(<span class="number">2</span>)));</span><br><span class="line">        System.out.println(tm.subMap(<span class="keyword">new</span> R(-<span class="number">1</span>), <span class="keyword">new</span> R(<span class="number">4</span>)));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="WeakHashMap实现类"><a href="#WeakHashMap实现类" class="headerlink" title="WeakHashMap实现类"></a>WeakHashMap实现类</h2><p>&emsp;WeakHashMap与HashMap的用法基本相似。与HashMap的区别在于，HashMap的key保留了对实际对象的强引用，而WeakHashMap保留的是弱引用。前者内部的key引用的所有对象不会被垃圾回收机制回收，HashMap也不会删除key对应的key-value;而后者不同，key引用的对象如果没有被其他强引用变量引用，则key引用的对象就有可能被回收，WeakHashMap也有可能删除对应的key-value。</p>
<h2 id="IdentityHashMap实现类"><a href="#IdentityHashMap实现类" class="headerlink" title="IdentityHashMap实现类"></a>IdentityHashMap实现类</h2><p>&emsp;IdentityHashMap提供了与HashMpa类似的基本相似的方法，也允许用null，也不保证顺序，唯一明显不同的就是，IdentityHashMap比HashMap更严格的比较是否相同，它使用“==”。</p>
<h2 id="EnumMap实现类"><a href="#EnumMap实现类" class="headerlink" title="EnumMap实现类"></a>EnumMap实现类</h2><p>&emsp;EnumMap是一个与枚举类一起使用的Map实现，EnumMap中的所有key都必须是单个枚举类的枚举值。创建EnumMap时必须显式或隐式指定它对应的枚举类。EnumMap具有如下特征:</p>
<ul>
<li>EnumMap在内部以数组形式保存，所以这种实现形式非常紧凑、高效。</li>
<li>EnumMap根据key的自然顺序（即枚举值在枚举类中的定义顺序）来维护key-value对的顺序。 当程序通过keySet()、entrySet()、 values()等方法遍历EnumMap时可以看到这种顺序。</li>
<li>EnumMap不允许使用null作为key,但允许使用null作为value。如果试图使用null作为key时将抛出NullPointerException异常。如果只是查询是否包含值为null的key，或只是删除值为null的key，都不会抛出异常。</li>
</ul>
<p>&emsp;在创建EnumMap时必须指定一个枚举类，从而将EnumMap和指定枚举类关联起来：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.EnumMap;</span><br><span class="line"></span><br><span class="line"><span class="keyword">enum</span> Season &#123;</span><br><span class="line">    SPRING, SUMMER, FALL, WINTER</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EnumMapTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        EnumMap enumMap=<span class="keyword">new</span> EnumMap(Season<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">        enumMap.put(Season.SUMMER,<span class="string">"炎炎夏日"</span>);</span><br><span class="line">        enumMap.put(Season.SPRING,<span class="string">"春暖花开"</span>);</span><br><span class="line">        System.out.println(enumMap);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="各Map实现类的性能分析"><a href="#各Map实现类的性能分析" class="headerlink" title="各Map实现类的性能分析"></a>各Map实现类的性能分析</h2><p>&emsp;虽然HashMap和Hashtable实现机制几乎一样，但由于Hashtable是一个古老的，线程安全的集合，所以通常它要慢一点。<br><br>&emsp;TreeMap通常要比HashMap，Hashtable要慢，尤其在插入，删除时。因为它是使用红黑树管理key-value对的。<br><br>&emsp;使用TreeMap有一个好处：TreeMap中的key-value对总是处于有序状态，无须专门进行排序操作。当TreeMap被填充之后，就可以调用keySet(),取得由key组成的Set，然后使用toArrayQ方法生成key的数组，接下来使用Arrays的binarySearch()方法在已排序的数组中快速地查询对象。<br><br>&emsp;对于一般的应用场景，程序应该多考虑使用HashMap,因为HashMap正是为快速查询设计的 (HashMap底层其实也是采用数组来存储key-value对）。但如果程序需要一个总是排好序的Map时，则<br>可以考虑使用TreeMap。<br><br>&emsp;LinkedHashMap比HashMap慢一点，因为它需要维护链表来保持Map中key-value时的添加顺序。IdentityHashMap性能没有特别出色之处，因为它采用与HashMap基本相似的实现，只是它使用”==” 而不是equalsO方法来判断元素相等。EnumMap的性能最好，但它只能使用同一个枚举类的枚举值作为key。</p>
<h1 id="HashSet和HashMap的性能选项"><a href="#HashSet和HashMap的性能选项" class="headerlink" title="HashSet和HashMap的性能选项"></a>HashSet和HashMap的性能选项</h1><p>&emsp;对于HashSet及其子类而言，它们采用hash算法来决定集合中元素的存储位置，并通过hash算法来控制集合的大小；对于HashMap、Hashtable及其子类而言，它们采用hash算法来决定Map中key 的存储，并通过hash算法来增加key集合的大小。<br><br>&emsp;hash表里可以存储元素的位置被称为“桶(bucket)”,在通常情况下，单个“桶”里存储一个元素, 此时有最好的性能：hash算法可以根据hashCode值计 算出“桶”的存储位置，接着从“桶”中取出元素。但hash表的状态是open的：在发生“hash冲突”的情况下，单个桶会存储多个元素，这些元素以链表形式存储,必须按顺序搜索。<br><br>&emsp;因为HashSet和HashMap、Hashtable都使用hash算法决定其元素(HashMap则只考虑key)的存储，因此HashSet、HashMap的hash表包含如下属性:</p>
<ul>
<li>容量(capacity)：hash表中桶的数量。</li>
<li>初始化容量(initial capacity)：创建hash表时桶的数量。HashMap和HashSet都允许在构造器中指定初始化容量。</li>
<li>尺寸(size)：当前hash表中记录的数量。</li>
<li>负载因子(load factor)：负载因子等于“size/capacity”。负载因子为0，表示空的hash表，0.5表示半满的hash表，依此类推。轻负载的hash表具有冲突少、适宜插入与查询的特点（但是使用Iterator迭代元素时比较慢）。</li>
</ul>
<p>&emsp;除此之外，hash表里还有一个“负载极限”，“负载极限”是一个0~1的数值，“负载极限”决定了hash表的最大填满程度。当hash表中的负载因子达到指定的“负载极限”时，hash表会自动成倍地增加容量（桶的数量），并将原有的对象重新分配，放入新的桶内，这称为rehashing。<br><br>&emsp;HashSet和HashMap、Hashtable的构造器允许指定一个负载极限，HashSet和HashMap、Hashtable默认的“负载极限”为 0.75，这表明当该hash表的3/4已经被填满时，hash表会发生rehashing。“负载极限”的默认值（0.75)是时间和空间成本上的一种折中：较高的“负载极限”可以降低hash表所占用的内存空间，但会增加查询数据的时间开销，而查询是最频繁的操作（HashMap的get()与put()方法都要用到查询）；较低的“负载极限”会提高查询数据的性能，但会增加hash表所占用的内存开销，程序员可以根据实际情况来调整HashSet和HashMap的“负载极限”值。</p>
<h1 id="操作结合的工具类Collections"><a href="#操作结合的工具类Collections" class="headerlink" title="操作结合的工具类Collections"></a>操作结合的工具类Collections</h1><p>&emsp;Java提供了一个操作Set，List和Map等集合的工具类：Collections.该工具类提供了大量对集合元素进行排序，查询，修改等操作，还提供了讲集合对象设置为不可变，集合对象实现同步控制等方法。<br></p>
<h2 id="排序操作"><a href="#排序操作" class="headerlink" title="排序操作"></a>排序操作</h2><p>&emsp;Collections提供了如下常用的类方法用于对List集合元素进行排序:</p>
<ul>
<li>void reverse(List list)：反转指定List集合中元素的顺序。</li>
<li>void shuffle(List list)：对 List集合元素进行随机排序（shuffle方法模拟了“洗牌”动作）。* void sort(List list)：根据元素的自然顺序对指定List集合的元素按升序进行排序。</li>
<li>void sort(List list, Comparator c)：根据指定Comparator产生的顺序对List集合元素进行排序。</li>
<li>void swap(List list, int i, int j)：将指定List集合中的i处元素和j处元素进行交换。</li>
<li>void rotate(List list, int distance)：当distance为正数时，将list集合的后distance个元素“整体”移到前面；当distance为负数时，将list集合的前distance个元素“整体”移到后面。该方法不会改变集合的长度。</li>
</ul>
<h2 id="查找、替换操作"><a href="#查找、替换操作" class="headerlink" title="查找、替换操作"></a>查找、替换操作</h2><p>&emsp;Collections还提供了如下常用的用于查找、替换集合元素的类方法:<br>int binarySearch(List list, Object key)：使用二分搜索法搜索指定的List集合，以获得指定对象在List集合中的索引。如果要使该方法可以正常工作，则必须保证List中的元素已经处于有序状态。</p>
<ul>
<li>Object max(Collection coll)：根据元素的自然顺序，返回给定集合中的最大元素。</li>
<li>Object max(Collection coll, Comparator comp)：根据Comparator指定的顺序,返回给定集合中的最大元素。</li>
<li>Object min(Collection coll)：根据元素的自然顺序，返回给定集合中的最小元素。</li>
<li>Object min(Collection coll, Comparator comp)：根据Comparator指定的顺序，返回给定集合中的最小元素。</li>
<li>void fill(List list, Object obj)：使用指定元素obj替换指定List集合中的所有元素。</li>
<li>int frequency(Collection c, Object o)：返回指定集合中指定元素的出现次数。</li>
<li>int indexOfSubList(List source, List target)：返回子List对象在父List对象中第一次出现的位置索引 ;如果父List中没有出现这样的子List,则返回-1。</li>
<li>int lastIndexOfSubList(List source, List target)：返回子List对象在父List对象中最后一次出现的位置索引；如果父List中没有出现这样的子List,则返回-1。</li>
<li>boolean replaceAll(List list, Object oldVal, Object newVal)：使用一个新值newVal替换List对象的所有旧值oldVal。</li>
</ul>
<h2 id="同步控制"><a href="#同步控制" class="headerlink" title="同步控制"></a>同步控制</h2><p>&emsp;Collections类中提供了多个synchronizedXxx()方法，该方法可以将指定集合包装成线程同步的集合，从而可以解决多线程并发访问集合时的线程安全问题。<br><br>&emsp;Java中常用的集合框架中的实现类HashSet、TreeSet、ArrayList、ArrayDeque、LinkedList、HashMap和TreeMap都是线程不安全的。如果有多个线程访问它们，而且有超过一个的线程试图修改它们，则存在线程安全的问题。Collections提供了多个类方法可以把它们包装成线程同步的集合。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SynchronizedTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Collection c = Collections.synchronizedCollection(<span class="keyword">new</span> ArrayList());</span><br><span class="line">        List list = Collections.synchronizedList(<span class="keyword">new</span> ArrayList());</span><br><span class="line">        Set s = Collections.synchronizedSet(<span class="keyword">new</span> HashSet());</span><br><span class="line">        Map m = Collections.synchronizedMap(<span class="keyword">new</span> HashMap());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="设置不可变集合"><a href="#设置不可变集合" class="headerlink" title="设置不可变集合"></a>设置不可变集合</h2><p>&emsp;Collections提供了如下三类方法来返回一个不可变的集合:</p>
<ul>
<li>emptyXxx()：返回一个空的、不可变的集合对象，此处的集合既可以是List，也可以是SortedSet、Set,还可以是Map、SortedMap等。</li>
<li>singletonXxx()：返回一个只包含指定对象（只有一个或一项元素）的、不可变的集合对象，此处的集合既可以是List,还可以是Map。</li>
<li>unmodifiableXxx()：返回指定集合对象的不可变视图，此处的集合既可以是List，也可以是Set、SortedSet,还可以是 Map、SorteMap 等。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UnmodifiableTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        List unmodifiableList = Collections.emptyList();</span><br><span class="line">        Set unmodifiableSet = Collections.singleton(<span class="string">"具体数学"</span>);</span><br><span class="line">        Map scores = <span class="keyword">new</span> HashMap();</span><br><span class="line">        scores.put(<span class="string">"语文"</span>, <span class="number">85</span>);</span><br><span class="line">        scores.put(<span class="string">"数学"</span>, <span class="number">95</span>);</span><br><span class="line">        Map unmodifiableMap = Collections.unmodifiableMap(scores);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Enumeration接口"><a href="#Enumeration接口" class="headerlink" title="Enumeration接口"></a>Enumeration接口</h1><p>&emsp;Enumeration接口是Iterator迭代器的“古老版本”，从JDK1.0开始，Enumeration接口就已经存在了（Iterator从JDK1.2才出现）。Enumeration接口只有两个名字很长的方法。<br></p>
<ul>
<li>boolean hasMoreElements()：如果此迭代器还有剩下的元素，则返回true。</li>
<li>Object nextElement()：返回该迭代器的下一个元素，如果还有的话（否则抛出异常）。</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>&emsp;给出一篇文章作为总结：<a href="https://www.cnblogs.com/ysocean/p/6555373.html" target="_blank" rel="noopener">https://www.cnblogs.com/ysocean/p/6555373.html</a>。<br><br>&emsp;这张图也很不错，单独放出来：<img src="/2018/05/10/Java%E7%AC%94%E8%AE%B010/%E9%9B%86%E5%90%88.jpg" alt="集合"><br>&emsp;全文完，共三万五千余字。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java笔记1</title>
    <url>/2018/03/29/Java%E7%AC%94%E8%AE%B01/</url>
    <content><![CDATA[<p>最近学习Java有了一些小收获，不成体系，在此记录。</p>
<a id="more"></a>

<p>1.Eclipse的源码体系结构</p>
<p>这里只提最基础的，毕竟刚开始学习。</p>
<p>首先需要新建一个Java project，一个project中可能会有多个package，包中是源码，也就是以<code>.java</code>结尾的文档，这其中就是类。一个<code>.java</code>的文档中可以有多个类，但是只能有一个公共类，且公共类的名字要与文档名相同。一个包中可以有多个<code>.java</code>的文档。</p>
<hr>
<p>2.一个包中可以有多个<code>.java</code>的文档，而且每个都可以有一个main方法。</p>
<hr>
<p>3.Java的数据类型分为两大类，包括基础数据类型和引用类型。</p>
<p>基础数据类型就是常说的八种，byte，short，int，long等等，但是要注意的是，Java不像C++那样没有严格指定数据的长度，而是规定死了，不需要为硬件平台的兼容担心。</p>
<p>注意，Boolean类型的不允许用数字转换，只有true和false。</p>
<hr>
<p>4.类是对象的抽象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Clothes</span> </span>&#123;</span><br><span class="line">	String color;</span><br><span class="line">	<span class="keyword">char</span> size;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>声明一个类，然后就可以让类为模板创建对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Clothes c1 = <span class="keyword">new</span> Clothes();</span><br></pre></td></tr></table></figure>

<p>注意，这里分两步。在堆上先new一个Clothes对象，然后将c1这个“门牌号”参考到新的对象/实例上。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Clothes2</span></span>&#123;</span><br><span class="line">	String color;</span><br><span class="line">	<span class="keyword">char</span> size;</span><br><span class="line">	Clothes2(String color,<span class="keyword">char</span> size)&#123;      <span class="comment">//构造函数</span></span><br><span class="line">		<span class="keyword">this</span>.color = color;</span><br><span class="line">		<span class="keyword">this</span>.size = size;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Field2</span></span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">	Clothes2 sun = <span class="function">nes <span class="title">Clothes2</span><span class="params">(<span class="string">"red"</span>,<span class="string">'S'</span>)</span></span>;</span><br><span class="line">	Clothes2 spring = <span class="function">nes <span class="title">Clothes2</span><span class="params">(<span class="string">"green"</span>,<span class="string">'M'</span>)</span></span>;</span><br><span class="line">	</span><br><span class="line">	System.out.printf(<span class="string">"sun (%s, %c)%n"</span>,sun.color,sun.size);</span><br><span class="line">	System.out.printf(<span class="string">"green (%s, %c)%n"</span>,green.color,green.size);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关于构造函数，我还没深入了解。</p>
<hr>
<p>5。关于java.util.BigDecimal</p>
<p>在程序中，基本数据类型数字总是不能精确表示，但是可以使用BigDecimal类。</p>
<p>BigDecimal类提供了许多操作数字的方法，具体参考API文档。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.math.BigDecimal;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DecimalDemo</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> staic <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        BigDecimal operand1 = <span class="keyword">new</span> BigDecimal(<span class="string">"1.0"</span>);</span><br><span class="line">        BigDecimal operand2 = <span class="keyword">new</span> BigDecimal(<span class="string">"0.8"</span>);</span><br><span class="line">        BigDecimal result = operand1.subtract(operand2);</span><br><span class="line">        Syatem.out.println(result); <span class="comment">//结果为0.2</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.math.BigDecimal;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DecimalDemo</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> staic <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        BigDecimal operand1 = <span class="keyword">new</span> BigDecimal(<span class="string">"0.1"</span>);</span><br><span class="line">        BigDecimal operand2 = <span class="keyword">new</span> BigDecimal(<span class="string">"0.1"</span>);</span><br><span class="line">        BigDecimal operand3 = <span class="keyword">new</span> BigDecimal(<span class="string">"0.1"</span>);</span><br><span class="line">        BigDecimal result = <span class="keyword">new</span> BigDecimal(<span class="string">"0.3"</span>);</span><br><span class="line">        <span class="keyword">if</span>(operand1.add(operand2).add(operand3).equals(result))&#123;</span><br><span class="line">            System.out.println(<span class="string">"等于0.3"</span>)；</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"不等于0.3"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果是等于！</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">BigDecimal operand1 = <span class="keyword">new</span> BigDecimal(<span class="string">"0.1"</span>);</span><br><span class="line">BigDecimal operand2 = <span class="keyword">new</span> BigDecimal(<span class="string">"0.1"</span>);</span><br><span class="line">System.out.println(operand1==operand2);</span><br></pre></td></tr></table></figure>

<p>这将显示false!</p>
<p>问题在于，<code>=</code>一般是用于指定参考名称参考到某个对象，意思是，<code>operand1</code>这个名字参考到<code>BigDecimal(&quot;0.1&quot;)</code>，一个堆内存空间（<code>BigDecimal(&quot;0.1&quot;)</code>）的标签。而<code>==</code>的意思是比较两个参考的对象是否是同一个，不是比较内容的值，也可以换个理解方式，<code>operand1</code>这个标签的值就是<code>BigDecimal(&quot;0.1&quot;)</code>，这种说法很不严谨。这里创建了两个对象，具有不同的标签，虽然他们的属性（“0.1”）是一样的。如果要比较内含值，则这样：<code>operand1.equals(operand2)</code>.</p>
<hr>
<p>6.基本类型打包器</p>
<p>在Java中一切都是对象，出来基本数据类型，这是为了效率留下来的，但是有的时候我们想依据这些基本数据类型打包一些操作（方法）给它，这样它就成为了对象。这里需要用到基本类型打包器。</p>
<p>它的用处简单说就是把基本数据变成对象，使其可以带有方法。有Long,Integer,Double,Float,Byte等。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IntegerDemo</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> data1=<span class="number">10</span>;</span><br><span class="line">        <span class="keyword">int</span> data2=<span class="number">20</span>;</span><br><span class="line">        Integer wrapper1=<span class="keyword">new</span> Integer(data1);</span><br><span class="line">        Integer wrapper2=<span class="keyword">new</span> Integer(data2);</span><br><span class="line">        System.out.println(data1/<span class="number">3</span>);</span><br><span class="line">        System.out.println(wrapper1.doubleValue()/<span class="number">3</span>);</span><br><span class="line">        System.out.println(wrapper1.doubleValue(wrapper2));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>打包器可以自动装箱也可以自动拆箱：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer wrapper=<span class="number">10</span>;<span class="comment">//自动装箱，把一个int的数字自动转换为Integer类型的对象</span></span><br><span class="line"><span class="keyword">int</span> foo=wrapper;<span class="comment">//自动拆箱，把一个Integer类的对象拆开</span></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Servlet</title>
    <url>/2020/06/27/Servlet/</url>
    <content><![CDATA[<p>Servlet使用.</p>
<a id="more"></a>

<p>servlet是Javaweb开发的重要部分.</p>
<p>一个简单的项目结构如下图:</p>
<p><img src="/2020/06/27/Servlet/%E9%A1%B9%E7%9B%AE%E7%BB%93%E6%9E%84.jpg" alt="结构图"></p>
<p>现在的项目还是开发模式,采用war explored,将web模块发布到Tomcat,web文件夹下的WEB-INF文件夹内有lib文件夹和web.xml,前者是在Tomcat中运行时需要用到的jar包,后者是servlet的配置文件.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">web-app</span> <span class="attr">xmlns</span>=<span class="string">"http://xmlns.jcp.org/xml/ns/javaee"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">version</span>=<span class="string">"4.0"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--给tomcat配置servlet程序--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--给Servlet程序写一个别名,一般是类名--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>HelloServlet2<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span>com.compony.yl.HelloServlet2<span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>HelloServlet3<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span>com.compony.yl.HelloServlet3<span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--给servlet配置访问地址--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--告诉服务器当前配置的额地址给那个servlet程序--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>HelloServlet2<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--访问地址,/表示http://ip:port/工程路径--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/hello2<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>HelloServlet3<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/hello3<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">web-app</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>每一个src文件夹下的servlet代码文件对应一个<servlet></servlet>配置,每个<servlet></servlet>配置又和<servlet-mapping></servlet-mapping>对应(这里只配了两个).当使用Tomcat访问web工程时,即打开<a href="http://localhost:8080/Book_Web时,默认访问该工程的index文件,即index.jsp" target="_blank" rel="noopener">http://localhost:8080/Book_Web时,默认访问该工程的index文件,即index.jsp</a>.</p>
<p><img src="/2020/06/27/Servlet/IDEA%E8%AE%BE%E7%BD%AE.jpg" alt="设置"></p>
<p>把url改为<a href="http://localhost:8080/Book_Web/hello3时,就会在web.xml中找到对应的servlet-mapping,然后根据里面的servlet-name找到servlet的全类名,并据此执行servlet程序" target="_blank" rel="noopener">http://localhost:8080/Book_Web/hello3时,就会在web.xml中找到对应的servlet-mapping,然后根据里面的servlet-name找到servlet的全类名,并据此执行servlet程序</a>.</p>
<p><img src="/2020/06/27/Servlet/%E5%85%B3%E7%B3%BB.jpg" alt="体系"></p>
<p>Servlet是顶级接口,GenericServlet是实现了Servlet接口,里面大部分都是空实现(不是空实现的方法都和ServletConfig相关),HttpServlet继承了GenericServlet,实现了一些方法.而自己写的Servlet方法一般是继承HttpServlet类,重写doGet和doPost方法,对不同类型的请求分别响应.</p>
<p>下面是实现Servlet接口的写法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.compony.yl;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.servlet.Servlet;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.ServletConfig;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.ServletRequest;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.ServletResponse;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletRequest;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YLxia</span></span><br><span class="line"><span class="comment"> * servlet生命周期:</span></span><br><span class="line"><span class="comment"> * 1.执行构造器方法</span></span><br><span class="line"><span class="comment"> * 2.执行init初始化方法</span></span><br><span class="line"><span class="comment"> * 3.执行service方法</span></span><br><span class="line"><span class="comment"> * 4.执行destroy销毁方法</span></span><br><span class="line"><span class="comment"> * 一般在实际开发中是继承HttpServlet类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloServlet</span> <span class="keyword">implements</span> <span class="title">Servlet</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">HelloServlet</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"执行构造器方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(ServletConfig servletConfig)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"执行init初始化方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ServletConfig <span class="title">getServletConfig</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">service</span><span class="params">(ServletRequest servletRequest, ServletResponse servletResponse)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//service方法是专门处理请求和响应的</span></span><br><span class="line">        <span class="comment">//ServletRequest的子类可以得到请求的方式</span></span><br><span class="line">        System.out.println(<span class="string">"使用了service方法"</span>);</span><br><span class="line">        HttpServletRequest request = (HttpServletRequest) servletRequest;</span><br><span class="line">        String method = request.getMethod();</span><br><span class="line">        <span class="keyword">if</span> (<span class="string">"GET"</span>.equals(method)) &#123;</span><br><span class="line">            doGet();</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">"POST"</span>.equals(method)) &#123;</span><br><span class="line">            doPost();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"使用了get方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doPost</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"使用了post方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getServletInfo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"执行destroy销毁方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中构造器方法,初始化方法和销毁方法在只会执行一次,service方法则是每次请求该servlet时都执行.</p>
<p>在HttpServlet中,doGet和doPost方法默认是发送错误信息,因此必须要重写.</p>
<p>继承HttpServlet的写法如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.compony.yl;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.servlet.ServletException;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServlet;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletRequest;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletResponse;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/6/26 21:53</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloServlet2</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"doGET"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doPost</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"doPOST"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>ServletConfig接口在Servlet程序的配置信息类.可以获取Servlet的别名,即servlet-name的值,可以获取初始化参数init-pattern,可以获取ServletContext对象.</p>
<p>这个接口在GenericServlet中实现,如果自己实现Servlet接口,其中init方法也会用到这个接口.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(ServletConfig servletConfig)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"执行init初始化方法"</span>);</span><br><span class="line">    System.out.println(<span class="string">"获取servlet别名"</span>+servletConfig.getServletName());</span><br><span class="line">    System.out.println(<span class="string">"获取初始化参数username的值"</span> + servletConfig.getInitParameter(<span class="string">"username"</span>));</span><br><span class="line">    System.out.println(<span class="string">"获取ServletContext对象"</span> + servletConfig.getServletContext());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Servlet程序和ServletConfig对象都是由Tomcat负责创建,Servlet程序是第一次访问时创建,ServletConfig是每个Servlet程序创建时就创建一个<strong>对应</strong>的.<br>此外,在HttpServlet中重写init方法时,如果去掉了<code>super.init(config);</code>这句,然后又使用了config对象,就会出现空指针异常.因为config这个对象是由GenericServlet持有的,即在GenericServlet中的init方法中将这个config保存起来,如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(ServletConfig config)</span> <span class="keyword">throws</span> ServletException </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.config = config;</span><br><span class="line">    <span class="keyword">this</span>.init();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果在HttpServlet中重写这个init方法时不保存config,那么此时HttpServlet对象就失去了config.</p>
<p>ServletContext的用处:它表示Servlet上下文,一个web工程只有一个ServletContext实例对象.ServletContext是一个域对象.</p>
<p>ServletContext可以像Map一样存取,删除数据,即setAttribute,getAttribute,removeAttribute方法.</p>
<p>作用一:获取web.xml中配置的上下文参数context-param</p>
<p>作用二:获取工程路径</p>
<p>作用三:获取工程部署在服务器的绝对路径</p>
<p>作用四:像Map一样存取数据</p>
<p>ServletContext在web.xml中对应context-param,而ServletConfig对应的是init-param,前者是多个Servlet共享的,后者是每个Servlet独占的.</p>
<p>HttpServletRequest类:</p>
<p>每次只要有请求进入tomcat服务器,tomcat就会把发送过来的协议信息封装到servletRequest中,对于HTTP协议就是把HTTP的信息封装到HttpServletRequest中.</p>
<p>对于请求,不论是get或者post,最重要的获得请求参数都是一样的方式:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doPost</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"doPOST"</span>);</span><br><span class="line">    String username = req.getParameter(<span class="string">"username"</span>);<span class="comment">//根据指定参数获得一个值</span></span><br><span class="line">    String[] hobbies = req.getParameterValues(<span class="string">"hobby"</span>);<span class="comment">//根据参数获得一个数组</span></span><br><span class="line">    Map&lt;String, String[]&gt; parameterMap = req.getParameterMap();<span class="comment">//获得所有参数组成的Map</span></span><br><span class="line">    parameterMap.forEach((key,value)-&gt; System.out.println(key+<span class="string">':'</span>+ Arrays.toString(value)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意用post获得的中文参数会出现乱码问题,需要设置请求体字符集:<code>req.setCharacterEncoding(&quot;UTF-8&quot;);//设置请求体的字符集</code></p>
<p>请求转发:web页面请求了servlet1后,servlet1转发到servlet2继续处理.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Servlet1中的代码</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"doGET"</span>);</span><br><span class="line">    <span class="comment">//获取请求参数</span></span><br><span class="line">    String username = req.getParameter(<span class="string">"username"</span>);</span><br><span class="line">    <span class="comment">//servlet1处理逻辑</span></span><br><span class="line">    System.out.println(<span class="string">"servlet1处理"</span>);</span><br><span class="line">    <span class="comment">//在ServletContext中设置servlet1的"标签",表示servlet1已处理完成</span></span><br><span class="line">    req.setAttribute(<span class="string">"key"</span>,<span class="string">"true"</span>);</span><br><span class="line">    <span class="comment">//请求转发的去向,具体为servlet2的url-pattern</span></span><br><span class="line">    RequestDispatcher requestDispatcher = req.getRequestDispatcher(<span class="string">"/hello3"</span>);</span><br><span class="line">    <span class="comment">//前进,将响应传递到servlet2</span></span><br><span class="line">    requestDispatcher.forward(req, resp);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//Servlet2中的代码</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (request.getAttribute(<span class="string">"key"</span>)==<span class="string">"true"</span>)&#123;<span class="comment">//检查servlet1是否处理完成</span></span><br><span class="line">        <span class="comment">//servlet2处理业务</span></span><br><span class="line">        System.out.println(<span class="string">"servlet2处理"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于”转发”来说,本质仍是一次请求,共享请求数据,因为在tomcat中只有一进一出.另外地址栏一直显示的是hello2,并没有在执行servlet2是变成hello3.另外用转发可以访问WEB-INF目录,不可以访问工程外部文件.</p>
<p>对于请求转发还有细节要注意:</p>
<p>现在在web目录下建立文件夹和文件:a/b/c.html,内容为:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"en"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Title<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">这是a下的b下的c.html页面<span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"../../index.jsp"</span>&gt;</span>跳回首页<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>里面提供了一个跳转到首页的超链接.而首页里提供了一个跳到c.html的超链接.对于这种跳转,它总是根据浏览器地址栏的地址来进行跳转的:</p>
<p>例如在c.html页面时:地址栏是<a href="http://localhost:8080/Book_Web/a/b/c.html,而加上超链接后为http://localhost:8080/Book_Web/a/b/c.html/../../index.jsp,其中c.html不是目录不算,两个..使得路径回到了http://localhost:8080/Book_Web,这样再加上index.jsp就能正确访问.但是请求转发由于地址栏不会变,即在首页跳转到c.html后地址栏显示的还是首页,这时若是c.html仍是通过超链接方式来跳转就会出问题" target="_blank" rel="noopener">http://localhost:8080/Book_Web/a/b/c.html,而加上超链接后为http://localhost:8080/Book_Web/a/b/c.html/../../index.jsp,其中c.html不是目录不算,两个..使得路径回到了http://localhost:8080/Book_Web,这样再加上index.jsp就能正确访问.但是请求转发由于地址栏不会变,即在首页跳转到c.html后地址栏显示的还是首页,这时若是c.html仍是通过超链接方式来跳转就会出问题</a>.</p>
<p><img src="/2020/06/27/Servlet/%E9%94%99%E8%AF%AF.jpg" alt="错误"></p>
<p>解决方法也多种多样,比如直接在c.html中写完整地址</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"en"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Title<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">这是a下的b下的c.html页面<span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"http://localhost:8080/Book_Web/index.jsp"</span>&gt;</span>跳回首页<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>另外还有一种方法,使用base标签.base标签可以设置当前页面中所有相对路径工作时参照哪个路径来进行跳转.</p>
<p>HttpServletResponse类也是每次请求进来Tomcat就会创建一个.这个对象表示所有的响应信息.响应是通过流将信息传给客户端.一个字节流,一个字符流.字节流常用于传递二进制数据,字符流用于回传字符.这两个同时只能存在一个.有响应字符自然也有响应字符集的设置来解决中文乱码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">    <span class="comment">// response.setCharacterEncoding("UTF-8");//设置响应体的字符集</span></span><br><span class="line">    <span class="comment">// response.setHeader("Content-Type", "text/html;charset=UTF-8");//设置浏览器的字符集</span></span><br><span class="line">    response.setContentType(<span class="string">"text/html; charset=UTF-8"</span>);<span class="comment">//此方法必须要在获取流之前使用</span></span><br><span class="line">    PrintWriter writer = response.getWriter();<span class="comment">//打印流,属于字符流</span></span><br><span class="line">    writer.write(<span class="string">"中国"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请求重定向:指客户端向服务器发送请求,然后服务器给客户端一个新地址,让客户端去访问新的地址.其中,服务器先客户端响应新地址是状态码为302,新地址在响应头中.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"请求重定向"</span>);</span><br><span class="line">    response.setStatus(<span class="number">302</span>);</span><br><span class="line">    response.setHeader(<span class="string">"Location"</span>, <span class="string">"http://localhost:8080/Book_Web/hello5"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>请求重定向时不能访问WEB-INF文件夹,可以访问工程外资源.</p>
<p>更推荐的一种写法:<code>response.sendRedirect(&quot;http://localhost:8080/Book_Web/hello5&quot;);</code></p>
<p>监听器:Listener,三大组件之一,另外两个时servlet和filter.监听器一共九种,现在只有ServletContextListener还有点用.该监听器监听ServletContext对象的创建和销毁,一旦监听到这两个事件,就会调用两个相应的方法反馈.</p>
<p>contextInitialized方法:ServletContext创建时调用</p>
<p>contextDestroyed方法:ServletContext销毁时调用.</p>
<p>文件上传和下载功能:</p>
<p>上传:</p>
<p>1.要有一个from标签,method=post请求</p>
<p>2.from标签的encType属性值必须为multipart/from-data值</p>
<p>3.在from标签中使用input type=file添加上传的文件</p>
<p>上传完成后,服务端还要正确的读取,首先是不能通过参数名这样的方式来读取,必须通过流.因为数据是以流的形式传输:</p>
<p><img src="/2020/06/27/Servlet/%E8%AF%B7%E6%B1%82.jpg" alt="请求"></p>
<p>multipart/from-data表示传输的数据为多段数据,中间有分隔符.</p>
<p>解析上传的数据要用commons-fileupload和commons-io两个包.常用方法如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doPost</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"上传"</span>);</span><br><span class="line">    <span class="comment">//ServletFileUpload用于解析上传的数据</span></span><br><span class="line">    <span class="comment">//FileItem表示每一个表单项</span></span><br><span class="line">    <span class="comment">//ServletFileUpload.isMultipartContent(request)判断数据是否是多段数据</span></span><br><span class="line">    <span class="comment">//public List&lt;FileItem&gt; parseRequest(HttpServletRequest req)解析上传数据,已过时</span></span><br><span class="line">    <span class="comment">//boolean FileItem.isFormField()判断是否是表单项</span></span><br><span class="line">    <span class="comment">//String FileItem.getFieldName()获取表单项name</span></span><br><span class="line">    <span class="comment">//String FileItem.getString()获取表单项值</span></span><br><span class="line">    <span class="comment">//String FileItem.getName()获取文件名</span></span><br><span class="line">    <span class="comment">//void FileItem.write(file)将上传的文件写入file</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//上传的图片(超过10kb)是放到默认的临时文件目录,但是临时文件目录也是有大小限制的</span></span><br><span class="line">    <span class="comment">//因此对于过大的图片要自己设置临时文件目录</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ServletFileUpload.isMultipartContent(request)) &#123;</span><br><span class="line">        <span class="comment">//创建FileItemFactory的唯一实现类</span></span><br><span class="line">        DiskFileItemFactory diskFileItemFactory = <span class="keyword">new</span> DiskFileItemFactory();</span><br><span class="line">        <span class="comment">//创建用于解析上传数据的工具类ServletUpload类</span></span><br><span class="line">        ServletFileUpload servletFileUpload = <span class="keyword">new</span> ServletFileUpload(diskFileItemFactory);</span><br><span class="line">        File temp = <span class="keyword">new</span> File(request.getSession().getServletContext().getRealPath(<span class="string">"/temp"</span>));</span><br><span class="line">        temp.mkdirs();</span><br><span class="line">        System.out.println(<span class="string">"建立成功"</span>);</span><br><span class="line">        diskFileItemFactory.setRepository(temp);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            List&lt;FileItem&gt; fileItems = servletFileUpload.parseRequest(request);</span><br><span class="line">            <span class="keyword">for</span> (FileItem item : fileItems) &#123;</span><br><span class="line">                <span class="keyword">if</span> (item.isFormField()) &#123;</span><br><span class="line">                    <span class="comment">//普通表单项</span></span><br><span class="line">                    System.out.println(item.getFieldName() + <span class="string">':'</span> + item.getString(<span class="string">"UTF-8"</span>));</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    System.out.println(item.getFieldName() + <span class="string">':'</span> + item.getName());</span><br><span class="line">                    item.write(<span class="keyword">new</span> File(<span class="string">"e:\\"</span> + item.getName()));</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下载文件:</p>
<p>获取要下载的文件名,读取要下载的文件内容,把数据回传给客户端,在回传前通过响应头告诉客户端返回的数据类型,此外还要通过响应头告诉客户端收到的数据是用于下载的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.yl.temp;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.apache.commons.io.IOUtils;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.servlet.ServletContext;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.ServletException;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.ServletOutputStream;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServlet;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletRequest;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletResponse;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.InputStream;</span><br><span class="line"><span class="keyword">import</span> java.net.URLEncoder;</span><br><span class="line"><span class="keyword">import</span> java.nio.charset.StandardCharsets;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/6/29 12:51</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DownloadServlet</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doPost</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">        <span class="comment">//要下载的文件的名字</span></span><br><span class="line">        String downloadFileName = <span class="string">"1.png"</span>;</span><br><span class="line">        <span class="comment">//读取要下载的文件内容</span></span><br><span class="line">        ServletContext servletContext = getServletContext();</span><br><span class="line"></span><br><span class="line">        <span class="comment">//获取要回传的数据的数据类型</span></span><br><span class="line">        String type = servletContext.getMimeType(<span class="string">"/file/"</span> + downloadFileName);</span><br><span class="line">        System.out.println(type);</span><br><span class="line">        <span class="comment">//通过响应头告诉客户端返回的数据类型</span></span><br><span class="line">        response.setContentType(type);</span><br><span class="line">        <span class="comment">//告诉客户端传输的数据是用于下载,不然图片就会显示出来</span></span><br><span class="line">        <span class="comment">//如果文件名要是中文,还要进行URL编码</span></span><br><span class="line">        <span class="comment">// response.setHeader("Content-Disposition", "attachment; filename=" + downloadFileName);</span></span><br><span class="line">        response.setHeader(<span class="string">"Content-Disposition"</span>, <span class="string">"attachment; filename="</span> + URLEncoder.encode(<span class="string">"迷路帖.png"</span>, StandardCharsets.UTF_8));</span><br><span class="line"></span><br><span class="line">        InputStream resourceAsStream = servletContext.getResourceAsStream(<span class="string">"/file/"</span> + downloadFileName);</span><br><span class="line">        <span class="comment">//获取响应的输出流</span></span><br><span class="line">        ServletOutputStream outputStream = response.getOutputStream();</span><br><span class="line">        <span class="comment">//赋值输入流的数据给输出流</span></span><br><span class="line">        IOUtils.copy(resourceAsStream, outputStream);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Cookie:Cookie是服务器通知客户端保存键值对的一种技术,客户端有Cookie后每次请求都发送给服务器.每个Cookie大小不能超过4kb.</p>
<p>创建Cookie:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">    Cookie cookie1 = <span class="keyword">new</span> Cookie(<span class="string">"key1"</span>, <span class="string">"value1"</span>);</span><br><span class="line">    <span class="comment">//通过响应头Set-Cookie同志客户端保存Cookie,客户端收到响应后有就修改,没有就创建</span></span><br><span class="line">    response.addCookie(cookie1);</span><br><span class="line">    response.setContentType(<span class="string">"text/html; charset=UTF-8"</span>);</span><br><span class="line">    response.getWriter().write(<span class="string">"Cookie1创建成功"</span>);</span><br><span class="line"></span><br><span class="line">    Cookie cookie2 = <span class="keyword">new</span> Cookie(<span class="string">"key2"</span>, <span class="string">"value2"</span>);</span><br><span class="line">    response.addCookie(cookie2);</span><br><span class="line">    response.getWriter().write(<span class="string">"Cookie2创建成功"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>获得Cookie:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (Cookie cookie : request.getCookies()) &#123;</span><br><span class="line">        response.setContentType(<span class="string">"text/html; charset=UTF-8"</span>);</span><br><span class="line">        response.getWriter().write(<span class="string">"Cookie["</span> + cookie.getName() + <span class="string">"="</span> + cookie.getValue() + <span class="string">"]&lt;br&gt;"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2020/06/27/Servlet/cookie.jpg" alt="cookie"></p>
<p>客户端存有的cookie会在请求头中发送给服务器.</p>
<p>修改Cookie:创建一个key值相同的新cookie发给客户端即可,或者调用cookie的setValue方法.</p>
<p>Cookie的生命周期:指Cookie什么时候被销毁,setMaxAge():正数,表示在指定的秒数后被删除;负数,表示浏览器一关Cookie就会被删;0表示马上删除Cookie</p>
<p>Cookie的path可以有效的过滤哪些cookie发送给服务器,哪些不发.path属性是通过请求的地址来过滤的.</p>
<p>cookieA:path=/</p>
<p>cookieB:path=/a/b/c/</p>
<p>请求地址1:<a href="http://localhost:8080/工程路径/a.html" target="_blank" rel="noopener">http://localhost:8080/工程路径/a.html</a></p>
<p>只会发送cookieA,a.html与cookieA同级.</p>
<p>请求地址2:<a href="http://localhost:8080/工程路径/a/b/c/a.html" target="_blank" rel="noopener">http://localhost:8080/工程路径/a/b/c/a.html</a></p>
<p>cookieA,cookieB都发.cookie默认的path是工程路径.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">cookie.setPath(request.getContextPath()+<span class="string">"/abc"</span>);</span><br></pre></td></tr></table></figure>

<p>用Cookie实现免用户名登录:第一次登录时服务器先判断用户名,密码是否正确,如果正确就允许登录,顺便把正确的用户名保存为cookie发送给客户端,由客户端保存下来.等到第二次登录时,客户端发送请求进入登录页面时会将cookie发给服务器,服务器收到后就把cookie包含的用户名放入登录页面,然后返回给客户端,客户端就可以不用输用户名登录.</p>
<p>Session:session是用来维护一个客户端和服务器之间关联的一种技术.</p>
<blockquote>
<ol>
<li><p>由于HTTP协议是无状态的协议，所以服务端需要记录用户的状态时，就需要用某种机制来识具体的用户，这个机制就是Session.典型的场景比如购物车，当你点击下单按钮时，由于HTTP协议无状态，所以并不知道是哪个用户操作的，所以服务端要为特定的用户创建了特定的Session，用用于标识这个用户，并且跟踪用户，这样才知道购物车里面有几本书。这个Session是保存在服务端的，有一个唯一标识。在服务端保存Session的方法很多，内存、数据库、文件都有。集群的时候也要考虑Session的转移，在大型的网站，一般会有专门的Session服务器集群，用来保存用户会话，这个时候 Session 信息都是放在内存的，使用一些缓存服务比如Memcached之类的来放 Session。</p>
</li>
<li><p>思考一下服务端如何识别特定的客户？这个时候Cookie就登场了。每次HTTP请求的时候，客户端都会发送相应的Cookie信息到服务端。实际上大多数的应用都是用 Cookie 来实现Session跟踪的，第一次创建Session的时候，服务端会在HTTP协议中告诉客户端，需要在 Cookie 里面记录一个Session ID，以后每次请求把这个会话ID发送到服务器，我就知道你是谁了。有人问，如果客户端的浏览器禁用了 Cookie 怎么办？一般这种情况下，会使用一种叫做URL重写的技术来进行会话跟踪，即每次HTTP交互，URL后面都会被附加上一个诸如 sid=xxxxx 这样的参数，服务端据此来识别用户。</p>
</li>
<li><p>Cookie其实还可以用在一些方便用户的场景下，设想你某次登陆过一个网站，下次登录的时候不想再次输入账号了，怎么办？这个信息可以写到Cookie里面，访问网站的时候，网站页面的脚本可以读取这个信息，就自动帮你把用户名给填了，能够方便一下用户。这也是Cookie名称的由来，给用户的一点甜头。</p>
</li>
</ol>
<p>所以，总结一下：Session是在服务端保存的一个数据结构，用来跟踪用户的状态，这个数据可以保存在集群、数据库、文件中；Cookie是客户端保存用户信息的一种机制，用来记录用户的一些信息，也是实现Session的一种方式。</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">    HttpSession session = request.getSession();</span><br><span class="line">    <span class="comment">//判断session是否是新的</span></span><br><span class="line">    <span class="keyword">boolean</span> aNew = session.isNew();</span><br><span class="line">    System.out.println(aNew);</span><br><span class="line">    <span class="comment">//获取session会话的唯一标识Id</span></span><br><span class="line">    String id = session.getId();</span><br><span class="line">    System.out.println(id);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">request.getSession().setAttribute(<span class="string">"skey1"</span>,<span class="string">"svalue1"</span>);<span class="comment">//设置session值</span></span><br><span class="line">Object skey1 = request.getSession().getAttribute(<span class="string">"skey1"</span>);<span class="comment">//读取session值</span></span><br></pre></td></tr></table></figure>

<p>session也可以设置超时时长;</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">HttpSession session = request.getSession();</span><br><span class="line"><span class="comment">//默认session超时时长为半小时,使用该方法为指定session设置超时时长,单位为秒.</span></span><br><span class="line">session.setMaxInactiveInterval(<span class="number">30</span>*<span class="number">60</span>);</span><br><span class="line"><span class="comment">//session马上超时</span></span><br><span class="line">session.invalidate();</span><br></pre></td></tr></table></figure>

<p>在web.xml中可以为所有session指定时长,单位为分钟:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">session-config</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">session-timeout</span>&gt;</span>20<span class="tag">&lt;/<span class="name">session-timeout</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">session-config</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>超时时长指的是在指定时间内没有新的请求.如果在30分钟后没有新请求,session就被销毁,但要是30分钟内有请求进来,超时时长就会被重置.</p>
<p>cookie和session关系:</p>
<p>当客户端第一次发出请求时,没有cookie,服务器创建会话对象session,而服务器每次创建session对象都会创建一个cookie对象,该cookie的key为JSESSION,value就是session的id值.这个特别的cookie发给客户端后,客户端就创建这个cookie对象.以后客户端的每次请求就会把cookie形式的session id发给服务端.然后通过这个传过来的id值去找内存中的session对象.</p>
<p>表单重复提交:</p>
<p>1.表单提交后刷新页面又一次提交,造成数据重复.解决方法是在doPost方法中成功处理数据提交后将页面<strong>重定向</strong>到新页面,此时刷新的就是新页面,不会重复提交数据.</p>
<p>2.用户正常提交,但是由于网络等原因迟迟未收到服务器响应信息,用户又点击提交.</p>
<p>3.用户提交后进入其他页面,又回退到提交页面,也会重复提交.</p>
<p>后两种情况要用到验证码.</p>
<p>当用户第一次请求登录页面,要给表单随机生成一个验证码字符串,然后把这个验证码存到session中,还要把验证码生成为图片显示在表单中.用户收到登陆页面的响应后开始填写信息和验证码(填的验证码在表单中)并提交.服务端收到信息后先获取session中的验证码并删除,获取表单信息和表单中的验证码,并将这个填写的验证码和session中的比较.相等就允许操作,不相等就不允许操作.那么第一肯定是可以的(只要验证码没写错),第二次就不可以,因为此时session中验证码为空,和用户提交的肯定不一样.</p>
<p>使用验证码需要导入谷歌的kaptcha包.</p>
<p>Filter:过滤器是三大组件之一,也是一个接口,主要作用是拦截请求,过滤响应.常见应用场景:权限检查,日记操作,事务管理.</p>
<p>权限管理:使得admin目录登录后才能访问.用户登录后会把用户信息保存在session中,所以可以通过检查session中是否有用户登录的信息来判断.</p>
<p>举例:admin目录中有一个a.jsp文件,在jsp文件中可以先获取session的user值,如果为空,则转发请求到登录页面.</p>
<p>但问题是,HTML和图片怎么办?就需要用到filter过滤器.</p>
<p>首先在web.xml中配置;</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>AdminFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span>com.company.yl.filter.AdminFilter<span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>AdminFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置拦截路径--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/admin/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.yl.filter;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.servlet.*;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletRequest;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpSession;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/6/30 10:09</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AdminFilter</span> <span class="keyword">implements</span> <span class="title">Filter</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">(ServletRequest req, ServletResponse resp, FilterChain chain)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">        HttpServletRequest httpServletRequest= (HttpServletRequest) req;</span><br><span class="line">        HttpSession session = httpServletRequest.getSession();</span><br><span class="line">        Object user = session.getAttribute(<span class="string">"user"</span>);</span><br><span class="line">        <span class="keyword">if</span> (user == <span class="keyword">null</span>) &#123;</span><br><span class="line">            req.getRequestDispatcher(<span class="string">"/login"</span>).forward(req, resp);</span><br><span class="line">        &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">            chain.doFilter(req, resp);<span class="comment">//过滤后继续执行的步骤</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(FilterConfig config)</span> <span class="keyword">throws</span> ServletException </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Filter的生命周期涉及到几个方法:构造器方法,init初始化方法,doFilter方法,destroy方法.前两个是工程启动时执行,filter创建.第三个是每次拦截到请求就会执行.最后一个是工程关闭时执行.</p>
<p>Filter与servlet类似,也有一个FilterConfig类.tomcat创建一个filter时就会创建一个filterconfig,这里面包含了配置信息.</p>
<p>FilterConfig的作用是获取filter的配置内容:获取filter的别名,获取init-param初始化参数,获取ServletContext对象.</p>
<p>FilterChain:过滤器链,多个过滤器组合起来</p>
<p><img src="/2020/06/27/Servlet/chain.jpg" alt="chain"></p>
<p>需要有两个filter,并在web.xml中配置这两个过滤器同时拦截同一个页面,拦截顺序由web.xml中顺序决定.</p>
<p>当不满足filter2时,那么执行的是filter1的前置代码,filter2的前置代码,然后直接回到filter1,执行filter1的后置代码.</p>
<p>如果filter1不满足条件,那就只会执行filter1的前置代码.</p>
<p>多个filter执行的特点是:所有filter和资源访问默认都在同一个线程中;多个filter执行时共用同一个request对象.</p>
<p>filter的拦截路径:精确匹配,/target.jsp,明确指定了请求地址;目录匹配,/admin/<em>,表示admin目录下所有资源;后缀匹配,\</em>.jsp,表示所有jsp的请求被拦截.注意,后缀名匹配不需要/开头.</p>
<p>Filter还有一个重要作用,可以统一处理事务.为doFilter方法加上try-catch-finally.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">(ServletRequest servletRequest, ServletResponse servletResponse, FilterChain filterChain)</span> <span class="keyword">throws</span> IOException, ServletException </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        filterChain.doFilter(servletRequest,servletResponse);</span><br><span class="line">        JdbcUtils.commitAndClose();<span class="comment">// 提交事务</span></span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        JdbcUtils.rollbackAndClose();<span class="comment">//回滚事务</span></span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(e);<span class="comment">//把异常抛给Tomcat管理展示友好的错误页面</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>doFilter放行,使得继续执行某些操作,例如下订单,如果正常进行就提交事务,不正常就回滚事务.这样可以避免在各个请求处理的servlet中都写这样的事务处理.</p>
<p>由Tomcat来管理展示错误的界面需要在web.xml中配置.首先准备好一个错误页面,然后在web.xml配置:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">error-page</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--针对500(服务器内部错误)--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">error-code</span>&gt;</span>500<span class="tag">&lt;/<span class="name">error-code</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">location</span>&gt;</span>/login<span class="tag">&lt;/<span class="name">location</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">error-page</span>&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>SSM整合开发</title>
    <url>/2020/07/07/SSM%E6%95%B4%E5%90%88%E5%BC%80%E5%8F%91/</url>
    <content><![CDATA[<p>SSM整合.</p>
<a id="more"></a>

<p>先导入maven依赖:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span> <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.company<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>SSM-CURD<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">packaging</span>&gt;</span>war<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">name</span>&gt;</span>SSM-CURD Maven Webapp<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- FIXME change it to the project's website --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://www.example.com<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">project.build.sourceEncoding</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">project.build.sourceEncoding</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">maven.compiler.source</span>&gt;</span>14<span class="tag">&lt;/<span class="name">maven.compiler.source</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">maven.compiler.target</span>&gt;</span>14<span class="tag">&lt;/<span class="name">maven.compiler.target</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.11<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-webmvc<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.2.7.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-jdbc<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.2.7.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.mybatis<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.5.5<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.mybatis<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis-spring<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.0.5<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>druid<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.1.23<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-java<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>8.0.20<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>jstl<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jstl<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>javax.servlet<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>javax.servlet-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.0.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">finalName</span>&gt;</span>SSM-CURD<span class="tag">&lt;/<span class="name">finalName</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">pluginManagement</span>&gt;</span><span class="comment">&lt;!-- lock down plugins versions to avoid using Maven defaults (may be moved to parent pom) --&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-clean-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.1.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- see http://maven.apache.org/ref/current/maven-core/default-bindings.html#Plugin_bindings_for_war_packaging --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-resources-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.0.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-compiler-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.8.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-surefire-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.22.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-war-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.2.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-install-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.5.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-deploy-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.8.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">pluginManagement</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>注意两点:1,在properties中设置编译级别;2,区别两个东西,一个是javax.servlet-api,另外一个是servlet-api,前者是servlet3.0后的,应该用这个.另外Spring用新的5.2.7的,开始写的5.0.2有安全问题,传到Github时给我发警告了.</p>
<p>上来最重要的配置就是配置servlet,即web.xml:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">web-app</span> <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns</span>=<span class="string">"http://java.sun.com/xml/ns/javaee"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_2_5.xsd"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">id</span>=<span class="string">"WebApp_ID"</span> <span class="attr">version</span>=<span class="string">"2.5"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">display-name</span>&gt;</span>Archetype Created Web Application<span class="tag">&lt;/<span class="name">display-name</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--启动Spring容器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context-param</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>contextConfigLocation<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>classpath:applicationContext.xml<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">context-param</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--用于创建ServletContext监听器,将Spring容器放入域中,保证唯一--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">listener</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">listener-class</span>&gt;</span>org.springframework.web.context.ContextLoaderListener<span class="tag">&lt;/<span class="name">listener-class</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">listener</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置前端的控制器,拦截所有请求--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>dispatcherServlet<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span>org.springframework.web.servlet.DispatcherServlet<span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">init-param</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>contextConfigLocation<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>classpath:springmvc.xml<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">init-param</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">load-on-startup</span>&gt;</span>1<span class="tag">&lt;/<span class="name">load-on-startup</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>dispatcherServlet<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--拦截的请求路径--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--设置字符过滤器,要放在所有过滤器前面--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>characterEncodingFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span>org.springframework.web.filter.CharacterEncodingFilter<span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">init-param</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>encoding<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>utf-8<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">init-param</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>characterEncodingFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置Rest风格的URL,将页面普通的请求转为相应风格的请求--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>hiddenHttpMethodFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span>org.springframework.web.filter.HiddenHttpMethodFilter<span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>hiddenHttpMethodFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">web-app</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ol>
<li>加载spring配置文件,配置spring容器.另外需要一个监听器,该 监听器实现了ServletContextListener接口,用来监听ServletContext对象.在该对象创建时将Spring容器放入这个域中,保证全局唯一.</li>
<li>设置Spring MVC的前端控制器(其本质是一个HttpServlet).在init-param中设置其配置文件.在servlet-mapping的url-pattern中设置拦截路径(注意只有一个斜杠).</li>
<li>设置字符编码过滤器,要放在所有过滤器前面.</li>
<li>设置Rest风格的URL.</li>
</ol>
<p>接着配置spring MVC:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:mvc</span>=<span class="string">"http://www.springframework.org/schema/mvc"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/beans</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/mvc</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/mvc/spring-mvc.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/context</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/context/spring-context.xsd"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--关闭默认的过滤规则--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.company"</span> <span class="attr">use-default-filters</span>=<span class="string">"false"</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--作为MVC框架,只扫描Controller--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">context:include-filter</span> <span class="attr">type</span>=<span class="string">"annotation"</span> <span class="attr">expression</span>=<span class="string">"org.springframework.stereotype.Controller"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">context:component-scan</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--视图解析器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"internalResourceViewResolver"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">class</span>=<span class="string">"org.springframework.web.servlet.view.InternalResourceViewResolver"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"prefix"</span> <span class="attr">value</span>=<span class="string">"/WEB-INF/views/"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"suffix"</span> <span class="attr">value</span>=<span class="string">".jsp"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--将SpringMVC不能处理的请求交给Tomcat--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:default-servlet-handler</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--开启springmvc注解的支持--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:annotation-driven</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>Spring MVC与Spring配合时关键在于分清职责.由于Spring MVC只需要管理Controller层,所以只需要扫描带有Controller注解的类即可.其他的不用管.其它与原来一样,设置视图解析器,负责将请求解析到/WEB-INF/views/下的jsp文件.开启springmvc相关的注解支持.此外,注意将springmvc不能处理的请求发给Tomcat,让原生的servlet处理.</p>
<p>然后是配置Spring:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:aop</span>=<span class="string">"http://www.springframework.org/schema/aop"</span> <span class="attr">xmlns:tx</span>=<span class="string">"http://www.springframework.org/schema/tx"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/aop https://www.springframework.org/schema/aop/spring-aop.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--组件扫描,但是不扫描控制器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.company"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">context:exclude-filter</span> <span class="attr">type</span>=<span class="string">"annotation"</span> <span class="attr">expression</span>=<span class="string">"org.springframework.stereotype.Controller"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">context:component-scan</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--将原本在Mybatis-config中的数据源管理交给spring来做--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:property-placeholder</span> <span class="attr">location</span>=<span class="string">"druid.properties"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"druidDataSource"</span> <span class="attr">class</span>=<span class="string">"com.alibaba.druid.pool.DruidDataSource"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driverClassName"</span> <span class="attr">value</span>=<span class="string">"$&#123;jdbc.driverClassName&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span> <span class="attr">value</span>=<span class="string">"$&#123;jdbc.url&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"$&#123;jdbc.username&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"$&#123;jdbc.password&#125;"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--管理mybatis操作数据库的会话对象SQlsession--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"sqlSessionFactoryBean"</span> <span class="attr">class</span>=<span class="string">"org.mybatis.spring.SqlSessionFactoryBean"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"configLocation"</span> <span class="attr">value</span>=<span class="string">"classpath:Mybatis-config.xml"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"druidDataSource"</span>/&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--设置映射文件路径--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"mapperLocations"</span> <span class="attr">value</span>=<span class="string">"classpath:mapper/*.xml"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--在所设置的包下,将所有的接口生成动态代理对象,并由Spring管理(mapper作为接口不能实例化,需要有一个动态代理实现类)--&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--sqlSession.getMapper(UserMapper.class)这句代码最终返回的应该就是一个由jdk动态代理生成的代理类,现在就是用这个bean创建所有mapper的代理类--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.mybatis.spring.mapper.MapperScannerConfigurer"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"basePackage"</span> <span class="attr">value</span>=<span class="string">"com.company.mapper"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--========事务管理========--&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置事务管理器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"dataSourceTransactionManager"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">class</span>=<span class="string">"org.springframework.jdbc.datasource.DataSourceTransactionManager"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"druidDataSource"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--开启基于注解的事务--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">aop:config</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--配置切点--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">aop:pointcut</span> <span class="attr">id</span>=<span class="string">"txPoint"</span> <span class="attr">expression</span>=<span class="string">"execution(* com.company.service..*(..))"</span>/&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--为切点配置事务增强--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">aop:advisor</span> <span class="attr">advice-ref</span>=<span class="string">"txAdvice"</span> <span class="attr">pointcut-ref</span>=<span class="string">"txPoint"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">aop:config</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--配置事务增强--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tx:advice</span> <span class="attr">id</span>=<span class="string">"txAdvice"</span> <span class="attr">transaction-manager</span>=<span class="string">"dataSourceTransactionManager"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">tx:attributes</span>&gt;</span></span><br><span class="line">            <span class="comment">&lt;!--所有方法都是事务的--&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">tx:method</span> <span class="attr">name</span>=<span class="string">"*"</span>/&gt;</span></span><br><span class="line">            <span class="comment">&lt;!--get类方法设置为只读,优化效率--&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">tx:method</span> <span class="attr">name</span>=<span class="string">"get*"</span> <span class="attr">read-only</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">tx:attributes</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tx:advice</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>Spring要做的事情比较多,首先主要是Bean管理,但是不管理Controller.接着配置了数据源,便于会话管理和事务管理.</p>
<p>Bean管理还涉及到了Mybatis中最重要的SqlSession(会话管理),这里添加了SqlSessionFactoryBean,用来创建SqlSessionFactory,这个工厂用来创建SqlSession.里面还有数据源的属性和映射文件位置的属性.同时还指定了Mybatis的配置文件.</p>
<p>Bean管理还有事务管理.Spring开启事务管理有注解和xml配置两种,这里采用的是xml.<code>&lt;tx:annotation-driven transaction-manager=&quot;transactionManager&quot; /&gt;</code>这样配置是开启注解事务,在方法上加@Transactional即可.但是使用注解与源代码耦合较高,这里还是使用xml的.使用AOP来做事务,首先设置切点<code>execution(* com.company.service..*(..))</code>表明service包中的所有类的所有方法都要切入.切入点的增强方法为下面的txAdvice,name指定了方法名.</p>
<p>接着是Mybatis配置,现在Mybatis中已经没有多少要配置的了.</p>
<p>配置工作完成后就是建数据库,然后根据数据库逆向生成Java Bean.这里略过.</p>
<p>下面是进行分页查询,这个要引入PageHelper.另外还要再mybatis的配置文件中配置.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">configuration</span></span></span><br><span class="line"><span class="meta">        <span class="meta-keyword">PUBLIC</span> <span class="meta-string">"-//mybatis.org//DTD Config 3.0//EN"</span></span></span><br><span class="line"><span class="meta">        <span class="meta-string">"http://mybatis.org/dtd/mybatis-3-config.dtd"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">settings</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">setting</span> <span class="attr">name</span>=<span class="string">"mapUnderscoreToCamelCase"</span> <span class="attr">value</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">setting</span> <span class="attr">name</span>=<span class="string">"logImpl"</span> <span class="attr">value</span>=<span class="string">"LOG4J"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">settings</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">typeAliases</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">package</span> <span class="attr">name</span>=<span class="string">"com.company.bean"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">typeAliases</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- com.github.pagehelper为PageHelper类所在包名 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugin</span> <span class="attr">interceptor</span>=<span class="string">"com.github.pagehelper.PageInterceptor"</span>&gt;</span></span><br><span class="line">            <span class="comment">&lt;!--&lt;property name="dialect" value="mysql"/&gt;--&gt;</span></span><br><span class="line">            <span class="comment">&lt;!-- 该参数默认为false --&gt;</span></span><br><span class="line">            <span class="comment">&lt;!-- 设置为true时，会将RowBounds第一个参数offset当成pageNum页码使用 --&gt;</span></span><br><span class="line">            <span class="comment">&lt;!-- 和startPage中的pageNum效果一样--&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"offsetAsPageNum"</span> <span class="attr">value</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line">            <span class="comment">&lt;!-- 该参数默认为false --&gt;</span></span><br><span class="line">            <span class="comment">&lt;!-- 设置为true时，使用RowBounds分页会进行count查询 --&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"rowBoundsWithCount"</span> <span class="attr">value</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line">            <span class="comment">&lt;!-- 设置为true时，如果pageSize=0或者RowBounds.limit = 0就会查询出全部的结果 --&gt;</span></span><br><span class="line">            <span class="comment">&lt;!-- （相当于没有执行分页查询，但是返回结果仍然是Page类型）--&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"pageSizeZero"</span> <span class="attr">value</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line">            <span class="comment">&lt;!-- 3.3.0版本可用 - 分页参数合理化，默认false禁用 --&gt;</span></span><br><span class="line">            <span class="comment">&lt;!-- 启用合理化时，如果pageNum&lt;1会查询第一页，如果pageNum&gt;pages会查询最后一页 --&gt;</span></span><br><span class="line">            <span class="comment">&lt;!-- 禁用合理化时，如果pageNum&lt;1或pageNum&gt;pages会返回空数据 --&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"reasonable"</span> <span class="attr">value</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring-AOP</title>
    <url>/2020/07/02/Spring-AOP/</url>
    <content><![CDATA[<p>Spring的AOP学习.</p>
<a id="more"></a>

<p>AOP,不通过修改源代码的方式添加新功能.</p>
<p>AOP使用动态代理实现功能增强.如果有接口,使用JDK动态代理,如果没有接口,使用CGLIB动态代理.前者是创建接口的实现类来增强被代理类功能.后者是通过创建子类来实现功能增强.</p>
<p>连接点:类里面哪些方法可以被增强,这些方法成为连接点.</p>
<p>切入点:实际被增强的方法就是切入点</p>
<p>通知(增强):实际增强的逻辑部分,有五种类型,前置通知,后置通知,环绕通知,异常通知,最终通知.</p>
<p>切面:是一种行为动作,把通知应用到切入点的过程.例如在登录前做日志记录之中前置通知就是切面.</p>
<p>利用AspectJ实现AOP操作:</p>
<p>切入点表达式:表示对类里哪个方法进行增强</p>
<p>语法:execution([权限修饰符][返回类型][类的全类名]<a href="[参数列表]">方法名称</a>)</p>
<p>例子:对com.company里UserDao的add方法增强:</p>
<p>execution(* com.compony.UserDao.add(..))    //*表示所有修饰符,..表示方法参数,返回类型被省略</p>
<p>例子:对对com.company里UserDao的所有方法增强:</p>
<p>execution(* com.compony.UserDao.*(..)) </p>
<p>对com.company里所有类的所有方法增强:</p>
<p>execution(* com.compony.*.*(..)) </p>
<p>进行通知的配置:先在spring中开启注解扫描,使用注解创建User和UserProxy对象,在增强类上添加注解,在spring配置文件中开启生成代理对象,然后配置不同类型的通知(在作为通知的方法上添加不同类型的注解和切入点表达式).</p>
<p>示例:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.aopanno;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Component;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/2 16:43</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">//被增强类</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"add"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.aopanno;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.aspectj.lang.ProceedingJoinPoint;</span><br><span class="line"><span class="keyword">import</span> org.aspectj.lang.annotation.*;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Component;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/2 16:45</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">//增强类</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Aspect</span><span class="comment">//表示生成代理对象</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserProxy</span> </span>&#123;</span><br><span class="line">    <span class="comment">//前置通知</span></span><br><span class="line">    <span class="meta">@Before</span>(value = <span class="string">"execution(* com.company.aopanno.User.add(..))"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">before</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Before"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@After</span>(value = <span class="string">"execution(* com.company.aopanno.User.add(..))"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">after</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"After"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@AfterReturning</span>(value = <span class="string">"execution(* com.company.aopanno.User.add(..))"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">afterReturn</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"AfterReturning"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@AfterThrowing</span>(value = <span class="string">"execution(* com.company.aopanno.User.add(..))"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">afterThrowing</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"AfterThrowing"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Around</span>(value = <span class="string">"execution(* com.company.aopanno.User.add(..))"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">around</span><span class="params">(ProceedingJoinPoint proceedingJoinPoint)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"环绕之前"</span>);</span><br><span class="line">        <span class="comment">//被增强的方法执行,方法可能有返回值</span></span><br><span class="line">        Object proceed = proceedingJoinPoint.proceed();</span><br><span class="line">        System.out.println(<span class="string">"环绕之后"</span>);</span><br><span class="line">        <span class="keyword">return</span> proceed;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>After是最终通知,不管有没有异常都会执行,而AfterReturning遇到异常就不会执行.<br>现在调用User中的add方法结果如下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">环绕之前</span><br><span class="line">Before</span><br><span class="line">add</span><br><span class="line">AfterReturning</span><br><span class="line">After</span><br><span class="line">环绕之后</span><br></pre></td></tr></table></figure>

<p>上面的代码可以做公共切入点的提取:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="meta">@Pointcut</span>(value = <span class="string">"execution(* com.company.aopanno.User.add(..))"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">pointdemo</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//前置通知</span></span><br><span class="line"><span class="meta">@Before</span>(value = <span class="string">"pointdemo()"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">before</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"Before"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果有多个增强类给一个切点,那么可以用@Order注解设置顺序.</p>
<p>完全注解的方式:只需要在原来的配置类上加上注解@EnableAspectJAutoProxy,表示开启Aspectj生成代理对象.</p>
]]></content>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring</title>
    <url>/2020/07/01/Spring-IOC/</url>
    <content><![CDATA[<p>Spring的IOC容器.</p>
<a id="more"></a>

<h1 id="IOC容器"><a href="#IOC容器" class="headerlink" title="IOC容器"></a>IOC容器</h1><p>(1).控制反转,把对象的创建和对象间的调用过程交给spring管理</p>
<p>(2).使用IOC目的,降低耦合度</p>
<h2 id="IOC原理"><a href="#IOC原理" class="headerlink" title="IOC原理"></a>IOC原理</h2><p>xml解析,工厂模式,反射</p>
<p>对象之间的相互调用的原始方式是直接在A类中new一个B类对象,这样耦合度太高.改进就是用工厂,在工厂中创建对象.而IOC就是用xml解析得到bean的类名,然后在工厂中根据类名创建对象,进一步降低耦合.IOC的底层就是对象工厂.spring提供两种IOC容器实现方式(两个接口):BeanFactory,ApplicationContext.前者是IOC容器的基本实现,是Spring内部使用的接口,开发人员一般不使用,后者是前者的子接口,有更多强大的子功能,开发人员使用.</p>
<p>BeanFactory加载配置文件后不会创建对象,使用这个对象时才会创建,ApplicationContext则是加载配置文件后就创建.</p>
<p>ApplicationContext的实现类:</p>
<p><img src="/2020/07/01/Spring-IOC/%E5%AE%9E%E7%8E%B0.jpg" alt="实现"></p>
<h2 id="两种配置方式"><a href="#两种配置方式" class="headerlink" title="两种配置方式"></a>两种配置方式</h2><h3 id="xml"><a href="#xml" class="headerlink" title="xml:"></a>xml:</h3><p>基于xml创建对象:<bean id="user" class="com.company.User"></bean>,其中bean标签有很多属性,例如:</p>
<ul>
<li>id:唯一标识</li>
<li>class:类的全路径</li>
</ul>
<p>创建对象是默认执行的是无参数构造方法.</p>
<p>基于xml注入属性:</p>
<p>DI:依赖注入,就是注入属性,第一种方式是使用set注入.第二种通过有参数的构造器注入.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Book</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String bname;</span><br><span class="line">    <span class="keyword">private</span> String bauthor;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setBname</span><span class="params">(String bname)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.bname = bname;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setBauthor</span><span class="params">(String bauthor)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.bauthor = bauthor;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Book&#123;"</span> +</span><br><span class="line">                <span class="string">"bname='"</span> + bname + <span class="string">'\''</span> +</span><br><span class="line">                <span class="string">", bauthor='"</span> + bauthor + <span class="string">'\''</span> +</span><br><span class="line">                <span class="string">'&#125;'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"book"</span> <span class="attr">class</span>=<span class="string">"com.company.test.Book"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--使用property完成属性注入,name是类里面属性的名字,value是向属性注入的值--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"bname"</span> <span class="attr">value</span>=<span class="string">"Java入门"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"bauthor"</span> <span class="attr">value</span>=<span class="string">"aa"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>有参构造注入需要有一个有参构造器,然后在xml中配置即可:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"orders"</span> <span class="attr">class</span>=<span class="string">"com.company.test.Orders"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--有参构造注入,可以通过name给指定属性赋值,也可以通过index索引赋值--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">constructor-arg</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">value</span>=<span class="string">"abc"</span>&gt;</span><span class="tag">&lt;/<span class="name">constructor-arg</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">constructor-arg</span> <span class="attr">name</span>=<span class="string">"address"</span> <span class="attr">value</span>=<span class="string">"chain"</span>&gt;</span><span class="tag">&lt;/<span class="name">constructor-arg</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>对于set注入还有种简化方式,p名称空间注入:</p>
<p>首先要给xml加入p名称空间:<code>xmlns:p=&quot;http://www.springframework.org/schema/p&quot;</code></p>
<p>然后在bean标签内设置:<code>&lt;bean id=&quot;book&quot; class=&quot;com.company.test.Book&quot; p:name=&quot;C#入门&quot; p:author=&quot;asd&quot;&gt;&lt;/bean&gt;</code></p>
<p>xml注入其他类型属性:</p>
<p>(1).null值:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"book"</span> <span class="attr">class</span>=<span class="string">"com.company.test.Book"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"author"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">null</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>(2).有特殊符号的值</p>
<p>在xml中转义要用到CDATA:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"book"</span> <span class="attr">class</span>=<span class="string">"com.company.test.Book"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"author"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">value</span>&gt;</span>&lt;![CDATA[</span><br><span class="line">        &lt;南京&gt;</span><br><span class="line">        ]]&gt;<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>注入外部bean:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"userService"</span> <span class="attr">class</span>=<span class="string">"service.UserService"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--在service中注入dao,name为属性的名称,ref为要注入的属性的bean的id--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"userDao"</span> <span class="attr">ref</span>=<span class="string">"userDaoImpl"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"userDaoImpl"</span> <span class="attr">class</span>=<span class="string">"dao.UserDaoImpl"</span>&gt;</span><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>与之类似的把bean写在内部:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"emp"</span> <span class="attr">class</span>=<span class="string">"bean.Emp"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">value</span>=<span class="string">"lucy"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"gender"</span> <span class="attr">value</span>=<span class="string">"女"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dept"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"dept"</span> <span class="attr">class</span>=<span class="string">"bean.Dept"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">value</span>=<span class="string">"财务部"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>级联赋值,在一个类中给另一个类的属性赋值:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"emp"</span> <span class="attr">class</span>=<span class="string">"bean.Emp"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">value</span>=<span class="string">"lucy"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"gender"</span> <span class="attr">value</span>=<span class="string">"女"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dept"</span> <span class="attr">ref</span>=<span class="string">"dept"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- name要有get方法 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dept.name"</span> <span class="attr">value</span>=<span class="string">"技术部"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>另一种写法就是在外部bean中给属性赋值.</p>
<p>xml注入集合属性:</p>
<p>1.数组和list类型,其中数组可用array和list标签,后者只能用list标签,还有个set集合用set标签即可.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"emp"</span> <span class="attr">class</span>=<span class="string">"bean.Emp"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">array</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">value</span>&gt;</span></span><br><span class="line">                java</span><br><span class="line">            <span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">value</span>&gt;</span></span><br><span class="line">                C#</span><br><span class="line">            <span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">value</span>&gt;</span></span><br><span class="line">                C++</span><br><span class="line">            <span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">array</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>2.map类型</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"emp"</span> <span class="attr">class</span>=<span class="string">"bean.Emp"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">map</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">entry</span> <span class="attr">key</span>=<span class="string">"aa"</span> <span class="attr">value</span>=<span class="string">"bb"</span>&gt;</span><span class="tag">&lt;/<span class="name">entry</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">entry</span> <span class="attr">key</span>=<span class="string">"cc"</span> <span class="attr">value</span>=<span class="string">"dd"</span>&gt;</span><span class="tag">&lt;/<span class="name">entry</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">map</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>在集合中设置对象类型的值:用多个bean标签添加多个对象,然后在集合的标签中用ref引用这些对象即可.</p>
<p>创建一个集合的bean,使其它bean能够直接使用:这时要引入新的名称空间</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:util</span>=<span class="string">"http://www.springframework.org/schema/util"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">                            http://www.springframework.org/schema/util http://www.springframework.org/schema/beans/spring-util.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">util:list</span> <span class="attr">id</span>=<span class="string">"booklist"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">value</span>&gt;</span>aa<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">value</span>&gt;</span>bb<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">value</span>&gt;</span>cc<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">util:list</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"Emp"</span> <span class="attr">class</span>=<span class="string">"bean.Emp"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"list"</span> <span class="attr">ref</span>=<span class="string">"booklist"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>引入新的名称空间,然后用<a href="util:list">util:list</a>标签即可.map,set则用<a href="util:set">util:set</a>等即可.</p>
<p>FactoryBean:在spring中有两种bean,自己创建的普通bean,另一种是一种工厂bean.(不是前面的BeanFactory).普通bean:在配置文件中定义bean类型,返回的就是该类型对象.而工厂bean在配置文件中定义的类型和返回的类型不一样.</p>
<p>工厂bean需要实现FactoryBean接口,并实现其方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> bean;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.company.test.Book;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.FactoryBean;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/1 19:13</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBean</span> <span class="keyword">implements</span> <span class="title">FactoryBean</span>&lt;<span class="title">Book</span>&gt; </span>&#123;</span><br><span class="line">    <span class="comment">//定义返回bean</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Book <span class="title">getObject</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        Book book = <span class="keyword">new</span> Book();</span><br><span class="line">        book.setAuthor(<span class="string">"aa"</span>);</span><br><span class="line">        <span class="keyword">return</span> book;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Class&lt;?&gt; getObjectType() &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isSingleton</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在配置文件中还是添加MyBean.在使用时:<code>Book book = context.getBean(&quot;MyBean&quot;, Book.class);</code>,通过MyBean得到的是Book.</p>
<p>Bean的作用域:在spring中可以设置bean是单示例还是多实例,默认是单实例的.在bean标签中的scope属性设置,该属性默认值是singleton,propotype表示多实例.若属性是前者,配置文件加载时就会创建单例对象,而后者是在调用getBean方法时才会创建对象.</p>
<p>Bean的生命周期:①通过构造器创建Bean实例(无参构造器),②为Bean属性和对其它Bean引用赋值(set),③调用Bean的初始化方法(需要配置),④Bean使用,⑤当容器关闭时,调用Bean的销毁方法(需要配置销毁方法).</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ClassPathXmlApplicationContext context = <span class="keyword">new</span> ClassPathXmlApplicationContext(<span class="string">"bean1.xml"</span>);</span><br><span class="line">    Orders order = context.getBean(<span class="string">"order"</span>, Orders<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    System.out.println(order);</span><br><span class="line">    <span class="comment">//手动销毁Bean对象</span></span><br><span class="line">    context.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"order"</span> <span class="attr">class</span>=<span class="string">"com.company.test.Orders"</span> <span class="attr">init-method</span>=<span class="string">"init"</span> <span class="attr">destroy-method</span>=<span class="string">"destroy"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">value</span>=<span class="string">"手机"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Orders</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Orders</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"无参构造方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        System.out.println(<span class="string">"set设置值"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"初始化方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"销毁"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Bean的后置处理:在调用初始化方法之前和之后会把bean实例传递给bean后置处理器的方法(如果使用后置处理器的话).</p>
<p>创建后置处理器需要实现接口BeanPostProcessor.里面有两个方法,对应初始化操作之前的后初始化之后的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBeanPost</span> <span class="keyword">implements</span> <span class="title">BeanPostProcessor</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"初始化之前执行"</span>);</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">postProcessAfterInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"初始化之后执行"</span>);</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后在配置文件中加入这个Bean即可.此外,后置处理器是对所有Bean生效的,即配置文件中的所有Bean在执行初始化方法前后都会执行后置处理,处理后置处理器自身.</p>
<p>前面通过value或者ref来指定属性值的都是手动装配,下面使用自动装配.</p>
<p>自动装配是根据指定规则(属性名称或者属性类型),spring自动将匹配的属性值进行注入.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"emp"</span> <span class="attr">class</span>=<span class="string">"bean.Emp"</span> <span class="attr">autowire</span>=<span class="string">"byName"</span>&gt;</span><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"dept"</span> <span class="attr">class</span>=<span class="string">"bean.Dept"</span>&gt;</span><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>autowire的常用值:byName表示根据名字注入,注入值bean的id值和类属性的名称一样.即第二行dept的bean的id和emp中属性名字一样.<br>byType表示根据类型去注入.使用这种方式要注意配置文件中不能有相同类型的Bean,因为不能确定装配哪个bean.在需要被注入的类Emp中有重复的属性类没关系.</p>
<p>使用配置文件:</p>
<p>原本使用数据库需要在配置文件中写明连接数据库的四大属性:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"dataSource"</span> <span class="attr">class</span>=<span class="string">"com.alibaba.druid.pool.DruidDataSource"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driverClassName"</span> <span class="attr">value</span>=<span class="string">"com.mysql.jc.jdbc.Driver"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span> <span class="attr">value</span>=<span class="string">"jdbc:mysql://localhost:3306/test"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"root"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"123"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>现在引入外部属性文件:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/context http://www.springframework.org/schema/beans/spring-context.xsd"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:property-placeholder</span> <span class="attr">location</span>=<span class="string">"classpath:jdbc.properties"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"dataSource"</span> <span class="attr">class</span>=<span class="string">"com.alibaba.druid.pool.DruidDataSource"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driverClassName"</span> <span class="attr">value</span>=<span class="string">"$&#123;prop.driverClass&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span> <span class="attr">value</span>=<span class="string">"$&#123;prop.url&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"$&#123;prop.username&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"$&#123;prop.password&#125;"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>首先要引入context名称空间,然后引入外部属性文件,然后根据属性文件中的key来获取value值.</p>
<h3 id="注解"><a href="#注解" class="headerlink" title="注解"></a>注解</h3><p>下面使用注解来实现IOC.</p>
<p>与创建对象相关的共四个注解:@Component,@Service,@Controller,@Repository,四个注解功能是一样的,但是习惯上是在特定的层使用特定的注解.第一个是宽泛的哪都行,最后一个是持久层用的.</p>
<p>使用注解方式创建对象需要引入aop这个包,然后开启组件扫描.这里需要引入context名称空间.扫描多个包可用逗号隔开.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--开启组件扫描--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.company"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>为类加上注解:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//value类似于原来的id,也可以不写,默认值为类名(首字母小写)</span></span><br><span class="line"><span class="meta">@Component</span>(value = <span class="string">"userService"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserService</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"service add"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>开启组件扫面中的一些细节:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--开启组件扫描--&gt;</span></span><br><span class="line"><span class="comment">&lt;!--use-default-filters="false"表示现在不适用默认的filter,使用自定义的,以达到扫描company包下部分类的功能--&gt;</span></span><br><span class="line"><span class="comment">&lt;!--include-filter设置扫描哪些类--&gt;</span></span><br><span class="line"><span class="comment">&lt;!--type="annotation"表示根据注解来过滤,后面一句表示只扫描带Controller注解的类--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.company"</span> <span class="attr">use-default-filters</span>=<span class="string">"false"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:include-filter</span> <span class="attr">type</span>=<span class="string">"annotation"</span> <span class="attr">expression</span>=<span class="string">"org.springframework.stereotype.Controller"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">context:component-scan</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--这里仍然使用默认的过滤器,即扫描包下所有类,但是给默认过滤器加上条件,排除Controller注解的类--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.company"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:exclude-filter</span> <span class="attr">type</span>=<span class="string">"annotation"</span> <span class="attr">expression</span>=<span class="string">"org.springframework.stereotype.Controller"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">context:component-scan</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>用注解实现属性注入:@AutoWired,表示根据属性类型注入,@Qualifer,根据属性名称注入@Resource,二者都可,@Value,注入普通类型属性,前三个注入的是对象类型的属性.</p>
<p>要在userService中调用userDaoImpl中的add方法,则要将后者注入给前者.首先用注解注入创建两个对象.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.dao;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Repository;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/1 22:37</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Repository</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserDaoImpl</span> <span class="keyword">implements</span> <span class="title">UserDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.service;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.company.dao.UserDao;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/1 22:10</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserService</span> </span>&#123;</span><br><span class="line">    <span class="comment">//为属性注入,现在不需要set方法了</span></span><br><span class="line">    <span class="meta">@Autowired</span> <span class="comment">//根据类型注入,去找UserDao类型</span></span><br><span class="line">    <span class="keyword">private</span> UserDao userDao;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"service add"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>@Qualifier注解要和@Autowired一起使用:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> UserDao userDao;</span><br></pre></td></tr></table></figure>
<p>在装配属性为接口时,如果接口只有一个实现类,或者有多个实现类但是只给一个实现类加了注解,这种情况下能找到具体的实现类.但是如果有多个,spring就无法确定这个属性到底是哪个实现类.因此要加上@Qualifier表明属性的类的名字.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserService</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="meta">@Qualifier</span>(value = <span class="string">"userDaoImpl2"</span>)</span><br><span class="line">    <span class="keyword">private</span> UserDao userDao;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"service add"</span>);</span><br><span class="line">        userDao.add();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>@Resource既可以当成@Autowired,也可以当成@Qualifier使用.该注解是javax中的.</p>
<p>对于普通属性可以用value注解,@value(value=”属性值”).</p>
<p>用配置类替代配置文件:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ComponentScan</span>(basePackages = <span class="string">"com.company"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SpringConfig</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ApplicationContext context = <span class="keyword">new</span> AnnotationConfigApplicationContext(SpringConfig<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    UserService userService = context.getBean(<span class="string">"userService"</span>, UserService<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    userService.add();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring MVC</title>
    <url>/2020/07/03/Spring-MVC/</url>
    <content><![CDATA[<p>SpringMVC使用.</p>
<a id="more"></a>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">web-app</span> <span class="meta-keyword">PUBLIC</span></span></span><br><span class="line"><span class="meta">        <span class="meta-string">"-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"</span></span></span><br><span class="line"><span class="meta">        <span class="meta-string">"http://java.sun.com/dtd/web-app_2_3.dtd"</span> &gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">web-app</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">display-name</span>&gt;</span>Archetype Created Web Application<span class="tag">&lt;/<span class="name">display-name</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>dispatcherServlet<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span>org.springframework.web.servlet.DispatcherServlet<span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--全局参数--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">init-param</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>contextConfigLocation<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>classpath:springmvc.xml<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">init-param</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--默认情况下servlet是调用时才创建,为了加载spring配置需要tomcat启动就创建,正数就是表示这个意思,正数越小优先级越高--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">load-on-startup</span>&gt;</span>1<span class="tag">&lt;/<span class="name">load-on-startup</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>dispatcherServlet<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">web-app</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>启动服务器,加载了一些配置文件:dispatcherServlet对象创建,这个servlet有一个初始化参数,用来加载spring的配置文件,同时设置了这个servlet的启动优先级,让tomcat已启动就加载它,使得spring配置能够立即生效.</p>
<p>dispatcherServlet是前端控制器,是指挥中心.根据servlet-mapping中的url-pattern,前端的/hello请求进入这个servlet(url-pattern只有一个/表示所有的都进来).</p>
<p>再看spring配置文件:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:mvc</span>=<span class="string">"http://www.springframework.org/schema/mvc"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/beans</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/mvc</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/mvc/spring-mvc.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/context</span></span></span><br><span class="line"><span class="tag"><span class="string">http://www.springframework.org/schema/context/spring-context.xsd"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.company"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--视图解析器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"internalResourceViewResolver"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">class</span>=<span class="string">"org.springframework.web.servlet.view.InternalResourceViewResolver"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"prefix"</span> <span class="attr">value</span>=<span class="string">"/WEB-INF/pages/"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"suffix"</span> <span class="attr">value</span>=<span class="string">".jsp"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--开启springmvc注解的支持--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:annotation-driven</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这里先看springmvc注解支持.在springmvc中通过注解告诉dispatcherServlet前端发来的请求由哪个controller响应.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@RequestMapping</span>(path = <span class="string">"/hello"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">sayHello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Hello SpringMVC"</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>里面的<code>path = &quot;/hello&quot;</code>表明该方法是响应/hello的.该方法返回<code>&quot;success&quot;</code>,dispatcherServlet就会用视图解析器跳到对应页面.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"internalResourceViewResolver"</span></span></span><br><span class="line"><span class="tag">        <span class="attr">class</span>=<span class="string">"org.springframework.web.servlet.view.InternalResourceViewResolver"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"prefix"</span> <span class="attr">value</span>=<span class="string">"/WEB-INF/pages/"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"suffix"</span> <span class="attr">value</span>=<span class="string">".jsp"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>prefix表示去哪个目录找,suffix表示找什么后缀的文件.</p>
<p>详细流程如下:</p>
<p><img src="/2020/07/03/Spring-MVC/%E6%B5%81%E7%A8%8B.jpg" alt="流程"></p>
<p>@RequestMapping用来建立URL与处理请求方法之间的对应关系.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.controller;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/3 21:08</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(path = <span class="string">"user"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@RequestMapping</span>(path = <span class="string">"/hello"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">sayHello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Hello SpringMVC"</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping</span>(path = <span class="string">"/test"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">testRequestMapping</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"testRequestMapping"</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在类上再加一个注解就相当于加了一层目录,访问时需要这样写:<a href="http://localhost:8080/SpringMVC/user/test" target="_blank" rel="noopener">http://localhost:8080/SpringMVC/user/test</a>.</p>
<p>@RequestMapping的属性:path和value都是指映射的路径.method规定了该方法接受那种请求方式:<code>@RequestMapping(path = &quot;/hello&quot;, method = {RequestMethod.POST, RequestMethod.GET})</code>.params规定了该方法接受的请求必须传入的参数:<code>@RequestMapping(path = &quot;/hello&quot;, params = {&quot;username&quot;}),如果请求为提供参数则不会响应.</code>.headers规定发送的请求头必须包含的部分:<code>headers={&quot;Accept&quot;}</code></p>
<p>如果请求传过来参数:</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;%--请求参数绑定--%&gt;</span><br><span class="line">&lt;a href="param/testParam?username=haha&amp;password=123"&gt;请求参数绑定&lt;/a&gt;</span><br></pre></td></tr></table></figure>
<p>只需要在响应的方法中加上对应的参数名,方法就能接收到参数值.这里注意不加@RequestMapping其实也可以使得testParam获得参数.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testParam"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testParam</span><span class="params">(String username, String password)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"执行了..."</span>);</span><br><span class="line">    System.out.println(username + <span class="string">':'</span> + password);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">"param/saveAccount"</span> <span class="attr">method</span>=<span class="string">"post"</span>&gt;</span></span><br><span class="line">    姓名:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"username"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    密码:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"password"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    金额:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"money"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"submit"</span> <span class="attr">value</span>=<span class="string">"提交"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果提交的参数较多,可以封装为一个Java Bean.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//把请求参数绑定到Javabean中</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/saveAccount"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">saveAccount</span><span class="params">(Account account)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"执行了..."</span>);</span><br><span class="line">    System.out.println(account);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Account的属性名必须要和提交的参数名一致.</p>
<p>如果Bean内部有一个引用类型:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">"param/saveAccount"</span> <span class="attr">method</span>=<span class="string">"post"</span>&gt;</span></span><br><span class="line">    姓名:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"username"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    密码:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"password"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    金额:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"money"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    用户姓名:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"user.uname"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    用户年龄:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"user.age"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"submit"</span> <span class="attr">value</span>=<span class="string">"提交"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>account内有个User类型,User的属性有uname和age.只需要在页面上改一下.注意用户输入参数时也要注意类型,年龄那就不能输字符.</p>
<p>还可以添加过滤器来处理中文乱码问题:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>characterEncodingFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span>org.springframework.web.filter.CharacterEncodingFilter<span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">init-param</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>encoding<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">init-param</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>characterEncodingFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>类似的,绑定集合也是在页面上做文章:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">"param/saveAccount"</span> <span class="attr">method</span>=<span class="string">"post"</span>&gt;</span></span><br><span class="line">    姓名:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"username"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    密码:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"password"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    金额:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"money"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">    用户姓名:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"list[0].uname"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>/&gt;</span></span><br><span class="line">    用户年龄:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"list[0].age"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    用户姓名:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"map['one'].uname"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>/&gt;</span></span><br><span class="line">    用户年龄:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"map['one'].age"</span>/&gt;</span><span class="tag">&lt;<span class="name">br</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"submit"</span> <span class="attr">value</span>=<span class="string">"提交"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>list,map是account中的的属性,list[0]表示在该索引处添加user对象的属性(已有泛型,所以确定是user的属性),map[‘one’].uname类似,表示给key为one的entry中添加user的属性.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> List&lt;User&gt; list;</span><br><span class="line"><span class="keyword">private</span> Map&lt;String, User&gt; map;</span><br></pre></td></tr></table></figure>

<p>一般提交的数据spring能做到从string正确转换为需要的格式,但是你不能确定傻逼用户会往里面填什么东西,因此必然会产生类型转换错误.所以要自定义类型转换器.</p>
<p>第一步,实现一个接口Converter,该接口有两个泛型.<code>public interface Converter&lt;S, T&gt;</code>,S表示要被转换的类型,T表示要转成的类型.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StringToDateConverter</span> <span class="keyword">implements</span> <span class="title">Converter</span>&lt;<span class="title">String</span>, <span class="title">Date</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Date <span class="title">convert</span><span class="params">(String source)</span> </span>&#123;</span><br><span class="line">        SimpleDateFormat format = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy-MM-dd"</span>);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            format.parse(source);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ParseException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--配置自定义类型转换器--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"conversionServiceFactoryBean"</span> <span class="attr">class</span>=<span class="string">"org.springframework.context.support.ConversionServiceFactoryBean"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"converters"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">set</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"com.company.utils.StringToDateConverter"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">set</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>获取Servlet原生API:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testServlet"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testServlet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> </span>&#123;</span><br><span class="line">    HttpSession session = request.getSession();</span><br><span class="line">    System.out.println(session);</span><br><span class="line">    ServletContext servletContext = session.getServletContext();</span><br><span class="line">    System.out.println(servletContext);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="常用注解"><a href="#常用注解" class="headerlink" title="常用注解"></a>常用注解</h2><p>requestParams:</p>
<p>如果前端传来的参数<code>&lt;a href=&quot;anno/testRequestParam?name=haha&quot;&gt;RequestParam&lt;/a&gt;</code>名字为name,但是方法里写的username,这就会匹配不到.在参数上加个注解:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testRequestParam"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testRequestParam</span><span class="params">(@RequestParam(name = <span class="string">"name"</span>)</span> String username) </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"执行了"</span>);</span><br><span class="line">    System.out.println(username);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个注解有个属性是required,默认为true,表示name(value)必须是给定的值,即现在传入的参数名必须为”name”,username都不行.</p>
<p>ewquestBody:</p>
<p>用于获取整个请求体.</p>
<p>pathVariable:</p>
<p>这个注解用来获得请求中占位符位置上的参数.<code>&lt;a href=&quot;anno/testPathVariable/10&quot;&gt;RequestParam&lt;/a&gt;</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testPathVariable/&#123;sid&#125;"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testRequestParam</span><span class="params">(@PathVariable(name = <span class="string">"sid"</span>)</span> String id) </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"执行了"</span>);</span><br><span class="line">    System.out.println(id);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种方式不使用?id=100这样子来传参数,而是用占位符.占位符中的和注解中的要一样,后面的id只是形参,可以不一样.这样做是为了实现restfule风格的url.即请求不是根据方法名来区别,要求每个都不一样,每个方法有自己的请求路径.restfule要求方法的路径是一样的,但是根据方法的请求方式来区别,即方法有不同的请求方式.如果有请求方式相同的方法,就要像上面那样用占位符将参数传过来,并且路径还不一样了,可以区分了.</p>
<p>但是表单只支持post get方法,其它方式不支持,所以可以用HiddentHttpMethodFilter实现更改请求的请求方式.</p>
<p>requestHeader:</p>
<p>获得请求头.</p>
<p>CookieValue:</p>
<p>获取cookie.<code>(@CookieValue(value = &quot;JSESSIONID&quot;) String cookieValue)</code></p>
<p>ModelAttribute:</p>
<p>加在一个方法上时,表示该方法会先于控制器的方法执行:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testModelAttribute"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testModelAttribute</span><span class="params">( )</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"testModelAttribute执行"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@ModelAttribute</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">showUser</span><span class="params">()</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"showUser方法执行"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当请求第一个方法时会先执行第二个方法.应用场景:需要将用户提交的数据传给一个Bean,但是传的数据并不能完整构建一个Bean,就可以用到.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testModelAttribute"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testModelAttribute</span><span class="params">(User user)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"testModelAttribute执行"</span>);</span><br><span class="line">    System.out.println(user);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@ModelAttribute</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> User <span class="title">showUser</span><span class="params">(String username)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"showUser方法执行"</span>);</span><br><span class="line">    User user = <span class="keyword">new</span> User();</span><br><span class="line">    user.setUname(username);</span><br><span class="line">    user.setAge(<span class="number">20</span>);</span><br><span class="line">    user.setDate(<span class="keyword">new</span> Date());</span><br><span class="line">    <span class="keyword">return</span> user;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>先执行后一个方法,该方法内已经构建了一个user对象,然后方法执行完user会被传到第一个方法,用用户提交的值重写赋值.当ModelAttribute注解的方法没有返回值时,则需要用Map将值存入,然后让第一个方法根据key取出.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testModelAttribute"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testModelAttribute</span><span class="params">(@ModelAttribute(<span class="string">"abc"</span>)</span> User user) </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"testModelAttribute执行"</span>);</span><br><span class="line">    System.out.println(user);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@ModelAttribute</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">showUser</span><span class="params">(String username, Map&lt;String, User&gt; map)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"showUser方法执行"</span>);</span><br><span class="line">    User user = <span class="keyword">new</span> User();</span><br><span class="line">    user.setUname(username);</span><br><span class="line">    user.setAge(<span class="number">20</span>);</span><br><span class="line">    user.setDate(<span class="keyword">new</span> Date());</span><br><span class="line">    map.put(<span class="string">"abc"</span>, user);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>SessionAttritube:用于多次执行控制器方法间的参数共享.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testSessionAttributes"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testModelAttribute</span><span class="params">(Model model)</span> </span>&#123;<span class="comment">//将这个键值对存入request域</span></span><br><span class="line">    System.out.println(<span class="string">"testSessionAttributes执行"</span>);</span><br><span class="line">    model.addAttribute(<span class="string">"msg"</span>, <span class="string">"hello"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>首先先把键值对放入request域,</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SessionAttributes</span>(value = &#123;<span class="string">"msg"</span>&#125;)<span class="comment">//将这个键值对存在session中</span></span><br></pre></td></tr></table></figure>

<p>取出值使用:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/getSessionAttributes"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">getModelAttribute</span><span class="params">(ModelMap modelMap)</span> </span>&#123;</span><br><span class="line">    String string = (String) modelMap.get(<span class="string">"msg"</span>);</span><br><span class="line">    System.out.println(string);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>清除键值对:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/deleteSessionAttributes"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">deleteSessionAttributes</span><span class="params">(SessionStatus status)</span> </span>&#123;</span><br><span class="line">    status.setComplete();</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>向前端出数据:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(value = <span class="string">"/testString"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testString</span><span class="params">(Model model)</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"执行testString"</span>);</span><br><span class="line">    <span class="comment">//模拟从数据库查出了user对象</span></span><br><span class="line">    User user = <span class="keyword">new</span> User();</span><br><span class="line">    user.setName(<span class="string">"美美"</span>);</span><br><span class="line">    user.setAge(<span class="number">25</span>);</span><br><span class="line">    user.setPassword(<span class="string">"123456"</span>);</span><br><span class="line">    <span class="comment">//model存数据</span></span><br><span class="line">    model.addAttribute(<span class="string">"user"</span>, user);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>主要是将对象放入request域中即可.</p>
<p>返回void:如果响应方法返回值是void的,默认情况下会去WEB-INF/pages下请求这个请求路径的jsp文件,即user/testVoid.jsp</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(value = <span class="string">"/testVoid"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testVoid</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ServletException,</span></span><br><span class="line"><span class="function">        IOException </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"执行testVoid"</span>);</span><br><span class="line">    <span class="comment">//编写请求转发,请求转发是一次请求,不用谢工程目录</span></span><br><span class="line">    <span class="comment">// request.getRequestDispatcher("/WEB-INF/pages/success.jsp").forward(request, response);</span></span><br><span class="line">    <span class="comment">//重定向</span></span><br><span class="line">    <span class="comment">// response.sendRedirect(request.getContextPath()+"/index.jsp");</span></span><br><span class="line">    <span class="comment">//直接响应</span></span><br><span class="line">    response.setCharacterEncoding(<span class="string">"UTF-8"</span>);</span><br><span class="line">    response.setContentType(<span class="string">"text/html; charset=UTF-8"</span>);</span><br><span class="line">    response.getWriter().println(<span class="string">"空返回值"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>返回值是ModelAndView:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(value = <span class="string">"/testModelAndView"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> ModelAndView <span class="title">testModelAndView</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ModelAndView mv = <span class="keyword">new</span> ModelAndView();</span><br><span class="line">    System.out.println(<span class="string">"testModelAndView"</span>);</span><br><span class="line">    User user = <span class="keyword">new</span> User();</span><br><span class="line">    user.setName(<span class="string">"美美"</span>);</span><br><span class="line">    user.setAge(<span class="number">25</span>);</span><br><span class="line">    user.setPassword(<span class="string">"123456"</span>);</span><br><span class="line">    <span class="comment">//把user对象存到mv对象中,其实就是存到request域中</span></span><br><span class="line">    mv.addObject(<span class="string">"user"</span>, user);</span><br><span class="line">    <span class="comment">//跳转到哪个页面</span></span><br><span class="line">    mv.setViewName(<span class="string">"success"</span>);</span><br><span class="line">    <span class="keyword">return</span> mv;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>重定向,请求转发:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testForwardOrRedirect"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testForwardOrRedirect</span><span class="params">()</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"testForwardOrRedirect"</span>);</span><br><span class="line">    <span class="comment">// return "forward:/WEB-INF/pages/success.jsp";</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">"redirect:/index.jsp"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>ResponseBody响应Json数据:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line">    <span class="comment">// 页面加载，绑定单击事件</span></span><br><span class="line">    $(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        $(<span class="string">"#btn"</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">            <span class="comment">// alert("hello btn");</span></span><br><span class="line">            <span class="comment">// 发送ajax请求</span></span><br><span class="line">            $.ajax(&#123;</span><br><span class="line">                <span class="comment">// 编写json格式，设置属性和值</span></span><br><span class="line">                url:<span class="string">"user/testAjax"</span>,</span><br><span class="line">                contentType:<span class="string">"application/json;charset=UTF-8"</span>,</span><br><span class="line">                data:<span class="string">'&#123;"username":"hehe","password":"123","age":30&#125;'</span>,</span><br><span class="line">                dataType:<span class="string">"json"</span>,</span><br><span class="line">                type:<span class="string">"post"</span>,</span><br><span class="line">                success:<span class="function"><span class="keyword">function</span>(<span class="params">data</span>)</span>&#123;</span><br><span class="line">                    <span class="comment">// data服务器端响应的json的数据，进行解析</span></span><br><span class="line">                    alert(data);</span><br><span class="line">                    alert(data.username);</span><br><span class="line">                    alert(data.password);</span><br><span class="line">                    alert(data.age);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line"></span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testAjax"</span>)</span><br><span class="line"><span class="comment">//@ResponseBody这个注解用来将User转为json</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@ResponseBody</span> <span class="function">User <span class="title">testAjax</span><span class="params">(@RequestBody User user)</span></span>&#123;<span class="comment">//原本这里是String body,后端Jackson会转成User</span></span><br><span class="line">    System.out.println(<span class="string">"testAjax方法执行了..."</span>);</span><br><span class="line">    <span class="comment">// 客户端发送ajax的请求，传的是json字符串，后端把json字符串封装到user对象中(利用jackson这个包)</span></span><br><span class="line">    System.out.println(user);</span><br><span class="line">    <span class="comment">// 做响应，模拟查询数据库</span></span><br><span class="line">    user.setUsername(<span class="string">"haha"</span>);</span><br><span class="line">    user.setAge(<span class="number">40</span>);</span><br><span class="line">    <span class="comment">// 做响应</span></span><br><span class="line">    <span class="keyword">return</span> user;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>@ResponseBody和@RequestBody一对,实现json和对象的转换.</p>
<blockquote>
<p>额外加一条:上面的代码基本和老师写的一样,调试的时候看到请求路径也是对的,可就是不访问user/testAjax,排查了一个小时,终于发现是过滤器url-pattern里少了一个斜杠:</p>
</blockquote>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">filter</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>characterEncodingFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">filter-class</span>&gt;</span>org.springframework.web.filter.CharacterEncodingFilter<span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">init-param</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>encoding<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">init-param</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>characterEncodingFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>文件上传:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--配置上传文件的解析器,其id必须是multipartResolver--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"multipartResolver"</span> <span class="attr">class</span>=<span class="string">"org.springframework.web.multipart.commons.CommonsMultipartResolver"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"maxUploadSize"</span> <span class="attr">value</span>=<span class="string">"10485760"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/fileupload2"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">fileupload2</span><span class="params">(HttpServletRequest request, MultipartFile upload)</span> <span class="keyword">throws</span> IOException </span>&#123;<span class="comment">//MultipartFile的名字必须和前端表单里的名字一样</span></span><br><span class="line">    <span class="comment">//使用spring来上传文件</span></span><br><span class="line">    <span class="comment">//spring需要配置一个文件解析器,用来解析请求中上传文件的那部分,该解析器返回一个上传文件的对象MultipartFile</span></span><br><span class="line"></span><br><span class="line">    String path = request.getSession().getServletContext().getRealPath(<span class="string">"/uploads/"</span>);</span><br><span class="line">    System.out.println(path);</span><br><span class="line">    File file = <span class="keyword">new</span> File(path);</span><br><span class="line">    <span class="keyword">if</span> (!file.exists()) &#123;</span><br><span class="line">        file.mkdirs();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    String filename = upload.getOriginalFilename();</span><br><span class="line">    filename = UUID.randomUUID().toString().replace(<span class="string">"-"</span>, <span class="string">""</span>)+<span class="string">"_"</span>+filename;</span><br><span class="line">    upload.transferTo(<span class="keyword">new</span> File(path, filename));</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>跨服务器传文件:</p>
<p>需要引入两个依赖:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.sun.jersey<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jersey-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.18.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.sun.jersey<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jersey-client<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.18.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/fileupload3"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">fileupload3</span><span class="params">(MultipartFile upload)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    <span class="comment">//定义上传文件服务器的路径</span></span><br><span class="line">    String path = <span class="string">"http://localhost:9090/springmvc/uploads/"</span>;</span><br><span class="line"></span><br><span class="line">    String filename = upload.getOriginalFilename();</span><br><span class="line">    filename = UUID.randomUUID().toString().replace(<span class="string">"-"</span>, <span class="string">""</span>) + <span class="string">"_"</span> + filename;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//创建客户端对象</span></span><br><span class="line">    Client client = Client.create();</span><br><span class="line">    <span class="comment">//和文件服务器连接</span></span><br><span class="line">    WebResource webResource = client.resource(path + filename);</span><br><span class="line">    webResource.put(upload.getBytes());</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="拦截器"><a href="#拦截器" class="headerlink" title="拦截器"></a>拦截器</h2><p>类似于过滤器:</p>
<p><img src="/2020/07/03/Spring-MVC/%E6%8B%A6%E6%88%AA%E5%99%A8.jpg" alt="拦截器"></p>
<p>多个拦截器组成的拦截器链执行顺序和过滤器也是类似的.二者的区别在于:过滤器是servlet规范中的,拦截器是spring mvc自己的.过滤器在url-pattern中配置/*后可以对所有访问拦截,而拦截器只拦截访问控制器的方法.自定义拦截器要实现HandlerInterceptor接口.</p>
<p>这个接口有三个默认方法,preHandle,postHandle,afterCompletion,分别对应预处理(controller方法执行前),后处理(controller方法执行后,页面跳转之前,post里可以跳转到新页面,但是success页面也会执行,只是不展现)和整个页面执行完后的处理(controller里return到success.jsp页面,该页面执行后执行这个方法).</p>
<p>首先配置拦截器:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--配置拦截器组--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mvc:interceptors</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:interceptor</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--要拦截的方法--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">mvc:mapping</span> <span class="attr">path</span>=<span class="string">"/user/*"</span>/&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--不要拦截的方法--&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--&lt;mvc:exclude-mapping path=""/&gt;--&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--配置拦截器对象--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"com.company.interceptor.MyInterceptor"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">mvc:interceptor</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mvc:interceptors</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>拦截器实现:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyInterceptor</span> <span class="keyword">implements</span> <span class="title">HandlerInterceptor</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 预处理,controller方法执行前</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> request</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> response</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> handler</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> true表示放行,执行下一个拦截器,如果没有就执行controller方法,false不放行</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> Exception</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"拦截器执行"</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Spring异常处理:</p>
<p>自定义异常类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.exception;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/5 14:13</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SysException</span> <span class="keyword">extends</span> <span class="title">Exception</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String message;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getMessage</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> message;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setMessage</span><span class="params">(String message)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.message = message;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">SysException</span><span class="params">(String message)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.message = message;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>抛出自定义异常:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/testException"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testException</span><span class="params">()</span> <span class="keyword">throws</span> SysException </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">int</span> a=<span class="number">10</span>/<span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> SysException(<span class="string">"发生错误"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>编写异常处理器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.exception;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.web.servlet.HandlerExceptionResolver;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.servlet.ModelAndView;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletRequest;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletResponse;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/5 14:18</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SysExceptionResolver</span> <span class="keyword">implements</span> <span class="title">HandlerExceptionResolver</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ModelAndView <span class="title">resolveException</span><span class="params">(HttpServletRequest httpServletRequest, HttpServletResponse httpServletResponse, Object o, Exception e)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//获取到异常对象</span></span><br><span class="line">        SysException exception=<span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">if</span> (e <span class="keyword">instanceof</span> SysException) &#123;</span><br><span class="line">            exception= (SysException) e;</span><br><span class="line">        &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">            e=<span class="keyword">new</span> SysException(<span class="string">"其他错误"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//创建ModelAndView</span></span><br><span class="line">        ModelAndView mv = <span class="keyword">new</span> ModelAndView();</span><br><span class="line">        mv.addObject(<span class="string">"errorMsg"</span>,e.getMessage());</span><br><span class="line">        mv.setViewName(<span class="string">"error"</span>);</span><br><span class="line">        <span class="keyword">return</span> mv;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>配置异常处理器</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--配置自定义异常处理器--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"sysExceptionResolver"</span> <span class="attr">class</span>=<span class="string">"com.company.exception.SysExceptionResolver"</span>/&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot高级</title>
    <url>/2020/07/27/SpringBoot%E9%AB%98%E7%BA%A7/</url>
    <content><![CDATA[<p>本部分设计SpringBoot的高级内容,包括缓存,消息,任务,检索,分布式等内容.</p>
<a id="more"></a>

<h1 id="SpringBoot缓存"><a href="#SpringBoot缓存" class="headerlink" title="SpringBoot缓存"></a>SpringBoot缓存</h1><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727220900803.png" alt="image-20200727220900803"></p>
<p>使用缓存时用到的部分注解:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727221025430.png" alt="image-20200727221025430"></p>
<p>在SpringBoot中使用缓存,首先要添加相关的starter:spring-boot-starter-cache.接着在启动类上加上<code>@EnableCaching</code>注解.</p>
<p>然后在加上@Cacheable注解:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Cacheable</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Employee <span class="title">getEmpById</span><span class="params">(Integer id)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"查询"</span> + id + <span class="string">"员工"</span>);</span><br><span class="line">    <span class="keyword">return</span> employeeMapper.getEmpById(id);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该注解有一些属性:</p>
<ul>
<li><p>value/cacheNames:Cache的名字,实际是一组数据的名字,即下图里的Emp Cache.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727223355222.png" alt="image-20200727223355222"></p>
<p>在一个CacheManager中有着多个Cache.而每个Cache又有着多个键值对.</p>
</li>
<li><p>key:缓存对象存储在Map集合中的key值，非必需.默认是方法参数值.以上面代码为例,参数传入1,则键值对为1-返回值.填写key时可以使用SpEL表达式:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727224630411.png" alt="image-20200727224630411"></p>
<p>注意caches那一条,这代表<strong>一个键值对可以存在多个Cache中</strong>.</p>
</li>
<li><p>keyGenerator:key的生成器.可以自己指定,要实现<code>KeyGenerator</code>接口.改参数与key互斥.</p>
</li>
<li><p>cacheManager指定缓存管理器.与之类似的是cacheResolver,也是二选一.默认的是CurrentMapCacheManager.</p>
</li>
<li><p>condition:指定符合条件的情况下缓存,也支持SpEL表达式.<strong>在方法执行之前之后都可以执行</strong>.比如要求id大于1之类的条件.此外支持条件的复合.</p>
</li>
<li><p>unless:当unless指定的条件为true时不缓存.且该表达式<strong>只在方法执行后判断</strong>.</p>
</li>
<li><p>sync是否使用异步模式</p>
</li>
</ul>
<p>使用缓存,引入缓存模块,这代表也有一个自动配置类CacheAutoConfiguration.同样是老套路,利用Bean注解注入了一些组件.另外还用@Import注解导入了CacheConfigurationImportSelector这个类.这个类实现了ImportSelector接口,具体作用就是一次性导入多个配置类:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728100508815.png" alt="image-20200728100508815"></p>
<p>其中SimpleCacheConfiguration默认生效.这个配置类注册了一个ConcurrentMapCacheManager.</p>
<p>缓存流程,以emp请求为例:</p>
<ol>
<li><p>首先根据请求上的注解<code>@Cacheable(cacheNames = &quot;emp&quot;, key = &quot;#p0&quot;)</code>找name为emp的Cache,具体方法是在ConcurrentMapCacheManager中:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728114817078.png" alt="image-20200728114817078"></p>
</li>
<li><p>现在是第一次访问,所以没有Cache.进入if,创建一个Cache.看看这个创建方法:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728115520036.png" alt="image-20200728115520036"></p>
<p>清晰的显示了Cache的结构,CurrentMapCache中的store(存具体键值对的地方)使用ConcurrentHashMap实现.接着创建完成的Cache被放入ConcurrentMapCacheManager的cacheMap中(<code>this.cacheMap.put(name,cache);</code>),根据put这种方式也可以看出CacheManager管理Cache也是键值对:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ConcurrentMap&lt;String, Cache&gt; cacheMap = <span class="keyword">new</span> ConcurrentHashMap&lt;&gt;(<span class="number">16</span>);</span><br></pre></td></tr></table></figure>

<p>所以CacheManager中:</p>
<ul>
<li>CacheManager–&gt;Cache(Cache默认是16个):用ConcurrentHashMap存储cacheNames和Cache,这个结构叫cachemap</li>
<li>Cache–&gt;key&amp;value:也是用ConcurrentHashMap存储key和value,这个结构叫store</li>
</ul>
</li>
<li><p>现在名为”emp”的Cache有了,并且放入了cachemap.然后中间经过其它操作,最终来到CurrentMapCache中的查找value的方法:</p>
</li>
</ol>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728122450834.png" alt="image-20200728122450834"></p>
<p>​    这里的key默认是使用keyGenerator生成的,具体是其实现类SimpleKeyGenerator.</p>
<p>​    现在第一次访问,查不到这个key.所以会直接调用目标方法获取查询结果.得到结果后存入store:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728135928839.png" alt="image-20200728135928839"></p>
<p>key的生成也是可以自定义的.只需要自己实现keyGenerator,然后加入容器.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span>(<span class="string">"myKeyGenerator"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> KeyGenerator <span class="title">keyGenerator</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> KeyGenerator() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> Object <span class="title">generate</span><span class="params">(Object target, Method method, Object... params)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> method.getName() + <span class="string">'['</span> + Arrays.asList(params) + <span class="string">']'</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后在注解上指定自定义的keyGenerator:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Cacheable</span>(cacheNames = <span class="string">"emp"</span>, keyGenerator = <span class="string">"myKeyGenerator"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> Employee <span class="title">getEmpById</span><span class="params">(Integer id)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"查询"</span> + id + <span class="string">"员工"</span>);</span><br><span class="line">    <span class="keyword">return</span> employeeMapper.getEmpById(id);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>另一个注解:@CachePut,既调用目标方法,有更新缓存.主要是用来<strong>更新数据库的数据同时更新缓存</strong>.它的运行时机是在调用完目标方法之后执行,因为要用结果更新缓存.使用该注解更新缓存尤其要注意的是cacheNames和key必须相同.如果调用上面的getEmp方法使得缓存有1号员工数据,这时使用update方法更新数据库内1号员工数据(更新方法上只有@Cacheable注解并指定了cacheName/value,因为是必须的),接着在用getEmp方法查就会得到缓存中过时的数据.因为@Cacheable没有指定key,默认是参数,而更新方法的参数和get方法的key并不一样.这就回导致更新缓存其实没有更新对应的.</p>
</li>
<li><p>@CacheEvict:缓存清除.通过key指定要删除的具体值,也可以是添加allEntries属性表示清除emp中的所有缓存.默认是在目标方法之后执行,可以使用beforeInvocation=true表示在之前执行.这种设置是为了避免在目标方法(delete方法之类的)出现异常,没有清除缓存.</p>
</li>
<li><p>@Caching:这是一个组合注解:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Cacheable[] cacheable() <span class="keyword">default</span> &#123;&#125;;</span><br><span class="line"></span><br><span class="line">CachePut[] put() <span class="keyword">default</span> &#123;&#125;;</span><br><span class="line"></span><br><span class="line">CacheEvict[] evict() <span class="keyword">default</span> &#123;&#125;;</span><br></pre></td></tr></table></figure>

<p>用法如下:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728152717498.png" alt="image-20200728152717498"></p>
</li>
<li><p>@CacheConfig注解用来加在类上指定一些公共的属性.例如,CacheManager,cacheNames/value,keyGenerator</p>
</li>
</ul>
<h2 id="整合Redis"><a href="#整合Redis" class="headerlink" title="整合Redis"></a>整合Redis</h2><p>需要一个spring-boot-starter-data-redis,然后在配置文件中写好主机地址等配置.注意,新版本的驱动是lettuce的,改驱动基于netty.</p>
<p>下面是一些基本操作.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpringbootcacheApplicationTests</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    StringRedisTemplate stringRedisTemplate;<span class="comment">//k-v是字符串</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    RedisTemplate redisTemplate;<span class="comment">//k-v是对象</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test01</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        stringRedisTemplate.opsForValue().append(<span class="string">"msg"</span>, <span class="string">"hello"</span>);</span><br><span class="line">        System.out.println(stringRedisTemplate.opsForValue().get(<span class="string">"msg"</span>));</span><br><span class="line">        stringRedisTemplate.opsForList().leftPush(<span class="string">"mylist"</span>, <span class="string">"aa"</span>);</span><br><span class="line">        stringRedisTemplate.opsForList().leftPush(<span class="string">"mylist"</span>, <span class="string">"bb"</span>);</span><br><span class="line">        stringRedisTemplate.opsForList().leftPush(<span class="string">"mylist"</span>, <span class="string">"cc"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果使用RedisTemplate存储对象,则该对象需要序列化.而默认的使用jdk的序列化来进行,但是在redis中可读性就非常差.</p>
<p>因此可以使用json来做序列化.这就需要自定义一个RedisTemplate:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedisConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> RedisTemplate&lt;Object, Employee&gt; <span class="title">redisTemplate</span><span class="params">(RedisConnectionFactory redisConnectionFactory)</span> </span>&#123;</span><br><span class="line">        RedisTemplate&lt;Object, Employee&gt; template = <span class="keyword">new</span> RedisTemplate&lt;&gt;();</span><br><span class="line">        template.setConnectionFactory(redisConnectionFactory);</span><br><span class="line">        template.setDefaultSerializer(<span class="keyword">new</span> Jackson2JsonRedisSerializer&lt;&gt;(Employee<span class="class">.<span class="keyword">class</span>))</span>;</span><br><span class="line">        <span class="keyword">return</span> template;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个新的RedisTemplate不仅配置了DefaultSerializer还做了泛型.此时原本的RedisTemplate就被顶替了,不想顶替改个名字就可以.</p>
<p>之前使用CacheConfigurationImportSelector导入了多个配置类,其中SimpleCacheConfiguration默认生效.而现在生效的是RedisCacheConfiguration,这个配置类添加了一个RedisCacheManager.</p>
<p>做的切换的原因是每个导入的配置类上都有一个<code>@ConditionalOnMissingBean(CacheManager.class)</code>注解.而导入这些配置类是按照上面那个数组的图来的,谁先进入容器添加了CacheManager谁就生效.</p>
<p>而RedisCacheManager会创建RedisCache.这里和默认的CurrentMapManager的逻辑是差不多的,只是在底层存储值时使用的是redis,以存储数据为例:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">put</span><span class="params">(String name, <span class="keyword">byte</span>[] key, <span class="keyword">byte</span>[] value, @Nullable Duration ttl)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   Assert.notNull(name, <span class="string">"Name must not be null!"</span>);</span><br><span class="line">   Assert.notNull(key, <span class="string">"Key must not be null!"</span>);</span><br><span class="line">   Assert.notNull(value, <span class="string">"Value must not be null!"</span>);</span><br><span class="line"></span><br><span class="line">   execute(name, connection -&gt; &#123;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (shouldExpireWithin(ttl)) &#123;</span><br><span class="line">         connection.set(key, value, Expiration.from(ttl.toMillis(), TimeUnit.MILLISECONDS), SetOption.upsert());</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">         connection.set(key, value);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">return</span> <span class="string">"OK"</span>;</span><br><span class="line">   &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>问题在于RedisCacheManager使用的RedisTemplate中序列化还是基于jdk的.因此需要自定义一个CacheManager:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> RedisCacheManager <span class="title">employeeRedisCacheManager</span><span class="params">(RedisConnectionFactory redisConnectionFactory)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//初始化一个RedisCacheWriter</span></span><br><span class="line">    RedisCacheWriter redisCacheWriter = RedisCacheWriter.nonLockingRedisCacheWriter(redisConnectionFactory);</span><br><span class="line">    <span class="comment">//设置CacheManager的值序列化方式为json序列化</span></span><br><span class="line">    RedisSerializer&lt;Object&gt; jsonSerializer = <span class="keyword">new</span> GenericJackson2JsonRedisSerializer();</span><br><span class="line">    RedisSerializationContext.SerializationPair&lt;Object&gt; pair = RedisSerializationContext.SerializationPair</span><br><span class="line">            .fromSerializer(jsonSerializer);</span><br><span class="line">    RedisCacheConfiguration defaultCacheConfig = RedisCacheConfiguration.defaultCacheConfig()</span><br><span class="line">            .serializeValuesWith(pair);</span><br><span class="line">    <span class="comment">//设置默认超过期时间是30秒</span></span><br><span class="line">    defaultCacheConfig.entryTtl(Duration.ofSeconds(<span class="number">30</span>));</span><br><span class="line">    <span class="comment">//初始化RedisCacheManager</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> RedisCacheManager(redisCacheWriter, defaultCacheConfig);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="SpringBoot与消息"><a href="#SpringBoot与消息" class="headerlink" title="SpringBoot与消息"></a>SpringBoot与消息</h1><h2 id="点对点模式和发布订阅模式"><a href="#点对点模式和发布订阅模式" class="headerlink" title="点对点模式和发布订阅模式"></a>点对点模式和发布订阅模式</h2><p>首先两种模式都只有一个发送者和多个接受者.点对点模式是接受者从消息队列中获取一个消息后,这个消息就移出队列,其它接受者收不到这个消息.而发布订阅模式则是多个接受者都能收到这个消息.</p>
<p>JMS（Java Message Service）JAVA消息服务:基于JVM消息代理的规范。ActiveMQ、HornetMQ是JMS实现 </p>
<p>AMQP（Advanced Message Queuing Protocol: – 高级消息队列协议，也是一个消息代理的规范，兼容JMS.RabbitMQ是AMQP的实现.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728220926793.png" alt="image-20200728220926793"></p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728220959795.png" alt="image-20200728220959795"></p>
<h2 id="RabbitMQ概念"><a href="#RabbitMQ概念" class="headerlink" title="RabbitMQ概念"></a>RabbitMQ概念</h2><ul>
<li><p>Message:消息,由消息头和消息体组成.消息体是不透明的.消息头有一系列可选属性组成.这些属性包括routing-key（路由键）、priority（相对于其他消息的优先权）、delivery-mode（指出 该消息可能需要持久性存储）等。</p>
</li>
<li><p>Publisher:消息的生产者，也是一个向交换器发布消息的客户端应用程序。</p>
</li>
<li><p>Exchange:交换器，用来接收生产者发送的消息并将这些消息路由给服务器中的队列。 Exchange有4种类型：direct(默认)，fanout, topic, 和headers，不同类型的Exchange转发消息的策略有所区别</p>
</li>
<li><p>Queue:消息队列，用来保存消息直到发送给消费者。它是消息的容器，也是消息的终点。一个消息可投入一个或多个队列。消息一直在队列里面，等待消费者连接到这个队列将其取走。生产者产生一个消息,发给消息服务器,由路由根据路由键判断将消息放到哪个消息队列.</p>
</li>
<li><p>Binding:绑定，用于消息队列和交换器之间的关联。一个绑定就是基于路由键将交换器和消息队列连 接起来的路由规则，所以可以将交换器理解成一个由绑定构成的路由表。 Exchange 和Queue的绑定可以是<strong>多对多</strong>的关系。</p>
</li>
<li><p>Connection:网络连接,比如一个TCP链接</p>
</li>
<li><p>Channel:信道，多路复用连接中的一条独立的双向数据流通道。信道是建立在真实的TCP连接内的虚 拟连接，AMQP 命令都是通过信道发出去的，不管是发布消息、订阅队列还是接收消息，这 些动作都是通过信道完成。因为对于操作系统来说建立和销毁 TCP 都是非常昂贵的开销，所 以引入了信道的概念，以复用一条 TCP 连接。</p>
</li>
<li><p>Consumer:消息的消费者，表示一个从消息队列中取得消息的客户端应用程序。</p>
</li>
<li><p>Virtual Host:虚拟主机，表示一批交换器、消息队列和相关对象。虚拟主机是共享相同的身份认证和加 密环境的独立服务器域。每个 vhost 本质上就是一个 mini 版的 RabbitMQ 服务器，拥有 自己的队列、交换器、绑定和权限机制。vhost 是 AMQP 概念的基础，必须在连接时指定， RabbitMQ 默认的 vhost 是 / 。</p>
</li>
<li><p>Broker:表示消息队列服务器实体.</p>
</li>
</ul>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728222420238.png" alt="image-20200728222420238"></p>
<p>另一张图:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728223611953.png" alt="image-20200728223611953"></p>
<p>RabbitMQ的Exchange分发消息有四种不同的分发策略.</p>
<ol>
<li><p>direct:消息中的路由键（routing key）如果和 Binding 中的 binding key 一致， 交换器就将消息发到对应的队列中。路由键与队 列名完全匹配，如果一个队列绑定到交换机要求路由键为 “dog”，则只转发 routing key 标记为“dog”的消息，不会转 发“dog.puppy”，也不会转发“dog.guard”等等。它是完全 匹配、单播的模式。</p>
</li>
<li><p>fanout:每个发到 fanout 类型交换器的消息都会分到所 有绑定的队列上去。fanout 交换器不处理路由键， 只是简单的将队列绑定到交换器上，每个发送 到交换器的消息都会被转发到与该交换器绑定 的所有队列上。很像子网广播，每台子网内的 主机都获得了一份复制的消息。fanout 类型转发 消息是最快的。</p>
</li>
<li><p>topic:交换器通过模式匹配分配消息的路由键属性，将路由键和某个模式进行匹配，此时队列 需要绑定到一个模式上。它将路由键和绑定键 的字符串切分成单词，这些单词之间用点隔开。 它同样也会识别两个通配符：符号“#”和符号 “*”。#匹配0个或多个单词， *匹配一个单词。</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200728224348271.png" alt="image-20200728224348271"></p>
</li>
</ol>
<p>此外,第四种headers功能上和direct一样,匹配的是消息的header而不是路由键,性能较差,几乎不用.</p>
<p>在SpringBoot中使用RabbitMQ需要导入spring-boot-starter-amqp.</p>
<p>老套路有一个RabbitMQAutoConfiguration作为配置类,该配置类导入了一些组件:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729133105399.png" alt="image-20200729133105399"></p>
<p>其中rabbitTemplate是类似于RedisTemplate的组件,基本操作都是依靠它.而amqpAdmin则是管理交换器,进行绑定等对RabbitMQ本身的操作.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729133324463.png" alt="image-20200729133324463"></p>
<p>用send方法发送消息需要自己构造一个消息Message:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729133511619.png" alt="image-20200729133511619"></p>
<p>需要一个字节数组和消息头.这样比较麻烦,还有另外一个convertAndSend方法.同样要交换器和路由键,不同的是发送的是一个对象,该方法会自动序列化并发送对象作为消息体.</p>
<p>下面是单播的代码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">contextLoads</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> map = <span class="keyword">new</span> HashMap&lt;String, Object&gt;();</span><br><span class="line">    map.put(<span class="string">"msg"</span>, <span class="string">"first message"</span>);</span><br><span class="line">    map.put(<span class="string">"data"</span>, Arrays.asList(<span class="keyword">new</span> Object[]&#123;<span class="string">"hello"</span>, <span class="string">"world"</span>, <span class="number">123</span>&#125;));</span><br><span class="line">    rabbitTemplate.convertAndSend(<span class="string">"exchange.direct"</span>, <span class="string">"company.news"</span>, map));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">receive</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> messgae = rabbitTemplate.receiveAndConvert(<span class="string">"company.news"</span>);</span><br><span class="line">    System.out.println(messgae.getClass());</span><br><span class="line">    System.out.println(messgae);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>默认的序列化方式是jdk的,所以要自己配置converter:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyAMQPConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> MessageConverter <span class="title">messageConverter</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Jackson2JsonMessageConverter();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>广播的代码与此类似,只需要修改exchange名字即可.</p>
<p>此外使用<code>@EnableRabbit</code>(加在启动类上)和<code>@RabbitListener(queues = &quot;company.news&quot;)</code>可以实现监听消息队列的功能.一旦company.news收到消息,加了@RabbitListener的方法就会执行.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RabbitListener</span>(queues = <span class="string">"company.news"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">receive</span><span class="params">(Book book)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"收到消息"</span>+book);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果发送的消息的消息体是一个Book(转为了json),这里就能成功转换并打印Book信息.如果要获得完整的消息内容,则将参数设为Message类型.</p>
<p>如果需要在程序里创建exchange等,则要用到上面说的AmqpAdmin.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729145631368.png" alt="image-20200729145631368"></p>
<p>declarexxx就是创建的,delete是删除的.其中创建exchange要传入一个exchange.改类是一个接口,有五个实现类,包括三个常用的,一个不常用的和一个自定义的:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729145834472.png" alt="image-20200729145834472"></p>
<p>构造器可以传入名字,</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729150231013.png" alt="image-20200729150231013"></p>
<p>其它的Queue,Binding也是与此类似:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729151455142.png" alt="image-20200729151455142"></p>
<h1 id="SpringBoot与检索"><a href="#SpringBoot与检索" class="headerlink" title="SpringBoot与检索"></a>SpringBoot与检索</h1><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729170151231.png" alt="image-20200729170151231"></p>
<p>索引相当于数据库,类型相当于表,文档相当于记录,属性相当于子段.</p>
<p>ES的管理是Restful风格的.添加数据用put请求发送json,在请求体里带上数据:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729174131919.png" alt="image-20200729174131919"></p>
<p>类似的删除是delete请求,带上id即可.</p>
<p>用get请求就是获取数据,head请求是查询是否有数据.</p>
<p>当使用put重复向某个id提交数据,那么数据被覆盖,且版本号会增加.</p>
<p>如果要搜素megacrop索引下的employee类型数据,则使用Get请求,URL这样写:<code>localhost:9200/megacorp/employee/_search</code>.注意此时清空postman的请求体.</p>
<p>如果要指定条件来查询,则需要在后面加上参数<code>q</code>:</p>
<p><code>localhost:9200/megacorp/employee/_search?q=age:25</code></p>
<p>此外还可以采用post请求方式,在请求体中加入用json写的查询条件,实现更复杂的筛选:</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"query"</span>: &#123;</span><br><span class="line">        <span class="attr">"match"</span>: &#123;</span><br><span class="line">            <span class="attr">"age"</span>: <span class="number">26</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>根据搜索的条件,ES能进行全文检索,列出相关结果,并不是严格按照条件检索.例如条件:</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"query"</span>: &#123;</span><br><span class="line">        <span class="attr">"match"</span>: &#123;</span><br><span class="line">            <span class="attr">"about"</span>: <span class="string">"1 test 34"</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果是下图.ES不仅给出了精确结果,还列出了相关的结果.并给每个结果打了一个相关性分数.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729201704626.png" alt="image-20200729201704626"></p>
<p>这是因为在全文检索中,<code>&quot;1 test 34&quot;</code>当做三个词语.如果想要这三个作为一个关键词,只需要把match改为match_phrase.这是就是短语检索.</p>
<h2 id="整合"><a href="#整合" class="headerlink" title="整合"></a>整合</h2><p>使用SpringBoot的自动配置要注意版本问题.自动导入的版本往往不能与自己使用的版本对应,因此要在pom文件中设置一下版本</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">java.version</span>&gt;</span>14<span class="tag">&lt;/<span class="name">java.version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">elasticsearch.version</span>&gt;</span>7.8.1<span class="tag">&lt;/<span class="name">elasticsearch.version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>我使用的是SpringBoot2.3.2版本.这里面使用spring-boot-starter-data-elasticsearch来自动导包,导的是elasticsearch-rest-high-level-client这个.这里变动的地方很多,注意区别.实际使用的是org\springframework\boot\autoconfigure\elasticsearch中的类(见下面代码),而不是org\springframework\boot\autoconfigure\data\elasticsearch中的.原本springboot2.1.x的ES的配置类是在data下的,现在又变了.这里面挺烦人的,再加上ES自己也在平凡变动,真的是头疼.总之,直接使用的配置相关都在这:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729233111392.png" alt="image-20200729233111392"></p>
<p>可以看下图,自动配置类导入了三个静态内部类.第二个就是要用的客户端.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200729232419060.png" alt="image-20200729232419060"></p>
<p>在一个配置类中,让一个客户端添加到容器中:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ElasticSearchConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> RestHighLevelClient <span class="title">restHighLevelClient</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> RestHighLevelClient(</span><br><span class="line">                RestClient.builder(</span><br><span class="line">                        <span class="keyword">new</span> HttpHost(<span class="string">"localhost"</span>, <span class="number">9200</span>, <span class="string">"http"</span>)</span><br><span class="line">                )</span><br><span class="line">        );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后测试管理索引(相当于管理库):</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">EsApplicationTests</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    RestHighLevelClient restHighLevelClient;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">testCreateIndex</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="comment">//创建索引请求</span></span><br><span class="line">        CreateIndexRequest my_index = <span class="keyword">new</span> CreateIndexRequest(<span class="string">"my_index"</span>);</span><br><span class="line">        <span class="comment">//执行请求</span></span><br><span class="line">        CreateIndexResponse createIndexResponse = restHighLevelClient.indices().create(my_index, RequestOptions.DEFAULT);</span><br><span class="line">        System.out.println(createIndexResponse);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">testExistIndex</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        GetIndexRequest my_index = <span class="keyword">new</span> GetIndexRequest(<span class="string">"my_index"</span>);</span><br><span class="line">        <span class="keyword">boolean</span> exists = restHighLevelClient.indices().exists(my_index, RequestOptions.DEFAULT);</span><br><span class="line">        System.out.println(exists);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">testDeleteIndex</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        DeleteIndexRequest deleteIndexRequest = <span class="keyword">new</span> DeleteIndexRequest(<span class="string">"my_index"</span>);</span><br><span class="line">        AcknowledgedResponse acknowledgedResponse = restHighLevelClient.indices().delete(deleteIndexRequest, RequestOptions.DEFAULT);</span><br><span class="line">        System.out.println(acknowledgedResponse);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接下来演示了添加文档:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">testAddDocument</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    HashMap&lt;String, Object&gt; jsonMap = <span class="keyword">new</span> HashMap&lt;String, Object&gt;();</span><br><span class="line">    jsonMap.put(<span class="string">"name"</span>, <span class="string">"aa"</span>);</span><br><span class="line">    jsonMap.put(<span class="string">"age"</span>, <span class="number">25</span>);</span><br><span class="line">    IndexRequest indexRequest = <span class="keyword">new</span> IndexRequest(<span class="string">"my_index"</span>);</span><br><span class="line">    indexRequest.id(<span class="string">"1"</span>);</span><br><span class="line">    <span class="comment">// indexRequest.source(new User("bb", 26));</span></span><br><span class="line">    indexRequest.source(jsonMap);</span><br><span class="line">    IndexResponse indexResponse = restHighLevelClient.index(indexRequest, RequestOptions.DEFAULT);</span><br><span class="line">    System.out.println(indexResponse);</span><br><span class="line">    System.out.println(indexResponse.status());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其它不写了,SpringBoot在ES的整合上做的太差了,一堆变动,搞不清楚2.1.x和2.2.x不同,2.3.x又不同.再加上SpringBoot里面命名有问题,版本支持比较老.总之这里就是一团浆糊.</p>
<p>另外有一篇参考:<a href="https://blog.csdn.net/chengyuqiang/article/details/102938266" target="_blank" rel="noopener">https://blog.csdn.net/chengyuqiang/article/details/102938266</a></p>
<h1 id="SpringBoot与任务"><a href="#SpringBoot与任务" class="headerlink" title="SpringBoot与任务"></a>SpringBoot与任务</h1><h2 id="异步任务"><a href="#异步任务" class="headerlink" title="异步任务"></a>异步任务</h2><p>假定现在有一个请求,这个请求内的方法执行必须要三秒.如果是普通的同步方式,即单线程的,那么就必须等待三秒才能给浏览器响应:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">hello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Thread.sleep(<span class="number">3000</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println(<span class="string">"处理数据..."</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@GetMapping</span>(<span class="string">"/hello"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">hello</span><span class="params">()</span></span>&#123;</span><br><span class="line">    asyncService.hello();</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码就是.必须要三秒钟后浏览器才能显示success结果.</p>
<p>通常的办法是再开一个线程来处理数据,原来的线程先给客户端返回结果.但是这样写起来比较麻烦.在SpringBoot中可以使用异步注解降低难度:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Async</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">hello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Thread.sleep(<span class="number">3000</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println(<span class="string">"处理数据..."</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>只需要加一个@async注解,并在启动类上加一个开启异步注解的注解<code>@EnableAsync</code>即可.</p>
<h2 id="定时任务"><a href="#定时任务" class="headerlink" title="定时任务"></a>定时任务</h2><p>可以在某个方法上加上@Scheduled注解表示这是一个定时任务.需要一个参数cron写明执行时间.写法是一个字符串,按照秒,分,时,日,月,周几排列.例如”0 * * * * MON-FRI”表示每月的周一到周五的每一天,每小时,每分钟的0秒执行.</p>
<p>此外还需要在启动类上加@EnableScheduling注解.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200730144149194.png" alt="image-20200730144149194"></p>
<p><code>&quot;0,10,20,30 * * * * MON-FRI&quot;</code>表示0秒,10秒,20秒,30秒都会执行.</p>
<p><code>&quot;0-10 * * * * MON-FRI&quot;</code>表示0-10秒内每秒都执行.</p>
<p><code>&quot;0/5 * * * * MON-FRI&quot;</code>表示从0开始每过5秒执行.</p>
<h2 id="邮件发送"><a href="#邮件发送" class="headerlink" title="邮件发送"></a>邮件发送</h2><p>引入mail的starter即可,写起来很简单.不展开了.</p>
<h1 id="SpringBoot与安全"><a href="#SpringBoot与安全" class="headerlink" title="SpringBoot与安全"></a>SpringBoot与安全</h1><p>导入SpringSecurity的starter.然后写SpringSecurity的配置类.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@EnableWebSecurity</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SecurityConfig</span> <span class="keyword">extends</span> <span class="title">WebSecurityConfigurerAdapter</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(HttpSecurity http)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="comment">//定制请求的授权规则</span></span><br><span class="line">        http.authorizeRequests().antMatchers(<span class="string">"/"</span>).permitAll()</span><br><span class="line">                .antMatchers(<span class="string">"/level1/**"</span>).hasRole(<span class="string">"VIP1"</span>)</span><br><span class="line">                .antMatchers(<span class="string">"/level2/**"</span>).hasRole(<span class="string">"VIP2"</span>)</span><br><span class="line">                .antMatchers(<span class="string">"/level3/**"</span>).hasRole(<span class="string">"VIP3"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>重要的是@EnableWebSecurity注解和继承WebSecurityConfigurerAdapter类.然后重写configure方法.规则的第一行表示”/“请求所有人都可以进入,下面三个请求则要有对应的角色才能进去.</p>
<p>既然需要角色,那么就需要登录功能.在方法里加上<code>http.formLogin();</code>则开启Security自动配置的登录功能.使用”/login”请求可以来到登录页.</p>
<p>如果访问没有权限的页面如上面的”/level1/**”请求,会自动跳转到登录页面.</p>
<p>有了登录就需要用户名和密码.这里用存在内存中的用户名密码演示:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(AuthenticationManagerBuilder auth)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    auth.inMemoryAuthentication()</span><br><span class="line">            .withUser(<span class="string">"zs"</span>).roles(<span class="string">"VIP1"</span>).password(<span class="string">"1234"</span>)</span><br><span class="line">            .and()</span><br><span class="line">            .withUser(<span class="string">"lisi"</span>).roles(<span class="string">"VIP2"</span>).password(<span class="string">"123"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>添加了两个user,并赋予了角色和密码.</p>
<p>此外还需要设置加密:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function">PasswordEncoder <span class="title">passwordEncoder</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> NoOpPasswordEncoder.getInstance();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里设置对密码不加密.</p>
<p>在第一个configure方法中加上<code>http.logout().logoutSuccessUrl(&quot;/&quot;);</code>可以实现注销功能.logoutSuccessUrl表示注销后跳转到哪个页面.</p>
<p><code>http.rememberMe</code>可以开启记住我功能.</p>
]]></content>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>ThreadLocal</title>
    <url>/2020/06/30/ThreadLocal/</url>
    <content><![CDATA[<p>ThreadLocal学习.</p>
<a id="more"></a>

<p>本文部分内容转载于简书:</p>
<p>作者：ingxin</p>
<p>链接：<a href="https://www.jianshu.com/p/3c5d7f09dfbd" target="_blank" rel="noopener">https://www.jianshu.com/p/3c5d7f09dfbd</a></p>
<p>threadlocal使用方法很简单<br>static final ThreadLocal<T> sThreadLocal = new ThreadLocal<T>();<br>sThreadLocal.set()<br>sThreadLocal.get()</T></T></p>
<p>threadlocal而是一个线程内部的存储类，可以在指定线程内存储数据，数据存储以后，只有指定线程可以得到存储数据，官方解释如下。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * This class provides thread-local variables.  These variables differ from</span></span><br><span class="line"><span class="comment"> * their normal counterparts in that each thread that accesses one (via its</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> get&#125; or &#123;<span class="doctag">@code</span> set&#125; method) has its own, independently initialized</span></span><br><span class="line"><span class="comment"> * copy of the variable.  &#123;<span class="doctag">@code</span> ThreadLocal&#125; instances are typically private</span></span><br><span class="line"><span class="comment"> * static fields in classes that wish to associate state with a thread (e.g.,</span></span><br><span class="line"><span class="comment"> * a user ID or Transaction ID).</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure>

<p>大致意思就是ThreadLocal提供了线程内存储变量的能力，这些变量不同之处在于每一个线程读取的变量是对应的互相独立的。通过get和set方法就可以得到当前线程对应的值。</p>
<p>做个不恰当的比喻，从表面上看ThreadLocal相当于维护了一个map，key就是当前的线程，value就是需要存储的对象。</p>
<p>这里的这个比喻是不恰当的，实际上是ThreadLocal的静态内部类ThreadLocalMap为每个Thread都维护了一个数组table，ThreadLocal确定了一个数组下标，而这个下标就是value存储的对应位置。</p>
<p>作为一个存储数据的类，关键点就在get和set方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//set 方法</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(T value)</span> </span>&#123;</span><br><span class="line">      <span class="comment">//获取当前线程</span></span><br><span class="line">      Thread t = Thread.currentThread();</span><br><span class="line">      <span class="comment">//实际存储的数据结构类型</span></span><br><span class="line">      ThreadLocalMap map = getMap(t);</span><br><span class="line">      <span class="comment">//如果存在map就直接set，没有则创建map并set</span></span><br><span class="line">      <span class="keyword">if</span> (map != <span class="keyword">null</span>)</span><br><span class="line">          map.set(<span class="keyword">this</span>, value);</span><br><span class="line">      <span class="keyword">else</span></span><br><span class="line">          createMap(t, value);</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line"><span class="comment">//getMap方法</span></span><br><span class="line"><span class="function">ThreadLocalMap <span class="title">getMap</span><span class="params">(Thread t)</span> </span>&#123;</span><br><span class="line">      <span class="comment">//thred中维护了一个ThreadLocalMap</span></span><br><span class="line">      <span class="keyword">return</span> t.threadLocals;</span><br><span class="line"> &#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//createMap</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">createMap</span><span class="params">(Thread t, T firstValue)</span> </span>&#123;</span><br><span class="line">      <span class="comment">//实例化一个新的ThreadLocalMap，并赋值给线程的成员变量threadLocals</span></span><br><span class="line">      t.threadLocals = <span class="keyword">new</span> ThreadLocalMap(<span class="keyword">this</span>, firstValue);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从上面代码可以看出每个线程持有一个ThreadLocalMap对象。每一个新的线程Thread都会实例化一个ThreadLocalMap并赋值给成员变量threadLocals，使用时若已经存在threadLocals则直接使用已经存在的对象。</p>
<h1 id="Thread"><a href="#Thread" class="headerlink" title="Thread"></a>Thread</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* ThreadLocal values pertaining to this thread. This map is maintained</span></span><br><span class="line"><span class="comment">     * by the ThreadLocal class. */</span></span><br><span class="line">    ThreadLocal.ThreadLocalMap threadLocals = <span class="keyword">null</span>;</span><br></pre></td></tr></table></figure>

<p>Thread中关于ThreadLocalMap部分的相关声明，接下来看一下createMap方法中的实例化过程。</p>
<h1 id="ThreadLocalMap"><a href="#ThreadLocalMap" class="headerlink" title="ThreadLocalMap"></a>ThreadLocalMap</h1><h2 id="set方法"><a href="#set方法" class="headerlink" title="set方法"></a>set方法</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Entry为ThreadLocalMap静态内部类，对ThreadLocal的弱引用</span></span><br><span class="line"><span class="comment">//同时让ThreadLocal和储值形成key-value的关系</span></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Entry</span> <span class="keyword">extends</span> <span class="title">WeakReference</span>&lt;<span class="title">ThreadLocal</span>&lt;?&gt;&gt; </span>&#123;</span><br><span class="line">    <span class="comment">/** The value associated with this ThreadLocal. */</span></span><br><span class="line">    Object value;</span><br><span class="line"></span><br><span class="line">    Entry(ThreadLocal&lt;?&gt; k, Object v) &#123;</span><br><span class="line">           <span class="keyword">super</span>(k);</span><br><span class="line">            value = v;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//ThreadLocalMap构造方法</span></span><br><span class="line">ThreadLocalMap(ThreadLocal&lt;?&gt; firstKey, Object firstValue) &#123;</span><br><span class="line">        <span class="comment">//内部成员数组，INITIAL_CAPACITY值为16的常量</span></span><br><span class="line">        table = <span class="keyword">new</span> Entry[INITIAL_CAPACITY];</span><br><span class="line">        <span class="comment">//位运算，结果与取模相同，计算出需要存放的位置</span></span><br><span class="line">        <span class="comment">//threadLocalHashCode比较有趣</span></span><br><span class="line">        <span class="keyword">int</span> i = firstKey.threadLocalHashCode &amp; (INITIAL_CAPACITY - <span class="number">1</span>);</span><br><span class="line">        table[i] = <span class="keyword">new</span> Entry(firstKey, firstValue);</span><br><span class="line">        size = <span class="number">1</span>;</span><br><span class="line">        setThreshold(INITIAL_CAPACITY);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过上面的代码不难看出在实例化ThreadLocalMap时创建了一个长度为16的Entry数组。通过hashCode与length位运算确定出一个索引值i，这个i就是被存储在table数组中的位置。</p>
<p>前面讲过每个线程Thread持有一个ThreadLocalMap类型的实例threadLocals，结合此处的构造方法可以理解成每个线程Thread都持有一个Entry型的数组table，而一切的读取过程都是通过操作这个数组table完成的。</p>
<p>显然table是set和get的焦点，在看具体的set和get方法前，先看下面这段代码。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//在某一线程声明了ABC三种类型的ThreadLocal</span></span><br><span class="line">ThreadLocal&lt;A&gt; sThreadLocalA = <span class="keyword">new</span> ThreadLocal&lt;A&gt;();</span><br><span class="line">ThreadLocal&lt;B&gt; sThreadLocalB = <span class="keyword">new</span> ThreadLocal&lt;B&gt;();</span><br><span class="line">ThreadLocal&lt;C&gt; sThreadLocalC = <span class="keyword">new</span> ThreadLocal&lt;C&gt;();</span><br></pre></td></tr></table></figure>

<p>由前面我们知道对于一个Thread来说只有持有一个ThreadLocalMap，所以ABC对应同一个ThreadLocalMap对象。为了管理ABC，于是将他们存储在一个数组的不同位置，而这个数组就是上面提到的Entry型的数组table。</p>
<p>那么问题来了，ABC在table中的位置是如何确定的？为了能正常够正常的访问对应的值，肯定存在一种方法计算出确定的索引值i。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//ThreadLocalMap中set方法。</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(ThreadLocal&lt;?&gt; key, Object value)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">          <span class="comment">// We don't use a fast path as with get() because it is at</span></span><br><span class="line">          <span class="comment">// least as common to use set() to create new entries as</span></span><br><span class="line">          <span class="comment">// it is to replace existing ones, in which case, a fast</span></span><br><span class="line">          <span class="comment">// path would fail more often than not.</span></span><br><span class="line"></span><br><span class="line">          Entry[] tab = table;</span><br><span class="line">          <span class="keyword">int</span> len = tab.length;</span><br><span class="line">          <span class="comment">//获取索引值，这个地方是比较特别的地方</span></span><br><span class="line">          <span class="keyword">int</span> i = key.threadLocalHashCode &amp; (len-<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">          <span class="comment">//遍历tab如果已经存在则更新值</span></span><br><span class="line">          <span class="keyword">for</span> (Entry e = tab[i];</span><br><span class="line">               e != <span class="keyword">null</span>;</span><br><span class="line">               e = tab[i = nextIndex(i, len)]) &#123;</span><br><span class="line">              ThreadLocal&lt;?&gt; k = e.get();</span><br><span class="line"></span><br><span class="line">              <span class="keyword">if</span> (k == key) &#123;</span><br><span class="line">                  e.value = value;</span><br><span class="line">                  <span class="keyword">return</span>;</span><br><span class="line">              &#125;</span><br><span class="line"></span><br><span class="line">              <span class="keyword">if</span> (k == <span class="keyword">null</span>) &#123;</span><br><span class="line">                  replaceStaleEntry(key, value, i);</span><br><span class="line">                  <span class="keyword">return</span>;</span><br><span class="line">              &#125;</span><br><span class="line">          &#125;</span><br><span class="line">          </span><br><span class="line">          <span class="comment">//如果上面没有遍历成功则创建新值</span></span><br><span class="line">          tab[i] = <span class="keyword">new</span> Entry(key, value);</span><br><span class="line">          <span class="keyword">int</span> sz = ++size;</span><br><span class="line">          <span class="comment">//满足条件数组扩容x2</span></span><br><span class="line">          <span class="keyword">if</span> (!cleanSomeSlots(i, sz) &amp;&amp; sz &gt;= threshold)</span><br><span class="line">              rehash();</span><br><span class="line">      &#125;</span><br></pre></td></tr></table></figure>

<p>在ThreadLocalMap中的set方法与构造方法能看到以下代码片段。</p>
<ul>
<li>int i = key.threadLocalHashCode &amp; (len-1)</li>
<li>int i = firstKey.threadLocalHashCode &amp; (INITIAL_CAPACITY - 1)</li>
<li>简而言之就是将threadLocalHashCode进行一个位运算（取模）得到索引i，threadLocalHashCode代码如下。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//ThreadLocal中threadLocalHashCode相关代码.</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> threadLocalHashCode = nextHashCode();</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">    * The next hash code to be given out. Updated atomically. Starts at</span></span><br><span class="line"><span class="comment">    * zero.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> AtomicInteger nextHashCode =</span><br><span class="line">    <span class="keyword">new</span> AtomicInteger();</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">    * The difference between successively generated hash codes - turns</span></span><br><span class="line"><span class="comment">    * implicit sequential thread-local IDs into near-optimally spread</span></span><br><span class="line"><span class="comment">    * multiplicative hash values for power-of-two-sized tables.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> HASH_INCREMENT = <span class="number">0x61c88647</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">    * Returns the next hash code.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">nextHashCode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">//自增</span></span><br><span class="line">    <span class="keyword">return</span> nextHashCode.getAndAdd(HASH_INCREMENT);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因为static的原因，在每次new ThreadLocal时因为threadLocalHashCode的初始化，会使threadLocalHashCode值自增一次，增量为0x61c88647。</p>
<p>0x61c88647是斐波那契散列乘数,它的优点是通过它散列(hash)出来的结果分布会比较均匀，可以很大程度上避免hash冲突，已初始容量16为例，hash并与15位运算计算数组下标结果如下：<br>hashCode |    数组下标<br>-|-<br>0x61c88647 |    7<br>0xc3910c8e |    14<br>0x255992d5 |    5<br>0x8722191c |    12<br>0xe8ea9f63 |    3<br>0x4ab325aa |    10<br>0xac7babf1 |    1<br>0xe443238 |    8<br>0x700cb87f |    15</p>
<p>总结如下：</p>
<p><strong>对于某一ThreadLocal来讲，他的索引值i是确定的，在不同线程之间访问时访问的是不同的table数组的同一位置即都为table[i]，只不过这个不同线程之间的table是独立的。</strong></p>
<p><strong>对于同一线程的不同ThreadLocal来讲，这些ThreadLocal实例共享一个table数组，然后每个ThreadLocal实例在table中的索引i是不同的。</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">get()方法</span><br><span class="line"><span class="comment">//ThreadLocal中get方法</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> T <span class="title">get</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Thread t = Thread.currentThread();</span><br><span class="line">    ThreadLocalMap map = getMap(t);</span><br><span class="line">    <span class="keyword">if</span> (map != <span class="keyword">null</span>) &#123;</span><br><span class="line">        ThreadLocalMap.Entry e = map.getEntry(<span class="keyword">this</span>);</span><br><span class="line">        <span class="keyword">if</span> (e != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="meta">@SuppressWarnings</span>(<span class="string">"unchecked"</span>)</span><br><span class="line">            T result = (T)e.value;</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> setInitialValue();</span><br><span class="line">&#125;</span><br><span class="line">    </span><br><span class="line"><span class="comment">//ThreadLocalMap中getEntry方法</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> Entry <span class="title">getEntry</span><span class="params">(ThreadLocal&lt;?&gt; key)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">int</span> i = key.threadLocalHashCode &amp; (table.length - <span class="number">1</span>);</span><br><span class="line">       Entry e = table[i];</span><br><span class="line">       <span class="keyword">if</span> (e != <span class="keyword">null</span> &amp;&amp; e.get() == key)</span><br><span class="line">            <span class="keyword">return</span> e;</span><br><span class="line">       <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> getEntryAfterMiss(key, i, e);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>理解了set方法，get方法也就清楚明了，无非是通过计算出索引直接从数组对应位置读取即可。</p>
<p>ThreadLocal实现主要涉及Thread，ThreadLocal，ThreadLocalMap这三个类。关于ThreadLocal的实现流程正如上面写的那样，实际代码还有许多细节处理的部分并没有在这里写出来。</p>
<h1 id="ThreadLocal特性"><a href="#ThreadLocal特性" class="headerlink" title="ThreadLocal特性"></a>ThreadLocal特性</h1><p>ThreadLocal和Synchronized都是为了解决多线程中相同变量的访问冲突问题，不同的点是</p>
<ul>
<li>Synchronized是通过线程等待，牺牲时间来解决访问冲突</li>
<li>ThreadLocal是通过每个线程单独一份存储空间，牺牲空间来解决冲突，并且相比于Synchronized，ThreadLocal具有线程隔离的效果，只有在线程内才能获取到对应的值，线程外则不能访问到想要的值。</li>
</ul>
<p>正因为ThreadLocal的线程隔离特性，使他的应用场景相对来说更为特殊一些。在android中Looper、ActivityThread以及AMS中都用到了ThreadLocal。当某些数据是以线程为作用域并且不同线程具有不同的数据副本的时候，就可以考虑采用ThreadLocal。</p>
<h1 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h1><p>ThreadLocal是一个弱引用,当ThreadLocal变为null后,就会被回收,即ThreadLocalMap中entry中的key没了,但是ThreadLocalMap的生命周期和ThreadLocal并不一样,这是就出现了key没了,value还在的问题,所以要用remove方法来删除对应的value.</p>
<p>ThreadLocal用来解决事务管理问题.在数据库层面进行事务操作是不够的,因为只能保证当前数据库连接的事务.如果对数据库的操作是在不同连接进行的,也就没法使操作在同一事务中进行.所以要用ThreadLocal保存数据库连接,使得相关的数据库操作都在同一个连接中的事务中.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.atguigu.utils;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.alibaba.druid.pool.DruidDataSource;</span><br><span class="line"><span class="keyword">import</span> com.alibaba.druid.pool.DruidDataSourceFactory;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.InputStream;</span><br><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.SQLException;</span><br><span class="line"><span class="keyword">import</span> java.util.Properties;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JdbcUtils</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> DruidDataSource dataSource;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> ThreadLocal&lt;Connection&gt; conns = <span class="keyword">new</span> ThreadLocal&lt;Connection&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Properties properties = <span class="keyword">new</span> Properties();</span><br><span class="line">            <span class="comment">// 读取 jdbc.properties属性配置文件</span></span><br><span class="line">            InputStream inputStream = JdbcUtils.class.getClassLoader().getResourceAsStream("jdbc.properties");</span><br><span class="line">            <span class="comment">// 从流中加载数据</span></span><br><span class="line">            properties.load(inputStream);</span><br><span class="line">            <span class="comment">// 创建 数据库连接 池</span></span><br><span class="line">            dataSource = (DruidDataSource) DruidDataSourceFactory.createDataSource(properties);</span><br><span class="line"></span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 获取数据库连接池中的连接</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 如果返回null,说明获取连接失败&lt;br/&gt;有值就是获取连接成功</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Connection <span class="title">getConnection</span><span class="params">()</span></span>&#123;</span><br><span class="line">        Connection conn = conns.get();</span><br><span class="line">        <span class="keyword">if</span> (conn == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                conn = dataSource.getConnection();<span class="comment">//从数据库连接池中获取连接</span></span><br><span class="line">                conns.set(conn); <span class="comment">// 保存到ThreadLocal对象中，供后面的jdbc操作使用</span></span><br><span class="line">                conn.setAutoCommit(<span class="keyword">false</span>); <span class="comment">// 设置为手动管理事务</span></span><br><span class="line">            &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> conn;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 提交事务，并关闭释放连接</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">commitAndClose</span><span class="params">()</span></span>&#123;</span><br><span class="line">        Connection connection = conns.get();</span><br><span class="line">        <span class="keyword">if</span> (connection != <span class="keyword">null</span>) &#123; <span class="comment">// 如果不等于null，说明 之前使用过连接，操作过数据库</span></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                connection.commit(); <span class="comment">// 提交 事务</span></span><br><span class="line">            &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    connection.close(); <span class="comment">// 关闭连接，资源资源</span></span><br><span class="line">                &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 一定要执行remove操作，否则就会出错。（因为Tomcat服务器底层使用了线程池技术）</span></span><br><span class="line">        conns.remove();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 回滚事务，并关闭释放连接</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">rollbackAndClose</span><span class="params">()</span></span>&#123;</span><br><span class="line">        Connection connection = conns.get();</span><br><span class="line">        <span class="keyword">if</span> (connection != <span class="keyword">null</span>) &#123; <span class="comment">// 如果不等于null，说明 之前使用过连接，操作过数据库</span></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                connection.rollback();<span class="comment">//回滚事务</span></span><br><span class="line">            &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    connection.close(); <span class="comment">// 关闭连接，资源资源</span></span><br><span class="line">                &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 一定要执行remove操作，否则就会出错。（因为Tomcat服务器底层使用了线程池技术）</span></span><br><span class="line">        conns.remove();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 关闭连接，放回数据库连接池</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> conn</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    public static void close(Connection conn)&#123;</span></span><br><span class="line"><span class="comment">        if (conn != null) &#123;</span></span><br><span class="line"><span class="comment">            try &#123;</span></span><br><span class="line"><span class="comment">                conn.close();</span></span><br><span class="line"><span class="comment">            &#125; catch (SQLException e) &#123;</span></span><br><span class="line"><span class="comment">                e.printStackTrace();</span></span><br><span class="line"><span class="comment">            &#125;</span></span><br><span class="line"><span class="comment">        &#125;</span></span><br><span class="line"><span class="comment">    &#125; */</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一上面的代码为例,从数据库连接池获取的连接被保存在ThreadLocal中,确保了所有操作都是在同一连接中,这样保证了事务的可行性.一个用户进入tomcat,就是一个线程,一个线程就只有一个连接.在代码里有<code>conns.remove</code>,就是避免多线程时一个用户不用了,该线程连带ThreadLocal中的值被其他线程使用.</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring注解</title>
    <url>/2020/07/17/Spring%E6%B3%A8%E8%A7%A3/</url>
    <content><![CDATA[<p>回顾和深入注解使用.</p>
<a id="more"></a>

<h1 id="Bean管理"><a href="#Bean管理" class="headerlink" title="Bean管理"></a>Bean管理</h1><h2 id="组件扫描"><a href="#组件扫描" class="headerlink" title="组件扫描"></a>组件扫描</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.config;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.company.bean.Person;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.ComponentScan;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.FilterType;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="keyword">static</span> org.springframework.context.annotation.ComponentScan.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/17 19:54</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ComponentScan</span>(value = <span class="string">"com.company"</span>, excludeFilters = &#123;</span><br><span class="line">        <span class="meta">@Filter</span>(type = FilterType.ANNOTATION, classes = &#123;Controller<span class="class">.<span class="keyword">class</span>, <span class="title">Service</span>.<span class="title">class</span>&#125;)</span></span><br><span class="line"><span class="class">&#125;)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">MainConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Person <span class="title">person</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Person(<span class="string">"李四"</span>, <span class="number">20</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是一个Spring的注解类,用Configuration这个注解表明它是注解类.ComponentScan注解用来扫描Bean,excludeFilters设置了排除扫描范围内的哪些内容,<code>type = FilterType.ANNOTATION</code>表示根据注解排除,<code>classes = {Controller.class, Service.class}</code>表示排除Controller,Service注解.类似的还有<code>includeFilters</code>设置只包含哪些类型,但是这又需要另一个配置<code>useDefaultFilters</code>.该配置设为false,将默认的过滤规则禁用.</p>
<p><code>@Filter(type = FilterType.ASSIGNABLE_TYPE, classes = {BookService.class})</code>另一种过滤方式:按照指定类型,包括指定类型的子类,实现类在内.</p>
<p>还有按照AspectJ表达式和正则表达式过滤,但不常用.最后还有自定义过滤方式.</p>
<p>下面使用Bean注解单独注入了一个类,Bean后面可以添加value设置Bean的id.默认为下面的方法名.</p>
<p>此外,配置类本身也是一个Component,它是必须被加载的,无论如何过滤不掉的.</p>
<p>ComponentScan注解本身是一个可重复注解,也就是说可以写多个(需要Java8).此外还有一个ComponentScans注解,它的值是一个ComponentScan数组.</p>
<p>现在演示自定义过滤方式:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.config;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.core.io.Resource;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.type.AnnotationMetadata;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.type.ClassMetadata;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.type.classreading.MetadataReader;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.type.classreading.MetadataReaderFactory;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.type.filter.TypeFilter;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/17 21:09</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyTypeFilter</span> <span class="keyword">implements</span> <span class="title">TypeFilter</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> metadataReader        读取到的当前正在扫描的类的信息</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> metadataReaderFactory 获取的其它任何类的信息</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">match</span><span class="params">(MetadataReader metadataReader, MetadataReaderFactory metadataReaderFactory)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="comment">//获取当前类注解的信息</span></span><br><span class="line">        AnnotationMetadata annotationMetadata = metadataReader.getAnnotationMetadata();</span><br><span class="line">        <span class="comment">//获取当前类的类信息</span></span><br><span class="line">        ClassMetadata classMetadata = metadataReader.getClassMetadata();</span><br><span class="line">        <span class="comment">//获取当前类资源(类路径等)</span></span><br><span class="line">        Resource resource = metadataReader.getResource();</span><br><span class="line">        String className = classMetadata.getClassName();</span><br><span class="line">        System.out.println(<span class="string">"-&gt;"</span>+className);</span><br><span class="line">        <span class="keyword">if</span> (className.contains(<span class="string">"er"</span>)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>自定义过滤需要实现TypeFilter接口,只有返回true才代表当前扫描的类匹配规则,加入容器.</p>
<p>测试一下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ApplicationContext context = <span class="keyword">new</span> AnnotationConfigApplicationContext(MainConfig<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    String[] names = context.getBeanDefinitionNames();</span><br><span class="line">    Arrays.stream(names).forEach(System.out::println);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-&gt;com.company.bean.PersonTest</span><br><span class="line">-&gt;com.company.config.MainConfigTest</span><br><span class="line">-&gt;com.company.bean.Person</span><br><span class="line">-&gt;com.company.config.MyTypeFilter</span><br><span class="line">-&gt;com.company.controller.BookController</span><br><span class="line">-&gt;com.company.dao.BookDao</span><br><span class="line">-&gt;com.company.service.BookService</span><br><span class="line">org.springframework.context.annotation.internalConfigurationAnnotationProcessor</span><br><span class="line">org.springframework.context.annotation.internalAutowiredAnnotationProcessor</span><br><span class="line">org.springframework.context.event.internalEventListenerProcessor</span><br><span class="line">org.springframework.context.event.internalEventListenerFactory</span><br><span class="line">mainConfig</span><br><span class="line">personTest</span><br><span class="line">person</span><br><span class="line">myTypeFilter</span><br><span class="line">bookController</span><br><span class="line">bookService</span><br></pre></td></tr></table></figure>

<p>这是结果.可以看到扫描了包里的类,然后把spring自身需要的bean和配置类,带er的类都加入容器.此外这个自定义过滤也加入了容器.</p>
<p>此外,在Spring中Bean默认是单例的,当然可以手动设置作用域.</p>
<p>通过Scope这个注解来实现,主要用到其中两个值:prototype,singleton(默认).前者是多实例,后者是单实例.</p>
<p>另外单实例的默认情况下,Bean在容器启动时就创建.而多实例时,显示获取Bean时才会创建.</p>
<p>对于单实例,可以让其懒加载,即第一次使用Bean时再创建Bean.<strong>只需要加上Lazy这个注解.</strong></p>
<p>Spring4中还提供了另一种加载Bean的方式:按照条件加载.</p>
<p>需要用到Conditional这个注解,这个注解中需要Condition这个值,这是一个自定义的条件类,需要实现Condition这个接口,然后实现里面的match方法,根据方法返回的Boolean值表示是否符合条件.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.config;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.config.ConfigurableListableBeanFactory;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.support.BeanDefinitionRegistry;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Condition;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.ConditionContext;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.env.Environment;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.type.AnnotatedTypeMetadata;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/18 11:09</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WindowsCondition</span> <span class="keyword">implements</span> <span class="title">Condition</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">matches</span><span class="params">(ConditionContext context, AnnotatedTypeMetadata metadata)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//获取ioc使用的BeanFactory</span></span><br><span class="line">        ConfigurableListableBeanFactory beanFactory = context.getBeanFactory();</span><br><span class="line">        <span class="comment">//获取类加载器</span></span><br><span class="line">        ClassLoader classLoader = context.getClassLoader();</span><br><span class="line">        <span class="comment">//获取环境</span></span><br><span class="line">        Environment environment = context.getEnvironment();</span><br><span class="line">        <span class="comment">//获取Bean定义的注册类</span></span><br><span class="line">        BeanDefinitionRegistry registry = context.getRegistry();</span><br><span class="line"></span><br><span class="line">        String property = environment.getProperty(<span class="string">"os.name"</span>);</span><br><span class="line">        <span class="keyword">assert</span> property != <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">return</span> property.contains(<span class="string">"Windows"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于导入第三方包作为组件,还可以使用import快速导入.具体方法是在配置类上加一个Import注解:@Import(导入的类.class),这个注解的值也可以是一个数组,一次导入多个类.Import导入的Bean名字默认是全类名.</p>
<p>此外还可以在Import里导入一个ImportSelector.它需要一个实现了ImportSelector接口的实现类.需要实现selectImports方法,该方法返回一个String数组,该数组为要导入的<strong>全类名</strong>.</p>
<p>另外也可以导入自定义导入方式:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.config;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.company.bean.RainBow;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.support.BeanDefinitionRegistry;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.support.RootBeanDefinition;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.ImportBeanDefinitionRegistrar;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.type.AnnotationMetadata;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/18 14:03</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyImportBeanDefinitionRegistrar</span> <span class="keyword">implements</span> <span class="title">ImportBeanDefinitionRegistrar</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> importingClassMetadata 导入的当前类的注解信息</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> registry               注册类</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">registerBeanDefinitions</span><span class="params">(AnnotationMetadata importingClassMetadata, BeanDefinitionRegistry registry)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">boolean</span> red = registry.containsBeanDefinition(<span class="string">"com.company.bean.Red"</span>);</span><br><span class="line">        <span class="keyword">boolean</span> yellow = registry.containsBeanDefinition(<span class="string">"com.company.bean.Yellow"</span>);</span><br><span class="line">        <span class="comment">//如果有这两个bean</span></span><br><span class="line">        <span class="keyword">if</span> (red &amp;&amp; yellow) &#123;</span><br><span class="line">            <span class="comment">//指定bean名</span></span><br><span class="line">            registry.registerBeanDefinition(<span class="string">"rainbow"</span>, <span class="keyword">new</span> RootBeanDefinition(RainBow<span class="class">.<span class="keyword">class</span>))</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后将这个自定义类加入Import注解即可.</p>
<p>BeanFactory:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBean</span> <span class="keyword">implements</span> <span class="title">FactoryBean</span>&lt;<span class="title">Book</span>&gt; </span>&#123;</span><br><span class="line">    <span class="comment">//定义返回bean</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Book <span class="title">getObject</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        Book book = <span class="keyword">new</span> Book();</span><br><span class="line">        book.setAuthor(<span class="string">"aa"</span>);</span><br><span class="line">        <span class="keyword">return</span> book;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//定义返回类型</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Class&lt;?&gt; getObjectType() &#123;</span><br><span class="line">        <span class="keyword">return</span> Book<span class="class">.<span class="keyword">class</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//定义是否是单例</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isSingleton</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Bean的生命周期"><a href="#Bean的生命周期" class="headerlink" title="Bean的生命周期"></a>Bean的生命周期</h2><p>以前对于Bean的初始化方法和销毁方法需要在xml中指定:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"order"</span> <span class="attr">class</span>=<span class="string">"com.company.test.Orders"</span> <span class="attr">init-method</span>=<span class="string">"init"</span> <span class="attr">destroy-method</span>=<span class="string">"destroy"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">value</span>=<span class="string">"手机"</span>&gt;</span><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Orders</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Orders</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"无参构造方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        System.out.println(<span class="string">"set设置值"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"初始化方法"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"销毁"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>现在换用注解只需要在@Bean的值里面添加initMethod=”init”,destroyMethod=”destroy”.</p>
<p>注意,对于多实例的Bean,IOC容器只负责创建,不负责管理销毁,因此销毁要自己来.</p>
<p>除了在Bean指定方法外,还可以使用InitializingBean接口</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">InitializingBean</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Invoked by the containing &#123;<span class="doctag">@code</span> BeanFactory&#125; after it has set all bean properties</span></span><br><span class="line"><span class="comment">	 * and satisfied &#123;<span class="doctag">@link</span> BeanFactoryAware&#125;, &#123;<span class="doctag">@code</span> ApplicationContextAware&#125; etc.</span></span><br><span class="line"><span class="comment">	 * &lt;p&gt;This method allows the bean instance to perform validation of its overall</span></span><br><span class="line"><span class="comment">	 * configuration and final initialization when all bean properties have been set.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@throws</span> Exception in the event of misconfiguration (such as failure to set an</span></span><br><span class="line"><span class="comment">	 * essential property) or if initialization fails for any other reason</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">afterPropertiesSet</span><span class="params">()</span> <span class="keyword">throws</span> Exception</span>;</span><br></pre></td></tr></table></figure>

<p>这个接口的方法会在BeanFactory创建完对象并设置好属性后执行,相当于上面的初始化方法.与之类似的销毁时的接口如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">DisposableBean</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Invoked by the containing &#123;<span class="doctag">@code</span> BeanFactory&#125; on destruction of a bean.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@throws</span> Exception in case of shutdown errors. Exceptions will get logged</span></span><br><span class="line"><span class="comment">	 * but not rethrown to allow other beans to release their resources as well.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span> <span class="keyword">throws</span> Exception</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这两个接口的具体用法是在某一个Bean的类中实现这两个接口和这两个方法.</p>
<p>现在还有第三种方式:使用@PostConstruct注解.该注解直接标注在方法上,用来做Bean属性赋值完成后做初始化方法.与之相对的是@PreDestroy注解.</p>
<p>此外还有一种特别的方式是使用BeanPostProcessor接口,Bean的后置处理器.它是用来对上面的初始化方法做处理.</p>
<p>postProcessBeforeInitialization:方法在初始化方法之前工作</p>
<p>postProcessAfterInitialization:方法在初始化方法之后工作</p>
<p>这里的初始化方法指上面三种都算在内.其具体原理是:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> Object <span class="title">initializeBean</span><span class="params">(<span class="keyword">final</span> String beanName, <span class="keyword">final</span> Object bean, @Nullable RootBeanDefinition mbd)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (System.getSecurityManager() != <span class="keyword">null</span>) &#123;</span><br><span class="line">			AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;</span><br><span class="line">				invokeAwareMethods(beanName, bean);</span><br><span class="line">				<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">			&#125;, getAccessControlContext());</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			invokeAwareMethods(beanName, bean);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		Object wrappedBean = bean;</span><br><span class="line">		<span class="keyword">if</span> (mbd == <span class="keyword">null</span> || !mbd.isSynthetic()) &#123;</span><br><span class="line">			wrappedBean = applyBeanPostProcessorsBeforeInitialization(wrappedBean, beanName);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			invokeInitMethods(beanName, wrappedBean, mbd);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> BeanCreationException(</span><br><span class="line">					(mbd != <span class="keyword">null</span> ? mbd.getResourceDescription() : <span class="keyword">null</span>),</span><br><span class="line">					beanName, <span class="string">"Invocation of init method failed"</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (mbd == <span class="keyword">null</span> || !mbd.isSynthetic()) &#123;</span><br><span class="line">			wrappedBean = applyBeanPostProcessorsAfterInitialization(wrappedBean, beanName);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> wrappedBean;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>



<p>这是AbstractAutowireCapableBeanFactory.java的源码.从第13行开始它先执行了applyBeanPostProcessorsBeforeInitialization,然后在try中执行了初始化方法,接着执行了applyBeanPostProcessorsAfterInitialization.</p>
<p>这两个方法内部是:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">applyBeanPostProcessorsBeforeInitialization</span><span class="params">(Object existingBean, String beanName)</span></span></span><br><span class="line"><span class="function">			<span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line"></span><br><span class="line">		Object result = existingBean;</span><br><span class="line">		<span class="keyword">for</span> (BeanPostProcessor processor : getBeanPostProcessors()) &#123;</span><br><span class="line">			Object current = processor.postProcessBeforeInitialization(result, beanName);</span><br><span class="line">			<span class="keyword">if</span> (current == <span class="keyword">null</span>) &#123;</span><br><span class="line">				<span class="keyword">return</span> result;</span><br><span class="line">			&#125;</span><br><span class="line">			result = current;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> result;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>遍历所有BeanPostProcessor然后挨个执行,一旦方法返回null则跳出执行.</p>
<p>反向溯源,在doCreateBean方法中:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">			populateBean(beanName, mbd, instanceWrapper);</span><br><span class="line">			exposedObject = initializeBean(beanName, exposedObject, mbd);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>

<p>populateBean先给属性赋值,然后再初始化.</p>
<p>为Bean的属性赋值可以使用value注解.使用该注解时可以直接赋值基本类型,也可以使用SpEL表达式(#{20-2})这种形式,也可以使用$来取配置文件中的值.为了使用配置文件,还需要在配置类上加上PropertySource注解,该注解可以赋值一个配置文件数组.</p>
<h2 id="自动注入"><a href="#自动注入" class="headerlink" title="自动注入"></a>自动注入</h2><p>Autowired自动注入,默认按类型去容器中找对应的组件,如果找到了多个同类型的组件就会把属性的名字作为id去查找组件.当然也可以使用Qualifier注解装配指定的组件id.也可以在Bean上使用Primary注解表示该Bean是首选的.当然,使用Qualifier来明确指定装配的Bean时,这种指定”首选”的方式就失效.</p>
<p>对于Autowired注入的组件必须是包含在容器中的,如果容器里不存在,就会报错.不过也可以添加required=false表示该组件不是必须的.</p>
<p>与Autowired类似的还有Resource和Inject注解(这两个注解属于Java规范,而不是Spring).具体来说,Resource功能和Autowired类似能够自动装配,默认是按照组件名称装配.</p>
<p>Inject注解使用需要导入依赖,功能和Autowired 类似.</p>
<p>Autowired注解不仅可以用来直接加在属性上来注入,还可以用setter方法注入,另外也可以放在构造器上.放在构造器上时,不仅可以放在方法上,也可以放在参数上.当某个Bean只有一个有参构造器,则这个有参构造器可以省略Autowired,参数上的Bean可以自动获取.</p>
<p>当用Bean标注一个方法,该方法参数所需的组件也能自动从容器中获取.</p>
<p>综上,构造器里和Bean注解标注的方法里都不需要写Autowired.</p>
<hr>
<p>如果需要在自定义的组件中调用Spring容器底层的一些组件,如ApplicationContext,BeanFactory等,则需要自定义组件实现对应的xxxAware接口,在创建自定义组件对象时,会调用接口规定的方法注入相关组件.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Red</span> <span class="keyword">implements</span> <span class="title">ApplicationContextAware</span> </span>&#123;</span><br><span class="line">  	<span class="keyword">private</span> ApplicationContext applicationContext;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setApplicationContext</span><span class="params">(ApplicationContext applicationContext)</span> <span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line">        System.out.println(applicationContext);</span><br><span class="line">        <span class="keyword">this</span>.applicationContext = applicationContext;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如上所示,Red这个Bean实现ApplicationContextAware接口,通过接口内的方法拿到了applicationContext.</p>
<p>每个xxxAware都对应着xxxAwareProcessor,即<strong>后置处理器</strong>.以ApplicationContextAwareProcessor为例,它实现了BeanPostProcessor接口.该类有个方法如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@Nullable</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (!(bean <span class="keyword">instanceof</span> EnvironmentAware || bean <span class="keyword">instanceof</span> EmbeddedValueResolverAware ||</span><br><span class="line">			bean <span class="keyword">instanceof</span> ResourceLoaderAware || bean <span class="keyword">instanceof</span> ApplicationEventPublisherAware ||</span><br><span class="line">			bean <span class="keyword">instanceof</span> MessageSourceAware || bean <span class="keyword">instanceof</span> ApplicationContextAware))&#123;</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	AccessControlContext acc = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (System.getSecurityManager() != <span class="keyword">null</span>) &#123;</span><br><span class="line">		acc = <span class="keyword">this</span>.applicationContext.getBeanFactory().getAccessControlContext();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (acc != <span class="keyword">null</span>) &#123;</span><br><span class="line">		AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;</span><br><span class="line">			invokeAwareInterfaces(bean);</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">		&#125;, acc);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span> &#123;</span><br><span class="line">		invokeAwareInterfaces(bean);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> bean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该方法开始是判断Bean是不是实现了xxxAware接口,接下来是一些权限判断.最主要的是最后invokeAwareInterfaces方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">invokeAwareInterfaces</span><span class="params">(Object bean)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> EnvironmentAware) &#123;</span><br><span class="line">			((EnvironmentAware) bean).setEnvironment(<span class="keyword">this</span>.applicationContext.getEnvironment());</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> EmbeddedValueResolverAware) &#123;</span><br><span class="line">			((EmbeddedValueResolverAware) bean).setEmbeddedValueResolver(<span class="keyword">this</span>.embeddedValueResolver);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> ResourceLoaderAware) &#123;</span><br><span class="line">			((ResourceLoaderAware) bean).setResourceLoader(<span class="keyword">this</span>.applicationContext);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> ApplicationEventPublisherAware) &#123;</span><br><span class="line">			((ApplicationEventPublisherAware) bean).setApplicationEventPublisher(<span class="keyword">this</span>.applicationContext);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> MessageSourceAware) &#123;</span><br><span class="line">			((MessageSourceAware) bean).setMessageSource(<span class="keyword">this</span>.applicationContext);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> ApplicationContextAware) &#123;</span><br><span class="line">			((ApplicationContextAware) bean).setApplicationContext(<span class="keyword">this</span>.applicationContext);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>现在是ApplicationContextAware,也就是最后一个,所以调用set方法传入了ApplicationContextAware这个Bean.</p>
<h2 id="Profile"><a href="#Profile" class="headerlink" title="Profile"></a>Profile</h2><p>profile是Spring提供的可以根据当前环境,动态的激活和切换一系列组件的功能.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.config;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.mchange.v2.c3p0.ComboPooledDataSource;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Value;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Profile;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.PropertySource;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.sql.DataSource;</span><br><span class="line"><span class="keyword">import</span> java.beans.PropertyVetoException;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/19 11:00</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@PropertySource</span>(<span class="string">"classpath:/jdbc.properties"</span>)</span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MainConfigProfile</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;user&#125;"</span>)</span><br><span class="line">    <span class="keyword">private</span> String user;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Profile</span>(<span class="string">"test"</span>)</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSource <span class="title">dataSourceTest</span><span class="params">(@Value(<span class="string">"$&#123;password&#125;"</span>)</span> String pwd) <span class="keyword">throws</span> PropertyVetoException </span>&#123;</span><br><span class="line">        ComboPooledDataSource dataSource = <span class="keyword">new</span> ComboPooledDataSource();</span><br><span class="line">        dataSource.setJdbcUrl(<span class="string">"jdbc:mysql://localhost:3306/test?serverTimezone=GMT"</span>);</span><br><span class="line">        dataSource.setUser(user);</span><br><span class="line">        dataSource.setPassword(pwd);</span><br><span class="line">        dataSource.setDriverClass(<span class="string">"com.mysql.cj.jdbc.Driver"</span>);</span><br><span class="line">        <span class="keyword">return</span> dataSource;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Profile</span>(<span class="string">"dev"</span>)</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSource <span class="title">dataSourceDev</span><span class="params">(@Value(<span class="string">"$&#123;password&#125;"</span>)</span> String pwd) <span class="keyword">throws</span> PropertyVetoException </span>&#123;</span><br><span class="line">        ComboPooledDataSource dataSource = <span class="keyword">new</span> ComboPooledDataSource();</span><br><span class="line">        dataSource.setJdbcUrl(<span class="string">"jdbc:mysql://localhost:3306/book?serverTimezone=GMT"</span>);</span><br><span class="line">        dataSource.setUser(user);</span><br><span class="line">        dataSource.setPassword(pwd);</span><br><span class="line">        dataSource.setDriverClass(<span class="string">"com.mysql.cj.jdbc.Driver"</span>);</span><br><span class="line">        <span class="keyword">return</span> dataSource;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>现在有两个Profile,分别是test和dev(名字随便).如果某个Bean不指定Profile,那么任何环境都能注册这个组件.</p>
<p>第一种切换环境的方式:在运行的配置中加入vm参数-Dspring.profile.active=环境名</p>
<p>第二种用代码实现,先看下原来用<code>ApplicationContext context = new AnnotationConfigApplicationContext(MainConfig2.class);</code>的代码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">AnnotationConfigApplicationContext</span><span class="params">(Class&lt;?&gt;... componentClasses)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>();</span><br><span class="line">		register(componentClasses);</span><br><span class="line">		refresh();</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到这种方式是先调用无参构造器,然后注册配置类,然后刷新.现在使用Profile就是在注册前加一步:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="comment">//创建一个context</span></span><br><span class="line">  AnnotationConfigApplicationContext context = <span class="keyword">new</span> AnnotationConfigApplicationContext();</span><br><span class="line">  <span class="comment">//设置需要激活的环境</span></span><br><span class="line">  context.getEnvironment().setActiveProfiles(<span class="string">"test"</span>, <span class="string">"dev"</span>);</span><br><span class="line">  <span class="comment">//注册主配置类</span></span><br><span class="line">  context.register(MainConfigProfile<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">  <span class="comment">//启动刷新容器</span></span><br><span class="line">  context.refresh();</span><br><span class="line">  String[] names = context.getBeanDefinitionNames();</span><br><span class="line">  Arrays.stream(names).forEach(System.out::println);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Profile注解不仅可以加在方法上用来动态添加Bean,也可以直接写在配置类上.只有是指定环境时才会加载配置类.</p>
<h1 id="AOP"><a href="#AOP" class="headerlink" title="AOP"></a>AOP</h1><p>首先写切面类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.company.aop;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.aspectj.lang.JoinPoint;</span><br><span class="line"><span class="keyword">import</span> org.aspectj.lang.annotation.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> xianzheTM</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/7/19 14:33</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Aspect</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LogAspects</span> </span>&#123;</span><br><span class="line">    <span class="comment">//若是在别的地方要用切点,则要写全类名;本类使用只要写方法名</span></span><br><span class="line">    <span class="meta">@Pointcut</span>(<span class="string">"execution(public int com.company.aop.MathCalculator.*(..))"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">pointCut</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Before</span>(<span class="string">"pointCut()"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">logStart</span><span class="params">(JoinPoint joinPoint)</span> </span>&#123;</span><br><span class="line">        Object[] args = joinPoint.getArgs();</span><br><span class="line">        System.out.println(joinPoint.getSignature().getName() + <span class="string">"运行...@Before参数列表是:&#123;"</span> + Arrays.asList(args) + <span class="string">"&#125;"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@After</span>(<span class="string">"pointCut()"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">logEnd</span><span class="params">(JoinPoint joinPoint)</span> </span>&#123;</span><br><span class="line">        System.out.println(joinPoint.getSignature().getName() + <span class="string">"结束...@After"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//joinpoint参数必须要放在第一位</span></span><br><span class="line">    <span class="meta">@AfterReturning</span>(value = <span class="string">"pointCut()"</span>, returning = <span class="string">"result"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">logReturn</span><span class="params">(JoinPoint joinPoint, Object result)</span> </span>&#123;</span><br><span class="line">        System.out.println(joinPoint.getSignature().getName() + <span class="string">"正常返回...@AfterReturning运行结果是:&#123;"</span> + result + <span class="string">"&#125;"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@AfterThrowing</span>(value = <span class="string">"pointCut()"</span>, throwing = <span class="string">"exception"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">logException</span><span class="params">(JoinPoint joinPoint, Exception exception)</span> </span>&#123;</span><br><span class="line">        System.out.println(joinPoint.getSignature().getName() + <span class="string">"异常...异常信息是:&#123;"</span> + exception + <span class="string">"&#125;"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意加@aspect注解,表示这是一个注解类.其中After通知是在方法结束后进行,不论方法是否正常执行.AfterReturning是在方法正常返回后执行.</p>
<p>在配置类中还要通过注解开启AOP:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@EnableAspectJAutoProxy</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MainConfigAOP</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> MathCalculator <span class="title">calculator</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> MathCalculator();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> LogAspects <span class="title">logAspects</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> LogAspects();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>@EnableAspectJAutoProxy该注解实际导入了AspectJAutoProxyRegistrar这个Bean:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200719174233.png" alt="20200719174233"></p>
<p>这个类的代码如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AspectJAutoProxyRegistrar</span> <span class="keyword">implements</span> <span class="title">ImportBeanDefinitionRegistrar</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Register, escalate, and configure the AspectJ auto proxy creator based on the value</span></span><br><span class="line"><span class="comment">	 * of the @&#123;<span class="doctag">@link</span> EnableAspectJAutoProxy#proxyTargetClass()&#125; attribute on the importing</span></span><br><span class="line"><span class="comment">	 * &#123;<span class="doctag">@code</span> <span class="doctag">@Configuration</span>&#125; class.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">registerBeanDefinitions</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">			AnnotationMetadata importingClassMetadata, BeanDefinitionRegistry registry)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		AopConfigUtils.registerAspectJAnnotationAutoProxyCreatorIfNecessary(registry);</span><br><span class="line"></span><br><span class="line">		AnnotationAttributes enableAspectJAutoProxy =</span><br><span class="line">				AnnotationConfigUtils.attributesFor(importingClassMetadata, EnableAspectJAutoProxy<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">		<span class="keyword">if</span> (enableAspectJAutoProxy != <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">if</span> (enableAspectJAutoProxy.getBoolean(<span class="string">"proxyTargetClass"</span>)) &#123;</span><br><span class="line">				AopConfigUtils.forceAutoProxyCreatorToUseClassProxying(registry);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">if</span> (enableAspectJAutoProxy.getBoolean(<span class="string">"exposeProxy"</span>)) &#123;</span><br><span class="line">				AopConfigUtils.forceAutoProxyCreatorToExposeProxy(registry);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该类的方法传入了一个BeanDefinitionRegistry,这个Bean前面用来自定义注册Bean,这里的用处也是如此.</p>
<p>该方法的第一行调用了AopConfigUtils的registerAspectJAnnotationAutoProxyCreatorIfNecessary方法,看方法名知道它(如果必要)注册了AspectJAnnotationAutoProxyCreator.根据方法调用栈:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200719201938.png" alt="20200719201938"></p>
<p>它来到这个方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> BeanDefinition <span class="title">registerAspectJAnnotationAutoProxyCreatorIfNecessary</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">			BeanDefinitionRegistry registry, @Nullable Object source)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> registerOrEscalateApcAsRequired(AnnotationAwareAspectJAutoProxyCreator<span class="class">.<span class="keyword">class</span>, <span class="title">registry</span>, <span class="title">source</span>)</span>;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p><code>registerOrEscalateApcAsRequired</code>表示如果需要就注册或逐步升级.方法传入了<code>AnnotationAwareAspectJAutoProxyCreator.class</code>.具体看这个方法:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200719204221.jpg" alt="20200719204221"></p>
<p>可以看到这里使用<code>cls</code>作为参数名表示<code>AnnotationAwareAspectJAutoProxyCreator.class</code>.紧接着做了一个判断,判断是否包含一个特定的Bean:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200719210613.png" alt="20200719210613"></p>
<p>它判断了一下是否包含<code>internalAutoProxyCreator</code>,现在是第一次运行,不包含.直接看后面:</p>
<p>用cls创建了一个RootBeanDefinition,即Bean的定义信息.接着将Bean的定义信息和Bean的名字注册:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200719213523141.png" alt="image-20200719213523141"></p>
<p>最终注册了一个internalAutoProxyCreator的Bean定义,这个Bean定义具体是cls,即AnnotationAwareAspectJAutoProxyCreator.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200719215918229.png" alt="image-20200719215918229"></p>
<p>查看AnnotationAwareAspectJAutoProxyCreator的类间关系:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200719221156187.png" alt="image-20200719221156187"></p>
<p>可以看到该类即实现了SmartInstantiationAwareBeanPostProcessor接口,又实现了BeanFactoryAware接口.现在搞明白这个类实现这两个接口干了什么就知道AOP怎么实现的了.</p>
<p>查看AbstractAutoProxyCreator这个类的实现,只关注和BeanFactory和后置处理器相关的内容,并打上断点(有些是空方法,不管):</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200719222443746.png" alt="image-20200719222443746"></p>
<p>从父类来到子类,发现setBeanFactory方法被重写了,里面调用了initBeanFactory方法.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200719222819497.png" alt="image-20200719222819497"></p>
<p>来到AnnotationAwareAspectJAutoProxyCreator发现initBeanFactory方法被重写了,说明上面调用的实际是这里的方法.为这几个关键方法打上断点debug一下.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200719224916953.png" alt="image-20200719224916953"></p>
<p>先从传入配置类,创建IOC容器开始.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200719223851657.png" alt="image-20200719223851657"></p>
<p>上面提过,注册配置类,调用refresh刷新容器.</p>
<p>在refresh方法里是注册bean后置处理器来拦截Bean的创建:<img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200719224343974.png" alt="image-20200719224343974"></p>
<p>在第四步中用到的方法第一行是这样的代码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String[] postProcessorNames = beanFactory.getBeanNamesForType(BeanPostProcessor<span class="class">.<span class="keyword">class</span>, <span class="title">true</span>, <span class="title">false</span>)</span>;</span><br></pre></td></tr></table></figure>

<p>获取了IOC容器中已经定义的需要创建对象的所有BeanPostProcessor.因为spring自身有一些默认的后置处理器,.外加如上面分析的那样,@EnableAspectJAutoProxy这个注解使得创建容器时定义了internalAutoProxyCreator (实际Bean定义是AnnotationAwareAspectJAutoProxyCreator).但此时只是定义了,还没有被创建.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200719230543268.png" alt="image-20200719230543268"></p>
<p>可以看到internalAutoProxyCreator和另一个Processor要被创建.(照理说应该不止两个的).这个方法的完整代码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">registerBeanPostProcessors</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">			ConfigurableListableBeanFactory beanFactory, AbstractApplicationContext applicationContext)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		String[] postProcessorNames = beanFactory.getBeanNamesForType(BeanPostProcessor<span class="class">.<span class="keyword">class</span>, <span class="title">true</span>, <span class="title">false</span>)</span>;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Register BeanPostProcessorChecker that logs an info message when</span></span><br><span class="line">		<span class="comment">// a bean is created during BeanPostProcessor instantiation, i.e. when</span></span><br><span class="line">		<span class="comment">// a bean is not eligible for getting processed by all BeanPostProcessors.</span></span><br><span class="line">		<span class="keyword">int</span> beanProcessorTargetCount = beanFactory.getBeanPostProcessorCount() + <span class="number">1</span> + postProcessorNames.length;</span><br><span class="line">		beanFactory.addBeanPostProcessor(<span class="keyword">new</span> BeanPostProcessorChecker(beanFactory, beanProcessorTargetCount));</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Separate between BeanPostProcessors that implement PriorityOrdered,</span></span><br><span class="line">		<span class="comment">// Ordered, and the rest.</span></span><br><span class="line">		List&lt;BeanPostProcessor&gt; priorityOrderedPostProcessors = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">		List&lt;BeanPostProcessor&gt; internalPostProcessors = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">		List&lt;String&gt; orderedPostProcessorNames = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">		List&lt;String&gt; nonOrderedPostProcessorNames = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">		<span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">			<span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered<span class="class">.<span class="keyword">class</span>)) </span>&#123;</span><br><span class="line">				BeanPostProcessor pp = beanFactory.getBean(ppName, BeanPostProcessor<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">				priorityOrderedPostProcessors.add(pp);</span><br><span class="line">				<span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">					internalPostProcessors.add(pp);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, Ordered<span class="class">.<span class="keyword">class</span>)) </span>&#123;</span><br><span class="line">				orderedPostProcessorNames.add(ppName);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				nonOrderedPostProcessorNames.add(ppName);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// First, register the BeanPostProcessors that implement PriorityOrdered.</span></span><br><span class="line">		sortPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line">		registerBeanPostProcessors(beanFactory, priorityOrderedPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Next, register the BeanPostProcessors that implement Ordered.</span></span><br><span class="line">		List&lt;BeanPostProcessor&gt; orderedPostProcessors = <span class="keyword">new</span> ArrayList&lt;&gt;(orderedPostProcessorNames.size());</span><br><span class="line">		<span class="keyword">for</span> (String ppName : orderedPostProcessorNames) &#123;</span><br><span class="line">			BeanPostProcessor pp = beanFactory.getBean(ppName, BeanPostProcessor<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">			orderedPostProcessors.add(pp);</span><br><span class="line">			<span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">				internalPostProcessors.add(pp);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		sortPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line">		registerBeanPostProcessors(beanFactory, orderedPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Now, register all regular BeanPostProcessors.</span></span><br><span class="line">		List&lt;BeanPostProcessor&gt; nonOrderedPostProcessors = <span class="keyword">new</span> ArrayList&lt;&gt;(nonOrderedPostProcessorNames.size());</span><br><span class="line">		<span class="keyword">for</span> (String ppName : nonOrderedPostProcessorNames) &#123;</span><br><span class="line">			BeanPostProcessor pp = beanFactory.getBean(ppName, BeanPostProcessor<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">			nonOrderedPostProcessors.add(pp);</span><br><span class="line">			<span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">				internalPostProcessors.add(pp);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		registerBeanPostProcessors(beanFactory, nonOrderedPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Finally, re-register all internal BeanPostProcessors.</span></span><br><span class="line">		sortPostProcessors(internalPostProcessors, beanFactory);</span><br><span class="line">		registerBeanPostProcessors(beanFactory, internalPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Re-register post-processor for detecting inner beans as ApplicationListeners,</span></span><br><span class="line">		<span class="comment">// moving it to the end of the processor chain (for picking up proxies etc).</span></span><br><span class="line">		beanFactory.addBeanPostProcessor(<span class="keyword">new</span> ApplicationListenerDetector(applicationContext));</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>主要分为两部分,先将postProcessorNames数组里的PostProcessor分类,按照priorityOrderedPostProcessors,orderedPostProcessorNames,nonOrderedPostProcessorNames等,划分依据是看它是否实现了PriorityOrdered和Ordered接口(19行和26行).</p>
<p>在debug中,现在代码来到41行.这就验证了上面的逻辑,internalAutoProxyCreator在orderedPostProcessorNames这个数组里.前面说过,</p>
<p>internalAutoProxyCreator的定义是AnnotationAwareAspectJAutoProxyCreator,而查看上面的关系图可知它确实实现了Ordered接口.</p>
<p>接着研究getBean这个方法,看其调用栈,掠过中间过程来到11.发现停在一个巨大的方法中,这里先看调用其它方法的地方:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720144726937.png" alt="image-20200720144726937"></p>
<p>注释写明这是初始化Bean实例的意思.那么倒推前面应该有创建Bean的地方:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720145003733.png" alt="image-20200720145003733"></p>
<p>果然找到了创建Bean的地方(Instantiate实例化的意思).createBeanInstance方法的前两个参数值:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720145152819.png" alt="image-20200720145152819"></p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720145223593.png" alt="image-20200720145223593"></p>
<p>这不就和前面的对上了.</p>
<p>回到初始化Bean的地方,populateBean方法是给Bean的属性赋值(populate意思是填充).属性赋值完成后就是初始化Bean(initializeBean方法).查看这个初始化方法,发现它调用了invokeAwareMethods方法.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720150339620.png" alt="image-20200720150339620"></p>
<p>先不管invokeAwareMethods方法,看看这个方法下面是什么:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720150521548.png" alt="image-20200720150521548"></p>
<p>老朋友又来了,使用在初始化之前的后置处理器方法.点开这个方法看看:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720150759158.png" alt="image-20200720150759158"></p>
<p>这不就是前面提过的AbstractAutowireCapableBeanFactory中的方法.遍历后置处理器并调用后置处理器的前置方法(初始化前的方法).</p>
<p>回到initializeBean方法,在这个apply方法后面是执行了invokeInitMethods方法.确实按照上面的逻辑,前置方法完了就该轮到初始化方法.初始化方法完了就是后置处理器的后置方法了.一看代码确实如此:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720151517109.png" alt="image-20200720151517109"></p>
<p>这几步是初始化和初始化的前置后置处理,那么Bean的创建前面已经有了,属性赋值也有了,这里清楚后回到开始的invokeAwareMethods.这里调用了setBeanFactory方法,这个setBeanFactory方法就是一开始所说被重写的那个setBeanFactory方法.这里被重写最主要的就是添加了一个initBeanFactory方法,该方法具体为:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720161918816.png" alt="image-20200720161918816"></p>
<p>主要是初始化BeanFactory.</p>
<p>至此,后置处理器创建完成.registerBeanPostProcessors方法的第四十一行终于执行完.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720162843535.png" alt="image-20200720162843535"></p>
<p>接着把后置处理器排了个序,然后注册.所谓的注册也就是让BeanFactory添加Bean:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720163057424.png" alt="image-20200720163057424"></p>
<p>而这里现在实际只有一个后置处理器,即AnnotationAwareAspectJAutoProxyCreator.这就是现在我们需要的东西.</p>
<p><strong>未完</strong></p>
]]></content>
      <tags>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis</title>
    <url>/2020/07/15/Redis/</url>
    <content><![CDATA[<p>Redis使用.</p>
<a id="more"></a>

<h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><ul>
<li>下载压缩包</li>
<li>传到Linux服务器解压</li>
<li>到文件目录下用make命令编译</li>
<li>make PREFIX=/usr/local/redis install安装到指定目录</li>
<li>到上一步的目录下<code>./redis-server</code>启动服务端</li>
<li><code>./redis-cli</code>进入客户端,如果是非本机连接:redis-cli -h IP地址 -p 端口(默认IP为本机,端口6379)</li>
</ul>
<h1 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h1><p>将解压文件夹里的redis.conf移动到安装目录,然后编辑该文件.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#表示现在只能本机访问</span><br><span class="line">bind 127.0.0.1</span><br><span class="line"></span><br><span class="line">#端口号</span><br><span class="line">port 6379</span><br><span class="line"></span><br><span class="line">#让redis以守护进程方式运行</span><br><span class="line">daemonize yes</span><br><span class="line"></span><br><span class="line">#设置数据库的数量，默认数据库为0，可以使用SELECT &lt;dbid&gt;命令在连接上指定数据库id</span><br><span class="line">databases 16</span><br><span class="line"></span><br><span class="line">#指定在多长时间内，有多少次更新操作，就将数据同步到数据文件，可以多个条件配合</span><br><span class="line">#save &lt;seconds&gt; &lt;changes&gt;</span><br><span class="line">#Redis默认配置文件中提供了三个条件：</span><br><span class="line">save 900 1</span><br><span class="line">save 300 10</span><br><span class="line">save 60 10000</span><br><span class="line">#分别表示900秒（15分钟）内有1个更改，300秒（5分钟）内有10个更改以及60秒内有10000个更改。</span><br><span class="line"></span><br><span class="line">#指定存储至本地数据库时是否压缩数据，默认为yes，Redis采用LZF(压缩算法）压缩，如果为了节省CPU时间，可以关闭该选项，但会导致数据库文件变的巨大</span><br><span class="line">rdbcompression yes</span><br><span class="line"></span><br><span class="line">#指定本地数据库文件名，默认值为dump.rdb</span><br><span class="line">dbfilename dump.rdb</span><br><span class="line"></span><br><span class="line">#指定本地数据库存放目录</span><br><span class="line">dir .&#x2F;</span><br><span class="line"></span><br><span class="line">#设置Redis连接密码，如果配置了连接密码，客户端在连接Redis时需要通过AUTH &lt;password&gt;命令提供密码，默认关闭</span><br><span class="line">requirepass foobared</span><br></pre></td></tr></table></figure>

<p>编辑文件内有一段include,用来引入其它配置文件,有共用的配置文件时需要用到.</p>
<h1 id="Redis内存维护策略"><a href="#Redis内存维护策略" class="headerlink" title="Redis内存维护策略"></a>Redis内存维护策略</h1><h2 id="为数据设置超时时间"><a href="#为数据设置超时时间" class="headerlink" title="为数据设置超时时间"></a>为数据设置超时时间</h2><p><code>ttl key1</code>查询key1的过期时间.没有设置过期时间的就是永不过期.-1为永不过期,-2表示已过期</p>
<p><code>expire key time</code>设置过期时间,以秒为单位.字符串要采用另一种方式<code>setex(String key,int seconds,String value)</code></p>
<h2 id="LRU算法"><a href="#LRU算法" class="headerlink" title="LRU算法"></a>LRU算法</h2><p>内存页面管理的算法,对于在内存中但又不用的数据块（内存块）叫做LRU，操作系统会根据哪些数据属于LRU而将其移出内存而腾出空间来加载另外的数据。</p>
<p>LRU-&gt;Least Recently Used</p>
<p>LFU-&gt;Least Frequently Used</p>
<ol>
<li>volatile-lru：设定超时时间的数据中,删除最不常使用的数据.</li>
<li>allkeys-lru：查询所有的key中最近最不常使用的数据进行删除，这是应用最广泛的策略.</li>
<li>volatile-random：在已经设定了超时的数据中随机删除.</li>
<li>allkeys-random：查询所有的key,之后随机删除.</li>
<li>volatile-ttl：查询全部设定超时时间的数据,之后排序,将马上将要过期的数据进行删除操作.</li>
<li>noeviction：如果设置为该属性,则不会进行删除操作,如果内存溢出则报错返回.</li>
<li>volatile-lfu：从所有配置了过期时间的键中驱逐使用频率最少的键</li>
<li>allkeys-lfu：从所有键中驱逐使用频率最少的键</li>
</ol>
<h1 id="Redis数据类型"><a href="#Redis数据类型" class="headerlink" title="Redis数据类型"></a>Redis数据类型</h1><ul>
<li>string</li>
<li>set</li>
<li>list</li>
<li>hash</li>
<li>zset</li>
</ul>
<p><code>key *</code>查询当前库的所有键</p>
<p><code>exists &lt;key&gt;</code>查询某个键是否存在</p>
<p><code>type &lt;key&gt;</code>查看键类型</p>
<p><code>del &lt;key&gt;</code>删除某个键</p>
<p><code>dbsize</code>查看当前库的key的数量</p>
<p><code>Flushdb</code>清空当前库</p>
<p><code>Flushall</code>清空全部</p>
<h2 id="string"><a href="#string" class="headerlink" title="string"></a>string</h2><p>string是最基本的类型,它是二进制安全的,可以包含任意数据,例如图片或者序列化的对象.value最大值为512M.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">set k1 v1</span><br><span class="line">ok</span><br><span class="line"></span><br><span class="line">get k1</span><br><span class="line">&quot;v1&quot;</span><br><span class="line"></span><br><span class="line">append k1 234&#x2F;&#x2F;在k1的value后追加234</span><br><span class="line">(integer)5</span><br><span class="line"></span><br><span class="line">strlen k1</span><br><span class="line">(integer)5</span><br><span class="line"></span><br><span class="line">setnx k1 abc&#x2F;&#x2F;SET if Not eXists</span><br><span class="line"></span><br><span class="line">set k2 12</span><br><span class="line">incr k2&#x2F;&#x2F;为k2自增1,注意要类型匹配能够加的才加</span><br><span class="line"></span><br><span class="line">decr k2&#x2F;&#x2F;减一</span><br><span class="line"></span><br><span class="line">incrby&#x2F;decrby k2 step&#x2F;&#x2F;自定义加减的步长</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mset k1 v1 k2 v2&#x2F;&#x2F;一次设置多个值</span><br><span class="line"></span><br><span class="line">mget k1 k2&#x2F;&#x2F;一次获取多个值</span><br><span class="line"></span><br><span class="line">msetnx k1 v1 k2 v2&#x2F;&#x2F;当k1,k2不存在时设置值</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">getrange k1 0 2&#x2F;&#x2F;获取指定key的指定范围内的子字符串,闭集</span><br><span class="line">&#x2F;&#x2F;与之类似的setrange是设置范围内的值</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">setex &lt;key&gt; &lt;过期时间&gt; &lt;value&gt;</span><br><span class="line"></span><br><span class="line">getset &lt;key&gt; &lt;value&gt;&#x2F;&#x2F;以新换旧,同时获得旧值</span><br></pre></td></tr></table></figure>

<p>严格来说,Redis中只有string类型,下面几种集合类型内部都是放的string.</p>
<h2 id="List"><a href="#List" class="headerlink" title="List"></a>List</h2><p>List是一个字符串列表,按照插入顺序排序.底层是一个双向链表,两端操作性能高.</p>
<p>lpush/rpush <key> <v1> <v2>:从左边或右边插入一个或多个值</v2></v1></key></p>
<p>lpop/rpop <key>:弹出一个值(list内不再保留)</key></p>
<p>rpoplpush <key1> <key2>:从key1右边弹出一个值压入key2左边</key2></key1></p>
<p>lrange <key> <start> <stop></stop>:按照所有下标获得元素,从左到右,0~-1可以表示全部</start></key></p>
<p>lindex <key> <index>:按照索引获得元素</index></key></p>
<p>llen <key>:获得列表长度</key></p>
<p>linsert <key> before <value> <newvalue>:在value的前面插入新值,after是在后面</newvalue></value></key></p>
<p>lrem <key> <n> <value>:从左边删除n个value,n为正数表示从左往右删,负数表示从右往左删,0表示删除所有</value></n></key></p>
<h2 id="Set"><a href="#Set" class="headerlink" title="Set"></a>Set</h2><p>Set是无序的,不重复的集合,底层是一个value为null的hash表.</p>
<p>sadd <key> <v1> <v2>:添加多个值</v2></v1></key></p>
<p>smembers <key>:获取所有值</key></p>
<p>sismember <key> <v1>:判断是否包含v</v1></key></p>
<p>scard <key>:返回个数</key></p>
<p>srem <key> <v1> <v2>:删除元素</v2></v1></key></p>
<p>spop <key>:随机弹出一个,删除</key></p>
<p>srandmember <key> <n>:随机取出n个值,不会删除</n></key></p>
<p>sinter <key1> <key2>:返回两个集合的交集</key2></key1></p>
<p>sunion <key1> <key2>:并集</key2></key1></p>
<p>sdiff <key1> <key2>:差集</key2></key1></p>
<h2 id="hash"><a href="#hash" class="headerlink" title="hash"></a>hash</h2><p>键值对,类似于Map&lt;String,String&gt;.</p>
<p>hset <key> <filed> <value>:key是hash的名字,filed是键值对的键</value></filed></key></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hset userInfo user:1010:uid 1010</span><br></pre></td></tr></table></figure>

<p>hmset <key> <filed> <value> <filed> <value>:设置多个键值对</value></filed></value></filed></key></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hmset userInfo user:1010:username admin user:1010:passwd 123</span><br></pre></td></tr></table></figure>

<p>hget key filed:取出键值对的值</p>
<p>hexists key <filed>:判断是否存在</filed></p>
<p>hkeys <key>:列出所有filed</key></p>
<p>hvals <key>:列出所有value</key></p>
<p>hgetall <key>:列出所有键值对</key></p>
<p>hincrby <key> <field> <increment>:为field所指的value加上increment值</increment></field></key></p>
<p>hsetnx <key> <field> <value>:不存在键值对时添加</value></field></key></p>
<h2 id="zset"><a href="#zset" class="headerlink" title="zset"></a>zset</h2><p>zset有序,但是不是按照元素本身排序,而是每个元素关联了一个评分,按照这个评分排序(从低到高).集合元素唯一,但评分不唯一.</p>
<p>zadd <key> <score1> <value1> <score2> <value2>:添加多个,如果添加value相同但分数不同,会把原来的分数覆盖</value2></score2></value1></score1></key></p>
<p>zrange <key> <start> <stop></stop>:返回下标范围内元素,withscores可以带分数返回</start></key></p>
<p>zrangebyscore <key> min max:返回分数范围内的value,同样也可以用withscores返回分数</key></p>
<p>zrevrangebyscore key max min:反过来</p>
<p>zincrby <key> <increment> <value>:为元素的分数加上增量</value></increment></key></p>
<p>zrem <key> <value>:删除指定元素</value></key></p>
<p>zcount <key> <min> <max>:统计分数区间内的个数</max></min></key></p>
<p>zrank <key> <value>:返回该元素在集合中的排名,从0开始</value></key></p>
<h2 id="用Java操作redis"><a href="#用Java操作redis" class="headerlink" title="用Java操作redis"></a>用Java操作redis</h2><p>需要用Jedis这个包,然后调用其方法即可.Jedis的方法与上面的方法完全一样.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    Jedis jedis = <span class="keyword">new</span> Jedis(<span class="string">"127.0.0.1"</span>, <span class="number">6379</span>);</span><br><span class="line">    String result = jedis.ping();</span><br><span class="line">    System.out.println(result);</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (i &lt; <span class="number">1000</span>) &#123;</span><br><span class="line">        String value = UUID.randomUUID().toString().replace(<span class="string">"-"</span>, <span class="string">""</span>).substring(<span class="number">0</span>, <span class="number">8</span>);</span><br><span class="line">        jedis.lpush(<span class="string">"name"</span>, value);</span><br><span class="line">        i++;</span><br><span class="line">    &#125;</span><br><span class="line">    jedis.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Redis事务"><a href="#Redis事务" class="headerlink" title="Redis事务"></a>Redis事务</h1><p>Redis事务的主要作用就是串联多个命令防止别的命令插队.</p>
<p>Multi开始事务,将后续命令加入队列,Exec执行事务(队列里的命令),discard取消事务.</p>
<p>执行事务出现错误有两种:第一,类似于编译时异常,比如几条命令中有一条写错了set写成srt,事务就会直接取消;第二,类似于运行时异常,事务开始执行后某一条命令出现错误,事务并不会取消,而是正确执行的就执行,错误的就取消.</p>
<p>可以通过watch监视一个或多个key,如果发生改变,那么与之相关的事务全部取消.与之相对的命令是unwatch.</p>
<p>三个特性:</p>
<ul>
<li>单独的隔离操作,事务中的命令都是串行的,事务在执行过程中不会被其它客户端的命令打断.</li>
<li>没有隔离级别,因为事务队列里的命令在未提交之前都没有实际执行,也就不存在Mysql里的脏读幻读等问题.</li>
<li>不保证原子性,同一个事务中有错误的话,并不影响事务中其它命令执行.</li>
</ul>
<p>现在设想一个场景:商品页面售货,有大量请求进来购买,服务器用redis记录商品数量,当数量小于等于0时停止售卖.</p>
<p>为了测试Redis在高并发情况下的事务,现在安装一个高并发模拟工具</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install apache2-utils</span><br></pre></td></tr></table></figure>

<p>这里面有一个ab工具用来模拟并发,执行命令:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ab -n 1000 -c 200 -p &#x2F;opt&#x2F;postfile -T &quot;application&#x2F;x-www-form-urlencoded&quot; 127.0.0.1:8080&#x2F;Redis&#x2F;doseckill</span><br></pre></td></tr></table></figure>

<p>-n表示请求个数,-c并发数(瞬间发送的请求个数),-p表示要发送的数据文件,-T表示请求头信息,后面是请求地址.</p>
<p>在没有启用事务且并发较高的情况下,有可能出现商品数量已经小于0,但仍在售卖的现象.例如有100个请求获取到商品数量为5,然后它们都去给数量减一,结果就造成数量为-95,出现超卖的问题.</p>
<p>这里其实应该使用watch监视库存.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Jedis jedis = <span class="keyword">new</span> Jedis(<span class="string">"127.0.0.1"</span>, <span class="number">6379</span>);</span><br><span class="line">jedis.watch(kc);<span class="comment">//监视redis中key为kc的值</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Transaction transaction = jedis.multi();</span><br><span class="line">transaction.decr(kcKey);</span><br><span class="line">transaction.sadd(userKey,uid);</span><br><span class="line">List&lt;Object&gt; list = transaction.exec();</span><br></pre></td></tr></table></figure>
<p>接着判断这个集合,如果为空表示事务执行失败.</p>
<p>但是这有个问题,使用watch后,一次并发的200次请求只有一个能成功让数量减一,而其它199个都因为事务的原因不能减.这不符合逻辑,因为此时商品数量还有.如果向参数设置那样一共就1000个请求,五次并发就没了,商品只卖了5个.</p>
<p>如何改进?这里使用lua脚本.该脚本有一定原子性,不会被其它命令插队.只需要让每个请求执行这个脚本即可.</p>
<h1 id="持久化"><a href="#持久化" class="headerlink" title="持久化"></a>持久化</h1><h2 id="RDB"><a href="#RDB" class="headerlink" title="RDB"></a>RDB</h2><p>RDB其实就是把数据以快照的形式保存在磁盘上。</p>
<p>RDB持久化是指在指定的时间间隔内将内存中的数据集快照写入磁盘。也是默认的持久化方式，这种方式是就是将内存中数据以快照的方式写入到二进制文件中,默认的文件名为dump.rdb。</p>
<p>既然RDB机制是通过把某个时刻的所有数据生成一个快照来保存，那么就应该有一种触发机制，是实现这个过程。对于RDB来说，提供了三种机制：save、bgsave、自动化。</p>
<p>1、save 触发方式</p>
<p>该命令会阻塞当前 Redis 服务器，执行 save 命令期间，Redis 不能处理其他命令，直到 RDB 过程完成为止。具体流程如下：</p>
<p><img src="https://pics1.baidu.com/feed/e7cd7b899e510fb3aa8c05042b22c093d0430ca7.jpeg?token=7ed4cf784a82d04e60b8dc72cf7e3c24&s=EDBAA5565D1859C85444707E02005071" alt></p>
<p>执行完成时候如果存在老的 RDB 文件，就把新的替代掉旧的。我们的客户端可能都是几万或者是几十万，这种方式显然不可取。</p>
<p>2、bgsave 触发方式</p>
<p>执行该命令时，Redis 会在后台异步进行快照操作，快照同时还可以响应客户端请求。具体流程如下：</p>
<p><img src="https://pics5.baidu.com/feed/023b5bb5c9ea15cefb035bc8431132f53b87b21e.jpeg?token=a72f072d65d2de548d71bb459cd0bf4f&s=05AAFE168FF04C8A10FD2DEE0300E032" alt></p>
<p>具体操作是 Redis 进程执行 fork 操作创建子进程，RDB 持久化过程由子进程负责，完成后自动结束。阻塞只发生在 fork 阶段，一般时间很短。基本上 Redis 内部所有的 RDB 操作都是采用 bgsave 命令。</p>
<p>3、自动触发</p>
<p>自动触发是由我们的配置文件来完成的。在 redis.conf 配置文件中，里面有如下配置，我们可以去设置：</p>
<p>①save：这里是用来配置触发 Redis 的 RDB 持久化条件，也就是什么时候将内存中的数据保存到硬盘。比如 “save m n”。表示 m 秒内数据集存在 n 次修改时，自动触发 bgsave。</p>
<p>默认如下配置：</p>
<p>#表示 900 秒内如果至少有 1 个 key 的值变化，则保存 </p>
<p>save 900 1</p>
<p>#表示 300 秒内如果至少有 10 个 key 的值变化，则保存 </p>
<p>save 300 10</p>
<p>#表示 60 秒内如果至少有 10000 个 key 的值变化，则保存 save 60 10000</p>
<p>不需要持久化，那么你可以注释掉所有的 save 行来停用保存功能。</p>
<p>②stop-writes-on-bgsave-error ：默认值为 yes。当启用了 RDB 且最后一次后台保存数据失败，Redis 是否停止接收数据。这会让用户意识到数据没有正确持久化到磁盘上，否则没有人会注意到灾难（disaster）发生了。如果 Redis 重启了，那么又可以重新开始接收数据了</p>
<p>③rdbcompression ；默认值是 yes。对于存储到磁盘中的快照，可以设置是否进行压缩存储。</p>
<p>④rdbchecksum ：默认值是 yes。在存储快照后，我们还可以让 redis 使用 CRC64 算法来进行数据校验，但是这样做会增加大约 10% 的性能消耗，如果希望获取到最大的性能提升，可以关闭此功能。</p>
<p>⑤dbfilename ：设置快照的文件名，默认是 dump.rdb</p>
<p>⑥dir：设置快照文件的存放路径，这个配置项一定是个目录，而不能是文件名。</p>
<p>我们可以修改这些配置来实现我们想要的效果。因为第三种方式是配置的，所以我们对前两种进行一个对比：</p>
<p><img src="https://pics5.baidu.com/feed/1c950a7b02087bf43b4490d50ac25f2a11dfcf7e.jpeg?token=22f387ba78130c6115420059481b2393&s=EF48A15796784D8816E1D9EB03007024" alt></p>
<p>4、RDB 的优势和劣势</p>
<p>①、优势</p>
<p>（1）RDB 文件紧凑，全量备份，非常适合用于进行备份和灾难恢复。</p>
<p>（2）生成 RDB 文件的时候，redis 主进程会 fork() 一个子进程来处理所有保存工作，主进程不需要进行任何磁盘 IO 操作。</p>
<p>（3）RDB 在恢复大数据集时的速度比 AOF 的恢复速度要快。</p>
<p>②、劣势</p>
<p>RDB 快照是一次全量备份，存储的是内存数据的二进制序列化形式，存储上非常紧凑。当进行快照持久化时，会开启一个子进程专门负责快照持久化，子进程会拥有父进程的内存数据，父进程修改内存子进程不会反应出来，所以在快照持久化期间修改的数据不会被保存，可能丢失数据。</p>
<p>三、AOF 机制</p>
<p>全量备份总是耗时的，有时候我们提供一种更加高效的方式 AOF，工作机制很简单，redis 会将每一个收到的写命令都通过 write 函数追加到文件中。通俗的理解就是日志记录。</p>
<p>配置:</p>
<p>表示是否开启AOF持久化：</p>
<p>　　appendonly yes(默认no,关闭)</p>
<p>AOF持久化配置文件的名称：</p>
<p>　　appendfilename “appendonly.aof”</p>
<p>AOF持久化策略(默认每秒)：</p>
<p>　　appendfsync always (同步持久化，每次发生数据变更会被立即记录到磁盘，性能差但数据完整性比较好)</p>
<p>　　appendfsync everysec (异步操作，每秒记录，如果一秒钟内宕机，有数据丢失)</p>
<p>　　appendfsync no （将缓存回写的策略交给系统，linux 默认是30秒将缓冲区的数据回写硬盘的）</p>
<p>AOF的Rewrite(重写) ：</p>
<p>　　定义：AOF采用文件追加的方式持久化数据，所以文件会越来越大，为了避免这种情况发生，增加了重写机制.</p>
<p>　　当AOF文件的大小超过了配置所设置的阙值时，Redis就会启动AOF文件压缩，只保留可以恢复数据的最小指令集，可以使用命令bgrewriteaof</p>
<p>触发机制：Redis会记录上次重写时的AOF文件大小，默认配置时当AOF文件大小是上次rewrite后大小的一倍且文件大于64M时触发</p>
<p>　　　　　auto-aof-rewrite-percentage 100 （一倍）</p>
<p>　　　　 auto-aof-rewrite-min-size 64mb</p>
<p>1、持久化原理</p>
<p>他的原理看下面这张图：</p>
<p><img src="https://pics3.baidu.com/feed/32fa828ba61ea8d3c2502e396b1b3848251f58b0.jpeg?token=394597ccd73bd15778c518b5c5be6998&s=2D62E7169D305F8A847546E20200B036" alt></p>
<p>每当有一个写命令过来时，就直接保存在我们的 AOF 文件中。</p>
<p>2、文件重写原理</p>
<p>AOF 的方式也同时带来了另一个问题。持久化文件会变的越来越大。为了压缩 aof 的持久化文件。redis 提供了 bgrewriteaof 命令。将内存中的数据以命令的方式保存到临时文件中，同时会 fork 出一条新进程来将文件重写。</p>
<p><img src="https://pics7.baidu.com/feed/09fa513d269759ee28454d2c4cea4b106c22dfd3.jpeg?token=86eda46b8bcd54a7a0e7d8a37d87bee8&s=EDB2A4579D317B824660D4DF0200E036" alt></p>
<p>重写 aof 文件的操作，并没有读取旧的 aof 文件，而是将整个内存中的数据库内容用命令的方式重写了一个新的 aof 文件，这点和快照有点类似。</p>
<p>3、AOF 也有三种触发机制</p>
<p>（1）每修改同步 always：同步持久化 每次发生数据变更会被立即记录到磁盘 性能较差但数据完整性比较好</p>
<p>（2）每秒同步 everysec：异步操作，每秒记录 如果一秒内宕机，有数据丢失</p>
<p>（3）不同 no：从不同步</p>
<p><img src="https://pics5.baidu.com/feed/b17eca8065380cd7df69859ba056a5325982816c.jpeg?token=a060f459d81c409c3d6c7208d2118888&s=AF4AA5574ED85CC841D04BE60300A036" alt></p>
<p>4、优点</p>
<p>（1）AOF 可以更好的保护数据不丢失，一般 AOF 会每隔 1 秒，通过一个后台线程执行一次 fsync 操作，最多丢失 1 秒钟的数据。（2）AOF 日志文件没有任何磁盘寻址的开销，写入性能非常高，文件不容易破损。</p>
<p>（3）AOF 日志文件即使过大的时候，出现后台重写操作，也不会影响客户端的读写。</p>
<p>（4）AOF 日志文件的命令通过非常可读的方式进行记录，这个特性非常适合做灾难性的误删除的紧急恢复。比如某人不小心用 flushall 命令清空了所有数据，只要这个时候后台 rewrite 还没有发生，那么就可以立即拷贝 AOF 文件，将最后一条 flushall 命令给删了，然后再将该 AOF 文件放回去，就可以通过恢复机制，自动恢复所有数据</p>
<p>5、缺点</p>
<p>（1）对于同一份数据来说，AOF 日志文件通常比 RDB 数据快照文件更大</p>
<p>（2）AOF 开启后，支持的写 QPS 会比 RDB 支持的写 QPS 低，因为 AOF 一般会配置成每秒 fsync 一次日志文件，当然，每秒一次 fsync，性能也还是很高的</p>
<p>（3）以前 AOF 发生过 bug，就是通过 AOF 记录的日志，进行数据恢复的时候，没有恢复一模一样的数据出来。</p>
<p>四、RDB 和 AOF 到底该如何选择</p>
<p>选择的话，两者加一起才更好。因为两个持久化机制你明白了，剩下的就是看自己的需求了，需求不同选择的也不一定，但是通常都是结合使用。有一张图可供总结：</p>
<p><img src="https://pics5.baidu.com/feed/8326cffc1e178a82c532308ef2117b8ba977e8ae.jpeg?token=fea28817e45f0e091b5be3854d856fbb&s=BD48B55F1C784C095E61DCEB0300D036" alt></p>
<p>对比了这几个特性，剩下的就是看自己了。</p>
<h2 id="主从复制"><a href="#主从复制" class="headerlink" title="主从复制"></a>主从复制</h2><h3 id="什么是主从复制"><a href="#什么是主从复制" class="headerlink" title="什么是主从复制"></a>什么是主从复制</h3><p>主从复制，是指将一台 Redis 服务器的数据，复制到其他的 Redis 服务器。前者称为主节点 (master)，后者称为从节点 (slave), 数据的复制是单向的，只能由主节点到从节点。</p>
<p>默认情况下，每台 Redis 服务器都是主节点；且一个主节点可以有多个从节点 (或没有从节点)，但一个从节点只能有一个主节点。</p>
<h3 id="主从复制的作用"><a href="#主从复制的作用" class="headerlink" title="主从复制的作用"></a>主从复制的作用</h3><ol>
<li>数据冗余：主从复制实现了数据的热备份，是持久化之外的一种数据冗余方式。</li>
<li>故障恢复：当主节点出现问题时，可以由从节点提供服务，实现快速的故障恢复；实际上是一种服务的冗余。</li>
<li>负载均衡：在主从复制的基础上，配合读写分离，可以由主节点提供写服务，由从节点提供读服务（即写 Redis 数据时应用连接主节点，读 Redis 数据时应用连接从节点），分担服务器负载；尤其是在写少读多的场景下，通过多个从节点分担读负载，可以大大提高 Redis 服务器的并发量。</li>
<li>读写分离：可以用于实现读写分离，主库写、从库读，读写分离不仅可以提高服务器的负载能力，同时可根据需求的变化，改变从库的数量；</li>
<li>高可用基石：除了上述作用以外，主从复制还是哨兵和集群能够实施的基础，因此说主从复制是 Redis 高可用的基础。</li>
</ol>
<h2 id="主从复制启用"><a href="#主从复制启用" class="headerlink" title="主从复制启用"></a>主从复制启用</h2><p>从节点开启主从复制，有 3 种方式：</p>
<ol>
<li>配置文件： 在从服务器的配置文件中加入：slaveof &lt;masterip&gt; &lt;masterport&gt;</li>
<li>启动命令： redis-server 启动命令后加入 –slaveof &lt;masterip&gt; &lt;masterport&gt;</li>
<li>客户端命令： Redis 服务器启动后，直接通过客户端执行命令：slaveof &lt;masterip&gt;<br>&lt;masterport&gt;，则该 Redis 实例成为从节点。</li>
</ol>
<blockquote>
<p>通过  info replication 命令可以看到复制的一些信息</p>
</blockquote>
<h2 id="主从复制原理"><a href="#主从复制原理" class="headerlink" title="主从复制原理"></a>主从复制原理</h2><p>主从复制过程大体可以分为 3 个阶段：连接建立阶段（即准备阶段）、数据同步阶段、命令传播阶段。</p>
<p>在从节点执行 slaveof 命令后，复制过程便开始运作，下面图示大概可以看到，<br>从图中可以看出复制过程大致分为 6 个过程<br><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717160722.png" alt="20200717160722"></p>
<blockquote>
<p>主从配置之后的日志记录也可以看出这个流程</p>
</blockquote>
<p>1）保存主节点（master）信息。<br>执行 slaveof 后 Redis 会打印如下日志：</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717160849.png" alt="20200717160849"></p>
<p>2）从节点（slave）内部通过每秒运行的定时任务维护复制相关逻辑，当定时任务发现存在新的主节点后，会尝试与该节点建立网络连接<br><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717160910.png" alt="20200717160910"><br>从节点与主节点建立网络连接</p>
<p>从节点会建立一个 socket 套接字，从节点建立了一个端口为 51234 的套接字，专门用于接受主节点发送的复制命令。从节点连接成功后打印如下日志：</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717161129.png" alt="20200717161129"></p>
<p>如果从节点无法建立连接，定时任务会无限重试直到连接成功或者执行 slaveof no one 取消复制</p>
<p>关于连接失败，可以在从节点执行 info replication 查看 master_link_down_since_seconds 指标，它会记录与主节点连接失败的系统时间。从节点连接主节点失败时也会每秒打印如下日志，方便发现问题：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Error condition on socket for SYNC: &#123;socket_error_reason&#125;</span><br></pre></td></tr></table></figure>

<p>3）发送 ping 命令。<br>连接建立成功后从节点发送 ping 请求进行首次通信，ping 请求主要目的如下：<br>· 检测主从之间网络套接字是否可用。<br>· 检测主节点当前是否可接受处理命令。<br>如果发送 ping 命令后，从节点没有收到主节点的 pong 回复或者超时，比如网络超时或者主节点正在阻塞无法响应命令，从节点会断开复制连接，下次定时任务会发起重连。<br><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717161041.png" alt="20200717161041"><br><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717161113.png" alt="20200717161113"></p>
<p>从节点发送的 ping 命令成功返回，Redis 打印如下日志，并继续后续复制流程：<br><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717161129%20(1).png" alt="20200717161129 (1)"></p>
<p>4）权限验证。如果主节点设置了 requirepass 参数，则需要密码验证，从节点必须配置 masterauth 参数保证与主节点相同的密码才能通过验证；如果验证失败复制将终止，从节点重新发起复制流程。</p>
<p>5）同步数据集。主从复制连接正常通信后，对于首次建立复制的场景，主节点会把持有的数据全部发送给从节点，这部分操作是耗时最长的步骤。</p>
<p>6）命令持续复制。当主节点把当前的数据同步给从节点后，便完成了复制的建立流程。接下来主节点会持续地把写命令发送给从节点，保证主从数据一致性。</p>
<h2 id="主从复制的细节"><a href="#主从复制的细节" class="headerlink" title="主从复制的细节"></a>主从复制的细节</h2><p>参考这个连接:<a href="https://www.cnblogs.com/daofaziran/p/10978628.html" target="_blank" rel="noopener">https://www.cnblogs.com/daofaziran/p/10978628.html</a></p>
<p>全量同步<br>Redis 全量复制一般发生在 Slave 初始化阶段，这时 Slave 需要将 Master 上的所有数据都复制一份。具体步骤如下： </p>
<ul>
<li>从服务器连接主服务器，发送 SYNC 命令； </li>
<li>主服务器接收到 SYNC 命名后，开始执行 BGSAVE 命令生成 RDB 文件并使用缓冲区记录此后执行的所有写命令； </li>
<li>主服务器 BGSAVE 执行完后，向所有从服务器发送快照文件，并在发送期间继续记录被执行的写命令； </li>
<li>从服务器收到快照文件后丢弃所有旧数据，载入收到的快照； </li>
<li>主服务器快照发送完毕后开始向从服务器发送缓冲区中的写命令； </li>
<li>从服务器完成对快照的载入，开始接收命令请求，并执行来自主服务器缓冲区的写命令；</li>
</ul>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717153700.png" alt="20200717153700"></p>
<p>完成上面几个步骤后就完成了从服务器数据初始化的所有操作，从服务器此时可以接收来自用户的读请求。</p>
<p><strong>增量同步</strong></p>
<p>Redis 增量复制是指 Slave 初始化后开始正常工作时主服务器发生的写操作同步到从服务器的过程。<br>增量复制的过程主要是主服务器每执行一个写命令就会向从服务器发送相同的写命令，从服务器接收并执行收到的写命令。</p>
<p><strong>Redis 主从同步策略</strong></p>
<p>主从刚刚连接的时候，进行全量同步；全同步结束后，进行增量同步。当然，如果有需要，slave 在任何时候都可以发起全量同步。redis 策略是，无论如何，首先会尝试进行增量同步，如不成功，要求从机进行全量同步。</p>
<p>注意点<br>如果多个 Slave 断线了，需要重启的时候，因为只要 Slave 启动，就会发送 sync 请求和主机全量同步，当多个同时出现的时候，可能会导致 Master IO 剧增宕机。</p>
<h2 id="额外的注意点"><a href="#额外的注意点" class="headerlink" title="额外的注意点:"></a>额外的注意点:</h2><ul>
<li><p>一个主机的从服务器后面又跟了一个从服务器,这样一个链条,当主服务器宕机后,可以使用slaveof no one使中间那台从服务器变为主服务器继续工作.</p>
</li>
<li><p>哨兵模式是上面那种情况的自动版,它会监听主机的工作情况.具体如下文所述:</p>
</li>
</ul>
<h2 id="一、哨兵模式概述"><a href="#一、哨兵模式概述" class="headerlink" title="一、哨兵模式概述"></a>一、哨兵模式概述</h2><p>哨兵模式是一种特殊的模式，首先 Redis 提供了哨兵的命令，哨兵是一个独立的进程，作为进程，它会独立运行。其原理是哨兵通过发送命令，等待 Redis 服务器响应，从而监控运行的多个 Redis 实例。</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717160225.png" alt="20200717160225"></p>
<p>这里的哨兵有两个作用</p>
<p>通过发送命令，让 Redis 服务器返回监控其运行状态，包括主服务器和<strong>从服务器</strong>。</p>
<p>当哨兵监测到 master 宕机，会自动将 slave 切换成 master，然后通过发布订阅模式通知其他的从服务器，修改配置文件，让它们切换主机。</p>
<p>然而一个哨兵进程对 Redis 服务器进行监控，可能会出现问题，为此，我们可以使用多个哨兵进行监控。各个哨兵之间还会进行监控，这样就形成了多哨兵模式。</p>
<p>用文字描述一下故障切换（failover）的过程。假设主服务器宕机，哨兵 1 先检测到这个结果，系统并不会马上进行 failover 过程，仅仅是哨兵 1 主观的认为主服务器不可用，这个现象成为主观下线。当后面的哨兵也检测到主服务器不可用，并且数量达到一定值时，那么哨兵之间就会进行一次投票，投票的结果由一个哨兵发起，进行 failover 操作。切换成功后，就会通过发布订阅模式，让各个哨兵把自己监控的从服务器实现切换主机，这个过程称为客观下线。这样对于客户端而言，一切都是透明的。</p>
<h2 id="二、Redis-配置哨兵模式"><a href="#二、Redis-配置哨兵模式" class="headerlink" title="二、Redis 配置哨兵模式"></a>二、Redis 配置哨兵模式</h2><p>配置 3 个哨兵和 1 主 2 从的 Redis 服务器来演示这个过程。</p>
<table>
<thead>
<tr>
<th>服务类型</th>
<th>是否是主服务器</th>
<th>IP 地址</th>
<th>端口</th>
</tr>
</thead>
<tbody><tr>
<td>Redis</td>
<td>是</td>
<td>192.168.11.128</td>
<td>6379</td>
</tr>
<tr>
<td>Redis</td>
<td>否</td>
<td>192.168.11.129</td>
<td>6379</td>
</tr>
<tr>
<td>Redis</td>
<td>否</td>
<td>192.168.11.130</td>
<td>6379</td>
</tr>
<tr>
<td>Sentinel</td>
<td>-</td>
<td>192.168.11.128</td>
<td>26379</td>
</tr>
<tr>
<td>Sentinel</td>
<td>-</td>
<td>192.168.11.129</td>
<td>26379</td>
</tr>
<tr>
<td>Sentinel</td>
<td>-</td>
<td>192.168.11.130</td>
<td>26379</td>
</tr>
</tbody></table>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717161324.png" alt="20200717161324"></p>
<p>多哨兵监控 Redis</p>
<p>首先配置 Redis 的主从服务器，修改 redis.conf 文件如下</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 使得Redis服务器可以跨网络访问</span><br><span class="line">bind 0.0.0.0</span><br><span class="line"># 设置密码</span><br><span class="line">requirepass &quot;123456&quot;</span><br><span class="line"># 指定主服务器，注意：有关slaveof的配置只是配置从服务器，主服务器不需要配置</span><br><span class="line">slaveof 192.168.11.128 6379</span><br><span class="line"># 主服务器密码，注意：有关slaveof的配置只是配置从服务器，主服务器不需要配置</span><br><span class="line">masterauth 123456</span><br></pre></td></tr></table></figure>

<p>上述内容主要是配置 Redis 服务器，从服务器比主服务器多一个 slaveof 的配置和密码。</p>
<p>配置 3 个哨兵，每个哨兵的配置都是一样的。在 Redis 安装目录下有一个 sentinel.conf 文件，copy 一份进行修改</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 禁止保护模式</span><br><span class="line">protected-mode no</span><br><span class="line"># 配置监听的主服务器，这里sentinel monitor代表监控，mymaster代表服务器的名称，可以自定义，192.168.11.128代表监控的主服务器，6379代表端口，2代表只有两个或两个以上的哨兵认为主服务器不可用的时候，才会进行failover操作。</span><br><span class="line">sentinel monitor mymaster 192.168.11.128 6379 2</span><br><span class="line"># sentinel author-pass定义服务的密码，mymaster是服务名称，123456是Redis服务器密码</span><br><span class="line"># sentinel auth-pass &lt;master-name&gt; &lt;password&gt;</span><br><span class="line">sentinel auth-pass mymaster 123456</span><br></pre></td></tr></table></figure>

<p>上述关闭了保护模式，便于测试。</p>
<p>有了上述的修改，我们可以进入 Redis 的安装目录的 src 目录，通过下面的命令启动服务器和哨兵</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 启动Redis服务器进程</span><br><span class="line">.&#x2F;redis-server ..&#x2F;redis.conf</span><br><span class="line"># 启动哨兵进程</span><br><span class="line">.&#x2F;redis-sentinel ..&#x2F;sentinel.conf</span><br></pre></td></tr></table></figure>

<p>注意启动的顺序。<strong>首先是主机（192.168.11.128）的 Redis 服务进程，然后启动从机的服务进程，最后启动 3 个哨兵的服务进程。</strong></p>
<p>以上关于主从复制和哨兵模式都是redis关于高可用,高并发的策略,但是都不是最终策略.Redis的终极策略是搭建Redis集群.</p>
<h2 id="Redis集群"><a href="#Redis集群" class="headerlink" title="Redis集群"></a>Redis集群</h2><p><strong>一、redis-cluster 设计</strong><br>Redis-Cluster 采用无中心结构，每个节点保存数据和整个集群状态, 每个节点都和其他所有节点连接。  </p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/20200717163629.png" alt="20200717163629"></p>
<p>其结构特点：<br>1、所有的 redis 节点彼此互联 (PING-PONG 机制), 内部使用二进制协议优化传输速度和带宽。<br>2、节点的 fail 是通过集群中超过半数的节点检测失效时才生效。<br>3、客户端与 redis 节点直连, 不需要中间 proxy 层. 客户端不需要连接集群所有节点, 连接集群中任何一个可用节点即可。<br>4、redis-cluster 把所有的物理节点映射到 [0-16383]slot 上（不一定是平均分配）,cluster 负责维护 node&lt;-&gt;slot&lt;-&gt;value。<br>5、Redis 集群预分好 16384 个桶，当需要在 Redis 集群中放置一个 key-value 时，根据 CRC16(key) mod 16384 的值，决定将一个 key 放到哪个桶中。</p>
<p><strong>a.redis cluster 节点分配</strong><br>现在我们是三个主节点分别是：A, B, C 三个节点，它们可以是一台机器上的三个端口，也可以是三台不同的服务器。那么，采用哈希槽 (hash slot) 的方式来分配 16384 个 slot 的话，它们三个节点分别承担的 slot 区间是：</p>
<ul>
<li><p>节点 A 覆盖 0－5460;</p>
</li>
<li><p>节点 B 覆盖 5461－10922;</p>
</li>
<li><p>节点 C 覆盖 10923－16383.</p>
<p>获取数据:<br>如果存入一个值，按照 redis cluster 哈希槽的<a href="http://lib.csdn.net/base/datastructure" target="_blank" rel="noopener">算法</a>： CRC16(‘key’)384 = 6782。 那么就会把这个 key 的存储分配到 B 上了。同样，当我连接 (A,B,C) 任何一个节点想获取’key’这个 key 时，也会这样的算法，然后内部跳转到 B 节点上获取数据</p>
<p>新增一个主节点:<br>新增一个节点 D，redis cluster 的这种做法是从各个节点的前面各拿取一部分 slot 到 D 上，我会在接下来的实践中实验。大致就会变成这样：</p>
</li>
<li><p>节点 A 覆盖 1365-5460</p>
</li>
<li><p>节点 B 覆盖 6827-10922</p>
</li>
<li><p>节点 C 覆盖 12288-16383</p>
</li>
<li><p>节点 D 覆盖 0-1364,5461-6826,10923-12287</p>
</li>
</ul>
<p>同样删除一个节点也是类似，移动完成后就可以删除这个节点了。</p>
<p><strong>b.Redis Cluster 主从模式</strong><br>redis cluster 为了保证数据的高可用性，加入了主从模式，一个主节点对应一个或多个从节点，主节点提供数据存取，从节点则是从主节点拉取数据备份，当这个主节点挂掉后，就会有这个从节点选取一个来充当主节点，从而保证集群不会挂掉</p>
<p>上面那个例子里, 集群有 ABC 三个主节点, 如果这 3 个节点都没有加入从节点，如果 B 挂掉了，我们就无法访问整个集群了。A 和 C 的 slot 也无法访问。</p>
<p>所以我们在集群建立的时候，一定要为每个主节点都添加了从节点, 比如像这样, 集群包含主节点 A、B、C, 以及从节点 A1、B1、C1, 那么即使 B 挂掉系统也可以继续正确工作。</p>
<p>B1 节点替代了 B 节点，所以 Redis 集群将会选择 B1 节点作为新的主节点，集群将会继续正确地提供服务。 当 B 重新开启后，它就会变成 B1 的从节点。</p>
<p>不过需要注意，如果节点 B 和 B1 同时挂了，Redis 集群就无法继续正确地提供服务了。</p>
<p><strong>二、redis 集群的搭建</strong><br>集群中至少应该有奇数个节点，所以至少有三个节点，每个节点至少有一个备份节点，所以下面使用 6 节点（主节点、备份节点由 redis-cluster 集群确定）<br>下载 <a href="https://redis.io/download" target="_blank" rel="noopener">redis</a><br><strong>1、安装 redis 节点指定端口</strong><br>解压 redis 压缩包，编译安装</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[root@localhost redis-3.2.0]# tar xzf redis-3.2.0.tar.gz</span><br><span class="line">[root@localhost redis-3.2.0]# cd redis-3.2.0</span><br><span class="line">[root@localhost redis-3.2.0]# make</span><br><span class="line">[root@localhost redis01]# make install PREFIX&#x3D;&#x2F;usr&#x2F;andy&#x2F;redis-cluster</span><br></pre></td></tr></table></figure>

<p>在 redis-cluster 下 修改 bin 文件夹为 redis01, 复制 redis.conf 配置文件<br>创建目录 redis-cluster 并在此目录下再创建 7000 7001 7002 7003 7004 7005 共 6 个目录，在 7000 中创建配置文件 redis.conf，内容如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">daemonize yes #后台启动</span><br><span class="line">port 7001 #修改端口号，从7001到7006</span><br><span class="line">cluster-enabled yes #开启cluster，去掉注释</span><br><span class="line">cluster-config-file nodes.conf #自动生成</span><br><span class="line">cluster-node-timeout 15000 #节点通信时间</span><br><span class="line">appendonly yes #持久化方式</span><br></pre></td></tr></table></figure>

<p>同时把 redis.conf 复制到其它目录中</p>
<p><strong>2、安装 redis-trib 所需的 ruby 脚本</strong><br>注意：centos7 默认的 ruby 版本太低 (2.0)，要卸载重装 (最低 2.2)</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum remove ruby</span><br><span class="line">yum install ruby</span><br><span class="line">yum install rubygems</span><br></pre></td></tr></table></figure>

<p>复制 redis 解压文件 src 下的 redis-trib.rb 文件到 redis-cluster 目录并安装 gem</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">gem install redis-3.x.x.gem</span><br></pre></td></tr></table></figure>

<p>若不想安装 src 目录下的 gem，也可以直接<code>gem install redis</code>。</p>
<p>注意，gem install 可能会报错<br>Unable to require openssl,install OpenSSL and rebuild ruby (preferred) or use ….<br>解决步骤：</p>
<ol>
<li>yum install openssl-devel -y</li>
<li>在 ruby 安装包 / root/ruby-x.x.x/ext/openssl，执行 ruby ./extconf.rb</li>
<li>执行 make, 若出现 make: *** No rule to make target <code>/include/ruby.h&#39;, needed by</code>ossl.o’. Stop.; 在 Makefile 顶部中的增加<code>top_srcdir = ../..</code></li>
<li>执行 make install</li>
</ol>
<p><strong>3、启动所有的 redis 节点</strong><br>可以写一个命令脚本 start-all.sh</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cd 7000</span><br><span class="line">redis-server redis.conf</span><br><span class="line">cd ..</span><br><span class="line">cd 7001</span><br><span class="line">redis-server redis.conf</span><br><span class="line">cd ..</span><br><span class="line">cd 7002</span><br><span class="line">redis-server redis.conf</span><br><span class="line">cd ..</span><br><span class="line">cd 7003</span><br><span class="line">redis-server redis.conf</span><br><span class="line">cd ..</span><br><span class="line">cd 7004</span><br><span class="line">redis-server redis.conf</span><br><span class="line">cd ..</span><br><span class="line">cd 7005</span><br><span class="line">redis-server redis.conf</span><br><span class="line">cd ..</span><br></pre></td></tr></table></figure>

<p>设置权限启动</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[root@localhost redis-cluster]# chmod 777 start-all.sh </span><br><span class="line">[root@localhost redis-cluster]# .&#x2F;start-all.sh</span><br></pre></td></tr></table></figure>

<p>查看 redis 进程启动状态</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[root@localhost redis-4.0.2]# ps -ef|grep cluster</span><br><span class="line">root      54956      1  0 19:17 ?        00:00:00 redis-server *:7000 [cluster]</span><br><span class="line">root      54961      1  0 19:17 ?        00:00:00 redis-server *:7001 [cluster]</span><br><span class="line">root      54966      1  0 19:17 ?        00:00:00 redis-server *:7002 [cluster]</span><br><span class="line">root      54971      1  0 19:17 ?        00:00:00 redis-server *:7003 [cluster]</span><br><span class="line">root      54976      1  0 19:17 ?        00:00:00 redis-server *:7004 [cluster]</span><br><span class="line">root      54981      1  0 19:17 ?        00:00:00 redis-server *:7005 [cluster]</span><br><span class="line">root      55071  24089  0 19:24 pts&#x2F;0    00:00:00 grep --color&#x3D;auto cluster</span><br></pre></td></tr></table></figure>

<p>可以看到 redis 的 6 个节点已经启动成功<br><strong>注意：这里并没有创建集群</strong></p>
<p><strong>4、使用 redis-trib.rb 创建集群</strong><br>注意：redis-trib.rb 在 redis/src 目录下。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.&#x2F;redis-trib.rb create --replicas 1 127.0.0.1:7001 127.0.0.1:7002 127.0.0.1:7003 127.0.0.1:7004 127.0.0.1:7005 127.0.0.1:7000</span><br></pre></td></tr></table></figure>

<p>使用 create 命令 –replicas 1 参数表示为每个主节点创建一个从节点，其他参数是实例的地址集合。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[root@localhost redis]# .&#x2F;src&#x2F;redis-trib.rb create --replicas 1 127.0.0.1:7001 127.0.0.1:7002 127.0.0.1:7003 127.0.0.1:7004 127.0.0.1:7005 127.0.0.1:7000</span><br><span class="line">&gt;&gt;&gt; Creating cluster</span><br><span class="line">&gt;&gt;&gt; Performing hash slots allocation on 6 nodes...</span><br><span class="line">Using 3 masters:</span><br><span class="line">127.0.0.1:7001</span><br><span class="line">127.0.0.1:7002</span><br><span class="line">127.0.0.1:7003</span><br><span class="line">Adding replica 127.0.0.1:7004 to 127.0.0.1:7001</span><br><span class="line">Adding replica 127.0.0.1:7005 to 127.0.0.1:7002</span><br><span class="line">Adding replica 127.0.0.1:7000 to 127.0.0.1:7003</span><br><span class="line">M: f4ee0a501f9aaf11351787a46ffb4659d45b7bd7 127.0.0.1:7001</span><br><span class="line">   slots:0-5460 (5461 slots) master</span><br><span class="line">M: 671a0524a616da8b2f50f3d11a74aaf563578e41 127.0.0.1:7002</span><br><span class="line">   slots:5461-10922 (5462 slots) master</span><br><span class="line">M: 18948dab5b07e3726afd1b6a42d5bf6e2f411ba1 127.0.0.1:7003</span><br><span class="line">   slots:10923-16383 (5461 slots) master</span><br><span class="line">S: 34e322ca50a2842e9f3664442cb11c897defba06 127.0.0.1:7004</span><br><span class="line">   replicates f4ee0a501f9aaf11351787a46ffb4659d45b7bd7</span><br><span class="line">S: 62a00566233fbff4467c4031345b1db13cf12b46 127.0.0.1:7005</span><br><span class="line">   replicates 671a0524a616da8b2f50f3d11a74aaf563578e41</span><br><span class="line">S: 2cb649ad3584370c960e2036fb01db834a546114 127.0.0.1:7000</span><br><span class="line">   replicates 18948dab5b07e3726afd1b6a42d5bf6e2f411ba1</span><br><span class="line">Can I set the above configuration? (type &#39;yes&#39; to accept): yes</span><br><span class="line">&gt;&gt;&gt; Nodes configuration updated</span><br><span class="line">&gt;&gt;&gt; Assign a different config epoch to each node</span><br><span class="line">&gt;&gt;&gt; Sending CLUSTER MEET messages to join the cluster</span><br><span class="line">Waiting for the cluster to join...</span><br><span class="line">&gt;&gt;&gt; Performing Cluster Check (using node 127.0.0.1:7001)</span><br><span class="line">M: f4ee0a501f9aaf11351787a46ffb4659d45b7bd7 127.0.0.1:7001</span><br><span class="line">   slots:0-5460 (5461 slots) master</span><br><span class="line">   1 additional replica(s)</span><br><span class="line">M: 671a0524a616da8b2f50f3d11a74aaf563578e41 127.0.0.1:7002</span><br><span class="line">   slots:5461-10922 (5462 slots) master</span><br><span class="line">   1 additional replica(s)</span><br><span class="line">S: 2cb649ad3584370c960e2036fb01db834a546114 127.0.0.1:7000</span><br><span class="line">   slots: (0 slots) slave</span><br><span class="line">   replicates 18948dab5b07e3726afd1b6a42d5bf6e2f411ba1</span><br><span class="line">S: 34e322ca50a2842e9f3664442cb11c897defba06 127.0.0.1:7004</span><br><span class="line">   slots: (0 slots) slave</span><br><span class="line">   replicates f4ee0a501f9aaf11351787a46ffb4659d45b7bd7</span><br><span class="line">M: 18948dab5b07e3726afd1b6a42d5bf6e2f411ba1 127.0.0.1:7003</span><br><span class="line">   slots:10923-16383 (5461 slots) master</span><br><span class="line">   1 additional replica(s)</span><br><span class="line">S: 62a00566233fbff4467c4031345b1db13cf12b46 127.0.0.1:7005</span><br><span class="line">   slots: (0 slots) slave</span><br><span class="line">   replicates 671a0524a616da8b2f50f3d11a74aaf563578e41</span><br><span class="line">[OK] All nodes agree about slots configuration.</span><br><span class="line">&gt;&gt;&gt; Check for open slots...</span><br><span class="line">&gt;&gt;&gt; Check slots coverage...</span><br><span class="line">[OK] All 16384 slots covered.</span><br></pre></td></tr></table></figure>

<p>上面显示创建成功，有 3 个主节点，3 个从节点，每个节点都是成功连接状态。</p>
<p><strong>三、redis 集群的测试</strong><br>测试存取值，客户端连接集群 redis-cli <strong>需要带上 -c</strong> ，redis-cli -c -p 端口号</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[root@localhost redis]# .&#x2F;redis-cli -c -p 7001</span><br><span class="line">127.0.0.1:7001&gt; set name andy</span><br><span class="line">-&gt; Redirected to slot [5798] located at 127.0.0.1:7002</span><br><span class="line">OK</span><br><span class="line">127.0.0.1:7002&gt; get name</span><br><span class="line">&quot;andy&quot;</span><br><span class="line">127.0.0.1:7002&gt;</span><br></pre></td></tr></table></figure>

<p>根据 redis-cluster 的 key 值分配，name 应该分配到节点 7002[5461-10922] 上，上面显示 redis cluster 自动从 7001 跳转到了 7002 节点。</p>
<p>测试一下 7000 从节点获取 name 值</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[root@localhost redis]# .&#x2F;redis-cli -c -p 7000</span><br><span class="line">127.0.0.1:7000&gt; get name</span><br><span class="line">-&gt; Redirected to slot [5798] located at 127.0.0.1:7002</span><br><span class="line">&quot;andy&quot;</span><br><span class="line">127.0.0.1:7002&gt;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>注意不要使用mset,mget这种一次管理多个值的方式,因为这几个值可能不在一起.在分开的服务器无法正确重定向.可以使用大括号将键值对加入一个组,放在一起.</p>
</blockquote>
<p><strong>四、集群节点选举</strong><br>现在模拟将 7002 节点挂掉，按照 redis-cluster 原理会选举会将 7002 的从节点 7005 选举为主节点。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[root@localhost redis-cluster]# ps -ef | grep redis</span><br><span class="line">root       7966      1  0 12:50 ?        00:00:29 .&#x2F;redis-server 127.0.0.1:7000 [cluster]</span><br><span class="line">root       7950      1  0 12:50 ?        00:00:28 .&#x2F;redis-server 127.0.0.1:7001 [cluster]</span><br><span class="line">root       7952      1  0 12:50 ?        00:00:29 .&#x2F;redis-server 127.0.0.1:7002 [cluster]</span><br><span class="line">root       7956      1  0 12:50 ?        00:00:29 .&#x2F;redis-server 127.0.0.1:7003 [cluster]</span><br><span class="line">root       7960      1  0 12:50 ?        00:00:29 .&#x2F;redis-server 127.0.0.1:7004 [cluster]</span><br><span class="line">root       7964      1  0 12:50 ?        00:00:29 .&#x2F;redis-server 127.0.0.1:7005 [cluster]</span><br><span class="line">root      11346  10581  0 14:57 pts&#x2F;2    00:00:00 grep --color&#x3D;auto redis</span><br><span class="line">[root@localhost redis-cluster]# kill 7952</span><br></pre></td></tr></table></figure>

<p>在查看集群中的 7002 节点</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[root@localhost src]# .&#x2F;redis-trib.rb check 127.0.0.1:7002</span><br><span class="line">&gt;&gt;&gt; Performing Cluster Check (using node 127.0.0.1:7002)</span><br><span class="line">S: 671a0524a616da8b2f50f3d11a74aaf563578e41 127.0.0.1:7002</span><br><span class="line">   slots: (0 slots) slave</span><br><span class="line">   replicates 62a00566233fbff4467c4031345b1db13cf12b46</span><br><span class="line">M: 18948dab5b07e3726afd1b6a42d5bf6e2f411ba1 127.0.0.1:7003</span><br><span class="line">   slots:10923-16383 (5461 slots) master</span><br><span class="line">   1 additional replica(s)</span><br><span class="line">M: 62a00566233fbff4467c4031345b1db13cf12b46 127.0.0.1:7005</span><br><span class="line">   slots:5461-10922 (5462 slots) master</span><br><span class="line">   1 additional replica(s)</span><br><span class="line">M: f4ee0a501f9aaf11351787a46ffb4659d45b7bd7 127.0.0.1:7001</span><br><span class="line">   slots:0-5460 (5461 slots) master</span><br><span class="line">   1 additional replica(s)</span><br><span class="line">S: 34e322ca50a2842e9f3664442cb11c897defba06 127.0.0.1:7004</span><br><span class="line">   slots: (0 slots) slave</span><br><span class="line">   replicates f4ee0a501f9aaf11351787a46ffb4659d45b7bd7</span><br><span class="line">S: 2cb649ad3584370c960e2036fb01db834a546114 127.0.0.1:7000</span><br><span class="line">   slots: (0 slots) slave</span><br><span class="line">   replicates 18948dab5b07e3726afd1b6a42d5bf6e2f411ba1</span><br><span class="line">[OK] All nodes agree about slots configuration.</span><br><span class="line">&gt;&gt;&gt; Check for open slots...</span><br><span class="line">&gt;&gt;&gt; Check slots coverage...</span><br><span class="line">[OK] All 16384 slots covered.</span><br></pre></td></tr></table></figure>

<p>可以看到集群连接不了 7002 节点，而 7005 有原来的 S 转换为 M 节点，代替了原来的 7002 节点。我们可以获取 name 值：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[root@localhost redis]# .&#x2F;redis-cli -c -p 7001</span><br><span class="line">127.0.0.1:7001&gt; get name</span><br><span class="line">-&gt; Redirected to slot [5798] located at 127.0.0.1:7005</span><br><span class="line">&quot;andy&quot;</span><br><span class="line">127.0.0.1:7005&gt; </span><br><span class="line">127.0.0.1:7005&gt;</span><br></pre></td></tr></table></figure>

<p>从 7001 节点连入，自动跳转到 7005 节点，并且获取 name 值。</p>
<p>现在我们将 7002 节点恢复，看是否会自动加入集群中以及充当的 M 还是 S 节点。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[root@localhost redis-cluster]# cd 7002</span><br><span class="line">[root@localhost 7002]# .&#x2F;redis-server redis.conf </span><br><span class="line">[root@localhost 7002]#</span><br></pre></td></tr></table></figure>

<p>再 check 一下 7002 节点，可以看到 7002 节点变成了 7005 的从节点。</p>
]]></content>
      <tags>
        <tag>redis</tag>
      </tags>
  </entry>
  <entry>
    <title>nio</title>
    <url>/2020/08/01/nio/</url>
    <content><![CDATA[<p>回过头补点基础.</p>
<a id="more"></a>

<h1 id="nio的不同"><a href="#nio的不同" class="headerlink" title="nio的不同"></a>nio的不同</h1><p>nio是面向缓冲区的.在io中,数据传输是”流”的模型,单向的,直接面向数据本身(通常意义的流,不是指那几个特别的).而在nio中,数据传输使用buffer,缓冲区.buffer类似于火车,而传输通道channel则是铁轨.这个通道是双向的,buffer在上面来回传输.</p>
<h1 id="buffer"><a href="#buffer" class="headerlink" title="buffer"></a>buffer</h1><p>buffer底层就是一个数组,用于存储不同类型的数据.具体来说,基本类型中,除boolean之外,都有对应的buffer.但是常用的就是bytebuffer,因为这个大小传输数据正合适.</p>
<p>分配一个指定大小的缓冲区:<code>ByteBuffer.allocate(1024)</code></p>
<p>buffer的几个关键属性:</p>
<ul>
<li>capacity:容量</li>
<li>limit:界限,缓冲区中可操作的范围,limit之后的数组内容不可操作.</li>
<li>position:位置</li>
</ul>
<p>当分配1024大小后,上面三个值为1024,1024,0.</p>
<p>接着使用put方法向数组放入五个字节数据,position变为5,其它不变.下一次写入从下标5开始.</p>
<p>如果要写数据,则要使用flip方法将position重置为0,即从0开始读取数据.limit变为5,表示从5开始的地方不可读取.</p>
<p>接着就可以使用get方法读取数据.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200801104708362.png" alt="image-20200801104708362"></p>
<p>get方法无参数,表示读取position位置的数据,然后position加一.</p>
<p>dst表示读取到哪个数组去.</p>
<p>读取方法会使得position相应增加.</p>
<p>使用rewind方法可以恢复position位置.可以从头开始读.</p>
<p>使用clear方法可以清空缓冲区,恢复最初状态.但是实际只是清空指针位置,数据本身还在里面.</p>
<p>还有另一个属性mark,mark方法用来标记position位置.当position发生变动后,可以使用reset方法使position回到mark标记的位置.</p>
<p>remaining方法返回缓冲区可操作数据的个数,就是limit减去position.</p>
<ul>
<li>allocate分配的非直接缓冲区建立在JVM内存中</li>
<li>allocateDirect分配的直接缓冲区建立在物理内存中.</li>
</ul>
<h1 id="通道"><a href="#通道" class="headerlink" title="通道"></a>通道</h1><p>通道channel只能与buffer交互.负责数据传输.</p>
<p>Channel接口有四个实现类:</p>
<ul>
<li>FileChannel</li>
<li>SocketChannel</li>
<li>ServerSocketChannel</li>
<li>DatagramChannel</li>
</ul>
<p>第一个是本地传输,第二第三个是tdp传输,第四个是udp传输.</p>
<p>获取通道:</p>
<p>1.针对支持通道的类提供了getChannel方法:</p>
<ul>
<li>本地IO<ul>
<li>FileInputStream/FileOutputStream</li>
<li>RandomAccessFile</li>
</ul>
</li>
<li>网络IO<ul>
<li>Socket</li>
<li>ServerSocket</li>
<li>DatagramSocket</li>
</ul>
</li>
</ul>
<p>2.NIO2中为各个通道提供了静态方法open</p>
<p>3.NIO2的工具类Files的newByteChannel方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    FileInputStream fis = <span class="keyword">new</span> FileInputStream(<span class="string">"F:\\迷路帖.jpg"</span>);</span><br><span class="line">    FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(<span class="string">"F:\\迷路帖1.jpg"</span>);</span><br><span class="line"></span><br><span class="line">    FileChannel inChannel = fis.getChannel();</span><br><span class="line">    FileChannel outChannel = fos.getChannel();</span><br><span class="line"></span><br><span class="line">    ByteBuffer buf = ByteBuffer.allocate(<span class="number">1024</span>);</span><br><span class="line">    <span class="comment">// 从通道读入缓冲区</span></span><br><span class="line">    <span class="keyword">while</span> (inChannel.read(buf) != -<span class="number">1</span>) &#123; <span class="comment">// 切换为读数据模式(将position置为0)</span></span><br><span class="line">        buf.flip();</span><br><span class="line">        <span class="comment">// 写入</span></span><br><span class="line">        outChannel.write(buf);</span><br><span class="line">        <span class="comment">// 清空缓冲区</span></span><br><span class="line">        buf.clear();</span><br><span class="line">    &#125;</span><br><span class="line">    outChannel.close();</span><br><span class="line">    inChannel.close();</span><br><span class="line">    fos.close();</span><br><span class="line">    fis.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面演示使用通道完成的文件复制,用的是非直接缓冲区.实际和原来的IO没多大区别.保留这种对流的操作方式纯粹是为了兼容.</p>
<p>现在用新的NIO2的open方式:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    <span class="comment">// 搭建通道</span></span><br><span class="line">    FileChannel inChannel = FileChannel.open(Paths.get(<span class="string">"F:\\迷路帖.jpg"</span>), StandardOpenOption.READ);</span><br><span class="line">    FileChannel outChannel = FileChannel.open(Paths.get(<span class="string">"F:\\迷路帖1.jpg"</span>), StandardOpenOption.WRITE,StandardOpenOption.READ, StandardOpenOption.CREATE_NEW);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 内存映射文件(在物理内存中的缓冲区,只有ByteBuffer支持这种写法),与allocateDirect方法效果一样,MappedByteBuffer是ByteBuffer的子类</span></span><br><span class="line">    MappedByteBuffer inMapBuf = inChannel.map(MapMode.READ_ONLY, <span class="number">0</span>, inChannel.size());</span><br><span class="line">    <span class="comment">// 第一个参数是设置缓冲区的读写模式,这里有只读或者可读可写模式,而为了写,连带着要可读,所以outChannel要加一个可读</span></span><br><span class="line">    MappedByteBuffer outMapBuf = outChannel.map(MapMode.READ_WRITE, <span class="number">0</span>, inChannel.size());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用一个数组从内存映射文件中取出数据再写入另一个内存映射文件</span></span><br><span class="line">    <span class="keyword">byte</span>[] dts = <span class="keyword">new</span> <span class="keyword">byte</span>[inMapBuf.limit()];</span><br><span class="line">    inMapBuf.get(dts);</span><br><span class="line">    outMapBuf.put(dts);</span><br><span class="line"></span><br><span class="line">    inChannel.close();</span><br><span class="line">    outChannel.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是更精简的写法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test3</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    <span class="comment">// 搭建通道</span></span><br><span class="line">    FileChannel inChannel = FileChannel.open(Paths.get(<span class="string">"F:\\迷路帖.jpg"</span>), StandardOpenOption.READ);</span><br><span class="line">    FileChannel outChannel = FileChannel.open(Paths.get(<span class="string">"F:\\迷路帖1.jpg"</span>), StandardOpenOption.WRITE, StandardOpenOption.READ, StandardOpenOption.CREATE_NEW);</span><br><span class="line">    <span class="comment">// 从输入通道直接传到输出通道</span></span><br><span class="line">    inChannel.transferTo(<span class="number">0</span>, inChannel.size(), outChannel);</span><br><span class="line">    inChannel.close();</span><br><span class="line">    outChannel.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种方法本质与上面一样,只不过是transfer方法内部建立了直接缓冲区.传输</p>
<p>方法还可以换个写法:<code>outChannel.transferFrom(inChannel, 0, inChannel.size());</code></p>
<p><strong>分散读取</strong>:将通道中的数据依次分散到多个缓冲区中</p>
<p><strong>聚集写入</strong>:将多个缓冲区的数据依次聚集到一个通道中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test4</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    RandomAccessFile raf = <span class="keyword">new</span> RandomAccessFile(<span class="string">"1.txt"</span>, <span class="string">"rw"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//获得通道</span></span><br><span class="line">    FileChannel fileChannel = raf.getChannel();</span><br><span class="line">    <span class="comment">//分配两个缓冲区</span></span><br><span class="line">    ByteBuffer buffer1 = ByteBuffer.allocate(<span class="number">100</span>);</span><br><span class="line">    ByteBuffer buffer2 = ByteBuffer.allocate(<span class="number">1000</span>);</span><br><span class="line"></span><br><span class="line">    ByteBuffer[] bufs = &#123;buffer1, buffer2&#125;;</span><br><span class="line">    fileChannel.read(bufs);<span class="comment">//分散读取,分别给每个缓冲区读取响应长度的数据</span></span><br><span class="line"></span><br><span class="line">    Arrays.stream(bufs).forEach(ByteBuffer::flip);</span><br><span class="line">    System.out.println(<span class="keyword">new</span> String(buffer1.array(), <span class="number">0</span>, buffer1.limit()));</span><br><span class="line">    System.out.println(buffer2.toString());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>原来的read或者write方法接受的是一个buffer,现在可以接收一个buffer数组.</p>
<h1 id="NIO的非阻塞模式"><a href="#NIO的非阻塞模式" class="headerlink" title="NIO的非阻塞模式"></a>NIO的非阻塞模式</h1><p>非阻塞模式是在网络IO中才有.在服务端和客户端中间 加了一个选择器.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">client</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    SocketChannel sChannel = SocketChannel.open(<span class="keyword">new</span> InetSocketAddress(<span class="string">"127.0.0.1"</span>, <span class="number">9090</span>));</span><br><span class="line">    FileChannel inChannel = FileChannel.open(Paths.get(<span class="string">"pom.xml"</span>), StandardOpenOption.READ);</span><br><span class="line">    ByteBuffer buffer = ByteBuffer.allocate(<span class="number">1024</span>);</span><br><span class="line">    <span class="keyword">while</span> (inChannel.read(buffer) != -<span class="number">1</span>) &#123;</span><br><span class="line">        buffer.flip();</span><br><span class="line">        sChannel.write(buffer);</span><br><span class="line">        buffer.clear();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> ((len = sChannel.read(buffer)) != -<span class="number">1</span>) &#123;</span><br><span class="line">        buffer.flip();</span><br><span class="line">        System.out.println(<span class="keyword">new</span> String(buffer.array(), <span class="number">0</span>, len));</span><br><span class="line">        buffer.clear();</span><br><span class="line">    &#125;</span><br><span class="line">    inChannel.close();</span><br><span class="line">    sChannel.close();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">server</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    ServerSocketChannel serverSocketChannel = ServerSocketChannel.open();</span><br><span class="line">    FileChannel outChannel = FileChannel.open(Paths.get(<span class="string">"pom"</span>), StandardOpenOption.WRITE, StandardOpenOption.CREATE);</span><br><span class="line">    serverSocketChannel.bind(<span class="keyword">new</span> InetSocketAddress(<span class="number">9090</span>));</span><br><span class="line">    SocketChannel socketChannel = serverSocketChannel.accept();</span><br><span class="line">    ByteBuffer buffer = ByteBuffer.allocate(<span class="number">1024</span>);</span><br><span class="line">    <span class="keyword">while</span> (socketChannel.read(buffer) != -<span class="number">1</span>) &#123;</span><br><span class="line">        buffer.flip();</span><br><span class="line">        outChannel.write(buffer);</span><br><span class="line">        buffer.clear();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    buffer.put(<span class="string">"接收完成"</span>.getBytes());</span><br><span class="line">    buffer.flip();</span><br><span class="line">    socketChannel.write(buffer);</span><br><span class="line"></span><br><span class="line">    socketChannel.close();</span><br><span class="line">    outChannel.close();</span><br><span class="line">    serverSocketChannel.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>ServerSocketChannel与SocketChannel:通过 ServerSocketChannel.accept() 方法监听新进来的连接。当 accept()方法返回的时候,它返回一个包含新进来的连接的 SocketChannel。</p>
<p>上面的代码是用inchannel读入文件到SocketChannel中,SocketChannel根据ip和端口号建立套接字连接,ServerSocketChannel监听端口,得到一个SocketChannel.然后将SocketChannel中的数据写入outChannel.接着在SocketChannel中放入buffer,buffer有回传数据.让客户端显示回传数据.</p>
<p>但是这里的问题是,客户端在发送完数据后没有关闭与服务端的连接.这条通道就一直占用着.即客户端做完该做的事后却一直连着服务端,没有正确关闭.</p>
<p>而现在使用选择器可以做到多路复用,非阻塞的传输.</p>
<p>如果客户端准备好连接,那选择器得到准备连接的状态,让服务器与之连接.如果准备好传输数据,那么就传输数据.中间这种状态的切换是通过轮询选择器的状态码实现.一旦状态码改变就执行.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">server</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    ServerSocketChannel serverSocketChannel = ServerSocketChannel.open();</span><br><span class="line">    serverSocketChannel.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line">    serverSocketChannel.bind(<span class="keyword">new</span> InetSocketAddress(<span class="number">9090</span>));</span><br><span class="line">    <span class="comment">//获取一个选择器</span></span><br><span class="line">    Selector selector = Selector.open();</span><br><span class="line">    <span class="comment">//将通道注册到选择器上,并且指定监听"OP_ACCEPT",即接收事件,表示服务器接收已经就绪</span></span><br><span class="line">    serverSocketChannel.register(selector, SelectionKey.OP_ACCEPT);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//当选择器中有"状态码"时</span></span><br><span class="line">    <span class="keyword">while</span> (selector.select() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">//遍历这些"状态码"</span></span><br><span class="line">        Iterator&lt;SelectionKey&gt; keyIterator = selector.selectedKeys().iterator();</span><br><span class="line">        <span class="keyword">while</span> (keyIterator.hasNext()) &#123;</span><br><span class="line">            SelectionKey key = keyIterator.next();</span><br><span class="line">            <span class="comment">//如果是表示已准备好接收数据</span></span><br><span class="line">            <span class="keyword">if</span> (key.isAcceptable()) &#123;</span><br><span class="line">                <span class="comment">//则获取客户端连接</span></span><br><span class="line">                SocketChannel socketChannel = serverSocketChannel.accept();</span><br><span class="line">                <span class="comment">//切换非阻塞模式</span></span><br><span class="line">                socketChannel.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line">                <span class="comment">//将该通道注册到选择器,监听"读就绪"</span></span><br><span class="line">                socketChannel.register(selector, SelectionKey.OP_READ);</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (key.isReadable()) &#123;</span><br><span class="line">                <span class="comment">//如果是读就绪,则通过key获得通道传输数据</span></span><br><span class="line">                SocketChannel sChannel = (SocketChannel) key.channel();</span><br><span class="line"></span><br><span class="line">                ByteBuffer buffer = ByteBuffer.allocate(<span class="number">1024</span>);</span><br><span class="line">                <span class="keyword">int</span> len=<span class="number">0</span>;</span><br><span class="line">                <span class="keyword">while</span> ((len = sChannel.read(buffer)) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    buffer.flip();</span><br><span class="line">                    System.out.println(<span class="keyword">new</span> String(buffer.array(), <span class="number">0</span>, len));</span><br><span class="line">                    buffer.clear();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//处理完当前这个状态码就要移出去.下次通道就绪再放到selector中</span></span><br><span class="line">            keyIterator.remove();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面是一个不太完整规范的代码.其中<code>selector.select()</code>的返回值是表示selector中有多少个通道就绪(有几个”状态码”),它会一直阻塞到直到selector中有就绪的管道.如果方法里有参数,则表示最多阻塞多少毫秒.表示面是完整的:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> selector;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.net.InetSocketAddress;</span><br><span class="line"><span class="keyword">import</span> java.nio.ByteBuffer;</span><br><span class="line"><span class="keyword">import</span> java.nio.channels.SelectionKey;</span><br><span class="line"><span class="keyword">import</span> java.nio.channels.Selector;</span><br><span class="line"><span class="keyword">import</span> java.nio.channels.ServerSocketChannel;</span><br><span class="line"><span class="keyword">import</span> java.nio.channels.SocketChannel;</span><br><span class="line"><span class="keyword">import</span> java.util.Iterator;</span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WebServer</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            ServerSocketChannel ssc = ServerSocketChannel.open();</span><br><span class="line">            ssc.socket().bind(<span class="keyword">new</span> InetSocketAddress(<span class="string">"127.0.0.1"</span>, <span class="number">8000</span>));</span><br><span class="line">            ssc.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line"></span><br><span class="line">            Selector selector = Selector.open();</span><br><span class="line">            <span class="comment">// 注册 channel，并且指定感兴趣的事件是 Accept</span></span><br><span class="line">            ssc.register(selector, SelectionKey.OP_ACCEPT);</span><br><span class="line"></span><br><span class="line">            ByteBuffer readBuff = ByteBuffer.allocate(<span class="number">1024</span>);</span><br><span class="line">            ByteBuffer writeBuff = ByteBuffer.allocate(<span class="number">128</span>);</span><br><span class="line">            writeBuff.put(<span class="string">"received"</span>.getBytes());</span><br><span class="line">            writeBuff.flip();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                <span class="keyword">int</span> nReady = selector.select();</span><br><span class="line">                Set&lt;SelectionKey&gt; keys = selector.selectedKeys();</span><br><span class="line">                Iterator&lt;SelectionKey&gt; it = keys.iterator();</span><br><span class="line"></span><br><span class="line">                <span class="keyword">while</span> (it.hasNext()) &#123;</span><br><span class="line">                    SelectionKey key = it.next();</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">if</span> (key.isAcceptable()) &#123;</span><br><span class="line">                        <span class="comment">// 创建新的连接，并且把连接注册到selector上，而且，</span></span><br><span class="line">                        <span class="comment">// 声明这个channel只对读操作感兴趣。</span></span><br><span class="line">                        SocketChannel socketChannel = ssc.accept();</span><br><span class="line">                        socketChannel.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line">                        socketChannel.register(selector, SelectionKey.OP_READ);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (key.isReadable()) &#123;</span><br><span class="line">                        SocketChannel socketChannel = (SocketChannel) key.channel();</span><br><span class="line">                        readBuff.clear();</span><br><span class="line">                        socketChannel.read(readBuff);</span><br><span class="line"></span><br><span class="line">                        readBuff.flip();</span><br><span class="line">                        System.out.println(<span class="string">"received : "</span> + <span class="keyword">new</span> String(readBuff.array()));</span><br><span class="line">                        key.interestOps(SelectionKey.OP_WRITE);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (key.isWritable()) &#123;</span><br><span class="line">                        writeBuff.rewind();</span><br><span class="line">                        SocketChannel socketChannel = (SocketChannel) key.channel();</span><br><span class="line">                        socketChannel.write(writeBuff);</span><br><span class="line">                        key.interestOps(SelectionKey.OP_READ);</span><br><span class="line">                    &#125;</span><br><span class="line">                    it.remove();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当服务器正常启动且有客户端连接上,流程是这样的 :</p>
<ol>
<li>获取ServerSocketChannel这个通道</li>
<li>将这个通道在selector注册,并且监听接收就绪事件</li>
<li>进入while(true)循环,得到所有的selectedKey.如果此时服务端正常工作,那么显然已经”接收就绪”.则创建一个SocketChannel客户端通道.并且在selector注册这个通道监听”读就绪”.</li>
<li>此时来到<code>it.remove();</code>,在selector中去掉ServerSocketChannel通道接收就绪的事件,下次传输就又可以加这个事件.此时如果客户端准备发送还没就绪,selector的selectedKey就是空的.因为where(true),所以就会一直等.这里还可以写成上面说的<code>selector.select()</code></li>
<li>当客户端终于发送就绪,触发了”读就绪”.进入else if (key.isReadable()).根据OP_READ这个key的key.channel得到之前注册的通道.</li>
</ol>
<p>然后开始传输数据.</p>
<h1 id="Pipe"><a href="#Pipe" class="headerlink" title="Pipe"></a>Pipe</h1><p>用于线程之间单向的传输数据.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200801215320731.png" alt="image-20200801215320731"></p>
<p>通过<code>Pipe.open()</code>方法打开管道:<code>Pipe pipe = Pipe.open();</code></p>
<p>要向管道写数据，需要访问sink通道:<code>Pipe.SinkChannel sinkChannel = pipe.sink();</code></p>
<p>通过调用SinkChannel的<code>write()</code>方法，将数据写入<code>SinkChannel</code>,像这样</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String newData = <span class="string">"New String to write to file..."</span> + System.currentTimeMillis();</span><br><span class="line">ByteBuffer buf = ByteBuffer.allocate(<span class="number">48</span>);</span><br><span class="line">buf.clear();</span><br><span class="line">buf.put(newData.getBytes());</span><br><span class="line"></span><br><span class="line">buf.flip();</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span>(buf.hasRemaining()) &#123;</span><br><span class="line">    sinkChannel.write(buf);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从读取管道的数据，需要访问source通道，像这样:<code>Pipe.SourceChannel sourceChannel = pipe.source();</code></p>
<p>调用source通道的<code>read()</code>方法来读取数据，像这样:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ByteBuffer buf = ByteBuffer.allocate(<span class="number">48</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> bytesRead = sourceChannel.read(buf);</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Webflux</title>
    <url>/2020/07/03/Webflux/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>json ajax</title>
    <url>/2020/06/30/json-ajax/</url>
    <content><![CDATA[<p>Json使用.</p>
<a id="more"></a>

<figure class="highlight"><table><tr><td class="code"><pre><span class="line">const jsonObj = &#123;</span><br><span class="line">            "key1": 12,</span><br><span class="line">            "key2": 13,</span><br><span class="line">            "key3": "aa",</span><br><span class="line">            "key4": true,</span><br><span class="line">            "key5": [1, "a", false],</span><br><span class="line">            "key6": &#123;"key6_1": 1, "key6_2": 2&#125;,</span><br><span class="line">            "key7": [&#123;"key7_1": 222, "aa": true&#125;, &#123;"c": "cv", "dd": 123&#125;]</span><br><span class="line">        &#125;;</span><br></pre></td></tr></table></figure>
<p>json本身是一个对象.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">alert(jsonObj.key1);</span><br><span class="line">alert(jsonObj.key6.key6_1);</span><br><span class="line">alert(jsonObj.key7[<span class="number">0</span>].key7_1);</span><br></pre></td></tr></table></figure>

<p>json有两种存在形式,一种是json对象,另一种是字符串.可以互相转化:JSON.stringify(),JSON.parse().在客户端和服务器之间传数据时用字符串,要取值时用json对象.</p>
<p>使用Gson包实现json与其它类型的转换:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">User user = <span class="keyword">new</span> User(<span class="number">1</span>, <span class="string">"aa"</span>, <span class="string">"aa"</span>, <span class="string">"aa@gmail.com"</span>);</span><br><span class="line">Gson gson = <span class="keyword">new</span> Gson();</span><br><span class="line">String s = gson.toJson(user);</span><br><span class="line">System.out.println(s);</span><br><span class="line"></span><br><span class="line">User user1 = gson.fromJson(s, user.getClass());</span><br><span class="line">System.out.println(user1);</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Gson gson = <span class="keyword">new</span> Gson();</span><br><span class="line">ArrayList&lt;User&gt; users = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">users.add(<span class="keyword">new</span> User(<span class="number">1</span>, <span class="string">"aa"</span>, <span class="string">"aa"</span>, <span class="string">"aa"</span>));</span><br><span class="line">users.add(<span class="keyword">new</span> User(<span class="number">2</span>, <span class="string">"bb"</span>, <span class="string">"bb"</span>, <span class="string">"bb"</span>));</span><br><span class="line">String json = gson.toJson(users);</span><br><span class="line"><span class="comment">//IDEA似乎不能正确推断我返回的类型,而是直接给我Object类型</span></span><br><span class="line">ArrayList&lt;User&gt; arrayList = gson.fromJson(json, <span class="keyword">new</span> TypeToken&lt;ArrayList&lt;User&gt;&gt;() &#123;&#125;.getType());</span><br><span class="line">arrayList.forEach(System.out::println);</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Gson gson = <span class="keyword">new</span> Gson();</span><br><span class="line">HashMap&lt;Integer, User&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">map.put(<span class="number">1</span>, <span class="keyword">new</span> User(<span class="number">1</span>, <span class="string">"aa"</span>, <span class="string">"aa"</span>, <span class="string">"aa"</span>));</span><br><span class="line">map.put(<span class="number">2</span>, <span class="keyword">new</span> User(<span class="number">2</span>, <span class="string">"bb"</span>, <span class="string">"bb"</span>, <span class="string">"bb"</span>));</span><br><span class="line"></span><br><span class="line">String json = gson.toJson(map);</span><br><span class="line"></span><br><span class="line">HashMap&lt;Integer, User&gt; hashMap = gson.fromJson(json, <span class="keyword">new</span> TypeToken&lt;HashMap&lt;Integer, User&gt;&gt;() &#123;</span><br><span class="line">&#125;.getType());</span><br><span class="line">hashMap.forEach((k, v) -&gt; System.out.println(k + <span class="string">":"</span> + v));</span><br></pre></td></tr></table></figure>

<p>ajax是用来异步的局部更新页面的技术,具体代码:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line">    <span class="comment">// 在这里使用javaScript语言发起Ajax请求，访问服务器AjaxServlet中javaScriptAjax</span></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">ajaxRequest</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"><span class="comment">// 				1、我们首先要创建XMLHttpRequest </span></span><br><span class="line">        <span class="keyword">var</span> xmlhttprequest = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">        <span class="comment">//调用open方法设置请求参数,?表示请求的参数,即action;true表示异步的.</span></span><br><span class="line">        xmlhttprequest.open(<span class="string">"GET"</span>, <span class="string">"http://localhost:8080/Books/ajaxServlet?action=javaScriptAjax"</span>, <span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//当readySatate属性改变时,就会调用onreadystatechange函数,readyState有五种状态,从0-4,4表示请求完成且响应就绪</span></span><br><span class="line">        <span class="comment">//这里意思就是一旦readyState状态改变就检查是不是服务器响应过来了</span></span><br><span class="line">        xmlhttprequest.onreadystatechange = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (xmlhttprequest.readyState == <span class="number">4</span> &amp;&amp; xmlhttprequest.status == <span class="number">200</span>) &#123;</span><br><span class="line">                alert(xmlhttprequest.responseText);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"><span class="comment">// 				3、调用send方法发送请求</span></span><br><span class="line">        xmlhttprequest.send();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="comment">// alert("我是最后一行的代码");</span></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>

<p>一般用Ajax是用JQuery来实现:</p>
<p>$.ajax方法:url:请求的地址,type:请求的类型,data:发送给服务器的数据,dataType:响应的数据类型,success:请求成功,响应的回调函数,类似与上面readyState改变且符合4&amp;200要求时所执行的代码.</p>
<p>其中data有两种格式:{key:value}或者name=value&amp;name=value.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">// ajax请求</span></span><br><span class="line">    $(<span class="string">"#ajaxBtn"</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        $.ajax(&#123;</span><br><span class="line">            url:<span class="string">"http://localhost:8080/Books/ajaxServlet"</span>,</span><br><span class="line">            <span class="comment">// data:"action=jQueryAjax",</span></span><br><span class="line">            data:&#123;<span class="attr">action</span>:<span class="string">"jQueryAjax"</span>&#125;,<span class="comment">//data是发送给服务器的数据,这里指的是请求的参数</span></span><br><span class="line">            type:<span class="string">"GET"</span>,</span><br><span class="line">            <span class="comment">//data表示返回的数据类型</span></span><br><span class="line">            success:<span class="function"><span class="keyword">function</span> (<span class="params">data</span>) </span>&#123;</span><br><span class="line">                <span class="comment">// alert("服务器返回的数据是：" + data);</span></span><br><span class="line">                <span class="comment">// var jsonObj = JSON.parse(data);</span></span><br><span class="line">                $(<span class="string">"#msg"</span>).html(<span class="string">" ajax 编号："</span> + data.id + <span class="string">" , 姓名："</span> + data.name);</span><br><span class="line">            &#125;,</span><br><span class="line">            dataType : <span class="string">"json"</span><span class="comment">//常用的text,json</span></span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>

<p>$.get方法和$.post方法是在上面的方法基础上进一步封装,简化代码.参数有url:请求的地址,data:发送的数据,callback:成功的回调函数,type:返回的数据类型.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">// ajax--get请求</span></span><br><span class="line">    $(<span class="string">"#getBtn"</span>).click(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="comment">//依次写url,data,callback,datatype,不需要写明</span></span><br><span class="line">        $.<span class="keyword">get</span>("http://localhost:8080/Books/ajaxServlet", "action=jQueryGet", function (data) &#123;</span><br><span class="line">            $(<span class="string">"#msg"</span>).html(<span class="string">" get 编号："</span> + data.id + <span class="string">" , 姓名："</span> + data.name);</span><br><span class="line">        &#125;, <span class="string">"json"</span>);</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ajax--post请求</span></span><br><span class="line">    $(<span class="string">"#postBtn"</span>).click(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="comment">// post请求</span></span><br><span class="line">        $.post(<span class="string">"http://localhost:8080/Books/ajaxServlet"</span>, <span class="string">"action=jQueryPost"</span>, <span class="function"><span class="keyword">function</span> (<span class="params">data</span>) </span>&#123;</span><br><span class="line">            $(<span class="string">"#msg"</span>).html(<span class="string">" post 编号："</span> + data.id + <span class="string">" , 姓名："</span> + data.name);</span><br><span class="line">        &#125;, <span class="string">"json"</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>

<p>$.getJSON是进一步简化,只有三个参数,dataType固定是json.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">// ajax--getJson请求</span></span><br><span class="line">    $(<span class="string">"#getJSONBtn"</span>).click(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        $.getJSON(<span class="string">"http://localhost:8080/16_json_ajax_i18n/ajaxServlet"</span>, <span class="string">"action=jQueryGetJSON"</span>, <span class="function"><span class="keyword">function</span> (<span class="params">data</span>) </span>&#123;</span><br><span class="line">            $(<span class="string">"#msg"</span>).html(<span class="string">" getJSON 编号："</span> + data.id + <span class="string">" , 姓名："</span> + data.name);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>

<p>serialize()方法:表单中所有表单项的内容全部获取到,并以name=value&amp;name=value的形式拼接.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"#submit"</span>).click(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 把参数序列化,在data中,首先把请求参数action写明,然后用&amp;将表单中的参数连接起来一起提交</span></span><br><span class="line">    $.getJSON(<span class="string">"http://localhost:8080/16_json_ajax_i18n/ajaxServlet"</span>, <span class="string">"action=jQuerySerialize&amp;"</span> + $(<span class="string">"#form01"</span>).serialize(), <span class="function"><span class="keyword">function</span> (<span class="params">data</span>) </span>&#123;</span><br><span class="line">        $(<span class="string">"#msg"</span>).html(<span class="string">" Serialize 编号："</span> + data.id + <span class="string">" , 姓名："</span> + data.name);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>用Ajax实现校验用户名是否存在:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"#username"</span>).blur(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="comment">//1 获取用户名</span></span><br><span class="line">    <span class="keyword">var</span> username = <span class="keyword">this</span>.value;</span><br><span class="line">    $.getJSON(<span class="string">"http://localhost:8080/book/userServlet"</span>,<span class="string">"action=ajaxExistsUsername&amp;username="</span> + username,<span class="function"><span class="keyword">function</span> (<span class="params">data</span>) </span>&#123;</span><br><span class="line">        <span class="comment">// existsUsername是返回的json中的key名</span></span><br><span class="line">        <span class="keyword">if</span> (data.existsUsername) &#123;</span><br><span class="line">            $(<span class="string">"span.errorMsg"</span>).text(<span class="string">"用户名已存在！"</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            $(<span class="string">"span.errorMsg"</span>).text(<span class="string">"用户名可用！"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>深入理解计算机系统读书笔记1</title>
    <url>/2018/01/22/%E3%80%8A%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E3%80%8B%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0%EF%BC%881%EF%BC%89/</url>
    <content><![CDATA[<p>​&emsp;前段时间把以前的随笔一不小心全都删了，很是痛心，有将近两个月没有动笔。现在到了寒假，深感不能浪费，于是又重拾笔头。内容主要包括读书笔记和补一些原来丢失的文章。</p>
<a id="more"></a>

<h1 id="1-编译系统"><a href="#1-编译系统" class="headerlink" title="1.编译系统"></a>1.编译系统</h1><p>&emsp;以GCC为例，分为四个部分，预处理器，编译器，汇编器，链接器。对于其他的编译过程，基本上也就是这样的流程，只不过有的会划分为三个部分而已。</p>
<ul>
<li><p>预处理器：主要处理‘#’标志的头文件，读取例如stdio.h的内容，插入到源文件中，生成扩展名为.i的文件。</p>
<ol>
<li><p>#include指令：用于包含文件，包含后代码就可以正确的调用该文件中声明的变量和函数。有两种使用方式：#include &lt;iostream.h&gt;和#include “stdafx.h”。尖括号的将包含系统默认目录的头文件或尖括号内的路径查找。通常是系统中自带的公共头文件。我安装了Mingw，所以用gcc/g++编译时 ，用的是./Mingw/include中的头文件。而双引号包含头文件则在源代码所在目录查找，没有则到默认目录找，通常用于包含程序作者编写的私有头文件。stdafx.h就是微软家自己弄的私有头文件，是非常有用的，不少初学者搞不懂是干嘛的，因此造成了非常多的错误。我会写篇文章再说说它。</p>
</li>
<li><p>#define和#undef：定义宏和取消定义宏。</p>
</li>
<li><p>#if:如果条件为真，则编译下面的代码；</p>
</li>
</ol>
</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ifndef：如果宏没有定义则编译；</span><br><span class="line"></span><br><span class="line">#endif：结束一个#if...#else条件编译块；</span><br><span class="line"></span><br><span class="line">#error：停止编译并显示错误信息。</span><br></pre></td></tr></table></figure>

<pre><code>具体用法请参看链接：

[参考链接]:（http://blog.csdn.net/wangxueji45/article/details/41753531）     ​</code></pre><ul>
<li><p>编译器：将.i的文件编译为汇编代码，扩展名为.s。</p>
</li>
<li><p>汇编器：将.s文件编译为机器语言指令，是二进制的，扩展名为.o。</p>
</li>
<li><p>链接器：负责处理将函数调用等内容链接到文件中。例如：调用了printf函数，而这个函数是标准C库的函数。printf函数存在于printf.o这个单独的预编译好了的文件中。</p>
<p>我这里只是写了比较可能用到的预处理器方面，其他都略过了，详细点的可以看下面的链接。我打算学习汇编，有可能的话将来会回来补这些东西。</p>
</li>
</ul>
<h2 id="参考链接-http-www-ruanyifeng-com-blog-2014-11-compiler-html"><a href="#参考链接-http-www-ruanyifeng-com-blog-2014-11-compiler-html" class="headerlink" title="[参考链接]: http://www.ruanyifeng.com/blog/2014/11/compiler.html"></a>[参考链接]: <a href="http://www.ruanyifeng.com/blog/2014/11/compiler.html" target="_blank" rel="noopener">http://www.ruanyifeng.com/blog/2014/11/compiler.html</a></h2><h1 id="2-系统的硬件组成"><a href="#2-系统的硬件组成" class="headerlink" title="2.系统的硬件组成"></a>2.系统的硬件组成</h1><ul>
<li>总线（bus）：总线的东西较多，我单开一篇写。</li>
<li>I/O设备 ：键盘，鼠标，显示器，磁盘。</li>
<li>主存：动态随机存取存储器（DRAM），即常说的内存。</li>
<li>处理器 ：CPU是解释或执行存储在主存中的指令的引擎，其核心是一个字长的存储设备（寄存器），称为程序计数器，在任何时候它指向主存中的某条机器语言指令（即含有该指令的地址）。另一个关键是算数逻辑单元（ALU）。注意，这个部分有许多说法，我认为《深入》这本书这里讲的有清楚，我参考王爽的《汇编语言》。</li>
</ul>
]]></content>
      <tags>
        <tag>读书笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>stdafx.h的原理及作用</title>
    <url>/2018/01/22/stdafx-h%E7%9A%84%E4%BD%9C%E7%94%A8%E5%8F%8A%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<p>​    在使用VS的过程中经常会见到stdafx.h这个头文件，初学者常常因为这个放弃了对VS的使用，又回到了垃圾的VC6.0的怀抱。</p>
<a id="more"></a>

<p>​    stdafx的英文全称为Standard Application Framework Extensions（标准应用程序框架的扩展）。stdafx.h这个头文件的作用是实现头文件预编译，就是把一个工程（解决方案）中要使用的一些MFC标准头文件（如Windows.h、Afxwin.h）预先编译，以后该工程文件编译时，不再编译这部分，可以加快编译速度。</p>
<p><img src="/2018/01/22/stdafx-h%E7%9A%84%E4%BD%9C%E7%94%A8%E5%8F%8A%E5%8E%9F%E7%90%86/%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88%E7%BB%93%E6%9E%84.png" alt="解决方案结构"></p>
<p>​    上图是解决方案的结构，可以看到stdafx.h和stdafx.cpp这两个东西，其内容是</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// stdafx.h : 标准系统包含文件的包含文件，</span></span><br><span class="line"><span class="comment">// 或是经常使用但不常更改的</span></span><br><span class="line"><span class="comment">// 特定于项目的包含文件</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> once</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"targetver.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;tchar.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// <span class="doctag">TODO:</span> 在此处引用程序需要的其他头文件</span></span><br></pre></td></tr></table></figure>

<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// stdafx.cpp : 只包括标准包含文件的源文件</span></span><br><span class="line"><span class="comment">// Project1.pch 将作为预编译标头</span></span><br><span class="line"><span class="comment">// stdafx.obj 将包含预编译类型信息</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"stdafx.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// <span class="doctag">TODO:</span> 在 STDAFX.H 中引用任何所需的附加头文件，</span></span><br><span class="line"><span class="comment">//而不是在此文件中引用</span></span><br></pre></td></tr></table></figure>

<p>​    可以看到，在stdafx.h中，包含了stdio.h头文件，所以我可以使用printf这个函数:<br><img src="/2018/01/22/stdafx-h%E7%9A%84%E4%BD%9C%E7%94%A8%E5%8F%8A%E5%8E%9F%E7%90%86/%E7%A4%BA%E4%BE%8B%E4%BB%A3%E7%A0%81.png" alt="示例代码"></p>
<p>​    stdafx.h的作用就在于，如果解决方案中有多个Project1这样的项目，需要大量的编译一些经常使用但不常更改的头文件，那么，就可以将这些头文件包含在stdafx.h中，对头文件进行预编译，若再次使用它们的话，只要包含stdafx.h就行。如果看明白了这个逻辑，你应该就发现，stdafx.h本质上就是一个将你需要的头文件打包预编译的新的头文件。那么，这个新的头文件显然不是stdafx.h本身，里面就几句话。那这个新东西在那呢？</p>
<p>​    我们先来看看如何预编译头文件。先指定一个头文件（.h）,默认是stdafx.h，包含想要预编译的头文件，然后用stdafx.h来生成一个编译头文件（.pch,Precompiled Header），因为头文件是不能直接编译的，所以还需一个.cpp/.c文件作为桥梁，默认的就是stdafx.cpp。接下来就是用它来生成pch文件。在stdafx.h的属性设置中，你会看到相关设置。</p>
<p><img src="/2018/01/22/stdafx-h%E7%9A%84%E4%BD%9C%E7%94%A8%E5%8F%8A%E5%8E%9F%E7%90%86/%E5%B1%9E%E6%80%A7.png" alt="属性"></p>
<p>​    第一栏，你可以选择创建还是使用，这里是创建，因为要生成pch文件给别的地方用，你看看project1.cpp的属性里，它就是使用，因为它包含了stdafx.h这个文件，它要用编译头文件。第二栏，指定预编译头文件，这意味着你可以创建多个预编译头文件，视需要在stdafx.cpp中包含它们。第三栏，指定生成pch文件的位置，这意味着你可以将pch拷贝复制给别人使用。设想，给客户使用预编译头文件时，如果要预编译的头文件很多，那也是很花时间的，如果直接把pch文件给别人，然后在属性里设置包含路径就可以直接用了。这里我遇到个问题，默认的pch文件名就是工程名.pch，但是一开时就是不生成，改成test.pch后才生成。</p>
<p>​    注意：所有在#include “stdafx.h”这条指令前的代码都被看作预编译的。因此这句话必须放在第一行。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Project1.cpp: 定义控制台应用程序的入口点。</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"stdafx.h"</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">"hello"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    其效果大约是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Project1.cpp: 定义控制台应用程序的入口点。</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"test.pch"</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">"hello"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    就是这么简单的东西，希望初学者不要一看到和书上，老师教的不一样就慌乱，不耐烦，多百度。</p>
]]></content>
      <tags>
        <tag>VS</tag>
      </tags>
  </entry>
  <entry>
    <title>内存空间分配</title>
    <url>/2018/01/30/%E5%86%85%E5%AD%98%E7%A9%BA%E9%97%B4%E5%88%86%E9%85%8D/</url>
    <content><![CDATA[<p>在计算机总线一文中介绍了内存地址空间，现在说明在C++中如何进行内存分配。</p>
<a id="more"></a>

<p>一个C/C++的程序占用的内存分为以下几个部分：</p>
<p>1.栈区（stack）：由编译器自动分配释放，存放函数的参数值，局部变量的值等，操作方式类似与数据结构的栈。</p>
<p>2.堆区（heap）：一般由程序员分配释放，若程序员不释放，程序结束时<strong>可能</strong>由OS释放。</p>
<p>3.全局区（static）：也叫静态数据内存空间，存储全局变量和静态变量，两个存在一起，但是已经初始化的放一起，没有初始化的放一起。程序结束后由系统释放。</p>
<p>4.文字常量区：常量字符串就是放在这，程序结束后由系统释放。</p>
<p>5.程序代码区：存放函数体的二进制代码。</p>
<p>但是还有的版本是1.2.3相同，<strong>4.5是自由存储区和常量存储区</strong>。</p>
<p><strong>我查了整整一天，发现后面这种其实更符合官方文档。后文会再谈。我这先采用自己的视角探讨内存分配。</strong></p>
<h1 id="可执行程序的文件结构"><a href="#可执行程序的文件结构" class="headerlink" title="可执行程序的文件结构"></a>可执行程序的文件结构</h1><h2 id="C程序"><a href="#C程序" class="headerlink" title="C程序"></a>C程序</h2><p>我在Deepin中用gcc（6.2.0）编译一个hello程序，再用<code>size</code>命令查看其可执行程序的结构，如图：</p>
<p><img src="/2018/01/30/%E5%86%85%E5%AD%98%E7%A9%BA%E9%97%B4%E5%88%86%E9%85%8D/C%E7%BB%93%E6%9E%84.png" alt="C结构"></p>
<p>可以看到，存储时（未加载到内存中）分为代码区（text），数据区（data），未初始化数据区（bss），后面两个是总和2192和2192的十六进制大小。</p>
<p>（1）代码区（text segment）。存放CPU执行的机器指令（machine instructions）。通常，代码区是可共享的（即另外的执行程序可以调用它），因为对于频繁被执行的程序，只需要在内存中有一份代码即可。代码区通常是只读的，使其只读的原因是防止程序意外地修改了它的指令。另外，代码区还规划了局部变量的相关信息。</p>
<p>（2）全局初始化数据区/静态数据区（initialized data segment/data segment）。该区包含了在程序中明确被初始化的全局变量、静态变量（包括全局静态变量和局部静态变量）和常量数据（如字符串常量）。例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">include&lt;stdio.h&gt;</span><br><span class="line"><span class="keyword">int</span> global=<span class="number">100</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> x=<span class="number">50</span>;</span><br><span class="line"><span class="keyword">const</span> pi=<span class="number">3.14</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	......</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fun1</span><span class="params">(<span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"> 	<span class="keyword">static</span> <span class="keyword">int</span>  count=<span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>全局变量<code>global</code>,<code>x</code>,全局静态变量<code>x</code>,局部静态变量<code>count</code>,常量<code>pi</code>。<code>global</code>和<code>x</code>的区别在于global可以在该程序的其他文件中使用，而x只在这这个源代码中使用。<code>static</code>有限定只能在当前文件中被调用的作用。</p>
<p>（3）未初始化数据区。亦称BSS区（uninitialized data segment），存入的是全局未初始化变量。BSS这个叫法是根据一个早期的汇编运算符而来，这个汇编运算符标志着一个块的开始。BSS区的数据在程序开始执行之前被内核初始化为0或者空指针（NULL）。例如声明一个数组，但没有赋值：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">long</span> sun[<span class="number">1000</span>];</span><br></pre></td></tr></table></figure>

<p><code>long</code>是<code>long int</code>的简写。</p>
<h2 id="C-程序"><a href="#C-程序" class="headerlink" title="C++程序"></a>C++程序</h2><p>在Windows10中用g++(6.3.0)编译C++程序，其结构是和C的一样的</p>
<p><img src="/2018/01/30/%E5%86%85%E5%AD%98%E7%A9%BA%E9%97%B4%E5%88%86%E9%85%8D/C++%E7%BB%93%E6%9E%84.png" alt="c++结构"></p>
<h1 id="运行时的结构"><a href="#运行时的结构" class="headerlink" title="运行时的结构"></a>运行时的结构</h1><p><img src="/2018/01/30/%E5%86%85%E5%AD%98%E7%A9%BA%E9%97%B4%E5%88%86%E9%85%8D/%E6%AF%94%E8%BE%83.jpg" alt="比较"></p>
<p>从上图可以看出，内存空间被分为五个部分。</p>
<p>（1）代码区（text segment）。代码区指令根据程序设计流程依次执行，对于顺序指令，则只会执行一次（每个进程），如果反复，则需要使用跳转指令，如果进行递归，则需要借助栈来实现。</p>
<p>代码区的指令中包括操作码和要操作的对象（或对象地址引用）。如果是立即数（即具体的数值，如5），将直接包含在代码中；如果是局部数据，将在栈区分配空间，然后引用该数据地址；如果是BSS区和数据区，在代码中同样将引用该数据地址。</p>
<p>（2）全局初始化数据区/静态数据区（Data Segment）。只初始化一次。</p>
<p>（3）未初始化数据区（BSS）。在运行时改变其值。</p>
<p>（4）栈区（stack）。由编译器自动分配释放，存放函数的参数值、局部变量的值等。其操作方式类似于数据结构中的栈。每当一个函数被调用，该函数返回地址和一些关于调用的信息，比如某些寄存器的内容，被存储到栈区。然后这个被调用的函数再为它的自动变量和临时变量在栈区上分配空间，这就是C实现函数递归调用的方法。每执行一次递归函数调用，一个新的栈框架就会被使用，这样这个新实例栈里的变量就不会和该函数的另一个实例栈里面的变量混淆。</p>
<p>（5）堆区（heap）。用于动态内存分配。堆在内存中位于bss区和栈区之间。一般由程序员分配和释放，若程序员不释放，程序结束时有可能由OS回收。</p>
<p>之所以分成这么多个区域，主要基于以下考虑：</p>
<p>一个进程在运行过程中，代码是根据流程依次执行的，只需要访问一次，当然跳转和递归有可能使代码执行多次，而数据一般都需要访问多次，因此单独开辟空间以方便访问和节约空间。</p>
<p>临时数据及需要再次使用的代码在运行时放入栈区中，生命周期短。</p>
<p>全局数据和静态数据有可能在整个程序执行过程中都需要访问，因此单独存储管理。</p>
<p>堆区由用户自由分配，以便管理。</p>
<p>C/C++语言中典型的存储类型有auto, extern, register, static 这四种，但是是有区别的，C++还有其他两种。存储类型说明了该变量要在进程的哪一个段中分配内存空间，可以为变量分配内存存储空间的有数据区、BBS区、栈区、堆区。下面来一一举例看一下这几个存储类型：</p>
<h2 id="1-auto存储类型"><a href="#1-auto存储类型" class="headerlink" title="1.auto存储类型"></a>1.auto存储类型</h2><p>auto只能用来标识局部变量（’{}’中的部分）的存储类型，对于局部变量，auto是默认的存储类型，不需要显示的指定。因此，auto标识的变量存储在栈区中。示例如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;  </span></span></span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">auto</span> <span class="keyword">int</span> i=<span class="number">1</span>; <span class="comment">//显示指定变量的存储类型  </span></span><br><span class="line">    <span class="keyword">int</span> j=<span class="number">2</span>;  </span><br><span class="line">  </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"i=%d\tj=%d\n"</span>,i,j);  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-extern存储类型"><a href="#2-extern存储类型" class="headerlink" title="2.extern存储类型"></a>2.extern存储类型</h2><p>extern用来声明在当前文件中引用在当前项目中的其它文件中定义的全局变量。如果全局变量未被初始化，那么将被存在BBS区中，且在编译时，自动将其值赋值为0，如果已经被初始化，那么就被存在数据区中。全局变量，不管是否被初始化，其生命周期都是整个程序运行过程中，为了节省内存空间，在当前文件中使用extern来声明其它文件中定义的全局变量时，就不会再为其分配内存空间。</p>
<p>示例如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;  </span></span></span><br><span class="line">  </span><br><span class="line"><span class="keyword">int</span> i=<span class="number">5</span>; <span class="comment">//定义全局变量，并初始化  </span></span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">(<span class="keyword">void</span>)</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"in subfunction i=%d\n"</span>,i);  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;  </span></span></span><br><span class="line">  </span><br><span class="line"><span class="keyword">extern</span> i; <span class="comment">//声明引用全局变量i  </span></span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"in main i=%d\n"</span>,i);  </span><br><span class="line">    test();  </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> gcc -o <span class="built_in">test</span> test.c file.c  <span class="comment">#编译连接  </span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> ./<span class="built_in">test</span>  <span class="comment">#运行</span></span></span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">结果：  </span><br><span class="line">  </span><br><span class="line">in main i&#x3D;5  </span><br><span class="line">in subfunction i&#x3D;5</span><br></pre></td></tr></table></figure>

<h2 id="3-register存储类型"><a href="#3-register存储类型" class="headerlink" title="3.register存储类型"></a>3.register存储类型</h2><p>声明为register的变量在由内存调入到CPU寄存器后，则常驻在CPU的寄存器中，因此访问register变量将在很大程度上提高效率，因为省去了变量由内存调入到寄存器过程中的好几个指令周期。如下示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;  </span></span></span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">register</span> <span class="keyword">int</span> i,sum=<span class="number">0</span>;  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;<span class="number">10</span>;i++)  </span><br><span class="line">        sum=sum+<span class="number">1</span>;  </span><br><span class="line">  </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d\n"</span>,sum);  </span><br><span class="line">      </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是在 C++11之前，这个关键字在C++中的用法始终未变，只是随着硬件和编译器变得越来越复杂，这种<br>提示表明变量用得很多，编译器可对其做特殊处理。而且寄存器的数量是有限的！</p>
<h2 id="4-static存储类型"><a href="#4-static存储类型" class="headerlink" title="4.static存储类型"></a>4.static存储类型</h2><p>被声明为静态类型的变量，无论是全局的还是局部的，都存储在数据区中，其生命周期为整个程序，如果是静态局部变量，其作用域为一对{}内，如果是静态全局变量，其作用域为当前文件。静态变量如果没有被初始化，则自动初始化为0。静态变量只能够初始化一次。示例如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;  </span></span></span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sum</span><span class="params">(<span class="keyword">int</span> a)</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">auto</span> <span class="keyword">int</span> c=<span class="number">0</span>;  </span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> b=<span class="number">5</span>;  </span><br><span class="line">  </span><br><span class="line">    c++;  </span><br><span class="line">    b++;  </span><br><span class="line">  </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"a=%d,\tc=%d,\tb=%d\t"</span>,a,c,b);  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">return</span> (a+b+c);  </span><br><span class="line">&#125;  </span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">int</span> i;  </span><br><span class="line">    <span class="keyword">int</span> a=<span class="number">2</span>;  </span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;<span class="number">5</span>;i++)  </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"sum(a)=%d\n"</span>,sum(a));  </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> gcc -o <span class="built_in">test</span> test.c  </span></span><br><span class="line"><span class="meta">$</span><span class="bash"> ./<span class="built_in">test</span>   </span></span><br><span class="line">a=2,    c=1,    b=6 sum(a)=9  </span><br><span class="line">a=2,    c=1,    b=7 sum(a)=10  </span><br><span class="line">a=2,    c=1,    b=8 sum(a)=11  </span><br><span class="line">a=2,    c=1,    b=9	sum(a)=12  </span><br><span class="line">a=2,    c=1,    b=10 sum(a)=13</span><br></pre></td></tr></table></figure>

<h2 id="5-字符串常量"><a href="#5-字符串常量" class="headerlink" title="5.字符串常量"></a>5.字符串常量</h2><p>字符串常量存储在数据区中，其生存期为整个程序运行时间，但作用域为当前文件，示例如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;  </span></span></span><br><span class="line">  </span><br><span class="line"><span class="keyword">char</span> *a=<span class="string">"hello"</span>;  </span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">char</span> *c=<span class="string">"hello"</span>;  </span><br><span class="line">      </span><br><span class="line">    <span class="keyword">if</span>(a==c)  </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"yes,a==c\n"</span>);  </span><br><span class="line">    <span class="keyword">else</span>  </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"no,a!=c\n"</span>);  </span><br><span class="line">&#125;  </span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">char</span> *b=<span class="string">"hello"</span>;  </span><br><span class="line">    <span class="keyword">char</span> *d=<span class="string">"hello2"</span>;  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">if</span>(a==b)  </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"yes,a==b\n"</span>);  </span><br><span class="line">    <span class="keyword">else</span>  </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"no,a!=b\n"</span>);  </span><br><span class="line">      </span><br><span class="line">    test();  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">if</span>(a==d)  </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"yes,a==d\n"</span>);  </span><br><span class="line">    <span class="keyword">else</span>  </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"no,a!=d\n"</span>);  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> gcc -o <span class="built_in">test</span> test.c  </span></span><br><span class="line"><span class="meta">$</span><span class="bash"> ./<span class="built_in">test</span>   </span></span><br><span class="line">yes,a==b  </span><br><span class="line">yes,a==c  </span><br><span class="line">no,a!=d</span><br></pre></td></tr></table></figure>

<p><strong>由于1~4的类型在C/C++中是一样的，所以我用的全是C的代码。</strong></p>
<p><strong>但是在C++11中，<code>auto</code>和<code>register</code>两种都有了新的含义！！！</strong></p>
<p><em>C++11新增了一个工具，让编译器能够根据初始值的类型推断变量的类型。为此，它重新定义了 auto 的含义。auto是一个C 语言关键字，但很少使用，有关其以前的含义，请参阅第9 章。在初始化声明中， 如果使用关键字auto,而不指定变量的类型，编译器将把变量的类型设置成与初始值相同：</em></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> n = <span class="number">100</span>； <span class="comment">// n is int </span></span><br><span class="line"><span class="keyword">auto</span> x = <span class="number">1.5</span>； <span class="comment">// x is double </span></span><br><span class="line"><span class="keyword">auto</span> y = <span class="number">1.3</span>el2L； <span class="comment">// y is long double</span></span><br></pre></td></tr></table></figure>

<p><em>然而，自动推断类型并非为这种简单情况而设计的；事实上，如果将其用于这种简单情形，甚至可能让您误入歧途。例如，假设您要将x、y 和 z 都指定为double类型，并编写了如下代码：</em></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> x = <span class="number">0.0</span>； <span class="comment">// ok, x is double because 0.0 is double </span></span><br><span class="line"><span class="keyword">double</span> y = <span class="number">0</span>； <span class="comment">// ok, 0 automatically converted to 0.0 </span></span><br><span class="line"><span class="keyword">auto</span> z = <span class="number">0</span>; <span class="comment">// oops, z is int because 0 is int</span></span><br></pre></td></tr></table></figure>

<p><em>显式地声明类型时，将变量初始化0 (而不是0.0)不会导致任何问题，但采用自动类型推断时，这却会导致问题。</em><br><em>处理复杂类型，如标准模块库(STL)中的类型时，自动类型推断的有时才能显现出来。例如，对于下述C++98代码：</em></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">double</span>&gt; scores； </span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">double</span>&gt;::iterator pv = scores.<span class="built_in">begin</span>()；</span><br></pre></td></tr></table></figure>

<p><em>c++n允许您将其重写为下面这样：</em></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">double</span>&gt; scores; </span><br><span class="line"><span class="keyword">auto</span> pv = scores.<span class="built_in">begin</span>()；</span><br></pre></td></tr></table></figure>

<p><em>本书后面讨论相关的主题时，将再次提到</em></p>
<p><em>在 C++11之前，这个关键字在C++中的用法始终未变，只是随着硬件和编译器变得越来越复杂，这种提示表明变量用得很多，编译器可对其做特殊处理。在 C++11中，这种提示作用也失去了，关键字register 只是显式地指出变量是自动的。鉴于关键字register只能用于原本就是自动的变量，使用它的唯一原因是， 指出程序员想使用一个自动变量，这个变量的名称可能与外部变量相同。<strong>这与auto以前的用途完全相同。</strong> 然而，保留关键字register的重要原因是，避免使用了该关键字的现有代码非法。</em></p>
<p><em>简而言之，新<code>auto</code>用于自动推断类型，新<code>register</code>的用法则与旧<code>auto</code>一样。</em></p>
<h1 id="什么是栈？"><a href="#什么是栈？" class="headerlink" title="什么是栈？"></a>什么是栈？</h1><p>由于自动变量的数目是随着函数的开始和结束而增减的（上面是main函数），因此程序必须在运行时对自动变量进行管理。常用的方法是留出一段内存，将其视为栈，以管理变量的增减。之所以叫栈，是因为新数据被象征性的放在旧数据的上面，即相邻的内存单元。当程序使用完后，将其从栈中删除。栈的默认长度取决于实现，但是可以通过编译器修改。程序使用两个指针来控制栈，一个指向栈底—栈开始的地方；另一个指向栈顶—下一个内存单元。当函数被调用时，其自动变量被加入栈，栈顶指针指向下一个可用的内存单元。函数结束后，栈顶指针被重置为函数被调用之前指向的内存单元，从而释放函数调用使用的自动变量的内存。</p>
<p>栈是LIFO（后进先出）的，即后加入栈中的变量先被弹出。这种设计简化了参数传递。函数调用将其参数的值放在栈顶，然后重新设置栈顶指针。被调用的函数根据其形参描述来确定每个参数的地址。例如，图9.3表明，函数fib()被调用时，传递一个2 字节的int和一个4 字节的long。这些值被加入到栈中。当 fib()开始执行时，它将名称real和 tell同这两个值关联起来。当 fib()结束时，栈顶指针重新指向以 前的位置。新值没有被删除，但不再被标记，它们所占据的空间将被下一个将值加入到栈中的函数调用所<br>使用（图9.3做了简化，因为函数调用可能传递其他信息，如返回地址）。</p>
<p><img src="/2018/01/30/%E5%86%85%E5%AD%98%E7%A9%BA%E9%97%B4%E5%88%86%E9%85%8D/9.3.png" alt="9.3"></p>
<p>注意,栈是从高地址向低地址增长的，大小是有限的。</p>
<h2 id="栈和堆的区别"><a href="#栈和堆的区别" class="headerlink" title="栈和堆的区别"></a><strong>栈和堆的区别</strong></h2><p>前面已经介绍过，栈是由编译器在需要时分配的，不需要时自动清除的变量存储区。里面的变量通常是局部变量、函数参数等。堆是由malloc()函数（C++语言为new运算符）分配的内存块，内存释放由程序员手动控制，在C语言为free函数完成（C++中为delete）。栈和堆的主要区别有以下几点：</p>
<h3 id="（1）管理方式不同。"><a href="#（1）管理方式不同。" class="headerlink" title="（1）管理方式不同。"></a><strong>（1）管理方式不同。</strong></h3><p>栈编译器自动管理，无需程序员手工控制；而堆空间的申请释放工作由程序员控制，容易产生内存泄漏。</p>
<h3 id="（2）空间大小不同。"><a href="#（2）空间大小不同。" class="headerlink" title="（2）空间大小不同。"></a><strong>（2）空间大小不同。</strong></h3><p>栈是向低地址扩展的数据结构，是一块连续的内存区域。这句话的意思是栈顶的地址和栈的最大容量是系统预先规定好的，当申请的空间超过栈的剩余空间时，将提示溢出。因此，用户能从栈获得的空间较小。</p>
<p>堆是向高地址扩展的数据结构，是不连续的内存区域。因为系统是用链表来存储空闲内存地址的，且链表的遍历方向是由低地址向高地址。由此可见，堆获得的空间较灵活，也较大。栈中元素都是一一对应的，不会存在一个内存块从栈中间弹出的情况。</p>
<h3 id="（3）是否产生碎片。"><a href="#（3）是否产生碎片。" class="headerlink" title="（3）是否产生碎片。"></a><strong>（3）是否产生碎片。</strong></h3><p>对于堆来讲，频繁的malloc/free（new/delete）势必会造成内存空间的不连续，从而造成大量的碎片，使程序效率降低（虽然程序在退出后操作系统会对内存进行回收管理）。对于栈来讲，则不会存在这个问题。</p>
<h3 id="（4）增长方向不同。"><a href="#（4）增长方向不同。" class="headerlink" title="（4）增长方向不同。"></a><strong>（4）增长方向不同。</strong></h3><p>堆的增长方向是向上的，即向着内存地址增加的方向；栈的增长方向是向下的，即向着内存地址减小的方向。</p>
<h3 id="（5）分配方式不同。"><a href="#（5）分配方式不同。" class="headerlink" title="（5）分配方式不同。"></a><strong>（5）分配方式不同。</strong></h3><p>堆都是程序中由malloc()函数动态申请分配并由free()函数释放的；栈的分配和释放是由编译器完成的，栈的动态分配由alloca()函数完成，但是栈的动态分配和堆是不同的，他的动态分配是由编译器进行申请和释放的，无需手工实现。</p>
<h3 id="（6）分配效率不同。"><a href="#（6）分配效率不同。" class="headerlink" title="（6）分配效率不同。"></a><strong>（6）分配效率不同。</strong></h3><p>栈是机器系统提供的数据结构，计算机会在底层对栈提供支持：分配专门的寄存器存放栈的地址，压栈出栈都有专门的指令执行。堆则是C函数库提供的，它的机制很复杂，例如为了分配一块内存，库函数会按照一定的算法（具体的算法可以参考数据结构/操作系统）在堆内存中搜索可用的足够大的空间，如果没有足够大的空间（可能是由于内存碎片太多），就有需要操作系统来重新整理内存空间，这样就有机会分到足够大小的内存，然后返回。显然，堆的效率比栈要低得多。</p>
<h2 id="自由存储区是什么？"><a href="#自由存储区是什么？" class="headerlink" title="自由存储区是什么？"></a>自由存储区是什么？</h2><table>
<thead>
<tr>
<th>内存划分</th>
<th>运行时结构</th>
</tr>
</thead>
<tbody><tr>
<td>栈（stack）</td>
<td>栈</td>
</tr>
<tr>
<td>堆（heap）</td>
<td>堆</td>
</tr>
<tr>
<td>全局/静态存储区</td>
<td>未初始化数据（BSS）</td>
</tr>
<tr>
<td>自由存储区</td>
<td>数据区（data）</td>
</tr>
<tr>
<td>常量存储区</td>
<td>代码区</td>
</tr>
</tbody></table>
<p>常量存储区和全局／静态存储区对应着数据区。BSS和代码区没有对应。而自由存储区在某种程度上可以视为堆的一部分。</p>
<h3 id="“free-store”-VS-“heap”"><a href="#“free-store”-VS-“heap”" class="headerlink" title="“free store” VS “heap”"></a>“free store” VS “heap”</h3><p>当我问你C++的内存布局时，你大概会回答：</p>
<p><em>“在C++中，内存区分为5个区，分别是堆、栈、自由存储区、全局/静态存储区、常量存储区”。</em></p>
<p>如果我接着问你自由存储区与堆有什么区别，你或许这样回答：</p>
<p><em>“malloc在堆上分配的内存块，使用free释放内存，而new所申请的内存则是在自由存储区上，使用delete来释放。”</em></p>
<p>这样听起来似乎也没错，但如果我接着问：</p>
<p><em>自由存储区与堆是两块不同的内存区域吗？它们有可能相同吗？</em></p>
<p>你可能就懵了。</p>
<p>事实上，我在网上看的很多博客，划分自由存储区与堆的分界线就是new/delete与malloc/free。然而，尽管C++标准没有要求，但很多编译器的new/delete都是以malloc/free为基础来实现的。那么请问：借以malloc实现的new，所申请的内存是在堆上还是在自由存储区上？</p>
<p>从技术上来说，堆（heap）是C语言和操作系统的术语。堆是操作系统所维护的一块特殊内存，它提供了动态分配的功能，当运行程序调用malloc()时就会从中分配，稍后调用free可把内存交还。而自由存储是C++中通过new和delete动态分配和释放对象的抽象概念，通过new来申请的内存区域可称为自由存储区。<strong><u>基本上，所有的C++编译器默认使用堆来实现自由存储，也即是缺省的全局运算符new和delete也许会按照malloc和free的方式来被实现，这时藉由new运算符分配的对象，说它在堆上也对，说它在自由存储区上也正确。</u></strong>但程序员也可以通过重载操作符，改用其他内存来实现自由存储，例如全局变量做的对象池，这时自由存储区就区别于堆了。我们所需要记住的就是：</p>
<p>堆是操作系统维护的一块内存，而自由存储是C++中通过new与delete动态分配和释放对象的抽象概念。堆与自由存储区并不等价。</p>
<h3 id="问题的来源"><a href="#问题的来源" class="headerlink" title="问题的来源"></a>问题的来源</h3><p>再回过头来来看看这个问题的起源在哪里。最先我们使用C语言的时候，并没有这样的争议，很明确地知道malloc/free是在堆上进行内存操作。直到我们在Bjarne Stroustrup的书籍中数次看到free store （自由存储区）,说实话，我一直把自由存储区等价于堆。而在Herb Sutter的《exceptional C++》中，明确指出了free store（自由存储区） 与 heap（堆） 是有区别的。关于自由存储区与堆是否等价的问题讨论，大概就是从这里开始的：</p>
<p><em>Free Store</em><br><em>The free store is one of the two dynamic memory areas, allocated/freed by new/delete. Object lifetime can be less than the time the storage is allocated; that is, free store objects can have memory allocated without being immediately initialized, and can be destroyed without the memory being immediately deallocated. During the period when the storage is allocated but outside the object’s lifetime, the storage may be accessed and manipulated through a void</em> but none of the proto-object’s nonstatic members or member functions may be accessed, have their addresses taken, or be otherwise manipulated.*</p>
<p><em>Heap</em><br><em>The heap is the other dynamic memory area, allocated/freed by malloc/free and their variants. Note that while the default global new and delete might be implemented in terms of malloc and free by a particular compiler, the heap is not the same as free store and memory allocated in one area cannot be safely deallocated in the other. Memory allocated from the heap can be used for objects of class type by placement-new construction and explicit destruction. If so used, the notes about free store object lifetime apply similarly here.</em></p>
<p>来源：<a href="http://www.gotw.ca/gotw/009.htm" target="_blank" rel="noopener">http://www.gotw.ca/gotw/009.htm</a></p>
<p>作者也指出，之所以把堆与自由存储区要分开来，是因为在C++标准草案中关于这两种区域是否有联系的问题一直很谨慎地没有给予详细说明，而且特定情况下new和delete是按照malloc和free来实现，或者说是放过来malloc和free是按照new和delete来实现的也没有定论。这两种内存区域的运作方式不同、访问方式不同，所以应该被当成不一样的东西来使用。</p>
<h3 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h3><ul>
<li>自由存储是C++中通过new与delete动态分配和释放对象的<strong>抽象概念</strong>，而堆（heap）是C语言和操作系统的术语，是操作系统维护的一块动态分配内存。</li>
<li>new所申请的内存区域在C++中称为自由存储区。藉由堆实现的自由存储，可以说new所申请的内存区域在堆上。</li>
<li>堆与自由存储区还是有区别的，它们并非等价。</li>
</ul>
<p>假如你来自C语言，从没接触过C++；或者说你一开始就熟悉C++的自由储存概念，而从没听说过C语言的malloc，可能你就不会陷入“自由存储区与堆好像一样，好像又不同”这样的迷惑之中。这就像Bjarne Stroustrup所说的：</p>
<blockquote>
<p>usually because they come from a different language background.</p>
</blockquote>
<p>大概只是语言背景不同罢了。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><table>
<thead>
<tr>
<th>分配方式1</th>
<th>分配方式2</th>
<th>运行时结构</th>
</tr>
</thead>
<tbody><tr>
<td>栈</td>
<td>栈</td>
<td>栈</td>
</tr>
<tr>
<td>堆</td>
<td>堆</td>
<td>堆</td>
</tr>
<tr>
<td>自由存储区</td>
<td>文字常量区（所有常量）</td>
<td>BSS</td>
</tr>
<tr>
<td>全局/静态存储区</td>
<td>全局/静态存储区</td>
<td>Data</td>
</tr>
<tr>
<td>常量存储区（包括字符）</td>
<td>程序代码区</td>
<td>代码区</td>
</tr>
</tbody></table>
<p><strong>2</strong>与<strong>1</strong>相比就是把自由存储区加到了堆中，再加了个代码区。两种方式孰优孰劣？</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>在AWT中绘图</title>
    <url>/2018/07/28/%E5%9C%A8AWT%E4%B8%AD%E7%BB%98%E5%9B%BE/</url>
    <content><![CDATA[<p>使用AWT做点简单图形.</p>
<a id="more"></a>

<h1 id="画图实现原理"><a href="#画图实现原理" class="headerlink" title="画图实现原理"></a>画图实现原理</h1><p>&emsp;在Component类里提供了和绘图有关的三个方法。</p>
<ul>
<li>paint(Graphics g)：绘制组件的外观。</li>
<li>update(Graphics g)：调用paint()方法，刷新组件外观。</li>
<li>repaint()：调用update()方法，刷新组件外观.</li>
</ul>
<p>&emsp;上面三个方法的调用关系为：repaint()方法调用update()方法,update()方法调用paint()方法。<br>Container类中的update()方法先以组件的背景色填充整个组件区域，然后调用paint()方法重画组件。<br>Container类的 update()方法代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">update</span><span class="params">(Graphics g)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (isShowing()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (! (peer <span class="keyword">instanceof</span> LightweightPeer)) &#123;</span><br><span class="line">                g.clearRect(<span class="number">0</span>, <span class="number">0</span>, width, height);</span><br><span class="line">            &#125;</span><br><span class="line">            paint(g);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>



<p>&emsp;普通组件(Component)的update()方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">update</span><span class="params">(Graphics g)</span> </span>&#123;</span><br><span class="line">        paint(g);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;下图是三者调用关系:</p>
<p><img src="/2018/07/28/%E5%9C%A8AWT%E4%B8%AD%E7%BB%98%E5%9B%BE/%E5%85%B3%E7%B3%BB.png" alt="关系"></p>
<p>&emsp;从 图中可以看出，程序不应该主动调用组件的paint()和 update()方法，这两个方法都由AWT系统负责调用。如果程序希望AWT系统重新绘制该组件，则调用该组件的repaint()方法即可。而paint()和update()方法通常被重写。 在通常情况下，程序通过重写paint()方法实现在AWT组件上绘图。重写update()或 paint()方法时，该方法里包含了一个Graphics类型的参数，通过该Graphics参数就可以实现绘图功能。</p>
<h1 id="使用Graphics类"><a href="#使用Graphics类" class="headerlink" title="使用Graphics类"></a>使用Graphics类</h1><p>&emsp;Graphics类提供了如下几个方法用于绘制几何图形和位图。</p>
<ul>
<li>drawLine()：绘制直线。</li>
<li>drawString()：绘制字符串。</li>
<li>drawRect()：绘制矩形。</li>
<li>drawRoundRect(): 绘制圆角矩形。</li>
<li>drawOval()：绘制椭圆形状。</li>
<li>drawPolygon()：绘制多边形边框。</li>
<li>drawArc()：绘制一段圆弧(可能是椭圆的圆弧)。</li>
<li>drawPolyline()：绘制折线。</li>
<li>fillRect()：填充一个矩形区域。</li>
<li>fillRoundRect()：填充一个圆角矩形区域。</li>
<li>fillOval()：填充椭圆区域。</li>
<li>fillPolygon()：填充一个多边形区域。</li>
<li>fillArcO：填充圆弧和圆弧两个端点到中心连线所包围的区域。</li>
<li>drawlmage()：绘制位图。</li>
</ul>
<p>&emsp;除此之外，Graphics还提供了setColor()和setFont()两个方法用于设置画笔的颜色和字体(仅当绘制字符串时有效)，其中setColor()方法需要传入一个Color参数，它可以使用RGB、CMYK等方式设 置一个颜色；而 setFont()方法需要传入一个Font参数，Font参数需要指定字体名、字体样式、字体 大小三个属性。</p>
<p>&emsp;AWT普通组件也可以通过Color()和 Font()方法来改变它的前景色和字体。除此之外，所有组件都有一个setBackground()方法用于设置组件的背景色。</p>
<p>&emsp;AWT专门提供一个Canvas类作为绘图的画布，程序可以通过创建Canvas的子类，并重写它的paint() 方法来实现绘图。下面程序示范了一个简单的绘图程序。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"><span class="keyword">import</span> java.util.Random;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/28 13:46</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleDraw</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String RECT_SHAPE = <span class="string">"rect"</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String OVAL_SHAPE = <span class="string">"oval"</span>;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"绘图"</span>);</span><br><span class="line">    <span class="keyword">private</span> JButton rect = <span class="keyword">new</span> JButton(<span class="string">"绘制矩形"</span>);</span><br><span class="line">    <span class="keyword">private</span> JButton oval = <span class="keyword">new</span> JButton(<span class="string">"绘制圆形"</span>);</span><br><span class="line">    <span class="keyword">private</span> String shape = <span class="string">""</span>;</span><br><span class="line">    <span class="keyword">private</span> MyCanvas drawArea = <span class="keyword">new</span> MyCanvas();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Panel p = <span class="keyword">new</span> Panel();</span><br><span class="line">        rect.addActionListener(e -&gt; &#123;</span><br><span class="line">            shape = RECT_SHAPE;</span><br><span class="line">            drawArea.repaint();</span><br><span class="line">        &#125;);</span><br><span class="line">        oval.addActionListener(e -&gt; &#123;</span><br><span class="line">            shape = OVAL_SHAPE;</span><br><span class="line">            drawArea.repaint();</span><br><span class="line">        &#125;);</span><br><span class="line">        p.add(rect);</span><br><span class="line">        p.add(oval);</span><br><span class="line"><span class="comment">//        Dimension用于设置组件的首选大小</span></span><br><span class="line">        drawArea.setPreferredSize(<span class="keyword">new</span> Dimension(<span class="number">250</span>, <span class="number">180</span>));</span><br><span class="line">        f.add(drawArea);</span><br><span class="line">        f.add(p, BorderLayout.SOUTH);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">MyCanvas</span> <span class="keyword">extends</span> <span class="title">Canvas</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">paint</span><span class="params">(Graphics g)</span> </span>&#123;</span><br><span class="line">            Random rand = <span class="keyword">new</span> Random();</span><br><span class="line">            <span class="keyword">if</span> (shape.equals(RECT_SHAPE)) &#123;</span><br><span class="line">                g.setColor(<span class="keyword">new</span> Color(<span class="number">42</span>, <span class="number">46</span>, <span class="number">220</span>));</span><br><span class="line">                g.drawRect(rand.nextInt(<span class="number">200</span>), rand.nextInt(<span class="number">120</span>), <span class="number">40</span>, <span class="number">60</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (shape.equals(OVAL_SHAPE)) &#123;</span><br><span class="line">                g.setColor(<span class="keyword">new</span> Color(<span class="number">200</span>, <span class="number">29</span>, <span class="number">30</span>));</span><br><span class="line">                g.fillOval(rand.nextInt(<span class="number">200</span>), rand.nextInt(<span class="number">12</span>), <span class="number">50</span>, <span class="number">40</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> SimpleDraw().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;Java也可用于开发一些动画。所谓动画，就是间隔一定的时间(通常小于0.1秒)重新绘制新的图像, 两次绘制的图像之间差异较小，肉眼看起来就成了所谓的动画。为了实现间隔一定的时间就重新调用组件的repaint()方法，可以借助于Swing提供的Timer类，Timer类是一个定时器，它有如下一个构造器。</p>
<ul>
<li>Timer(int delay, ActionListener listener)：每间隔delay毫秒 ，系统自动触发ActionListener监听器里的事件处理器(actionPerformed()方法)。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.ActionListener;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.KeyAdapter;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.KeyEvent;</span><br><span class="line"><span class="keyword">import</span> java.util.Random;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PinBall</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 桌面宽度 &#123;<span class="doctag">@value</span>&#125;</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> TABLE_WIDTH = <span class="number">300</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 桌面高度 &#123;<span class="doctag">@value</span>&#125;</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> TABLE_HEIGHT = <span class="number">400</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 球拍的垂直位置 &#123;<span class="doctag">@value</span>&#125;</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> RACKET_Y = <span class="number">340</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 球拍的高度 &#123;<span class="doctag">@value</span>&#125;</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> RACKET_HEIGHT = <span class="number">20</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 球拍的宽度 &#123;<span class="doctag">@value</span>&#125;</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> RACKET_WIDTH = <span class="number">60</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 球的大小 &#123;<span class="doctag">@value</span>&#125;</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> BALL_SIZE = <span class="number">16</span>;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"探求游戏"</span>);</span><br><span class="line">    Random rand = <span class="keyword">new</span> Random();</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 小球的纵向速度</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> ySpeed = <span class="number">10</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 返回一个-0.5-0.5的比率，用于控制小球的运行方向</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">double</span> xyRate = rand.nextDouble() - <span class="number">0.5</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 小球的横向速度</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> xSpeed = (<span class="keyword">int</span>) (ySpeed * xyRate * <span class="number">2</span>);</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 水平坐标</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> ballX = rand.nextInt(<span class="number">200</span>) + <span class="number">20</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 纵向坐标</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> ballY = rand.nextInt(<span class="number">10</span>) + <span class="number">20</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 球拍的水平位置</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> racketX = rand.nextInt(<span class="number">200</span>);</span><br><span class="line">    <span class="keyword">private</span> MyCanvas tableArea = <span class="keyword">new</span> MyCanvas();</span><br><span class="line">    Timer timer;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">boolean</span> isLose = <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        tableArea.setPreferredSize(<span class="keyword">new</span> Dimension(TABLE_WIDTH, TABLE_HEIGHT));</span><br><span class="line">        f.add(tableArea);</span><br><span class="line">        KeyAdapter keyProcessor = <span class="keyword">new</span> KeyAdapter() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">keyPressed</span><span class="params">(KeyEvent e)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">if</span> (e.getKeyCode() == KeyEvent.VK_LEFT) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (racketX &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                        racketX -= <span class="number">10</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (e.getKeyCode() == KeyEvent.VK_RIGHT) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (racketX &lt; TABLE_HEIGHT - RACKET_WIDTH) &#123;</span><br><span class="line">                        racketX += <span class="number">10</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        f.addKeyListener(keyProcessor);</span><br><span class="line">        tableArea.addKeyListener(keyProcessor);</span><br><span class="line"></span><br><span class="line">        ActionListener taskPerformer = evt -&gt; &#123;</span><br><span class="line">            <span class="keyword">if</span> (ballX &lt;= <span class="number">0</span> || ballX &gt;= TABLE_WIDTH - BALL_SIZE) &#123;</span><br><span class="line">                xSpeed = -xSpeed;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (ballY &gt;= RACKET_Y - BALL_SIZE &amp;&amp; (ballX &lt; racketX || ballX &gt; racketX + RACKET_WIDTH)) &#123;</span><br><span class="line">                timer.stop();</span><br><span class="line">                isLose = <span class="keyword">true</span>;</span><br><span class="line">                tableArea.repaint();</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (ballY &lt;= <span class="number">0</span> || (ballY &gt;= RACKET_Y - BALL_SIZE &amp;&amp; ballX &lt;= racketX + RACKET_WIDTH)) &#123;</span><br><span class="line">                ySpeed = -ySpeed;</span><br><span class="line">            &#125;</span><br><span class="line">            ballY += ySpeed;</span><br><span class="line">            ballX += xSpeed;</span><br><span class="line">            tableArea.repaint();</span><br><span class="line">        &#125;;</span><br><span class="line">        timer = <span class="keyword">new</span> Timer(<span class="number">100</span>, taskPerformer);</span><br><span class="line">        timer.start();</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> PinBall().init();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">MyCanvas</span> <span class="keyword">extends</span> <span class="title">Canvas</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">paint</span><span class="params">(Graphics g)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (isLose) &#123;</span><br><span class="line">                g.setColor(<span class="keyword">new</span> Color(<span class="number">255</span>, <span class="number">0</span>, <span class="number">0</span>));</span><br><span class="line">                g.setFont(<span class="keyword">new</span> Font(<span class="string">"Times"</span>, Font.BOLD, <span class="number">30</span>));</span><br><span class="line">                g.drawString(<span class="string">"游戏已结束"</span>, <span class="number">50</span>, <span class="number">200</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                g.setColor(<span class="keyword">new</span> Color(<span class="number">240</span>, <span class="number">240</span>, <span class="number">80</span>));</span><br><span class="line">                g.fillOval(ballX, ballY, BALL_SIZE, BALL_SIZE);</span><br><span class="line">                g.setColor(<span class="keyword">new</span> Color(<span class="number">80</span>, <span class="number">80</span>, <span class="number">200</span>));</span><br><span class="line">                g.fillRect(racketX, RACKET_Y, RACKET_WIDTH, RACKET_HEIGHT);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>反射</title>
    <url>/2020/06/07/%E5%8F%8D%E5%B0%84/</url>
    <content><![CDATA[<p>非常重要的反射.</p>
<a id="more"></a>

<p>下面是反射的用处的一些展示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.Reflect;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.lang.reflect.Constructor;</span><br><span class="line"><span class="keyword">import</span> java.lang.reflect.Field;</span><br><span class="line"><span class="keyword">import</span> java.lang.reflect.Method;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="meta">@org</span>.junit.Test</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">//正常情况下，私有属性和方法无法使用</span></span><br><span class="line">        Person person = <span class="keyword">new</span> Person(<span class="string">"Tom"</span>, <span class="number">12</span>);</span><br><span class="line">        person.age = <span class="number">10</span>;</span><br><span class="line">        System.out.println(person.toString());</span><br><span class="line">        person.showPerson();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@org</span>.junit.Test</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        Class&lt;Person&gt; personClass = Person<span class="class">.<span class="keyword">class</span></span>;</span><br><span class="line">        <span class="comment">//通过反射创建Person类的对象</span></span><br><span class="line">        Constructor&lt;Person&gt; con = personClass.getConstructor(String<span class="class">.<span class="keyword">class</span>, <span class="title">int</span>.<span class="title">class</span>)</span>;</span><br><span class="line">        Person tom = con.newInstance(<span class="string">"Tom"</span>, <span class="number">13</span>);</span><br><span class="line">        <span class="comment">//用反射调属性</span></span><br><span class="line">        Field age = personClass.getDeclaredField(<span class="string">"age"</span>);</span><br><span class="line">        age.set(tom, <span class="number">11</span>);</span><br><span class="line">        <span class="comment">//用反射调方法</span></span><br><span class="line">        Method show = personClass.getDeclaredMethod(<span class="string">"showPerson"</span>);</span><br><span class="line">        show.invoke(tom);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//用反射来访问私有构造器</span></span><br><span class="line">        Constructor&lt;Person&gt; con1 = personClass.getDeclaredConstructor(String<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">        con1.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">        Person jerry = con1.newInstance(<span class="string">"Jerry"</span>);</span><br><span class="line">        <span class="comment">//用反射修改私有属性</span></span><br><span class="line">        Field name = personClass.getDeclaredField(<span class="string">"name"</span>);</span><br><span class="line">        name.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">        name.set(jerry, <span class="string">"Buluto"</span>);</span><br><span class="line">        System.out.println(jerry);</span><br><span class="line">        <span class="comment">//调私有方法</span></span><br><span class="line">        Method showNation = personClass.getDeclaredMethod(<span class="string">"showNation"</span>, String<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">        showNation.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">        String nation = (String) showNation.invoke(jerry, <span class="string">"china"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当使用Java命令将字节码文件加载到内存后，内存中的这个类就称为运行时类。一个Class的实例就对应这一个运行时类。获取Class的实例的方式有四种：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//通过不同方法获得的Class实例其实是同一个，加载到内存中的运行时类会缓存一段时间</span></span><br><span class="line"><span class="comment">//方式一</span></span><br><span class="line">Class personclass1 = Person<span class="class">.<span class="keyword">class</span></span>;</span><br><span class="line"><span class="comment">//方式二</span></span><br><span class="line">Person p = <span class="keyword">new</span> Person();</span><br><span class="line">Class personclass2 = p.getClass();</span><br><span class="line"><span class="comment">//方式三</span></span><br><span class="line">Class personclass3 = Class.forname(<span class="string">"com.study.Reflect.Person"</span>)<span class="comment">//类名写完整</span></span><br><span class="line"><span class="comment">//方式四，使用类的加载器：ClassLoader</span></span><br><span class="line">ClassLoader classloader = Test<span class="class">.<span class="keyword">class</span>.<span class="title">getClassLoader</span>()</span>;</span><br><span class="line">Class personclass4 = classloader.loadClass(<span class="string">"com.study.Reflect.Person"</span>);</span><br></pre></td></tr></table></figure>

<p>用反射能够获取到这个运行时类的很多信息，包括父类的，接口，注解的等，下面先定义一个Person类和其父类，接口，注解，方便演示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.Reflect;</span><br><span class="line"></span><br><span class="line"><span class="meta">@MyAnnotation</span>(value = <span class="string">"hi"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> <span class="keyword">extends</span> <span class="title">Creature</span>&lt;<span class="title">String</span>&gt; <span class="keyword">implements</span> <span class="title">Comparable</span>&lt;<span class="title">String</span>&gt;, <span class="title">MyInterface</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Person</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@MyAnnotation</span>(value = <span class="string">"abc"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Person</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Person(String name, <span class="keyword">int</span> age) &#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@MyAnnotation</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> String <span class="title">show</span><span class="params">(String nation)</span> </span>&#123;</span><br><span class="line">        System.out.println((<span class="string">"我的国籍是："</span> + nation));</span><br><span class="line">        <span class="keyword">return</span> nation;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">display</span><span class="params">(String interests)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> interests;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"I'm people"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(String o)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.Reflect;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.Serializable;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Creature</span>&lt;<span class="title">T</span>&gt; <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">char</span> gender;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">double</span> weight;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">breathe</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"呼吸"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">eat</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"吃"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.Reflect;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyInterface</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">info</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.Reflect;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.lang.annotation.Retention;</span><br><span class="line"><span class="keyword">import</span> java.lang.annotation.RetentionPolicy;</span><br><span class="line"><span class="keyword">import</span> java.lang.annotation.Target;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="keyword">static</span> java.lang.annotation.ElementType.*;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Target</span>(&#123;TYPE, FIELD, METHOD, PARAMETER, CONSTRUCTOR, LOCAL_VARIABLE&#125;)<span class="comment">//可以修饰的目标</span></span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)<span class="comment">//生命周期，RUNTIME代表注解运行时加载到内存</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> MyAnnotation &#123;</span><br><span class="line">     <span class="function">String <span class="title">value</span><span class="params">()</span> <span class="keyword">default</span> "hello"</span>;<span class="comment">//注解属性，默认值为hello</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面两个测试获取了Person的运行时类各种属性的一些信息：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test2</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Class&lt;Person&gt; personClass = Person<span class="class">.<span class="keyword">class</span></span>;</span><br><span class="line">        Field[] fields = personClass.getFields();<span class="comment">//获取运行时类和其父类中public的属性</span></span><br><span class="line">        <span class="keyword">for</span> (Field f : fields) &#123;</span><br><span class="line">            System.out.println(f);</span><br><span class="line">            <span class="comment">//public int com.ylxianzhe.Reflect.Person.id</span></span><br><span class="line">            <span class="comment">//public double com.ylxianzhe.Reflect.Creature.weight</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Field[] declaredFields = personClass.getDeclaredFields();<span class="comment">//获取运行时类的所有属性，不包含父类的</span></span><br><span class="line">        <span class="keyword">for</span> (Field declaredField : declaredFields) &#123;</span><br><span class="line">            System.out.println(declaredField);</span><br><span class="line">            <span class="comment">//private java.lang.String com.ylxianzhe.Reflect.Person.name</span></span><br><span class="line">            <span class="comment">//int com.ylxianzhe.Reflect.Person.age</span></span><br><span class="line">            <span class="comment">//public int com.ylxianzhe.Reflect.Person.id</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Class&lt;Person&gt; personClass = Person<span class="class">.<span class="keyword">class</span></span>;</span><br><span class="line">        Field[] declaredFields = personClass.getDeclaredFields();</span><br><span class="line">        <span class="keyword">for</span> (Field f : declaredFields) &#123;</span><br><span class="line">            <span class="comment">//获取权限修饰符</span></span><br><span class="line">            System.out.print(Modifier.toString(f.getModifiers())+<span class="string">'\t'</span>);</span><br><span class="line">            <span class="comment">//获取数据类型</span></span><br><span class="line">            Class&lt;?&gt; type = f.getType();</span><br><span class="line">            System.out.print(type.getName()+<span class="string">'\t'</span>);</span><br><span class="line">            <span class="comment">//获取变量名</span></span><br><span class="line">            System.out.println(f.getName());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面获取了方法的一些信息：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test3</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Class&lt;Person&gt; personClass = Person<span class="class">.<span class="keyword">class</span></span>;</span><br><span class="line">    Method[] methods = personClass.getMethods();<span class="comment">//获取运行时类和父类的public方法</span></span><br><span class="line">    <span class="keyword">for</span> (Method method : methods) &#123;</span><br><span class="line">        System.out.println(method);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Method[] declaredMethods = personClass.getDeclaredMethods();<span class="comment">//获取运行时类中声明的所有方法</span></span><br><span class="line">    <span class="keyword">for</span> (Method declaredMethod : declaredMethods) &#123;</span><br><span class="line">        System.out.println(declaredMethod);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其它信息：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test4</span><span class="params">()</span> </span>&#123;</span><br><span class="line">Class&lt;Person&gt; personClass = Person<span class="class">.<span class="keyword">class</span></span>;</span><br><span class="line">Method[] declaredMethods = personClass.getDeclaredMethods();<span class="comment">//拿到方法列表</span></span><br><span class="line"><span class="keyword">for</span> (Method method : declaredMethods) &#123;</span><br><span class="line">    Annotation[] annotations = method.getAnnotations();<span class="comment">//拿到每个方法的注解列表，注解必须要RUNTIME</span></span><br><span class="line">    <span class="keyword">for</span> (Annotation annotation : annotations) &#123;</span><br><span class="line">        System.out.print(annotation);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//获取方法的修饰符</span></span><br><span class="line">    System.out.print(Modifier.toString(method.getModifiers()) + <span class="string">"\t"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//获取方法的返回值类型</span></span><br><span class="line">    System.out.print(method.getReturnType() + <span class="string">"\t"</span>);</span><br><span class="line">    System.out.println();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//获取获取方法名</span></span><br><span class="line">    System.out.print(method.getName());</span><br><span class="line">    System.out.print(<span class="string">'('</span>);</span><br><span class="line">    <span class="comment">//获取形参列表</span></span><br><span class="line">    Class&lt;?&gt;[] parameterTypes = method.getParameterTypes();</span><br><span class="line">    <span class="keyword">if</span> (!(parameterTypes.length == <span class="number">0</span>)) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; parameterTypes.length; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i == parameterTypes.length - <span class="number">1</span>) &#123;</span><br><span class="line">                System.out.print(parameterTypes[i].getName() + <span class="string">"args_"</span> + i);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.print(parameterTypes[i].getName()+<span class="string">"args_"</span>+i+<span class="string">","</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.print(<span class="string">')'</span>);</span><br><span class="line">    <span class="comment">//获取异常数组</span></span><br><span class="line">    Class&lt;?&gt;[] exceptionTypes = method.getExceptionTypes();</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//获取构造器数组，构造器是public的,不包括父类的</span></span><br><span class="line">Constructor&lt;?&gt;[] constructors = personClass.getConstructors();</span><br><span class="line"><span class="comment">//获取运行时类中的所有构造器</span></span><br><span class="line">Constructor&lt;?&gt;[] declaredConstructors = personClass.getDeclaredConstructors();</span><br><span class="line"><span class="comment">//获取父类</span></span><br><span class="line">Class&lt;? <span class="keyword">super</span> Person&gt; superclass = personClass.getSuperclass();</span><br><span class="line">System.out.println(superclass);<span class="comment">//com.ylxianzhe.Reflect.Creature</span></span><br><span class="line"><span class="comment">//获取带泛型的父类</span></span><br><span class="line">Type genericSuperclass = personClass.getGenericSuperclass();</span><br><span class="line">System.out.println(genericSuperclass);<span class="comment">//com.ylxianzhe.Reflect.Creature&lt;java.lang.String&gt;</span></span><br><span class="line"><span class="comment">//获取带泛型的父类的泛型,将Type类型的genericSuperclass强转为ParameterizedType</span></span><br><span class="line"><span class="comment">//关于Type参见：https://www.cnblogs.com/linghu-java/p/8067886.html</span></span><br><span class="line">ParameterizedType parameterizedType= (ParameterizedType) genericSuperclass;</span><br><span class="line">Type[] actualTypeArguments = parameterizedType.getActualTypeArguments();</span><br><span class="line">System.out.println(actualTypeArguments[<span class="number">0</span>].getTypeName());</span><br><span class="line">System.out.println(((Class)actualTypeArguments[<span class="number">0</span>]).getSimpleName());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test5</span><span class="params">()</span> </span>&#123;</span><br><span class="line">Class&lt;Person&gt; personClass = Person<span class="class">.<span class="keyword">class</span></span>;</span><br><span class="line"><span class="comment">//获取接口,不能拿到父类实现额接口</span></span><br><span class="line">Class&lt;?&gt;[] interfaces = personClass.getInterfaces();</span><br><span class="line"><span class="keyword">for</span> (Class&lt;?&gt; aClass : interfaces) &#123;</span><br><span class="line">    System.out.println(aClass);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//得打父类的接口</span></span><br><span class="line">Class&lt;?&gt;[] interfaces1 = personClass.getSuperclass().getInterfaces();</span><br><span class="line"><span class="comment">//获取运行时类的所在包</span></span><br><span class="line">Package aPackage = personClass.getPackage();</span><br><span class="line">System.out.println(aPackage);</span><br><span class="line"><span class="comment">//获取注解</span></span><br><span class="line">Annotation[] annotations = personClass.getAnnotations();</span><br><span class="line"><span class="keyword">for</span> (Annotation annotation : annotations) &#123;</span><br><span class="line">    System.out.println(annotation);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是获取指定结构的方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test6</span><span class="params">()</span> <span class="keyword">throws</span>  Exception</span>&#123;</span><br><span class="line">    Class&lt;Person&gt; personClass = Person<span class="class">.<span class="keyword">class</span></span>;</span><br><span class="line">    <span class="comment">//创建运行时类的对象,新方式，上面的过时了</span></span><br><span class="line">    Person person = personClass.getDeclaredConstructor().newInstance();</span><br><span class="line">    <span class="comment">//获取指定的属性，要求是public的</span></span><br><span class="line">    Field id = personClass.getField(<span class="string">"id"</span>);</span><br><span class="line">    <span class="comment">//设置属性值,set方法参数一指定设置的对象，参数二指定设置值</span></span><br><span class="line">    id.set(person, <span class="number">123</span>);</span><br><span class="line">    <span class="comment">//获得指定对象的属性值</span></span><br><span class="line">    <span class="keyword">int</span> pId = (<span class="keyword">int</span>) id.get(person);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//以上方法不常用</span></span><br><span class="line">    <span class="comment">//用getDeclaredField来获取运行时类的所有方法，包括私有的</span></span><br><span class="line">    Field name = personClass.getDeclaredField(<span class="string">"name"</span>);</span><br><span class="line">    <span class="comment">//要想使用私有的还需设置权限</span></span><br><span class="line">    name.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">    name.set(person,<span class="string">"Tom"</span>);</span><br><span class="line">    System.out.println(name.get(person));</span><br><span class="line"></span><br><span class="line">    <span class="comment">//获取指定的方法,第一个参数指定方法名，后面的可变形参指定方法的参数列表</span></span><br><span class="line">    Method show = personClass.getDeclaredMethod(<span class="string">"show"</span>, String<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    show.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">    <span class="comment">//invoke第一个参数是方法的调用者，后面的可变形参指定实参列表</span></span><br><span class="line">    <span class="comment">//invoke方法的返回值就是对应类中调用方法的返回值</span></span><br><span class="line">    String chn = (String) show.invoke(person, <span class="string">"CHN"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//调用静态方法</span></span><br><span class="line">    Method showDesc = personClass.getDeclaredMethod(<span class="string">"showDesc"</span>);</span><br><span class="line">    showDesc.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">    <span class="comment">//静态方法的调用者是类，也可以写个null，因为showDesc本身就是通过personClass这个运行时类来的，自然知道静态方法的调用者</span></span><br><span class="line">    Object o = showDesc.invoke(Person<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    System.out.println(o);<span class="comment">//没有返回值就是null</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test7</span><span class="params">()</span> <span class="keyword">throws</span> Exception</span>&#123;</span><br><span class="line">    Class&lt;Person&gt; personClass = Person<span class="class">.<span class="keyword">class</span></span>;</span><br><span class="line">    <span class="comment">//该方法只要写构造器参数</span></span><br><span class="line">    Constructor&lt;Person&gt; constructor = personClass.getDeclaredConstructor(String<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    constructor.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">    Person tom = constructor.newInstance(<span class="string">"Tom"</span>);<span class="comment">//这里实际就是把上一个测试中的新建运行时类对象的方法拆开</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>学会反射后就用来做动态代理，下面是静态代理：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.Reflect.Proxy;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">ClothFactory</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">produceCloth</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//代理类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProxyClothFactory</span> <span class="keyword">implements</span> <span class="title">ClothFactory</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> ClothFactory factory;<span class="comment">//用被代理类对象实例化</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ProxyClothFactory</span><span class="params">(ClothFactory factory)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.factory = factory;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">produceCloth</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"代理工厂做一些准备工作"</span>);</span><br><span class="line">        factory.produceCloth();</span><br><span class="line">        System.out.println(<span class="string">"代理工厂做一些后续工作"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//被代理类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NikeClothFactory</span> <span class="keyword">implements</span> <span class="title">ClothFactory</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">produceCloth</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Nike生产运动服"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StaticProxy</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//创建被代理类对象</span></span><br><span class="line">        NikeClothFactory nike = <span class="keyword">new</span> NikeClothFactory();</span><br><span class="line">        <span class="comment">//创建代理类对象</span></span><br><span class="line">        ClothFactory proxyClothFactory = <span class="keyword">new</span> ProxyClothFactory(nike);</span><br><span class="line">        proxyClothFactory.produceCloth();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>首先要有一个代理类和被代理类都要实现的接口，接着代理类内部包含一个被代理类，实际执行方法用的就是这个内部的被代理类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.Reflect.Proxy;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.lang.reflect.InvocationHandler;</span><br><span class="line"><span class="keyword">import</span> java.lang.reflect.Method;</span><br><span class="line"><span class="keyword">import</span> java.lang.reflect.Proxy;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Human</span> </span>&#123;</span><br><span class="line">    <span class="function">String <span class="title">getBelief</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">eat</span><span class="params">(String food)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//被代理类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SuperMan</span> <span class="keyword">implements</span> <span class="title">Human</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getBelief</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"I can fly"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">eat</span><span class="params">(String food)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"我喜欢吃"</span> + food);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//根据加载的被代理类动态的创建一个代理类及其对象</span></span><br><span class="line"><span class="comment">//通过代理类的对象动态的调用被代理类的同名方法</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProxyFactory</span> </span>&#123;</span><br><span class="line">    <span class="comment">//调用此方法，返回一个代理类的对象，object：被代理类对象</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">getProxyInstance</span><span class="params">(Object object)</span> </span>&#123;</span><br><span class="line">        MyInvocationHandler handler = <span class="keyword">new</span> MyInvocationHandler();</span><br><span class="line">        handler.bind(object);</span><br><span class="line">        <span class="comment">//第二个参数得到被代理类实现的接口，第三个参数得到代理类需要做的方法</span></span><br><span class="line">        <span class="keyword">return</span> Proxy.newProxyInstance(object.getClass().getClassLoader(), object.getClass().getInterfaces(), handler);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyInvocationHandler</span> <span class="keyword">implements</span> <span class="title">InvocationHandler</span> </span>&#123;<span class="comment">//Handler是代理类中对被代理对象的处理方法</span></span><br><span class="line">    <span class="keyword">private</span> Object object;<span class="comment">//需要使用被代理类的对象进行赋值，由于真正的方法运行还是需要被代理类，所以代理类要包含一个被代理类</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">bind</span><span class="params">(Object obj)</span> </span>&#123;<span class="comment">//getProxyInstance传入的被代理类被Handler包含</span></span><br><span class="line">        <span class="keyword">this</span>.object = obj;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//当通过代理类的对象调用方法a时，就会自动调用下面的方法</span></span><br><span class="line">    <span class="comment">//将被代理类要执行的方法a的功能放在invoke方法内</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">invoke</span><span class="params">(Object proxy, Method method, Object[] args)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">        <span class="comment">//method即为代理类对象调用的方法，此方法也就作为被代理类对象调用的方法</span></span><br><span class="line">        <span class="comment">//object:被代理类对象</span></span><br><span class="line">        <span class="comment">//method的返回值就作为*当前类*的invoke方法的返回值</span></span><br><span class="line">        <span class="keyword">return</span> method.invoke(object, args);<span class="comment">//利用反射，执行object的method方法</span></span><br><span class="line">        <span class="comment">//Handler还可以在此处做一些增强方法</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ProxyTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SuperMan superMan = <span class="keyword">new</span> SuperMan();</span><br><span class="line">        Human proxyInstance = (Human) ProxyFactory.getProxyInstance(superMan);</span><br><span class="line">        proxyInstance.eat(<span class="string">"火锅"</span>);</span><br><span class="line">        System.out.println(proxyInstance.getBelief());</span><br><span class="line"></span><br><span class="line">        NikeClothFactory nikeClothFactory = <span class="keyword">new</span> NikeClothFactory();</span><br><span class="line">        ClothFactory proxyClothFactory = (ClothFactory) ProxyFactory.getProxyInstance(nikeClothFactory);</span><br><span class="line">        proxyClothFactory.produceCloth();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>常用类（复习4）</title>
    <url>/2020/05/27/%E5%B8%B8%E7%94%A8%E7%B1%BB%EF%BC%88%E5%A4%8D%E4%B9%A04%EF%BC%89/</url>
    <content><![CDATA[<p>常用类复习.</p>
<a id="more"></a>

<h1 id="String"><a href="#String" class="headerlink" title="String"></a>String</h1><p>String内部是一个<code>private final byte[] value</code>,故它是不可变的。以前是char数组，现在改了。</p>
<p>1.字面量是在方法区的字符串常量池</p>
<p>2.new出来的在堆中</p>
<p>3.字面量拼接的结果是在常量池</p>
<p>4.拼接时若涉及到变量，那就是在堆</p>
<p>5.intern()方法是“池化”的意思，将某字符串“放到”常量池。</p>
<p>具体来说：堆中有“abcd”字符串对象，调用这个方法后会在常量池中找有没有这个字面量，若有，则返回该字面量引用，若没有，则复制一份放到常量池。在jdk1.7之后，不再这样做。因为从堆中<strong>复制</strong>对象到方法区并不快速，而且常量池放太多会引发溢出错误。1.7后是这样处理的：若是没有则将字符串对象的引用放到常量池。另外，1.7时字符串常量池放到了堆中。后来1.8又放回去了，只不过这是方法区不再是方法区，而是元空间。</p>
<p>基本数据类型，包装类–&gt;String：用String的valueOf(xxx)方法</p>
<p>String–&gt;基本数据类型，包装类:用包装类的静态方法：parseXxx(str)</p>
<p>String–&gt;char数组：String的toCharArray()</p>
<p>char数组–&gt;String：用String的构造器</p>
<p>String–&gt;byte数组：用String的getBytes(),可以使用默认的字符集，也可以指定字符集</p>
<p>byte数组–&gt;String：用String的构造器</p>
<h2 id="其他相关类"><a href="#其他相关类" class="headerlink" title="其他相关类"></a>其他相关类</h2><p>StringBuffer效率低，线程安全，可变的。</p>
<p>StringBuilder效率高，线程不安全，可变的。</p>
<p>二者都是继承于AbstractStringBuilder，这个抽象类用<code>byte[] value</code>存储字符串，故是可变的。</p>
<p>二者内部数组默认长度是16，<code>new StringBuilder()</code>长度为16，<code>new StringBuilder(&quot;abc&quot;)</code>，长度为19.但是用length方法结果是0和3，即统计字符数。</p>
<p>若容量不足，就会扩容。默认会扩大为原长度*2+2，然后将原数组拷贝进去。</p>
<p>故建议在用构造器时指定容量，避免扩容。</p>
<h1 id="日期时间API"><a href="#日期时间API" class="headerlink" title="日期时间API"></a>日期时间API</h1><h2 id="JDK8之前"><a href="#JDK8之前" class="headerlink" title="JDK8之前"></a>JDK8之前</h2><p><code>System.currentTimeMillis()</code>:1970-1-1到现在的毫秒数。</p>
<p>Date类：</p>
<blockquote>
<p>java.util.Date类：</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Date date = <span class="keyword">new</span> Date();</span><br><span class="line">System.out.println(date.toString());<span class="comment">//Wed May 27 20:36:03 CST 2020</span></span><br><span class="line">System.out.println(date.getTime());<span class="comment">//1590583085383,1970-1-1到现在的毫秒数</span></span><br><span class="line">Date date1 = <span class="keyword">new</span> Date(<span class="number">15908530064L</span>);<span class="comment">//long型的毫秒数，date的构造器大部分已过时，除了这个和空参构造器</span></span><br><span class="line">System.out.println(date1);<span class="comment">//Sat Jul 04 11:02:10 CST 1970</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>java.sql.Data类：对应于数据库中的日期类型变量,而且它是util.data的子类。</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">java.sql.Date date2 = <span class="keyword">new</span> java.sql.Date(<span class="number">12345665412L</span>);</span><br><span class="line">System.out.println(date2);<span class="comment">//1970-05-24</span></span><br></pre></td></tr></table></figure>

<p>以上两种互相转换：多态和强转（new了一个子类，利用多态给父类，再强转为子类），如果要util.date直接转为sql.date，即一个真正的父类（不是子类多态后的父类）转为子类，那么让util.date的getTime方法作为sql.date的构造器参数即可。</p>
<blockquote>
<p>java.text.SimpleDateFormat类：对Date类的格式化和解析</p>
</blockquote>
<p>两个操作：</p>
<p>格式化：日期–&gt;字符串</p>
<p>解析：字符串–&gt;日期</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Date date = <span class="keyword">new</span> Date();</span><br><span class="line">System.out.println(date.toString());</span><br><span class="line"></span><br><span class="line">SimpleDateFormat sdf = <span class="keyword">new</span> SimpleDateFormat();</span><br><span class="line">String format = sdf.format(date);</span><br><span class="line">System.out.println(format);<span class="comment">//2020/5/28 上午11:18</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">        Date date1 = sdf.parse(<span class="string">"2020/5/28 上午11:18"</span>);</span><br><span class="line">        System.out.println(date1);<span class="comment">//Thu May 28 11:18:00 CST 2020</span></span><br><span class="line">    &#125; <span class="keyword">catch</span> (ParseException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>还可以在构造器中加入参数，指定格式。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">DateFormat dateFormat = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"dd-MM-yy:HH:mm:ss"</span>);</span><br><span class="line">Date date = <span class="keyword">new</span> Date();</span><br><span class="line">String dateStr = dateFormat.format(date);</span><br><span class="line">System.out.println(dateStr);<span class="comment">//28-05-20:11:37:17</span></span><br><span class="line">Date date1 = dateFormat.parse(<span class="string">"27-06-20:11:00:00"</span>);</span><br><span class="line">System.out.println(date1);<span class="comment">//Sat Jun 27 11:00:00 CST 2020</span></span><br></pre></td></tr></table></figure>

<p>将指定格式日期转为sql.Date：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">SimpleDateFormat dateFormat = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy-MM-hh"</span>);</span><br><span class="line">Date date = dateFormat.parse(<span class="string">"2020-10-20"</span>);</span><br><span class="line">java.sql.Date date1 = <span class="keyword">new</span> java.sql.Date(date.getTime());</span><br><span class="line">System.out.println(date1);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>Calendar类（抽象类）</p>
</blockquote>
<p>其子类为<code>GregorianCalendar</code>,通过这个创建对象，或者用<code>Calendar.getInstance</code>方法，实际还是一个<code>GregorianCalendar</code>类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Calendar calendar = Calendar.getInstance();</span><br><span class="line">System.out.println(calendar.get(Calendar.DAY_OF_MONTH));<span class="comment">//也可以用set方法直接修改calendar中的值，还有add添加天数</span></span><br></pre></td></tr></table></figure>

<p>日历类–&gt;Date：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Date date=calendar.getTime();</span><br></pre></td></tr></table></figure>

<p>Date–&gt;日历类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">calender.setTime(<span class="keyword">new</span> Date);</span><br></pre></td></tr></table></figure>

<h2 id="JDK8"><a href="#JDK8" class="headerlink" title="JDK8"></a>JDK8</h2><blockquote>
<p>LocalDate,LocalTime,LocalDateTime</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">LocalDate localDate =LocalDate.now();<span class="comment">//获取时间</span></span><br><span class="line">LocalTime localTime = LocalTime.now();</span><br><span class="line">LocalDateTime localDateTime = LocalDateTime.now();</span><br><span class="line">LocalDateTime localDateTime1 = LocalDateTime.of(<span class="number">2020</span>, <span class="number">12</span>, <span class="number">20</span>, <span class="number">9</span>, <span class="number">30</span>);<span class="comment">//设置时间</span></span><br><span class="line">System.out.println(localDate);</span><br><span class="line">System.out.println(localTime);</span><br><span class="line">System.out.println(localDateTime);</span><br><span class="line">System.out.println(localDateTime1);</span><br><span class="line">System.out.println(localDateTime.getDayOfMonth());<span class="comment">//getXXX获取信息</span></span><br><span class="line">LocalDate localDate1 = localDate.withDayOfMonth(<span class="number">22</span>);<span class="comment">//修改设置相关属性，不可变性</span></span><br><span class="line">LocalDateTime localDateTime2 = localDateTime.plusMonths(<span class="number">3</span>);<span class="comment">//加</span></span><br><span class="line">LocalDateTime localDateTime3 = localDateTime.minusMinutes(<span class="number">3</span>);<span class="comment">//减</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>Instant时间戳</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Instant instant = Instant.now();</span><br><span class="line">System.out.println(instant);</span><br><span class="line">OffsetDateTime time = instant.atOffset(ZoneOffset.ofHours(<span class="number">8</span>));<span class="comment">//加上偏移量</span></span><br><span class="line"><span class="keyword">long</span> toEpochMilli = instant.toEpochMilli();<span class="comment">//计算毫秒数</span></span><br><span class="line">Instant instant1 = Instant.ofEpochMilli(<span class="number">1564865123455L</span>);<span class="comment">//给定毫秒数获取Instant实例</span></span><br></pre></td></tr></table></figure>

<p>吐了，时间相关的太多了，不记了。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>多线程（复习3）</title>
    <url>/2020/05/25/%E5%A4%9A%E7%BA%BF%E7%A8%8B%EF%BC%88%E5%A4%8D%E4%B9%A03%EF%BC%89/</url>
    <content><![CDATA[<p>多线程。</p>
<a id="more"></a>

<h1 id="创建多线程的方式"><a href="#创建多线程的方式" class="headerlink" title="创建多线程的方式"></a>创建多线程的方式</h1><h2 id="继承Thread类"><a href="#继承Thread类" class="headerlink" title="继承Thread类"></a>继承Thread类</h2><p>1.创建于继承于Thread类的子类</p>
<p>2.重写子类的run()方法–&gt;将此线程要做的事写在run方法中</p>
<p>3.创建子类对象</p>
<p>4.调用子类的start()方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        MyThread myThread = <span class="keyword">new</span> MyThread();</span><br><span class="line">        myThread.start();<span class="comment">//启动线程，调用run()方法</span></span><br><span class="line">        <span class="comment">//再启动一个线程不能再调一遍start方法，它内部有个threadstatus，start调用过后就改变了，没法再用</span></span><br><span class="line">        <span class="comment">//直接新建一个MyThread对象即可。</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i % <span class="number">2</span> != <span class="number">0</span>) &#123;</span><br><span class="line">                System.out.println(i+<span class="string">"********main**********"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                System.out.println(i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>两个线程同时运行，具体如何执行要看CPU的调度。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> Thread() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (i % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="comment">//currentThread()静态方法，返回当前执行的线程，getname获得线程的名字，setname设置名字</span></span><br><span class="line">                        System.out.println(Thread.currentThread().getName() + <span class="string">" "</span> + i);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;.start();</span><br><span class="line">        <span class="keyword">new</span> Thread() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (i % <span class="number">2</span> != <span class="number">0</span>) &#123;</span><br><span class="line">                        System.out.println(Thread.currentThread().getName() + <span class="string">" "</span> + i);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                System.out.println(i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>用匿名内部类，实现一个thread的匿名子类并且重写run方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        MyThread myThread = <span class="keyword">new</span> MyThread();</span><br><span class="line">        myThread.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                System.out.println(Thread.currentThread().getName() + <span class="string">':'</span> + i);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (i % <span class="number">20</span> == <span class="number">0</span>) &#123;<span class="comment">//线程让步的意思，释放当前CPU的执行权，但并不带表让给另一个线程。下一刻可能又被抢回来了</span></span><br><span class="line">            <span class="comment">//Java14必须要写明yield()的调用者，以前可以不写</span></span><br><span class="line">                MyThread.yield();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        MyThread myThread = <span class="keyword">new</span> MyThread();</span><br><span class="line">        myThread.start();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i= <span class="number">0</span>; i &lt; <span class="number">100</span> ; i++)&#123;</span><br><span class="line">            <span class="keyword">if</span> (i % <span class="number">2</span> != <span class="number">0</span>) &#123;</span><br><span class="line">                System.out.println(Thread.currentThread().getName() + <span class="string">':'</span> + i);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (i == <span class="number">20</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    myThread.join();<span class="comment">//在main线程里调用myThread.join()，意思是让main进入阻塞状态，先让myThread线程执行</span></span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>还有个方法stop()。终止当前线程，已过时。</p>
<p>还有sleep()方法，sleep(1000)代表线程停一秒。一秒钟之内，CPU无法执行该线程，一秒后可以执行，但不一定是马上执行。使用时要用try-catch处理异常。</p>
<p>还有个isAlive()方法，判断线程是否存活。</p>
<h2 id="线程的调度"><a href="#线程的调度" class="headerlink" title="线程的调度"></a>线程的调度</h2><p>多线程采用时间片的额策略，来回在几个线程中切换。这就涉及到优先级问题。</p>
<p>MAX_PRIORITY:10,</p>
<p>MIN_PRIORITY:1,</p>
<p>NORM_PRIORITY:5.</p>
<p>可以使用getPriority()返回线程优先级，setPriority()设置优先级。线程创建时继承父线程的优先级。<br>低优先级只是得到调度的概率低，不是绝对在高优先级后执行。</p>
<h2 id="实现Runnable接口"><a href="#实现Runnable接口" class="headerlink" title="实现Runnable接口"></a>实现Runnable接口</h2><p>1.创建一个实现了Runnable接口的类</p>
<p>2.实现类中要实现Runnable接口的抽象方法run()</p>
<p>3.创建实现类对象</p>
<p>4.将该对象传入Thread类的构造器，并创建Thread类对象</p>
<p>5.通过Thread类对象调用start()</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">new</span> Thread(<span class="keyword">new</span> MyThread()).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(<span class="keyword">new</span> MyThread()).start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                System.out.println(Thread.currentThread().getName() + <span class="string">':'</span> + i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一般开发中用实现接口的方式，可以避免单继承的缺点，同时由于一个Runnable接口实现类传给多个Three构造器，实现类中的数据天然就是共享的。共同点是两种方法都要重写run()方法。</p>
<h1 id="线程的生命周期"><a href="#线程的生命周期" class="headerlink" title="线程的生命周期"></a>线程的生命周期</h1><p>Thread.State定义了线程的多个状态。State是一个枚举类，里面有五个对象。</p>
<p>五种状态：</p>
<p>新建（没有开始start()方法），</p>
<p>就绪（开始了start()方法，可以执行，但还没分配CPU资源），</p>
<p>运行（分配到CPU资源，正在执行），</p>
<p>阻塞（被挂起，或者用了join()方法，但不是切换到另一线程执行的意思，那种还是就绪，而这种是绝对的不能执行，即使CPU资源分配了），</p>
<p>死亡（执行完run()，调用stop()，出现异常且没处理）。<br>当阻塞结束后是回到就绪状态，而不是运行状态。<br>触发阻塞的方法：sleep()，join()，等待同步锁，wait(),suspend(),(suspend过时)。阻塞不是线程的最终状态，它必定会回到就绪，最终是死亡。</p>
<p>回到就绪状态：sleep时间到，join结束，获取到同步锁，notify()/notifyAll()唤醒，resume()（已过时）<br><img src="/2020/05/25/%E5%A4%9A%E7%BA%BF%E7%A8%8B%EF%BC%88%E5%A4%8D%E4%B9%A03%EF%BC%89/%E5%89%AA%E8%B4%B4%E6%9D%BF%E5%9B%BE%E7%89%87.jpg" alt></p>
<h1 id="线程同步"><a href="#线程同步" class="headerlink" title="线程同步"></a>线程同步</h1><p>多个线程同时处理一个数据就会出现线程安全问题。必要一个线程a处理完数据后才能让另一个线程b处理。即使线程a出现了阻塞也不能切换给线程b。</p>
<h2 id="方式一：同步代码块"><a href="#方式一：同步代码块" class="headerlink" title="方式一：同步代码块"></a>方式一：同步代码块</h2><p>用synchronize关键字。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span>(同步监视器)&#123;<span class="comment">//同步监视器，俗称锁，任何一个类的对象都可充当锁</span></span><br><span class="line">    <span class="comment">//要被同步的代码，即操作共享数据的代码</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>这个锁必须只能有一把，即每个线程都是这把锁</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> ticket = <span class="number">100</span>;</span><br><span class="line">    Object lock = <span class="keyword">new</span> Object();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (lock) &#123;<span class="comment">//或者直接把this作为锁</span></span><br><span class="line">                <span class="keyword">if</span> (ticket &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">":卖票，票号："</span> + ticket);</span><br><span class="line">                    ticket--;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果把lock的创建放在run()内部，就不是只有一把锁了。<br>若是使用继承thread类的方式，则不仅要给数据加static，锁也要加static。也可以简便写MyThread.class，即将继承Thread的子类的类对象作为锁。因为每个类都有一个类对象，且唯一。</p>
<h2 id="方式二：同步方法"><a href="#方式二：同步方法" class="headerlink" title="方式二：同步方法"></a>方式二：同步方法</h2><p>如果需要操作共享数据的代码在一个方法内部，则可以将方法声明为同步的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread1</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> ticket = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        show();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (ticket &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">":卖票，票号："</span> + ticket);</span><br><span class="line">                    ticket--;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>继承类的写法如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> ticket = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        show();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (ticket &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">":卖票，票号："</span> + ticket);</span><br><span class="line">                    ticket--;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>同步方法实际还是和同步代码块一样用的同步监视器，只不过不需要显式的写出来，非静态的同步方法的锁是this，静态的同步方法的锁是当前类的类对象。</p>
<h2 id="方式三：Lock锁"><a href="#方式三：Lock锁" class="headerlink" title="方式三：Lock锁"></a>方式三：Lock锁</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread1</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> ticket = <span class="number">100</span>;</span><br><span class="line">    <span class="keyword">private</span> ReentrantLock lock = <span class="keyword">new</span> ReentrantLock(<span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                lock.lock();</span><br><span class="line">                <span class="keyword">if</span> (ticket &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">":卖票，票号："</span> + ticket);</span><br><span class="line">                    ticket--;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                lock.unlock();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>简要写下，后面再深入研究。</p>
<h1 id="线程间的通信"><a href="#线程间的通信" class="headerlink" title="线程间的通信"></a>线程间的通信</h1><p>如下代码，想让两个线程交替打印，但是往往被一个线程给全部占了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test1</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        MyThread1 myThread1 = <span class="keyword">new</span> MyThread1();</span><br><span class="line">        Thread t1 = <span class="keyword">new</span> Thread(myThread1);</span><br><span class="line">        Thread t2 = <span class="keyword">new</span> Thread(myThread1);</span><br><span class="line">        t1.setName(<span class="string">"线程1"</span>);</span><br><span class="line">        t2.setName(<span class="string">"线程2"</span>);</span><br><span class="line">        t1.start();</span><br><span class="line">        t2.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread1</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> number = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (number &lt; <span class="number">100</span>) &#123;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">":"</span> + number);</span><br><span class="line">                    number++;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>让线程一让给线程二就是线程的通讯。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test1</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        MyThread1 myThread1 = <span class="keyword">new</span> MyThread1();</span><br><span class="line">        Thread t1 = <span class="keyword">new</span> Thread(myThread1);</span><br><span class="line">        Thread t2 = <span class="keyword">new</span> Thread(myThread1);</span><br><span class="line">        t1.setName(<span class="string">"线程1"</span>);</span><br><span class="line">        t2.setName(<span class="string">"线程2"</span>);</span><br><span class="line">        t1.start();</span><br><span class="line">        t2.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread1</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> number = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">                notify();<span class="comment">//唤醒另一个线程，让其进入就绪状态，如果有多个，就会唤醒优先级高的，notifyAll则是唤醒所有</span></span><br><span class="line">                <span class="keyword">if</span> (number &lt; <span class="number">100</span>) &#123;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">":"</span> + number);</span><br><span class="line">                    number++;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    wait();<span class="comment">//线程一进入阻塞，执行wait()会释放锁，让线程二执行</span></span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>notify，notifyAll，wait使用必须是在同步代码块或同步方法中。且三个方法的调用者必须是同步监视器。这三个方法定义在Object类中。</p>
<p>sleep与wait的异同：</p>
<p>同：两个调用后都会让线程进入阻塞状态。</p>
<p>异：sleep是在Thread中声明的，而wait是在Object中。所以调用时sleep可以在任何需要时就调用，而wait必须在同步代码块或同步方法中。wait还会释放锁，sleep不会。</p>
<h1 id="其他两种创建多线程的方式"><a href="#其他两种创建多线程的方式" class="headerlink" title="其他两种创建多线程的方式"></a>其他两种创建多线程的方式</h1><h2 id="实现Callable接口"><a href="#实现Callable接口" class="headerlink" title="实现Callable接口"></a>实现Callable接口</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.Callable;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.ExecutionException;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.FutureTask;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test2</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        NumThread numThread = <span class="keyword">new</span> NumThread();</span><br><span class="line">        FutureTask futureTask = <span class="keyword">new</span> FutureTask(numThread);<span class="comment">//接受Callable的返回值</span></span><br><span class="line">        <span class="keyword">new</span> Thread(futureTask).start();<span class="comment">//被执行</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Object o = futureTask.get();</span><br><span class="line">            System.out.println((<span class="keyword">int</span>)o);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException | ExecutionException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NumThread</span> <span class="keyword">implements</span> <span class="title">Callable</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                sum += i;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> sum;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>1.创建一个Callable接口的实现类</p>
<p>2.将此线程要执行的操作写在call()方法中</p>
<p>3.创建Callable接口实现类的对象</p>
<p>4.将此实现类对象传递到FutureTask构造器</p>
<p>5.将FutureTask对象传递到Thread类的构造器，创建Thread的对象，并调用start方法。</p>
<p>该方法的优点：</p>
<p>1.call可以有返回值</p>
<p>2.call可以抛出异常</p>
<p>3.Callable是支持泛型的。</p>
<p>Future接口可以对Runnable，Callable任务的执行结果进行取消，查询，获取结果等。FutureTask是这个接口的唯一实现类，但它又同时实现了Runnable接口，所以FutureTask即用来接受Callable的返回值，也用来被线程执行。</p>
<h2 id="创建线程池"><a href="#创建线程池" class="headerlink" title="创建线程池"></a>创建线程池</h2><p>ExecutorService：真正的线程池接口。常见子类ThreadPoolExecutor。</p>
<p>execute(Runnable command):执行任务，没有返回值，一般执行Runnable</p>
<p>Future submit(Callable task):执行任务，没有返回值，一般执行Callable</p>
<p>void shutdown():关闭连接池</p>
<p>Executors:工具类，线程池的工厂类，用于创建并返回不同类型的线程池</p>
<p>Executors.newCachedThreadPool()：创建一个可根据需要创建新线程的线程池</p>
<p>Executors.newFixedThreadPool(n)：创建一个可重用固定线程数的线程池</p>
<p>Executors.newSingleThreadPool()：一个线程的线程池</p>
<p>Executors.newScheduledThreadPool(n)：一个可在给定延迟后运行命令或定期执行的线程池</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.thread;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.ExecutorService;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.Executors;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.ThreadPoolExecutor;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NumberThread</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                System.out.println(i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ExecutorService service = Executors.newFixedThreadPool(<span class="number">10</span>);<span class="comment">//ExecutorService是接口，Executors工具类返回的实际是ThreadPoolExecutor</span></span><br><span class="line">        <span class="comment">//ThreadPoolExecutor继承于AbstractExecutorService抽象类，它又实现了ExecutorService</span></span><br><span class="line">        <span class="comment">//ThreadPoolExecutor可以设置线程池的属性</span></span><br><span class="line">        ThreadPoolExecutor service1 = (ThreadPoolExecutor) service;</span><br><span class="line">        service1.execute(<span class="keyword">new</span> NumberThread());<span class="comment">//适合使用与Runnable,可以用匿名内部类或lambda表达式</span></span><br><span class="line">        <span class="comment">//service.submit();//适合使用与Callable</span></span><br><span class="line">        service.execute(</span><br><span class="line">                () -&gt; &#123;</span><br><span class="line">                    <span class="comment">// ... do something inside runnable task</span></span><br><span class="line">                    System.out.println(<span class="number">1000</span>);</span><br><span class="line">                &#125;);</span><br><span class="line">        service.shutdown();</span><br><span class="line">        service1.shutdown();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>指针探幽1</title>
    <url>/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/</url>
    <content><![CDATA[<p>指针是个深坑，然而老师不教，只好自学。本篇主要是基础。</p>
<p>emmm，发现坑太深，我先补补前面的内容。</p>
<a id="more"></a>

<h1 id="一．基础"><a href="#一．基础" class="headerlink" title="一．基础"></a>一．基础</h1><p>C++或者C里面最容易让人糊涂应该是指针了,不管是初学者甚至是有经验的童鞋有时候在用指针的时候也会出现一些很隐蔽的错误.<br>指针本身就是一个很绕的概念,而指针又能够和很多的结构比如数组(二维数组),字符串,内存分配和管理等等一些联系起来变成更加绕的概念.所以基础不好的同学常常会犯一些很无厘头的错误,但是高手也会犯错误,而且更加隐蔽.<br>接下来所有的文章内容并不能够保证你能够完完全全避免开发中的错误,但是文章的目标是帮助很多童鞋对于指针概念做一个系统性的归纳.减少犯错误的几率.或者是帮助刚刚入门的boy更加快速深入的理解指针,把基础打得扎实一点.<br>恩,很多人都说指针很难,那为什么还要学指针呢?对于这样的问题,拒绝回答.</p>
<h2 id="Ⅰ-内存和地址"><a href="#Ⅰ-内存和地址" class="headerlink" title="Ⅰ.内存和地址"></a>Ⅰ.内存和地址</h2><p>我们已经很熟悉一些基本的存储单位了,比如一个bit(位)用存储0或者1.也可以把几个bit合起来表示更大的数字,比如一个byte(字节)就包含了8个bit.这些都是很基础很简单的东西.然后<strong>我们可以把计算机的内存想象成一个字节数组,内存中的每一个地址表示一个字节</strong>.<br><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/20160913094036516.png" alt><br>每个字节中都能够存储一定位数的内容,因此,每个字节都能够通过一些地址来标识.有时候,一个字节不够,怎么办呢?那么就同时用很多个字节来表示,比如一个int在我的系统里面就用了4个字节。<br>下面的图是上面那幅图片每两个字节合并在一起之后样子.这个因为是两个字节合并,所以它成为比一个字节更大的内存单位(废话),能够存储的信息就更多(废话).但是虽然一个字是两个字节合并的,但是它仍然只包含一个地址,也就是说,合并之后只需要一个地址来找到合并之后的内存.<br><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/20160913094127985.png" alt><br>因此,我们可以得到:<br><strong>1.内存中的每个位置都由一个独一无二的地址表示.</strong><br><strong>2.内存中的每个位置都包含一个值.</strong><br>通俗一点<strong>,我们可以通过一个地址,来找到内存中的某个具体位置,然后访问到(得到)该位置的值(允许的话).这就是内存和地址简单的思想</strong>.</p>
<h2 id="Ⅱ-指针含义与创建方式"><a href="#Ⅱ-指针含义与创建方式" class="headerlink" title="Ⅱ.指针含义与创建方式"></a>Ⅱ.指针含义与创建方式</h2><p>指针这个名字确实折磨过很多人,这个名字是个好名字,同时也是一个非常不好的名字. 说它好,是因为指针这个东西很形象的体现了它的功能:指针指针,指到某个地方某个位置.非常形象. 它不是个好名字是因为她的名字有时候掩盖了它的真实含义.一般来说，<strong>指针是一个其值为地址的变量。（就是一个存储地址的变量）</strong><br>所以,要养成一种条件反射,看到指针首先不是想到他能够指向哪里,而是想到<strong>这个变量存放的是一个地址,是这个地址指向哪里哪里</strong>.(比如,char类型的变量中存放的是char类型的数据.int变量中存放的是int类型的数据.指针中存放的是一个地址!!!)<br>反复的把上面的概念消化之后,我们就来看两个基本的运算符:<strong>&amp;(取址运算符)</strong>和<strong>*(间接访问运算符/解引用指针)</strong></p>
<p>首先是&amp;运算符:当它后面跟一个变量名的时候,给出这个变量名的地址.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="keyword">int</span> a=<span class="number">5</span>;</span><br><span class="line">   <span class="keyword">double</span> b=<span class="number">10.4</span>;</span><br><span class="line">   <span class="built_in">cout</span>&lt;&lt;<span class="string">"Address of a:"</span>&lt;&lt;&amp;a&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">   <span class="built_in">cout</span>&lt;&lt;<span class="string">"Address of b:"</span>&lt;&lt;&amp;b&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/20160913094450158.png" alt></p>
<p>至于*运算符:就是后面跟一个指针的时候,得到指针指向的内存中的内容.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="keyword">int</span> a=<span class="number">5</span>;</span><br><span class="line">   <span class="keyword">double</span> b=<span class="number">10.4</span>;</span><br><span class="line"></span><br><span class="line">   <span class="built_in">cout</span>&lt;&lt;<span class="string">"Address of a:"</span>&lt;&lt;&amp;a&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">   <span class="built_in">cout</span>&lt;&lt;<span class="string">"Address of b:"</span>&lt;&lt;&amp;b&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">   <span class="built_in">cout</span>&lt;&lt;<span class="string">"a:"</span>&lt;&lt;*(&amp;a)&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/%E6%8C%87%E9%92%88.png" alt><br>通过上面的例子你会发现,这里输出的地址是16进制的整形.其实事实上,在大多数系统的内部，<strong>指针所存的地址值一般是一个无符号的整数</strong>。但是，这并不代表指针和整形有一样的运算规则。指针类型是一种新的类型，而不是一种整数类型。ANSI专门为指针提供了%p输出格式</p>
<p>理解上面两个基本的运算符之后,就可以正式开始讲指针的创建了.<br><strong>声明指针的模板:</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">指向地址的数据类型 * 指针变量名;</span><br></pre></td></tr></table></figure>

<p>其中,<code>*</code>号<strong>必须带</strong>,用以表明现在创建的是一个指针类型的变量.同时,当你看到<strong>创建变量的语句中</strong>带有星号<code>*</code>的话,那么说明那个变量必定是一个指针变量!<br>是不是很简单!举一个例子进一步来理解上面那个的含义.比如我想创建一个指针变量(存放地址的变量),这个指针(地址)是指向一个存储整形的内存.那么我就可以写为:<code>int * leo</code>;同理,指向char的我可以写成<code>char * c;</code>其实是很简单的.<br>这里结合前面的内容,简单的写一个例子,并且介绍一些写法.(最开始让初学者迷惑的地方就是这里了,因为创建时候的<code>*</code>号,解除指针时候的<code>*</code>号,各种符号混在一起,一般就直接懵逼了.但是要是好好掌握一些经验结论,这里很容易过去.)</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">5</span>, b = <span class="number">6</span>, c = <span class="number">7</span>, d = <span class="number">8</span>;</span><br><span class="line">    <span class="keyword">double</span> e = <span class="number">3.1415</span>;</span><br><span class="line">    <span class="comment">//单独赋值,并且*和p_a紧挨着</span></span><br><span class="line">    <span class="keyword">int</span> *p_a = &amp;a;</span><br><span class="line">    <span class="comment">//多个赋值(既有指针,又有普通变量)</span></span><br><span class="line">    <span class="keyword">int</span> *p_b = &amp;b, *p_c = &amp;c, *p_d = &amp;d, temp = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//单独赋值,double类型</span></span><br><span class="line">    <span class="keyword">double</span> *p_e = &amp;e;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; p_a &lt;&lt; <span class="built_in">endl</span> &lt;&lt; p_b &lt;&lt; <span class="built_in">endl</span> &lt;&lt; p_c &lt;&lt; <span class="built_in">endl</span> &lt;&lt; p_d &lt;&lt; <span class="built_in">endl</span> &lt;&lt; p_e &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; temp &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/%E6%8C%87%E9%92%881.png" alt><br><strong>上面这个例子有一些可以提炼的经验:</strong><br>首先,就是创建指向int或者double的指针的创建方式,这个前面都讲了很多次了.在这个例子里面也可以很容易的找到,所以这里就不啰嗦了.<br>然后就是创建时候的写法.比如 <code>int *p_a=&amp;a;</code>这句话中,<code>*</code>号是紧挨着p_a的,而在<code>int * p_b=&amp;b,*p_c=&amp;c,*p_d=&amp;d,temp=100;</code> 这句话中,<code>*</code>号是可以不挨着p_b和p_c的.也就是说,创建指针变量的时候,星号的位置是很自由的.<strong>只要是出现了星号,而不管中间是不是有空格,我们便认为,这算是创建了一个指针变量</strong>.<br>在别人写的代码中,你会看到很多的写法,其实本质就是这样.比如有些人喜欢写成<code>int* p_a=&amp;a;</code>在这里,<code>*</code>号紧挨着int,因为有人理解为int的指针类型即int<em>.所以,写法这么多,初学者肯定是会迷惑的,记住前面的经验,这样就见怪不怪了.<br>接下来,还是在`int * p_b=&amp;b,</em> p_c=&amp;c,<em>p_d=&amp;d,temp=100;<code>这句话里面,最后接了一个temp=100,千万不要也把temp也当做了一个指针变量,他只是一个普通的变量.也就是说,同一句话里面,可以混合多种类型的赋值,指针的带</code></em><code>号,普通的不带</code>*`号.</p>
<h2 id="Ⅲ-使用指针"><a href="#Ⅲ-使用指针" class="headerlink" title="Ⅲ.使用指针"></a>Ⅲ.使用指针</h2><p>使用指针的方式有很多,我们这里谈谈最基础的,后面会更加深入的讲指针的使用.前面已经讲过了指针的创建,接下来主要是讲指针的初始化问题和赋值以及解除引用等等问题.<br>首先是初始化问题.在前面的那一个例子中,我们在创建指针的时候都顺便初始化了,那是不是创建指针就一定要初始化?肯定不是!创建指针的时候也可以不初始化.因为<strong>有时候你创建一个指针只是为了后续的工作</strong>,根本没有东西拿来初始化.那么到现在,我们解决了第一个问题,那就是<strong>创建指针可以初始化也可以不初始化</strong>,.那么你肯定会说,这么简单为什么要单独拿出来讲?是因为后来的某些操作是要考虑是否初始化的问题的.好了,不绕弯了,这里的操作主要是讲间接访问(解引用指针)带来的一些问题.不多说,直接上例子.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> num=<span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//p_a没有初始化</span></span><br><span class="line">    <span class="keyword">int</span> *p_a1,*p_a2;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"p_a1:"</span>&lt;&lt;p_a1&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"p_a2:"</span>&lt;&lt;p_a2&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> *p_b=&amp;num;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"p_b:"</span>&lt;&lt;p_b&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//同类型指针可以赋值</span></span><br><span class="line">    <span class="comment">//没有初始化的指针依然可以被赋值(地址)</span></span><br><span class="line">    p_a1=p_b;</span><br><span class="line">    p_a2=&amp;num;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"changed p_a1:"</span>&lt;&lt;p_a1&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"changed p_a2:"</span>&lt;&lt;p_a2&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/%E6%8C%87%E9%92%882.png" alt><br>一句一句来分析这个例子.首先我们创建了一个整形,值为5,然后我们创建了两个指向int类型的指针<code>p_a1</code>,<code>P_a2</code>,但是我们没有初始化.从最后的运行结果来看,指针不初始化是可行的.然后创建了一个指针<code>p_b</code>并且初始化了,所以从结果来看,就是num的地址.<br>接下来有两句:<code>p_a1=p_b; p_a2=&amp;num;</code><br>前面那句是直接把p_b这个指向int类型的指针直接赋给了p_a1这个也是指向int类型的指针,那么从效果上面来看,相当于<code>p_a1</code>和<code>p_b</code>指向同样的地址…..好了,不装逼了,因为指针就是存储地址的嘛,其实就是把这个变量中存储的地址赋给了另外一个变量…..非常简单.<br>后面第二句就是说,即使不初始化,我还是能够接收地址.(类比<code>int n;n=5</code>).<br>所以总结起来就是即使没有初始化,我依然能够两个指针之间赋值(类型肯定要一样啦)和接受地址.也没有什么困难的.那么接着看.</p>
<p>把上面的代码小小的改动一些,代码变成下面这样.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> num=<span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//p_a没有初始化</span></span><br><span class="line">    <span class="keyword">int</span> *p_a1,*p_a2;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"p_a1:"</span>&lt;&lt;p_a1&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;<span class="string">"p_a2:"</span>&lt;&lt;p_a2&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    *p_a1=<span class="number">12</span>;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;*p_a1&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里就加了一句<code>*p_a1=12;</code>这句话是很危险的.我们创建了一个指向int的指针,但是并没有初始化,也就是说,指针会得到一个随机的地址(至少大部分系统上面是这样),可<strong>创建指针的过程并不包含对于某个整形内存上空间的开辟</strong>.<br><code>*p_a1=12;</code>的过程就是把12这个整形放到p_a1指向的内存的过程.但是空间都没有开辟,怎么放呢?所以这个语句最终是不是运行成功都取决于运气.不同的系统上面可以有不同的结果.但是这样的话就算是运行成功了又有什么意义呢?<br>所以,其他的都不说<strong>,至少在解引用指针的时候,你需要保证你的指针被正确的初始化或者正确的被赋过某个地址.</strong>不然,那样的解引用指针操作无意义且危险.</p>
<p><strong>重要:</strong><br>既然前面谈到了解引用指针(间接访问),那就再来说说指针常量与指针的强制转化.<br>假如我们想在一个地址500存放一个整数100,没错,我们已经知道这个地址是500了,所以我们就能够这么赋值</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">*<span class="number">500</span>=<span class="number">100</span>;</span><br></pre></td></tr></table></figure>

<p>这句话的意思是很明确的,先把地址500解引用然后把100放进这个内存.但是这句话是错的,因为前面说过指针类型是一种特殊的类型.但是我们这里的500就是一个很普通的整形.他是能够表示500这个地址没有错,但是它的类型不适合.<strong>因此,我们要把这个普通的整形强制转换为指向整形的指针类型</strong>.因此可以这样写</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">*(<span class="keyword">int</span> *)<span class="number">500</span>=<span class="number">100</span></span><br></pre></td></tr></table></figure>

<p>其实使用这个的机会很少,但是并不意味这个不重要,首先在某些硬件问题里面确实是想访问某些硬件上面特定的地址的时候我们可以用这个方法.后面讲到内存管理的时候,也会回来这里.</p>
<h2 id="Ⅳ-指针运算"><a href="#Ⅳ-指针运算" class="headerlink" title="Ⅳ.指针运算"></a>Ⅳ.指针运算</h2><p>指针的运算有<strong>算术运算</strong>和<strong>关系运算(比较大小等等),</strong>但是在这里仅仅是提一下,因为这部分的内容是和后面指针与数组有关系的.在后面指针与数组会单独讲这些.</p>
<h2 id="Ⅴ-NULL指针和void"><a href="#Ⅴ-NULL指针和void" class="headerlink" title="Ⅴ.NULL指针和void*"></a>Ⅴ.NULL指针和void*</h2><p>有很多对于指针不是很熟悉的童鞋在上数据结构课的时候一般是很懵逼的,因为用C讲数据结构的时候,涉及到很多的内存开辟回收,以及指针问题(链式结构).然后很多人没有学好不是因为逻辑能力差,而是因为在一些代码或者是伪代码中对于指针的认识模棱两可.以至于本来代码是学习数据结构思想的好工具,变为了学习数据结构的负担.</p>
<p><strong>首先先讲NULL指针.</strong><br>NULL指针是一种非常特殊的指针(废话,不然单独说它干嘛?),不指向任何东西.<strong>表示不指向任何东西的指针</strong>(哦,感觉好厉害的样子).但是事实上面,这种技巧非常有用.(当你被链表虐的时候就知道了).<br>我们怎么把一个指针变为NULL指针呢?很简单,赋给这个指针一个0就行了.不用转化,就是整形的0.同样,直接赋NULL也是行的.看下面的例子.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//测试NULL是不是0</span></span><br><span class="line">    <span class="keyword">if</span>(<span class="literal">NULL</span>==<span class="number">0</span>)</span><br><span class="line">        <span class="built_in">cout</span>&lt;&lt;<span class="string">"yes"</span>&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//转化为NULL指针</span></span><br><span class="line">    <span class="keyword">int</span> *p_a=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> *p_b=<span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/%E6%8C%87%E9%92%883.png" alt><br>例子很简单,就不解释了.<br>有一个非常重要的是,因为NULL不指向任何地方,所以,也就<strong>肯定不能够解引用了</strong>.这点一定要注意.因为连地址都没有,怎么得到不存在的地址中的值呢?所以要是想你的程序健壮,最好是在解引用之前加一个判断是否为NULL指针的步骤,要是你有足够的信心以后都不会有问题,那么不加也罢.<br>下面这个例子是当我试图对一个NULL指针解引用之后的程序运行情况.(就是上面那个例子加了一句话而已)</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//测试NULL是不是0</span></span><br><span class="line">    <span class="keyword">if</span>(<span class="literal">NULL</span>==<span class="number">0</span>)</span><br><span class="line">        <span class="built_in">cout</span>&lt;&lt;<span class="string">"yes"</span>&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//转化为NULL指针</span></span><br><span class="line">    <span class="keyword">int</span> *p_a=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> *p_b=<span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//试图解引用</span></span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;*p_a&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/%E6%8C%87%E9%92%884.png" alt><br>直接崩了,其实也挺好,至少比找不到的隐形错误要好.<br>前面的例子中有一句是判断NULL==0的,这里需要注意一下,NULL是一个预处理的变量,值为0,在头文件cstdlib中定义,(我这里并没有显式载入也能够用),因此用到NULL这个预处理变量的时候,尽量带上cstdlib头文件,规范一点,避免不必要的错误.<br>说到这里,其实你反而应该疑惑了,我们前面已经说过了,指针存放的是一个地址.一般来说,地址是整形没错,但是它是一种新的类型来表示地址.和整形并不能够兼容或者运算.但是当使用0来表示空指针的时候,我们便会疑惑,0到底是整形常量还是一个指针常量?<br>因此,在C++11中,新引入了一种特殊类型的字面值<strong>nullptr</strong>来初始化指针为空指针.他能够被转换成任何类型的指针.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun</span><span class="params">(<span class="keyword">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> num+<span class="number">10</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//表达式0==nullptr为真值</span></span><br><span class="line">    <span class="keyword">if</span>(<span class="number">0</span>==<span class="literal">nullptr</span>)</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"yes"</span>&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> a=<span class="number">5</span>;</span><br><span class="line">    <span class="keyword">int</span> *p=<span class="literal">nullptr</span>;</span><br><span class="line">    p=&amp;a;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"Address of a:"</span>&lt;&lt;p&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/%E6%8C%87%E9%92%885.png" alt><br>讲到现在,发现前面很多的错误都是由于解引用没有初始化的指针引起来的.所以,这里提个建议,就是<strong>尽量定义了对象之后再定义指向这个对象的指针</strong>,对于<strong>不清楚的指向哪里的指针,一律初始化为nullptr(C++11)或者NULL(0)</strong>.之后再判断是否指向对象再进行相应的操作.</p>
<p><strong>接下来讲void*.</strong><br><code>Void*</code>是一种特殊类型的指针,能够用来<strong>存放任何类型对象的地址</strong>.通俗来说,就是我<strong>不知道这个指针指向的是什么类型的对象</strong>.<br>要是还是理解不了那就上例子:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun</span><span class="params">(<span class="keyword">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> num+<span class="number">10</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">double</span> x=<span class="number">25.5</span>;</span><br><span class="line">    <span class="comment">//普通指针的话类型要严格保证</span></span><br><span class="line">    <span class="keyword">double</span> *p=&amp;x;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//void* 类型可以接受任意类型对象地址</span></span><br><span class="line">    <span class="keyword">void</span> *p_v=&amp;x;</span><br><span class="line">    <span class="keyword">void</span> *p_v2=p;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"p_v:"</span>&lt;&lt;p_v&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"p_v2:"</span>&lt;&lt;p_v2&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/%E6%8C%87%E9%92%886.png" alt><br>Void *暂时了解到这里就行了,后面内存分配的时候还会专门和他打交道.</p>
<h2 id="Ⅵ-指针的指针"><a href="#Ⅵ-指针的指针" class="headerlink" title="Ⅵ.指针的指针"></a>Ⅵ.指针的指针</h2><p>指针的指针就是指向指针的指针.再多讲就绕晕了.直接看定义和例子吧.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="keyword">int</span> a=<span class="number">10</span>;</span><br><span class="line">   <span class="keyword">int</span> *p_a=&amp;a;</span><br><span class="line">   <span class="keyword">int</span> **pp_a=&amp;p_a;</span><br><span class="line"></span><br><span class="line">   <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"p_a:"</span>&lt;&lt;p_a&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>&lt;&lt;<span class="string">"*p_a:"</span>&lt;&lt;*p_a&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">   <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">   <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"PP_a:"</span>&lt;&lt;pp_a&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>&lt;&lt;<span class="string">"*pp_a:"</span>&lt;&lt;*pp_a&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>&lt;&lt;<span class="string">"**pp_a"</span>&lt;&lt;**pp_a&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/29/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD1/%E6%8C%87%E9%92%887.png" alt><br>通过这个例子,我们发现其创建方式也是和普通指针的创建方式是差不多的,除了创建时候的两个星号<code>**</code>.那创建指针必定需要指针类型,指针类型怎么选择呢?通俗一点说,就是跟着前面的走:我们发现创建整形时候使用的是<code>int</code>,比如这里的<code>int a=10;</code>.那我们创建指向a的指针的时候,肯定必须也要是int了,比如这里的<code>int *p_a=&amp;a;</code>.最后创建指针的指针的时候,也就用int了.比如这里的<code>int **pp_a=&amp;p_a;</code> .并不困难<br>另外一个就是解引用的时候,带上两个星号,就回到的最开始的那个变量.这么说有点模糊.详细来说,<code>**pp_a</code>因为<code>*</code>的从右向左的结合性,这个表达式可以写成<code>*(*pp_a),</code>那么我们知道<code>pp_a</code>存放的是<code>p_a</code>的地址,<code>*pp_a</code>就是表示<code>p_a</code>这个地址中存放的内容即a的地址(不能晕!!!).那么<code>*(*pp_a)</code>就相当于<code>*p_a</code>或者a.<br>至此,基本的概念部分就结束啦.</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>异常（复习2）</title>
    <url>/2020/05/23/%E5%BC%82%E5%B8%B8%EF%BC%88%E5%A4%8D%E4%B9%A02%EF%BC%89/</url>
    <content><![CDATA[<p>异常复习.</p>
<a id="more"></a>

<h1 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h1><p>顶级父类是Throwable，下属两个，error和exception，error不处理，主要处理exception。exception又分为编译时异常和运行时异常。</p>
<h1 id="处理机制"><a href="#处理机制" class="headerlink" title="处理机制"></a>处理机制</h1><h2 id="try-catch-finally"><a href="#try-catch-finally" class="headerlink" title="try-catch-finally"></a>try-catch-finally</h2><p>处理异常</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">test</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span><span class="comment">//使用单元测试：方法必须是public，没有返回值，没有参数，类必须是public，构造器无参</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> num=<span class="number">0</span>;</span><br><span class="line">        String string = <span class="string">"hello"</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            num = Integer.valueOf(string);</span><br><span class="line">            System.out.println(<span class="number">1</span>);<span class="comment">//这里不执行</span></span><br><span class="line">        &#125; <span class="keyword">catch</span> (NumberFormatException e) &#123;</span><br><span class="line">            <span class="comment">//异常类型要对应，可以写多个catch，然后进行类型匹配，匹配到一个就跳出try-catch结构</span></span><br><span class="line">            System.out.println(<span class="string">"转换异常"</span>);</span><br><span class="line">            System.out.println(e.getMessage());<span class="comment">//打印异常信息</span></span><br><span class="line">            e.printStackTrace();<span class="comment">//更详细的信息</span></span><br><span class="line">        &#125;<span class="keyword">catch</span>(Exception e)&#123;<span class="comment">//如果把有子父类关系的放颠倒了，则会出错，因为父类在上，子类无法执行到</span></span><br><span class="line">            <span class="comment">//...</span></span><br><span class="line">        &#125;<span class="keyword">finally</span>&#123;<span class="comment">//可选的</span></span><br><span class="line">            <span class="comment">//抛出异常前一定会执行的代码，即使catch或try中有return语句</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//try-catch结构结束，异常已被处理，继续执行</span></span><br><span class="line">        System.out.println(<span class="number">2</span>);<span class="comment">//执行</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这其中还可以用throw手动抛出异常：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        student student = <span class="keyword">new</span> student();</span><br><span class="line">        student.regist(<span class="number">1</span>);</span><br><span class="line">        System.out.println(student);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">student</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">regist</span><span class="params">(<span class="keyword">int</span> id)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (id &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">this</span>.id = id;</span><br><span class="line">        &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"非法数据"</span>);<span class="comment">//抛的是运行时异常，可以不处理</span></span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> Exception(<span class="string">"非法数据"</span>);<span class="comment">//这样写就要显式的处理了，可以tc,或者throws</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="throws"><a href="#throws" class="headerlink" title="throws"></a>throws</h2><p>把异常向上抛,甩锅。当某处发生异常，则会向上抛出异常对象，后面的代码不再执行。子类重写的方法抛出的异常要不大于父类的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">method</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        File file = <span class="keyword">new</span> File(<span class="string">"hello.txt"</span>);</span><br><span class="line">        FileInputStream inputStream = <span class="keyword">new</span> FileInputStream(file);</span><br><span class="line">        <span class="keyword">int</span> data=inputStream.read();</span><br><span class="line">        <span class="keyword">while</span> (data != -<span class="number">1</span>) &#123;</span><br><span class="line">            System.out.println((<span class="keyword">char</span>) data);</span><br><span class="line">            data = inputStream.read();</span><br><span class="line">        &#125;</span><br><span class="line">        inputStream.close();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="选择"><a href="#选择" class="headerlink" title="选择"></a>选择</h1><p>若父类中没有抛异常，则子类也不能抛异常。要是子类非要处理异常，则用try-catch来处理。<br>若几个方法调用之间有递进关系，建议整体try-catch。</p>
<h1 id="自定义异常类"><a href="#自定义异常类" class="headerlink" title="自定义异常类"></a>自定义异常类</h1><p>先继承一个异常类，Exception或者RuntimeException。<br>然后要有一个<code>static final long serialVersionUID</code>,用作标识符。<br>提供几个重载的构造器。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>指针常量OR常量指针</title>
    <url>/2018/07/30/%E6%8C%87%E9%92%88%E5%B8%B8%E9%87%8FOR%E5%B8%B8%E9%87%8F%E6%8C%87%E9%92%88/</url>
    <content><![CDATA[<p>指针常量OR常量指针,傻傻搞不清.</p>
<a id="more"></a>

<p>先找到<em>，然后看</em>的两边，右边是对指针p本身的限定，左边是对p所指向的东西的限定。</p>
<p> 注：本文中我们将p的所指笼统地称为“东西”。</p>
<p>例如</p>
<p>1.const int <em>p<br>右边没有限定成分，表明p就是我们熟悉的普通指针，p的内容（即值，也就是它指向的东西的地址）是可以改变的；</em>的左边是const int，表明p指向的东西是一个const的int，我们不能通过p来修改这个int,因为它是const的。（关于“不能通过p来修改”，我们后边还会详细解释）</p>
<p>2.int* const p<br>右边是const，表明p本身是const的，我们不能对p的内容进行修改（例如++p;是不可以的），<em>的左边是int，即p指向的东西是普通的int，我们可以通过p来修改它（例如</em>p=100;是可以的）。</p>
<p>3.const int* const p<br>右边是const，表明指针p本身是const的，*的左边是const int，表明p指向的int也是const的。即这种情况下，p本身不能修改，同时也不能通过p修改它所指向的那个int。</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>指针探幽2</title>
    <url>/2018/08/01/%E6%8C%87%E9%92%88%E6%8E%A2%E5%B9%BD2/</url>
    <content><![CDATA[<p>再补充一些关于指针的注意点.</p>
<a id="more"></a>

<h1 id="一-野指针及C-指针使用注意点"><a href="#一-野指针及C-指针使用注意点" class="headerlink" title="一.野指针及C++指针使用注意点"></a>一.野指针及C++指针使用注意点</h1><p><strong>避免野指针的产生</strong></p>
<p><strong>“野指针”</strong>的成因主要有：</p>
<p>1）指针变量没有被初始化。任何指针变量刚被创建时不会自动成为NULL指针，它的缺省值是随机的，它会乱指一气。所以，指针变量在创建的同时应当被初始化，要么将指针设置为NULL，要么让它指向合法的内存。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *p; <span class="comment">//此时p为野指针</span></span><br></pre></td></tr></table></figure>



<p>2）指针p被free或者delete之后，没有置为NULL，让人误以为p是个合法的指针.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *p=<span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">10</span>];  <span class="comment">//指向堆中分配的内存首地址，p存储在栈区</span></span><br><span class="line"><span class="built_in">cin</span>&gt;&gt; p;</span><br><span class="line"><span class="keyword">delete</span> []p; <span class="comment">//p重新变为野指针</span></span><br></pre></td></tr></table></figure>



<p>3）指针操作超越了变量的作用范围。          </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *p=<span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">10</span>]; <span class="comment">//指向堆中分配的内存首地址</span></span><br><span class="line"><span class="built_in">cin</span>&gt;&gt; p;</span><br><span class="line"><span class="built_in">cout</span>&lt;&lt;*(p+<span class="number">10</span>); <span class="comment">//可能输出未知数据</span></span><br></pre></td></tr></table></figure>



<p><strong>指针的注意点：</strong></p>
<p><strong>a.指针指向常量存储区对象</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *p=<span class="string">"abc"</span>;</span><br></pre></td></tr></table></figure>

<p>此时p指向的是一个字符串常量，不能对*p的内容进行写操作，如srtcpy(p,s)是错误的，因为p的内容为“abc”字符串常量，该数据存储在常量存储区，但可以对指针p进行操作，让其指向其他的内存空间。</p>
<p><strong>b.资源泄漏</strong></p>
<p><strong>问题：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">char</span> *p = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">3</span>];  <span class="comment">//分配三个字符空间，p指向该内存空间</span></span><br><span class="line">    p = <span class="string">"ab"</span>;             <span class="comment">//此时p指向常量“ab”,而不再是new char分配的内存空间了，从而造成了资源泄漏</span></span><br><span class="line">    <span class="keyword">delete</span>[]p;         <span class="comment">//释放时报错</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果：报错</p>
<p>改进:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> *p=<span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">3</span>];  <span class="comment">//分配三个字符空间，p指向该内存空间</span></span><br><span class="line">    <span class="built_in">strcpy</span>(p,<span class="string">"ab"</span>);      <span class="comment">//将"ab"存储到p指向的内存空间</span></span><br><span class="line">    <span class="keyword">delete</span> []p;         <span class="comment">//ok</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>c.内存越界</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1 char *p&#x3D;new char[3];  &#x2F;&#x2F;分配三个字符空间，p指向该内存空间</span><br><span class="line">2 strcpy(p,&quot;abcd&quot;);    &#x2F;&#x2F;将abcd存处在分配的内存空间中，由于strlen(&quot;abcd&quot;)&#x3D;4&gt;3，越界</span><br><span class="line">3 delete []p;         &#x2F;&#x2F;ok</span><br></pre></td></tr></table></figure>



<p><strong>d.返回值是指针</strong></p>
<p><strong>问题：</strong>数组p[]中的内容为“hello world”,存储在栈区，函数结束时内容被清除，p变为野指针，可能导致乱码</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">char</span> *<span class="title">f</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> p[]=<span class="string">"abc"</span>;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;f()&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>有的编译器会报错,有的会执行但是出现乱码.</p>
<p><strong>改进：</strong></p>
<p>1.加static限定，延长数组生存期</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">char</span> *<span class="title">f</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">char</span> p[]=<span class="string">"abc"</span>;  <span class="comment">//此时数组为静态数组，存储在全局/静态区，生存期到程序结束，因此函数结束时不会销毁p</span></span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;f()&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>2.定义成指针型数组 </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">char</span> *<span class="title">f</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> *p=<span class="string">"abc"</span>;    <span class="comment">//"abc"存储在文字常量区，p是指向常量的指针，生存期到程序结束</span></span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;f()&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> <strong>e.指针做形参</strong></p>
<p>即所谓的地址传递，我们都知道地址传递的方式，形参的改变会导致实参的改变，但要注意的是，这里的改变是指指针所指内容的改变，而不是指针值的改变。因此，当形参改变会导致实参改变时，指针所指的内容是非const类型的，否则会出错。</p>
<p>1.改变指针内容：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="keyword">int</span> *a,<span class="keyword">int</span> *b)</span>   <span class="comment">//交换的是*a,*b，即指针的内容，而不是指针a,b</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="keyword">int</span> t;</span><br><span class="line">   t=*a;</span><br><span class="line">   *a=*b;</span><br><span class="line">   *b=t;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>2.改变指针值： </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">(<span class="keyword">char</span> *p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    p=<span class="string">"cba"</span>;    <span class="comment">//“cba”存放在文字常量区，让p指向常量"abc",这里改变的是指针值，实参并不会改变</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> *p=<span class="string">"abc"</span>;  <span class="comment">//“abc”存放在文字常量区，p指向常量"abc"</span></span><br><span class="line">    fun(p);</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;p&lt;&lt;<span class="built_in">endl</span>;   <span class="comment">//输出"abc",而不是"cba"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码会报错,不能将 “const char *” 类型的值分配到 “char *” 类型的实体,所以改为:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    p = <span class="string">"cba"</span>;    <span class="comment">//“cba”存放在文字常量区，让p指向常量"abc",这里改变的是指针值，实参并不会改变</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *p = <span class="string">"abc"</span>;  <span class="comment">//“abc”存放在文字常量区，p指向常量"abc"</span></span><br><span class="line">    fun(p);</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; p &lt;&lt; <span class="built_in">endl</span>;   <span class="comment">//输出"abc",而不是"bca"</span></span><br><span class="line">    system(<span class="string">"pause"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样的代码是失败的,因为VS告诉我们,Parameter ‘p’ is only assigned but neveraccessed.直接把第五行删了也没问题.</p>
<h2 id="二-指针和数组"><a href="#二-指针和数组" class="headerlink" title="二.指针和数组"></a>二.指针和数组</h2><p>说到指针和数组,这里直接说三个最基本的规则,这三个规则基本上能够概括指针和数组的常见关系了.</p>
<ol>
<li>一看到数组，就要知道<strong>数组名可以当做这个数组的第一个地址</strong>。（<code>a=&amp;a[0]</code>）</li>
<li><strong>对于指向数组的指针，指针+n表示往后移动n个位置</strong></li>
<li>指针也可以像数组那样用<code>p_a[n]</code>这种形式直接取元素，本质是<code>*(p_a+n)</code></li>
</ol>
<h2 id="三-指针和字符串"><a href="#三-指针和字符串" class="headerlink" title="三.指针和字符串"></a>三.指针和字符串</h2><p>指针和字符串相信是很多人一直弄混淆的地方了.其中确实有些地方需要理一理. <strong>指针和字符串主要是指针用于c风格的字符串</strong>.</p>
<p>这里首先把重要的几个规则列出来,这几个规则足够弄清楚指针和字符串之间的关系了.</p>
<ol>
<li>已经知道，<strong>c风格的字符串本质就是一个字符数组</strong>，所以，<strong>数组名就是第一个元素的地址</strong>。同样，一个指向char的指针变量也能够实现字符串的一些东西.</li>
<li><strong>C++中，对于引号引起来的字符串，也代表第一个元素的地址</strong>。</li>
<li><code>cout</code>对象认为，char的地址是字符串的地址，因此它打印该处的地址处的字符， 然后继续打印后面的字符。直到遇到空字符<code>’\0’</code>之后才停止。</li>
</ol>
<p>这里举一个例子来详细说明.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstring&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> animal[<span class="number">20</span>]=<span class="string">"bear"</span>;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* bird=<span class="string">"wren"</span>;</span><br><span class="line">    <span class="keyword">char</span>* p_s;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//数组名也是指针</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;animal&lt;&lt;<span class="string">" and "</span>&lt;&lt;bird&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//直接输出指针</span></span><br><span class="line">    p_s=animal;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"p_s:"</span>&lt;&lt;p_s&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//想要输出真的地址怎么办?</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"address of animal:"</span>&lt;&lt;(<span class="keyword">int</span>*)animal&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol>
<li>永远应该记住，双引号引起来的C风格的字符串常量，字符数组，字符指针变量这几个是相通等价的。</li>
<li>注意<code>const char* bird=&quot;wren&quot;</code>;要是不使用const会有警告,因为不能够用可变的指针来修改常量.</li>
<li>当cout后面是其他的指针的时候，会输出该指针的值也就是一个地址，但是cout遇到char* 的时候，就会输出当前char*指向的值，同时也会输出后面的值，直到遇到空字符为止。</li>
<li>所以在上面程序的20和21行有一个（<code>int*</code>）强制转化为整形指针，不然就没得输出了。</li>
</ol>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>接口与内部类（复习1）</title>
    <url>/2020/05/23/%E6%8E%A5%E5%8F%A3%E4%B8%8E%E5%8C%BF%E5%90%8D%E5%86%85%E9%83%A8%E7%B1%BB%EF%BC%88%E5%A4%8D%E4%B9%A01%EF%BC%89/</url>
    <content><![CDATA[<p>接口与匿名内部类.</p>
<a id="more"></a>

<h1 id="与匿名类配合"><a href="#与匿名类配合" class="headerlink" title="与匿名类配合"></a>与匿名类配合</h1><p>接口和匿名类配合在一起，减少代码量。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> test;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YLxia</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Computer computer = <span class="keyword">new</span> Computer();</span><br><span class="line">        Flash flash = <span class="keyword">new</span> Flash();</span><br><span class="line">        computer.transferData(flash);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Computer</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">transferData</span><span class="params">(USB usb)</span> </span>&#123;</span><br><span class="line">        usb.start();</span><br><span class="line">        System.out.println(<span class="string">"传输"</span>);</span><br><span class="line">        usb.stop();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">USB</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">start</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">stop</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Flash</span> <span class="keyword">implements</span> <span class="title">USB</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">start</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"U pan start"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">stop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"U pan stop"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Printer</span> <span class="keyword">implements</span> <span class="title">USB</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">start</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"flash start"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">stop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"flash stop"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如上代码所示，你得实现USB的子类，实现其方法才可以用。但是配合匿名类则不用将它们实现出来。</p>
<p>上面是第一种，创建了接口的非匿名实现类的非匿名对象。</p>
<p>还有三种：</p>
<p>接口的非匿名实现类的匿名对象；</p>
<p>接口的匿名实现类的非匿名对象；</p>
<p>接口的匿名实现类的匿名对象；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">computer.transferData(<span class="keyword">new</span> Printer());</span><br><span class="line">        USB phone = <span class="keyword">new</span> USB() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">start</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                System.out.println(<span class="string">"phone start"</span>);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">stop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                System.out.println(<span class="string">"phone stop"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        computer.transferData(phone);</span><br><span class="line">        computer.transferData(<span class="keyword">new</span> USB() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">start</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                System.out.println(<span class="string">"mp3 start"</span>);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">stop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                System.out.println(<span class="string">"mp3 stop"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br></pre></td></tr></table></figure>
<h1 id="接口备注："><a href="#接口备注：" class="headerlink" title="接口备注："></a>接口备注：</h1><p>为规范，接口是公共的，所以接口里的东西<strong>都是</strong>公共的，不论是否加上了public修饰符。<br>对于接口中常量，系统会自动加上public static final这三个修饰符，同时，这也意味着只能在定义是指定默认值。<br>接口里的方法全是抽象方法，而在Java8中允许在接口中定义默认方法。</p>
<h1 id="接口应用"><a href="#接口应用" class="headerlink" title="接口应用"></a>接口应用</h1><h2 id="代理模式"><a href="#代理模式" class="headerlink" title="代理模式"></a>代理模式</h2><p>下面是静态代理：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> test;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YLxia</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Server server = <span class="keyword">new</span> Server();</span><br><span class="line">        ProxyServer proxyServer = <span class="keyword">new</span> ProxyServer(server);</span><br><span class="line">        proxyServer.browse();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Network</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">browse</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//被代理类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Server</span> <span class="keyword">implements</span> <span class="title">Network</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">browse</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"真实的访问"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//代理类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProxyServer</span> <span class="keyword">implements</span> <span class="title">Network</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Network network;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ProxyServer</span><span class="params">(Network network)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.network = network;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">check</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"检查"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">browse</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        check();</span><br><span class="line">        network.browse();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> test;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YLxia</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> x=<span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> x=<span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> <span class="keyword">extends</span> <span class="title">B</span> <span class="keyword">implements</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">px</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="keyword">super</span>.x);</span><br><span class="line">        System.out.println(A.x);<span class="comment">//A中的x是全局常量</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> Test().px();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>1.当继承的父类和实现的接口中有相同签名的方法时，优先使用父类的方法。</p>
<p>2.当接口的父接口中也有同样的默认方法时，就近原则调用子接口的方法。</p>
<p>3.当实现的多个接口中有相同签名的方法时，必须在实现类中通过重写方法解决冲突问题，否者无法通过编译，在重写的方法中可以通过 接口名.super.方法名(); 的方式显示调用需要的方法。</p>
<h1 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h1><p>当实现类实现了多个接口，覆盖了接口的默认方法，然后在实现类中又想调用接口的默认方法，这时要这样写：</p>
<p>``java<br>InterfaceA.super.default();<br>InterfaceB.super.default();</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">意思是原本super.default();是调用父级方法，但现在分不清是哪个，所以在前面加上限定。</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>事件处理</title>
    <url>/2018/07/21/%E4%BA%8B%E4%BB%B6%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<p>&emsp;本文讲述GUI中的事件处理。</p>
<a id="more"></a>

<h1 id="模型及流程"><a href="#模型及流程" class="headerlink" title="模型及流程"></a>模型及流程</h1><p>&emsp;为了使图形界面能够接收用户的操作，必须给各个组件加上事件处理机制。在事件处理的过程中，主要涉及三类对象。</p>
<ul>
<li>Event Source （事件源）：事件发生的场所，通常就是各个组件，例如按钮、窗口、菜单等。</li>
<li>Event （事件）：事件封装了GUI组件上发生的特定事情（通常就是一次用户操作）。如果程序需 要获得GUI组件上所发生事件的相关信息，都通过Event对象来取得。</li>
<li>Event Listener（事件监听器）：负责监听事件源所发生的事件，并对各种事件做出响应处理。</li>
</ul>
<p>&emsp;当用户单击一个按钮，或者单击某个菜单项，或者单击窗口右上角的状态按钮时，这些动作就会触发一个相应的事件，该事件由AWT封装成相应的Event对象，该事件会触发事件源上注册的事件监听器（特殊的Java对象），事件监听器调用对应的事件处理器（事件监听器里的实例方法）来做出相应的响应。 AWT的事件处理机制是一种委派式（Delegation）事件处理方式——普通组件（事件源）将事件的处理工作委托给特定的对象（事件监听器）；当该事件源发生指定的事件时，就通知所委托的事件监听器，由事件监听器来处理这个事件。每个组件均可以针对特定的事件指定一个或多个事件监听对象，每个事件监听器也可以监听一个或多个事件源。因为同一个事件源上可能发生多种事件，委派式事件处理方式可以把事件源上可能发生的不同的事件分别授权给不同的事件监听器来处理；同时也可以让一类事件都使用同一个事件监听器来处理。</p>
<p><img src="/2018/07/21/%E4%BA%8B%E4%BB%B6%E5%A4%84%E7%90%86/%E6%B5%81%E7%A8%8B.png" alt="流程"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.ActionEvent;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.ActionListener;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/21 14:53</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EventQs</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"事件测试"</span>);</span><br><span class="line">    <span class="keyword">private</span> JButton ok = <span class="keyword">new</span> JButton(<span class="string">"确定"</span>);</span><br><span class="line">    <span class="keyword">private</span> TextField tf = <span class="keyword">new</span> TextField(<span class="number">30</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        ok.addActionListener(<span class="keyword">new</span> OkListener());</span><br><span class="line">        f.add(tf);</span><br><span class="line">        f.add(ok, BorderLayout.SOUTH);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">OkListener</span> <span class="keyword">implements</span> <span class="title">ActionListener</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">actionPerformed</span><span class="params">(ActionEvent e)</span> </span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"你单击了OK按钮"</span>);</span><br><span class="line">            tf.setText(<span class="string">"Hello"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> EventQs().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;上面的代码中JButton类组件OK按钮就是事件源，对于这个组件，用<code>ok.addActionListener(new OkListener());</code>注册事件监听器，然后实现了一个ActionListener接口，即定义了具体的事件监听器类。这个接口实际是一个函数式接口，因此可以使用Lambda表达式。</p>
<p>&emsp;从上面程序中可以看出，实现AWT事件处理机制的步骤如下:<br>1 实现事件监听器类，该监听器类是一个特殊的Java类 ，必须实现一个XxxListener接口。</p>
<p>2 创建普通组件(事件源)，创建事件监听器对象。</p>
<p>3 调用addXxxListener()方法将事件监听器对象注册给普通组件(事件源)。 当事件源上发生指定事件时，AWT会触发事件监听器，由事件监听器调用相应的方法(事件处理器)来处理事件，事件源上所发生的事件会作为参数传入事件处理器。</p>
<h1 id="事件和事件监听器"><a href="#事件和事件监听器" class="headerlink" title="事件和事件监听器"></a>事件和事件监听器</h1><p>&emsp;AWT事件机制涉及三个成员：事件源、事件和事件监听器，其中事件源最容易创建，只要通过new来创建一个AWT组件，该组件就是事件源；事件是由系统自动产生的，无须程序员关心。所以，实现事件监听器是整个事件处理的核心。<br>事件监听器必须实现事件监听器接口，AWT提供了大量的事件监听器接口用于实现不同类型的事件监听器，用于监听不同类型的事件。AWT中提供了丰富的事件类，用于封装不同组件上所发生的特定操作—— AWT的事件类都是AWTEvent类的子类，AWTEvent是 EventObject的子类。</p>
<p>&emsp;AWT事件分为两大类：低级事件和高级事件。</p>
<p>&emsp;1.低级事件<br>&emsp;低级事件是指基于特定动作的事件。比如进入、点击、拖放等动作的鼠标事件，当组件得到焦点、失去焦点时触发焦点事件。</p>
<ul>
<li>ComponentEvent：组件事件，当组件尺寸发生变化、位置发生移动、显示/隐藏状态发生改变时触发该事件。</li>
<li>ContainerEvent：容器事件，当容器里发生添加组件、删除组件时触发该事件。</li>
<li>WindowEvent：窗口事件，当窗口状态发生改变（如打开、关闭、最大化、最小化）时触发该 事件。</li>
<li>FocusEvent：焦点事件，当组件得到焦点或失去焦点时触发该事件。</li>
<li>KeyEvent：键盘事件，当按键被按下、松开、单击时触发该事件。</li>
<li>MouseEvent：鼠标事件，当进行单击、按下、松开、移动鼠标等动作时触发该事件。</li>
<li>PaintEvent：组件绘制事件，该事件是一个特殊的事件类型，当GUI组件调用叩date/paint方法 来呈现自身时触发该事件，该事件并非专用于事件处理模型。</li>
</ul>
<p>&emsp;2.高级事件（语义事件）<br>&emsp;高级事件是基于语义的事件，它可以不和特定的动作相关联，而依赖于触发此事件的类。比如，在TextField中按Enter键会触发ActionEvent事件，在滑动 条上移动滑块会触发AdjustmentEvent事件，选中项目列 表的某一项就会触ItemEvent事件。</p>
<ul>
<li>ActionEvent：动作事件，当按钮、菜单项被单击， 在 TextField中按Enter键时触发该事件。</li>
<li>AdjustmentEvent：调节事件，在滑动条上移动滑 块以调节数值时触发该事件。</li>
<li>ItemEvent：选项事件，当用户选中某项，或取消 选中某项时触发该事件。</li>
<li>TextEvent：文本事件，当文本框、文本域里的文 本发生改变时触发该事件。</li>
</ul>
<p><img src="/2018/07/21/%E4%BA%8B%E4%BB%B6%E5%A4%84%E7%90%86/%E4%BA%8B%E4%BB%B6%E7%BB%A7%E6%89%BF.png" alt="事件继承"></p>
<p>&emsp;不同的事件需要使用不同的监听器监听，不同的监听器需要实现不同的监听器接口，当指定事件发<br>生后，事件监听器就会调用所包含的事件处理器（实例方法）来处理事件。下表显示了事件、监听<br>器接口和处理器之间的对应关系。</p>
<p><img src="/2018/07/21/%E4%BA%8B%E4%BB%B6%E5%A4%84%E7%90%86/%E5%AF%B9%E5%BA%94%E5%85%B3%E7%B3%BB.png" alt="对应关系"></p>
<p>&emsp;下面是一个监听器监听多个组件，一个组件被多个监听器监听的效果。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.ActionEvent;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.ActionListener;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/23 17:30</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MultiListener</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试"</span>);</span><br><span class="line">    <span class="keyword">private</span> TextArea ta = <span class="keyword">new</span> TextArea(<span class="number">6</span>, <span class="number">40</span>);</span><br><span class="line">    <span class="keyword">private</span> Button b1 = <span class="keyword">new</span> Button(<span class="string">"按钮1"</span>);</span><br><span class="line">    <span class="keyword">private</span> Button b2 = <span class="keyword">new</span> Button(<span class="string">"按钮2"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        FirstListener f1 = <span class="keyword">new</span> FirstListener();</span><br><span class="line">        b1.addActionListener(f1);</span><br><span class="line">        b1.addActionListener(<span class="keyword">new</span> SecondListener());</span><br><span class="line">        b2.addActionListener(f1);</span><br><span class="line">        f.add(ta);</span><br><span class="line">        Panel p = <span class="keyword">new</span> Panel();</span><br><span class="line">        p.add(b1);</span><br><span class="line">        p.add(b2);</span><br><span class="line">        f.add(p, BorderLayout.SOUTH);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">FirstListener</span> <span class="keyword">implements</span> <span class="title">ActionListener</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">actionPerformed</span><span class="params">(ActionEvent e)</span> </span>&#123;</span><br><span class="line"><span class="comment">//            getActionCommand用于获得按钮上的名字，方便区分多个按钮，返回字符串，类似的getSource返回对象</span></span><br><span class="line">            ta.append(<span class="string">"第一个事件监听器被触发，事件源是："</span></span><br><span class="line">                    + e.getActionCommand() + <span class="string">"\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">SecondListener</span> <span class="keyword">implements</span> <span class="title">ActionListener</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">actionPerformed</span><span class="params">(ActionEvent e)</span> </span>&#123;</span><br><span class="line">            ta.append(<span class="string">"单击了"</span> + e.getActionCommand() + <span class="string">"\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> MultiListener().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;注意这里使用JButton和Button的效果不一样，两个监听器触发的顺序不一样。</p>
<p>&emsp;下面是关闭窗口的代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.WindowEvent;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.WindowListener;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/24 17:47</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WindowListenerTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试"</span>);</span><br><span class="line">    <span class="keyword">private</span> TextArea ta = <span class="keyword">new</span> TextArea(<span class="number">6</span>, <span class="number">40</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        f.addWindowListener(<span class="keyword">new</span> MyListener());</span><br><span class="line">        f.add(ta);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">MyListener</span> <span class="keyword">implements</span> <span class="title">WindowListener</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowOpened</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">            ta.append(<span class="string">"窗口被初次打开\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowClosing</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">            ta.append(<span class="string">"窗口正在被关闭\n"</span>);</span><br><span class="line">            System.exit(<span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowClosed</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">            ta.append(<span class="string">"窗口关闭成功\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowIconified</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">            ta.append(<span class="string">"窗口被最小化\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowDeiconified</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">            ta.append(<span class="string">"窗口被恢复\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowActivated</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">            ta.append(<span class="string">"窗口被激活\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowDeactivated</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">            ta.append(<span class="string">"窗口失去焦点\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> WindowListenerTest().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h1 id="事件适配器"><a href="#事件适配器" class="headerlink" title="事件适配器"></a>事件适配器</h1><p>&emsp;类似上面的代码中，当我们只需要关闭窗口的事件监听时，其他的监听器就是多余的，因此需要用到事件适配器。思路是，写一个类实现接口中的方法，但是方法体为空，当需要某个监听器时，继承改适配器，将某个监听器的方法体补全。</p>
<p>&emsp;下面列出了常用的监听器接口的适配器：</p>
<p><img src="/2018/07/21/%E4%BA%8B%E4%BB%B6%E5%A4%84%E7%90%86/%E9%80%82%E9%85%8D%E5%99%A8.png" alt="适配器"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.WindowAdapter;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.WindowEvent;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/25 23:28</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WindowAdapterTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试"</span>);</span><br><span class="line">    <span class="keyword">private</span> TextArea ta = <span class="keyword">new</span> TextArea(<span class="number">6</span>, <span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        f.addWindowListener(<span class="keyword">new</span> Mylistener());</span><br><span class="line">        f.add(ta);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">Mylistener</span> <span class="keyword">extends</span> <span class="title">WindowAdapter</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowClosing</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">super</span>.windowClosing(e);</span><br><span class="line">            System.out.println(<span class="string">"用户关闭窗口\n"</span>);</span><br><span class="line">            System.exit(<span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> WindowListenerTest().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="使用内部类实现监听器"><a href="#使用内部类实现监听器" class="headerlink" title="使用内部类实现监听器"></a>使用内部类实现监听器</h1><p>&emsp;在MultiListener的代码中，在一个外部类中包含了两个内部类，内部类方便复用，可以自由访问外部类的GUI组件，这是优点。</p>
<h1 id="使用外部类实现监听器"><a href="#使用外部类实现监听器" class="headerlink" title="使用外部类实现监听器"></a>使用外部类实现监听器</h1><p>&emsp;事件监听器<strong>通常</strong>是属于特定组件的，而且外部类不能自由访问GUI组件，所以外部类的形式用的不多。强烈不建议使用这种方式。</p>
<h1 id="类本身作为事件监听器类"><a href="#类本身作为事件监听器类" class="headerlink" title="类本身作为事件监听器类"></a>类本身作为事件监听器类</h1><p>&emsp;感觉是一种很变扭的写法，让GUI类继承事件适配器，然后直接在改类里写监听器，结构混乱了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.WindowAdapter;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.WindowEvent;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/26 0:09</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleEventHandler</span> <span class="keyword">extends</span> <span class="title">WindowAdapter</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试"</span>);</span><br><span class="line">    <span class="keyword">private</span> TextArea ta = <span class="keyword">new</span> TextArea(<span class="number">6</span>, <span class="number">40</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        f.addWindowListener(<span class="keyword">this</span>);</span><br><span class="line">        f.add(ta);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowClosing</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"用户关闭窗口\n"</span>);</span><br><span class="line">        System.exit(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> SimpleEventHandler().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="匿名内部类实现监听器"><a href="#匿名内部类实现监听器" class="headerlink" title="匿名内部类实现监听器"></a>匿名内部类实现监听器</h1><p>&emsp;最广泛的运用形式。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.awt.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.WindowAdapter;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.WindowEvent;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/7/26 0:20</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AnonymousEventHandler</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Frame f = <span class="keyword">new</span> Frame(<span class="string">"测试"</span>);</span><br><span class="line">    <span class="keyword">private</span> TextArea ta = <span class="keyword">new</span> TextArea(<span class="number">6</span>, <span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        f.addWindowListener(<span class="keyword">new</span> WindowAdapter() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowClosing</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">                System.out.println(<span class="string">"用户试图关闭窗口\n"</span>);</span><br><span class="line">                System.exit(<span class="number">0</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        f.add(ta);</span><br><span class="line">        f.pack();</span><br><span class="line">        f.setVisible(<span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> AnonymousEventHandler().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>数据库笔记1</title>
    <url>/2018/04/19/%E6%95%B0%E6%8D%AE%E5%BA%93%E7%AC%94%E8%AE%B01/</url>
    <content><![CDATA[<p>&emsp;前面一直没有重视数据库的知识,今天一上课发现有好多不懂的地方，现在开始补一补。本文将参考福达的SQL必知必会，强调SQL语句的使用，对于数据库的理论知识讲的不多。</p>
<a id="more"></a>

<h1 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h1><h2 id="数据库-表与模式"><a href="#数据库-表与模式" class="headerlink" title="数据库,表与模式"></a>数据库,表与模式</h2><p>&emsp;数据库：保存有组织的数据的容器。注意与数据库软件的区别，这些软件是DBMS，数据库管理系统。<br><br>&emsp;表:某种特定类型的数据的结构化清单，一个数据库中的表的名字绝不能相同，但是不同数据库中的可以。<br><br>&emsp;模式：关于数据库和表的布局及特征的信息。表具有一些特性，这些特性定义了数据如何存储，包含存储什么样数据，数据如何分解，各部分信息如何命名等信息，这就是模式。模式可以用于描述特定的表，也可以描述整个数据库。</p>
<h2 id="列和数据类型"><a href="#列和数据类型" class="headerlink" title="列和数据类型"></a>列和数据类型</h2><p>&emsp;列(column):表中的一个字段，所有表都是由一个或多个列组成。在使用时应注意分解数据，将一条信息分解为合适的n个字段。例如：对于一条详细的地址，有的时候可以只要一个字段，但是有的时候需要门牌号单独作为一个字段用于排序。<br><br>&emsp;数据类型：数据类型有多种，关于数字的，字符的，日期的，等等。问题在于，基本的数据类型基本都一样，但是部分高级的数据类型在某些SQL语言中是没有的。而且，偶然会有相同的数据类型在不同的DBMS有不同的名字。虽然有SQL标准，但是各大厂商显然没有对此达成一致。<br></p>
<h2 id="行和主键"><a href="#行和主键" class="headerlink" title="行和主键"></a>行和主键</h2><p>&emsp;行：表中的数据是按行存储的，所保存的每个记录存储在行内。<br>&emsp;主键：表中的每一行都应该有一列或几列唯一标识自己，顾客表可以使用顾客编号，订单表可以使用订单ID。可以没有主键，但是应该尽量保证每个表都有一个主键。主键的值不能重复，例如主键为学号，就不能有两个一样的学号。同时，也不能为空，不能某个学生没有学号。主键列中的值不允许修改或更新，也不能重用(删去某行后，这行的主键值不能给后面的新行)。<br><br>&emsp;显然，这本书对于理论知识介绍的非常简略，E-R图，外键等东西都没有（外键有，但是放到了非常后面）。</p>
<h1 id="检索数据"><a href="#检索数据" class="headerlink" title="检索数据"></a>检索数据</h1><h2 id="SELECT语句"><a href="#SELECT语句" class="headerlink" title="SELECT语句"></a>SELECT语句</h2><p>&emsp;<code>SELECT</code>作为关键字可以大写也可以小写，大小写混用也没关系，但不允许用作表或者列的名字。同时语句应用分号结尾，多条SQL语句也要用分号分隔。语句间的空格也将被忽略，所以一条语句可以分成几行写，也可以写在一行。使用SELECT检索表数据，必须给出检索的内容和检索的范围。</p>
<h3 id="检索单个列"><a href="#检索单个列" class="headerlink" title="检索单个列"></a>检索单个列</h3><p>输入：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> prod_name</span><br><span class="line"><span class="keyword">FROM</span> Products;</span><br></pre></td></tr></table></figure>

<p>分析：<br>上面这句从Products表中检索prod_name这列。<br><br>输出：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">prod_name</span><br><span class="line"><span class="comment">------------------</span></span><br><span class="line">Fish bean bag toy</span><br><span class="line">Bird bean bag toy</span><br><span class="line">Rabbit bean bag toy</span><br><span class="line">8 inch teddy bear</span><br><span class="line">12 inch teddy bear</span><br><span class="line">18 inch teddy bear</span><br><span class="line">Raggedy Ann</span><br><span class="line">King doll</span><br><span class="line">Queen doll</span><br></pre></td></tr></table></figure>

<p>当没有明确排序查询结果时，则返回的数据没有特定的顺序。</p>
<h3 id="检索多个列"><a href="#检索多个列" class="headerlink" title="检索多个列"></a>检索多个列</h3><p>输入：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> prod_id, prod_name, prod_price</span><br><span class="line"><span class="keyword">FROM</span> Products;</span><br></pre></td></tr></table></figure>

<p>分析：<br>从Products中检索出三列数据。<br><br>输出:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">prod_id       prod_name                prod_price</span><br><span class="line"><span class="comment">---------     --------------------     ---------</span></span><br><span class="line">BNBG01        Fish bean bag toy        3.4900</span><br><span class="line">BNBG02        Bird bean bag toy        3.4900</span><br><span class="line">BNBG03        Rabbit bean bag toy      3.4900</span><br><span class="line">BR01          8 inch teddy bear        5.9900</span><br><span class="line">BR02          12 inch teddy bear       8.9900</span><br><span class="line">BR03          18 inch teddy bear       11.9900</span><br><span class="line">RGAN01        Raggedy Ann              4.9900</span><br><span class="line">RYL01         King doll                9.4900</span><br><span class="line">RYL02         Queen dool               9.4900</span><br></pre></td></tr></table></figure>

<h3 id="检索所有列"><a href="#检索所有列" class="headerlink" title="检索所有列"></a>检索所有列</h3><p>输入：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> Products;</span><br></pre></td></tr></table></figure>

<h3 id="检索不同的值"><a href="#检索不同的值" class="headerlink" title="检索不同的值"></a>检索不同的值</h3><p>&emsp;假定某一列的值存在重复的，但是只想检索出具有唯一性的值时就可以用<code>DISTINCT</code>。例如，供应商ID只有3个，但是检索产品ID时，会检索出许多个，这时就要用该关键字筛选。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">DISTINCT</span> vend_id</span><br><span class="line"><span class="keyword">FROM</span> Products;</span><br></pre></td></tr></table></figure>

<p>&emsp;注意，如果该关键字的后面指定了多个列，这种筛选将会在所有列进行。</p>
<h3 id="限制结果"><a href="#限制结果" class="headerlink" title="限制结果"></a>限制结果</h3><p>&emsp;检索的结果可能会很多，甚至是每一行。当只需要查看一定数量的行时，也可以写出SQL语句。但是各数据库的SQL语句并不一样。<br><br>在SQL Server和Access中可以使用TOP关键字限制<strong>最多</strong>返回几行:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> TOP <span class="number">5</span> prod_name</span><br><span class="line"><span class="keyword">FROM</span> Products;</span><br></pre></td></tr></table></figure>

<p>DB2:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> prod_name</span><br><span class="line"><span class="keyword">FROM</span> Products</span><br><span class="line"><span class="keyword">FETCH</span> <span class="keyword">FIRST</span> <span class="number">5</span> <span class="keyword">ROWS</span> <span class="keyword">ONLY</span>;</span><br></pre></td></tr></table></figure>

<p>Oracle:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> prod_name</span><br><span class="line"><span class="keyword">FROM</span> Products</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">ROWNUM</span> &lt;= <span class="number">5</span>;</span><br></pre></td></tr></table></figure>

<p>MySQL、MariaDB、PostgreSQL或者SQLite:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> prod_name</span><br><span class="line"><span class="keyword">FROM</span> Products</span><br><span class="line"><span class="keyword">LIMIT</span> <span class="number">5</span>;</span><br></pre></td></tr></table></figure>

<p>若要得到后面的数据可以这么写：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> prod_name</span><br><span class="line"><span class="keyword">FROM</span> Products</span><br><span class="line"><span class="keyword">LIMIT</span> <span class="number">5</span> <span class="keyword">OFFSET</span> <span class="number">5</span>;</span><br></pre></td></tr></table></figure>

<p>&emsp;<code>LIMIT 5 OFFSET 5</code>指示MySQL等DBMS返回从第5行起的5行数据。第一个数字是指从哪儿开始，第二个数字是检索的行数。</p>
]]></content>
      <tags>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>数组拾遗</title>
    <url>/2018/02/01/%E6%95%B0%E7%BB%84%E6%8B%BE%E9%81%97/</url>
    <content><![CDATA[<p>数组运用的已经较为熟练了，但是还有些细节没注意。本文后面主要是字符串。</p>
<a id="more"></a>

<h1 id="1-数组初始化"><a href="#1-数组初始化" class="headerlink" title="1.数组初始化"></a>1.数组初始化</h1><p>初始化数组时，提供的值可以少于数组的元素数目。例如，下面的语句只初始化hotelTips的前两个元素：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">float</span> hotelTips[<span class="number">5</span>]=&#123;<span class="number">5.0</span>,<span class="number">2.5</span>&#125;;</span><br></pre></td></tr></table></figure>

<p>如果只对数组的一部分进行初始化，则编译器将把其他元素设置为0。因此，将数组中所有的元素都初始化为0 非常简单—— 只要显式地将第一个元素初始化为0,然后让编译器将其他元素都初始化为0即可：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">long</span> totals[<span class="number">500</span>]=&#123;<span class="number">0</span>&#125;;</span><br></pre></td></tr></table></figure>

<p>如果初始化数组时方括号内（[]）为空，C++编译器将计算元素个数。例如，对于下面的声明：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">short things[]=&#123;<span class="number">1</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">8</span>&#125;;</span><br></pre></td></tr></table></figure>


<p>编译器将使things数组包含4 个元素。</p>
<h1 id="2-C-11数组初始化方法"><a href="#2-C-11数组初始化方法" class="headerlink" title="2.C++11数组初始化方法"></a>2.C++11数组初始化方法</h1><p>C++11将使用大括号的初始化（列表初始化）作为一种通用初始化方式，可用于所有类型。数组以前就可使用列表初始化，但 C++11中的列表初始化新增了一些功能。</p>
<p>首先，初始化数组时，可省略等号（=）:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> earning[<span class="number">4</span>] &#123;<span class="number">1.2e4</span>,<span class="number">1.6e4</span>,<span class="number">1.1e4</span>,<span class="number">1.7e4</span>&#125;;</span><br></pre></td></tr></table></figure>

<p>其次，可不在大括号内包含任何东西，这将把所有元素都设置为零：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> counts[<span class="number">10</span>] = &#123;&#125;;</span><br><span class="line"><span class="keyword">float</span> balances[<span class="number">100</span>] &#123;&#125;;</span><br></pre></td></tr></table></figure>

<p>第三，列表初始化禁止缩窄转换。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">long</span> plifs[] = &#123;<span class="number">25</span>,<span class="number">92</span>,<span class="number">3.0</span>&#125;;				<span class="comment">//错误</span></span><br><span class="line"><span class="keyword">char</span> slifs[<span class="number">4</span>] &#123;<span class="string">'h'</span>,<span class="string">'i'</span>,<span class="number">1122011</span>,<span class="string">'\0'</span>&#125;;	 <span class="comment">//错误</span></span><br><span class="line"><span class="keyword">char</span> tlifs[<span class="number">4</span>] &#123;<span class="string">'h'</span>,<span class="string">'i'</span>,<span class="number">112</span>,<span class="string">'\0'</span>&#125;;</span><br></pre></td></tr></table></figure>

<p>在VS和code block中均出现警告或错误。</p>
<p>在上述代码中，第一条语句不能通过编译，因为将浮点数转换为整型是缩窄操作，即使浮点数的小数点后面为零。第二条语句也不能通过编译，因为1122011超出了char变量的取值范围（这里假设char变量的长度为8 位）。第三条语句可通过编译，因为虽然112是一个int值，但它在char变量的取值范围内。</p>
<h1 id="3-字符串"><a href="#3-字符串" class="headerlink" title="3.字符串"></a>3.字符串</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> cat[<span class="number">4</span>] = &#123;<span class="string">'c'</span>,<span class="string">'a'</span>,<span class="string">'t'</span>,<span class="string">'\0'</span>&#125;;	<span class="comment">//a string</span></span><br><span class="line"><span class="keyword">char</span> cat2[<span class="number">4</span>] = &#123;<span class="string">'c'</span>,<span class="string">'a'</span>,<span class="string">'t'</span>,<span class="string">'!'</span>&#125;;	<span class="comment">//a char array but not a string</span></span><br></pre></td></tr></table></figure>

<p>这两个数组都是char数组，但只有第二个数组是字符串。空字符对C-风格字符串而言至关重要。例如， C++有很多处理字符串的函数，其中包括cout使用的那些函数。它们都逐个地处理字符串中的字符，直到到达空字符为止。如果使用cout显示上面的cat这样的字符串，则将显示前3个字符，发现空字符后停止，但是，如果使用cout显示上面的cat2数组（它不是字符串），cout将打印出数组中的8个字母，并接着将内存中随后的各个字节解释为要打印的字符，直到遇到空字符为止。于空字符（实际上是被设置为0 的字节）在内存中很常见，因此这一过程将很快停止。但尽管如此，还是不应将不是字符串的字符数组当作字符串来处理。在VS的调试中，观察到cat2出现了“烫烫”的乱码，改为cat2[5]后，出现“字符串无效”。</p>
<p>有一种更好的、将字符数组初始化为字符串的方法—— 只需使用一个用引号括起的字符串即可，这种字符串被称为字符串常量（string constant）或字符串字面值（string literal）, 如下所示:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> bird[<span class="number">11</span>] = <span class="string">"Mr.Cheeps"</span>;</span><br><span class="line"><span class="keyword">char</span> fish[] = <span class="string">"Bubbles"</span>;</span><br></pre></td></tr></table></figure>

<p>用引号括起的字符串隐式地包括结尾的空字符，因此不用显式地包括它（参见图4.2）。另外，各种C++输入工具通过键盘输入，将字符串读入到char数组中时，将自动加上结尾的空字。</p>
<p>应确保数组足够大，能够存储字符串中所有字符——包括空字符。使用字符串常量初始化字符数组是这样的一种情况，即让编译器计算元素数目更为安全。让数组比字符串长没有什么害处，只是会浪费一些空间而己。这是因为处理字符串的函数根据空字符的位置，而不是数组长度来进行处理。C++对字符串长度没有限制。</p>
<p>注意，字符串常量（使用双引号）不能与字符常量（使用单引号）互换。字符常量（如’S’） 是字符串编码的简写表示。在 ASCII系统上，’S‘只是83的另一种写法，因此，下面的语句将83赋给shirt_size：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> shirt_size = <span class="string">'S'</span>;</span><br></pre></td></tr></table></figure>

<p><strong>更加值得注意的是:”S”表示的是地址，而不是字符！</strong></p>
<p>对于<code>char</code>型，只有单引号；</p>
<p>对于<code>char</code>型数组，有单引号和双引号；</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> cat[<span class="number">4</span>] = &#123;<span class="string">'c'</span>,<span class="string">'a'</span>,<span class="string">'t'</span>,<span class="string">'\0'</span>&#125;;</span><br><span class="line"><span class="keyword">char</span> bird[<span class="number">11</span>] = <span class="string">"Mr.Cheeps"</span>;</span><br></pre></td></tr></table></figure>

<p>进一步的探讨在指针部分。</p>
<h1 id="4-在数组中使用字符串"><a href="#4-在数组中使用字符串" class="headerlink" title="4.在数组中使用字符串"></a>4.在数组中使用字符串</h1><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> str1[<span class="number">10</span>];</span><br><span class="line"><span class="built_in">cin</span>&gt;&gt;str1;</span><br></pre></td></tr></table></figure>

<p>不需要像其他类型数组那样，一个一个地给元素赋值。</p>
<p>cin使用空白(空格、制表符和换行符)来确定字符串的结束位置，这意味着cin在获 取字符数组输入时只读取一个单词。读取该单词后，cin将该字符串放到数组中，并自动在结尾添加空字符。</p>
<p>这种读取方式有时会带来一些麻烦。</p>
<h2 id="1-面向行的输入：cin-getline"><a href="#1-面向行的输入：cin-getline" class="headerlink" title="(1).面向行的输入：cin.getline()"></a>(1).面向行的输入：cin.getline()</h2><p>该函数有三个参数。第一个参数是用来存储输入行的数组的名称，第二个参数是要读取的字符数，第三个是终止字符，默认是’\n’。如果这个参数为20 ,则函数最多读取19个字符，余下的空间用于存储自动在结尾处添加的空字符。getline()成员函数在读取指定数目的字符或遇到换行符时停止读取。</p>
<h2 id="2-面向行的输入：cin-get"><a href="#2-面向行的输入：cin-get" class="headerlink" title="(2).面向行的输入：cin.get()"></a>(2).面向行的输入：cin.get()</h2><p>istream类有另一个名为get()的成员函数，该函数有几种变体。其中一种变 体的工作方式与getline()类似，它们接受的参数相同，解释参数的方式也相同，并且都读取到<strong>行尾</strong>。但get并不再读取并丢弃换行符，而是将其留在输入队列中。假设我们连续两次调用get():</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cin</span>.get(name,ArSize);</span><br><span class="line"><span class="built_in">cin</span>.get(dessert,ArSize);</span><br></pre></td></tr></table></figure>

<p>由于第一次调用后，换行符将留在输入队列中，因此第二次调用时看到的第一个字符便是换行符。因此 get()认为已到达行尾，而没有发现任何可读取的内容。</p>
<p>使用不带任何参数的cin.get()调用可读取下一个字符(即使是换行符)， 因此可以用它来处理换行符，为读取下一行输入做好准备。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cin</span>.get(name,ArSize);</span><br><span class="line"><span class="built_in">cin</span>.get();</span><br><span class="line"><span class="built_in">cin</span>.get(dessert,ArSize);</span><br></pre></td></tr></table></figure>

<p>另一种使用get()的方式是将两个类成员函数拼接起来(合并)，如下所示：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cin.get(name,ArSize).get();</span><br></pre></td></tr></table></figure>

<p>之所以可以这样做，是由于cin.get (name, ArSize) 返回一个cin对象，该对象随后将被用来调用get()函数。</p>
<h2 id="Tips：缓冲区"><a href="#Tips：缓冲区" class="headerlink" title="Tips：缓冲区"></a>Tips：缓冲区</h2><p>缓冲区是输入时非常重要的一环，当我们在键盘上按下<code>name</code>并回车时，实际上是进入了缓冲区，现在里面的字符是<code>name</code>和<code>\n</code>。’\n‘代表了回车。每当cin读取字符时，读的就是缓冲区里的东西。很多时候输入是复杂的，空格，回车，换行夹杂在一起，要正确的读取输入关键就是<strong>明确使用的函数的终止符和是否会丢弃终止符。</strong>例如：<code>cin</code>遇到空格，回车，换行就停止，同时还将它们丢弃（排除），所以读两行输入时，没有像<code>cin.get()</code>那样要读取掉一个换行符。</p>
<h1 id="5-string类"><a href="#5-string类" class="headerlink" title="5.string类"></a>5.string类</h1><h2 id="Tips："><a href="#Tips：" class="headerlink" title="Tips："></a>Tips：</h2><p><code>strlen</code>函数用来确定字符串长度，需要<code>cstring</code>头文件，老式的则是<code>string.h</code>，string类则需要<code>string</code>头文件。在C/C++中，头文件要么是开头加``c<code>要么结尾加</code>.h<code>，或者都不加。C一般是前面两种，C++是后面这种。也有例外，如</code>stdio.h<code>和</code>iostream`。</p>
<p>要使用string类，还需要名称空间std。使用时，string是将字符串作为一种数据类型的。string类定义隐藏了字符串的数组性质，能够像处理普通变量那样处理字符串。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line">  <span class="built_in">string</span> str1;</span><br><span class="line">  <span class="built_in">string</span> str2 = <span class="string">"hello"</span>;</span><br><span class="line">  <span class="built_in">string</span> str3 = &#123;<span class="string">"world"</span>&#125;;</span><br><span class="line">  <span class="built_in">string</span> str4 += str3;</span><br><span class="line">  <span class="built_in">cin</span>&gt;&gt;str1;</span><br><span class="line">  <span class="built_in">cout</span>&lt;&lt;str1+str2;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>类设计让程序能够自动处理string的大小。例如，<code>str1</code>的声明创建一个长度为0 的 string对象，但程序 将输入读取到<code>str1</code>中时，将自动调整<code>str1</code>的长度。</p>
<p>这使得与使用数组相比，使用string对象更方便，也更安全。从理论上说，可以将char数组视为一组用于存储一个字符串的char存储单元，而 string类变量是一个表示字符串的实体。</p>
<p>同时，string类与变量一样，可以赋值，拼接和附加。</p>
<p>在新增string类之前，要把一个字符串赋给另一个字符串是不能直接用等号的。附加字符到末尾也不能用’+=‘。而是要用到<code>cstring</code>中的函数。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;			//make string class available</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstring&gt;			//C-style string library</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line">	<span class="keyword">char</span> charr1[<span class="number">20</span>];</span><br><span class="line">	<span class="keyword">char</span> charr2[<span class="number">20</span>] = <span class="string">"jaguar"</span>;</span><br><span class="line">	<span class="built_in">string</span> str1;</span><br><span class="line">	<span class="built_in">string</span> str2 = <span class="string">"panther"</span>;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// assignment for string objects and character arrays </span></span><br><span class="line">	str1 = str2;				<span class="comment">// copy str2 to strl</span></span><br><span class="line">	<span class="built_in">strcpy</span>(charr1, charr2);	 <span class="comment">//copy charr2 to charrl</span></span><br><span class="line"></span><br><span class="line">							 <span class="comment">// appending for string objects and character arrays </span></span><br><span class="line">	str1 += <span class="string">" paste"</span>;			<span class="comment">//add paste to end of strl</span></span><br><span class="line">	<span class="built_in">strcat</span>(charr1, <span class="string">" juice"</span>);	 <span class="comment">//add juice to end of charrl</span></span><br><span class="line"></span><br><span class="line">								 <span class="comment">// finding the length of a string object and a C-style string </span></span><br><span class="line">	<span class="keyword">int</span> len1 = str1.<span class="built_in">size</span>();	 <span class="comment">//obtain length of	strl</span></span><br><span class="line">	<span class="keyword">int</span> len2 = <span class="built_in">strlen</span>(charr1);	<span class="comment">//obtain length of charrl</span></span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"The string "</span> &lt;&lt; str1 &lt;&lt; <span class="string">" contains "</span> &lt;&lt; len1 &lt;&lt; <span class="string">" characters.\n"</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"The string "</span> &lt;&lt; charr1 &lt;&lt; <span class="string">" contains "</span> &lt;&lt; len2 &lt;&lt; <span class="string">" characters.\n"</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意，以上代码在VS中会有两个警告和两个错误：</p>
<p><img src="/2018/02/01/%E6%95%B0%E7%BB%84%E6%8B%BE%E9%81%97/warning.png" alt="warning"></p>
<p>第一个可能丢失数据是因为<code>str1.size</code>的返回值类型是<code>size_t</code>,查看定义发现是<code>unsigned long long int</code>型，将<code>len1</code></p>
<p>和<code>len2</code>改为这种类型就可以，不改也没关系。</p>
<p>第三个是因为在VS看来<code>strcpy</code>和<code>strcat</code>这种老式的函数存在安全问题，微软重新定义了一个一样的但更安全的函数<code>strcpy_s</code>和<code>strcat_s</code>。改成这样就可以了。当然，也可以关闭这种安全检查。错误信息里已经提示了。</p>
<p>可以看到，string类的使用比C函数方便多了，也更安全。例如在C中：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> site[<span class="number">10</span>] = <span class="string">"house"</span>;</span><br><span class="line"><span class="built_in">strcat</span>(site,<span class="string">" of pancakes"</span>);</span><br></pre></td></tr></table></figure>

<p>这将会覆盖相邻内存导致一些错误。而string类可以自动调整大小。C 函数库确实提供了与strcat()和 strcpy()类似的函数— stmcat()和 stmcpy() , 它们接受指出目标数组最大允许长度的第三个参数，因此更为安全，但使用它们进一步增加了编写程序的复杂度。</p>
<h2 id="string类的I-O"><a href="#string类的I-O" class="headerlink" title="string类的I/O"></a>string类的I/O</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;	// make string class available</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstring&gt;	// C-style string library</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>; <span class="keyword">char</span> charr[<span class="number">20</span>]; <span class="built_in">string</span> str;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of string in charr before input:"</span> &lt;&lt; <span class="built_in">strlen</span>(charr) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of string in str before input:"</span> &lt;&lt; str.size() &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"Enter a line of text:\n"</span>;</span><br><span class="line">	<span class="built_in">cin</span>.getline(charr, <span class="number">20</span>);	<span class="comment">// indicate maximum length</span></span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"You entered： "</span> &lt;&lt; charr &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"Enter another line of text:\n"</span>;</span><br><span class="line">	getline(<span class="built_in">cin</span>, str);	<span class="comment">// cin now an argument； no length specifier</span></span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"You entered： "</span> &lt;&lt; str &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of string in charr after input:"</span> &lt;&lt; <span class="built_in">strlen</span>(charr) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"Length of string in str after input:"</span> &lt;&lt; str.size() &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	system(<span class="string">"pause"</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在用户输入之前，该程序指出数组charr中的字符串长度为27 ,这比该数组的长度要大。这里要两点需要说明。首先，未初始化的数组的内容是未定义的；其次，函数strlen()从数组的第一个元素开始计算字节数，直到遇到空字符。在这个例子中，对于未被初始化的数据，第一个空字符的出现位置是随机的。</p>
<p>注意到第13行，string类读取行时，与第十行的有着显著的区别。这里没有使用句点表示法，这表明这个getline()不是类方法。它 将 cin作为参数，指出到哪里去查找 输入。另外，也没有指出字符串长度的参数，因为string对象将根据字符串的长度自动调整自己的大小。</p>
<p>为何第十行是istream的类方法，第十三行的不是呢？这是因为string类是C++98才出现的，更早的istream在设计时没有考虑到string类型。但是这种代码为何可行呢？</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cin</span>&gt;&gt;str;<span class="comment">//str is a string object</span></span><br></pre></td></tr></table></figure>

<p>这里涉及到string类的一个友元函数，在函数部分在讲。</p>
]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>新特性</title>
    <url>/2020/06/10/%E6%96%B0%E7%89%B9%E6%80%A7/</url>
    <content><![CDATA[<p>Java中一些新特性的学习.</p>
<a id="more"></a>

<h1 id="Lambda和方法引用"><a href="#Lambda和方法引用" class="headerlink" title="Lambda和方法引用"></a>Lambda和方法引用</h1><p>对于函数式接口，原本需要创建匿名类来重写其中的抽象方法，如下所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Comparator&lt;Integer&gt; comparator = <span class="keyword">new</span> Comparator&lt;&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(Integer o1, Integer o2)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> Integer.compare(o1, o2);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">int</span> compare = comparator.compare(<span class="number">12</span>, <span class="number">13</span>);</span><br><span class="line">System.out.println(compare);</span><br></pre></td></tr></table></figure>
<p>上面的代码可以缩减为：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Comparator&lt;Integer&gt; comparator = (o1, o2) -&gt; Integer.compare(o1, o2);</span><br><span class="line">System.out.println(comparator.compare(<span class="number">20</span>, <span class="number">25</span>));</span><br></pre></td></tr></table></figure>

<p>省略了方法名，方法体的大括号，return。</p>
<p>还可以用方法引用进一步简化。当Lambda表达式内的方法是现成的，就可以进一步省略：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Comparator&lt;Integer&gt; comparator = Integer::compare;</span><br></pre></td></tr></table></figure>

<p>再举一个具体的例子:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.java8.demo;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">StringFunc</span> </span>&#123;</span><br><span class="line">    <span class="function">String <span class="title">func</span><span class="params">(String string)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MethodRefDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">stringOp</span><span class="params">(StringFunc sf, String s)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sf.func(s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String inStr = <span class="string">"lambda"</span>;</span><br><span class="line">        String s = stringOp(<span class="keyword">new</span> StringFunc() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> String <span class="title">func</span><span class="params">(String string)</span> </span>&#123;</span><br><span class="line">                String result = <span class="string">""</span>;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> i = string.length() - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">                    result += string.charAt(i);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">return</span> result;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;, inStr);</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码中stringOp方法第一个参数需要一个StringFunc接口的实例，于是用匿名类写了一个，并且重写了func方法来反转字符串。现在将这一部分换成Lambda表达式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    String inStr = <span class="string">"lambda"</span>;</span><br><span class="line">    String s = stringOp(string -&gt; &#123;</span><br><span class="line">        String result = <span class="string">""</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = string.length() - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">            result += string.charAt(i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;, inStr);</span><br><span class="line">    System.out.println(s);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里函数式接口的实例用一个稍微长一点的Lambda来表示。现在把反转的逻辑抽出来，使Lambda短一点：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MethodRefDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">stringOp</span><span class="params">(StringFunc sf, String s)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sf.func(s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String inStr = <span class="string">"lambda"</span>;</span><br><span class="line">        String s = stringOp(string -&gt; &#123;</span><br><span class="line">            <span class="keyword">return</span> reverse(string);</span><br><span class="line">        &#125;, inStr);</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> String <span class="title">reverse</span><span class="params">(String string)</span> </span>&#123;</span><br><span class="line">        String result = <span class="string">""</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = string.length() - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">            result += string.charAt(i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>将反转的逻辑抽出来作为一个方法，再去掉点括号和return：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    String inStr = <span class="string">"lambda"</span>;</span><br><span class="line">    String s = stringOp(string -&gt; reverse(string), inStr);</span><br><span class="line">    System.out.println(s);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>写成这样后，还能继续简略。这里的reverse方法是现成的，不像上上一步中要在Lambda表达式中自己写反转逻辑。而方法引用就是为此准备的，对于Lamdba表达式中已经存在的方法可以简化为：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    String inStr = <span class="string">"lambda"</span>;</span><br><span class="line">    String s = stringOp(MethodRefDemo::reverse, inStr);</span><br><span class="line">    System.out.println(s);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种是类::静态方法的引用。把上面reverse方法放到一个类中而且是非静态方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">op</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">reverse</span><span class="params">(String string)</span> </span>&#123;</span><br><span class="line">        String result = <span class="string">""</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = string.length() - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">            result += string.charAt(i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MethodRefDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">stringOp</span><span class="params">(StringFunc sf, String s)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sf.func(s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        op op = <span class="keyword">new</span> op();</span><br><span class="line">        String inStr = <span class="string">"lambda"</span>;</span><br><span class="line">        String s = stringOp(op::reverse, inStr);</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面就是实例::非静态方法。下面是另一种，若Lambda参数列表中的第一个参数是实例方法的参数调用者，而第二个参数是实例方法的参数时，可以使用对象方法引用。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test8</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Comparator&lt;String&gt; comparator = (s1, s2) -&gt; s1.compareTo(s2);</span><br><span class="line">    System.out.println(comparator.compare(<span class="string">"ac"</span>, <span class="string">"ab"</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是Lambda写法，参数一s1是实例方法compareTo的调用者，而第二个参数s2是compareTo的参数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test8</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Comparator&lt;String&gt; comparator = String::compareTo;</span><br><span class="line">    System.out.println(comparator.compare(<span class="string">"ac"</span>, <span class="string">"ab"</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是构造方法引用：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test9</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Supplier&lt;Person&gt; sp1 = () -&gt; <span class="keyword">new</span> Person();</span><br><span class="line">    Supplier&lt;Person&gt; sp2 = Person::<span class="keyword">new</span>;<span class="comment">//Supplier&lt;T&gt;，T代表返回值</span></span><br><span class="line">    sp2.get().info();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>无参数的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test9</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Function&lt;Integer,Person&gt; function1= id -&gt; <span class="keyword">new</span> Person(id);</span><br><span class="line">    Function&lt;Integer,Person&gt; function2= Person::<span class="keyword">new</span>;<span class="comment">//Function&lt;T, R&gt;，T代表参数，R代表返回值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>一个参数的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test9</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    BiFunction&lt;String, Integer, Person&gt; function1 = (name, age) -&gt; <span class="keyword">new</span> Person(name, age);</span><br><span class="line">    BiFunction&lt;String, Integer, Person&gt; function2 = Person::<span class="keyword">new</span>;<span class="comment">//BiFunction&lt;T, U, R&gt;，T,U代表两个参数，R代表返回值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>两个参数的。</p>
<p>与构造器引用类似的数组引用:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test9</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Function&lt;Integer, String[]&gt; function1 = length -&gt; <span class="keyword">new</span> String[length];</span><br><span class="line">    Function&lt;Integer, String[]&gt; function2 = String[]::<span class="keyword">new</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Stream"><a href="#Stream" class="headerlink" title="Stream"></a>Stream</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.java8.Stream;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.*;</span><br><span class="line"><span class="keyword">import</span> java.util.stream.Collectors;</span><br><span class="line"><span class="keyword">import</span> java.util.stream.IntStream;</span><br><span class="line"><span class="keyword">import</span> java.util.stream.Stream;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StreamTest</span> </span>&#123;</span><br><span class="line">    <span class="comment">//Stream自己不存储数据，只操作数据</span></span><br><span class="line">    <span class="comment">//Stream不改变源对象，会返回一个新Stream</span></span><br><span class="line">    <span class="comment">//Stream延迟执行，终止操作时才会一次执行完</span></span><br><span class="line">    <span class="comment">//终止操作产生的结果不能再被Stream使用</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;<span class="comment">//得到stream</span></span><br><span class="line">        List&lt;Employee&gt; employees = Employee.getEmployees();</span><br><span class="line">        <span class="comment">//得到一个顺序流</span></span><br><span class="line">        Stream&lt;Employee&gt; stream1 = employees.stream();</span><br><span class="line">        <span class="comment">//得到一个并行流</span></span><br><span class="line">        Stream&lt;Employee&gt; parallelStream = employees.parallelStream();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span>[] arr = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;</span><br><span class="line">        <span class="comment">//如果是用基本数据类型来获得流，返回的就是XxxStream;如果是自定义类型的，那就是Stream&lt;T&gt;</span></span><br><span class="line">        IntStream stream2 = Arrays.stream(arr);</span><br><span class="line">        <span class="comment">//包装类也是用泛型</span></span><br><span class="line">        List&lt;Integer&gt; integers = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line">        Stream&lt;Integer&gt; stream3 = integers.stream();</span><br><span class="line"></span><br><span class="line">        Stream&lt;Integer&gt; stream = Stream.of(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//上面三种流是有限流，下面是无限流</span></span><br><span class="line">        <span class="comment">// Stream从0开始，经过UnaryOperator函数加2，得到一个新Stream，再次使用UnaryOperator函数加2，一直循环下去，这里加了中间操作，限制为10次</span></span><br><span class="line">        Stream.iterate(<span class="number">0</span>, t -&gt; t + <span class="number">2</span>).limit(<span class="number">10</span>).forEach(System.out::println);</span><br><span class="line">        <span class="comment">//生成10个随机数</span></span><br><span class="line">        Stream.generate(Math::random).limit(<span class="number">10</span>).forEach(System.out::println);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> </span>&#123;<span class="comment">//中间操作</span></span><br><span class="line">        List&lt;Employee&gt; list = Employee.getEmployees();</span><br><span class="line">        Stream&lt;Employee&gt; stream = list.stream();</span><br><span class="line"></span><br><span class="line">        <span class="comment">//筛选和切片</span></span><br><span class="line">        stream.filter(e -&gt; e.getSalary() &gt; <span class="number">6500</span>).forEach(System.out::println);</span><br><span class="line">        <span class="comment">//不能在stream上接着操作，“水流一旦流过就不能回到开头去操作”，limit(3)，限制个数为前三条数据</span></span><br><span class="line">        list.stream().limit(<span class="number">3</span>).forEach(System.out::println);</span><br><span class="line">        <span class="comment">//跳过三个，若是流内元素不足，返回一个空流</span></span><br><span class="line">        list.stream().skip(<span class="number">3</span>).forEach(System.out::println);</span><br><span class="line">        <span class="comment">//distinct筛选，通过流内元素的hashcode和equals方法去重</span></span><br><span class="line">        list.stream().distinct().forEach(System.out::println);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//映射</span></span><br><span class="line">        List&lt;String&gt; list1 = Arrays.asList(<span class="string">"aac"</span>, <span class="string">"bb"</span>, <span class="string">"ccbb"</span>, <span class="string">"dgd"</span>);</span><br><span class="line">        list1.stream().map(String::toUpperCase).forEach(System.out::println);</span><br><span class="line"></span><br><span class="line">        list.stream().map(Employee::getName).filter(name -&gt; name.length() &gt;= <span class="number">3</span>).forEach(System.out::println);</span><br><span class="line">        <span class="comment">//fromStringToStream返回一个流，使得streamStream变成一个由stream组成的stream</span></span><br><span class="line">        Stream&lt;Stream&lt;Character&gt;&gt; streamStream = list1.stream().map(StreamTest::fromStringToStream);</span><br><span class="line">        streamStream.forEach(s -&gt; s.forEach(System.out::println));<span class="comment">//先遍历每个流，再遍历流内的元素</span></span><br><span class="line">        <span class="comment">//flatmap与map的关系：当一个数组集合A（两个元素）用add方法添加另一个数组集合B（B有三个元素），A就有三个元素，第三个是一个集合B；</span></span><br><span class="line">        <span class="comment">// 而用addAll方法则会把B拆开放到A中，使得A有五个元素。flatmap与addAll类似</span></span><br><span class="line">        Stream&lt;Character&gt; characterStream = list1.stream().flatMap(StreamTest::fromStringToStream);<span class="comment">//由Character组成的流</span></span><br><span class="line">        characterStream.forEach(System.out::println);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//排序</span></span><br><span class="line">        List&lt;Integer&gt; list2 = Arrays.asList(<span class="number">12</span>, <span class="number">13</span>, <span class="number">5</span>, <span class="number">54</span>, <span class="number">21</span>, <span class="number">88</span>, <span class="number">56</span>, <span class="number">41</span>, <span class="number">32</span>, <span class="number">55</span>, -<span class="number">4</span>, -<span class="number">9</span>, -<span class="number">52</span>, <span class="number">0</span>, <span class="number">4</span>);</span><br><span class="line">        list2.stream().sorted().forEach(System.out::println);<span class="comment">//自然排序</span></span><br><span class="line">        list.stream().sorted(Comparator.comparingInt(Employee::getAge)).forEach(System.out::println);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Stream&lt;Character&gt; <span class="title">fromStringToStream</span><span class="params">(String string)</span> </span>&#123;</span><br><span class="line">        ArrayList&lt;Character&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span> (Character c : string.toCharArray()) &#123;</span><br><span class="line">            list.add(c);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> list.stream();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test3</span><span class="params">()</span> </span>&#123;<span class="comment">//终止操作</span></span><br><span class="line">        List&lt;Employee&gt; employees = Employee.getEmployees();</span><br><span class="line"></span><br><span class="line">        <span class="comment">//匹配与查找</span></span><br><span class="line">        <span class="comment">//检查是否匹配所有元素</span></span><br><span class="line">        <span class="keyword">boolean</span> b = employees.stream().allMatch(e -&gt; e.getAge() &gt; <span class="number">20</span>);</span><br><span class="line">        <span class="comment">//检查是否至少匹配一个元素</span></span><br><span class="line">        <span class="keyword">boolean</span> b1 = employees.stream().anyMatch(e -&gt; e.getSalary() &lt; <span class="number">5000</span>);</span><br><span class="line">        <span class="comment">//检查是否没有匹配的元素</span></span><br><span class="line">        <span class="keyword">boolean</span> b2 = employees.stream().noneMatch(e -&gt; e.getSalary() &lt; <span class="number">5000</span>);</span><br><span class="line">        <span class="comment">//findFirst,返回第一个元素</span></span><br><span class="line">        Optional&lt;Employee&gt; first = employees.stream().findFirst();</span><br><span class="line">        <span class="comment">//findAny,返回流中的任意一个元素,因为是串行流，总是第一个</span></span><br><span class="line">        Optional&lt;Employee&gt; any = employees.stream().findAny();</span><br><span class="line">        System.out.println(any);</span><br><span class="line">        Optional&lt;Employee&gt; any1 = employees.parallelStream().findAny();</span><br><span class="line">        System.out.println(any1);</span><br><span class="line">        <span class="comment">//统计个数</span></span><br><span class="line">        <span class="keyword">long</span> count = employees.stream().filter(e -&gt; e.getSalary() &gt; <span class="number">5500</span>).count();</span><br><span class="line">        <span class="comment">//返回工资的最大值</span></span><br><span class="line">        Optional&lt;Double&gt; max = employees.stream().map(Employee::getSalary).max(Double::compareTo);</span><br><span class="line">        <span class="comment">//返回有最少工资的员工</span></span><br><span class="line">        Optional&lt;Employee&gt; min1 = employees.stream().min((e1, e2) -&gt; Double.compare(e1.getSalary(), e2.getSalary()));</span><br><span class="line">        Optional&lt;Employee&gt; min2 = employees.stream().min(Comparator.comparingDouble(Employee::getSalary));<span class="comment">//comparingXxx方法返回一个Comparator</span></span><br><span class="line">        <span class="comment">//内部迭代</span></span><br><span class="line">        employees.stream().forEach(System.out::println);</span><br><span class="line">        employees.forEach(System.out::println);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test4</span><span class="params">()</span> </span>&#123;<span class="comment">//终止操作归约、收集</span></span><br><span class="line">        List&lt;Integer&gt; list = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>, <span class="number">9</span>);</span><br><span class="line">        <span class="comment">//第一个参数identity为初始值</span></span><br><span class="line">        Integer sun = list.stream().reduce(<span class="number">0</span>, Integer::sum);</span><br><span class="line">        List&lt;Employee&gt; employees = Employee.getEmployees();</span><br><span class="line">        Optional&lt;Double&gt; reduce = employees.stream().map(Employee::getSalary).reduce(Double::sum);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//收集：collect(Collector c)，Collector接口中方法实现决定流如何收集</span></span><br><span class="line">        <span class="comment">//另有Collectors实用类提供很多静态方法，可以创建常见收集器实例</span></span><br><span class="line">        List&lt;Employee&gt; employeeList = employees.stream().filter(e -&gt; e.getSalary() &gt; <span class="number">6000</span>).collect(Collectors.toList());</span><br><span class="line">        employeeList.forEach(System.out::println);</span><br><span class="line"></span><br><span class="line">        Set&lt;Employee&gt; employeeSet = employees.stream().filter(e -&gt; e.getSalary() &gt; <span class="number">6000</span>).collect(Collectors.toSet());</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Optional类"><a href="#Optional类" class="headerlink" title="Optional类"></a>Optional类</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ylxianzhe.java8;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.ylxianzhe.java8.Stream.Employee;</span><br><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Optional;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OptionalTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Employee tom = <span class="keyword">new</span> Employee(<span class="number">11</span>, <span class="string">"tom"</span>, <span class="number">22</span>, <span class="number">7845.23</span>);</span><br><span class="line">        Optional&lt;Employee&gt; optional = Optional.of(tom);<span class="comment">//tom必须非空</span></span><br><span class="line">        <span class="comment">//orElse方法表示optional里对象如果不空，那就返回optional里的，如果空那就返回参数other里的</span></span><br><span class="line">        Employee employee = optional.orElse(<span class="keyword">new</span> Employee(<span class="number">44</span>,<span class="string">"hh"</span>,<span class="number">45</span>,<span class="number">8899</span>));</span><br><span class="line">        System.out.println(employee);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Employee tom = <span class="keyword">new</span> Employee(<span class="number">11</span>, <span class="string">"tom"</span>, <span class="number">22</span>, <span class="number">7845.23</span>);</span><br><span class="line">        tom=<span class="keyword">null</span>;</span><br><span class="line">        Optional&lt;Employee&gt; optional = Optional.ofNullable(tom);<span class="comment">//现在可以为空</span></span><br><span class="line">        <span class="comment">//orElse方法表示optional里对象如果不空，那就返回optional里的，如果空那就返回参数other里的</span></span><br><span class="line">        Employee employee1 = optional.orElse(<span class="keyword">new</span> Employee(<span class="number">44</span>,<span class="string">"hh"</span>,<span class="number">45</span>,<span class="number">8899</span>));</span><br><span class="line">        System.out.println(employee1);</span><br><span class="line">        <span class="comment">//若other参数也是null的，则返回null</span></span><br><span class="line">        Employee employee2 = optional.orElse(<span class="keyword">null</span>);</span><br><span class="line">        System.out.println(employee2);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>探秘Java中String、StringBuilder以及StringBuffer</title>
    <url>/2018/08/12/%E6%8E%A2%E7%A7%98Java%E4%B8%ADString%E3%80%81StringBuilder%E4%BB%A5%E5%8F%8AStringBuffer/</url>
    <content><![CDATA[<p>本篇转载于<a href="http://www.cnblogs.com/dolphin0520/p/3778589.html" target="_blank" rel="noopener">http://www.cnblogs.com/dolphin0520/p/3778589.html</a> </p>
<a id="more"></a>

<h1 id="一-你了解String类吗？"><a href="#一-你了解String类吗？" class="headerlink" title="一.你了解String类吗？"></a>一.你了解String类吗？</h1><p>　　想要了解一个类，最好的办法就是看这个类的实现源代码，String类的实现在\jdk1.6.0_14\src\java\lang\String.java文件中。</p>
<p>　　打开这个类文件就会发现String类是被final修饰的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">String</span></span></span><br><span class="line"><span class="class">    <span class="keyword">implements</span> <span class="title">java</span>.<span class="title">io</span>.<span class="title">Serializable</span>, <span class="title">Comparable</span>&lt;<span class="title">String</span>&gt;, <span class="title">CharSequence</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="comment">/** The value is used for character storage. */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">char</span> value[];</span><br><span class="line"> </span><br><span class="line">    <span class="comment">/** The offset is the first index of the storage that is used. */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> offset;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">/** The count is the number of characters in the String. */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> count;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">/** Cache the hash code for the string */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> hash; <span class="comment">// Default to 0</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">/** use serialVersionUID from JDK 1.0.2 for interoperability */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = -<span class="number">6849794470754667710L</span>;</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>　　从上面可以看出几点：</p>
<p>1）String类是final类，也即意味着String类不能被继承，并且它的成员方法都默认为final方法。在Java中，被final修饰的类是不允许被继承的，并且该类中的成员方法都默认为final方法。在早期的JVM实现版本中，被final修饰的方法会被转为内嵌调用以提升执行效率。而从Java SE5/6开始，就渐渐摈弃这种方式了。因此在现在的Java SE版本中，不需要考虑用final去提升方法调用效率。只有在确定不想让该方法被覆盖时，才将方法设置为final。</p>
<p>2）上面列举出了String类中所有的成员属性，从上面可以看出String类其实是通过char数组来保存字符串的。</p>
<p>　　下面再继续看String类的一些方法实现：</p>
 <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">substring</span><span class="params">(<span class="keyword">int</span> beginIndex, <span class="keyword">int</span> endIndex)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (beginIndex &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> StringIndexOutOfBoundsException(beginIndex);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (endIndex &gt; count) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> StringIndexOutOfBoundsException(endIndex);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (beginIndex &gt; endIndex) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> StringIndexOutOfBoundsException(endIndex - beginIndex);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ((beginIndex == <span class="number">0</span>) &amp;&amp; (endIndex == count)) ? <span class="keyword">this</span> :</span><br><span class="line">        <span class="keyword">new</span> String(offset + beginIndex, endIndex - beginIndex, value);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line"> <span class="function"><span class="keyword">public</span> String <span class="title">concat</span><span class="params">(String str)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> otherLen = str.length();</span><br><span class="line">    <span class="keyword">if</span> (otherLen == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">char</span> buf[] = <span class="keyword">new</span> <span class="keyword">char</span>[count + otherLen];</span><br><span class="line">    getChars(<span class="number">0</span>, count, buf, <span class="number">0</span>);</span><br><span class="line">    str.getChars(<span class="number">0</span>, otherLen, buf, count);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> String(<span class="number">0</span>, count + otherLen, buf);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line"> <span class="function"><span class="keyword">public</span> String <span class="title">replace</span><span class="params">(<span class="keyword">char</span> oldChar, <span class="keyword">char</span> newChar)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (oldChar != newChar) &#123;</span><br><span class="line">        <span class="keyword">int</span> len = count;</span><br><span class="line">        <span class="keyword">int</span> i = -<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">char</span>[] val = value; <span class="comment">/* avoid getfield opcode */</span></span><br><span class="line">        <span class="keyword">int</span> off = offset;   <span class="comment">/* avoid getfield opcode */</span></span><br><span class="line"> </span><br><span class="line">        <span class="keyword">while</span> (++i &lt; len) &#123;</span><br><span class="line">        <span class="keyword">if</span> (val[off + i] == oldChar) &#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (i &lt; len) &#123;</span><br><span class="line">        <span class="keyword">char</span> buf[] = <span class="keyword">new</span> <span class="keyword">char</span>[len];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span> ; j &lt; i ; j++) &#123;</span><br><span class="line">            buf[j] = val[off+j];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (i &lt; len) &#123;</span><br><span class="line">            <span class="keyword">char</span> c = val[off + i];</span><br><span class="line">            buf[i] = (c == oldChar) ? newChar : c;</span><br><span class="line">            i++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> String(<span class="number">0</span>, len, buf);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br></pre></td></tr></table></figure>

<p>从上面的三个方法可以看出，无论是sub操、concat还是replace操作都不是在原有的字符串上进行的，而是重新生成了一个新的字符串对象。也就是说进行这些操作后，最原始的字符串并没有被改变。</p>
<p>　　在这里要永远记住一点：</p>
<p>　　“对String对象的任何改变都不影响到原对象，相关的任何change操作都会生成新的对象”。</p>
<p>　　在了解了于String类基础的知识后，下面来看一些在平常使用中容易忽略和混淆的地方。</p>
<h1 id="二-深入理解String、StringBuffer、StringBuilder"><a href="#二-深入理解String、StringBuffer、StringBuilder" class="headerlink" title="二.深入理解String、StringBuffer、StringBuilder"></a>二.深入理解String、StringBuffer、StringBuilder</h1><p>1.String str=”hello world”和String str=new String(“hello world”)的区别</p>
<p>　　想必大家对上面2个语句都不陌生，在平时写代码的过程中也经常遇到，那么它们到底有什么区别和联系呢？下面先看几个例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">         </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String str1 = <span class="string">"hello world"</span>;</span><br><span class="line">        String str2 = <span class="keyword">new</span> String(<span class="string">"hello world"</span>);</span><br><span class="line">        String str3 = <span class="string">"hello world"</span>;</span><br><span class="line">        String str4 = <span class="keyword">new</span> String(<span class="string">"hello world"</span>);</span><br><span class="line">         </span><br><span class="line">        System.out.println(str1==str2);</span><br><span class="line">        System.out.println(str1==str3);</span><br><span class="line">        System.out.println(str2==str4);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果是:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">false</span><br><span class="line">true</span><br><span class="line">false</span><br></pre></td></tr></table></figure>

<p>为什么会出现这样的结果？下面解释一下原因：</p>
<p>　　在前面一篇讲解关于JVM内存机制的一篇博文中提到 ，在class文件中有一部分 来存储编译期间生成的 字面常量以及符号引用，这部分叫做class文件常量池，在运行期间对应着方法区的运行时常量池。</p>
<p>　　因此在上述代码中，String str1 = “hello world”;和String str3 = “hello world”; 都在编译期间生成了 字面常量和符号引用，运行期间字面常量”hello world”被存储在运行时常量池（当然只保存了一份）。通过这种方式来将String对象跟引用绑定的话，JVM执行引擎会先在运行时常量池查找是否存在相同的字面常量，如果存在，则直接将引用指向已经存在的字面常量；否则在运行时常量池开辟一个空间来存储该字面常量，并将引用指向该字面常量。</p>
<p>　　总所周知，通过new关键字来生成对象是在堆区进行的，而在堆区进行对象生成的过程是不会去检测该对象是否已经存在的。因此通过new来创建对象，创建出的一定是不同的对象，即使字符串的内容是相同的。</p>
<p>2.String、StringBuffer以及StringBuilder的区别</p>
<p>　　既然在Java中已经存在了String类，那为什么还需要StringBuilder和StringBuffer类呢？</p>
<p>　　那么看下面这段代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">         </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String string = <span class="string">""</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">10000</span>;i++)&#123;</span><br><span class="line">            string += <span class="string">"hello"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这句 string += “hello”;的过程相当于将原有的string变量指向的对象内容取出与”hello”作字符串相加操作再存进另一个新的String对象当中，再让string变量指向新生成的对象。如果大家还有疑问可以反编译其字节码文件便清楚了： </p>
<p><img src="/2018/08/12/%E6%8E%A2%E7%A7%98Java%E4%B8%ADString%E3%80%81StringBuilder%E4%BB%A5%E5%8F%8AStringBuffer/pic01.jpg" alt></p>
<p>从这段反编译出的字节码文件可以很清楚地看出：从第8行开始到第35行是整个循环的执行过程，并且每次循环会new出一个StringBuilder对象，然后进行append操作，最后通过toString方法返回String对象。也就是说这个循环执行完毕new出了10000个对象，试想一下，如果这些对象没有被回收，会造成多大的内存资源浪费。从上面还可以看出：string+=”hello”的操作事实上会自动被JVM优化成：</p>
<p>　　StringBuilder str = new StringBuilder(string);</p>
<p>　　str.append(“hello”);</p>
<p>　　str.toString();</p>
<p>　　再看下面这段代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">         </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        StringBuilder stringBuilder = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">10000</span>;i++)&#123;</span><br><span class="line">            stringBuilder.append(<span class="string">"hello"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>反编译字节码文件得到：</p>
<p><img src="/2018/08/12/%E6%8E%A2%E7%A7%98Java%E4%B8%ADString%E3%80%81StringBuilder%E4%BB%A5%E5%8F%8AStringBuffer/pic02.jpg" alt></p>
<p>从这里可以明显看出，这段代码的for循环式从13行开始到27行结束，并且new操作只进行了一次，也就是说只生成了一个对象，append操作是在原有对象的基础上进行的。因此在循环了10000次之后，这段代码所占的资源要比上面小得多。</p>
<p>　　那么有人会问既然有了StringBuilder类，为什么还需要StringBuffer类？查看源代码便一目了然，事实上，StringBuilder和StringBuffer类拥有的成员属性以及成员方法基本相同，区别是StringBuffer类的成员方法前面多了一个关键字：synchronized，不用多说，这个关键字是在多线程访问时起到安全保护作用的,也就是说StringBuffer是线程安全的。</p>
<p>　　下面摘了2段代码分别来自StringBuffer和StringBuilder，insert方法的具体实现：</p>
<p>StringBuilder的insert方法 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> StringBuilder <span class="title">insert</span><span class="params">(<span class="keyword">int</span> index, <span class="keyword">char</span> str[], <span class="keyword">int</span> offset,</span></span></span><br><span class="line"><span class="function"><span class="params">                              <span class="keyword">int</span> len)</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">      <span class="keyword">super</span>.insert(index, str, offset, len);</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>StringBuffer的insert方法： </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> StringBuffer <span class="title">insert</span><span class="params">(<span class="keyword">int</span> index, <span class="keyword">char</span> str[], <span class="keyword">int</span> offset,</span></span></span><br><span class="line"><span class="function"><span class="params">                                            <span class="keyword">int</span> len)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>.insert(index, str, offset, len);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h1 id="三-不同场景下三个类的性能测试"><a href="#三-不同场景下三个类的性能测试" class="headerlink" title="三.不同场景下三个类的性能测试"></a>三.不同场景下三个类的性能测试</h1><p>从第二节我们已经看出了三个类的区别，这一小节我们来做个小测试，来测试一下三个类的性能区别：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> time = <span class="number">50000</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        testString();</span><br><span class="line">        testStringBuffer();</span><br><span class="line">        testStringBuilder();</span><br><span class="line">        test1String();</span><br><span class="line">        test2String();</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testString</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        String s=<span class="string">""</span>;</span><br><span class="line">        <span class="keyword">long</span> begin = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;time; i++)&#123;</span><br><span class="line">            s += <span class="string">"java"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">long</span> over = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">"操作"</span>+s.getClass().getName()+<span class="string">"类型使用的时间为："</span>+(over-begin)+<span class="string">"毫秒"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testStringBuffer</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        StringBuffer sb = <span class="keyword">new</span> StringBuffer();</span><br><span class="line">        <span class="keyword">long</span> begin = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;time; i++)&#123;</span><br><span class="line">            sb.append(<span class="string">"java"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">long</span> over = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">"操作"</span>+sb.getClass().getName()+<span class="string">"类型使用的时间为："</span>+(over-begin)+<span class="string">"毫秒"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testStringBuilder</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        StringBuilder sb = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">        <span class="keyword">long</span> begin = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;time; i++)&#123;</span><br><span class="line">            sb.append(<span class="string">"java"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">long</span> over = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">"操作"</span>+sb.getClass().getName()+<span class="string">"类型使用的时间为："</span>+(over-begin)+<span class="string">"毫秒"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">test1String</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">long</span> begin = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;time; i++)&#123;</span><br><span class="line">            String s = <span class="string">"I"</span>+<span class="string">"love"</span>+<span class="string">"java"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">long</span> over = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">"字符串直接相加操作："</span>+(over-begin)+<span class="string">"毫秒"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">test2String</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        String s1 =<span class="string">"I"</span>;</span><br><span class="line">        String s2 = <span class="string">"love"</span>;</span><br><span class="line">        String s3 = <span class="string">"java"</span>;</span><br><span class="line">        <span class="keyword">long</span> begin = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;time; i++)&#123;</span><br><span class="line">            String s = s1+s2+s3;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">long</span> over = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">"字符串间接相加操作："</span>+(over-begin)+<span class="string">"毫秒"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>测试结果（win7，Eclipse，JDK6)： </p>
<p><img src="/2018/08/12/%E6%8E%A2%E7%A7%98Java%E4%B8%ADString%E3%80%81StringBuilder%E4%BB%A5%E5%8F%8AStringBuffer/pic03.jpg" alt></p>
<p>上面提到string+=”hello”的操作事实上会自动被JVM优化，看下面这段代码： </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> time = <span class="number">50000</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        testString();</span><br><span class="line">        testOptimalString();</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testString</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        String s=<span class="string">""</span>;</span><br><span class="line">        <span class="keyword">long</span> begin = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;time; i++)&#123;</span><br><span class="line">            s += <span class="string">"java"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">long</span> over = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">"操作"</span>+s.getClass().getName()+<span class="string">"类型使用的时间为："</span>+(over-begin)+<span class="string">"毫秒"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testOptimalString</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        String s=<span class="string">""</span>;</span><br><span class="line">        <span class="keyword">long</span> begin = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;time; i++)&#123;</span><br><span class="line">            StringBuilder sb = <span class="keyword">new</span> StringBuilder(s);</span><br><span class="line">            sb.append(<span class="string">"java"</span>);</span><br><span class="line">            s=sb.toString();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">long</span> over = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">"模拟JVM优化操作的时间为："</span>+(over-begin)+<span class="string">"毫秒"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>执行结果： </p>
<p><img src="/2018/08/12/%E6%8E%A2%E7%A7%98Java%E4%B8%ADString%E3%80%81StringBuilder%E4%BB%A5%E5%8F%8AStringBuffer/pic04.jpg" alt></p>
<p>得到验证。</p>
<p>　　下面对上面的执行结果进行一般性的解释：</p>
<p>　　1）对于直接相加字符串，效率很高，因为在编译器便确定了它的值，也就是说形如”I”+”love”+”java”; 的字符串相加，在编译期间便被优化成了”Ilovejava”。这个可以用javap -c命令反编译生成的class文件进行验证。</p>
<p>　　对于间接相加（即包含字符串引用），形如s1+s2+s3; 效率要比直接相加低，因为在编译器不会对引用变量进行优化。</p>
<p>　　2）String、StringBuilder、StringBuffer三者的执行效率：</p>
<p>　　StringBuilder &gt; StringBuffer &gt; String</p>
<p>　　当然这个是相对的，不一定在所有情况下都是这样。</p>
<p>　　比如String str = “hello”+ “world”的效率就比 StringBuilder st  = new StringBuilder().append(“hello”).append(“world”)要高。</p>
<p>　　因此，这三个类是各有利弊，应当根据不同的情况来进行选择使用：</p>
<p>　　当字符串相加操作或者改动较少的情况下，建议使用 String str=”hello”这种形式；</p>
<p>　　当字符串相加操作较多的情况下，建议使用StringBuilder，如果采用了多线程，则使用StringBuffer。</p>
<h1 id="四-常见的关于String、StringBuffer的面试题"><a href="#四-常见的关于String、StringBuffer的面试题" class="headerlink" title="四.常见的关于String、StringBuffer的面试题"></a>四.常见的关于String、StringBuffer的面试题</h1><p>下面是一些常见的关于String、StringBuffer的一些面试笔试题，若有不正之处，请谅解和批评指正。</p>
<ol>
<li>下面这段代码的输出结果是什么？</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String a = <span class="string">"hello2"</span>; </span><br><span class="line">String b = <span class="string">"hello"</span> + <span class="number">2</span>;</span><br><span class="line">System.out.println((a == b));</span><br></pre></td></tr></table></figure>

<p>　　输出结果为：true。原因很简单，”hello”+2在编译期间就已经被优化成”hello2”，因此在运行期间，变量a和变量b指向的是同一个对象。</p>
<ol start="2">
<li>下面这段代码的输出结果是什么？</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String a = <span class="string">"hello2"</span>; </span><br><span class="line">String b = <span class="string">"hello"</span>; </span><br><span class="line">String c = b + <span class="number">2</span>; </span><br><span class="line">System.out.println((a == c));</span><br></pre></td></tr></table></figure>

<p>　　输出结果为:false。由于有符号引用的存在，所以  String c = b + 2;不会在编译期间被优化，不会把b+2当做字面常量来处理的，因此这种方式生成的对象事实上是保存在堆上的。因此a和c指向的并不是同一个对象。javap -c得到的内容：</p>
<p><img src="/2018/08/12/%E6%8E%A2%E7%A7%98Java%E4%B8%ADString%E3%80%81StringBuilder%E4%BB%A5%E5%8F%8AStringBuffer/pic05.jpg" alt></p>
<p>3.下面这段代码的输出结果是什么？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String a = <span class="string">"hello2"</span>; </span><br><span class="line"><span class="keyword">final</span> String b = <span class="string">"hello"</span>; </span><br><span class="line">String c = b + <span class="number">2</span>;</span><br><span class="line">System.out.println((a == c));</span><br></pre></td></tr></table></figure>

<p>　　输出结果为：true。对于被final修饰的变量，会在class文件常量池中保存一个副本，也就是说不会通过连接而进行访问，对final变量的访问在编译期间都会直接被替代为真实的值。那么String c = b + 2;在编译期间就会被优化成：String c = “hello” + 2; 下图是javap -c的内容：</p>
<p><img src="/2018/08/12/%E6%8E%A2%E7%A7%98Java%E4%B8%ADString%E3%80%81StringBuilder%E4%BB%A5%E5%8F%8AStringBuffer/pic06.jpg" alt></p>
<p>4.下面这段代码输出结果为?</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String a = <span class="string">"hello2"</span>;</span><br><span class="line">        <span class="keyword">final</span> String b = getHello();</span><br><span class="line">        String c = b + <span class="number">2</span>;</span><br><span class="line">        System.out.println((a == c));</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">getHello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"hello"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出结果为false。这里面虽然将b用final修饰了，但是由于其赋值是通过方法调用返回的，那么它的值只能在运行期间确定，因此a和c指向的不是同一个对象。</p>
<p>5.下面这段代码的输出结果是什么？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String a = <span class="string">"hello"</span>;</span><br><span class="line">        String b =  <span class="keyword">new</span> String(<span class="string">"hello"</span>);</span><br><span class="line">        String c =  <span class="keyword">new</span> String(<span class="string">"hello"</span>);</span><br><span class="line">        String d = b.intern();</span><br><span class="line">         </span><br><span class="line">        System.out.println(a==b);</span><br><span class="line">        System.out.println(b==c);</span><br><span class="line">        System.out.println(b==d);</span><br><span class="line">        System.out.println(a==d);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">false</span><br><span class="line">false</span><br><span class="line">false</span><br><span class="line">true</span><br></pre></td></tr></table></figure>

<p>这里面涉及到的是String.intern方法的使用。在String类中，intern方法是一个本地方法，在JAVA SE6之前，intern方法会在运行时常量池中查找是否存在内容相同的字符串，如果存在则返回指向该字符串的引用，如果不存在，则会将该字符串入池，并返回一个指向该字符串的引用。因此，a和d指向的是同一个对象。</p>
<p>6.String str = new String(“abc”)创建了多少个对象？</p>
<p>　　这个问题在很多书籍上都有说到比如《Java程序员面试宝典》，包括很多国内大公司笔试面试题都会遇到，大部分网上流传的以及一些面试书籍上都说是2个对象，这种说法是片面的。</p>
<p>如果有不懂得地方可以参考这篇帖子：</p>
<p>　　<a href="http://rednaxelafx.iteye.com/blog/774673/" target="_blank" rel="noopener">http://rednaxelafx.iteye.com/blog/774673/</a></p>
<p>　　首先必须弄清楚创建对象的含义，创建是什么时候创建的？这段代码在运行期间会创建2个对象么？毫无疑问不可能，用javap -c反编译即可得到JVM执行的字节码内容：</p>
<p><img src="/2018/08/12/%E6%8E%A2%E7%A7%98Java%E4%B8%ADString%E3%80%81StringBuilder%E4%BB%A5%E5%8F%8AStringBuffer/pic07.jpg" alt></p>
<p>7.下面这段代码1）和2）的区别是什么？ </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String str1 = <span class="string">"I"</span>;</span><br><span class="line">        <span class="comment">//str1 += "love"+"java";        1)</span></span><br><span class="line">        str1 = str1+<span class="string">"love"</span>+<span class="string">"java"</span>;      <span class="comment">//2)</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>1）的效率比2）的效率要高，1）中的”love”+”java”在编译期间会被优化成”lovejava”，而2）中的不会被优化。下面是两种方式的字节码：</p>
<p>　　1）的字节码：</p>
<p><img src="/2018/08/12/%E6%8E%A2%E7%A7%98Java%E4%B8%ADString%E3%80%81StringBuilder%E4%BB%A5%E5%8F%8AStringBuffer/pic08.jpg" alt>2）的字节码： </p>
<p><img src="/2018/08/12/%E6%8E%A2%E7%A7%98Java%E4%B8%ADString%E3%80%81StringBuilder%E4%BB%A5%E5%8F%8AStringBuffer/pic09.jpg" alt></p>
<p>可以看出，在1）中只进行了一次append操作，而在2）中进行了两次append操作。 </p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>html总结</title>
    <url>/2018/10/26/html%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>总结一些HTML的知识.</p>
<a id="more"></a>

<h1 id="常用标签"><a href="#常用标签" class="headerlink" title="常用标签"></a>常用标签</h1><h2 id="文字标签和注释标签"><a href="#文字标签和注释标签" class="headerlink" title="文字标签和注释标签"></a>文字标签和注释标签</h2><h3 id="文字标签"><a href="#文字标签" class="headerlink" title="文字标签"></a>文字标签</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">font</span> <span class="attr">size</span>=<span class="string">"5"</span> <span class="attr">color</span>=<span class="string">"red"</span>&gt;</span> <span class="tag">&lt;/<span class="name">font</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>属性:</p>
<ul>
<li>size:文字大小,范围1-7</li>
<li>color:文字颜色<ul>
<li>两种表示方式<ul>
<li>英文单词</li>
<li>使用十六进制数表示</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="注释标签"><a href="#注释标签" class="headerlink" title="注释标签"></a>注释标签</h3><p>HTML的注释:’<!--HTML的注释-->‘</p>
<h2 id="标题标签-水平线和特殊字符"><a href="#标题标签-水平线和特殊字符" class="headerlink" title="标题标签 水平线和特殊字符"></a>标题标签 水平线和特殊字符</h2><h3 id="标题标签"><a href="#标题标签" class="headerlink" title="标题标签"></a>标题标签</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">h1</span>&gt;</span> <span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h2</span>&gt;</span> <span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br><span class="line">...</span><br><span class="line"><span class="tag">&lt;<span class="name">h6</span>&gt;</span> <span class="tag">&lt;/<span class="name">h6</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>从1-6字体大小依次缩小,同时<strong>自动换行</strong>.</p>
<h3 id="水平线"><a href="#水平线" class="headerlink" title="水平线"></a>水平线</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>标签内结束,不成对.</p>
<p>属性:</p>
<ul>
<li>size</li>
<li>color</li>
</ul>
<h3 id="特殊字符"><a href="#特殊字符" class="headerlink" title="特殊字符"></a>特殊字符</h3><p>对特殊字符转义:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="symbol">&amp;lt;</span>	<span class="tag">&lt;</span></span><br><span class="line"><span class="tag">&amp;<span class="attr">rt</span>;	&gt;</span></span><br><span class="line">&amp;nbsp   空格</span><br></pre></td></tr></table></figure>

<h2 id="列表标签"><a href="#列表标签" class="headerlink" title="列表标签"></a>列表标签</h2><p><code>&lt;dl&gt;&lt;/dl&gt;</code>:表示列表的范围</p>
<ul>
<li>在dl里面<code>&lt;dt&gt;&lt;/dt&gt;</code>:表示上层内容</li>
<li>在dl里面<code>&lt;dd&gt;&lt;/dd&gt;</code>:表示上层内容</li>
</ul>
<p><code>&lt;ol&gt;&lt;/ol&gt;</code>:有序标签的范围</p>
<ul>
<li>用<code>&lt;li&gt;&lt;/li&gt;</code>表示内容</li>
<li>属性 type,设置排序符号:1(默认),a,i</li>
</ul>
<p><code>&lt;ul&gt;&lt;/ul&gt;</code>:无序列表标签</p>
<ul>
<li>用<code>&lt;li&gt;&lt;/li&gt;</code>表示内容</li>
<li>属性 type,设置排序符号:空心圆circle,实心圆disc(默认),实心方块square</li>
</ul>
<h2 id="图片标签"><a href="#图片标签" class="headerlink" title="图片标签"></a>图片标签</h2><p><code>&lt;img src=&quot;&quot;/&gt;</code></p>
<ul>
<li>src:路径</li>
<li>width:宽度</li>
<li>height:高度</li>
<li>alt:图片上的文字</li>
</ul>
<h2 id="路径"><a href="#路径" class="headerlink" title="路径"></a>路径</h2><ul>
<li>相对路径</li>
<li>绝对路径</li>
</ul>
]]></content>
      <tags>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>线性表</title>
    <url>/2019/03/08/%E7%BA%BF%E6%80%A7%E8%A1%A8/</url>
    <content><![CDATA[<p>&emsp;什么是线性表?</p>
<a id="more"></a>

<h1 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h1><p>&emsp;由同类型数据元素构成有序序列的线性结构.</p>
<ul>
<li><p>表中的元素个数为表的长度</p>
</li>
<li><p>没有元素叫空表</p>
</li>
<li><p>起始位置叫表头,结束位置叫表尾</p>
</li>
</ul>
<p>&emsp;抽象数据类型描述:</p>
<ul>
<li><p>数据对象集:线性表是n(&gt;=0)个元素构成的有序序列</p>
</li>
<li><p>操作集：</p>
<ul>
<li><code>List MakeEmpty()</code>:初始化一个空线性表</li>
<li><code>ElementType FindKth(int K,List L)</code>:根据位序K返回相应的元素</li>
<li><code>int Find(ElementType X,List L)</code>:在线性表L中查找X第一次出现的位置</li>
<li><code>void Insert(ElementType X,int i,List L)</code>:在位序i前插入一个新元素X</li>
<li><code>void Delete(int i,List L)</code>:删除指定位序的元素</li>
<li><code>int Length(List L)</code>:返回线性表L的长度n </li>
</ul>
</li>
</ul>
<h1 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h1><h2 id="利用数组的连续存储空间实现"><a href="#利用数组的连续存储空间实现" class="headerlink" title="利用数组的连续存储空间实现"></a>利用数组的连续存储空间实现</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span> *<span class="title">List</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">LNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	ElementType Data[MAXSIZE];</span><br><span class="line">	<span class="keyword">int</span> Last;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">LNode</span> <span class="title">L</span>;</span></span><br><span class="line">List PtrL;</span><br></pre></td></tr></table></figure>

<h2 id="主要操作的实现"><a href="#主要操作的实现" class="headerlink" title="主要操作的实现"></a>主要操作的实现</h2><p>1.初始化</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">List <span class="title">MakeEmpty</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	List PtrL;</span><br><span class="line">	PtrL = (List)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(struct LNode));</span><br><span class="line">	PtrL-&gt;Last = <span class="number">-1</span>;</span><br><span class="line">	<span class="keyword">return</span> PtrL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>2.查找</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Find</span><span class="params">(ElementType X, List PtrL)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">while</span> (i &lt;= PtrL-&gt;Last &amp;&amp; PtrL-&gt;Data[i] != X)</span><br><span class="line">	&#123;</span><br><span class="line">		i++;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (i &gt; PtrL-&gt;Last)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">return</span> i;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>3.插入,注意移动时要从后面开始</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Insert</span><span class="params">(ElementType X, <span class="keyword">int</span> i, List PtrL)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> j;</span><br><span class="line">	<span class="keyword">if</span> (Ptrl-&gt;Last == MAXSIZE - <span class="number">1</span>)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"表满"</span>);</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (i &lt; <span class="number">1</span> || i &gt; PtrL-&gt;Last + <span class="number">2</span>)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"位置不合法"</span>);</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> (j = PtrL-&gt;Last; j &lt; i - <span class="number">1</span>; j--)</span><br><span class="line">	&#123;</span><br><span class="line">        PtrL-&gt;Data[j + <span class="number">1</span>] = PtrL-&gt;Data[j];</span><br><span class="line">    &#125;</span><br><span class="line">	PtrL-&gt;Data[i - <span class="number">1</span>] = X;</span><br><span class="line">	PtrL-&gt;Last++;</span><br><span class="line">	<span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>4.删除</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Delete</span><span class="params">(<span class="keyword">int</span> i, List PtrL)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> j;</span><br><span class="line">	<span class="keyword">if</span> (i &lt; <span class="number">1</span> || i &gt; PtrL-&gt;Last + <span class="number">1</span>)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"不存在"</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> (j = i; j &lt;= PtrL-&gt;Last; j++)</span><br><span class="line">	&#123;</span><br><span class="line">		PtrL-&gt;Data[j - <span class="number">1</span>] = PtrL-&gt;Data[j];</span><br><span class="line">	&#125;</span><br><span class="line">	PtrL-&gt;Last--;</span><br><span class="line">	<span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>深入理解计算机系统读书笔记2</title>
    <url>/2018/01/27/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B02/</url>
    <content><![CDATA[<p>最近读《深入》一书遇到了非常多的问题，内容太杂较深，外国人的语言习惯也不一样，翻译过来的书翻译的怪怪的。所以读书进展很慢，我打算进度放缓，主攻《汇编语言》一书。</p>
<a id="more"></a>

<p>1.字</p>
<p>每台计算机都有一个<strong>字长</strong>，指明整数和指针的标称大小，同时字长也部分决定了虚拟地址空间的大小，如32位的80386CPU，其虚拟地址空间最大为4GB（2^32=4,294,967,296。4,294,967,296/1024/1024/1024=4GB，共有4,294,967,296个存储器单元地址，共4,294,967,296byte）。但是字长与地址总线没有对应关系。</p>
<p>2.数据大小</p>
<p>丢的文章中有篇写到了。。。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;climits&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> n_int = INT_MAX;</span><br><span class="line">	short n_short = SHRT_MAX;</span><br><span class="line">	<span class="keyword">long</span> n_long = LONG_MAX;</span><br><span class="line">	<span class="keyword">long</span> <span class="keyword">long</span> n_llong = LLONG_MAX;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"int is "</span> &lt;&lt; <span class="keyword">sizeof</span>(<span class="keyword">int</span>) &lt;&lt; <span class="string">" bytes."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"short is "</span> &lt;&lt; <span class="keyword">sizeof</span>(n_short) &lt;&lt; <span class="string">" bytes."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"long is "</span> &lt;&lt; <span class="keyword">sizeof</span>(<span class="keyword">long</span>) &lt;&lt; <span class="string">" bytes."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"long long is "</span> &lt;&lt; <span class="keyword">sizeof</span>(<span class="keyword">long</span> <span class="keyword">long</span>) &lt;&lt; <span class="string">" bytes."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"char* "</span> &lt;&lt; <span class="keyword">sizeof</span>(<span class="keyword">char</span>*) &lt;&lt; <span class="string">" bytes."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"Maximum values:"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"int:"</span> &lt;&lt; n_int &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"short:"</span> &lt;&lt; n_short &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"long:"</span> &lt;&lt; n_long &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"long long:"</span> &lt;&lt; n_llong &lt;&lt; <span class="built_in">endl</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"Minimum int value ="</span> &lt;&lt; INT_MIN &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"Bits per byte ="</span> &lt;&lt; CHAR_BIT &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	</span><br><span class="line">	system(<span class="string">"pause"</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/27/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B02/%E6%95%B0%E6%8D%AE%E5%A4%A7%E5%B0%8F.png" alt="数据大小"></p>
<p>注意char<em>!也可以试试<code>void *</code>，也是*</em>8字节**。</p>
<p>3.寻址和字节顺序</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">char</span> *byte_pointer; <span class="comment">//定义一个声明char型指针的方式,用于强制类型转换，不论原来是什么，指针指向的被看成是字符</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">show_bytes</span><span class="params">(byte_pointer start, <span class="keyword">int</span> len)</span><span class="comment">//参数：char型指针start和长度</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> i;</span><br><span class="line">	<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; len; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"%.2x"</span>, start[i]);<span class="comment">//</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">"\n"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">show_int</span><span class="params">(<span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	show_bytes((byte_pointer)&amp;x, <span class="keyword">sizeof</span>(<span class="keyword">int</span>));<span class="comment">//对x取地址，int的存储长度</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">show_float</span><span class="params">(<span class="keyword">float</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	show_bytes((byte_pointer)&amp;x, <span class="keyword">sizeof</span>(<span class="keyword">float</span>));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">show_pointer</span><span class="params">(<span class="keyword">void</span> *x)</span><span class="comment">//void表示没有相关联的数据类型</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	show_bytes((byte_pointer)&amp;x, <span class="keyword">sizeof</span>(<span class="keyword">void</span> *));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test_show_bytes</span><span class="params">(<span class="keyword">int</span> val)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> ival = val;</span><br><span class="line">	<span class="keyword">float</span> fval = <span class="keyword">float</span>(ival);</span><br><span class="line">	<span class="keyword">int</span> *pval = &amp;ival;</span><br><span class="line">	<span class="keyword">int</span> *ppval = &amp;val;</span><br><span class="line">	show_int(ival);</span><br><span class="line">	show_float(fval);</span><br><span class="line">	show_pointer(pval);</span><br><span class="line">	show_pointer(ppval);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	test_show_bytes(<span class="number">12345</span>);</span><br><span class="line">	system(<span class="string">"pause"</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/01/27/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B02/%E7%BB%93%E6%9E%9C.png" alt="结果"></p>
<p>结果的第一和第二行是<code>12345</code>的整型和浮点型(浮点型为什么是这样以后在写)的十六进制表示，第三行是<code>pval</code>内的值，即ival的地址，第四行是<code>ppval</code>内的值，即val的地址。</p>
<p><code>34  fa  6f  f1  fc</code>和下面第一行的<code>fc  f1  6f  fa  34</code>有这显而易见的对应关系，这就牵扯到字节顺序。</p>
<p>pval        0x000000fcf16ffa34     int *</p>
<p>ppval    0x000000fcf16ffb90     int * </p>
<p><code>fc  f1  6f  fa  34</code>是本应是真正的值，只是在存储空间中并不是按照这种对应关系存储的。细心的话你会发现<code>12345</code>的十六进制是<code>3039</code>而不是<code>3930</code>,显然，它是倒过来放的。低位有效数字在前，高位数字在后。这就是所谓的“小端法”。</p>
<p>假定十六进制数字0x1234567,其小端法表示和大端法表示分别为：</p>
<p>大端法：</p>
<table>
<thead>
<tr>
<th>0x100</th>
<th>0x101</th>
<th>0x102</th>
<th>0x103</th>
</tr>
</thead>
<tbody><tr>
<td>01</td>
<td>23</td>
<td>45</td>
<td>67</td>
</tr>
</tbody></table>
<p>小端法：</p>
<table>
<thead>
<tr>
<th>0x100</th>
<th>0x101</th>
<th>0x102</th>
<th>0x103</th>
</tr>
</thead>
<tbody><tr>
<td>67</td>
<td>45</td>
<td>23</td>
<td>01</td>
</tr>
</tbody></table>
<p>tips：十六进制与二进制转化。一个内存单元一字节，8bit，一个单元分为两组转化为十六进制。</p>
<p><img src="/2018/01/27/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B02/%E8%BD%AC%E6%8D%A2.jpg" alt="转换"></p>
<p>《深入》一书中写到“大多数Intel兼容机都采用这种规则（小端法）”，“许多比较新的微处理器使用双端法”。</p>
<p>一些系统和机器的情况如下表：</p>
<p><img src="/2018/01/27/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B02/%E8%A1%A8%E6%A0%BC.jpg" alt="表格"></p>
<p>（Windows为32位）</p>
<p>在64位的情况下，指针用的是<strong>八字节</strong>!!!而32位的只有四字节。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a=<span class="number">3</span>;</span><br><span class="line">    <span class="keyword">int</span> b=a;</span><br><span class="line">    <span class="keyword">int</span> *p1=&amp;a;</span><br><span class="line">    <span class="keyword">int</span> *p2=&amp;b;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;p1&lt;&lt;<span class="string">' '</span>&lt;&lt;p2&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">   system(<span class="string">"pause"</span>);</span><br><span class="line">   <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码在我的电脑上用不同编译器运行有不同的表现形式。（Windows10 64位 i7-3740MQ）</p>
<p>GCC 6.3.0（MinGW）</p>
<p><code>0x61ff14 0x61ff10</code></p>
<p>VS2017</p>
<p><code>0000007B8F58FD04 0000007B8F58FD24</code></p>
<p>注意到GCC的就是内存单元的编号，差了4字节，说明<code>int a</code>和<code>int b</code>是相邻存储的，但是编号形式是0x61ff14，是十六进制的。而VS中也是十六进制，相差20，十进制是32，也是4字节。GCC是按字节编号，而VS是按位编号。</p>
<p>回到<code>39300000</code>和<code>00e44046</code>这两个数字，其真实数字是<code>3039 0000</code>和<code>4640 e400</code>,其二进制分别是：</p>
<p><code>0011    0000    0011    1001    0000    0000    0000    0000</code>    </p>
<p><code>0100    0110    0100    0000    1110    0100    0000    0000</code></p>
<p>在补0对齐后</p>
<p><code>0000    0000    1100    0000    1110    0100    0000    0000    0000    00</code></p>
<p><code>0100    0110    0100    0000    1110    0100    0000    0000</code>    </p>
<p>注意中间有一部分是相同的，这就涉及到浮点数如何表示的问题了。</p>
]]></content>
      <tags>
        <tag>读书笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>结构体</title>
    <url>/2018/02/10/%E7%BB%93%E6%9E%84%E4%BD%93/</url>
    <content><![CDATA[<p>本篇内容包括结构体，共用体和枚举。</p>
<a id="more"></a>

<h1 id="一-结构体"><a href="#一-结构体" class="headerlink" title="一.结构体"></a>一.结构体</h1><h2 id="1-声明"><a href="#1-声明" class="headerlink" title="1.声明"></a>1.声明</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">inflatable</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">  <span class="keyword">char</span> name[<span class="number">20</span>];	<span class="comment">//结构的成员</span></span><br><span class="line">  <span class="keyword">float</span> volume;</span><br><span class="line">  <span class="keyword">double</span> price;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如上所示，声明了一个结构体。结构定义的是新类型（inflatable）的特征。定义了结构后就可以声明这种类型的变量。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">inflatable</span> <span class="title">hat</span>;</span></span><br><span class="line">inflatable mainframe;</span><br></pre></td></tr></table></figure>

<p>可以省略<code>struct</code>，这样就强调<code>inflatable</code>是一种新的类型。</p>
<p>对于结构的声明，应注意位置，关键是在使用的地方的前面，但是，最好在<code>main</code>函数的前面。C++不提倡使用外部变量，但是提倡外部结构声明。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">inflatable guest =</span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">"yl"</span>,</span><br><span class="line">  <span class="number">1.88</span>,</span><br><span class="line">  <span class="number">29.99</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>关键在于用逗号分隔。</p>
<p>在C++11中是与此类似的，等号是可选的，如果大括号内是空的，那么值将是0，字符串每字节也是0。同时，不予许窄缩。</p>
<p>这种声明方式也是可以的，但是显然不好。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">perks</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">int</span> key_number; <span class="keyword">char</span> car [<span class="number">12</span>];</span><br><span class="line">&#125; mr_glitz =</span><br><span class="line">&#123;</span><br><span class="line">	<span class="number">7</span>,<span class="comment">// value for mr_glitz.key_number member </span></span><br><span class="line">    <span class="string">"Packard"</span> <span class="comment">// value for mr_glitz.car member</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>还可以声明没有名称的结构类型，方法是省略名称，同时定义一种结构类型和一个这种类型的变量：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> 	// <span class="title">no</span> <span class="title">tag</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">int</span> x; <span class="comment">// 2 members inty; </span></span><br><span class="line">&#125; position; <span class="comment">// a structure variable</span></span><br></pre></td></tr></table></figure>

<p>照常可以访问成员，但是在后面就不可以在创建该类型的结构。</p>
<h2 id="2-结构数组"><a href="#2-结构数组" class="headerlink" title="2.结构数组"></a>2.结构数组</h2><p>结构中可以包含的东西有很多，包括数组。也可以创建一个由结构组成的数组。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">inflatable gifts[<span class="number">100</span>]; <span class="comment">// array of 100 inflatable structures</span></span><br></pre></td></tr></table></figure>

<h1 id="二-共用体"><a href="#二-共用体" class="headerlink" title="二.共用体"></a>二.共用体</h1><p>共用体（union）是一种数据格式，它能够存储不同的数据类型，但只能同时存储其中的一种类型。也就是说，结构可以同时存储int、long和 double,共用体只能存储int、long或double。共用体的句法与结构相似，但含义不同。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">union</span> one4all</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">int</span> int_val; </span><br><span class="line">    <span class="keyword">long</span> long_val;</span><br><span class="line">    <span class="keyword">double</span> double_val;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>可以使用one4all变量来存储int、long和 double,条件是在不同的时间进行：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">one4all pail;</span><br><span class="line">pail.int_val = <span class="number">15</span>; <span class="comment">// store an int </span></span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; pail.int_val;</span><br><span class="line">pail.double_val = <span class="number">1.38</span>; <span class="comment">// store a double, int value is lost </span></span><br><span class="line"><span class="built_in">cout</span> « pail.double_val;</span><br></pre></td></tr></table></figure>

<p>由于共 用体每次只能存储一个值，因此它必须有足够的空间来存储最大的成员，所以，共用体的长度为其最大成员的长度。</p>
<p>共用体也是可以匿名。</p>
<h1 id="三-枚举"><a href="#三-枚举" class="headerlink" title="三.枚举"></a>三.枚举</h1><p>C++的enum工具提供了另一种创建符号常量的方式，这种方式可以代替const。它还允许定义新类型,但必须按严格的限制进行。使用enum的句法与使用结构相似。例如，请看下面的语句：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> spectrum &#123;red, orange, yellow, green, blue, violet, indigo, ultraviolet&#125;；</span><br></pre></td></tr></table></figure>

<ul>
<li>让 spectrum成为新类型的名称；spectrum被称为枚举(enumeration)，就像struct变量被称为结构一样。</li>
</ul>
<ul>
<li>将 red、orange、yellow等作为符号常量，它们对应整数值0~7。这些常量叫作枚举量(enumerator)。</li>
</ul>
<p>在默认情况下，将整数值赋给枚举量，第一个枚举量的值为0 ,第二个枚举量的值为1，依次类推。</p>
<p>可以用枚举名来声明这种类型的变量。</p>
<p>枚举变量具有一些特殊的属性，下面来看一看。<br>在不进行强制类型转换的情况下，只能将定义枚举时使用的枚举量赋给这种枚举的变量，如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">band = blue;<span class="comment">// valid, blue is an enumerator </span></span><br><span class="line">band = <span class="number">2000</span>; <span class="comment">// invalid, 2000 not an enumerator</span></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>网络原理(1)</title>
    <url>/2018/10/25/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-1/</url>
    <content><![CDATA[<p>本篇介绍一些网络的前提内容.</p>
<a id="more"></a>

<h1 id="协议与划分层次"><a href="#协议与划分层次" class="headerlink" title="协议与划分层次"></a>协议与划分层次</h1><h2 id="五层协议"><a href="#五层协议" class="headerlink" title="五层协议"></a>五层协议</h2><p><img src="/2018/10/25/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-1/%E4%BA%94%E5%B1%82.png" alt="五层划分"></p>
<p>OSI的划分过于复杂,运行效率低,层次不合理,有些功能在多个层次重复出现.而TCP/IP协议的下面两层并没有具体内容,因此在实际学习过程中折中一下.</p>
<h3 id="1-应用层-application-layer"><a href="#1-应用层-application-layer" class="headerlink" title="(1)应用层(application layer)"></a>(1)应用层(application layer)</h3><p>应用层是体系结构中的最高层。应用层的任务是通过应用进程间的交互来完成特定网络应用。应用层协议定义的是应用进程间通信和交互的规则。这里的进程就是指主机中正在运行的程序。对于不同的网络应用需要有不同的应用层协议。在互联网中的应用层协议很多，如域名系统DNS,支持万维网应用的HTTP协议，支持电子邮件的SMTP协议，等等。我们把应用层交互的数据单元称为报文(message)。</p>
<h3 id="2-运输层-transport-layer"><a href="#2-运输层-transport-layer" class="headerlink" title="(2)运输层(transport layer)"></a>(2)运输层(transport layer)</h3><p>运输层的任务就是负责向两台主机中进程之间的通信提供通用的数据传输服务。应用进程利用该服务传送应用层报文。所谓“通用的”，是指并不针对某个特定网络应用，而是多种应用可以使用同一个运输层服务。由于一台主机可同时运行多个进程，因此运输层有复用和分用的功能。复用就是多个应用层进程可同时使用下面运输层的服务，分用和复用相反，是运输层把收到的信息分别交付上面应用层中的相应进程。</p>
<p>运输层主要使用以下两种协议:</p>
<ul>
<li>传输控制协议TCP(Transmission Control Protocol):提供面向连接的、可靠的数据传输服务，其数据传输的单位是报文段(segment)。</li>
<li>用户数据报协议UDP(User Datagram Protocol):提供无连接的、尽最大努力(best-effort)的数据传输服务(不保证数据传输的可靠性)，其数据传输的单位是用户数据报。</li>
</ul>
<h3 id="3-网络层-network-layer"><a href="#3-网络层-network-layer" class="headerlink" title="(3)网络层(network layer)"></a>(3)网络层(network layer)</h3><p>网络层负责为分组交换网上的不同主机提供通信服务。在发送数据时，网络层把运输层产生的报文段或用户数据报封装成分组或包进行传送。在TCP/IP体系中，由于网络层使用IP协议，因此分组也叫做IP数据报，或简称为数据报。</p>
<h3 id="4-数据链路层-data-link-layer"><a href="#4-数据链路层-data-link-layer" class="headerlink" title="(4)数据链路层(data link layer)"></a>(4)数据链路层(data link layer)</h3><p>数据链路层常简称为链路层。两台主机之间的数据传输，总是在一段一段的链路上传送的，这就需要使用专门的链路层的协议。在两个相邻结点之间传送数据时，数据链路层将网络层交下来的IP数据报组装成帧(framing),在两个相邻结点间的链路上传送帧(frame)。每一帧包括数据和必要的控制信息(如同步信息、地址信息、差错控制等)。 在接收数据时，控制信息使接收端能够知道一个帧从哪个比特开始和到哪个比特结束。这样，数据链路层在收到一个帧后，就可从中提取出数据部分，上交给网络层。控制信息还使接收端能够检测到所收到的帧中有无差错。如发现有差错，数据链路层就简单地丢弃这个出了差错的帧，以免继续在网络中传送下去白白浪费网络资源。如果需要改正数据在数据链路层传输时出现的差错(这就是说，数据链路层不仅要检错，而且要纠错)，那么就要采用可靠传输协议来纠正出现的差错。这种方法会使数据链路层的协议复杂些。</p>
<h3 id="5-物理层-physical-layer"><a href="#5-物理层-physical-layer" class="headerlink" title="(5)物理层(physical layer)"></a>(5)物理层(physical layer)</h3><p>在物理层上所传数据的单位是比特。发送方发送1(或0)时，接收方应当收到1(或0)而不是0(或1)。因此物理层要考虑用多大的电压代表“1” 或 “0”，以及接收方如何识别出发送方所发送的比特。物理层还要确定连接电缆的头应当有多少根引脚以及各引脚应如何连接。当然，解释比特代表的意思，就不是物理层的任务。请注意，传递信息所利用的一些物理媒体，如双绞线、同轴电缆、光缆、无线信道等，并不在物理层协议之内而是在物理层协议的下面。因此也有人把物理层下面的物理媒体当作第0层。</p>
<h1 id="计算机网络的性能指标"><a href="#计算机网络的性能指标" class="headerlink" title="计算机网络的性能指标"></a>计算机网络的性能指标</h1><ul>
<li><p>速率</p>
</li>
<li><p>带宽:在计算机网络中，带宽用来表示网络中某通道传送数据的能力，因此网络带宽表示在单位时间内网络中的某信道所能通过的”最高数据率”。</p>
</li>
<li><p>吞吐量:吞吐量(throughput)表示在单位时间内通过某个网络(或信道、接口)的实际的数据量。</p>
</li>
<li><p><strong>时延</strong>:时延（delay或latency）是指数据(一个报文或分组，甚至比特)从网络(或链路)的一端传送到另一端所需的时间。时延是个很重要的性能指标，它有时也称为延迟或迟延。网络中的时延是由以下几个不同的部分组成的:</p>
<ul>
<li><p><strong>发送时延</strong> 发送时延(transmission delay)是主机或路由器发送数据帧所需要的时间，也就是从发送数据帧的第一个比特算起，到该帧的最后一个比特发送完毕所需的时间。<br>因此发送时延也叫做传输时延.发送时延的计算公式是：<br>$$<br>发送时延=\frac{数据帧长度(bit)}{发送速率(bit/s)}<br>$$</p>
</li>
<li><p><strong>传播时延</strong> 传播时延(propagation delay)是电磁波在信道中传播一定的距离需要花费的时间。传播时延的计算公式是：<br>$$<br>传播时延=\frac{信道长度(m)}{电磁波在信道上的传播速率(m/s)}<br>$$<br>电磁波在自由空间的传播速率是光速，即<code>3.0x10^5km/s</code>。电磁波在网络传输媒体中的传播速率比在自由空间要略低一些：在铜线电缆中的传播速率约为<code>2.3x10^5km/s</code>,在光纤中的传播速率约为<code>2.0x10^5km/s</code>。例如，1000 km 长的光纤线路产生的传播时延大约为5ms.</p>
<p>以上两种时延有本质上的不同。但只要理解这两种时延发生的地方就不会把它们弄 混。发送时延发生在机器内部的发送器中(一般就是网络适配器),与传输信道的长度（或信号传送的距离）没有任何关系。但传播时延则发生在机器外部的传输信道媒体上，而与信号的发送速率无关。信号传送的距离越远，传播时延就越大.</p>
<p>下面还有两种时延也需要考虑，但比较容易理解:</p>
</li>
<li><p><strong>处理时延</strong> 主机或路由器在收到分组时要花费一定的时间进行处理，例如分析分组的首部、从分组中提取数据部分、进行差错检验或查找适当的路由等，这就产生了处理时延.</p>
</li>
<li><p><strong>排队时延</strong> 分组在经过网络传输时，要经过许多路由器。但分组在进入路由器后要先在输入队列中排队等待处理。在路由器确定了转发接口后，还要在输出队列中排队等待转发。这就产生了排队时延。排队时延的长短往往取决于网络当时的通信量。当网络的通信量很大时会发生队列溢出，使分组丢失，这相当于排队时延为无穷大。</p>
<p>这样，数据在网络中经历的总时延就是以上四种时延之和：<br>$$<br>总时延= 发送时延+ 传播时延+ 处理时延+ 排队时延<br>$$<br>一般说来，小时延的网络要优于大时延的网络。在某些情况下，一个低速率、小时延的网络很可能要优于一个高速率但大时延的网络.</p>
<p><img src="/2018/10/25/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-1/%E6%97%B6%E5%BB%B6.png" alt></p>
</li>
</ul>
</li>
<li><p>时延带宽积</p>
<p>把以上讨论的网络性能的两个度量—— 传播时延和带宽—— 相乘，就得到另一个很有用的度量：传播时延带宽积，即:<br>$$<br>时延带宽积=传播时延\times带宽<br>$$<br>我们可以用下面的示意图来表示时延带宽积。这是一个代表链路的圆柱形管道，管道的长度是链路的传播时延（请注意，现在以时间作为单位来表示链路长度），而管道的截面积是链路的带宽。因此时延带宽积就表示这个管道的体积，表示这样的链路可容纳多少个比特。</p>
<p><img src="/2018/10/25/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-1/%E6%97%B6%E5%BB%B6%E5%B8%A6%E5%AE%BD%E7%A7%AF.png" alt></p>
</li>
</ul>
<p>不难看出，管道中的比特数表示从发送端发出的但尚未到达接收端的比特。对于一条正在传送数据的链路，只有在代表链路的管道都充满比特时，链路才得到充分的利用。</p>
<ul>
<li><p>往返时间RTT</p>
<p>在计算机网络中，往返时间RTT(Round-Trip Time)也是一个重要的性能指标。这是因为在许多情况下，互联网上的信息不仅仅单方向传输而是双向交互的。因此，我们有时很需要知道双向交互一次所需的时间。例如，A向B发送数据。如果数据长度是100MB ,发送速率是100Mbit/s，那么<br>$$<br>发送时间=\frac{数据长度}{发送速率}=\frac{100\times2^{20}\times8}{100\times10^{6}}\approx8.39s<br>$$<br>如果B正确收完100MB的数据后，就立即向A发送确认。再假定A只有在收到B的确认信息后，才能继续向B 发送数据。显然，这需要等待一个往返时间RTT(这里假定确认信息很短，可忽略B发送确认的时间).如果RTT=2s ，那么可以算出A向B发送数据的有效数据率.<br>$$<br>有效数据率=\frac{数据长度}{发送时间+RTT}=\frac{100\times2^{20}\times8}{8.39+2}\approx80.7\times10^6bit/s\approx80.7Mbit/s<br>$$<br>比原来的数据率100 Mbit/s小不少.</p>
</li>
<li><p>利用率</p>
<p>利用率有信道利用率和网络利用率两种。信道利用率指出某信道有百分之几的时间是被利用的(有数据通过).完全空闲的信道的利用率是零。网络利用率则是全网络的信道利用率的加权平均值。信道利用率并非越高越好。这是因为，根据排队论的理论，当某信道的利用率增大时，该信道引起的时延也就迅速增加。这和高速公路的情况有些相似。当高速公路上的车流量很大时，由于在公路上的某些地方会出现堵塞，因此行车所需的时间就会变长。网络也有类似的情况。当网络的通信量很少时，网络产生的时延并不大。但在网络通信量不断增大的情况下，由于分组在网络结点(路由器或结点交换机)进行处理时需要排队等候，因此网络引起的时延就会增大。如果令<em>D<sub>0</sub></em>表示网络空闲时的时延，<em>D</em>表示网络当前的时延，那么在适当的假定条件下，可以用下面的简单公式来表示<em>D</em>,<em>D<sub>0</sub></em>和利用率<em>U</em>之间的关系：<br>$$<br>D=\frac{D_0}{1-U}<br>$$<br>这里<em>U</em>是网络的利用率，数值在0到1之间。当网络的利用率达到其容量的1/2时，时延就要加倍。特别值得注意的就是：当网络的利用率接近最大值1时，网络的时延就趋于无穷大。因此我们必须有这样的概念：信道或网络的利用率过高会产生非常大的时延。下图给出了上述概念的示意图。因此一些拥有较大主干网的ISP通常控制信道利用率不超过50%。如果超过了就要准备扩容，增大线路的带宽。</p>
<p><img src="/2018/10/25/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-1/%E5%88%A9%E7%94%A8%E7%8E%87.png" alt></p>
</li>
</ul>
<h1 id="分组交换"><a href="#分组交换" class="headerlink" title="分组交换"></a>分组交换</h1><p>分组交换则采用存储转发技术。把一个报文划分为几个分组后再进行传送。通常我们把要发送的整块数据称为一个报文(message)。在发送报文之前，先把较长的报文划分成为一个个更小的等长数据段，例如，每个数据段为1024bit。在每一个数据段前面，加上一些由必要的控制信息组成的首部(header)后，就构成了一个分组(packet)。分组又称为“包”，而分组的首部也可称为“包头”。分组是在互联网中传送的数据单元。分组中<br>的“首部”是非常重要的，正是由于分组的首部包含了诸如目的地址和源地址等重要控制信息，每一个分组才能在互联网中独立地选择传输路径，并被正确地交付到分组传输的终点。</p>
<p><img src="/2018/10/25/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-1/%E5%88%86%E7%BB%84.png" alt></p>
<p>现在假定图中的主机H<sub>1</sub>向主机H<sub>5</sub>发送数据。主机H<sub>1</sub>先将分组逐个地发往与它 直接相连的路由器A。此时，除链路H<sub>1</sub>-A外，其他通信链路并不被目前通信的双方所占用。需要注意的是，即使是链路H<sub>1</sub>-A ,也只是当分组正在此链路上传送时才被占用。在各分组传送之间的空闲时间，链路H<sub>1</sub>-A仍可为其他主机发送的分组使用.</p>
<p>路由器A把主机H<sub>1</sub>发来的分组放入缓存。假定从路由器A的转发表中查出应把该分组转发到链路A-C。于是分组就传送到路由器C。当分组正在链路A-C传送时，该分组并不占用网络其他部分的资源。</p>
<p>路由器C继续按上述方式查找转发表，假定查出应转发到路由器E。当分组到达路由器E后，路由器E就最后把分组直接交给主机H<sub>5</sub>。</p>
<p>假定在某一个分组的传送过程中，链路A-C的通信量太大，那么路由器A可以把分组沿另一个路由传送，即先转发到路由器B，再转发到路由器E ,最后把分组送到主机H<sub>5</sub>。在网络中可同时有多台主机进行通信，如主机H<sub>5</sub>也可以经过路由器B和E与主机H<sub>6</sub>通信。</p>
<p>这里要注意，路由器暂时存储的是一个个短分组，而不是整个的长报文。短分组是暂存在路由器的存储器(即内存)中而不是存储在磁盘中的。这就保证了较高的交换速率。</p>
<p>优点:</p>
<table>
<thead>
<tr>
<th>优点</th>
<th>手段</th>
</tr>
</thead>
<tbody><tr>
<td>高效</td>
<td>分组传输的过程中动态分配传输带宽，对通信链路是逐段占用</td>
</tr>
<tr>
<td>灵活</td>
<td>为每一个分组独立地选择最合适的转发路由</td>
</tr>
<tr>
<td>迅速</td>
<td>以分组作为传送单位，可以不先建立连接就能向其他主机发送分组</td>
</tr>
<tr>
<td>可靠</td>
<td>保证可靠性的网络协议；分布式多路由的分组交换网，使网络有很好的生存性</td>
</tr>
</tbody></table>
<p>分组交换也带来一些新的问题。例如，分组在各路由器存储转发时需要排队，这就会造成一定的时延。因此，必须尽量设法减少这种时延。</p>
<p>此外，由于分组交换不像电路交换那样通过建立连接来保证通信时所需的各种资源，因而无法确保通信时端到端所需的带宽。分组交换带来的另一个问题是各分组必须携带的控制信息也造成了一定的开销(overhead).整个分组交换网还需要专门的管理和控制机制。</p>
]]></content>
      <tags>
        <tag>网络</tag>
      </tags>
  </entry>
  <entry>
    <title>网络原理(2)</title>
    <url>/2018/11/03/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-2/</url>
    <content><![CDATA[<p>本篇介绍网络的物理层.</p>
<p>版权声明,本文基于王道考研书籍整理,并非原创.</p>
<a id="more"></a>

<p>本篇的重要内容:</p>
<p>(一) 通信基础</p>
<ul>
<li>信道,信号,带宽,码元,波特,速率,信源与信宿等基本概念</li>
<li>奈奎斯特定理与香农定理:编码与调制</li>
<li>电路交换,报文交换与分组交换:数据报与虚电路</li>
</ul>
<p>(二) 传输介质</p>
<ul>
<li>双绞线,同轴电缆,光纤与无线传输介质；物理层接口的特性</li>
</ul>
<p>(三) 物理层设备</p>
<ul>
<li>中继器,集线器</li>
</ul>
<h1 id="2-1通信基础"><a href="#2-1通信基础" class="headerlink" title="2.1通信基础"></a>2.1通信基础</h1><h2 id="2-1-1基本概念"><a href="#2-1-1基本概念" class="headerlink" title="2.1.1基本概念"></a>2.1.1基本概念</h2><h4 id="1-数据-信号与码元"><a href="#1-数据-信号与码元" class="headerlink" title="1.数据,信号与码元"></a>1.数据,信号与码元</h4><p>数据是指传送信息的实体.信号则是数据的电气的或电磁的表现,是数据在传输过程中的存在形式.数据和信号都可以用”模拟的”或”数字的”来修饰:①连续变化的数据(或信号)称为模拟数据(或模拟信号);②取值仅允许为有限的几个离散数值的数据(或信号)称为数字数据(或数字信号).</p>
<p>码元是指用一个固定时长的信号波形(数字脉冲),表示一位k进制数字,代表不同离散数值的基本波形,是数字通信中数字信号的计量单位,这个吋长内的信号称为k进制码元,而该时长称为码元宽度.1码元可以携带多个比特的信息量.例如,在使用二进制编码时,只有两种不同的码元,一种代表0状态,另一种代表1状态.</p>
<h4 id="2-信源-信道与信宿"><a href="#2-信源-信道与信宿" class="headerlink" title="2.信源,信道与信宿"></a>2.信源,信道与信宿</h4><p>信源是产生和发送数据的源头.信宿是接收数据的终点,它们通常都是计算机或其他数字终端装置.发送端信源发出的信息需要通过变换器转换成适合于在信道上传输的信号,而通过信道传输到接收端的信号先由反变换器转换恢复成原始的信息,再发送给信宿.</p>
<p>信道与电路并不等同,信道是信号的传输媒介.一个信道可以看成是一条线路的逻辑部件, 一般用来表示向某一个方向传送信息的介质,因此一条通信线路往往包含一条发送信道和一条接收信道.噪声源是信道上的噪声(即对信号的干扰)以及分散在通信系统其他各处的噪声的集中表示.</p>
<p>信道按传输信号形式的不同可分为传送模拟信号的模拟信道和传送数字信号的数字信道两大类；信道按传输介质的不同可分为无线信道和有线信道.</p>
<p>信道上传送的信号有基带信号和宽带信号之分.基带信号是将数字信号1和0直接用两种不同的电压表示,然后送到数字信道上去传输(称为基带传输);宽带信号是将基带信号进行调制后形成的频分复用模拟信号,然后传送到模拟信道上去传输(称为宽带传输/频带传输).</p>
<p>从通信双方信息的交互方式看,可以有三种基本方式:</p>
<p>1) 单工通信.只有一个方向的通信而没有反方向的交互,仅需要一条信道.例如.无线电广播,电视广播就属于这种类型.<br>2) 半双工通信.通信的双方都可以发送或接收信息,但任何一方都不能同时发送和接收,需要两条信道.<br>3) 全双工通信.通信双方可以同时发送和接受信息,也需要两条信道.</p>
<p>信道的极限容量是指信道的最高码元传输速率或信道的极限信息传输速率.</p>
<h4 id="3-速率-波特与带宽"><a href="#3-速率-波特与带宽" class="headerlink" title="3.速率,波特与带宽"></a>3.速率,波特与带宽</h4><p>速率也叫数据率,是指数据的传输速率,表示单位时间内传输的数据量.可以用码元传输速率和信息传输速率表示.</p>
<p>1) 码元传输速率.又可称为码元速率,波形速率等,它表示单位时间内数字通信系统所传输的码元个数(也可称为脉冲个数或信号变化的次数),单位是波特(Baud).1波特表示数字通信系统每秒传输一个码元.这里的码元可以是多进制的,也可以是二进制的,但码元速率与进制数无关.<br>2) 信息传输速率.又可称为信息速率,比特率等,它表示单位时间内数字通信系统传输的二进制码元个数(即比特数),单位是比特/秒(b/s).</p>
<blockquote>
<p>若一个码元携带bit的信息量,则 M Baud的码元传输速率所对应的信息传输速率为$M\times n\quad bit/s$</p>
</blockquote>
<p>带宽原是指信号具有的频带宽度,单位是赫兹(Hz).在实际网络中,由于数据率是信道最 重要的指标之一,而带宽与数据率存在数值上的互换关系,常用来表示网络的通信线路所能传输数据的能力.因此,带宽表示在单位时间内从网络中的某一点到另一点所能通过的”最髙数据率”.显然,此时带宽的单位不再是Hz ,而是bit/s,或b/s.</p>
<h2 id="2-1-2奈奎斯特定理与香农定理"><a href="#2-1-2奈奎斯特定理与香农定理" class="headerlink" title="2.1.2奈奎斯特定理与香农定理"></a>2.1.2奈奎斯特定理与香农定理</h2><h3 id="1-奎奈斯特定理"><a href="#1-奎奈斯特定理" class="headerlink" title="1.奎奈斯特定理"></a>1.奎奈斯特定理</h3><p>奈奎斯特(Nyquist)定理又称为奈氏准则,它指出在理想低通(没有噪声,带宽有限)的信道中,极限码元传输率为2W Baud.其中,W是理想低通信道的带宽,单位为Hz.若用V表示每个码元离散电平的数目(码元的离散电平数目是指有多少种不同的码元,比如有16种不同的码元,则需要4位二进制位,因此数据传输率是码元传输率的4倍),则极限数据率为<br>$$<br>理想低通信道下的极限数据传输率=2\times{W}\times{log_2V}(b/s)<br>$$</p>
<h3 id="2-香农定理"><a href="#2-香农定理" class="headerlink" title="2.香农定理"></a>2.香农定理</h3><p>香农(Shannon)定理给出了带宽受限且有高斯白噪声干扰的信道的极限数据传输速率,当用此速率进行传输时,可以做到不产生误差.香农定理定义为<br>$$<br>信道的极限数据传输速率=W\times{log_2({1+S/N})}(b/s)<br>$$<br>式中,W 为信道的带宽,S 为信道所传输信号的平均功率,N 为信道内部的髙斯噪声功率.S/N为信噪比,即信号的平均功率和噪声的平均功率之比,信噪比=10log<sub>10</sub>(S/N)(单位:dB), 如,当 S/N=10时,信噪比为10dB,而当S/N=1000时,信噪比为30dB.</p>
<h2 id="2-1-3编码与调制"><a href="#2-1-3编码与调制" class="headerlink" title="2.1.3编码与调制"></a>2.1.3编码与调制</h2><p>数据无论是数字的还是模拟的,为了传输的目的都必须转变成信号,把数据变换为模拟信号的过程称为调制,把数据变换为数字信号的过程称为编码.</p>
<p>数字数据可以通过数字发送器转换成数字信号传输,也可以通过调制器转换成模拟信号传输:同样,模拟数据可以<br>通过PCM编码器转换成数字信号传输,也可以通过放大器调制器转换成模拟信号传输.这样就形成了下列四种编码方式.</p>
<h3 id="1-数字数据编码为数字信号"><a href="#1-数字数据编码为数字信号" class="headerlink" title="1.数字数据编码为数字信号"></a>1.数字数据编码为数字信号</h3><p>数字数据编码用于基带传输中,即在基本不改变数字数据信号频率的情况下,直接传输数字信号.对于这种编码方式,具体用什么样的数字信号表示0 以及用什么样的数字信号表示1就是所谓的编码.编码的规则可以有多种,原则上只要能有效地把1和0 区分开即可,常用的编码方法有以下几种:</p>
<p><img src="/2018/11/03/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-2/%E7%BC%96%E7%A0%81%E6%96%B9%E5%BC%8F.png" alt></p>
<p>(1) 非归零码<br>非归零码(NRZ)是用两个电压来代表两个二进制数字,如用低电平表示0 ,用高电平表示1；或者相反.这种编码虽然容易实现,但没有检错功能,而且也无法判断一个码元的开始和结束,以至于收发双方难以保持同步.<br>(2) 曼彻斯特编码<br>曼彻斯特编码(Manchester Encoding) 将一个码元分成两个相等的间隔,前一个间隔为髙 电平后一个间隔为低电平表示码元1 :码元0 则正好相反.也可以采用相反的规定.该编码的特点是在每一个码元的中间出现电平跳变,位中间的跳变既作时钟信号(可用于同步),又作数据信号,但它所占的频带宽度是原始的基带宽度的两倍.以太网使用的编码方式就是曼彻斯特编码.<br>(3) 差分曼彻斯特编码<br>差分曼彻斯特编码常用于局域网传输,其规则是:若码元为1,则前半个码元的电平与上一个码元的后半个码元的电平相同,若为0 ,则相反.该编码的特点是,在每个码元的中间,都有一次电平的跳转,可以实现自同步,且抗干扰性较好.</p>
<p>(4)4B/5B编码<br>将欲发送数据流的每4位作为一组,然后按照4B/5B编码规则将其转换成相应的5 位码.5位码共32种组合,但只采用其中的16种对应16种不同的4 位码,其他的16种作为控制码(帧的开始和结束,线路的状态信息等)或保留.</p>
<p><a href="https://www.cnblogs.com/lifan3a/articles/5257728.html" target="_blank" rel="noopener">参考链接</a></p>
<h3 id="2-数字数据调制为模拟信号"><a href="#2-数字数据调制为模拟信号" class="headerlink" title="2.数字数据调制为模拟信号"></a>2.数字数据调制为模拟信号</h3><p>数字数据调制技术在发送端将数字信号转换为模拟信号,而在接收端将模拟倍号还原为数字<br>信号,分别对应于调制解调器的调制和解调过程.基本的调制方法有:</p>
<p>1) 幅移键控(ASK),通过改变载波信号的振幅来表示数字信号1和 0 , 而载波的频率和相<br>位都不改变.比较容易实现,但抗干扰能力差.<br>2) 频移键控(FSK),通过改变载波信号的频率来表示数字信号1 和 0 , 而载波的振幅和相 位都不改变.容易实现,抗干扰能力强.目前应用较为广泛.<br>3) 相移键控(PSK), 通过改变载波信号的相位来表示数字信号1 和 0 . 而载波的振幅和频 率都不改变.它又分为绝对调相和相对调相.<br>4) 正交振幅调制(QAM),在频率相同的前提下,将 ASK与 PSK结合起来,形成叠加信号. 设波特率为B ,采用m个相位,每个相位有n 种振幅,则该QAM技术的数据传输率R 为<br>$$<br>R=B\times{log_2{(m\times{n})}}\quad(b/s)<br>$$<br>如图所示是二进制幅移键控,频移键控和相移键控的例子,2ASK中用载波有幅度和无幅度分别表示数字数据的” 1” 和”0”:2FSK中用两种不同的频率分别表示数字数据” 1” 和”0”;2PSK中用相位0和相位分别表示数字数据的”1” 和 “0”,是一种绝对调相方式.</p>
<p><img src="/2018/11/03/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-2/%E8%B0%83%E7%9B%B8.png" alt></p>
<h3 id="3-模拟数据编码为数字信号"><a href="#3-模拟数据编码为数字信号" class="headerlink" title="3.模拟数据编码为数字信号"></a>3.模拟数据编码为数字信号</h3><p>该编码方法最典型的例子就是常用于对音频信号进行编码的脉码调制(PCM), 它主要包括三个步骤,即抽样,量化和编码.</p>
<p>先來介绍采样定理:在通信领域带宽是指信号最髙频率与最低频率之差,单位为Hz.因此 将模拟信号转换成数字信号时,假设原始信号中的最大频率为<em>f</em>,那么采样频率必须大于或等于最大频率<em>f<sub>采样</sub></em>的两倍,才能保证采样后的数字信号完整保留原始模拟信号的信息.</p>
<h3 id="4-模拟数据调制为模拟信号"><a href="#4-模拟数据调制为模拟信号" class="headerlink" title="4.模拟数据调制为模拟信号"></a>4.模拟数据调制为模拟信号</h3><p>为了实现传输的有效性,可能需要较髙的频率.这种调制方式还可以使用频分(FDM)复用技术,充分利用带宽资源,在电话机和本地局交换机所传输的信号是采用模拟信号传输模拟数据的编码方式；模拟的声音数据是加载到模拟的载波信号中传输的.</p>
<h2 id="2-1-4电路交换-报文交换和分组交换"><a href="#2-1-4电路交换-报文交换和分组交换" class="headerlink" title="2.1.4电路交换,报文交换和分组交换"></a>2.1.4电路交换,报文交换和分组交换</h2><h3 id="1-电路交换"><a href="#1-电路交换" class="headerlink" title="1.电路交换"></a>1.电路交换</h3><p>在进行数据传输前,两个结点之间必须先建立一条专用(双方独占)的物理通信路径(由通信双方之间的交换设备和链路逐段连接而成),该路径可能经过许多中间结点.该线路在整个数据传输期间一直被独占,直到通信结束后才被释放.因此,电路交换技术分为三个阶段:连接建立,数据传输和连接释放.</p>
<p>电路交换技术的优点是:</p>
<p>1) 通信时延小.由于通信线路为通信双方用户专用,数据直达,所以传输数据的时延非常小.当传输的数据量较大时,这一优点非常明显.<br>2) 有序传输,双方通信时按发送顺序传送数据,不存在失序问题.<br>3) 没有冲突.不同的通信双方拥有不同的信道,不会出现争用物理信道的问题.<br>4) 适用范围广.电路交换既适用于传输模拟信号,又适用于传输数字信号,<br>5) 实时性强,通信双方之间的物理通路一旦建立,双方可以随时通信.<br>6) 控制简单,电路交换的交换设备(交换机等)及控制均较简单.<br>电路交换技术的缺点是:</p>
<p>1) 建立连接时间长.电路交换的平均连接建立时间对计算机通信来说太长.<br>2) 线路独占,使用效率低.电路交换连接建立后,物理通路被通信双方独占,即使通信线路空闲,也不能供其他用户使用,因而信道利用率低.<br>3) 灵活性差.只要在通信双方建立的通路中的任何一点出了故障,就必须重新拨号建立新的连接,这对十分紧急和重要的通信是很不利的.<br>4) 难以规格化.电路交换时,数据直达,不同类型,不同规格,不同速率的终端很难相互进行通信,也难以在通信过程中进行差错控制.</p>
<p>要注意,当电路建立以后,除了源结点和目的结点,电路上的任何结点都采取”直通式”接收数据和发送数据,即不会存在存储转发所耗费的时间.</p>
<h3 id="2-报文交换"><a href="#2-报文交换" class="headerlink" title="2.报文交换"></a>2.报文交换</h3><p>数据交换的单位是报文,报文携带有目标地址,源地址等信息.报文交换在交换结点采用的是存储转发的传输方式.<br>报文交换技术的优点是:</p>
<p>1) 无需建立连接:报文交换不需要为通信双方预先建立一条专用的通信线路,不存在建立连接时延,用户可以随时发送报文.<br>2) 动态分配线路:当发送方把报文交给交换设备时,交换设备先存储整个报文,然后选择一条合适的空闲线路,将报文发送出去,<br>3) 提髙线路可靠性:如果某条传输路径发生故障,可重新选择另一条路径传输数据,所以提髙了传输的可靠性.<br>4) 提高线路利用率:通信双方不是固定占有一条通信线路,而是在不同的时间一段一段地部分占有这条物理通道,因而大大提高了通信线路的利用率.<br>5) 提供多目标服务:一个报文可以同时发送往多个目的地址,这在电路交换中是很难实现的.<br>报文交换技术的缺点是:</p>
<p>1) 由于数据进入交换结点后要经历存储,转发这一过程,从而引起转发时延(包括接收报文,检验正确性,排队,发送时间等).<br>2) 报文交换对报文的大小没有限制,这就要求网络结点需要有较大的缓存空间.</p>
<h3 id="3-分组交换"><a href="#3-分组交换" class="headerlink" title="3.分组交换"></a>3.分组交换</h3><p>同报文交换一样,分组交换也采用了存储转发方式,但解决了报文交换中大报文传输的问题.分组交换限制了每次传送的数据块大小的上限,把大的数据块划分为合理的小数据块,再加上一 些必要的控制信息(如源地址,目的地址和编号信息等),构成分组(Packet).网络结点根据控 制信息把分组送到下一结点,下一结点接收到分组后,暂时保存下来并排队等待传输,然后根据分组控制信息选择它的下一个结点,直到到达目的结点.</p>
<p>分组交换的优点是:</p>
<p>1) 无建立时延.不需要为通信双方预先建立一条专用的通信线路,不存在连接建立时延,用户可随时发送分组.<br>2) 线路利用率髙.通信双方不是固定占有一条通信线路,而是在不同的时间一段一段地部分占有这条物理通路,因而大大提髙了通信线路的利用率.<br>3) 简化了存储管理(相对于报文交换).因为分组的长度固定,相应的缓冲区的大小也固定,在交换结点中存储器的管理通常被简化为对缓冲区的管理,相对比较容易.<br>4) 加速传输:分组是逐个传输,可以使后一个分组的存储操作与前一个分组的转发操作并行,这种流水线方式减少了报文的传输时间.此外,传输一个分组所需的缓冲区比传输一次报文所需的缓冲区小得多,这样因缓冲区不足而等待发送的几率及时间也必然少得多.<br>5) 减少了出错几率和重发数据量:因为分组较短,其出错几率必然减少,所以每次重发的数据量也就大大减小,这样不仅提高了可靠性,也减少了传输时延.<br>分组交换的缺点是:</p>
<p>1) 存在传输时延.尽管分组交换比报文交换的传输时延少,但相对于电路交换仍存在存储转发时延,而且其结点交换机必须具有更强的处理能力.<br>2) 需要传输额外的信息量.每个小数据块都要加上源,目的地址和分组编号等信息,从而构成分组,使传送的信息量大约增大5%〜 10%, —定程度上降低了通信效率,增加了处理的时间,使控制复杂,时延増加.<br>3) 当分组交换采用数据报服务时,可能出现失序,丢失或重复分组,分组到达目的结点时,要对分组按编号进行排序等工作,增加了麻烦.若采用虚电路服务,虽无失序问题,但有呼叫建立,数据传输和虚电路释放三个过程.</p>
<h2 id="2-1-5数据报和虚电路"><a href="#2-1-5数据报和虚电路" class="headerlink" title="2.1.5数据报和虚电路"></a>2.1.5数据报和虚电路</h2><p>分组交换根据其通信子网向端点系统提供的服务,还可以进一步分为面向连接的虚电路方式和无连接的数据报方式.这两种服务方式都由网络层提供.要注意数据报方式和虚电路方式是分组交换的两种方式.</p>
<h3 id="1-数据报"><a href="#1-数据报" class="headerlink" title="1.数据报"></a>1.数据报</h3><p>当作为通信子网用户的端系统要发送一个报文吋,在端系统中实现的卨层协议先把报文拆成若干个带有序号的数据单元,并在网络层加上地址等控制信息后形成数据报分组(即网络层PDU).中间结点存储分组一段很短的时间,找到最佳的路由后,尽快转发每个分组.不同的分组可以走不同的路径,也可以按不同的顺序到达目的结点.</p>
<p><img src="/2018/11/03/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-2/%E6%95%B0%E6%8D%AE%E6%8A%A5.png" alt></p>
<p>1) 主机A 先将分组逐个发往与它直接相连的交换结点A ,交换结点A 将收到的分组缓存.<br>2) 然后查找自己的转发表.由于不同时刻的网络状态不一样,因此转发表的内容可能不完全相同,因此有的分组转发给交换结点C ,有的分组转发给交换结点D.<br>3) 网络中的其他结点收到分组后,类似地转发分组,直到分组最终到达主机B.当分组正在某一链路上传送时,分组并不占有网络的其他部分资源.因为采用存储转发技术, 资源是共享的,所以主机A 在发送分组时,主机B也可以同时向其他主机发送分组.</p>
<p>通过上面的例子,我们可以总结出数据报服务具有如下特点:</p>
<p>1) 发送分组前不需要建立连接.发送方可随时发送分组,网络中的结点可随时接收分组.<br>2) 网络尽最大努力交付,传输不保证可靠性,所以可能丢失:为每个分组独立地选择路由,转发的路径可能不同,因而分组不一定按序到达目的结点.<br>3) 发送的分组中要包括发送端和接收端的完整地址,以便可以独立传输.<br>4) 分组在交换结点存储转发时,需要排队等候处理,这会带来一定的时延.当通过交换结点的通信量较大或网络发生拥塞时,这种时延会大大增加,交换结点还可根据情况丢弃部分分组.<br>5) 网络具有冗余路径,当某一交换结点或一段链路出现故障时,可相应地更新转发表,寻找另一条路径转发分组,对故障的适应能力强.<br>6) 存储转发的延时一般较小,提高了网络的吞吐量.<br>7) 收发双方不独占某一链路,资源利用率较髙.</p>
<h3 id="2-虚电路"><a href="#2-虚电路" class="headerlink" title="2.虚电路"></a>2.虚电路</h3><p>虚电路方式试图将数据报方式与电路交换方式结合起来,充分发挥两种方法的优点,以达到最佳的数据交换效果.在分组发送之前,要求在发送方和接收方建立一条逻辑上相连的虚电路,并且连接一旦建立,虚电路所对应的物理路径也就固定了.与电路交换类似,整个通信过程分为三个阶段:虚电路建立,数据传输与虚电路释放阶段.</p>
<p>在虚电路方式中,端系统每次建立虚电路时,选择一个没用过的虚电路号分配给该虚电路,以区别于本系统中的其他虚电路.在传送数据时,每个数据分组不仅要有分组号,检验和等控制信息,还要有它要通过的虚电路号,以区别于其他虚电路上的分组.在虚电路网络中的每个结点上都维持一张虚电路表,它的每一项记录了一个打开的虚电路的信息,包括在接收链路和发送链路上的虚电路号,前一结点和下一结点的标识,数据的传输是双向进行的,上述信息是在虚电路的建立过程中确定的.</p>
<p><img src="/2018/11/03/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-2/%E8%99%9A%E7%94%B5%E8%B7%AF.png" alt></p>
<p>1) 为了进行数据传输,主机A与主机B之间先建立一条逻辑通路,主机A发出一个特殊的”呼叫请求”分组,该分组通过中间结点送往主机B,如果主机B同意连接,就发送”呼叫应答”分组予以确认.<br>2) 在虚电路建立之后,主机A就可以向主机B发送数据分组.当然,主机B也可以在该虚电路上向主机A发送数据.<br>3) 传送结束后主机A通过发送”释放请求”分组以拆除虚电路,整个连接就逐段断开了.</p>
<p>通过上面的例子,可以总结出虚电路服务具有如下特点：</p>
<p>1) 虚电路通信链路的建立和拆除需要时间开销,对交互式的应用和小量的短分组情况,显得很浪费,但对长时间,频繁的数据交换,则效率较髙.<br>2) 虚电路的路由选择体现在连接建立阶段,连接建立后,传输路径就确定了.<br>3) 虚电路提供了可靠的通信功能,能保证每个分组正确且有序到达.此外,还可以对两个数据端点的流燉进行控制,当接收方来不及接收数据时,可以通知发送方暂缓发送.<br>4) 虚电路有一个致命的弱点,即当网络中的某个结点或某条链路出故障而彻底失效时,则所有经过该结点或该链路的虚电路将遭到破坏.<br>5) 分组首部并不包含目的地址,而是包含虚电路标识符,相对数据报方式开销小.</p>
<p>虚电路之所以是”虚”的,是因为这条电路不是专用的,每个结点到其他结点之间的链路可能同时有若干虚电路通过,它也可能同时与多个结点之间具有虚电路.每条虚电路支持特定的两个端系统之间的数据传输,两个端系统之间也可以有多条虚电路为不同的进程服务,这些虚电路的实际路由可能相同也可能不相同.</p>
<h1 id="2-2传输介质"><a href="#2-2传输介质" class="headerlink" title="2.2传输介质"></a>2.2传输介质</h1><h2 id="2-2-1双绞线-同轴电缆-光纤和无线传输介质"><a href="#2-2-1双绞线-同轴电缆-光纤和无线传输介质" class="headerlink" title="2.2.1双绞线,同轴电缆,光纤和无线传输介质"></a>2.2.1双绞线,同轴电缆,光纤和无线传输介质</h2><h2 id="2-2-2物理层接口的特性"><a href="#2-2-2物理层接口的特性" class="headerlink" title="2.2.2物理层接口的特性"></a>2.2.2物理层接口的特性</h2><p>1) 机械特性 主要定义物理连接的边界点,即接插装置.规定物理连接时所采用的规格、引线的数目、引脚的数量和排列情况等.<br>2) 电气特性 规定传输二进制位时,线路上信号的电压髙低、阻抗匹配、传输速率和距离限制等.<br>3) 功能特性 指明某条线上出现的某一电平的电压表示何种意义,接口部件的信号线(数据线、控制线、定时线等)的用途.<br>4) 规程特性 主要定义各条物理线路的工作规程和时序关系.</p>
<p>常用的物理层接口标准有EIA RS-232-C,ADSL和SONET/SDH等.</p>
<h1 id="2-3物理层设备"><a href="#2-3物理层设备" class="headerlink" title="2.3物理层设备"></a>2.3物理层设备</h1><h2 id="2-3-1中继器"><a href="#2-3-1中继器" class="headerlink" title="2.3.1中继器"></a>2.3.1中继器</h2><p>中继器又称为转发器,主要功能是将信号整形并放大再转发出去,以消除信号由于经过一长段电缆,因噪声或其他原因而造成的失真和袞减,使信号的波形和强度达到所需要的要求,来扩大网络传输的距离,其原理是信号再生(而不是简单地将衰减的信号放大),中继器有两个端口,将一个端口输入的数据从另一个端口发送出去,它仅作用于信号的电气部分,而不管数据中是否有错误数据或不适于网段的数据,</p>
<p>从理论上讲,中继器的使用数目是无限的,网络因而也可以无限延长,但事实上这是不可能的,因为网络标准中都对信号的延迟范围作了具体的规定,中继器只能在此规定范围内进行有效的工作,否则会引起网络故障,例如,在采用粗同轴电缆的10BASE5以太网规范中,互相串联的中继器的个数不能超过4 个,而且用4 个中继器串联的5 段通信介质中只有3 个段可以挂接计算机,其余两个段只能用做扩展通信范围的链路段,不能挂接计算机,这就是所谓的”5-4-3规则”.</p>
<blockquote>
<p>注意：放大器和中继器都是起放大作用,只不过放大器放大的是模拟信号,原理是将衰减的信号放大,中继器放大的是数字信号,原理是将衰减的信号整形再生,如果某个网络设备具有存储转发的功能,那么可以认为该设备可以连接两个不同的协议,如果该网络设备没有存储转发功能,则认为该设备不能连接两个不同的协议:中继器是没有存储转发功能的,因此中继器不能连接两个速率不同的网段,中继器两端的网段一定要是同一个协议.</p>
</blockquote>
<h2 id="2-3-2集线器"><a href="#2-3-2集线器" class="headerlink" title="2.3.2集线器"></a>2.3.2集线器</h2><p>集线器(Hub)实质上是一个多端口的中继器,也工作在物理层,在Hub工作时,当一个端 口接收到数据信号后,由于信号在从端口到Hub的传输过程中已有了衰减,所以Hub便将该信号 进行整形放大,使之再生(恢复)到发送时的状态,紧接着转发到其他所有(除输入端口以外)处于工作状态的端口上,如果同时有两个或多个端口输入,则输出时会发生冲突,致使这些数据都成为无效的,从Hub的工作方式可以看出,它在网络中只起到信号放大和转发作用,其目的是 扩大网络的传输范围,而不具备信号的定向传送能力,即信号传输的方向是固定,是一个标准的共享式设备,</p>
<p>Hub主要用于使用双绞线组建共享网络,是解决从服务器连接到桌面最经济的方案,在交换 式网络中,Hub直接与交换机相连,将交换机端口的数据送到桌面,使用Hub组网灵活,它把所 有结点的通信集中在以其为中心的结点上,对结点相连的工作站进行集中管理,不让出问题的工作站影响整个网络的正常运行,并且用户的加入和退出也很自由,</p>
<p>由Hub组成的网络是共享式网络,在逻辑上仍然是一个总线网,Hub每个端口连接的网络部 分是同一个网络的不同网段,同时Hub也只能够在半双工下工作,网络的吞吐率因而受到限制, </p>
<blockquote>
<p>注意：多台计算机同时通信必然会发生,所以集线器不能分割冲突域,所有集线器端口都属于同一个冲突域,集线器在一个时钟周期中只能传输一组信息,如果一台集线器连接的机器数目较多,并i多台机器经常需要同时通信,将导致信息的碰撞,使得集线器的工作效率很差,比如一个带宽为10Mb/s的集线器上连接了8台计算机,当这8台计算机同时工作时,每台计算机真正所拥有的带宽为10/8 Mb/s=1.25Mb/s.</p>
</blockquote>
<p><img src="/2018/11/03/%E7%BD%91%E7%BB%9C%E5%8E%9F%E7%90%86-2/%E6%80%BB%E7%BB%93.png" alt></p>
]]></content>
      <tags>
        <tag>网络</tag>
      </tags>
  </entry>
  <entry>
    <title>网络编程</title>
    <url>/2020/06/04/%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<p>网络编程.</p>
<a id="more"></a>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@org</span>.junit.Test</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    InetAddress inet = <span class="keyword">null</span>;</span><br><span class="line">    InetAddress localHost = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        inet = InetAddress.getByName(<span class="string">"www.baidu.com"</span>);<span class="comment">//通过域名或则IP地址实例化</span></span><br><span class="line">        localHost = InetAddress.getLocalHost();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (UnknownHostException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println(inet);</span><br><span class="line">    System.out.println(localHost);</span><br><span class="line">    System.out.println(inet.getHostName());<span class="comment">//获取对象的域名</span></span><br><span class="line">    System.out.println(inet.getHostAddress());<span class="comment">//获取百度的IP地址</span></span><br><span class="line">    System.out.println(localHost.getHostAddress());<span class="comment">//获取对象的地址</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>主要是用socket指明hostname和端口号的问题。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@org</span>.junit.Test</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">client</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Socket socket = <span class="keyword">null</span>;</span><br><span class="line">    OutputStream os = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        InetAddress inet = InetAddress.getByName(<span class="string">"127.0.0.1"</span>);</span><br><span class="line">        socket = <span class="keyword">new</span> Socket(inet, <span class="number">8899</span>);</span><br><span class="line">        os = socket.getOutputStream();</span><br><span class="line">        os.write(<span class="string">"hello"</span>.getBytes());</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (os != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                os.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (socket != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                socket.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@org</span>.junit.Test</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">server</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ServerSocket serverSocket = <span class="keyword">null</span>;</span><br><span class="line">    Socket socket = <span class="keyword">null</span>;</span><br><span class="line">    InputStream is = <span class="keyword">null</span>;</span><br><span class="line">    ByteArrayOutputStream baos = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        serverSocket = <span class="keyword">new</span> ServerSocket(<span class="number">8899</span>);</span><br><span class="line">        socket = serverSocket.accept();</span><br><span class="line">        is = socket.getInputStream();</span><br><span class="line"></span><br><span class="line">        baos = <span class="keyword">new</span> ByteArrayOutputStream();</span><br><span class="line">        <span class="keyword">int</span> len;</span><br><span class="line">        <span class="keyword">byte</span>[] bytes = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">5</span>];</span><br><span class="line">        <span class="keyword">while</span> ((len = is.read(bytes)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            baos.write(bytes);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(baos.toString());</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (baos != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                baos.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (is != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                is.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (socket != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                socket.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (serverSocket != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                serverSocket.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码是客户端向服务端发送了一句话然后打印。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@org</span>.junit.Test</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">client</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Socket socket = <span class="keyword">null</span>;</span><br><span class="line">    OutputStream os = <span class="keyword">null</span>;</span><br><span class="line">    FileInputStream fis = <span class="keyword">null</span>;</span><br><span class="line">    ByteArrayOutputStream bos = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        InetAddress inet = InetAddress.getByName(<span class="string">"localhost"</span>);</span><br><span class="line">        socket = <span class="keyword">new</span> Socket(inet, <span class="number">10086</span>);</span><br><span class="line">        os = socket.getOutputStream();</span><br><span class="line">        fis = <span class="keyword">new</span> FileInputStream(<span class="string">"关系.png"</span>);</span><br><span class="line">        <span class="keyword">byte</span>[] buffer = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">int</span> len;</span><br><span class="line">        <span class="keyword">while</span> ((len = fis.read(buffer)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            os.write(buffer,<span class="number">0</span>,len);</span><br><span class="line">        &#125;</span><br><span class="line">        socket.shutdownOutput();<span class="comment">//关闭socket的输出流，不然服务端的read会一直等着客户端发数据</span></span><br><span class="line">        InputStream is = socket.getInputStream();</span><br><span class="line">        bos = <span class="keyword">new</span> ByteArrayOutputStream();</span><br><span class="line">        <span class="keyword">int</span> len1;</span><br><span class="line">        <span class="keyword">byte</span>[] bytes = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">20</span>];</span><br><span class="line">        <span class="keyword">while</span> ((len1 = is.read(bytes)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            bos.write(bytes,<span class="number">0</span>,len1);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(bos.toString());</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (fis != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                fis.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (os != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                os.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (socket != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                socket.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (bos != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                bos.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@org</span>.junit.Test</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">server</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ServerSocket serverSocket = <span class="keyword">null</span>;</span><br><span class="line">    Socket accept = <span class="keyword">null</span>;</span><br><span class="line">    InputStream gis = <span class="keyword">null</span>;</span><br><span class="line">    FileOutputStream fos = <span class="keyword">null</span>;</span><br><span class="line">    OutputStream os = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        serverSocket = <span class="keyword">new</span> ServerSocket(<span class="number">10086</span>);</span><br><span class="line">        accept = serverSocket.accept();</span><br><span class="line">        gis = accept.getInputStream();</span><br><span class="line">        fos = <span class="keyword">new</span> FileOutputStream(<span class="keyword">new</span> File(<span class="string">"copy.png"</span>));</span><br><span class="line">        <span class="keyword">int</span> len;</span><br><span class="line">        <span class="keyword">byte</span>[] buffer = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">while</span> ((len = gis.read(buffer)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            fos.write(buffer,<span class="number">0</span>,len);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"收到图片"</span>);</span><br><span class="line">        os = accept.getOutputStream();</span><br><span class="line">        os.write(<span class="string">"收到你发的图片了"</span>.getBytes());</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (fos != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                fos.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (gis != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                gis.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (accept != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                accept.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (serverSocket != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                serverSocket.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (os != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                os.close();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面是用客户端发送了一张图片，然后服务端接收后向客户端回应一条消息。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>计算机总线</title>
    <url>/2018/01/24/%E8%AE%A1%E7%AE%97%E6%9C%BA%E6%80%BB%E7%BA%BF/</url>
    <content><![CDATA[<p>本篇谈谈总线及内存地址空间。</p>
<a id="more"></a>

<h1 id="CPU对存储器（内存）的读写"><a href="#CPU对存储器（内存）的读写" class="headerlink" title="CPU对存储器（内存）的读写"></a>CPU对存储器（内存）的读写</h1><p>存储器被划分成多个存储单元，存储单元从零开始顺序编号。这些编号可以看作存储单元在存储器中的地址。就像一条街，每个房子都有门牌号码。<br>C P U 要从内存中读数据，首先要指定存储单元的地址。也就是说它要先确定它要读取哪一个存储单元中的数据。就像在一条街上找人，先要确定他住在哪个房子里。<br>另外，在一台微机中，不只有存储器这一种器件。CPU 在读写数据时还要指明，它要对哪一个器件进行操作，进行哪种操作，是从中读出数据，还是向里面写入数据。  </p>
<p>可见，CPU 要想进行数据的读写，必须和外部器件（标准的说法是芯片）进行下面3 类信息的交互。  </p>
<ul>
<li>存储单元的地址（地址信息）;</li>
<li>器件的选择，读或写的命令（控制信息）;</li>
<li>读或写的数据（数据信息）。</li>
</ul>
<p>那么CPU 是通过什么将地址、数据和控制信息传到存储器芯片中的呢？电子计算机能处理、传输的信息都是电信号，电信号当然要用导线传送。在计算机中专门有连接CPU和其他芯片的导线，通常称为总线。总线从物理上来讲，就是一根根导线的集合。根据传送信息的不同，总线从逻辑上又分为3 类，地址总线、控制总线和数据总线。</p>
<p>CPU从 3 号单元中读取数据的过程如下。</p>
<p><img src="/2018/01/24/%E8%AE%A1%E7%AE%97%E6%9C%BA%E6%80%BB%E7%BA%BF/%E8%AF%BB%E5%8F%96.png" alt="读取"></p>
<p>（1） CPU 通过地址线将地址信息3 发出。</p>
<p>（2） CPU 通过控制线发出内存读命令，选中存储器芯片，并通知它，将要从中读取数据。  </p>
<p>（3） 存储器将3 号单元中的数据8通过数据线送入CPU。</p>
<p>写操作与读操作的步骤相似。如向3 号单元写入数据26。</p>
<p>（1）CPU通过地址线将地址信息3发出。</p>
<p>（2）CPU通过控制线发出内存写命令，选中存储器芯片，并通知它，要向其中写入<br>数据。</p>
<p>（3）CPU通过数据线将数据26送入内存的3 号单元中。<br>从上面我们知道了 CPU是如何进行数据读写的。可是，如何命令计算机进行数据的读写呢？<br>要让一个计算机或微处理器工作，应向它输入能够驱动它进行工作的电平信息(机器码)。<br>对于8086CPU，下面的机器码，能够完成从3 号单元读数据。<br>​    机器码： 101000010000001100000000<br>​    含义： 从 3 号单元读取数据送入寄存器AX<br>CPU接收这条机器码后将完成我们上面所述的读写工作。机器码难于记忆，用汇编指令来表示，情况如下。<br>​    机器码： 10100001 00000011 00000000<br>​    对应的汇编指令：MOV AX,[3]<br>​    含义： 传送3 号单元的内容入AX</p>
<h1 id="地址总线"><a href="#地址总线" class="headerlink" title="地址总线"></a>地址总线</h1><p>地址总线是用来指定存储器单元的，它能传送多少个不同的信息，就决定了CPU的寻址能力。假定CPU有10根地址总线，那么可以表示0~1023共1024个状态，及1024个不同的地址，这就限制了你的存储器（主存）的大小。</p>
<p>下图是指定第11个内存单元时的情况:</p>
<p><img src="/2018/01/24/%E8%AE%A1%E7%AE%97%E6%9C%BA%E6%80%BB%E7%BA%BF/11.png" alt="11"></p>
<p>11的二进制是1011。</p>
<h1 id="数据总线"><a href="#数据总线" class="headerlink" title="数据总线"></a>数据总线</h1><p>CPU与内存或其他器件之间的数据传送是通过数据总线来进行的。数据总线的宽度决定了 CPU和外界的数据传送速度。8 根数据总线一次可传送一个8 位二进制数据（即一个字节）。16根数据总线一次可传送两个字节。<br>8088CPU的数据总线宽度为8，8086CPU的数据总线宽度为16。我们来分别看一下它们向内存中写入数据89D8H时，是如何通过数据总线传送数据的。下图展示了两种CPU数据总线上的数据传送情况。（H表示16进制，89D8的二进制为1000 1001 1101 1000，十进制是35288）</p>
<p><img src="/2018/01/24/%E8%AE%A1%E7%AE%97%E6%9C%BA%E6%80%BB%E7%BA%BF/8088.png" alt="8088"></p>
<p><img src="/2018/01/24/%E8%AE%A1%E7%AE%97%E6%9C%BA%E6%80%BB%E7%BA%BF/8086.png" alt="8086"></p>
<h1 id="控制总线"><a href="#控制总线" class="headerlink" title="控制总线"></a>控制总线</h1><p>CPU对外部器件的控制是通过控制总线来进行的。在这里控制总线是个总称，控制总线是一些不同控制线的集合。有多少根控制总线，就意味着CPU提供了对外部器件的多少种控制。所以，控制总线的宽度决定了 CPU对外部器件的控制能力。<br>前面所讲的内存读或写命令是由几根控制线综合发出的，其中有一根称为“读信号输出”的控制线负责由CPU向外传送读信号，CPU向该控制线上输出低电平表示将要读取数据；有一根称为“写信号输出”的控制线则负责传送写信号。</p>
<h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><h2 id="练习"><a href="#练习" class="headerlink" title="练习"></a>练习</h2><p>(1) 1 个 CPU的寻址能力为8KB ,那么它的地址总线的宽度为（13）。</p>
<p>​    <em>8KB=8192B,即8192个内存单元，2^13=8192.</em></p>
<p>(2) 1KB的存储器有（1024）个存储单元。存储单元的编号从（0-1023）。</p>
<p>​    <em>一个存储单元为1B</em></p>
<p>(3) 1KB的存储器可以存储（8192）个 bit, （1024）个 Byte。</p>
<p>(4) 1GB、1MB、 1KB 分别是（1073741842，1048576，1024）Byte。 </p>
<p>(5) 8080、8088、80286、80386的地址总线宽度分别为16根、20根、24根 、32根，则它们的寻址能力分别为：</p>
<p>​    <em>2^16/1024=64KB</em></p>
<p>​    <em>2^20/1024/1024=1MB</em></p>
<p>​    <em>2^24/1021/1024=16MB</em></p>
<p>​    <em>2^32/1024/1024/1024=4GB</em></p>
<p>(6) 8080、8088、8086、80286、80386的数据总线宽度分别为8 根、8 根、16根、16根、32根。则它们一次可以传送的数据为:</p>
<p>​    <em>1B,1B,2B,2B,4B</em></p>
<p>(7) 从内存中读取1024字节的数据，8086至少要读（512） 次，80386至少要读（256） 次。<br>(8) 在存储器中，数据和程序以（二进制）形式存放。</p>
<h1 id="内存地址空间概述"><a href="#内存地址空间概述" class="headerlink" title="内存地址空间概述"></a>内存地址空间概述</h1><p>一个CPU的地址总线宽度为10，那么可以寻找1024个内存单元，这1024个内存单元构成了CPU的内存地址空间。</p>
<h2 id="主板"><a href="#主板" class="headerlink" title="主板"></a>主板</h2><p>在每一台PC 机中，都有一个主板，主板上有核心器件和一些主要器件，这些器件通过总线(地址总线、数据总线、控制总线)相连。这些器件有CPU、存储器、外围芯片组、扩展插槽等。扩展插槽上一般插有RAM内存条和各类接口卡。（存疑，存储器就是内存啊，为什么扩展插槽的RAM内存条不算在里面。）</p>
<h2 id="接口卡"><a href="#接口卡" class="headerlink" title="接口卡"></a>接口卡</h2><p>计算机系统中，所有可用程序控制其工作的设备，必须受到CPU的控制。CPU对外部设备都不能直接控制，如显示器、音箱、打印机等。直接控制这些设备进行工作的是插在扩展插槽上的接口卡。扩展插槽通过总线和CPU相连，所以接口卡也通过总线同CPU相连。CPU可以直接控制这些接口卡，从而实现CPU对外设的间接控制。简单地讲，就是 CPU通过总线向接口卡发送命令，接口卡根据CPU的命令控制外设进行工作。</p>
<h2 id="各类存储器芯片"><a href="#各类存储器芯片" class="headerlink" title="各类存储器芯片"></a>各类存储器芯片</h2><p>一 台 PC机中，装有多个存储器芯片，这些存储器芯片从物理连接上看是独立的、不同的器件。从读写属性上看分为两类：随机存储器（RAM）和只读存储器（ROM）。随机存储器可读可写，但必须带电存储，关机后存储的内容丢失；只读存储器只能读取不能写入，关机后其中的内容不丢失。这些存储器从功能和连接上又可分为以下几类。</p>
<ul>
<li>随机存储器，用于存放供CPU使用的绝大部分程序和数据，主随机存储器一般由两个位置上的 RAM组成，装在主板上RAM和插在扩展插槽上的RAM。</li>
</ul>
<ul>
<li>装有 BIOS（Basic Input/Output System ,基本输入/输出系统）的 ROM，BIOS是由主板和各类接口卡（如显卡、网卡等）厂商提供的软件系统，可以通过它利用该硬件设备进行最基本的输入输出。在主板和某些接口卡上插有存储相应BIOS的 ROM。例如，主 板 上 的 ROM 中存储着主板的BIOS（通常称为系统BIOS）；显卡上的ROM中存储着显卡的BIOS；如果网卡上装有ROM ,那其中就可以存储网卡的BIOS。</li>
</ul>
<ul>
<li>接口卡上的RAM，某些接口卡需要对大批量输入、输出数据进行暂时存储，在其上装有RAM。最典型的是显示卡上的RAM, 一般称为显存。显示卡随时将显存中的数据向显示器上输出。换句话说，我们将需要显示的内容写入显存，就会出现在显示器上。</li>
</ul>
<h2 id="内存地址空间"><a href="#内存地址空间" class="headerlink" title="内存地址空间"></a>内存地址空间</h2><p>上述的那些存储器，在物理上是独立的器件，但是在以下两点上相同。</p>
<ul>
<li>都和CPU的总线相连。</li>
</ul>
<ul>
<li>CPU对它们进行读或写的时候都通过控制线发出内存读写命令。</li>
</ul>
<p>这也就是说，CPU在操控它们的时候，把它们都当作内存来对待，把它们总的看作一个由若干存储单元组成的逻辑存储器，这个逻辑存储器就是我们所说的内存地址空间。</p>
<p>下图展示了CPU将个类存储器看作一个整体的逻辑存储器的情况。</p>
<p><img src="/2018/01/24/%E8%AE%A1%E7%AE%97%E6%9C%BA%E6%80%BB%E7%BA%BF/%E5%86%85%E5%AD%98%E7%A9%BA%E9%97%B4.png" alt="内存空间"></p>
<p>在图中，所有的物理存储器被看作一个由若干存储单元组成的逻辑存储器，每个物理存储器在这个逻辑存储器中占有一个地址段，即一段地址空间。CPU在这段地址空间中读写数据，实际上就是在相对应的物理存储器中读写数据。</p>
<p>假设，图中的内存地址空间的地址段分配如下。<br>​    <em>地址0-7FFFH的 32KB空间为主随机存储器的地址空间；</em><br>​    <em>地址8000H〜9FFFH的 8KB空间为显存地址空间；</em><br>​    <em>地址AOOOH〜FFFFH的 24KB空间为各个ROM的地址空间。</em><br>这样，CPU向内存地址为1000H的内存单元中写入数据，这个数据就被写入主随机存储器中；CPU向内存地址为8000H的内存单元中写入数据，这个数据就被写入显存中，然后会被显卡输出到显示器上；CPU向内存地址为C000H的内存单元中写入数据的操作是没有结果的，C000H单元中的内容不会被改变，C000H单元实际上就是ROM存储器中的一个单元。<br>内存地址空间的大小受CPU地址总线宽度的限制。8086CPU的地址总线宽度为20,可 以 传 送 2^20个不同的地址信息(大小从0 至 2^20-1)。即可以定位22G个内存单元，则8086PC的内存地址空间大小为1MB。同理，80386CPU的地址总线宽度为3 2 ,则内存地址空间最大为4GB。</p>
<p>下篇写CPU的寄存器。</p>
<p>以上。</p>
]]></content>
      <tags>
        <tag>汇编</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL锁</title>
    <url>/2020/07/15/MySQL%E9%94%81/</url>
    <content><![CDATA[<p>MySQL锁.</p>
<a id="more"></a>

<blockquote>
<p>本文由 <a href="http://ksria.com/simpread/" target="_blank" rel="noopener">简悦 SimpRead</a> 转码， 原文地址 <a href="https://zhuanlan.zhihu.com/p/29150809" target="_blank" rel="noopener">zhuanlan.zhihu.com</a></p>
</blockquote>
<p>锁是计算机协调多个进程或线程并发访问某一资源的机制。锁保证数据并发访问的一致性、有效性；锁冲突也是影响数据库并发访问性能的一个重要因素。锁是 Mysql 在服务器层和存储引擎层的的并发控制。</p>
<p>加锁是消耗资源的，锁的各种操作，包括获得锁、检测锁是否是否已解除、释放锁等。</p>
<h2 id="锁机制"><a href="#锁机制" class="headerlink" title="锁机制"></a><strong>锁机制</strong></h2><h2 id="共享锁与排他锁"><a href="#共享锁与排他锁" class="headerlink" title="共享锁与排他锁"></a><strong>共享锁与排他锁</strong></h2><ul>
<li>共享锁（读锁）：其他事务可以读，但不能写。</li>
<li>排他锁（写锁） ：其他事务不能读取，也不能写。</li>
</ul>
<h2 id="粒度锁"><a href="#粒度锁" class="headerlink" title="粒度锁"></a><strong>粒度锁</strong></h2><p>MySQL 不同的存储引擎支持不同的锁机制，所有的存储引擎都以自己的方式显现了锁机制，服务器层完全不了解存储引擎中的锁实现：</p>
<ul>
<li>MyISAM 和 MEMORY 存储引擎采用的是表级锁（table-level locking）</li>
<li>BDB 存储引擎采用的是页面锁（page-level locking），但也支持表级锁</li>
<li>InnoDB 存储引擎既支持行级锁（row-level locking），也支持表级锁，但<strong>默认情况下是采用行级锁</strong>。</li>
</ul>
<p>默认情况下，表锁和行锁都是自动获得的， 不需要额外的命令。</p>
<p>但是在有的情况下， 用户需要明确地进行锁表或者进行事务的控制， 以便确保整个事务的完整性，这样就需要使用事务控制和锁定语句来完成。</p>
<h2 id="不同粒度锁的比较："><a href="#不同粒度锁的比较：" class="headerlink" title="不同粒度锁的比较："></a><strong>不同粒度锁的比较：</strong></h2><ul>
<li><p>表级锁：开销小，加锁快；不会出现死锁；锁定粒度大，发生锁冲突的概率最高，并发度最低。</p>
</li>
<li><p>这些存储引擎通过总是一次性同时获取所有需要的锁以及总是按相同的顺序获取表锁来避免死锁。</p>
</li>
<li><p>表级锁更适合于以查询为主，并发用户少，只有少量按索引条件更新数据的应用，如 Web 应用</p>
</li>
<li><p>行级锁：开销大，加锁慢；会出现死锁；锁定粒度最小，发生锁冲突的概率最低，并发度也最高。</p>
</li>
<li><p>最大程度的支持并发，同时也带来了最大的锁开销。</p>
</li>
<li><p>在 InnoDB 中，除单个 SQL 组成的事务外，<br>锁是逐步获得的，这就决定了在 InnoDB 中发生死锁是可能的。</p>
</li>
<li><p>行级锁只在存储引擎层实现，而 Mysql 服务器层没有实现。 行级锁更适合于有大量按索引条件并发更新少量不同数据，同时又有并发查询的应用，如一些在线事务处理（OLTP）系统</p>
</li>
<li><p>页面锁：开销和加锁时间界于表锁和行锁之间；会出现死锁；锁定粒度界于表锁和行锁之间，并发度一般。</p>
</li>
</ul>
<h2 id="MyISAM-表锁"><a href="#MyISAM-表锁" class="headerlink" title="MyISAM 表锁"></a><strong>MyISAM 表锁</strong></h2><h2 id="MyISAM-表级锁模式："><a href="#MyISAM-表级锁模式：" class="headerlink" title="MyISAM 表级锁模式："></a><strong>MyISAM 表级锁模式：</strong></h2><ul>
<li>表共享读锁 （Table Read Lock）：不会阻塞其他用户对同一表的读请求，但会阻塞对同一表的写请求；</li>
<li>表独占写锁 （Table Write Lock）：会阻塞其他用户对同一表的读和写操作；</li>
</ul>
<p>MyISAM 表的读操作与写操作之间，以及写操作之间是串行的。当一个线程获得对一个表的写锁后， 只有持有锁的线程可以对表进行更新操作。 其他线程的读、 写操作都会等待，直到锁被释放为止。</p>
<p>默认情况下，<strong>写锁比读锁具有更高的优先级</strong>：当一个锁释放时，这个锁会优先给写锁队列中等候的获取锁请求，然后再给读锁队列中等候的获取锁请求。 （This ensures that updates to a table are not “starved” even when there is heavy SELECT activity for the table. However, if there are many updates for a table, SELECT statements wait until there are no more updates.）。</p>
<p>这也正是 MyISAM 表不太适合于有大量更新操作和查询操作应用的原因，因为，大量的更新操作会造成查询操作很难获得读锁，从而可能永远阻塞。同时，一些需要长时间运行的查询操作，也会使写线程 “饿死” ，应用中应尽量避免出现长时间运行的查询操作（在可能的情况下可以通过使用中间表等措施对 SQL 语句做一定的 “分解” ，使每一步查询都能在较短时间完成，从而减少锁冲突。如果复杂查询不可避免，应尽量安排在数据库空闲时段执行，比如一些定期统计可以安排在夜间执行）。</p>
<p>可以<strong>设置改变读锁和写锁的优先级</strong>：</p>
<ul>
<li>通过指定启动参数 low-priority-updates，使 MyISAM 引擎默认给予读请求以优先的权利。</li>
<li>通过执行命令 SET LOW_PRIORITY_UPDATES=1，使该连接发出的更新请求优先级降低。</li>
<li>通过指定 INSERT、UPDATE、DELETE 语句的 LOW_PRIORITY 属性，降低该语句的优先级。</li>
<li>给系统参数 max_write_lock_count 设置一个合适的值，当一个表的读锁达到这个值后，MySQL 就暂时将写请求的优先级降低，给读进程一定获得锁的机会。</li>
</ul>
<h2 id="MyISAM-加表锁方法："><a href="#MyISAM-加表锁方法：" class="headerlink" title="MyISAM 加表锁方法："></a><strong>MyISAM 加表锁方法：</strong></h2><p>MyISAM 在执行查询语句（SELECT）前，会自动给涉及的表加读锁，在执行更新操作<br>（UPDATE、DELETE、INSERT 等）前，会自动给涉及的表加写锁，这个过程并不需要用户干预，因此，用户一般不需要直接用 LOCK TABLE 命令给 MyISAM 表显式加锁。</p>
<p>在自动加锁的情况下，MyISAM 总是一次获得 SQL 语句所需要的全部锁，这也正是 MyISAM 表不会出现死锁（Deadlock Free）的原因。</p>
<p>MyISAM 存储引擎支持并发插入，以减少给定表的读和写操作之间的争用：</p>
<p>如果 MyISAM 表在数据文件中间没有空闲块，则行始终插入数据文件的末尾。 在这种情况下，你可以自由混合并发使用 MyISAM 表的 INSERT 和 SELECT 语句而不需要加锁——你可以在其他线程进行读操作的时候，同时将行插入到 MyISAM 表中。 文件中间的空闲块可能是从表格中间删除或更新的行而产生的。 如果文件中间有空闲快，则并发插入会被禁用，但是当所有空闲块都填充有新数据时，它又会自动重新启用。 要控制此行为，可以使用 MySQL 的 concurrent_insert 系统变量。</p>
<p>如果你使用 LOCK TABLES 显式获取表锁，则可以请求 READ LOCAL 锁而不是 READ 锁，以便在锁定表时，其他会话可以使用并发插入。</p>
<ul>
<li>当 concurrent_insert 设置为 0 时，不允许并发插入。</li>
<li>当 concurrent_insert 设置为 1 时，如果 MyISAM 表中没有空洞（即表的中间没有被删除的行），MyISAM 允许在一个线程读表的同时，另一个线程从表尾插入记录。这也是 MySQL 的默认设置。</li>
<li>当 concurrent_insert 设置为 2 时，无论 MyISAM 表中有没有空洞，都允许在表尾并发插入记录。</li>
</ul>
<h2 id="查询表级锁争用情况："><a href="#查询表级锁争用情况：" class="headerlink" title="查询表级锁争用情况："></a><strong>查询表级锁争用情况：</strong></h2><p>可以通过检查 table_locks_waited 和 table_locks_immediate 状态变量来分析系统上的表锁的争夺，如果 Table_locks_waited 的值比较高，则说明存在着较严重的表级锁争用情况：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; SHOW STATUS LIKE &#39;Table%&#39;;</span><br><span class="line">+-----------------------+---------+</span><br><span class="line">| Variable_name | Value |</span><br><span class="line">+-----------------------+---------+</span><br><span class="line">| Table_locks_immediate | 1151552 |</span><br><span class="line">| Table_locks_waited | 15324 |</span><br><span class="line">+-----------------------+---------+</span><br></pre></td></tr></table></figure>

<h2 id="InnoDB-行级锁和表级锁"><a href="#InnoDB-行级锁和表级锁" class="headerlink" title="InnoDB 行级锁和表级锁"></a><strong>InnoDB 行级锁和表级锁</strong></h2><h2 id="InnoDB-锁模式："><a href="#InnoDB-锁模式：" class="headerlink" title="InnoDB 锁模式："></a><strong>InnoDB 锁模式：</strong></h2><p>InnoDB 实现了以下两种类型的<strong>行锁</strong>：</p>
<ul>
<li>共享锁（S）：允许一个事务去读一行，阻止其他事务获得相同数据集的排他锁。</li>
<li>排他锁（X）：允许获得排他锁的事务更新数据，阻止其他事务取得相同数据集的共享读锁和排他写锁。</li>
</ul>
<p>为了允许行锁和表锁共存，实现多粒度锁机制，InnoDB 还有两种内部使用的意向锁（Intention Locks），这两种意向锁都是<strong>表锁</strong>：</p>
<ul>
<li>意向共享锁（IS）：事务打算给数据行加行共享锁，事务在给一个数据行加共享锁前必须先取得该表的 IS 锁。</li>
<li>意向排他锁（IX）：事务打算给数据行加行排他锁，事务在给一个数据行加排他锁前必须先取得该表的 IX 锁。</li>
</ul>
<p><strong>锁模式的兼容情况：</strong></p>
<p><img src="https://pic3.zhimg.com/v2-37761612ead11ddc3762a4c20ddab3f3_b.jpg" alt>)<img src="https://pic3.zhimg.com/v2-37761612ead11ddc3762a4c20ddab3f3_r.jpg" alt></p>
<p>（如果一个事务请求的锁模式与当前的锁兼容， InnoDB 就将请求的锁授予该事务； 反之， 如果两者不兼容，该事务就要等待锁释放。）</p>
<h2 id="InnoDB-加锁方法："><a href="#InnoDB-加锁方法：" class="headerlink" title="InnoDB 加锁方法："></a><strong>InnoDB 加锁方法：</strong></h2><ul>
<li><p>意向锁是 InnoDB 自动加的， 不需用户干预。</p>
</li>
<li><p>对于 UPDATE、 DELETE 和 INSERT 语句， InnoDB<br>会自动给涉及数据集加排他锁（X)；</p>
</li>
<li><p>对于普通 SELECT 语句，InnoDB 不会加任何锁；<br>事务可以通过以下语句显式给记录集加共享锁或排他锁：</p>
</li>
<li><p>共享锁（S）：SELECT * FROM table_name WHERE … LOCK IN SHARE MODE。 其他 session 仍然可以查询记录，并也可以对该记录加 share mode 的共享锁。但是如果当前事务需要对该记录进行更新操作，则很有可能造成死锁。</p>
</li>
<li><p>排他锁（X)：SELECT * FROM table_name WHERE … FOR UPDATE。其他 session 可以查询该记录，但是不能对该记录加共享锁或排他锁，而是等待获得锁</p>
</li>
<li><p><strong>隐式锁定：</strong></p>
</li>
</ul>
<p>InnoDB 在事务执行过程中，使用两阶段锁协议：</p>
<p>随时都可以执行锁定，InnoDB 会根据隔离级别在需要的时候自动加锁；</p>
<p>锁只有在执行 commit 或者 rollback 的时候才会释放，并且所有的锁都是在<strong>同一时刻</strong>被释放。</p>
<ul>
<li><strong>显式锁定 ：</strong></li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">select ... lock in share mode &#x2F;&#x2F;共享锁 </span><br><span class="line">select ... for update &#x2F;&#x2F;排他锁</span><br></pre></td></tr></table></figure>

<p><strong>select for update：</strong></p>
<p>在执行这个 select 查询语句的时候，会将对应的索引访问条目进行上排他锁（X 锁），也就是说这个语句对应的锁就相当于 update 带来的效果。</p>
<p>select *** for update 的使用场景：为了让自己查到的数据确保是最新数据，并且查到后的数据只允许自己来修改的时候，需要用到 for update 子句。</p>
<p><strong>select lock in share mode ：</strong>in share mode 子句的作用就是将查找到的数据加上一个 share 锁，这个就是表示其他的事务只能对这些数据进行简单的 select 操作，并不能够进行 DML 操作。select *** lock in share mode 使用场景：为了确保自己查到的数据没有被其他的事务正在修改，也就是说确保查到的数据是最新的数据，并且不允许其他人来修改数据。但是自己不一定能够修改数据，因为有可能其他的事务也对这些数据 使用了 in share mode 的方式上了 S 锁。</p>
<p><strong>性能影响：</strong><br>select for update 语句，相当于一个 update 语句。在业务繁忙的情况下，如果事务没有及时的 commit 或者 rollback 可能会造成其他事务长时间的等待，从而影响数据库的并发使用效率。<br>select lock in share mode 语句是一个给查找的数据上一个共享锁（S 锁）的功能，它允许其他的事务也对该数据上 S 锁，但是不能够允许对该数据进行修改。如果不及时的 commit 或者 rollback 也可能会造成大量的事务等待。</p>
<p><strong>for update 和 lock in share mode 的区别：</strong></p>
<p>前一个上的是排他锁（X 锁），一旦一个事务获取了这个锁，其他的事务是没法在这些数据上执行 for update ；后一个是共享锁，多个事务可以同时的对相同数据执行 lock in share mode。</p>
<h2 id="InnoDB-行锁实现方式："><a href="#InnoDB-行锁实现方式：" class="headerlink" title="InnoDB 行锁实现方式："></a><strong>InnoDB 行锁实现方式：</strong></h2><ul>
<li>InnoDB 行锁是通过给索引上的索引项加锁来实现的，这一点 MySQL 与 Oracle 不同，后者是通过在数据块中对相应数据行加锁来实现的。InnoDB 这种行锁实现特点意味着：只有通过索引条件检索数据，InnoDB 才使用行级锁，否则，InnoDB 将使用表锁！</li>
<li>不论是使用主键索引、唯一索引或普通索引，InnoDB 都会使用行锁来对数据加锁。</li>
<li>只有执行计划真正使用了索引，才能使用行锁：即便在条件中使用了索引字段，但是否使用索引来检索数据是由 MySQL 通过判断不同执行计划的代价来决定的，如果 MySQL 认为全表扫描效率更高，比如对一些很小的表，它就不会使用索引，这种情况下 InnoDB 将使用表锁，而不是行锁。因此，在分析锁冲突时，<br>别忘了检查 SQL 的执行计划（可以通过 explain 检查 SQL 的执行计划），以确认是否真正使用了索引。（更多阅读：<a href="https://link.zhihu.com/?target=http%3A//mp.weixin.qq.com/s/h4B84UmzAUJ81iBY_FXNOg">MySQL 索引总结</a>）</li>
<li>由于 MySQL 的行锁是针对索引加的锁，不是针对记录加的锁，所以虽然多个 session 是访问不同行的记录， 但是如果是使用相同的索引键， 是会出现锁冲突的（后使用这些索引的 session 需要等待先使用索引的 session 释放锁后，才能获取锁）。 应用设计的时候要注意这一点。</li>
</ul>
<h2 id="InnoDB-的间隙锁："><a href="#InnoDB-的间隙锁：" class="headerlink" title="InnoDB 的间隙锁："></a><strong>InnoDB 的间隙锁：</strong></h2><p>当我们用范围条件而不是相等条件检索数据，并请求共享或排他锁时，InnoDB 会给符合条件的已有数据记录的索引项加锁；对于键值在条件范围内但并不存在的记录，叫做 “间隙（GAP)”，InnoDB 也会对这个“间隙” 加锁，这种锁机制就是所谓的间隙锁（Next-Key 锁）。</p>
<p>很显然，在使用范围条件检索并锁定记录时，InnoDB 这种加锁机制会阻塞符合条件范围内键值的并发插入，这往往会造成严重的锁等待。因此，在实际应用开发中，尤其是并发插入比较多的应用，我们要尽量优化业务逻辑，尽量使用相等条件来访问更新数据，避免使用范围条件。</p>
<p><strong>InnoDB 使用间隙锁的目的：</strong></p>
<ol>
<li>防止幻读，以满足相关隔离级别的要求；</li>
<li>满足恢复和复制的需要：</li>
</ol>
<p>MySQL 通过 BINLOG 录入执行成功的 INSERT、UPDATE、DELETE 等更新数据的 SQL 语句，并由此实现 MySQL 数据库的恢复和主从复制。MySQL 的恢复机制（复制其实就是在 Slave Mysql 不断做基于 BINLOG 的恢复）有以下特点：</p>
<p>一是 MySQL 的恢复是 SQL 语句级的，也就是重新执行 BINLOG 中的 SQL 语句。</p>
<p>二是 MySQL 的 Binlog 是按照事务提交的先后顺序记录的， 恢复也是按这个顺序进行的。</p>
<p>由此可见，MySQL 的恢复机制要求：在一个事务未提交前，其他并发事务不能插入满足其锁定条件的任何记录，也就是不允许出现幻读。</p>
<h2 id="InnoDB-在不同隔离级别下的一致性读及锁的差异："><a href="#InnoDB-在不同隔离级别下的一致性读及锁的差异：" class="headerlink" title="InnoDB 在不同隔离级别下的一致性读及锁的差异："></a><strong>InnoDB 在不同隔离级别下的一致性读及锁的差异：</strong></h2><p>锁和多版本数据（MVCC）是 InnoDB 实现一致性读和 ISO/ANSI SQL92 隔离级别的手段。</p>
<p>因此，在不同的隔离级别下，InnoDB 处理 SQL 时采用的一致性读策略和需要的锁是不同的：</p>
<p><img src="https://picb.zhimg.com/v2-c83c6459f8dc93a5f157fe1e3080088d_b.jpg" alt>)<img src="https://picb.zhimg.com/v2-c83c6459f8dc93a5f157fe1e3080088d_r.jpg" alt>)<img src="https://pic3.zhimg.com/v2-568951f4cdfeb9416042627a7b94c4ac_b.jpg" alt>)<img src="https://pic3.zhimg.com/v2-568951f4cdfeb9416042627a7b94c4ac_r.jpg" alt></p>
<p>对于许多 SQL，隔离级别越高，InnoDB 给记录集加的锁就越严格（尤其是使用范围条件的时候），产生锁冲突的可能性也就越高，从而对并发性事务处理性能的 影响也就越大。</p>
<p>因此， 我们在应用中， 应该尽量使用较低的隔离级别， 以减少锁争用的机率。实际上，通过优化事务逻辑，大部分应用使用 Read Commited 隔离级别就足够了。对于一些确实需要更高隔离级别的事务， 可以通过在程序中执行 SET SESSION TRANSACTION ISOLATION</p>
<p>LEVEL REPEATABLE READ 或 SET SESSION TRANSACTION ISOLATION LEVEL SERIALIZABLE 动态改变隔离级别的方式满足需求。</p>
<h2 id="获取-InnoDB-行锁争用情况："><a href="#获取-InnoDB-行锁争用情况：" class="headerlink" title="获取 InnoDB 行锁争用情况："></a><strong>获取 InnoDB 行锁争用情况：</strong></h2><p>可以通过检查 InnoDB_row_lock 状态变量来分析系统上的行锁的争夺情况：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; show status like &#39;innodb_row_lock%&#39;; </span><br><span class="line">+-------------------------------+-------+ </span><br><span class="line">| Variable_name | Value | </span><br><span class="line">+-------------------------------+-------+ </span><br><span class="line">| InnoDB_row_lock_current_waits | 0 | </span><br><span class="line">| InnoDB_row_lock_time | 0 | </span><br><span class="line">| InnoDB_row_lock_time_avg | 0 | </span><br><span class="line">| InnoDB_row_lock_time_max | 0 | </span><br><span class="line">| InnoDB_row_lock_waits | 0 | </span><br><span class="line">+-------------------------------+-------+ </span><br><span class="line">5 rows in set (0.01 sec)</span><br></pre></td></tr></table></figure>

<h2 id="LOCK-TABLES-和-UNLOCK-TABLES"><a href="#LOCK-TABLES-和-UNLOCK-TABLES" class="headerlink" title="LOCK TABLES 和 UNLOCK TABLES"></a><strong>LOCK TABLES 和 UNLOCK TABLES</strong></h2><p>Mysql 也支持 lock tables 和 unlock tables，这都是在服务器层（MySQL Server 层）实现的，和存储引擎无关，它们有自己的用途，并不能替代事务处理。 （除了禁用了 autocommint 后可以使用，其他情况不建议使用）：</p>
<ul>
<li>LOCK TABLES 可以锁定用于当前线程的表。如果表被其他线程锁定，则当前线程会等待，直到可以获取所有锁定为止。</li>
<li>UNLOCK TABLES 可以释放当前线程获得的任何锁定。当前线程执行另一个 LOCK TABLES 时，<br>或当与服务器的连接被关闭时，所有由当前线程锁定的表被隐含地解锁</li>
</ul>
<h2 id="LOCK-TABLES-语法："><a href="#LOCK-TABLES-语法：" class="headerlink" title="LOCK TABLES 语法："></a><strong>LOCK TABLES 语法：</strong></h2><ul>
<li>在用 LOCK TABLES 对 InnoDB 表加锁时要注意，要将 AUTOCOMMIT 设为 0，否则 MySQL 不会给表加锁；</li>
<li>事务结束前，不要用 UNLOCK TABLES 释放表锁，因为 UNLOCK TABLES 会隐含地提交事务；</li>
<li>COMMIT 或 ROLLBACK 并不能释放用 LOCK TABLES 加的表级锁，必须用 UNLOCK TABLES 释放表锁。</li>
</ul>
<p>正确的方式见如下语句：<br>例如，如果需要写表 t1 并从表 t 读，可以按如下做：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">SET AUTOCOMMIT&#x3D;0; </span><br><span class="line">LOCK TABLES t1 WRITE, t2 READ, ...; </span><br><span class="line">[do something with tables t1 and t2 here]; </span><br><span class="line">COMMIT; </span><br><span class="line">UNLOCK TABLES;</span><br></pre></td></tr></table></figure>

<h2 id="使用-LOCK-TABLES-的场景："><a href="#使用-LOCK-TABLES-的场景：" class="headerlink" title="使用 LOCK TABLES 的场景："></a><strong>使用 LOCK TABLES 的场景：</strong></h2><p>给表显示加表级锁（InnoDB 表和 MyISAM 都可以），一般是为了在一定程度模拟事务操作，实现对某一时间点多个表的一致性读取。（与 MyISAM 默认的表锁行为类似）</p>
<p>在用 LOCK TABLES 给表显式加表锁时，必须同时取得所有涉及到表的锁，并且 MySQL 不支持锁升级。也就是说，在执行 LOCK TABLES 后，只能访问显式加锁的这些表，不能访问未加锁的表；同时，如果加的是读锁，那么只能执行查询操作，而不能执行更新操作。</p>
<p>其实，在 MyISAM 自动加锁（表锁）的情况下也大致如此，MyISAM 总是一次获得 SQL 语句所需要的全部锁，这也正是 MyISAM 表不会出现死锁（Deadlock Free）的原因。</p>
<p>例如，有一个订单表 orders，其中记录有各订单的总金额 total，同时还有一个 订单明细表 order_detail，其中记录有各订单每一产品的金额小计 subtotal，假设我们需要检 查这两个表的金额合计是否相符，可能就需要执行如下两条 SQL：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Select sum(total) from orders; </span><br><span class="line">Select sum(subtotal) from order_detail;</span><br></pre></td></tr></table></figure>

<p>这时，如果不先给两个表加锁，就可能产生错误的结果，因为第一条语句执行过程中，<br>order_detail 表可能已经发生了改变。因此，正确的方法应该是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Lock tables orders read local, order_detail read local; </span><br><span class="line">Select sum(total) from orders; </span><br><span class="line">Select sum(subtotal) from order_detail; </span><br><span class="line">Unlock tables;</span><br></pre></td></tr></table></figure>

<p>（在 LOCK TABLES 时加了 “local” 选项，其作用就是允许当你持有表的读锁时，其他用户可以在满足 MyISAM 表并发插入条件的情况下，在表尾并发插入记录（MyISAM 存储引擎支持“并发插入”））</p>
<h2 id="死锁（Deadlock-Free）"><a href="#死锁（Deadlock-Free）" class="headerlink" title="死锁（Deadlock Free）"></a><strong>死锁（Deadlock Free）</strong></h2><ul>
<li><p><strong>死锁产生：</strong></p>
</li>
<li><p>死锁是指两个或多个事务在同一资源上相互占用，并请求锁定对方占用的资源，从而导致恶性循环。</p>
</li>
<li><p>当事务试图以不同的顺序锁定资源时，就可能产生死锁。多个事务同时锁定同一个资源时也可能会产生死锁。</p>
</li>
<li><p>锁的行为和顺序和存储引擎相关。以同样的顺序执行语句，有些存储引擎会产生死锁有些不会——死锁有双重原因：真正的数据冲突；存储引擎的实现方式。</p>
</li>
<li><p><strong>检测死锁：</strong>数据库系统实现了各种死锁检测和死锁超时的机制。InnoDB 存储引擎能检测到死锁的循环依赖并立即返回一个错误。</p>
</li>
<li><p><strong>死锁恢复：</strong>死锁发生以后，只有部分或完全回滚其中一个事务，才能打破死锁，InnoDB 目前处理死锁的方法是，将持有最少行级排他锁的事务进行回滚。所以事务型应用程序在设计时必须考虑如何处理死锁，多数情况下只需要重新执行因死锁回滚的事务即可。</p>
</li>
<li><p><strong>外部锁的死锁检测：</strong>发生死锁后，InnoDB 一般都能自动检测到，并使一个事务释放锁并回退，另一个事务获得锁，继续完成事务。但在涉及外部锁，或涉及表锁的情况下，InnoDB 并不能完全自动检测到死锁， 这需要通过设置锁等待超时参数 innodb_lock_wait_timeout 来解决</p>
</li>
<li><p><strong>死锁影响性能：</strong>死锁会影响性能而不是会产生严重错误，因为 InnoDB 会自动检测死锁状况并回滚其中一个受影响的事务。在高并发系统上，当许多线程等待同一个锁时，死锁检测可能导致速度变慢。 有时当发生死锁时，禁用死锁检测（使用 innodb_deadlock_detect 配置选项）可能会更有效，这时可以依赖 innodb_lock_wait_timeout 设置进行事务回滚。</p>
</li>
</ul>
<h2 id="MyISAM-避免死锁："><a href="#MyISAM-避免死锁：" class="headerlink" title="MyISAM 避免死锁："></a><strong>MyISAM 避免死锁：</strong></h2><ul>
<li>在自动加锁的情况下，MyISAM 总是一次获得 SQL 语句所需要的全部锁，所以 MyISAM 表不会出现死锁。</li>
</ul>
<h2 id="InnoDB-避免死锁："><a href="#InnoDB-避免死锁：" class="headerlink" title="InnoDB 避免死锁："></a><strong>InnoDB 避免死锁：</strong></h2><ul>
<li>为了在单个 InnoDB 表上执行多个并发写入操作时避免死锁，可以在事务开始时通过为预期要修改的每个元祖（行）使用 SELECT … FOR UPDATE 语句来获取必要的锁，即使这些行的更改语句是在之后才执行的。</li>
<li>在事务中，如果要更新记录，应该直接申请足够级别的锁，即排他锁，而不应先申请共享锁、更新时再申请排他锁，因为这时候当用户再申请排他锁时，其他事务可能又已经获得了相同记录的共享锁，从而造成锁冲突，甚至死锁</li>
<li>如果事务需要修改或锁定多个表，则应在每个事务中以相同的顺序使用加锁语句。 在应用中，如果不同的程序会并发存取多个表，应尽量约定以相同的顺序来访问表，这样可以大大降低产生死锁的机会</li>
<li>通过 SELECT … LOCK IN SHARE MODE 获取行的读锁后，如果当前事务再需要对该记录进行更新操作，则很有可能造成死锁。</li>
<li>改变事务隔离级别</li>
</ul>
<p>如果出现死锁，可以用 SHOW INNODB STATUS 命令来确定最后一个死锁产生的原因。返回结果中包括死锁相关事务的详细信息，如引发死锁的 SQL 语句，事务已经获得的锁，正在等待什么锁，以及被回滚的事务等。据此可以分析死锁产生的原因和改进措施。</p>
<h2 id="一些优化锁性能的建议"><a href="#一些优化锁性能的建议" class="headerlink" title="一些优化锁性能的建议"></a><strong>一些优化锁性能的建议</strong></h2><ul>
<li>尽量使用较低的隔离级别；</li>
<li>精心设计索引， 并尽量使用索引访问数据， 使加锁更精确， 从而减少锁冲突的机会</li>
<li>选择合理的事务大小，小事务发生锁冲突的几率也更小</li>
<li>给记录集显示加锁时，最好一次性请求足够级别的锁。比如要修改数据的话，最好直接申请排他锁，而不是先申请共享锁，修改时再请求排他锁，这样容易产生死锁</li>
<li>不同的程序访问一组表时，应尽量约定以相同的顺序访问各表，对一个表而言，尽可能以固定的顺序存取表中的行。这样可以大大减少死锁的机会</li>
<li>尽量用相等条件访问数据，这样可以避免间隙锁对并发插入的影响</li>
<li>不要申请超过实际需要的锁级别</li>
<li>除非必须，查询时不要显示加锁。 MySQL 的 MVCC 可以实现事务中的查询不用加锁，优化事务性能；MVCC 只在 COMMITTED READ（读提交）和 REPEATABLE READ（可重复读）两种隔离级别下工作</li>
<li>对于一些特定的事务，可以使用表锁来提高处理速度或减少死锁的可能</li>
</ul>
<h2 id="乐观锁、悲观锁"><a href="#乐观锁、悲观锁" class="headerlink" title="乐观锁、悲观锁"></a><strong>乐观锁、悲观锁</strong></h2><ul>
<li><strong>乐观锁 (Optimistic Lock)</strong>：假设不会发生并发冲突，只在提交操作时检查是否违反数据完整性。 乐观锁不能解决脏读的问题。</li>
</ul>
<p>乐观锁, 顾名思义，就是很乐观，每次去拿数据的时候都认为别人不会修改，所以不会上锁，但是在更新的时候会判断一下在此期间别人有没有去更新这个数据，可以使用版本号等机制。乐观锁适用于多读的应用类型，这样可以提高吞吐量，像数据库如果提供类似于 write_condition 机制的其实都是提供的乐观锁。</p>
<ul>
<li><strong>悲观锁 (Pessimistic Lock)</strong>：假定会发生并发冲突，屏蔽一切可能违反数据完整性的操作。</li>
</ul>
<p>悲观锁，顾名思义，就是很悲观，每次去拿数据的时候都认为别人会修改，所以每次在拿数据的时候都会上锁，这样别人想拿这个数据就会 block 直到它拿到锁。传统的关系型数据库里边就用到了很多这种锁机制，比如行锁，表锁等，读锁，写锁等，都是在做操作之前先上锁。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">参考：</span><br><span class="line">《高性能MySQL》</span><br><span class="line">《深入浅出MySQL》</span><br><span class="line">http:&#x2F;&#x2F;www.cnblogs.com&#x2F;liushuiwuqing&#x2F;p&#x2F;3966898.html  </span><br><span class="line">https:&#x2F;&#x2F;dev.mysql.com&#x2F;doc&#x2F;refman&#x2F;5.7&#x2F;en&#x2F;internal-locking.html </span><br><span class="line">http:&#x2F;&#x2F;www.cnblogs.com&#x2F;0201zcr&#x2F;p&#x2F;4782283.html</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>集合（复习6）</title>
    <url>/2020/05/29/%E9%9B%86%E5%90%88%EF%BC%88%E5%A4%8D%E4%B9%A06%EF%BC%89/</url>
    <content><![CDATA[<p>复习集合.</p>
<p>Java集合分为Collection接口和Map接口两个体系。Collection又分为List接口和Set接口，List元素有序，可重复。Set元素无序，不可重复。Map则是键值对。</p>
<p>List：ArrayList,LinkedList,Vector</p>
<p>Set:HashSet,LinkedHashSet,TreeSet</p>
<p>Map:HashMap,LinkedHashMap,TreeMap,HashTable,Properties</p>
<h1 id="Collection"><a href="#Collection" class="headerlink" title="Collection"></a>Collection</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">Test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Collection coll=<span class="keyword">new</span> ArrayList();</span><br><span class="line">        <span class="comment">//add(E e)</span></span><br><span class="line">        coll.add(<span class="number">123</span>);</span><br><span class="line">        coll.add(<span class="string">"hell"</span>);</span><br><span class="line">        <span class="comment">//addAll()，把某集合全部加进来</span></span><br><span class="line">        <span class="comment">//isEmpty(),判断是否为空</span></span><br><span class="line">        <span class="comment">//clear()，清空集合元素</span></span><br><span class="line">        <span class="comment">//contains()，判断是否包含某个对象（用内容判断，即调用equals判断，若是没有重写equals就不行）</span></span><br><span class="line">        <span class="comment">//containsAll(Collection coll),判断coll的数据是否都被包含在调用者的集合中</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//remove(Object o),移除参数指明的元素，因为要判断是否存在，故也要重写equals</span></span><br><span class="line">        <span class="comment">//removeAll(coll),移除所有,求差集</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//retainAll(coll)，求交集</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//hashCode()返回哈希值</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//toArray()，转换为数组</span></span><br><span class="line">        <span class="comment">//asList()，数组转为List</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//iterator(),返回一个iterator接口的实例，用于遍历集合（Collection）</span></span><br><span class="line">        Iterator iterator = coll.iterator();<span class="comment">//集合对象每次调用iterator方法都会得到一个全新的iterator对象</span></span><br><span class="line">        System.out.println(iterator.next());</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">            Object obj = iterator.next();</span><br><span class="line">            <span class="keyword">if</span> (<span class="string">"Tom"</span>.equals(obj)) &#123;</span><br><span class="line">                iterator.remove();<span class="comment">//删除，如果还未调用next，或者上一次调用next方法后已经用过remove方法紧接着继续用，都会出异常</span></span><br><span class="line">                <span class="comment">//第一种是因为next指针还没指到第一个，第二种是因为remove之后，next指针还没向下移动就继续删</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (coll.iterator().hasNext()) &#123;<span class="comment">//必须要重新拿个iterator，因为上面遍历后，next指针不在开头了</span></span><br><span class="line">            <span class="comment">//指针下移，返回指针所指元素</span></span><br><span class="line">            System.out.println(iterator.next());</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="List"><a href="#List" class="headerlink" title="List"></a>List</h2><p>ArrayList作为List的主要实现类线程不安全，效率高，底层使用Object数组,Vector作为List的古老实现类，比List接口还要早，线程安全，效率低,底层也是用的Object数组。LinkedList底层使用双向链表实现，插入删除效率高。</p>
<p>ArrayList的源码：</p>
<p>jdk7中</p>
<p><code>ArrayList list=new ArrayList()</code>底层创建了一个长度为10的Object[]数组elementData。默认扩容是1.5倍。建议直接构造器指定好容量。jdk8底层Object数组 elementData初始化为{}，没有创建长度为10.</p>
<p>List中常用方法：</p>
<p>add(index,ele),在索引位置加入元素</p>
<p>addAll(index,Collection)</p>
<p>get(index)</p>
<p>indexOf(object o),返回o首次出现的位置</p>
<p>LastIndexOf(object o)</p>
<p>remove(index)</p>
<p>set(index,ele)</p>
<p>sublist(fromIndex,toIndex),返回指定位置的子集合</p>
<h2 id="Set"><a href="#Set" class="headerlink" title="Set"></a>Set</h2><p>HashSet是主要实现类，线程不安全，可以存null值，LinkedHashSet作为HashSet子类，又链表结构，看似是有序的。TreeSet底层用红黑树实现，可以按照添加对象的指定属性排序。</p>
<p>Set子接口没有定义新方法。</p>
<p>无序性：以HashSet为例，HashSet底层是HashMap，再底层其实还是数组但是不是紧挨着排列的，具体存储位置要看Hash值，依靠这个决定在数组中的位置。</p>
<p>不可重复：很简单，重复的元素哈希值也是一样的。所以要重写Hash方法。</p>
<p>添加过程，以HashSet为例：先算哈希值，用该哈希值算出位置，若该位置上空，则直接放入，若不为空，则先用哈希值判断，若不同则用链表添加，若同再用equals判断是否相等，若相等，则不放入，若不相等，则用链表放在该位置。</p>
<p>所以，在类中没有重写hashcode方法，只重写了equals方法时，两个内容一样的对象可以放到HashSet中，因为算出来的hash值不一样，位置也就不一样了，直接就放进去，没有调用equals方法。</p>
<p>LinkedHashSet是在HashSet原有基础上为每个元素添加了一对指针域，使得遍历可以按照添加顺序来。对于频繁遍历的操作，LinkedHashSet更加高效。</p>
<p>TreeSet只能添加同类的元素。而且可以按照添加的元素的属性进行排序。排序分为自然排序，即自定义类要实现Comparable接口。TreeSet的自然排序是用compareTo()方法，不是equals.而定制排序需要在TreeSet的构造器中传入一个Comparator。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">Test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Person p1 = <span class="keyword">new</span> Person(<span class="number">1001</span>, <span class="string">"aa"</span>);</span><br><span class="line">    Person p2 = <span class="keyword">new</span> Person(<span class="number">1002</span>, <span class="string">"bb"</span>);</span><br><span class="line"></span><br><span class="line">    HashSet set = <span class="keyword">new</span> HashSet();</span><br><span class="line">    set.add(p1);</span><br><span class="line">    set.add(p2);</span><br><span class="line">    System.out.println(set);<span class="comment">//[Person&#123;age=1002, name='bb'&#125;, Person&#123;age=1001, name='aa'&#125;]</span></span><br><span class="line"></span><br><span class="line">    p1.name = <span class="string">"cc"</span>;</span><br><span class="line">    set.remove(p1);</span><br><span class="line">    System.out.println(set);<span class="comment">//[Person&#123;age=1002, name='bb'&#125;, Person&#123;age=1001, name='cc'&#125;]</span></span><br><span class="line"></span><br><span class="line">    set.add(<span class="keyword">new</span> Person(<span class="number">1002</span>, <span class="string">"cc"</span>));</span><br><span class="line">    System.out.println(set);<span class="comment">//[Person&#123;age=1002, name='bb'&#125;, Person&#123;age=1002, name='cc'&#125;, Person&#123;age=1001, name='cc'&#125;]</span></span><br><span class="line"></span><br><span class="line">    set.add(<span class="keyword">new</span> Person(<span class="number">1001</span>, <span class="string">"aa"</span>));</span><br><span class="line">    System.out.println(set);<span class="comment">//[Person&#123;age=1002, name='bb'&#125;, Person&#123;age=1001, name='cc'&#125;, Person&#123;age=1001, name='cc'&#125;, Person&#123;age=1001, name='aa'&#125;]</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>remove方法会在HashSet集合中去找p1,找法就是计算哈希值，而此时p1的name被改变了，故哈希值也会改变，所以就找不到原来位置上的p1，因此remove没有删掉p1。</p>
<p>而后面再添加的(1001，“cc”)这个对象是使用这两个属性得到的地址值，故不会和原来的(1001,”cc”)重复，因为原来的位置是用(1001, “aa”)算的。</p>
<p>后来再添加的(1001, “aa”)依然能加进去。因为用哈希值算出的地址上现在已经有了(1001,”cc”)<em>【改名字得来的那个cc】</em>,但是这时候会用equals判断是否相同，显然是不同的，故会用链表的形式加进去。</p>
<h1 id="Map"><a href="#Map" class="headerlink" title="Map"></a>Map</h1><p>Map接口主要有（Hashtable类，子类Properties），（HashMap类，子类LinkedHashMap），（SortedMap接口，实现类TreeMap）。</p>
<p>HashMap是Map的主要实现类，效率高，线程不安全，而Hashtable是一个古老的实现类，线程安全，效率低，类似于Vector。HashMap是无序的，而TreeMap是有序的，另外LinkedHashMap用于遍历操作较多的时候。几个的关系和Set类似。HashMap可以添加null的key-value，而Hashtable不可以。</p>
<p>LinkedHashMap也是有一对指针，记录了添加顺序。</p>
<p>TreeMap使用key来排序，不考虑value。</p>
<p>HashMap底层在jdk7用的使数组加链表，jdk8又加入了红黑树。</p>
<p>Properties用来处理配置文件，key-value都是string。</p>
<p>Map添加的是entry，一个entry由一对key-value构成，无序，不可重复。key不可以重复，且无序。value可以重复，也是无序的。</p>
<p><img src="/2020/05/29/%E9%9B%86%E5%90%88%EF%BC%88%E5%A4%8D%E4%B9%A06%EF%BC%89/image-20200530102858149.png" alt="image-20200530102858149"></p>
<p>HashMap里面key的对象要重写hashcode和equals方法，保证不可重复。而value的对象只要重写下equals方法。</p>
<p>HashMap的原理：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">HaspMap map = <span class="keyword">new</span> HashMap();</span><br></pre></td></tr></table></figure>

<p>在jdk7中，在实例化后，底层创建了一个长度16的数组Entry[] table。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">map.put(key1,value1);<span class="comment">//已经添加过多次</span></span><br></pre></td></tr></table></figure>

<p>首先计算key1的hashcode，再计算出在entry中的存放位置，如果此位置为空，则key1-value1添加成功。如果不为空，意味着该位置有一个或多个数据以链表存储（key2-value2），先用哈希值判断是否相同，若不同则用链表添加，若哈希值相同，则再用key的equals方法判断，不相等就用value1替换value2。使得该位置的元素变为key2-vlue1（这种情况的key1和key2相同，但value不一定）。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">HashMap hashMap = <span class="keyword">new</span> HashMap();</span><br><span class="line">hashMap.put(<span class="string">"aa"</span>, <span class="number">12</span>);<span class="comment">//添加</span></span><br><span class="line">hashMap.put(<span class="string">"aa"</span>, <span class="number">22</span>);<span class="comment">//修改</span></span><br><span class="line">System.out.println(hashMap);</span><br></pre></td></tr></table></figure>

<p>HashMap的扩容默认是扩容两倍。</p>
<p>在jdk8中，类似于ArrayList，没有直接创建长度16的数组，并且数组名字改为了Node[]，首次调用put方法时才会创建长度16的数组。当数组某一个索引位置上以链表存在的元素个数超过8且当前数组长度超过64时，此位置上的链表改为红黑树存储。</p>
<p>DEFAULT_INITIAL_CAPACITY:默认容量16</p>
<p>DEFAULT_LOAD_FACTOR:默认加载因子0.75</p>
<p>threshold：扩容的临界值=16*0.75=12</p>
<p>TREEIFY_THRESHOLD:Bucket中链表长度大于该默认值8，转化为红黑树</p>
<p>MIN_TREEIFY_CAPACITY:树化改造的bucket的最小值，64，即超过该值才会树化。</p>
<p>遍历Map，没有给Map用的遍历器。</p>
<p>关键是获得key构成的Set集合，value构成的Collection，entry构成的set。这样每个都可以遍历了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">map.keySet()<span class="comment">//返回Map的Set集合</span></span><br><span class="line">map.values()<span class="comment">//返回Map的collection</span></span><br><span class="line">map.entry()<span class="comment">//返回map的entry对</span></span><br></pre></td></tr></table></figure>

<p>TreeMap的key必须是同一类的对象，因为要按照key排序，同样是自然排序（对象实现Comparable接口）和定制排序（TreeMap构造器传入Comparator）。 </p>
<p>Properties是用来处理配置文件的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Properties properties = <span class="keyword">new</span> Properties();</span><br><span class="line">FileInputStream fileInputStream = <span class="keyword">new</span> FileInputStream(<span class="string">"src/com/ylxianzhe/jdbc.properties"</span>);</span><br><span class="line">properties.load(fileInputStream);</span><br><span class="line">String name = properties.getProperty(<span class="string">"name"</span>);</span><br><span class="line">System.out.println(name);</span><br></pre></td></tr></table></figure>

<h1 id="Collections"><a href="#Collections" class="headerlink" title="Collections"></a>Collections</h1><p>Collections工具类不仅可以操作Collection，也可以操作Map。</p>
<p>reverse(List),对list反转</p>
<p>shuffle(list),使list随机排序</p>
<p>sort(list),根据元素的自然顺序排序</p>
<p>sort(list,Comparator),根据指定的comparator排序</p>
<p>swap(list,int,int),交换list中i和j处元素</p>
<p>Object max(Collection,comparator),根据指定的comparator返回最大值</p>
<p>Object max(Collection)返回最大值</p>
<p>Object minmax(Collection,comparator)根据指定的comparator返回最大值</p>
<p>Object min(Collection)返回最小值</p>
<p>上面一些涉及到最值的方法都意味着元素对象时可比较的，要实现Comparable接口</p>
<p>int Frequency(Collection,object),返回指定元素出现的次数</p>
<p>void copy(list dest,list src),把src复制到dest,注意要提前使得dest的size与src的size相等。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ArrayList dest = <span class="keyword">new</span> ArrayList(<span class="number">10</span>);<span class="comment">//错误写法，10指定的是底层数组的长度，而不是size，即元素个数</span></span><br><span class="line">List dest = Arrays.asList(<span class="keyword">new</span> Object[src.size()])<span class="comment">//创建src.size个对象，并转为List来占位</span></span><br></pre></td></tr></table></figure>

<p>最重要的是，几大类主要都是线程不安全的，安全的又不用了，所以Collections提供了一些线程安全的方法。</p>
<p>synchronizedXXX方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ArrayList list = <span class="keyword">new</span> ArrayList()；</span><br><span class="line">List list = Collections.synchronizedList(list);<span class="comment">//将list变为线程安全的</span></span><br></pre></td></tr></table></figure>

<h1 id="泛型"><a href="#泛型" class="headerlink" title="泛型"></a>泛型</h1><p>用&lt;&gt;来指明/标记集合里是什么类型的数据，避免全都当成Object类型。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ArrayList&lt;String&gt; strings = <span class="keyword">new</span> ArrayList&lt;&gt;();<span class="comment">//后面的可以省略</span></span><br></pre></td></tr></table></figure>

<p>自定义泛型</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Computer</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    String name;</span><br><span class="line">    <span class="keyword">int</span> age;</span><br><span class="line">    T object;<span class="comment">//T可以看作是一种占位符，或者说是宏，当使用时传入T的实际参数，指明了下面用到T的地方的类型</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Computer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Computer</span><span class="params">(String name, <span class="keyword">int</span> age, T object)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">        <span class="keyword">this</span>.object = object;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Computer&lt;String&gt; computer = <span class="keyword">new</span> Computer&lt;&gt;();</span><br><span class="line">        Computer computer1 = <span class="keyword">new</span> Computer();<span class="comment">//如果定义了泛型却不用，那么T代表的就是Object</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">subComputer</span> <span class="keyword">extends</span> <span class="title">Computer</span>&lt;<span class="title">String</span>&gt; </span>&#123;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果子类继承父类是写明了泛型参数，那么对于子类来说就不存在泛型了，类型已经确定。要想子类也有泛型，这样写：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">subComputer</span>&lt;<span class="title">T</span>&gt; <span class="keyword">extends</span> <span class="title">Computer</span>&lt;<span class="title">String</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用泛型可以在&lt;&gt;内写多个参数。</p>
<p>泛型类构造器上不需要写泛型，但是实例化时要写。</p>
<p>泛型不同的引用不能相互赋值。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ArrayList&lt;String&gt; list1=...;</span><br><span class="line">ArrayList&lt;Integer&gt; list2=...;</span><br><span class="line">list1=list2;<span class="comment">//错误</span></span><br></pre></td></tr></table></figure>

<p>在静态结构中不能使用<strong>泛型类</strong>，因为静态内容在类加载时加载，而此时泛型的具体类型还不确定，需要到new这个泛型类的对象时才确定泛型具体类型。</p>
<p>异常类不能使用泛型。</p>
<p>泛型方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">Computer</span><span class="params">(String name, <span class="keyword">int</span> age, T object)</span> </span>&#123;</span><br><span class="line">     <span class="keyword">this</span>.name = name;</span><br><span class="line">     <span class="keyword">this</span>.age = age;</span><br><span class="line">     <span class="keyword">this</span>.object = object;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这不是泛型方法！</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> &lt;E&gt; <span class="function">List&lt;E&gt; <span class="title">copy</span><span class="params">(E[] arr)</span> </span>&#123;</span><br><span class="line">    ArrayList&lt;E&gt; list = <span class="keyword">new</span> ArrayList&lt;E&gt;;</span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="keyword">return</span> list;</span><br><span class="line">&#125;</span><br><span class="line">Integer[] arr = <span class="keyword">new</span> Integer[]&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;;</span><br><span class="line">List&lt;Integer&gt; list = order.copy(arr);<span class="comment">//传入Integer类型数组，即表民E代表的是Integer，所以list是Integer类型。</span></span><br></pre></td></tr></table></figure>
<p>这种才是。另外，泛型方法可以是静态的。因为泛型方法的泛型和类没有任何关系，类本身是不是泛型的也没有关系。上面的那种不可以是因为必须要在实例化对象时才知道类型；这里可以是因为类加载不需要知道泛型方法的具体类型，直接调用方法就行了，是在调用这个静态方法时确定。</p>
<p>泛型的用处：举例，操作数据库时写一个泛型类DAO，里面有一些基本的操作，而对于具体的Bean，则继承这个泛型类并传入具体的泛型类型，这样就可以使得每个Bean有对应的DAO。</p>
<p>类A是类B的父类，G&lt;A&gt;不是G&lt;B&gt;的父类，即B的引用可以赋值给A的引用（多态），但是G&lt;B&gt;的引用不能给G&lt;A&gt;的引用。</p>
<p>反过来，类A是类B的父类，A&lt;G&gt;就是B&lt;G&gt;的父类。</p>
<p>通配符的使用:</p>
<p>通配符：?。G&lt;A&gt;和G&lt;B&gt;的公共父类是G&lt;?&gt;。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        List&lt;Object&gt; list1 = <span class="keyword">null</span>;</span><br><span class="line">        List&lt;String&gt; list2 = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">        List&lt;?&gt; list = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">        list = list1;</span><br><span class="line">        list = list2;</span><br><span class="line"></span><br><span class="line">        print(list1);</span><br><span class="line">        print(list2);</span><br><span class="line"></span><br><span class="line">        list2.add(<span class="string">"aa"</span>);<span class="comment">//可以添加</span></span><br><span class="line">        list2.add(<span class="string">"bb"</span>);</span><br><span class="line">        list.add(<span class="string">"aa"</span>);<span class="comment">//对于List&lt;?&gt;不可以向内部添加数据，除了null，因为?代表了所有可能，如果可以写的话，list里装的类型就可能不同意，泛型也没意义了</span></span><br><span class="line"></span><br><span class="line">        Object o = list.get(<span class="number">0</span>);<span class="comment">//读取是可以的</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">(List&lt;?&gt; list)</span> </span>&#123;</span><br><span class="line">        Iterator&lt;?&gt; iterator = list.iterator();</span><br><span class="line">        <span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">            Object next = iterator.next();</span><br><span class="line">            System.out.println(next);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>有限制的通配符：</p>
<p><code>&lt;? extends Person&gt;</code>表示小于等于：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;? extends Person&gt; list1=<span class="keyword">null</span>;</span><br><span class="line">List&lt;Student&gt; list3=<span class="keyword">null</span>;</span><br><span class="line">List&lt;Person&gt; list4=<span class="keyword">null</span>;</span><br><span class="line">List&lt;Object&gt; list5=<span class="keyword">null</span>;</span><br><span class="line">list1=list3;<span class="comment">//可以，list3的泛型比list1的小，Student是Person的子类</span></span><br><span class="line">list1=list4;<span class="comment">//可以</span></span><br><span class="line">list1=list5;<span class="comment">//不可以</span></span><br></pre></td></tr></table></figure>

<p><code>&lt;? super Person&gt;</code>表示大于等于。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;? extends Person&gt; list1=<span class="keyword">null</span>;</span><br><span class="line">List&lt;? <span class="keyword">super</span> Person&gt; list2=<span class="keyword">null</span>;</span><br><span class="line">List&lt;Student&gt; list3=<span class="keyword">new</span> ArrayList&lt;Student&gt;();</span><br><span class="line">List&lt;Person&gt; list4=<span class="keyword">new</span> ArrayList&lt;Person&gt;();</span><br><span class="line">List&lt;Object&gt; list5=<span class="keyword">new</span> ArrayList&lt;Object&gt;();</span><br><span class="line"></span><br><span class="line">list1=list3;</span><br><span class="line">Person p=list1.get(<span class="number">0</span>);<span class="comment">//最小可以写到Person，List1里能装的是小于等于Person的，如果P的类型比Person还小，那就有可能list1里装的实际比p大，引起错误</span></span><br><span class="line"></span><br><span class="line">list2=list4;</span><br><span class="line">Object obj=list2.get(<span class="number">0</span>);<span class="comment">//只能写object，因为list2里的数据都是大于等于Person的，不拿object装不住</span></span><br><span class="line"></span><br><span class="line">list1.add(<span class="keyword">new</span> Student());<span class="comment">//不可以，list1代表的是能装小于等于Person的，若是代表的是Student的子类，此时又放进去一个父类显然不行</span></span><br><span class="line"></span><br><span class="line">list2.add(<span class="keyword">new</span> Student());<span class="comment">//可以，list2最小是装Person的，现在往里装Person的子类显然可以，比Person大的父类就不可以。</span></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>输入输出</title>
    <url>/2018/07/28/%E8%BE%93%E5%85%A5%E8%BE%93%E5%87%BA/</url>
    <content><![CDATA[<p>&emsp;Java笔记11篇原来是写java的输入输出的,但是写着写着发现太乱,没有体系,实在没法理解,所以另写一篇作为总结与复习.</p>
<a id="more"></a>

<h1 id="File"><a href="#File" class="headerlink" title="File"></a>File</h1><h2 id="常用方法"><a href="#常用方法" class="headerlink" title="常用方法"></a>常用方法</h2><p>先复习下File相关的内容,File代表文件和文件夹.下面是一些常用方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestFile</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"d:/LOLFolder/LOL.exe"</span>);</span><br><span class="line">        System.out.println(<span class="string">"当前文件是："</span> +f);</span><br><span class="line">        <span class="comment">//文件是否存在</span></span><br><span class="line">        System.out.println(<span class="string">"判断是否存在："</span>+f.exists());</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//是否是文件夹</span></span><br><span class="line">        System.out.println(<span class="string">"判断是否是文件夹："</span>+f.isDirectory());</span><br><span class="line">          </span><br><span class="line">        <span class="comment">//是否是文件（非文件夹）</span></span><br><span class="line">        System.out.println(<span class="string">"判断是否是文件："</span>+f.isFile());</span><br><span class="line">          </span><br><span class="line">        <span class="comment">//文件长度</span></span><br><span class="line">        System.out.println(<span class="string">"获取文件的长度："</span>+f.length());</span><br><span class="line">          </span><br><span class="line">        <span class="comment">//文件最后修改时间</span></span><br><span class="line">        <span class="keyword">long</span> time = f.lastModified();</span><br><span class="line">        Date d = <span class="keyword">new</span> Date(time);</span><br><span class="line">        System.out.println(<span class="string">"获取文件的最后修改时间："</span>+d);</span><br><span class="line">        <span class="comment">//设置文件修改时间为1970.1.1 08:00:00</span></span><br><span class="line">        f.setLastModified(<span class="number">0</span>);</span><br><span class="line">          </span><br><span class="line">        <span class="comment">//文件重命名</span></span><br><span class="line">        File f2 =<span class="keyword">new</span> File(<span class="string">"d:/LOLFolder/DOTA.exe"</span>);</span><br><span class="line">        f.renameTo(f2);</span><br><span class="line">        System.out.println(<span class="string">"把LOL.exe改名成了DOTA.exe"</span>);</span><br><span class="line">         </span><br><span class="line">        System.out.println(<span class="string">"注意： 需要在D:\\LOLFolder确实存在一个LOL.exe,\r\n才可以看到对应的文件长度、修改时间等信息"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestFile</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">  </span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"d:/LOLFolder/skin/garen.ski"</span>);</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 以字符串数组的形式，返回当前文件夹下的所有文件（不包含子文件及子文件夹）</span></span><br><span class="line">        f.list();</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 以文件数组的形式，返回当前文件夹下的所有文件（不包含子文件及子文件夹）</span></span><br><span class="line">        File[]fs= f.listFiles();</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 以字符串形式返回获取所在文件夹</span></span><br><span class="line">        f.getParent();</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 以文件形式返回获取所在文件夹</span></span><br><span class="line">        f.getParentFile();</span><br><span class="line">        <span class="comment">// 创建文件夹，如果父文件夹skin不存在，创建就无效</span></span><br><span class="line">        f.mkdir();</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 创建文件夹，如果父文件夹skin不存在，就会创建父文件夹</span></span><br><span class="line">        f.mkdirs();</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 创建一个空文件,如果父文件夹skin不存在，就会抛出异常</span></span><br><span class="line">        f.createNewFile();</span><br><span class="line">        <span class="comment">// 所以创建一个空文件之前，通常都会创建父目录</span></span><br><span class="line">        f.getParentFile().mkdirs();</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 列出所有的盘符c: d: e: 等等</span></span><br><span class="line">        f.listRoots();</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// 刪除文件</span></span><br><span class="line">        f.delete();</span><br><span class="line">  </span><br><span class="line">        <span class="comment">// JVM结束的时候，刪除文件，常用于临时文件的删除</span></span><br><span class="line">        f.deleteOnExit();</span><br><span class="line">  </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="练习"><a href="#练习" class="headerlink" title="练习"></a>练习</h2><h3 id="1-遍历文件夹"><a href="#1-遍历文件夹" class="headerlink" title="1.遍历文件夹"></a>1.遍历文件夹</h3><p>一般说来操作系统都会安装在C盘，所以会有一个 C:\WINDOWS目录。  遍历这个目录下所有的文件(不用遍历子目录)  找出这些文件里，最大的和最小(非0)的那个文件，打印出他们的文件名.</p>
<p>注意题目啊,要求是找文件而不是文件夹,文件夹用length方法结果是0.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.util.Comparator;</span><br><span class="line"><span class="keyword">import</span> java.util.TreeSet;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileTraverse</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> File f = <span class="keyword">new</span> File(<span class="string">"D:\\test"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File[] fileArray = f.listFiles();</span><br><span class="line">        TreeSet&lt;File&gt; fileSet = <span class="keyword">new</span> TreeSet&lt;&gt;((f1, f2) -&gt; (f1.length() &lt; f2.length()) ? -<span class="number">1</span> : ((f1.length() == f2.length() ? <span class="number">0</span> : <span class="number">1</span>)));</span><br><span class="line">        <span class="keyword">for</span> (File file : fileArray</span><br><span class="line">        ) &#123;</span><br><span class="line">            fileSet.add(file);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(f.length());</span><br><span class="line">        System.out.println(<span class="string">"min is"</span> + fileSet.first() + <span class="string">' '</span> + fileSet.first().length());</span><br><span class="line">        System.out.println(<span class="string">"max is"</span> + fileSet.last() + <span class="string">' '</span> + fileSet.last().length());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>不得不说,IDEA真的厉害,上面的是我写的原始代码,它提示我修改为下面的:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"><span class="keyword">import</span> java.util.Comparator;</span><br><span class="line"><span class="keyword">import</span> java.util.TreeSet;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileTraverse</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> File f = <span class="keyword">new</span> File(<span class="string">"D:\\test"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File[] fileArray = f.listFiles();</span><br><span class="line">        TreeSet&lt;File&gt; fileSet = <span class="keyword">new</span> TreeSet&lt;&gt;(Comparator.comparingLong(File::length));<span class="comment">//方法引用</span></span><br><span class="line">        <span class="comment">//断言</span></span><br><span class="line">        <span class="keyword">assert</span> fileArray != <span class="keyword">null</span>;</span><br><span class="line">        fileSet.addAll(Arrays.asList(fileArray));</span><br><span class="line">        System.out.println(f.length());</span><br><span class="line">        System.out.println(<span class="string">"min is"</span> + fileSet.first() + <span class="string">' '</span> + fileSet.first().length());</span><br><span class="line">        System.out.println(<span class="string">"max is"</span> + fileSet.last() + <span class="string">' '</span> + fileSet.last().length());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关于方法引用和断言,我会在写一篇.</p>
<p>下面是不使用容器写的代码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> file;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestFile</span> </span>&#123;</span><br><span class="line">   </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"c:\\windows"</span>);</span><br><span class="line">        File[] fs = f.listFiles();</span><br><span class="line">        <span class="keyword">if</span>(<span class="keyword">null</span>==fs)</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        <span class="keyword">long</span> minSize = Integer.MAX_VALUE;</span><br><span class="line">        <span class="keyword">long</span> maxSize = <span class="number">0</span>;</span><br><span class="line">        File minFile = <span class="keyword">null</span>;</span><br><span class="line">        File maxFile = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">for</span> (File file : fs) &#123;</span><br><span class="line">            <span class="keyword">if</span>(file.isDirectory())</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            <span class="keyword">if</span>(file.length()&gt;maxSize)&#123;</span><br><span class="line">                maxSize = file.length();</span><br><span class="line">                maxFile = file;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(file.length()!=<span class="number">0</span> &amp;&amp; file.length()&lt;minSize)&#123;</span><br><span class="line">                minSize = file.length();</span><br><span class="line">                minFile = file;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.printf(<span class="string">"最大的文件是%s，其大小是%,d字节%n"</span>,maxFile.getAbsoluteFile(),maxFile.length());</span><br><span class="line">        System.out.printf(<span class="string">"最小的文件是%s，其大小是%,d字节%n"</span>,minFile.getAbsoluteFile(),minFile.length());</span><br><span class="line">   </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-遍历子文件夹"><a href="#2-遍历子文件夹" class="headerlink" title="2.遍历子文件夹"></a>2.遍历子文件夹</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> file;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestFile</span> </span>&#123;</span><br><span class="line">      </span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">long</span> minSize = Integer.MAX_VALUE;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">long</span> maxSize = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">static</span> File minFile = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">static</span> File maxFile = <span class="keyword">null</span>;</span><br><span class="line">      </span><br><span class="line">    <span class="comment">//使用递归来遍历一个文件夹的子文件</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">listFiles</span><span class="params">(File file)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(file.isFile())&#123;</span><br><span class="line">            <span class="keyword">if</span>(file.length()&gt;maxSize)&#123;</span><br><span class="line">                maxSize = file.length();</span><br><span class="line">                maxFile = file;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(file.length()!=<span class="number">0</span> &amp;&amp; file.length()&lt;minSize)&#123;</span><br><span class="line">                minSize = file.length();</span><br><span class="line">                minFile = file;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">if</span>(file.isDirectory())&#123;</span><br><span class="line">            File[] fs = file.listFiles();</span><br><span class="line">            <span class="keyword">if</span>(<span class="keyword">null</span>!=fs)</span><br><span class="line">            <span class="keyword">for</span> (File f : fs) &#123;</span><br><span class="line">                listFiles(f);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"c:\\windows"</span>);</span><br><span class="line">        listFiles(f);</span><br><span class="line">        System.out.printf(<span class="string">"最大的文件是%s，其大小是%,d字节%n"</span>,maxFile.getAbsoluteFile(),maxFile.length());</span><br><span class="line">        System.out.printf(<span class="string">"最小的文件是%s，其大小是%,d字节%n"</span>,minFile.getAbsoluteFile(),minFile.length());</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="输入输出流"><a href="#输入输出流" class="headerlink" title="输入输出流"></a>输入输出流</h1><p><img src="/2018/07/28/%E8%BE%93%E5%85%A5%E8%BE%93%E5%87%BA/JavaIO.png" alt="体系"></p>
<p>这张图表明了流的体系结构,<strong>斜体是基类,粗体是节点流</strong>.我们从三个方面来划分它们的种类.</p>
<ul>
<li>输入流和输出流</li>
<li>字节流和字符流</li>
<li>节点流和处理流</li>
</ul>
<p>以上的具体划分参见Java笔记11那篇.</p>
<h2 id="以字节流的形式读取文件内容"><a href="#以字节流的形式读取文件内容" class="headerlink" title="以字节流的形式读取文件内容"></a>以字节流的形式读取文件内容</h2><p>InputStream是字节输入流，同时也是抽象类，只提供方法声明，不提供方法的具体实现。<br>FileInputStream 是InputStream子类，以FileInputStream为例进行文件读取.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">//准备文件lol.txt其中的内容是AB，对应的ASCII分别是65 66</span></span><br><span class="line">            File f =<span class="keyword">new</span> File(<span class="string">"d:/lol.txt"</span>);</span><br><span class="line">            <span class="comment">//创建基于文件的输入流</span></span><br><span class="line">            FileInputStream fis =<span class="keyword">new</span> FileInputStream(f);</span><br><span class="line">            <span class="comment">//创建字节数组，其长度就是文件的长度</span></span><br><span class="line">            <span class="keyword">byte</span>[] all =<span class="keyword">new</span> <span class="keyword">byte</span>[(<span class="keyword">int</span>) f.length()];</span><br><span class="line">            <span class="comment">//以字节流的形式读取文件所有内容</span></span><br><span class="line">            fis.read(all);</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">byte</span> b : all) &#123;</span><br><span class="line">                <span class="comment">//打印出来是65 66</span></span><br><span class="line">                System.out.println(b);</span><br><span class="line">            &#125;</span><br><span class="line">             </span><br><span class="line">            <span class="comment">//每次使用完流，都应该进行关闭</span></span><br><span class="line">            fis.close();</span><br><span class="line">              </span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">          </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="以字节流的形式向文件写入数据"><a href="#以字节流的形式向文件写入数据" class="headerlink" title="以字节流的形式向文件写入数据"></a>以字节流的形式向文件写入数据</h2><p>OutputStream是字节输出流，同时也是抽象类，只提供方法声明，不提供方法的具体实现。 FileOutputStream 是OutputStream子类，以FileOutputStream 为例向文件写出数据.</p>
<p>注: 如果文件d:/lol2.txt不存在，写出操作会自动创建该文件。  但是如果是文件d:/xyz/lol2.txt，而目录xyz又不存在，会抛出异常.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 准备文件lol2.txt其中的内容是空的</span></span><br><span class="line">            File f = <span class="keyword">new</span> File(<span class="string">"d:/lol2.txt"</span>);</span><br><span class="line">            <span class="comment">// 准备长度是2的字节数组，用88,89初始化，其对应的字符分别是X,Y</span></span><br><span class="line">            <span class="keyword">byte</span> data[] = &#123; <span class="number">88</span>, <span class="number">89</span> &#125;;</span><br><span class="line"> </span><br><span class="line">            <span class="comment">// 创建基于文件的输出流</span></span><br><span class="line">            FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(f);</span><br><span class="line">            <span class="comment">// 把数据写入到输出流</span></span><br><span class="line">            fos.write(data);</span><br><span class="line">            <span class="comment">// 关闭输出流</span></span><br><span class="line">            fos.close();</span><br><span class="line">             </span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="练习一"><a href="#练习一" class="headerlink" title="练习一"></a>练习一</h2><h3 id="1-拆分文件"><a href="#1-拆分文件" class="headerlink" title="1.拆分文件"></a>1.拆分文件</h3><p>找到一个大于100k的文件，按照100k为单位，拆分成多个子文件，并且以编号作为文件名结束。 比如文件 eclipse.exe，大小是309k。 拆分之后，成为</p>
<p>eclipse.exe-0</p>
<p>eclipse.exe-1</p>
<p>eclipse.exe-2</p>
<p>eclipse.exe-3</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> eachSize = <span class="number">100</span> * <span class="number">1024</span>; <span class="comment">// 100k</span></span><br><span class="line">        File srcFile = <span class="keyword">new</span> File(<span class="string">"d:/eclipse.exe"</span>);</span><br><span class="line">        splitFile(srcFile, eachSize);</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 拆分的思路，先把源文件的所有内容读取到内存中，然后从内存中挨个分到子文件里</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> srcFile 要拆分的源文件</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> eachSize 按照这个大小，拆分</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">splitFile</span><span class="params">(File srcFile, <span class="keyword">int</span> eachSize)</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">        <span class="keyword">if</span> (<span class="number">0</span> == srcFile.length())</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"文件长度为0，不可拆分"</span>);</span><br><span class="line">  </span><br><span class="line">        <span class="keyword">byte</span>[] fileContent = <span class="keyword">new</span> <span class="keyword">byte</span>[(<span class="keyword">int</span>) srcFile.length()];</span><br><span class="line">        <span class="comment">// 先把文件读取到数组中</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            FileInputStream fis = <span class="keyword">new</span> FileInputStream(srcFile);</span><br><span class="line">            fis.read(fileContent);</span><br><span class="line">            fis.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 计算需要被划分成多少份子文件</span></span><br><span class="line">        <span class="keyword">int</span> fileNumber;</span><br><span class="line">        <span class="comment">// 文件是否能被整除得到的子文件个数是不一样的</span></span><br><span class="line">        <span class="comment">// (假设文件长度是25，每份的大小是5，那么就应该是5个)</span></span><br><span class="line">        <span class="comment">// (假设文件长度是26，每份的大小是5，那么就应该是6个)</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="number">0</span> == fileContent.length % eachSize)</span><br><span class="line">            fileNumber = (<span class="keyword">int</span>) (fileContent.length / eachSize);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            fileNumber = (<span class="keyword">int</span>) (fileContent.length / eachSize) + <span class="number">1</span>;</span><br><span class="line">  </span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; fileNumber; i++) &#123;</span><br><span class="line">            String eachFileName = srcFile.getName() + <span class="string">"-"</span> + i;</span><br><span class="line">            File eachFile = <span class="keyword">new</span> File(srcFile.getParent(), eachFileName);</span><br><span class="line">            <span class="keyword">byte</span>[] eachContent;</span><br><span class="line">  </span><br><span class="line">            <span class="comment">// 从源文件的内容里，复制部分数据到子文件</span></span><br><span class="line">            <span class="comment">// 除开最后一个文件，其他文件大小都是100k</span></span><br><span class="line">            <span class="comment">// 最后一个文件的大小是剩余的</span></span><br><span class="line">            <span class="keyword">if</span> (i != fileNumber - <span class="number">1</span>) <span class="comment">// 不是最后一个</span></span><br><span class="line">                eachContent = Arrays.copyOfRange(fileContent, eachSize * i, eachSize * (i + <span class="number">1</span>));</span><br><span class="line">            <span class="keyword">else</span> <span class="comment">// 最后一个</span></span><br><span class="line">                eachContent = Arrays.copyOfRange(fileContent, eachSize * i, fileContent.length);</span><br><span class="line">  </span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 写出去</span></span><br><span class="line">                FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(eachFile);</span><br><span class="line">                fos.write(eachContent);</span><br><span class="line">                <span class="comment">// 记得关闭</span></span><br><span class="line">                fos.close();</span><br><span class="line">                System.out.printf(<span class="string">"输出子文件%s，其大小是 %d字节%n"</span>, eachFile.getAbsoluteFile(), eachFile.length());</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/2018/07/28/%E8%BE%93%E5%85%A5%E8%BE%93%E5%87%BA/%E6%B5%81%E7%A8%8B.png" alt></p>
<h3 id="2-合并文件"><a href="#2-合并文件" class="headerlink" title="2.合并文件"></a>2.合并文件</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.FileNotFoundException;</span><br><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> javax.security.auth.DestroyFailedException;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        murgeFile(<span class="string">"d:/"</span>, <span class="string">"eclipse.exe"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 合并的思路，就是从eclipse.exe-0开始，读取到一个文件，就开始写出到 eclipse.exe中，直到没有文件可以读</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> folder</span></span><br><span class="line"><span class="comment">     *            需要合并的文件所处于的目录</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> fileName</span></span><br><span class="line"><span class="comment">     *            需要合并的文件的名称</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> FileNotFoundException</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">murgeFile</span><span class="params">(String folder, String fileName)</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 合并的目标文件</span></span><br><span class="line">            File destFile = <span class="keyword">new</span> File(folder, fileName);</span><br><span class="line">            FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(destFile);</span><br><span class="line">            <span class="keyword">int</span> index = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                <span class="comment">//子文件</span></span><br><span class="line">                File eachFile = <span class="keyword">new</span> File(folder, fileName + <span class="string">"-"</span> + index++);</span><br><span class="line">                <span class="comment">//如果子文件不存在了就结束</span></span><br><span class="line">                <span class="keyword">if</span> (!eachFile.exists())</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line"> </span><br><span class="line">                <span class="comment">//读取子文件的内容</span></span><br><span class="line">                FileInputStream fis = <span class="keyword">new</span> FileInputStream(eachFile);</span><br><span class="line">                <span class="keyword">byte</span>[] eachContent = <span class="keyword">new</span> <span class="keyword">byte</span>[(<span class="keyword">int</span>) eachFile.length()];</span><br><span class="line">                fis.read(eachContent);</span><br><span class="line">                fis.close();</span><br><span class="line">                 </span><br><span class="line">                <span class="comment">//把子文件的内容写出去</span></span><br><span class="line">                fos.write(eachContent);</span><br><span class="line">                fos.flush();</span><br><span class="line">                System.out.printf(<span class="string">"把子文件 %s写出到目标文件中%n"</span>,eachFile);</span><br><span class="line">            &#125;</span><br><span class="line"> </span><br><span class="line">            fos.close();</span><br><span class="line">            System.out.printf(<span class="string">"最后目标文件的大小：%,d字节"</span> , destFile.length());</span><br><span class="line">        &#125; <span class="keyword">catch</span> (FileNotFoundException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="关闭流"><a href="#关闭流" class="headerlink" title="关闭流"></a>关闭流</h2><h3 id="在try中关闭"><a href="#在try中关闭" class="headerlink" title="在try中关闭"></a>在try中关闭</h3><p>在try的作用域里关闭文件输入流，在前面的练习中都是使用这种方式，这样做有一个弊端； 如果文件不存在，或者读取的时候出现问题而抛出异常，那么就不会执行这一行关闭流的代码，存在巨大的资源占用隐患.不推荐使用.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            File f = <span class="keyword">new</span> File(<span class="string">"d:/lol.txt"</span>);</span><br><span class="line">            FileInputStream fis = <span class="keyword">new</span> FileInputStream(f);</span><br><span class="line">            <span class="keyword">byte</span>[] all = <span class="keyword">new</span> <span class="keyword">byte</span>[(<span class="keyword">int</span>) f.length()];</span><br><span class="line">            fis.read(all);</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">byte</span> b : all) &#123;</span><br><span class="line">                System.out.println(b);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 在try 里关闭流</span></span><br><span class="line">            fis.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="在finally中关闭"><a href="#在finally中关闭" class="headerlink" title="在finally中关闭"></a>在finally中关闭</h3><p>这是标准的关闭流的方式 </p>
<ol>
<li>首先把流的引用声明在try的外面，如果声明在try里面，其作用域无法抵达finally.</li>
<li>在finally关闭之前，要先判断该引用是否为空</li>
<li>关闭的时候，需要再一次进行try catch处理 </li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"d:/lol.txt"</span>);</span><br><span class="line">        FileInputStream fis = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            fis = <span class="keyword">new</span> FileInputStream(f);</span><br><span class="line">            <span class="keyword">byte</span>[] all = <span class="keyword">new</span> <span class="keyword">byte</span>[(<span class="keyword">int</span>) f.length()];</span><br><span class="line">            fis.read(all);</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">byte</span> b : all) &#123;</span><br><span class="line">                System.out.println(b);</span><br><span class="line">            &#125;</span><br><span class="line"> </span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">// 在finally 里关闭流</span></span><br><span class="line">            <span class="keyword">if</span> (<span class="keyword">null</span> != fis)</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line"> </span><br><span class="line">                    fis.close();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                    <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="使用try-的方式"><a href="#使用try-的方式" class="headerlink" title="使用try()的方式"></a>使用try()的方式</h3><p>把流定义在try()里,try,catch或者finally结束的时候，会自动关闭这种编写代码的方式叫做try-with-resources，这是从JDK7开始支持的技术.</p>
<p>所有的流，都实现了一个接口叫做 AutoCloseable，任何类实现了这个接口，都可以在try()中进行实例化.并且在try, catch, finally结束的时候自动关闭，回收相关资源。 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"d:/lol.txt"</span>);</span><br><span class="line">  </span><br><span class="line">        <span class="comment">//把流定义在try()里,try,catch或者finally结束的时候，会自动关闭</span></span><br><span class="line">        <span class="keyword">try</span> (FileInputStream fis = <span class="keyword">new</span> FileInputStream(f)) &#123;</span><br><span class="line">            <span class="keyword">byte</span>[] all = <span class="keyword">new</span> <span class="keyword">byte</span>[(<span class="keyword">int</span>) f.length()];</span><br><span class="line">            fis.read(all);</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">byte</span> b : all) &#123;</span><br><span class="line">                System.out.println(b);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">  </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="使用字符流读取文件"><a href="#使用字符流读取文件" class="headerlink" title="使用字符流读取文件"></a>使用字符流读取文件</h2><p>FileReader是Reader子类，以FileReader为例进行文件读取 .</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 准备文件lol.txt其中的内容是AB</span></span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"d:/lol.txt"</span>);</span><br><span class="line">        <span class="comment">// 创建基于文件的Reader</span></span><br><span class="line">        <span class="keyword">try</span> (FileReader fr = <span class="keyword">new</span> FileReader(f)) &#123;</span><br><span class="line">            <span class="comment">// 创建字符数组，其长度就是文件的长度</span></span><br><span class="line">            <span class="keyword">char</span>[] all = <span class="keyword">new</span> <span class="keyword">char</span>[(<span class="keyword">int</span>) f.length()];</span><br><span class="line">            <span class="comment">// 以字符流的形式读取文件所有内容</span></span><br><span class="line">            fr.read(all);</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">char</span> b : all) &#123;</span><br><span class="line">                <span class="comment">// 打印出来是A B</span></span><br><span class="line">                System.out.println(b);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="使用字符流把字符串写入到文件"><a href="#使用字符流把字符串写入到文件" class="headerlink" title="使用字符流把字符串写入到文件"></a>使用字符流把字符串写入到文件</h2><p>FileWriter是Writer的子类，以FileWriter为例把字符串写入到文件.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileWriter;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 准备文件lol2.txt</span></span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"d:/lol2.txt"</span>);</span><br><span class="line">        <span class="comment">// 创建基于文件的Writer</span></span><br><span class="line">        <span class="keyword">try</span> (FileWriter fr = <span class="keyword">new</span> FileWriter(f)) &#123;</span><br><span class="line">            <span class="comment">// 以字符流的形式把数据写入到文件中</span></span><br><span class="line">            String data=<span class="string">"abcdefg1234567890"</span>;</span><br><span class="line">            <span class="keyword">char</span>[] cs = data.toCharArray();</span><br><span class="line">            fr.write(cs);</span><br><span class="line">  </span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">  </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="练习二"><a href="#练习二" class="headerlink" title="练习二"></a>练习二</h2><h3 id="1-加密文件"><a href="#1-加密文件" class="headerlink" title="1.加密文件"></a>1.加密文件</h3><p>准备一个文本文件(非二进制)，其中包含ASCII码的字符和中文字符。</p>
<p>设计一个方法:</p>
<p> <code>public static void encodeFile(File encodingFile, File encodedFile);</code></p>
<p>在这个方法中把encodingFile的内容进行加密，然后保存到encodedFile文件中。</p>
<p>加密算法： </p>
<ol>
<li>数字：</li>
</ol>
<ul>
<li>如果不是9的数字，在原来的基础上加1，比如5变成6, 3变成4</li>
<li>如果是9的数字，变成0</li>
</ul>
<ol start="2">
<li>字母字符：</li>
</ol>
<ul>
<li>如果是非z字符，向右移动一个，比如d变成e, G变成H</li>
<li>如果是z，z-&gt;a, Z-A。</li>
<li>字符需要保留大小写</li>
</ul>
<ol start="3">
<li>非字母字符</li>
</ol>
<ul>
<li>比如’,&amp;^ 保留不变，中文也保留不变</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.FileWriter;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> encodingFile</span></span><br><span class="line"><span class="comment">     *            被加密的文件</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> encodedFile</span></span><br><span class="line"><span class="comment">     *            加密后保存的位置</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">encodeFile</span><span class="params">(File encodingFile, File encodedFile)</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">try</span> (FileReader fr = <span class="keyword">new</span> FileReader(encodingFile); FileWriter fw = <span class="keyword">new</span> FileWriter(encodedFile)) &#123;</span><br><span class="line">            <span class="comment">// 读取源文件</span></span><br><span class="line">            <span class="keyword">char</span>[] fileContent = <span class="keyword">new</span> <span class="keyword">char</span>[(<span class="keyword">int</span>) encodingFile.length()];</span><br><span class="line">            fr.read(fileContent);</span><br><span class="line">            System.out.println(<span class="string">"加密前的内容："</span>);</span><br><span class="line">            System.out.println(<span class="keyword">new</span> String(fileContent));</span><br><span class="line"> </span><br><span class="line">            <span class="comment">// 进行加密</span></span><br><span class="line">            encode(fileContent);</span><br><span class="line">            <span class="comment">// 把加密后的内容保存到目标文件</span></span><br><span class="line">            System.out.println(<span class="string">"加密后的内容："</span>);</span><br><span class="line">            System.out.println(<span class="keyword">new</span> String(fileContent));</span><br><span class="line"> </span><br><span class="line">            fw.write(fileContent);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">encode</span><span class="params">(<span class="keyword">char</span>[] fileContent)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; fileContent.length; i++) &#123;</span><br><span class="line">            <span class="keyword">char</span> c = fileContent[i];</span><br><span class="line">            <span class="keyword">if</span> (isLetterOrDigit(c)) &#123;</span><br><span class="line">                <span class="keyword">switch</span> (c) &#123;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">'9'</span>:</span><br><span class="line">                    c = <span class="string">'0'</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">'z'</span>:</span><br><span class="line">                    c = <span class="string">'a'</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">'Z'</span>:</span><br><span class="line">                    c = <span class="string">'A'</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">default</span>:</span><br><span class="line">                    c++;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            fileContent[i] = c;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">boolean</span> <span class="title">isLetterOrDigit</span><span class="params">(<span class="keyword">char</span> c)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 不使用Character类的isLetterOrDigit方法是因为，中文也会被判断为字母</span></span><br><span class="line">        String letterOrDigital = <span class="string">"0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz"</span>;</span><br><span class="line">        <span class="keyword">return</span> -<span class="number">1</span> == letterOrDigital.indexOf(c) ? <span class="keyword">false</span> : <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File encodingFile = <span class="keyword">new</span> File(<span class="string">"E:/project/j2se/src/Test1.txt"</span>);</span><br><span class="line">        File encodedFile = <span class="keyword">new</span> File(<span class="string">"E:/project/j2se/src/Test2.txt"</span>);</span><br><span class="line">        encodeFile(encodingFile, encodedFile);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-解密文件"><a href="#2-解密文件" class="headerlink" title="2.解密文件"></a>2.解密文件</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.FileWriter;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> decodingFile</span></span><br><span class="line"><span class="comment">     *            被解密的文件</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> decodedFile</span></span><br><span class="line"><span class="comment">     *            解密后保存的位置</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">decodeFile</span><span class="params">(File decodingFile, File decodedFile)</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">try</span> (FileReader fr = <span class="keyword">new</span> FileReader(decodingFile); FileWriter fw = <span class="keyword">new</span> FileWriter(decodedFile)) &#123;</span><br><span class="line">            <span class="comment">// 读取源文件</span></span><br><span class="line">            <span class="keyword">char</span>[] fileContent = <span class="keyword">new</span> <span class="keyword">char</span>[(<span class="keyword">int</span>) decodingFile.length()];</span><br><span class="line">            fr.read(fileContent);</span><br><span class="line">            System.out.println(<span class="string">"源文件的内容:"</span>);</span><br><span class="line">            System.out.println(<span class="keyword">new</span> String(fileContent));</span><br><span class="line">            <span class="comment">// 进行解密</span></span><br><span class="line">            decode(fileContent);</span><br><span class="line">            System.out.println(<span class="string">"解密后的内容:"</span>);</span><br><span class="line">            System.out.println(<span class="keyword">new</span> String(fileContent));</span><br><span class="line">            <span class="comment">// 把解密后的内容保存到目标文件</span></span><br><span class="line">            fw.write(fileContent);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">decode</span><span class="params">(<span class="keyword">char</span>[] fileContent)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; fileContent.length; i++) &#123;</span><br><span class="line">            <span class="keyword">char</span> c = fileContent[i];</span><br><span class="line">            <span class="keyword">if</span> (isLetterOrDigit(c)) &#123;</span><br><span class="line">                <span class="keyword">switch</span> (c) &#123;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">'0'</span>:</span><br><span class="line">                    c = <span class="string">'9'</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">'a'</span>:</span><br><span class="line">                    c = <span class="string">'z'</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">'A'</span>:</span><br><span class="line">                    c = <span class="string">'Z'</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">default</span>:</span><br><span class="line">                    c--;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            fileContent[i] = c;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">boolean</span> <span class="title">isLetterOrDigit</span><span class="params">(<span class="keyword">char</span> c)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 不使用Character类的isLetterOrDigit方法是因为，中文也会被判断为字母</span></span><br><span class="line">        String letterOrDigital =<span class="string">"0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz"</span>;</span><br><span class="line">        <span class="keyword">return</span> -<span class="number">1</span> == letterOrDigital.indexOf(c) ? <span class="keyword">false</span> : <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File decodingFile = <span class="keyword">new</span> File(<span class="string">"E:/project/j2se/src/Test2.txt"</span>);</span><br><span class="line">        File decodedFile = <span class="keyword">new</span> File(<span class="string">"E:/project/j2se/src/Test1.txt"</span>);</span><br><span class="line"> </span><br><span class="line">        decodeFile(decodingFile, decodedFile);</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="编码问题"><a href="#编码问题" class="headerlink" title="编码问题"></a>编码问题</h2><p>编码问题真的是非常烦,我总结了三点注意事项:</p>
<ul>
<li><p>注意环境的默认编码</p>
<p>这里的环境指的的是你的IDE默认编码,读取的文件的编码,保存时的编码,总之要做到对于文件会是什么编码要心里有数</p>
</li>
<li><p>搞清打开文件要用什么编码</p>
</li>
<li><p>将上面两者对应起来</p>
</li>
</ul>
<p>更多编码问题参见连接:<a href="http://how2j.cn/k/io/io-encoding/695.html#step2489" target="_blank" rel="noopener">http://how2j.cn/k/io/io-encoding/695.html#step2489</a></p>
<h2 id="缓存流"><a href="#缓存流" class="headerlink" title="缓存流"></a>缓存流</h2><p>按照前面的划分:</p>
<ul>
<li>输入流和输出流</li>
<li>字节流和字符流</li>
<li>节点流和处理流</li>
</ul>
<p>已经从输入输出角度和字符字节角度讲了一些,而节点流是连接物理节点的,处理流是连接节点流的.还有和特殊类别是缓存流.先看看一个通俗解释:</p>
<blockquote>
<p>flush本意是冲刷，这个方法大概取自它引申义冲马桶的意思，马桶有个池子，你往里面扔东西，会暂时保存在池子里，只有你放水冲下去，东西才会进入下水道。同理很多流都有一个这样的池子，专业术语叫缓冲区，当你print或者write的时候，会暂时保存在缓冲区，并没有发送出去，这是出于效率考虑的，因为数据不会自己发送过去，必须有其他机制，而且这个很消耗资源，就像马桶你需要很多水，才能冲走，你如果扔一点东西，就冲一次水，那你水费要爆表了，同样如果你写一行文字，或者一个字节，就要马上发送出去，那网络流量，CPU使用率等等都要爆表了，所以一般只有在你真正需要发送否则无法继续的时候，调用flush，将数据发送出去。</p>
</blockquote>
<p>里面提到的flush()方法是用来将缓冲强制清空的方法(不是删除数据,而是在缓冲区未满的情况下将数据提前输入/输出),关闭流时会自动调用此方法.</p>
<h3 id="使用缓存流读取数据"><a href="#使用缓存流读取数据" class="headerlink" title="使用缓存流读取数据"></a>使用缓存流读取数据</h3><p>注意,缓存流都是处理流,需要在一个现成的节点流.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> java.io.BufferedReader;</span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 准备文件lol.txt其中的内容是</span></span><br><span class="line">        <span class="comment">// garen kill teemo</span></span><br><span class="line">        <span class="comment">// teemo revive after 1 minutes</span></span><br><span class="line">        <span class="comment">// teemo try to garen, but killed again</span></span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"d:/lol.txt"</span>);</span><br><span class="line">        <span class="comment">// 创建文件字符流</span></span><br><span class="line">        <span class="comment">// 缓存流必须建立在一个存在的流的基础上</span></span><br><span class="line">        <span class="keyword">try</span> (</span><br><span class="line">                FileReader fr = <span class="keyword">new</span> FileReader(f);</span><br><span class="line">                BufferedReader br = <span class="keyword">new</span> BufferedReader(fr);</span><br><span class="line">            )</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                <span class="comment">// 一次读一行</span></span><br><span class="line">                String line = br.readLine();</span><br><span class="line">                <span class="keyword">if</span> (<span class="keyword">null</span> == line)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                System.out.println(line);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">  </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="使用缓存流写出数据"><a href="#使用缓存流写出数据" class="headerlink" title="使用缓存流写出数据"></a>使用缓存流写出数据</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileWriter;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.PrintWriter;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line">   </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 向文件lol2.txt中写入三行语句</span></span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"d:/lol2.txt"</span>);</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">try</span> (</span><br><span class="line">                <span class="comment">// 创建文件字符流</span></span><br><span class="line">                FileWriter fw = <span class="keyword">new</span> FileWriter(f);</span><br><span class="line">                <span class="comment">// 缓存流必须建立在一个存在的流的基础上              </span></span><br><span class="line">                PrintWriter pw = <span class="keyword">new</span> PrintWriter(fw);              </span><br><span class="line">        ) &#123;</span><br><span class="line">            pw.println(<span class="string">"garen kill teemo"</span>);</span><br><span class="line">            pw.println(<span class="string">"teemo revive after 1 minutes"</span>);</span><br><span class="line">            pw.println(<span class="string">"teemo try to garen, but killed again"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">   </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="数据流"><a href="#数据流" class="headerlink" title="数据流"></a>数据流</h2><p>为什么要用数据流?当向某个文件连续写入数字1,2,3时,如果用普通的输出流,就必须认为加上一些标识符(例如’,’用来分割数据,不至于变成123).同样的为了正确取出数据,在读入后还需要将这些标志性的字符去掉.而使用数据流就可以避免这些操作.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> stream;</span><br><span class="line">      </span><br><span class="line"><span class="keyword">import</span> java.io.DataInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.DataOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.FileOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line">      </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestStream</span> </span>&#123;</span><br><span class="line">      </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        write();</span><br><span class="line">        read();</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">read</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        File f =<span class="keyword">new</span> File(<span class="string">"d:/lol.txt"</span>);</span><br><span class="line">        <span class="keyword">try</span> (</span><br><span class="line">                FileInputStream fis  = <span class="keyword">new</span> FileInputStream(f);</span><br><span class="line">                DataInputStream dis =<span class="keyword">new</span> DataInputStream(fis);</span><br><span class="line">        )&#123;</span><br><span class="line">            <span class="keyword">boolean</span> b= dis.readBoolean();</span><br><span class="line">            <span class="keyword">int</span> i = dis.readInt();</span><br><span class="line">            String str = dis.readUTF();</span><br><span class="line">             </span><br><span class="line">            System.out.println(<span class="string">"读取到布尔值:"</span>+b);</span><br><span class="line">            System.out.println(<span class="string">"读取到整数:"</span>+i);</span><br><span class="line">            System.out.println(<span class="string">"读取到字符串:"</span>+str);</span><br><span class="line"> </span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">write</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        File f =<span class="keyword">new</span> File(<span class="string">"d:/lol.txt"</span>);</span><br><span class="line">        <span class="keyword">try</span> (</span><br><span class="line">                FileOutputStream fos  = <span class="keyword">new</span> FileOutputStream(f);</span><br><span class="line">                DataOutputStream dos =<span class="keyword">new</span> DataOutputStream(fos);</span><br><span class="line">        )&#123;</span><br><span class="line">            dos.writeBoolean(<span class="keyword">true</span>);</span><br><span class="line">            dos.writeInt(<span class="number">300</span>);</span><br><span class="line">            dos.writeUTF(<span class="string">"123 this is gareen"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>有几点要注意:</p>
<ul>
<li>DataOutputStream的文件要DataInputStream来打开,否则会产生异常</li>
<li>注意写入和读取时的顺序</li>
</ul>
<h2 id="对象流"><a href="#对象流" class="headerlink" title="对象流"></a>对象流</h2><p>对象流指的是可以直接把一个对象以流的形式传输给其他的介质，比如硬盘.一个对象以流的形式进行传输，叫做序列化。 该对象所对应的类，必须是实现Serializable接口.</p>
<h3 id="练习-1"><a href="#练习-1" class="headerlink" title="练习"></a>练习</h3><p>准备一个长度是10，类型是Hero的数组，使用10个Hero对象初始化该数组,然后把该数组序列化到一个文件heros.lol接着使用ObjectInputStream读取该文件，并转换为Hero数组，验证该数组中的内容，是否和序列化之前一样.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.Serializable;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Hero</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">1L</span>;</span><br><span class="line">    String name;</span><br><span class="line">    <span class="keyword">float</span> hp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.*;</span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SerialFileTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> capacity = <span class="number">10</span>;</span><br><span class="line">        List&lt;Hero&gt; heroes = <span class="keyword">new</span> ArrayList&lt;&gt;(capacity);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; capacity; i++) &#123;</span><br><span class="line">            String n = <span class="string">"hero"</span> + String.valueOf(i);</span><br><span class="line">            <span class="keyword">int</span> h = <span class="number">616</span> + i;</span><br><span class="line">            Hero hero = <span class="keyword">new</span> Hero() &#123;&#123;</span><br><span class="line">                name = n;</span><br><span class="line">                hp = h;</span><br><span class="line">            &#125;&#125;;</span><br><span class="line">            heroes.add(hero);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"./src/heroes.lol"</span>);</span><br><span class="line">        <span class="keyword">try</span> (FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(f);</span><br><span class="line">             ObjectOutputStream oos = <span class="keyword">new</span> ObjectOutputStream(fos)) &#123;</span><br><span class="line">            oos.writeObject(heroes);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> (FileInputStream fis = <span class="keyword">new</span> FileInputStream(f);</span><br><span class="line">             ObjectInputStream ois = <span class="keyword">new</span> ObjectInputStream(fis)) &#123;</span><br><span class="line">            List&lt;Hero&gt; list = (List&lt;Hero&gt;) ois.readObject();</span><br><span class="line">            <span class="keyword">for</span> (Hero hero : list) &#123;</span><br><span class="line">                System.out.println(hero.name);</span><br><span class="line">                System.out.println(hero.hp);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="标准输入输出"><a href="#标准输入输出" class="headerlink" title="标准输入输出"></a>标准输入输出</h2><p>System.in的定义是<code>public static final InputStream in = null;</code>,而System.out定义是<code>public static final PrintStream out = null;</code>,所以标准输入输出是字节流.</p>
<p>运用:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.*;</span><br><span class="line"><span class="keyword">import</span> java.util.Scanner;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ObjectCreate</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        Scanner sc = <span class="keyword">new</span> Scanner(System.in);</span><br><span class="line">        System.out.println(<span class="string">"请输入类名:"</span>);</span><br><span class="line">        String className = sc.nextLine();</span><br><span class="line">        System.out.println(<span class="string">"请输入属性类别:"</span>);</span><br><span class="line">        String property = sc.nextLine();</span><br><span class="line">        System.out.println(<span class="string">"请输入属性名称:"</span>);</span><br><span class="line">        String name = sc.nextLine();</span><br><span class="line">        getFile(className, property, name);</span><br><span class="line">        System.out.println(<span class="string">"名为"</span> + className + <span class="string">".java的文件已生成,存放在d:/"</span> + className + <span class="string">".java"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">getFile</span><span class="params">(String className, String property, String name)</span> </span>&#123;</span><br><span class="line">        File outFile = <span class="keyword">new</span> File(<span class="string">"./src/"</span> + className + <span class="string">".java"</span>);</span><br><span class="line">        File inFile = <span class="keyword">new</span> File(<span class="string">"./src/model.txt"</span>);</span><br><span class="line">        <span class="keyword">try</span> (FileReader fr = <span class="keyword">new</span> FileReader(inFile);</span><br><span class="line">             BufferedReader br = <span class="keyword">new</span> BufferedReader(fr)</span><br><span class="line">        ) &#123;</span><br><span class="line">            StringBuilder read = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">            <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                String s = br.readLine();</span><br><span class="line">                <span class="keyword">if</span> (s == <span class="keyword">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                read.append(s).append(<span class="string">"\r\n"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println(read);</span><br><span class="line">            System.out.println(<span class="string">"这里是分割线-----------------------------------------"</span>);</span><br><span class="line">            String write = read.toString().replaceAll(<span class="string">"@class@"</span>, className);</span><br><span class="line">            write = write.replaceAll(<span class="string">"@type@"</span>, property);</span><br><span class="line">            write = write.replaceAll(<span class="string">"@property@"</span>, name);</span><br><span class="line">            <span class="keyword">char</span>[] c = name.toCharArray();</span><br><span class="line">            <span class="keyword">if</span> (c[<span class="number">0</span>] &gt;= <span class="string">'a'</span> &amp;&amp; c[<span class="number">0</span>] &lt;= <span class="string">'z'</span>) &#123;</span><br><span class="line">                c[<span class="number">0</span>] = (<span class="keyword">char</span>) (c[<span class="number">0</span>] - <span class="number">32</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            name = <span class="keyword">new</span> String(c);</span><br><span class="line">            write = write.replaceAll(<span class="string">"@Uproperty@"</span>, name);</span><br><span class="line">            System.out.println(write);</span><br><span class="line">            FileWriter fileWriter = <span class="keyword">new</span> FileWriter(outFile);</span><br><span class="line">            BufferedWriter bw = <span class="keyword">new</span> BufferedWriter(fileWriter);</span><br><span class="line">            bw.write(write);</span><br><span class="line">            br.close();</span><br><span class="line">            bw.flush();</span><br><span class="line">            bw.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>本篇中有不少东西没讲,包括RandomAccessFile,NIO,NIO.2,对象序列化都没讲,所以这部分会另开一篇.</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>物理地址</title>
    <url>/2018/02/18/%E7%89%A9%E7%90%86%E5%9C%B0%E5%9D%80/</url>
    <content><![CDATA[<p>什么是物理地址？</p>
<a id="more"></a>

<p><em>注意，本篇中数字后的D和H表示十进制和十六进制，注意区别</em></p>
<p>物理地址不是真实地址。CPU访问的是虚拟地址空间，每一个内存单元在空间中都有个唯一的地址。</p>
<p>CPU通过地址总线送入存储器的，必须是一个内存单元的物理地址。在 CPU向地址总线上发出物理地址之前，必须要在内部先形成这个物理地址。不同的CPU可以有不同的形成物理地址的方式。</p>
<p>8086是16位结构的CPU ,这也就是说，在8086内部，能够一次性处理、传输、暂时存储的信息的最大长度是16位的。内存单元的地址在送上地址总线之前，必须在CPU中处理、传输、暂时存放，对于16位CPU ,能一次性处理、传输、暂时存储16位的地址。</p>
<ul>
<li>运算器一次最多可以处理16位的数据；</li>
<li>寄存器的最大宽度为16位；</li>
<li>寄存器和运算器之间的通路为16位。</li>
</ul>
<h1 id="重要"><a href="#重要" class="headerlink" title="重要"></a>重要</h1><p>8086CPU有 20位地址总线，可以传送20位地址，达到1MB寻址能力。即能表示2^20个不同的地址，一共是2^20^=1048576byte=1024k=1M。（一个地址指向一个内存单元，8bit）。但是8086CPU又是16位结构，在内部一次性处理、传输、暂时存储的地址为16位。从8086CPU的内部结构来看，如果将地址从内部简单地发出，那么它只能送出16位的地址，表现出的寻址能力只有64KB。</p>
<p>8086CPU采用一种在内部用两个16位地址合成的方法来形成一个20位的物理地址。</p>
<p>（所以，内存的大小关键在于地址总线的宽度。）</p>
<p><img src="/2018/02/18/%E7%89%A9%E7%90%86%E5%9C%B0%E5%9D%80/%E9%83%A8%E4%BB%B6.png" alt="逻辑结构"></p>
<p>当8086CPU要读写内存时：</p>
<ol>
<li>CPU中的相关部件提供两个16位的地址，一个称为段地址，另一个称为偏移地址;</li>
<li>段地址和偏移地址通过内部总线送入一个称为地址加法器的部件；</li>
<li>地址加法器将两个16位地址合成为一个20位的物理地址；</li>
<li>地址加法器通过内部总线将20位物理地址送入输入输出控制电路；</li>
<li>输入输出控制电路将20位物理地址送上地址总线；</li>
<li>20位物理地址被地址总线传送到存储器。</li>
</ol>
<p>地址加法器采用<strong>物理地址=段地址X16D+偏移地址</strong>的方法用段地址和偏移地址合成物理地址。例如，8086CPU要访问地址为123C8(H)的内存单元，此时</p>
<table>
<thead>
<tr>
<th></th>
<th>十六位地址A</th>
<th>十六位地址B</th>
</tr>
</thead>
<tbody><tr>
<td>第一步</td>
<td>1230（段地址）</td>
<td>00C8（偏移地址）</td>
</tr>
<tr>
<td>第二步</td>
<td>送入地址加法器</td>
<td>送入地址加法器</td>
</tr>
<tr>
<td>第三步</td>
<td>1230<del>H</del>*16<del>D</del></td>
<td>00C8</td>
</tr>
<tr>
<td>第四步</td>
<td>12300+00C8=123C8</td>
<td></td>
</tr>
<tr>
<td>第五步</td>
<td>送到地址总线</td>
<td></td>
</tr>
</tbody></table>
<p>“段地址*16D”更好的说法是左移4位。 一个数据的二进制形式左移N位，相当于该数据乘以2 的N次方。</p>
<p>一个数据的十六进制形式左移1位，相当于乘以16；—个数据的十进制形式左移1位，相当于乘以10；一个X进制的数据左移1位，相当于乘以X。</p>
<p><strong>注意</strong>：在内存中全是二进制的，对于乘多少的数字最好不要关注太多，非常容易混乱，每次算的时候还是用位移运算。</p>
<h1 id="“段地址x16D-偏移地址-物理地址”的本质含义"><a href="#“段地址x16D-偏移地址-物理地址”的本质含义" class="headerlink" title="“段地址x16D+偏移地址=物理地址”的本质含义"></a>“段地址x16D+偏移地址=物理地址”的本质含义</h1><p>“段地址X16+偏移地址=物理地址”的本质含义是：CPU在访问内存时，用一个基础地址（段地址X16）和一个相对于基础地址的偏移地址相加，给出内存单元的物理地址。<br>更一般地说，8086CPU的这种寻址功能是“基础地址+偏移地址=物理地址”寻址模式的一种具体实现方案。8086CPU中，段地址X16可看作是基础地址。</p>
<p><strong>注意：</strong>16是十进制的。在乘的时候要乘10H，因为段地址是十六进制的。</p>
<p>下面，我用两个与CPU无关的例子做进一步的比喻说明。<br>第一个比喻说明“基础地址+偏移地址=物理地址”的思想。<br>比如说，学校、体育馆、图书馆同在一条笔直的单行路上， 学校位于路的起点（从路的起点到学校距离是0米）。</p>
<p><img src="/2018/02/18/%E7%89%A9%E7%90%86%E5%9C%B0%E5%9D%80/%E4%BD%8D%E7%BD%AE.png" alt="位置"></p>
<p>你要去图书馆，问我那里的地址，我可以用两种方式告诉你图书馆的地址：<br>（1） 从学校走2826m到图书馆。这 2826m可以认为是图书馆的物理地址。 </p>
<p>（2） 从学校走2000m到体育馆，从体育馆再走826m到图书馆。第一个距离2000m， 是相对于起点的基础地址，第二个距离826m是相对于基础地址的偏移地址（以基础地址为起点的地址）。</p>
<p>第一种方式是直接给出物理地址2826m ,而第二种方式是用基础地址和偏移地址相加来得到物理地址的。<br>第二个比喻进一步说明“段地址X16D+偏移地址=物理地址”的思想。<br>我们为上面的例子加一些限制条件，比如，只能通过纸条来互相通信，你问我图书馆的地址我只能将它写在纸上告诉你。显然，我必须有一张可以容纳4 位数据的纸条，才能写下2826这个数据。</p>
<p><img src="/2018/02/18/%E7%89%A9%E7%90%86%E5%9C%B0%E5%9D%80/%E7%BA%B8%E6%9D%A11.png" alt="纸条"></p>
<p>不巧的是，我没有能容纳4 位数据的纸条，仅有两张可以容纳3 位数据的纸条。这样我只能以这种方式告诉你2826这个数据。</p>
<p><img src="/2018/02/18/%E7%89%A9%E7%90%86%E5%9C%B0%E5%9D%80/%E7%BA%B8%E6%9D%A12.png" alt="纸条"></p>
<p>在第一张纸上写上200D（段地址），在第二张纸上写上826D（偏移地址）。假设我们事前对这种情况又有过相关的约定：你得到这两张纸后，做这样的运算：200D（段地址)*10D+826D（偏移地址）=2826D（物理地址）。<br>8086CPU就是这样一个只能提供两张3位数据纸条的CPU。当然，现在的许多计算机都是32位的，即字长是4字节，8字节的也很多。</p>
<h1 id="段的概念"><a href="#段的概念" class="headerlink" title="段的概念"></a>段的概念</h1><p>其实，内存并没有分段，段的划分来自于CPU ,由于8086CPU用 “基础地址(段地址*16)+偏移地址=物理地址”的方式给出内存单元的物理地址，使得我们可以用分段的方式来管理内存。如图所示，我们可以认为：地址10000H<del>100FFH的内存单元组成一个段，该段的起始地址（基础地址）为 10000H，段地址为1000H，大小为100H；我们也可以认为地址10000H</del>1007FH、10080H~100FFH的内存单元组成两个段，它们的起始地址（基础地址）为：10000H和10080H，段地址为:1000H和 1008H,大小都为80H。</p>
<p><img src="/2018/02/18/%E7%89%A9%E7%90%86%E5%9C%B0%E5%9D%80/%E6%AE%B5.png" alt="段"></p>
<p>以后，在编程时可以根据需要，将若干地址连续的内存单元看作一个段，用段地址X16定位段的起始地址（基础地址），用偏移地址定位段中的内存单元。有两点需要注意：段地址X16必然是16的倍数，所以一个段的起始地址也一定是16的倍数；偏移地址为16位，16位地址的寻址能力为64KB,所以一个段的长度最大为64KB。</p>
<p>注意，因为只要加起来是那个物理地址，所以可以用不同的段地址和偏移地址构成同一个物理地址。</p>
<h1 id="段寄存器"><a href="#段寄存器" class="headerlink" title="段寄存器"></a>段寄存器</h1><p>段地址由段寄存器提供，8086CPU有4个段寄存器:CS,DS,SS,ES.这里先看看CS和另一个指令指针寄存器IP.在8086PC机中,任意时刻，设CS中的内容为M, IP中的内容为N, 8086CPU将从内存M*16+N单元开始，读取一条指令并执行.</p>
]]></content>
      <tags>
        <tag>汇编</tag>
      </tags>
  </entry>
  <entry>
    <title>Java多线程</title>
    <url>/2018/08/13/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/</url>
    <content><![CDATA[<p>重头戏,多线程.</p>
<a id="more"></a>

<h1 id="绪论"><a href="#绪论" class="headerlink" title="绪论"></a>绪论</h1><h2 id="线程与进程-并行与并发"><a href="#线程与进程-并行与并发" class="headerlink" title="线程与进程,并行与并发"></a>线程与进程,并行与并发</h2><p>首先要理解进程(Processor)和线程(Thread)的区别</p>
<p>进程：启动一个LOL.exe就叫一个进程。 接着又启动一个DOTA.exe，这叫两个进程。 </p>
<p>线程：线程是在进程内部同时做的事情，比如打开一个图片识别进程,先识别一张图片,再识别下一张这不是多线程,当同时进行时才是.</p>
<p>再补充点什么是并行和并发:</p>
<blockquote>
<p>你吃饭吃到一半，电话来了，你一直到吃完了以后才去接，这就说明你不支持并发也不支持并行。<br>你吃饭吃到一半，电话来了，你停了下来接了电话，接完后继续吃饭，这说明你支持并发。<br>你吃饭吃到一半，电话来了，你一边打电话一边吃饭，这说明你支持并行。</p>
<p>并发的关键是你有处理多个任务的能力，不一定要同时。<br>并行的关键是你有同时处理多个任务的能力。</p>
</blockquote>
<blockquote>
<p>吃饭的时候先接电话跟后接电话的比较更像是中断优先级高低的不同，并发应该是一手筷子，一手电话，说一句话，咽一口饭。 并行才是咽一口饭同时说一句话，而这光靠一张嘴是办不到的，至少两张嘴。 </p>
</blockquote>
<blockquote>
<p>如果某个系统支持两个或者多个动作（Action）<strong>同时存在</strong>，那么这个系统就是一个<strong>并发系统</strong>。如果某个系统支持两个或者多个动作<strong>同时执行</strong>，那么这个系统就是一个<strong>并行系统</strong>。并发系统与并行系统这两个定义之间的关键差异在于<strong>“存在”</strong>这个词。</p>
<p>在并发程序中可以同时拥有两个或者多个线程。这意味着，如果程序在单核处理器上运行，那么这两个线程将交替地换入或者换出内存。这些线程是同时“存在”的——每个线程都处于执行过程中的某个状态。如果程序能够并行执行，那么就一定是运行在多核处理器上。此时，程序中的每个线程都将分配到一个独立的处理器核上，因此可以同时运行。</p>
<p>我相信你已经能够得出结论——<strong>“并行”概念是“并发”概念的一个子集</strong>。也就是说，你可以编写一个拥有多个线程或者进程的并发程序，但如果没有多核处理器来执行这个程序，那么就不能以并行方式来运行代码。因此，凡是在求解单个问题时涉及多个执行流程的编程模式或者执行行为，都属于并发编程的范畴。</p>
<p>摘自：《并发的艺术》 — 〔美〕布雷谢斯 (这一段是,上面两段不是)</p>
</blockquote>
<p>可以看出,我们讨论多线程编程是在讨论并发编程.当然java也能做并行,但现在不讨论.</p>
<h2 id="并发值得吗"><a href="#并发值得吗" class="headerlink" title="并发值得吗"></a>并发值得吗</h2><p>换句话说,多线程能提高效率吗?</p>
<p>举个栗子:</p>
<p>两个小孩吃两碗饭,只有一双筷子,他两排队吃和轮流吃的时间是一样的(忽略交换筷子等细微时间),因为我不管你怎么吃,人就两个人,饭也是两碗饭,就只有一双筷子,最终时间都是一样的.从这个例子来看,<strong>仅仅从完成任务本身</strong>,并发没有多大意义.但是,</p>
<p>换个例子:</p>
<p>挖隧道,从两头向中间挖,只有一个工人,如果让他两头跑着挖显然没有从一头挖快,中间有赶路的时间耽搁.变一下,工人要干五个小时要休息一个小时(类比CPU等待I/O资源),把赶路的时间算在休息时间里面,这样就使得两头挖的时间短一点.(可能例子不太合适)</p>
<p>总之,多线程有没有提升效率要视情况而定,纯粹考虑计算效率没什么太大意义,就像下面我要用的例子,英雄加血和减血使用多线程的效率没准比不用还低,但是你不能不用.</p>
<p>创建多线程有3种方式，分别是</p>
<ul>
<li>继承线程类</li>
<li>实现Runnable接口</li>
<li>匿名类</li>
</ul>
<h1 id="一个线程的生命周期"><a href="#一个线程的生命周期" class="headerlink" title="一个线程的生命周期"></a>一个线程的生命周期</h1><p>线程是一个动态执行的过程，它也有一个从产生到死亡的过程。</p>
<p>下图显示了一个线程完整的生命周期。</p>
<p><img src="/2018/08/13/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/java-thread.jpg" alt></p>
<ul>
<li><p>新建状态:</p>
<p>使用 <strong>new</strong> 关键字和 <strong>Thread</strong> 类或其子类建立一个线程对象后，该线程对象就处于新建状态。它保持这个状态直到程序 <strong>start()</strong>这个线程。</p>
</li>
<li><p>就绪状态:</p>
<p>当线程对象调用了start()方法之后，该线程就进入就绪状态。就绪状态的线程处于就绪队列中，要等待JVM里线程调度器的调度。</p>
</li>
<li><p>运行状态:</p>
<p>如果就绪状态的线程获取 CPU 资源，就可以执行 <strong>run()</strong>，此时线程便处于运行状态。处于运行状态的线程最为复杂，它可以变为阻塞状态、就绪状态和死亡状态。</p>
</li>
<li><p>阻塞状态:</p>
<p>如果一个线程执行了sleep(睡眠）、suspend(挂起）等方法，失去所占用资源之后，该线程就从运行状态进入阻塞状态。在睡眠时间已到或获得设备资源后可以重新进入就绪状态。可以分为三种：</p>
<ul>
<li>等待阻塞：运行状态中的线程执行 wait()方法，使线程进入到等待阻塞状态。</li>
<li>同步阻塞：线程在获取 synchronized 同步锁失败(因为同步锁被其他线程占用)。</li>
<li>其他阻塞：通过调用线程的 sleep() 或 join()发出了 I/O 请求时，线程就会进入到阻塞状态。当sleep() 状态超时，join()等待线程终止或超时，或者 I/O 处理完毕，线程重新转入就绪状态。</li>
</ul>
</li>
<li><p>死亡状态:</p>
<p>一个运行状态的线程完成任务或者其他终止条件发生时，该线程就切换到终止状态</p>
</li>
</ul>
<p><img src="/2018/08/13/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/state.jpg" alt></p>
<h1 id="线程的优先级"><a href="#线程的优先级" class="headerlink" title="线程的优先级"></a>线程的优先级</h1><p>每一个 Java 线程都有一个优先级，这样有助于操作系统确定线程的调度顺序。</p>
<p>Java 线程的优先级是一个整数，其取值范围是 1 (Thread.MIN_PRIORITY） - 10 (Thread.MAX_PRIORITY）。</p>
<p>默认情况下，每一个线程都会分配一个优先级 NORM_PRIORITY(5）。</p>
<p>具有较高优先级的线程对程序更重要，并且应该在低优先级的线程之前分配处理器资源。但是，线程优先级不能保证线程执行的顺序，而且非常依赖于平台。</p>
<h1 id="创建一个线程"><a href="#创建一个线程" class="headerlink" title="创建一个线程"></a>创建一个线程</h1><h2 id="继承线程类"><a href="#继承线程类" class="headerlink" title="继承线程类"></a>继承线程类</h2><p>先放出Hero类的代码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> charactor;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.io.Serializable;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Hero</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">float</span> hp;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> damage;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">attackHero</span><span class="params">(Hero h)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">//为了表示攻击需要时间，每次攻击暂停1000毫秒</span></span><br><span class="line">            Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        h.hp-=damage;</span><br><span class="line">        System.out.format(<span class="string">"%s 正在攻击 %s, %s的血变成了 %.0f%n"</span>,name,h.name,h.name,h.hp);</span><br><span class="line">         </span><br><span class="line">        <span class="keyword">if</span>(h.isDead())</span><br><span class="line">            System.out.println(h.name +<span class="string">"死了！"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isDead</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>&gt;=hp?<span class="keyword">true</span>:<span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用多线程，就可以做到盖伦在攻击提莫的同时，赏金猎人也在攻击盲僧</p>
<p>设计一个类KillThread继承Thread，并且重写run方法</p>
<p>启动线程办法：实例化一个KillThread对象，并且调用其start方法</p>
<p>就可以观察到赏金猎人攻击盲僧的同时，盖伦也在攻击提莫.</p>
<p>设计一个KillThread,继承了Thread,重写了run()方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">KillThread</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">private</span> Hero h1;</span><br><span class="line">    <span class="keyword">private</span> Hero h2;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">KillThread</span><span class="params">(Hero h1, Hero h2)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.h1 = h1;</span><br><span class="line">        <span class="keyword">this</span>.h2 = h2;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">while</span>(!h2.isDead())&#123;</span><br><span class="line">            h1.attackHero(h2);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>创建四个Hero,并实例化两个线程,第一个线程盖伦打提莫,第二个线程赏金猎人打盲僧:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">         </span><br><span class="line">        Hero gareen = <span class="keyword">new</span> Hero();</span><br><span class="line">        gareen.name = <span class="string">"盖伦"</span>;</span><br><span class="line">        gareen.hp = <span class="number">616</span>;</span><br><span class="line">        gareen.damage = <span class="number">50</span>;</span><br><span class="line"> </span><br><span class="line">        Hero teemo = <span class="keyword">new</span> Hero();</span><br><span class="line">        teemo.name = <span class="string">"提莫"</span>;</span><br><span class="line">        teemo.hp = <span class="number">300</span>;</span><br><span class="line">        teemo.damage = <span class="number">30</span>;</span><br><span class="line">         </span><br><span class="line">        Hero bh = <span class="keyword">new</span> Hero();</span><br><span class="line">        bh.name = <span class="string">"赏金猎人"</span>;</span><br><span class="line">        bh.hp = <span class="number">500</span>;</span><br><span class="line">        bh.damage = <span class="number">65</span>;</span><br><span class="line">         </span><br><span class="line">        Hero leesin = <span class="keyword">new</span> Hero();</span><br><span class="line">        leesin.name = <span class="string">"盲僧"</span>;</span><br><span class="line">        leesin.hp = <span class="number">455</span>;</span><br><span class="line">        leesin.damage = <span class="number">80</span>;</span><br><span class="line">         </span><br><span class="line">        KillThread killThread1 = <span class="keyword">new</span> KillThread(gareen,teemo);</span><br><span class="line">        killThread1.start();</span><br><span class="line">        KillThread killThread2 = <span class="keyword">new</span> KillThread(bh,leesin);</span><br><span class="line">        killThread2.start();</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>另一个例子:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ThreadDemo</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">   <span class="keyword">private</span> Thread t;</span><br><span class="line">   <span class="keyword">private</span> String threadName;</span><br><span class="line">   </span><br><span class="line">   ThreadDemo( String name) &#123;</span><br><span class="line">      threadName = name;</span><br><span class="line">      System.out.println(<span class="string">"Creating "</span> +  threadName );</span><br><span class="line">   &#125;</span><br><span class="line">   </span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">      System.out.println(<span class="string">"Running "</span> +  threadName );</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">4</span>; i &gt; <span class="number">0</span>; i--) &#123;</span><br><span class="line">            System.out.println(<span class="string">"Thread: "</span> + threadName + <span class="string">", "</span> + i);</span><br><span class="line">            <span class="comment">// 让线程睡眠一会</span></span><br><span class="line">            Thread.sleep(<span class="number">50</span>);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;<span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">         System.out.println(<span class="string">"Thread "</span> +  threadName + <span class="string">" interrupted."</span>);</span><br><span class="line">      &#125;</span><br><span class="line">      System.out.println(<span class="string">"Thread "</span> +  threadName + <span class="string">" exiting."</span>);</span><br><span class="line">   &#125;</span><br><span class="line">   </span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">start</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">      System.out.println(<span class="string">"Starting "</span> +  threadName );</span><br><span class="line">      <span class="keyword">if</span> (t == <span class="keyword">null</span>) &#123;</span><br><span class="line">         t = <span class="keyword">new</span> Thread (<span class="keyword">this</span>, threadName);</span><br><span class="line">         t.start ();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String args[])</span> </span>&#123;</span><br><span class="line">      ThreadDemo T1 = <span class="keyword">new</span> ThreadDemo( <span class="string">"Thread-1"</span>);</span><br><span class="line">      T1.start();</span><br><span class="line">      </span><br><span class="line">      ThreadDemo T2 = <span class="keyword">new</span> ThreadDemo( <span class="string">"Thread-2"</span>);</span><br><span class="line">      T2.start();</span><br><span class="line">   &#125;   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="实现Runable接口"><a href="#实现Runable接口" class="headerlink" title="实现Runable接口"></a>实现Runable接口</h2><p>创建类Battle，实现Runnable接口</p>
<p>启动的时候，首先创建一个Battle对象，然后再根据该battle对象创建一个线程对象，并启动</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Battle battle1 = <span class="keyword">new</span> Battle(gareen,teemo);</span><br><span class="line"><span class="keyword">new</span> Thread(battle1).start();</span><br></pre></td></tr></table></figure>

<p>battle1对象实现了Runnable接口，所以有run方法，但是<strong>直接调用run方法，并不会启动一个新的线程。</strong></p>
<p>必须，借助一个线程对象的start()方法，才会启动一个新的线程。</p>
<p>所以，在创建Thread对象的时候，把battle1作为构造方法的参数传递进去，这个线程启动的时候，就会去执行battle1.run()方法了。 </p>
<p>Battle类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Battle</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">private</span> Hero h1;</span><br><span class="line">    <span class="keyword">private</span> Hero h2;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Battle</span><span class="params">(Hero h1, Hero h2)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.h1 = h1;</span><br><span class="line">        <span class="keyword">this</span>.h2 = h2;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">while</span>(!h2.isDead())&#123;</span><br><span class="line">            h1.attackHero(h2);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">         </span><br><span class="line">        Hero gareen = <span class="keyword">new</span> Hero();</span><br><span class="line">        gareen.name = <span class="string">"盖伦"</span>;</span><br><span class="line">        gareen.hp = <span class="number">616</span>;</span><br><span class="line">        gareen.damage = <span class="number">50</span>;</span><br><span class="line"> </span><br><span class="line">        Hero teemo = <span class="keyword">new</span> Hero();</span><br><span class="line">        teemo.name = <span class="string">"提莫"</span>;</span><br><span class="line">        teemo.hp = <span class="number">300</span>;</span><br><span class="line">        teemo.damage = <span class="number">30</span>;</span><br><span class="line">         </span><br><span class="line">        Hero bh = <span class="keyword">new</span> Hero();</span><br><span class="line">        bh.name = <span class="string">"赏金猎人"</span>;</span><br><span class="line">        bh.hp = <span class="number">500</span>;</span><br><span class="line">        bh.damage = <span class="number">65</span>;</span><br><span class="line">         </span><br><span class="line">        Hero leesin = <span class="keyword">new</span> Hero();</span><br><span class="line">        leesin.name = <span class="string">"盲僧"</span>;</span><br><span class="line">        leesin.hp = <span class="number">455</span>;</span><br><span class="line">        leesin.damage = <span class="number">80</span>;</span><br><span class="line">         </span><br><span class="line">        Battle battle1 = <span class="keyword">new</span> Battle(gareen,teemo);</span><br><span class="line">         </span><br><span class="line">        <span class="keyword">new</span> Thread(battle1).start();</span><br><span class="line"> </span><br><span class="line">        Battle battle2 = <span class="keyword">new</span> Battle(bh,leesin);</span><br><span class="line">        <span class="keyword">new</span> Thread(battle2).start();</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从本质上来说,通过继承Thread实现多线程也是实现Runnable接口,因为Thread实现了Runnable.而Runnable是一个函数式接口,可以使用匿名类来创建线程.</p>
<h2 id="匿名类"><a href="#匿名类" class="headerlink" title="匿名类"></a>匿名类</h2><p>使用匿名类，继承Thread,重写run方法，直接在run方法中写业务代码</p>
<p>匿名类的一个好处是可以很方便的访问外部的局部变量。 前提是外部的局部变量需要被声明为final。(JDK7以后就不需要了)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">          </span><br><span class="line">        Hero gareen = <span class="keyword">new</span> Hero();</span><br><span class="line">        gareen.name = <span class="string">"盖伦"</span>;</span><br><span class="line">        gareen.hp = <span class="number">616</span>;</span><br><span class="line">        gareen.damage = <span class="number">50</span>;</span><br><span class="line">  </span><br><span class="line">        Hero teemo = <span class="keyword">new</span> Hero();</span><br><span class="line">        teemo.name = <span class="string">"提莫"</span>;</span><br><span class="line">        teemo.hp = <span class="number">300</span>;</span><br><span class="line">        teemo.damage = <span class="number">30</span>;</span><br><span class="line">          </span><br><span class="line">        Hero bh = <span class="keyword">new</span> Hero();</span><br><span class="line">        bh.name = <span class="string">"赏金猎人"</span>;</span><br><span class="line">        bh.hp = <span class="number">500</span>;</span><br><span class="line">        bh.damage = <span class="number">65</span>;</span><br><span class="line">          </span><br><span class="line">        Hero leesin = <span class="keyword">new</span> Hero();</span><br><span class="line">        leesin.name = <span class="string">"盲僧"</span>;</span><br><span class="line">        leesin.hp = <span class="number">455</span>;</span><br><span class="line">        leesin.damage = <span class="number">80</span>;</span><br><span class="line">          </span><br><span class="line">        <span class="comment">//匿名类</span></span><br><span class="line">        Thread t1= <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                <span class="comment">//匿名类中用到外部的局部变量teemo，必须把teemo声明为final</span></span><br><span class="line">                <span class="comment">//但是在JDK7以后，就不是必须加final的了</span></span><br><span class="line">                <span class="keyword">while</span>(!teemo.isDead())&#123;</span><br><span class="line">                    gareen.attackHero(teemo);</span><br><span class="line">                &#125;              </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">         </span><br><span class="line">        t1.start();</span><br><span class="line">          </span><br><span class="line">        Thread t2= <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                <span class="keyword">while</span>(!leesin.isDead())&#123;</span><br><span class="line">                    bh.attackHero(leesin);</span><br><span class="line">                &#125;              </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t2.start();</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">      </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Thread和Runnable的区别"><a href="#Thread和Runnable的区别" class="headerlink" title="Thread和Runnable的区别"></a>Thread和Runnable的区别</h2><p>如果一个类继承Thread，则不适合资源共享。但是如果实现了Runable接口的话，则很容易的实现资源共享。</p>
<p><strong>总结：</strong></p>
<p><strong>实现Runnable接口比继承Thread类所具有的优势：</strong></p>
<p><strong>1）：适合多个相同的程序代码的线程去处理同一个资源</strong></p>
<p><strong>2）：可以避免java中的单继承的限制</strong></p>
<p><strong>3）：增加程序的健壮性，代码可以被多个线程共享，代码和数据独立</strong></p>
<p><strong>4）：线程池只能放入实现Runable或callable类线程，不能直接放入继承Thread的类</strong></p>
<p><strong>提醒一下大家：main方法其实也是一个线程。在java中所以的线程都是同时启动的，至于什么时候，哪个先执行，完全看谁先得到CPU的资源。</strong></p>
<p><strong>在java中，每次程序运行至少启动2个线程。一个是main线程，一个是垃圾收集线程。因为每当使用java命令执行一个类的时候，实际上都会启动一个ＪＶＭ，每一个ｊＶＭ实习在就是在操作系统中启动了一个进程。</strong></p>
<h1 id="常见线程方法"><a href="#常见线程方法" class="headerlink" title="常见线程方法"></a>常见线程方法</h1><h2 id="当前线程暂停"><a href="#当前线程暂停" class="headerlink" title="当前线程暂停"></a>当前线程暂停</h2><p>Thread.sleep(1000); 表示当前线程暂停1000毫秒 ，其他线程不受影响  Thread.sleep(1000); 会抛出InterruptedException 中断异常，因为当前线程sleep的时候，有可能被停止，这时就会抛出InterruptedException</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">         </span><br><span class="line">        Thread t1= <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                <span class="keyword">int</span> seconds =<span class="number">0</span>;</span><br><span class="line">                <span class="keyword">while</span>(<span class="keyword">true</span>)&#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.printf(<span class="string">"已经玩了LOL %d 秒%n"</span>, seconds++);</span><br><span class="line">                &#125;              </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t1.start();</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="加入到当前线程中"><a href="#加入到当前线程中" class="headerlink" title="加入到当前线程中"></a>加入到当前线程中</h2><p>首先解释一下主线程的概念</p>
<p>所有进程，至少会有一个线程即主线程，即main方法开始执行，就会有一个看不见的主线程存在。 </p>
<p>在42行执行t.join，即表明在主线程中加入该线程。主线程会等待该线程结束完毕，才会往下运行。 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">final</span> Hero gareen = <span class="keyword">new</span> Hero();</span><br><span class="line">        gareen.name = <span class="string">"盖伦"</span>;</span><br><span class="line">        gareen.hp = <span class="number">616</span>;</span><br><span class="line">        gareen.damage = <span class="number">50</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">final</span> Hero teemo = <span class="keyword">new</span> Hero();</span><br><span class="line">        teemo.name = <span class="string">"提莫"</span>;</span><br><span class="line">        teemo.hp = <span class="number">300</span>;</span><br><span class="line">        teemo.damage = <span class="number">30</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">final</span> Hero bh = <span class="keyword">new</span> Hero();</span><br><span class="line">        bh.name = <span class="string">"赏金猎人"</span>;</span><br><span class="line">        bh.hp = <span class="number">500</span>;</span><br><span class="line">        bh.damage = <span class="number">65</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">final</span> Hero leesin = <span class="keyword">new</span> Hero();</span><br><span class="line">        leesin.name = <span class="string">"盲僧"</span>;</span><br><span class="line">        leesin.hp = <span class="number">455</span>;</span><br><span class="line">        leesin.damage = <span class="number">80</span>;</span><br><span class="line"></span><br><span class="line">        Thread t1= <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            System.out.println(Thread.currentThread().getName()+<span class="string">" start"</span>);</span><br><span class="line">            <span class="keyword">while</span>(!teemo.isDead())&#123;</span><br><span class="line">                gareen.attackHero(teemo);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        t1.start();</span><br><span class="line">        System.out.println(Thread.currentThread().getName()+<span class="string">" start"</span>);</span><br><span class="line">        <span class="comment">//代码执行到这里，一直是main线程在运行</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">//t1线程加入到main线程中来，只有t1线程运行结束，才会继续往下走</span></span><br><span class="line">            t1.join();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(Thread.currentThread().getName()+<span class="string">" start"</span>);</span><br><span class="line">        Thread t2= <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                System.out.println(Thread.currentThread().getName()+<span class="string">" start"</span>);</span><br><span class="line">                <span class="keyword">while</span>(!leesin.isDead())&#123;</span><br><span class="line">                    bh.attackHero(leesin);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="comment">//会观察到盖伦把提莫杀掉后，才运行t2线程</span></span><br><span class="line">        t2.start();</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>另一个例子:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Thread1</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Thread1</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(name);</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(Thread.currentThread().getName() + <span class="string">" 线程运行开始!"</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">            System.out.println(<span class="string">"子线程"</span> + name + <span class="string">"运行 : "</span> + i);</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                sleep((<span class="keyword">int</span>) Math.random() * <span class="number">10</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(Thread.currentThread().getName() + <span class="string">" 线程运行结束!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread2</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        System.out.println(Thread.currentThread().getName() + <span class="string">"主线程运行开始!"</span>);</span><br><span class="line">        Thread1 mTh1 = <span class="keyword">new</span> Thread1(<span class="string">"A"</span>);</span><br><span class="line">        Thread1 mTh2 = <span class="keyword">new</span> Thread1(<span class="string">"B"</span>);</span><br><span class="line">        mTh1.start();</span><br><span class="line">        mTh2.start();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mTh1.join();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mTh2.join();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(Thread.currentThread().getName() + <span class="string">"主线程运行结束!"</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>特别注意:join()不是表示子线程开始,而是表示主线程在这里等子线程!</strong>可能在join之前子线程已经运行完了,join是用来确保主线程比子线程后结束.</p>
<h2 id="线程优先级"><a href="#线程优先级" class="headerlink" title="线程优先级"></a>线程优先级</h2><p>当线程处于竞争关系的时候，优先级高的线程会有更大的几率获得CPU资源 .</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">t1.setPriority(Thread.MAX_PRIORITY);</span><br><span class="line">t2.setPriority(Thread.MIN_PRIORITY);</span><br></pre></td></tr></table></figure>

<h2 id="临时暂停"><a href="#临时暂停" class="headerlink" title="临时暂停"></a>临时暂停</h2><p>当前线程，临时暂停，使得其他线程可以有更多的机会占用CPU资源.仅仅是给了一次机会,有可能暂停完了还是原来那个线程继续执行.并且相同或更高优先级更有可能得到这个机会,低优先级是没有机会的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ThreadYield</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ThreadYield</span><span class="params">(String name)</span> </span>&#123;  </span><br><span class="line">        <span class="keyword">super</span>(name);  </span><br><span class="line">    &#125;  </span><br><span class="line">   </span><br><span class="line">    <span class="meta">@Override</span>  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">50</span>; i++) &#123;  </span><br><span class="line">            System.out.println(<span class="string">""</span> + <span class="keyword">this</span>.getName() + <span class="string">"-----"</span> + i);  </span><br><span class="line">            <span class="comment">// 当i为30时，该线程就会把CPU时间让掉，让其他或者自己的线程执行(也就是谁先抢到谁执行）  </span></span><br><span class="line">            <span class="keyword">if</span> (i ==<span class="number">30</span>) &#123;  </span><br><span class="line">                <span class="keyword">this</span>.yield();  </span><br><span class="line">            &#125;  </span><br><span class="line">        &#125;  </span><br><span class="line">      </span><br><span class="line">&#125;  </span><br><span class="line">&#125;  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;  </span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;  </span><br><span class="line">          </span><br><span class="line">        ThreadYield yt1 = <span class="keyword">new</span> ThreadYield(<span class="string">"张三"</span>);  </span><br><span class="line">        ThreadYield yt2 = <span class="keyword">new</span> ThreadYield(<span class="string">"李四"</span>);  </span><br><span class="line">        yt1.start();  </span><br><span class="line">        yt2.start();  </span><br><span class="line">    &#125;  </span><br><span class="line">  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：</p>
<p>第一种情况：李四(线程）当执行到30时会CPU时间让掉，这时张三(线程）抢到CPU时间并执行。</p>
<p>第二种情况：李四(线程）当执行到30时会CPU时间让掉，这时李四(线程）抢到CPU时间并执行。</p>
<p><strong>sleep()和yield()的区别</strong></p>
<p>sleep()和yield()的区别:sleep()使当前线程进入停滞状态，所以执行sleep()的线程在指定的时间内肯定不会被执行；yield()只是使当前线程重新回到可执行状态(看开始的第二张图)，所以执行yield()的线程有可能在进入到可执行状态后马上又被执行。 </p>
<p>sleep方法使当前运行中的线程睡眼一段时间，进入不可运行状态，这段时间的长短是由程序设定的，yield方法使当前线程让出CPU占有权，但让出的时间是不可设定的。实际上，yield()方法对应了如下操作：先检测当前是否有相同优先级的线程处于同可运行状态，如有，则把CPU 的占有权交给此线程，否则，继续运行原来的线程。所以yield()方法称为“退让”，它把运行机会让给了同等优先级的其他线程.</p>
<p>另外，sleep 方法允许较低优先级的线程获得运行机会，但yield()方法执行时，当前线程仍处在可运行状态，所以，不可能让出较低优先级的线程些时获得 CPU 占有权。在一个运行系统中，如果较高优先级的线程没有调用sleep方法，又没有受到 I\O 阻塞，那么，较低优先级线程只能等待所有较高优先级的线程运行结束，才有机会运行。 </p>
<h2 id="守护线程"><a href="#守护线程" class="headerlink" title="守护线程"></a>守护线程</h2><p>守护线程的概念是:当一个进程里，所有的线程都是守护线程的时候，结束当前进程。 </p>
<p>就好像一个公司有销售部，生产部这些和业务挂钩的部门。除此之外，还有后勤，行政等这些支持部门。 如果一家公司销售部，生产部都解散了，那么只剩下后勤和行政，那么这家公司也可以解散了。 </p>
<p>守护线程就相当于那些支持部门，如果一个进程只剩下守护线程，那么进程就会自动结束。 守护线程通常会被用来做日志，性能统计等工作。这么看来守护线程叫做支持线程也不错呢.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">          </span><br><span class="line">        Thread t1= <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                <span class="keyword">int</span> seconds =<span class="number">0</span>;</span><br><span class="line">                 </span><br><span class="line">                <span class="keyword">while</span>(<span class="keyword">true</span>)&#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.printf(<span class="string">"已经玩了LOL %d 秒%n"</span>, seconds++);</span><br><span class="line">                     </span><br><span class="line">                &#125;              </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t1.setDaemon(<span class="keyword">true</span>);</span><br><span class="line">        t1.start();</span><br><span class="line">          </span><br><span class="line">    &#125;</span><br><span class="line">      </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码不会有任何结果.</p>
<h1 id="同步问题"><a href="#同步问题" class="headerlink" title="同步问题"></a>同步问题</h1><h2 id="原因分析"><a href="#原因分析" class="headerlink" title="原因分析"></a>原因分析</h2><p>当多个线程同时修改一个数据时有可能产生同步问题.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread3</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">final</span> Hero gareen = <span class="keyword">new</span> Hero();</span><br><span class="line">        gareen.name = <span class="string">"盖伦"</span>;</span><br><span class="line">        gareen.hp = <span class="number">10000</span>;</span><br><span class="line"></span><br><span class="line">        System.out.printf(<span class="string">"盖伦的初始血量是 %.0f%n"</span>, gareen.hp);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//多线程同步问题指的是多个线程同时修改一个数据的时候，导致的问题</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//假设盖伦有10000滴血，并且在基地里，同时又被对方多个英雄攻击</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//用JAVA代码来表示，就是有多个线程在减少盖伦的hp</span></span><br><span class="line">        <span class="comment">//同时又有多个线程在恢复盖伦的hp</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//n个线程增加盖伦的hp</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> n = <span class="number">10000</span>;</span><br><span class="line"></span><br><span class="line">        Thread[] addThreads = <span class="keyword">new</span> Thread[n];</span><br><span class="line">        Thread[] reduceThreads = <span class="keyword">new</span> Thread[n];</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            Thread t = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">                gareen.recover();</span><br><span class="line">                System.out.println(Thread.currentThread().getName());</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">            t.start();</span><br><span class="line">            addThreads[i] = t;</span><br><span class="line">            <span class="comment">//System.out.println(Thread.currentThread().getName());</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//n个线程减少盖伦的hp</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            Thread t = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">                System.out.println(Thread.currentThread().getName());</span><br><span class="line">                gareen.hurt();</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">            t.start();</span><br><span class="line">            reduceThreads[i] = t;</span><br><span class="line">            <span class="comment">//System.out.println(Thread.currentThread().getName());</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//等待所有增加线程结束</span></span><br><span class="line">        <span class="keyword">for</span> (Thread t : addThreads) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t.join();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//等待所有减少线程结束</span></span><br><span class="line">        <span class="keyword">for</span> (Thread t : reduceThreads) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t.join();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//代码执行到这里，所有增加和减少线程都结束了</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//增加和减少线程的数量是一样的，每次都增加，减少1.</span></span><br><span class="line">        <span class="comment">//那么所有线程都结束后，盖伦的hp应该还是初始值</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//但是事实上观察到的是：</span></span><br><span class="line"></span><br><span class="line">        System.out.printf(<span class="string">"%d个增加线程和%d个减少线程结束后%n盖伦的血量变成了 %.0f%n"</span>, n, n, gareen.hp);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>10000个加血线程和10000个减血线程对hp操作,结果有可能不是10000.<img src="/2018/08/13/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/787.png" alt></p>
<p>考虑这种极端情况:增加线程加血还未完成,减血线程却读取了hp值1000,进行减血,这是加血线程将hp改为10001,然后减血线程改为9999.</p>
<p>对于上面的代码还引起了我一点思考,两个for循环明明有先后顺序,也就是先增加加血线程10000个,再增加减血线程1000个,为什么执行时却是两种线程交替呢?</p>
<p>经过仔细思考我终于后知后觉明白了多线程的意义,并且初步搞清了上面的问题,看下面:</p>
<h2 id="再看六种线程状态"><a href="#再看六种线程状态" class="headerlink" title="再看六种线程状态"></a>再看六种线程状态</h2><p>不同于文章开头的大致分法,关于线程状态的准确说法是下面六种(线程的状态是用枚举值来描述的,严格讲就6种枚举值,但是便于理解时几种状态的说法略有不同.):</p>
<ol>
<li>初始(NEW):新创建了一个线程对象，但还没有调用start()方法。</li>
<li>运行(RUNNABLE):Java线程中将就绪(ready)和运行中(running）两种状态笼统的成为“运行”。线程对象创建后，其他线程(比如main线程）调用了该对象的start()方法。该状态的线程位于可运行线程池中，等待被线程调度选中，获取cpu 的使用权，此时处于就绪状态(ready）。就绪状态的线程在获得cpu 时间片后变为运行中状态(running）。</li>
<li>阻塞(BLOCKED)：表示线程阻塞于锁。</li>
<li>等待(WAITING)：进入该状态的线程需要等待其他线程做出一些特定动作(通知或中断）。</li>
<li>超时等待(TIME_WAITING)：该状态不同于WAITING，它可以在指定的时间内自行返回。</li>
<li>终止(TERMINATED)：表示该线程已经执行完毕。 </li>
</ol>
<p>另一个版本的状态图,实际没有差别:</p>
<p><img src="/2018/08/13/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/2018070117435683.jpeg" alt></p>
<p>回到上面的问题:受到那张图的影响,我以为20000个线程全是交替进行的.其实压根不是,我多多线程没理解.由于学艺不精,想不到什么查看线程信息的方法,就用了个笨办法,在recover()方法和hurt()方法中都加了一句<code>System.out.println(Thread.currentThread().getName()+&quot;recover&quot;);</code>这样当每个线程执行时,就能知道大概的顺序.结果是在第一个减血线程执行之前,只存在加血线程,当出现了第一个减血线程后(大约在一般位置),加血逐渐减少,减血逐渐增多,然后只有减血.同步问题就出现在两者都存在的时候.</p>
<p>这样的情况应该才是正确的,第一个for循环中,我假定start()后部分加血线程立即开始执行,其他的肯定会有部分来不及执行,这时第二个for循环开始,减血线程加进来了,这就造成两种线程混在一起.</p>
<h2 id="解决思路"><a href="#解决思路" class="headerlink" title="解决思路"></a>解决思路</h2><p>解决同步问题的思路是:在加血线程操作hp时,禁止减血线程操作,如下图:</p>
<p><img src="/2018/08/13/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/788.png" alt></p>
<p>但是这又引起了我一个思考:既然是一个线程执行完加血操作后在进行减血线程,那么本质上线程是轮替工作的,既然如此还要多线程干嘛?</p>
<p>假定下面的情况:</p>
<p><img src="/2018/08/13/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/%E4%BB%BB%E5%8A%A1.png" alt></p>
<p>情况一:十个线程各自操作各自的对象,没有干扰</p>
<p>在这种情况下,不存在同步问题,每个线程有十个任务.如果不进行多线程,就只能每个对象挨个来,就像银行只开一个窗口,所有人都要排队等,显然不合适,多线程在这里的意义是,先执行线程一的任务一,然后轮换给线程二的任务一,就像银行开了十个窗口,但是只有一个工作人员(CPU核数不足),这个工作人员在十个窗口间来回跑动,给十个对象服务.</p>
<p>我原来有个错误认知,以为多线程可以加快程序速度(同时运行),但实际只是将顺序执行变成了交替执行,它的目的是造成一种同时运行的假象.原本我画三张画,后面的人就是在干等,我用多线程的话,我就能先给后面的人一张草图,而最终两种方式的时间是一样的.</p>
<p><img src="/2018/08/13/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/%E4%BB%BB%E5%8A%A12.png" alt></p>
<p>情况二:只有一个对象</p>
<p>用前面英雄加血回血的例子,复杂化,十个线程包括回血,减血,各种buff和debuff.显然不可以让它们依次执行,比如要减100的血,加150的血,加一个迟滞光环和加速光环,那么先减20的血,然后切换线程加buff,再切换线程加debuff,再切换线程加100的血,回到减血那减80,再加50.而且要避免加血和减血同时操作,要让hp是当前线程独占的.</p>
<h2 id="synchronized同步对象概念"><a href="#synchronized同步对象概念" class="headerlink" title="synchronized同步对象概念"></a>synchronized同步对象概念</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Object someObject =<span class="keyword">new</span> Object();</span><br><span class="line"><span class="keyword">synchronized</span> (someObject)&#123;</span><br><span class="line">  <span class="comment">//此处的代码只有占有了someObject后才可以执行</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>synchronized表示当前线程独占对象someObject</p>
<p>当前线程独占了对象someObject，如果有其他线程试图占有对象someObject，就会等待，直到当前线程释放对someObject的占用。someObject又叫同步对象，所有的对象，都可以作为同步对象</p>
<p>释放同步对象的方式：synchronized块自然结束，或者有异常抛出</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.text.SimpleDateFormat;</span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> String <span class="title">now</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> SimpleDateFormat(<span class="string">"HH:mm:ss"</span>).format(<span class="keyword">new</span> Date());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> Object someObject = <span class="keyword">new</span> Object();</span><br><span class="line"></span><br><span class="line">        Thread t1 = <span class="keyword">new</span> Thread() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    System.out.println(now() + <span class="string">" t1 线程已经运行"</span>);</span><br><span class="line">                    System.out.println(now() + <span class="keyword">this</span>.getName() + <span class="string">" 试图占有对象：someObject"</span>);</span><br><span class="line">                    <span class="keyword">synchronized</span> (someObject) &#123;</span><br><span class="line"></span><br><span class="line">                        System.out.println(now() + <span class="keyword">this</span>.getName() + <span class="string">" 占有对象：someObject"</span>);</span><br><span class="line">                        Thread.sleep(<span class="number">5000</span>);</span><br><span class="line">                        System.out.println(now() + <span class="keyword">this</span>.getName() + <span class="string">" 释放对象：someObject"</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.println(now() + <span class="string">" t1 线程结束"</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t1.setName(<span class="string">" t1"</span>);</span><br><span class="line">        t1.start();</span><br><span class="line">        System.out.println(now() + <span class="string">" t1线程已初始化"</span>);</span><br><span class="line">        Thread t2 = <span class="keyword">new</span> Thread() &#123;</span><br><span class="line"></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    System.out.println(now() + <span class="string">" t2 线程已经运行"</span>);</span><br><span class="line">                    System.out.println(now() + <span class="keyword">this</span>.getName() + <span class="string">" 试图占有对象：someObject"</span>);</span><br><span class="line">                    <span class="keyword">synchronized</span> (someObject) &#123;</span><br><span class="line">                        System.out.println(now() + <span class="keyword">this</span>.getName() + <span class="string">" 占有对象：someObject"</span>);</span><br><span class="line">                        Thread.sleep(<span class="number">5000</span>);</span><br><span class="line">                        System.out.println(now() + <span class="keyword">this</span>.getName() + <span class="string">" 释放对象：someObject"</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.println(now() + <span class="string">" t2 线程结束"</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t2.setName(<span class="string">" t2"</span>);</span><br><span class="line">        t2.start();</span><br><span class="line">        System.out.println(now() + <span class="string">" t2线程已初始化"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果是:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">08</span> t1线程已初始化</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">08</span> t1 线程已经运行</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">08</span> t2线程已初始化</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">08</span> t2 线程已经运行</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">08</span> t2 试图占有对象：someObject</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">08</span> t1 试图占有对象：someObject</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">08</span> t2 占有对象：someObject</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">13</span> t2 释放对象：someObject</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">13</span> t2 线程结束</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">13</span> t1 占有对象：someObject</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">18</span> t1 释放对象：someObject</span><br><span class="line"><span class="number">16</span>:<span class="number">03</span>:<span class="number">18</span> t1 线程结束</span><br></pre></td></tr></table></figure>

<p>上面的结果符合预期,但是偶尔有几次发现第一行和第二行结果颠倒了,运行在初始化之前,仔细检查后发现,问题出在:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">t1.start();</span><br><span class="line">System.out.println(now() + <span class="string">" t1线程已初始化"</span>);</span><br></pre></td></tr></table></figure>

<p>有可能出现t1线程start后开始执行run()方法,结果这是主线程的代码还没跑到第二行,所以应该把上面两行颠倒.</p>
<h3 id="几种用法"><a href="#几种用法" class="headerlink" title="几种用法"></a>几种用法</h3><h4 id="使用synchronized解决同步问题"><a href="#使用synchronized解决同步问题" class="headerlink" title="使用synchronized解决同步问题"></a>使用synchronized解决同步问题</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line">   </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">final</span> Object someObject = <span class="keyword">new</span> Object();</span><br><span class="line">         </span><br><span class="line">        <span class="keyword">final</span> Hero gareen = <span class="keyword">new</span> Hero();</span><br><span class="line">        gareen.name = <span class="string">"盖伦"</span>;</span><br><span class="line">        gareen.hp = <span class="number">10000</span>;</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">int</span> n = <span class="number">10000</span>;</span><br><span class="line">  </span><br><span class="line">        Thread[] addThreads = <span class="keyword">new</span> Thread[n];</span><br><span class="line">        Thread[] reduceThreads = <span class="keyword">new</span> Thread[n];</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            Thread t = <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                     </span><br><span class="line">                    <span class="comment">//任何线程要修改hp的值，必须先占用someObject</span></span><br><span class="line">                    <span class="keyword">synchronized</span> (someObject) &#123;</span><br><span class="line">                        gareen.recover();</span><br><span class="line">                    &#125;</span><br><span class="line">                     </span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;;</span><br><span class="line">            t.start();</span><br><span class="line">            addThreads[i] = t;</span><br><span class="line">              </span><br><span class="line">        &#125;</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            Thread t = <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                    <span class="comment">//任何线程要修改hp的值，必须先占用someObject</span></span><br><span class="line">                    <span class="keyword">synchronized</span> (someObject) &#123;</span><br><span class="line">                        gareen.hurt();</span><br><span class="line">                    &#125;</span><br><span class="line">                     </span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;;</span><br><span class="line">            t.start();</span><br><span class="line">            reduceThreads[i] = t;</span><br><span class="line">        &#125;</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">for</span> (Thread t : addThreads) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t.join();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (Thread t : reduceThreads) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t.join();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">          </span><br><span class="line">        System.out.printf(<span class="string">"%d个增加线程和%d个减少线程结束后%n盖伦的血量是 %.0f%n"</span>, n,n,gareen.hp);</span><br><span class="line">          </span><br><span class="line">    &#125;</span><br><span class="line">       </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种用法中,并不让加血和回血方法独占<code>gareen</code>,而是创建了<strong>一个</strong>someObject对象,让每个线程都要独占它,这样也做到了同一时间内,hp只能被一个线程修改.</p>
<h4 id="使用hero对象作为同步对象"><a href="#使用hero对象作为同步对象" class="headerlink" title="使用hero对象作为同步对象"></a>使用hero对象作为同步对象</h4><p>这种方法是索性让<code>gareen</code>来作为同步对象</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line">   </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">final</span> Hero gareen = <span class="keyword">new</span> Hero();</span><br><span class="line">        gareen.name = <span class="string">"盖伦"</span>;</span><br><span class="line">        gareen.hp = <span class="number">10000</span>;</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">int</span> n = <span class="number">10000</span>;</span><br><span class="line">  </span><br><span class="line">        Thread[] addThreads = <span class="keyword">new</span> Thread[n];</span><br><span class="line">        Thread[] reduceThreads = <span class="keyword">new</span> Thread[n];</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            Thread t = <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                     </span><br><span class="line">                    <span class="comment">//使用gareen作为synchronized</span></span><br><span class="line">                    <span class="keyword">synchronized</span> (gareen) &#123;</span><br><span class="line">                        gareen.recover();</span><br><span class="line">                    &#125;</span><br><span class="line">                     </span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;;</span><br><span class="line">            t.start();</span><br><span class="line">            addThreads[i] = t;</span><br><span class="line">              </span><br><span class="line">        &#125;</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            Thread t = <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                    <span class="comment">//使用gareen作为synchronized</span></span><br><span class="line">                    <span class="comment">//在方法hurt中有synchronized(this)</span></span><br><span class="line">                    gareen.hurt();</span><br><span class="line">                     </span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;;</span><br><span class="line">            t.start();</span><br><span class="line">            reduceThreads[i] = t;</span><br><span class="line">        &#125;</span><br><span class="line">          </span><br><span class="line">        <span class="keyword">for</span> (Thread t : addThreads) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t.join();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (Thread t : reduceThreads) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t.join();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">          </span><br><span class="line">        System.out.printf(<span class="string">"%d个增加线程和%d个减少线程结束后%n盖伦的血量是 %.0f%n"</span>, n,n,gareen.hp);</span><br><span class="line">          </span><br><span class="line">    &#125;</span><br><span class="line">       </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>也可以修改下Hero里的代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> charactor;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Hero</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">float</span> hp;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> damage;</span><br><span class="line">     </span><br><span class="line">    <span class="comment">//回血</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">recover</span><span class="params">()</span></span>&#123;</span><br><span class="line">        hp=hp+<span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="comment">//掉血</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">hurt</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">//使用this作为同步对象</span></span><br><span class="line">        <span class="keyword">synchronized</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">            hp=hp-<span class="number">1</span>;   </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">attackHero</span><span class="params">(Hero h)</span> </span>&#123;</span><br><span class="line">        h.hp-=damage;</span><br><span class="line">        System.out.format(<span class="string">"%s 正在攻击 %s, %s的血变成了 %.0f%n"</span>,name,h.name,h.name,h.hp);</span><br><span class="line">        <span class="keyword">if</span>(h.isDead())</span><br><span class="line">            System.out.println(h.name +<span class="string">"死了！"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isDead</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>&gt;=hp?<span class="keyword">true</span>:<span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="在方法前，加上修饰符synchronized"><a href="#在方法前，加上修饰符synchronized" class="headerlink" title="在方法前，加上修饰符synchronized"></a>在方法前，加上修饰符synchronized</h4><p>这种方法也是使用Hero对象作为同步对象:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> charactor;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Hero</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">float</span> hp;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> damage;</span><br><span class="line">     </span><br><span class="line">    <span class="comment">//回血</span></span><br><span class="line">    <span class="comment">//直接在方法前加上修饰符synchronized</span></span><br><span class="line">    <span class="comment">//其所对应的同步对象，就是this</span></span><br><span class="line">    <span class="comment">//和hurt方法达到的效果一样</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">recover</span><span class="params">()</span></span>&#123;</span><br><span class="line">        hp=hp+<span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="comment">//掉血</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">hurt</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">//使用this作为同步对象</span></span><br><span class="line">        <span class="keyword">synchronized</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">            hp=hp-<span class="number">1</span>;   </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">attackHero</span><span class="params">(Hero h)</span> </span>&#123;</span><br><span class="line">        h.hp-=damage;</span><br><span class="line">        System.out.format(<span class="string">"%s 正在攻击 %s, %s的血变成了 %.0f%n"</span>,name,h.name,h.name,h.hp);</span><br><span class="line">        <span class="keyword">if</span>(h.isDead())</span><br><span class="line">            System.out.println(h.name +<span class="string">"死了！"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isDead</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>&gt;=hp?<span class="keyword">true</span>:<span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>和上面的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">hurt</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">//使用this作为同步对象</span></span><br><span class="line">        <span class="keyword">synchronized</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">            hp=hp-<span class="number">1</span>;   </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>是同样的效果,都是用的this,调用了哪个对象的hurt()方法,this就指代的是哪个对象.</p>
<h2 id="线程安全的类"><a href="#线程安全的类" class="headerlink" title="线程安全的类"></a>线程安全的类</h2><p>如果一个类，其方法都是有synchronized修饰的，那么该类就叫做线程安全的类</p>
<p>同一时间，只有一个线程能够进入这种类的一个实例去修改数据，进而保证了这个实例中的数据的安全(不会同时被多线程修改而变成脏数据) </p>
<p>比如StringBuffer和StringBuilder的区别:StringBuffer的方法都是有synchronized修饰的，StringBuffer就叫做线程安全的类,而StringBuilder就不是线程安全的类.</p>
<p>在集合那篇也提到了一些线程安全的类.Collections类还提供了用于同步控制的方法.</p>
<h1 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h1><p>当用synchronized来解决同步问题时,又会产生死锁问题.原因非常简单,当线程A占有对象1后,试图占有对象二,而线程B占有着对象二,又试图占有对象一,两个线程都在等待对方结束,最终产生死锁.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread4</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> Hero a = <span class="keyword">new</span> Hero();</span><br><span class="line">        a.name = <span class="string">"A"</span>;</span><br><span class="line">        <span class="keyword">final</span> Hero b = <span class="keyword">new</span> Hero();</span><br><span class="line">        b.name = <span class="string">"B"</span>;</span><br><span class="line">        <span class="keyword">final</span> Hero c = <span class="keyword">new</span> Hero();</span><br><span class="line">        c.name = <span class="string">"C"</span>;</span><br><span class="line">        Thread t1 = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (a) &#123;</span><br><span class="line">                System.out.println(<span class="string">" t1 hold A ..."</span>);</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                System.out.println(<span class="string">"t1 try to hold B, and wait B to release ..."</span>);</span><br><span class="line">                <span class="keyword">synchronized</span> (b) &#123;</span><br><span class="line">                    System.out.println(<span class="string">"t1 hold B !!!"</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        t1.start();</span><br><span class="line">        Thread t2 = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (b) &#123;</span><br><span class="line">                System.out.println(<span class="string">" t2 hold B ..."</span>);</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                System.out.println(<span class="string">"t2 try to hold C, and wait C to release ..."</span>);</span><br><span class="line">                <span class="keyword">synchronized</span> (c) &#123;</span><br><span class="line">                    System.out.println(<span class="string">"t2 hold C !!!"</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        t2.start();</span><br><span class="line"></span><br><span class="line">        Thread t3 = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (c) &#123;</span><br><span class="line">                System.out.println(<span class="string">" t3 hold C ..."</span>);</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                System.out.println(<span class="string">"t3 try to hold A, and wait A to release ..."</span>);</span><br><span class="line">                <span class="keyword">synchronized</span> (a) &#123;</span><br><span class="line">                    System.out.println(<span class="string">"t3 hold A !!!"</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        t3.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="交互"><a href="#交互" class="headerlink" title="交互"></a>交互</h1><p>线程之间有交互通知的需求，考虑如下情况：</p>
<p>有两个线程，处理同一个英雄。一个加血，一个减血。</p>
<p>减血的线程，发现血量=1，就停止减血，直到加血的线程为英雄加了血，才可以继续减血.</p>
<h2 id="笨办法"><a href="#笨办法" class="headerlink" title="笨办法"></a>笨办法</h2><p>设计个循环判断,当血量是一的时候就不停的循环,等着血量不为一.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> charactor;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Hero</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">float</span> hp;</span><br><span class="line">      </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> damage;</span><br><span class="line">      </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">recover</span><span class="params">()</span></span>&#123;</span><br><span class="line">        hp=hp+<span class="number">1</span>;</span><br><span class="line">    &#125;    </span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">hurt</span><span class="params">()</span></span>&#123;</span><br><span class="line">            hp=hp-<span class="number">1</span>;   </span><br><span class="line">    &#125;</span><br><span class="line">      </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">attackHero</span><span class="params">(Hero h)</span> </span>&#123;</span><br><span class="line">        h.hp-=damage;</span><br><span class="line">        System.out.format(<span class="string">"%s 正在攻击 %s, %s的血变成了 %.0f%n"</span>,name,h.name,h.name,h.hp);</span><br><span class="line">        <span class="keyword">if</span>(h.isDead())</span><br><span class="line">            System.out.println(h.name +<span class="string">"死了！"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">   </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isDead</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>&gt;=hp?<span class="keyword">true</span>:<span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> charactor.Hero;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">        <span class="keyword">final</span> Hero gareen = <span class="keyword">new</span> Hero();</span><br><span class="line">        gareen.name = <span class="string">"盖伦"</span>;</span><br><span class="line">        gareen.hp = <span class="number">616</span>;</span><br><span class="line">           </span><br><span class="line">        Thread t1 = <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                <span class="keyword">while</span>(<span class="keyword">true</span>)&#123;</span><br><span class="line">                     </span><br><span class="line">                    <span class="comment">//因为减血更快，所以盖伦的血量迟早会到达1</span></span><br><span class="line">                    <span class="comment">//使用while循环判断是否是1，如果是1就不停的循环</span></span><br><span class="line">                    <span class="comment">//直到加血线程回复了血量</span></span><br><span class="line">                    <span class="keyword">while</span>(gareen.hp==<span class="number">1</span>)&#123;</span><br><span class="line">                        <span class="keyword">continue</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                     </span><br><span class="line">                    gareen.hurt();</span><br><span class="line">                    System.out.printf(<span class="string">"t1 为%s 减血1点,减少血后，%s的血量是%.0f%n"</span>,gareen.name,gareen.name,gareen.hp);</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">10</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line"> </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t1.start();</span><br><span class="line"> </span><br><span class="line">        Thread t2 = <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                <span class="keyword">while</span>(<span class="keyword">true</span>)&#123;</span><br><span class="line">                    gareen.recover();</span><br><span class="line">                    System.out.printf(<span class="string">"t2 为%s 回血1点,增加血后，%s的血量是%.0f%n"</span>,gareen.name,gareen.name,gareen.hp);</span><br><span class="line"> </span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line"> </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t2.start();</span><br><span class="line">           </span><br><span class="line">    &#125;</span><br><span class="line">        </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但这种方法不好.</p>
<h2 id="使用wait-和notify"><a href="#使用wait-和notify" class="headerlink" title="使用wait()和notify()"></a>使用wait()和notify()</h2><p>在Hero类中：</p>
<p>hurt()减血方法:当hp=1的时候，执行<strong>this.wait()</strong>. </p>
<p>this.wait()表示让占有this的线程等待，并临时释放占有.</p>
<p>进入hurt方法的线程必然是减血线程，this.wait()会让减血线程临时释放对this的占有。这样加血线程，就有机会进入recover()加血方法了。 </p>
<p>recover()加血方法：增加了血量，执行<strong>this.notify()</strong>; </p>
<p>this.notify()表示通知那些等待在this的线程，可以苏醒过来了.</p>
<p>等待在this的线程，恰恰就是减血线程.一旦recover()结束，加血线程释放了this，减血线程，就可以重新占有this，并执行后面的减血工作。 </p>
<p><img src="/2018/08/13/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/796.png" alt></p>
<p>改动在Hero类中:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> charactor;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Hero</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">float</span> hp;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> damage;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">recover</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        hp = hp + <span class="number">1</span>;</span><br><span class="line">        System.out.printf(<span class="string">"%s 回血1点,增加血后，%s的血量是%.0f%n"</span>, name, name, hp);</span><br><span class="line">        <span class="comment">// 通知那些等待在this对象上的线程，可以醒过来了，如第20行，等待着的减血线程，苏醒过来</span></span><br><span class="line">        <span class="keyword">this</span>.notify();</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">hurt</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (hp == <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 让占有this的减血线程，暂时释放对this的占有，并等待</span></span><br><span class="line">                <span class="keyword">this</span>.wait();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        hp = hp - <span class="number">1</span>;</span><br><span class="line">        System.out.printf(<span class="string">"%s 减血1点,减少血后，%s的血量是%.0f%n"</span>, name, name, hp);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">attackHero</span><span class="params">(Hero h)</span> </span>&#123;</span><br><span class="line">        h.hp -= damage;</span><br><span class="line">        System.out.format(<span class="string">"%s 正在攻击 %s, %s的血变成了 %.0f%n"</span>, name, h.name, h.name, h.hp);</span><br><span class="line">        <span class="keyword">if</span> (h.isDead())</span><br><span class="line">            System.out.println(h.name + <span class="string">"死了！"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isDead</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span> &gt;= hp ? <span class="keyword">true</span> : <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里需要强调的是，wait方法和notify方法，并不是Thread线程上的方法，它们是Object上的方法。</p>
<p>因为所有的Object都可以被用来作为同步对象，所以准确的讲，wait和notify是同步对象上的方法。</p>
<p>wait()的意思是：让占用了这个同步对象的线程，临时释放当前的占用，并且等待。 所以调用wait是有前提条件的，一定是在synchronized块里，否则就会出错。</p>
<p>notify() 的意思是，通知一个等待在这个同步对象上的线程，你可以苏醒过来了，有机会重新占用当前对象了。</p>
<p>notifyAll() 的意思是，通知所有的等待在这个同步对象上的线程，你们可以苏醒过来了，有机会重新占用当前对象了。 </p>
<h2 id="使用notify-的问题"><a href="#使用notify-的问题" class="headerlink" title="使用notify()的问题"></a>使用notify()的问题</h2><p>然而并不是从此就高枕无忧了,请看:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread5</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">final</span> Hero gareen = <span class="keyword">new</span> Hero();</span><br><span class="line">        gareen.name = <span class="string">"盖伦"</span>;</span><br><span class="line">        gareen.hp = <span class="number">616</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">            Thread t = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                    gareen.hurt();</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">            &#125;);</span><br><span class="line">            t.start();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++) &#123;</span><br><span class="line">            Thread t = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                    gareen.recover();</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">            &#125;);</span><br><span class="line">            t.start();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Hero类代码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Hero</span> </span>&#123;</span><br><span class="line">    String name;</span><br><span class="line">    <span class="keyword">float</span> hp;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> damage;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//回血</span></span><br><span class="line">    <span class="function"><span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">recover</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (hp &gt;= <span class="number">1000</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">this</span>.wait();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        hp = hp + <span class="number">1</span>;</span><br><span class="line">        System.out.printf(<span class="string">"%s 回血1点,增加血后，%s的血量是%.0f%n"</span>, name, name, hp);</span><br><span class="line">        <span class="keyword">this</span>.notify();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//掉血</span></span><br><span class="line">    <span class="function"><span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">hurt</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (hp &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">this</span>.wait();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        hp = hp - <span class="number">1</span>;</span><br><span class="line">        System.out.printf(<span class="string">"%s 减血1点,减少血后，%s的血量是%.0f%n"</span>, name, name, hp);</span><br><span class="line">        <span class="keyword">this</span>.notify();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">attackHero</span><span class="params">(Hero h)</span> </span>&#123;</span><br><span class="line">        h.hp -= damage;</span><br><span class="line">        System.out.format(<span class="string">"%s 正在攻击 %s, %s的血变成了 %.0f%n"</span>, name, h.name, h.name, h.hp);</span><br><span class="line">        <span class="keyword">if</span> (h.isDead()) &#123;</span><br><span class="line">            System.out.println(h.name + <span class="string">"死了！"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">isDead</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span> &gt;= hp ? <span class="keyword">true</span> : <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Hero类要求在hp为1时等待加血线程,hp为1000时等待减血线程.开启了五个减血线程和两个加血线程,当然数量也可以倒过来,注意:sleep都设置为100,结果发现hp值会突破定下的上界和下界.如果位置加血线程和减血线程数量相同,改为sleep事件不对等,也会出现这样的结果,原因在于:</p>
<p>当线程数不对等时,notify唤醒的线程就有较大可能是同类线程:一个减血线程发现hp为1,自己wait,notify别的线程,而此时不仅仅是加血线程在wait,也有更多的减血在wait.如果进入另一个减血线程,那么执行的是if块后面的代码,hp-1,不经过hp边界判断.</p>
<p>sleep数值不对等时也是类似的情况,sleep数值较大的造成了某类线程活跃的较少.</p>
<p>改进是:将<code>if (hp &gt;= 1000)</code>和<code>if (hp &lt;= 1000)</code>改为<code>while (hp &gt;= 1000)</code>和<code>while (hp &gt;= 1000)</code>.改为while后wait()结束仍旧在hp边界判断中,这样避免了hp溢出.</p>
<h1 id="线程池"><a href="#线程池" class="headerlink" title="线程池"></a>线程池</h1><p>在程序启动的时候就创建若干线程来响应处理，它们被称为线程池，里面的线程叫工作线程</p>
<p><strong>第一</strong>：降低资源消耗。通过重复利用已创建的线程降低线程创建和销毁造成的消耗。</p>
<p><strong>第二</strong>：提高响应速度。当任务到达时，任务可以不需要等到线程创建就能立即执行。</p>
<p><strong>第三</strong>：提高线程的可管理性。 </p>
<h2 id="自定义一个线程池"><a href="#自定义一个线程池" class="headerlink" title="自定义一个线程池"></a>自定义一个线程池</h2><p>线程池的思路和生产者消费者模型是很接近的。 </p>
<ol>
<li>准备一个任务容器 </li>
<li>一次性启动10个消费者线程 </li>
<li>刚开始任务容器是空的，所以线程都wait在上面。</li>
<li>直到一个外部线程往这个任务容器中扔了一个“任务”，就会有一个消费者线程被唤醒notify</li>
<li>这个消费者线程取出“任务”，并且执行这个任务，执行完毕后，继续等待下一次任务的到来。 </li>
<li>如果短时间内，有较多的任务加入，那么就会有多个线程被唤醒，去执行这些任务。  </li>
</ol>
<p>在整个过程中，都不需要创建新的线程，而是<strong>循环使用</strong>这些已经存在的线程</p>
<p><img src="/2018/08/13/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/2600.png" alt></p>
<p>线程池的代码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadPool</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 线程池大小</span></span><br><span class="line">    <span class="keyword">int</span> threadPoolSize;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 任务容器</span></span><br><span class="line">    <span class="keyword">final</span> LinkedList&lt;Runnable&gt; tasks = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 试图消费任务的线程</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ThreadPool</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        threadPoolSize = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 给予名字并启动10个任务消费者线程</span></span><br><span class="line">        <span class="keyword">synchronized</span> (tasks) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; threadPoolSize; i++) &#123;</span><br><span class="line">                <span class="keyword">new</span> TaskConsumeThread(<span class="string">"任务消费者线程 "</span> + i).start();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">	</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(Runnable r)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//这里的synchronized保证添加任务的外部线程的线程安全</span></span><br><span class="line">        <span class="keyword">synchronized</span> (tasks) &#123;</span><br><span class="line">            tasks.add(r);</span><br><span class="line">            <span class="comment">// 唤醒等待的任务消费者线程和添加任务线程</span></span><br><span class="line">            tasks.notifyAll();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">TaskConsumeThread</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="title">TaskConsumeThread</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">super</span>(name);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Runnable task;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"启动： "</span> + <span class="keyword">this</span>.getName());</span><br><span class="line">            <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                <span class="keyword">synchronized</span> (tasks) &#123;</span><br><span class="line">                    <span class="keyword">while</span> (tasks.isEmpty()) &#123;</span><br><span class="line">                        <span class="keyword">try</span> &#123;</span><br><span class="line">                            tasks.wait();</span><br><span class="line">                        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                            e.printStackTrace();</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                    task = tasks.removeLast();</span><br><span class="line">                    <span class="comment">// 允许添加任务的线程可以继续添加任务,也唤醒了添加任务线程</span></span><br><span class="line">                    tasks.notifyAll();</span><br><span class="line"></span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">//取出任务后就放弃了独占并开始执行</span></span><br><span class="line">                System.out.println(<span class="keyword">this</span>.getName() + <span class="string">" 获取到任务，并执行"</span>);</span><br><span class="line">                task.run();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意上面执行任务是直接使用run()方法,也就是下面的Lambda表达式位置,这样就是执行一个普通的类方法,只在当前线程执行.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread7</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ThreadPool pool = <span class="keyword">new</span> ThreadPool();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">            Runnable task = () -&gt; &#123;</span><br><span class="line">                <span class="comment">//System.out.println("执行任务");</span></span><br><span class="line">                <span class="comment">//任务可能是打印一句话</span></span><br><span class="line">                <span class="comment">//可能是访问文件</span></span><br><span class="line">                <span class="comment">//可能是做排序</span></span><br><span class="line">            &#125;;</span><br><span class="line"></span><br><span class="line">            pool.add(task);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Java自带线程池"><a href="#Java自带线程池" class="headerlink" title="Java自带线程池"></a>Java自带线程池</h2><p>线程池类ThreadPoolExecutor在包java.util.concurrent下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ThreadPoolExecutor threadPool= <span class="keyword">new</span> ThreadPoolExecutor(<span class="number">10</span>, <span class="number">15</span>, <span class="number">60</span>, TimeUnit.SECONDS, <span class="keyword">new</span> LinkedBlockingQueue&lt;Runnable&gt;());</span><br></pre></td></tr></table></figure>

<ul>
<li>第一个参数10 表示这个线程池初始化了10个线程在里面工作 </li>
<li>第二个参数15 表示如果10个线程不够用了，就会自动增加到最多15个线程 </li>
<li>第三个参数60 结合第四个参数TimeUnit.SECONDS，表示经过60秒，多出来的线程还没有接到活儿，就会回收，最后保持池子里就10个 </li>
<li>第四个参数TimeUnit.SECONDS 如上 </li>
<li>第五个参数 new LinkedBlockingQueue() 用来放任务的集合  </li>
<li>execute方法用于添加新的任务 </li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.LinkedBlockingQueue;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.ThreadPoolExecutor;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.TimeUnit;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line">   </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">           </span><br><span class="line">        ThreadPoolExecutor threadPool= <span class="keyword">new</span> ThreadPoolExecutor(<span class="number">10</span>, <span class="number">15</span>, <span class="number">60</span>, TimeUnit.SECONDS, <span class="keyword">new</span> LinkedBlockingQueue&lt;Runnable&gt;());</span><br><span class="line">           </span><br><span class="line">        threadPool.execute(<span class="keyword">new</span> Runnable()&#123;</span><br><span class="line">   </span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated method stub</span></span><br><span class="line">                System.out.println(<span class="string">"任务1"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">               </span><br><span class="line">        &#125;);</span><br><span class="line">   </span><br><span class="line">    &#125;</span><br><span class="line">   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="练习"><a href="#练习" class="headerlink" title="练习"></a>练习</h2><p>使用线程池同步查找特定文件中的内容.</p>
<p>创建一个大小为10的线程池,查找文件夹中的java文件,并找到里面有public的文件.</p>
<p>线程池还是那个线程池:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> YL</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadPool</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 线程池大小</span></span><br><span class="line">    <span class="keyword">int</span> threadPoolSize;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 任务容器</span></span><br><span class="line">    <span class="keyword">final</span> LinkedList&lt;Runnable&gt; tasks = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 试图消费任务的线程</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ThreadPool</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        threadPoolSize = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 启动10个任务消费者线程</span></span><br><span class="line">        <span class="keyword">synchronized</span> (tasks) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; threadPoolSize; i++) &#123;</span><br><span class="line">                <span class="keyword">new</span> TaskConsumeThread(<span class="string">"任务消费者线程 "</span> + i).start();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(Runnable r)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">synchronized</span> (tasks) &#123;</span><br><span class="line">            tasks.add(r);</span><br><span class="line">            <span class="comment">// 唤醒等待的任务消费者线程</span></span><br><span class="line">            tasks.notifyAll();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">TaskConsumeThread</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="title">TaskConsumeThread</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">super</span>(name);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Runnable task;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                <span class="comment">//这个synchronized保证线程池线程独占任务列表</span></span><br><span class="line">                <span class="keyword">synchronized</span> (tasks) &#123;</span><br><span class="line">                    <span class="keyword">while</span> (tasks.isEmpty()) &#123;</span><br><span class="line">                        <span class="keyword">try</span> &#123;</span><br><span class="line">                            tasks.wait();</span><br><span class="line">                        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                            e.printStackTrace();</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                    task = tasks.removeLast();</span><br><span class="line">                    <span class="comment">// 允许添加任务的线程可以继续添加任务</span></span><br><span class="line">                    tasks.notifyAll();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">//取出任务后就放弃了独占并开始执行</span></span><br><span class="line">                task.run();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>大体思路是,先递归找到java文件,然后将找到的文件打包为一个个任务交给任务容器,再由任务消费者线程获取任务并运行.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line">   </span><br><span class="line">    <span class="keyword">static</span> ThreadPool pool= <span class="keyword">new</span> ThreadPool();</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">search</span><span class="params">(File file, String search)</span> </span>&#123;</span><br><span class="line">         </span><br><span class="line">        <span class="keyword">if</span> (file.isFile()) &#123;</span><br><span class="line">            <span class="keyword">if</span>(file.getName().toLowerCase().endsWith(<span class="string">".java"</span>))&#123;</span><br><span class="line">                SearchFileTask task = <span class="keyword">new</span> SearchFileTask(file, search);</span><br><span class="line">                pool.add(task);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (file.isDirectory()) &#123;</span><br><span class="line">            File[] fs = file.listFiles();</span><br><span class="line">            <span class="keyword">for</span> (File f : fs) &#123;</span><br><span class="line">                search(f, search);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">       </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File folder =<span class="keyword">new</span> File(<span class="string">"e:\\project"</span>);</span><br><span class="line">        search(folder,<span class="string">"Magic"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SearchFileTask</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">private</span> File file;</span><br><span class="line">    <span class="keyword">private</span> String search;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">SearchFileTask</span><span class="params">(File file,String search)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.file = file;</span><br><span class="line">        <span class="keyword">this</span>.search= search;</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">         </span><br><span class="line">        String fileContent = readFileConent(file);</span><br><span class="line">        <span class="keyword">if</span>(fileContent.contains(search))&#123;</span><br><span class="line">            System.out.printf( <span class="string">"线程: %s 找到子目标字符串%s,在文件:%s%n"</span>,Thread.currentThread().getName(), search,file);</span><br><span class="line">             </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">readFileConent</span><span class="params">(File file)</span></span>&#123;</span><br><span class="line">        <span class="keyword">try</span> (FileReader fr = <span class="keyword">new</span> FileReader(file)) &#123;</span><br><span class="line">            <span class="keyword">char</span>[] all = <span class="keyword">new</span> <span class="keyword">char</span>[(<span class="keyword">int</span>) file.length()];</span><br><span class="line">            fr.read(all);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> String(all);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">  </span><br><span class="line">    &#125;  </span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Lock"><a href="#Lock" class="headerlink" title="Lock"></a>Lock</h1><h2 id="使用Lock"><a href="#使用Lock" class="headerlink" title="使用Lock"></a>使用Lock</h2><p>使用Lock也能类似的达到同步效果.</p>
<p>Lock是一个接口，为了使用一个Lock对象，需要用到:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br></pre></td></tr></table></figure>

<p>与synchronized类似的是,调用Lock对象的lock()方法表示当前线程占有Lock对象,</p>
<p>不同的是,synchronized块结束后就释放对象,而要释放Lock对象要调用unlock()方法,为了保证释放的执行，往往会把unlock() 放在finally中进行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.text.SimpleDateFormat;</span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Lock;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.ReentrantLock;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">now</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> SimpleDateFormat(<span class="string">"HH:mm:ss"</span>).format(<span class="keyword">new</span> Date());</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">log</span><span class="params">(String msg)</span> </span>&#123;</span><br><span class="line">        System.out.printf(<span class="string">"%s %s %s %n"</span>, now() , Thread.currentThread().getName() , msg);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"> </span><br><span class="line">        Thread t1 = <span class="keyword">new</span> Thread() &#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    log(<span class="string">"线程启动"</span>);</span><br><span class="line">                    log(<span class="string">"试图占有对象：lock"</span>);</span><br><span class="line"> </span><br><span class="line">                    lock.lock();</span><br><span class="line"> </span><br><span class="line">                    log(<span class="string">"占有对象：lock"</span>);</span><br><span class="line">                    log(<span class="string">"进行5秒的业务操作"</span>);</span><br><span class="line">                    Thread.sleep(<span class="number">5000</span>);</span><br><span class="line"> </span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    log(<span class="string">"释放对象：lock"</span>);</span><br><span class="line">                    lock.unlock();</span><br><span class="line">                &#125;</span><br><span class="line">                log(<span class="string">"线程结束"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t1.setName(<span class="string">"t1"</span>);</span><br><span class="line">        t1.start();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">//先让t1飞2秒</span></span><br><span class="line">            Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e1) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e1.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        Thread t2 = <span class="keyword">new</span> Thread() &#123;</span><br><span class="line"> </span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    log(<span class="string">"线程启动"</span>);</span><br><span class="line">                    log(<span class="string">"试图占有对象：lock"</span>);</span><br><span class="line"> </span><br><span class="line">                    lock.lock();</span><br><span class="line"> </span><br><span class="line">                    log(<span class="string">"占有对象：lock"</span>);</span><br><span class="line">                    log(<span class="string">"进行5秒的业务操作"</span>);</span><br><span class="line">                    Thread.sleep(<span class="number">5000</span>);</span><br><span class="line"> </span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    log(<span class="string">"释放对象：lock"</span>);</span><br><span class="line">                    lock.unlock();</span><br><span class="line">                &#125;</span><br><span class="line">                log(<span class="string">"线程结束"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t2.setName(<span class="string">"t2"</span>);</span><br><span class="line">        t2.start();</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="使用Try-lock"><a href="#使用Try-lock" class="headerlink" title="使用Try-lock"></a>使用Try-lock</h2><p>使用synchronized有可能产生死锁问题,因为它是非常钻牛角尖的.回忆死锁部分,线程1占有对象A后还要占有B,线程2占有了B后还要占有A,两边都在等待.</p>
<p>而try-lock会在指定时间内试图占用,能占到就占,不能占到就等会,等的久了就不等了.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.text.SimpleDateFormat;</span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.TimeUnit;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Lock;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.ReentrantLock;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">now</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> SimpleDateFormat(<span class="string">"HH:mm:ss"</span>).format(<span class="keyword">new</span> Date());</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">log</span><span class="params">(String msg)</span> </span>&#123;</span><br><span class="line">        System.out.printf(<span class="string">"%s %s %s %n"</span>, now() , Thread.currentThread().getName() , msg);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"> </span><br><span class="line">        Thread t1 = <span class="keyword">new</span> Thread() &#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">boolean</span> locked = <span class="keyword">false</span>;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    log(<span class="string">"线程启动"</span>);</span><br><span class="line">                    log(<span class="string">"试图占有对象：lock"</span>);</span><br><span class="line"> </span><br><span class="line">                    locked = lock.tryLock(<span class="number">1</span>,TimeUnit.SECONDS);</span><br><span class="line">                    <span class="keyword">if</span>(locked)&#123;</span><br><span class="line">                        log(<span class="string">"占有对象：lock"</span>);</span><br><span class="line">                        log(<span class="string">"进行5秒的业务操作"</span>);</span><br><span class="line">                        Thread.sleep(<span class="number">5000</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">else</span>&#123;</span><br><span class="line">                        log(<span class="string">"经过1秒钟的努力，还没有占有对象，放弃占有"</span>);</span><br><span class="line">                    &#125;</span><br><span class="line"> </span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                     </span><br><span class="line">                    <span class="keyword">if</span>(locked)&#123;</span><br><span class="line">                        log(<span class="string">"释放对象：lock"</span>);</span><br><span class="line">                        lock.unlock();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                log(<span class="string">"线程结束"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t1.setName(<span class="string">"t1"</span>);</span><br><span class="line">        t1.start();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">//先让t1飞2秒</span></span><br><span class="line">            Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e1) &#123;</span><br><span class="line">            <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">            e1.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        Thread t2 = <span class="keyword">new</span> Thread() &#123;</span><br><span class="line"> </span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">boolean</span> locked = <span class="keyword">false</span>;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    log(<span class="string">"线程启动"</span>);</span><br><span class="line">                    log(<span class="string">"试图占有对象：lock"</span>);</span><br><span class="line"> </span><br><span class="line">                    locked = lock.tryLock(<span class="number">1</span>,TimeUnit.SECONDS);</span><br><span class="line">                    <span class="keyword">if</span>(locked)&#123;</span><br><span class="line">                        log(<span class="string">"占有对象：lock"</span>);</span><br><span class="line">                        log(<span class="string">"进行5秒的业务操作"</span>);</span><br><span class="line">                        Thread.sleep(<span class="number">5000</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">else</span>&#123;</span><br><span class="line">                        log(<span class="string">"经过1秒钟的努力，还没有占有对象，放弃占有"</span>);</span><br><span class="line">                    &#125;</span><br><span class="line"> </span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                     </span><br><span class="line">                    <span class="keyword">if</span>(locked)&#123;</span><br><span class="line">                        log(<span class="string">"释放对象：lock"</span>);</span><br><span class="line">                        lock.unlock();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                log(<span class="string">"线程结束"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        t2.setName(<span class="string">"t2"</span>);</span><br><span class="line">        t2.start();</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>locked = lock.tryLock(</code>1``,TimeUnit.SECONDS);<code></code>,tryLock()方法返回一个Boolean值,可以设定具体等待时间.这样的话trylock就不一定能占用对象,所以后面释放锁时一定要判断是否占用成功,如果没成功就释放会抛出异常.</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li>Lock是一个接口，而synchronized是Java中的关键字，synchronized是内置的语言实现，Lock是代码层面的实现。</li>
<li>Lock可以选择性的获取锁，如果一段时间获取不到，可以放弃。synchronized不行，会一根筋一直获取下去。 借助Lock的这个特性，就能够规避死锁，synchronized必须通过谨慎和良好的设计，才能减少死锁的发生。</li>
<li>synchronized在发生异常和同步块结束的时候，会自动释放锁。而Lock必须手动释放， 所以如果忘记了释放锁，一样会造成死锁。 </li>
</ol>
<h2 id="另一种交互"><a href="#另一种交互" class="headerlink" title="另一种交互"></a>另一种交互</h2><p>前面使用wait(),notify(),notifyAll()和synchronized来交互,现在用Condition对象的await(),signal(),signalAll()来和Lock交互.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Condition;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Lock;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.ReentrantLock;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyStack</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line"> </span><br><span class="line">    LinkedList&lt;T&gt; values = <span class="keyword">new</span> LinkedList&lt;T&gt;();</span><br><span class="line"> </span><br><span class="line">    Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line">    Condition condition = lock.newCondition();</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">push</span><span class="params">(T t)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            lock.lock();</span><br><span class="line">            <span class="keyword">while</span> (values.size() &gt;= <span class="number">200</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    condition.await();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            condition.signalAll();</span><br><span class="line">            values.addLast(t);</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">pull</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        T t=<span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            lock.lock();</span><br><span class="line">            <span class="keyword">while</span> (values.isEmpty()) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    condition.await();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            condition.signalAll();</span><br><span class="line">            t= values.removeLast();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> t;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">peek</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> values.getLast();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="原子访问"><a href="#原子访问" class="headerlink" title="原子访问"></a>原子访问</h1><h2 id="原子性操作概念"><a href="#原子性操作概念" class="headerlink" title="原子性操作概念"></a>原子性操作概念</h2><p>所谓的原子性操作即不可中断的操作，比如赋值操作<code>int i=7</code>,而<code>i++</code>就不是,它需要先取i的值,然后i+1,然后将新值赋给i,这其中每一步都有可能被别的线程插入,导致同步问题.</p>
<h2 id="AtomicInteger"><a href="#AtomicInteger" class="headerlink" title="AtomicInteger"></a>AtomicInteger</h2><p>JDK6以后，新增加了一个包java.util.concurrent.atomic，里面有各种原子类，比如AtomicInteger。 而AtomicInteger提供了各种自增，自减等方法，这些方法都是原子性的。换句话说，自增方法incrementAndGet是线程安全的，同一个时间，只有一个线程可以调用这个方法。 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.atomic.AtomicInteger;</span><br><span class="line">   </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line">   </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        AtomicInteger atomicI =<span class="keyword">new</span> AtomicInteger();</span><br><span class="line">        <span class="keyword">int</span> i = atomicI.decrementAndGet();</span><br><span class="line">        <span class="keyword">int</span> j = atomicI.incrementAndGet();</span><br><span class="line">        <span class="keyword">int</span> k = atomicI.addAndGet(<span class="number">3</span>);</span><br><span class="line">         </span><br><span class="line">    &#125;</span><br><span class="line">   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是比较两种自增的代码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> multiplethread;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.atomic.AtomicInteger;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestThread</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> value = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> AtomicInteger atomicValue =<span class="keyword">new</span> AtomicInteger();</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> number = <span class="number">100000</span>;</span><br><span class="line">        Thread[] ts1 = <span class="keyword">new</span> Thread[number];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; number; i++) &#123;</span><br><span class="line">            Thread t =<span class="keyword">new</span> Thread()&#123;</span><br><span class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                    value++;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;;</span><br><span class="line">            t.start();</span><br><span class="line">            ts1[i] = t;</span><br><span class="line">        &#125;</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//等待这些线程全部结束</span></span><br><span class="line">        <span class="keyword">for</span> (Thread t : ts1) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t.join();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">         </span><br><span class="line">        System.out.printf(<span class="string">"%d个线程进行value++后，value的值变成:%d%n"</span>, number,value);</span><br><span class="line">        Thread[] ts2 = <span class="keyword">new</span> Thread[number];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; number; i++) &#123;</span><br><span class="line">            Thread t =<span class="keyword">new</span> Thread()&#123;</span><br><span class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">                    atomicValue.incrementAndGet();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;;</span><br><span class="line">            t.start();</span><br><span class="line">            ts2[i] = t;</span><br><span class="line">        &#125;</span><br><span class="line">         </span><br><span class="line">        <span class="comment">//等待这些线程全部结束</span></span><br><span class="line">        <span class="keyword">for</span> (Thread t : ts2) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t.join();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                <span class="comment">// TODO Auto-generated catch block</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.printf(<span class="string">"%d个线程进行atomicValue.incrementAndGet();后，atomicValue的值变成:%d%n"</span>, number,atomicValue.intValue());</span><br><span class="line">    &#125;</span><br><span class="line">        </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>多线程部分到此结束,但是遗漏了非常多的内容没有学,时间比较赶,过段时间我会把知识点在梳理一遍.当然,多线程是个很复杂的问题,人家大佬能直接写本书,以后有空就学吧.</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot</title>
    <url>/2020/07/20/Spring%20Boot/</url>
    <content><![CDATA[<p>本部分涉及SpringBoot的核心部分或者说基础部分.</p>
<a id="more"></a>

<h1 id="Spring-Boot入门"><a href="#Spring-Boot入门" class="headerlink" title="Spring Boot入门"></a>Spring Boot入门</h1><h2 id="1-SpringBoot简介"><a href="#1-SpringBoot简介" class="headerlink" title="1.SpringBoot简介"></a>1.SpringBoot简介</h2><blockquote>
<p>简化Spring应用开发的一个框架；</p>
<p>整个Spring技术栈的一个大整合；</p>
<p>J2EE开发的一站式解决方案；</p>
</blockquote>
<h2 id="2-微服务"><a href="#2-微服务" class="headerlink" title="2.微服务"></a>2.微服务</h2><p>2014，martin fowler</p>
<p>微服务：架构风格（服务微化）</p>
<p>一个应用应该是一组小型服务；可以通过HTTP的方式进行互通；</p>
<p>单体应用：ALL IN ONE</p>
<p>微服务：每一个功能元素最终都是一个可独立替换和独立升级的软件单元；</p>
<p><a href="https://martinfowler.com/articles/microservices.html#MicroservicesAndSoa" target="_blank" rel="noopener">详细参照微服务文档</a></p>
<h2 id="3-Spring-Boot-HelloWorld"><a href="#3-Spring-Boot-HelloWorld" class="headerlink" title="3.Spring Boot HelloWorld"></a>3.Spring Boot HelloWorld</h2><p>一个功能：</p>
<p>浏览器发送hello请求，服务器接受请求并处理，响应Hello World字符串；</p>
<h3 id="1-创建一个maven工程；（jar）"><a href="#1-创建一个maven工程；（jar）" class="headerlink" title="1.创建一个maven工程；（jar）"></a>1.创建一个maven工程；（jar）</h3><h3 id="2-导入spring-boot相关的依赖"><a href="#2-导入spring-boot相关的依赖" class="headerlink" title="2.导入spring boot相关的依赖"></a>2.导入spring boot相关的依赖</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.5.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3-编写一个主程序启动Spring-Boot应用"><a href="#3-编写一个主程序启动Spring-Boot应用" class="headerlink" title="3.编写一个主程序启动Spring Boot应用"></a>3.编写一个主程序启动Spring Boot应用</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *  <span class="doctag">@SpringBootApplication</span> 来标注一个主程序类，说明这是一个Spring Boot应用</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloWorldMainApplication</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Spring应用启动起来</span></span><br><span class="line">        SpringApplication.run(HelloWorldMainApplication<span class="class">.<span class="keyword">class</span>,<span class="title">args</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-编写相关的Controller-Service"><a href="#4-编写相关的Controller-Service" class="headerlink" title="4.编写相关的Controller,Service"></a>4.编写相关的Controller,Service</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ResponseBody</span></span><br><span class="line">    <span class="meta">@RequestMapping</span>(<span class="string">"/hello"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">hello</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Hello World!"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-运行主程序测试"><a href="#5-运行主程序测试" class="headerlink" title="5.运行主程序测试"></a>5.运行主程序测试</h3><h3 id="6-简化部署"><a href="#6-简化部署" class="headerlink" title="6.简化部署"></a>6.简化部署</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 这个插件，可以将应用打包成一个可执行的jar包；--&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">           <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">               <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">               <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">           <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>将这个应用打成jar包，直接使用java -jar的命令进行执行.</p>
<h2 id="4-Hello-World探究"><a href="#4-Hello-World探究" class="headerlink" title="4.Hello World探究"></a>4.Hello World探究</h2><h3 id="1-POM文件"><a href="#1-POM文件" class="headerlink" title="1.POM文件"></a>1.POM文件</h3><h4 id="1-父项目"><a href="#1-父项目" class="headerlink" title="1.父项目"></a>1.父项目</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.5.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line"></span><br><span class="line">他的父项目是</span><br><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-dependencies<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.5.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">relativePath</span>&gt;</span>../../spring-boot-dependencies<span class="tag">&lt;/<span class="name">relativePath</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">他来真正管理Spring Boot应用里面的所有依赖版本；</span><br></pre></td></tr></table></figure>

<p>spring-boot-dependencies中列出来非常多的开发要用的jar包版本.因此在自己的pom文件中不需要写版本,除了里面没有列出来的.</p>
<h4 id="2-启动器"><a href="#2-启动器" class="headerlink" title="2.启动器"></a>2.启动器</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>spring-boot-starter</strong>-==web==：</p>
<p>Spring Boot将所有的功能场景都抽取出来，做成一个个的starters（启动器），只需要在项目里面引入这些starter相关场景的所有依赖都会导入进来。要用什么功能就导入什么场景的启动器.现在是写Web项目,自然用spring-boot-starter-web这个启动器.看一下里面的依赖:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.3.1.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>compile<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-json<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.3.1.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>compile<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-tomcat<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.3.1.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>compile<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>compile<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-webmvc<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>compile<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以看到里面导入了webmvc,tomcat,json等必要的包或启动器.此外还包含了spring-boot-starter这个基本的启动器.</p>
<h3 id="2-主程序类，主入口类"><a href="#2-主程序类，主入口类" class="headerlink" title="2.主程序类，主入口类"></a>2.主程序类，主入口类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *  <span class="doctag">@SpringBootApplication</span> 来标注一个主程序类，说明这是一个Spring Boot应用</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloWorldMainApplication</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Spring应用启动起来</span></span><br><span class="line">        SpringApplication.run(HelloWorldMainApplication<span class="class">.<span class="keyword">class</span>,<span class="title">args</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>@<strong>SpringBootApplication</strong>:    Spring Boot应用标注在某个类上说明这个类是SpringBoot的主配置类，SpringBoot就应该运行这个类的main方法来启动SpringBoot应用.查看这个注解的代码:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720212401438.png" alt="image-20200720212401438"></p>
<p>可以看到这是一个复合注解.</p>
<p>@<strong>SpringBootConfiguration</strong>:Spring Boot的配置类.在查看这个注解的代码:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720212549125.png" alt="image-20200720212549125"></p>
<p>看到了熟悉的Configuration注解,这是Spring提供的.Configuration注解也是一个被Component注解的,因此最终SpringBootApplication也是一个组件.</p>
<p><strong>EnableAutoConfiguration</strong>:开启自动配置功能,部分代码:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720213216735.png" alt="image-20200720213216735"></p>
<p>该注解又被AutoConfigurationPackage注解.这个注解导入了一个组件:<code>@Import(AutoConfigurationPackages.Registrar.class)</code>.Debug一下,发现进入了里面的Register类的方法中:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720214302494.png" alt="image-20200720214302494"></p>
<p>里面有个new PackageImports.看名字知道是导包的意思.查看getPackageNames值:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720214432504.png" alt="image-20200720214432504"></p>
<p>即==将主配置类（@SpringBootApplication标注的类）的所在包及下面所有子包里面的所有组件扫描到Spring容器==</p>
<p>如何知道包名?通过加在主配置类上的@<strong>SpringBootApplication</strong>的metadata获得:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720214814367.png" alt="image-20200720214814367"></p>
<p>回到<strong>EnableAutoConfiguration</strong>注解,它也导入了一个组件:<code>@Import(AutoConfigurationImportSelector.class)</code>,查看代码:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720230445962.png" alt="image-20200720230445962"></p>
<p>这是其中一部分.其中的configurations是导入的配置类.将所有需要导入的组件以全类名的方式返回.这些组件就会被添加到容器中.会给容器中导入非常多的自动配置类（xxxAutoConfiguration）.就是给容器中导入这个场景需要的所有组件，并配置好这些组件.如图所示,里面全都是配置类.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720230756029.png" alt="image-20200720230756029"></p>
<p>问题是这些配置哪来的?其实在上面图里这个方法:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720231355716.png" alt="image-20200720231355716"></p>
<p>点进去看看:,里面用到了一个方法<code>loadFactoryNames</code>,查看这个方法:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720231646355.png" alt="image-20200720231646355"></p>
<p>来到了一个getResources方法,里面有个常量<img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720231736683.png" alt="image-20200720231736683"></p>
<p>嗨呀,这下明白了,去文件夹里找找:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720232014909.png" alt="image-20200720232014909"></p>
<p>可以看到SpringBoot相关的包里有这个文件,里面有非常多的配置.</p>
<p>找到autoconfig那个包,看里面的配置文件:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200720232217177.png" alt="image-20200720232217177"></p>
<p>这个配置文件就和那个长度为124的配置列表对上了.</p>
<p>==Spring Boot在启动的时候从类路径下的META-INF/spring.factories中获取EnableAutoConfiguration指定的值，将这些值作为自动配置类导入到容器中，自动配置类就生效，帮我们进行自动配置工作.==</p>
<p>所以,一开始的spring-boot-dependencies这个pom文件就落实到org.springframework.boot.autoconfigure这个包里.</p>
<h2 id="5-使用Spring-Initializer快速创建Spring-Boot项目"><a href="#5-使用Spring-Initializer快速创建Spring-Boot项目" class="headerlink" title="5.使用Spring Initializer快速创建Spring Boot项目"></a>5.使用Spring Initializer快速创建Spring Boot项目</h2><p>具体创建很简单,不演示.</p>
<ul>
<li>resources文件夹中目录结构<ul>
<li>static：保存所有的静态资源, js css  images,</li>
<li>templates：保存所有的模板页面,（Spring Boot默认jar包使用嵌入式的Tomcat，默认不支持JSP页面）,可以使用模板引擎（freemarker.thymeleaf）,</li>
<li>application.properties：Spring Boot应用的配置文件,可以修改一些默认设置.</li>
</ul>
</li>
</ul>
<h1 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h1><h2 id="1-配置文件"><a href="#1-配置文件" class="headerlink" title="1.配置文件"></a>1.配置文件</h2><p>SpringBoot使用一个全局的配置文件，配置文件名是固定的:</p>
<p>•application.properties</p>
<p>•application.yml</p>
<p>该配置文件用来修改SpringBoot的默认值.</p>
<h2 id="2-YAML语法："><a href="#2-YAML语法：" class="headerlink" title="2.YAML语法："></a>2.YAML语法：</h2><h3 id="1-基本语法"><a href="#1-基本语法" class="headerlink" title="1.基本语法"></a>1.基本语法</h3><p>k:(空格)v：表示一对键值对(空格必须有).</p>
<p>以<strong>空格</strong>的缩进来控制层级关系,只要是左对齐的一列数据,都是同一个层级的</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">    <span class="attr">port:</span> <span class="number">8081</span></span><br><span class="line">    <span class="attr">path:</span> <span class="string">/hello</span></span><br></pre></td></tr></table></figure>

<p>属性和值也是<strong>大小写敏感</strong>.</p>
<ul>
<li><p>对于字面量(数字,字符串,布尔):k: v.其中字符串默认不用加单引号,或双引号.如果加上了单引号,字符串里的特殊字符会转义,而双引号不会.</p>
</li>
<li><p>对于对象,Map:可以看作属性和值的键值对</p>
</li>
</ul>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">person:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">张三</span></span><br><span class="line">    <span class="attr">age:</span> <span class="number">20</span></span><br></pre></td></tr></table></figure>

<p>还可以用大括号变成一行内:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">person:</span> <span class="string">&#123;name:</span> <span class="string">张三,age:</span> <span class="number">20</span><span class="string">&#125;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>对于数组(List,Set)</li>
</ul>
<p>用<strong>-</strong>值表示数组中的一个元素</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">pets:</span></span><br><span class="line"> <span class="bullet">-</span> <span class="string">cat</span></span><br><span class="line"> <span class="bullet">-</span> <span class="string">dog</span></span><br><span class="line"> <span class="bullet">-</span> <span class="string">pig</span></span><br></pre></td></tr></table></figure>

<p>行内写法</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">pets:</span> <span class="string">[cat,dog,pig]</span></span><br></pre></td></tr></table></figure>

<h2 id="3-配置文件值注入"><a href="#3-配置文件值注入" class="headerlink" title="3.配置文件值注入"></a>3.配置文件值注入</h2><p>配置文件</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">person:</span></span><br><span class="line">    <span class="attr">lastName:</span> <span class="string">hello</span></span><br><span class="line">    <span class="attr">age:</span> <span class="number">18</span></span><br><span class="line">    <span class="attr">boss:</span> <span class="literal">false</span></span><br><span class="line">    <span class="attr">birth:</span> <span class="number">2017</span><span class="string">/12/12</span></span><br><span class="line">    <span class="attr">maps:</span> <span class="string">&#123;k1:</span> <span class="string">v1,k2:</span> <span class="number">12</span><span class="string">&#125;</span></span><br><span class="line">    <span class="attr">lists:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">lisi</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">zhaoliu</span></span><br><span class="line">    <span class="attr">dog:</span></span><br><span class="line">      <span class="attr">name:</span> <span class="string">小狗</span></span><br><span class="line">      <span class="attr">age:</span> <span class="number">12</span></span><br></pre></td></tr></table></figure>

<p>javaBean</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 将配置文件中配置的每一个属性的值，映射到这个组件中</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@ConfigurationProperties</span>：告诉SpringBoot将本类中的所有属性和配置文件中相关的配置进行绑定；</span></span><br><span class="line"><span class="comment"> *      prefix = "person"：配置文件中哪个下面的所有属性进行一一映射</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * 只有这个组件是容器中的组件，才能容器提供的<span class="doctag">@ConfigurationProperties</span>功能；</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"person"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String lastName;</span><br><span class="line">    <span class="keyword">private</span> Integer age;</span><br><span class="line">    <span class="keyword">private</span> Boolean boss;</span><br><span class="line">    <span class="keyword">private</span> Date birth;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Map&lt;String,Object&gt; maps;</span><br><span class="line">    <span class="keyword">private</span> List&lt;Object&gt; lists;</span><br><span class="line">    <span class="keyword">private</span> Dog dog;</span><br></pre></td></tr></table></figure>

<p>另外还需导入一个依赖:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--导入配置文件处理器，配置文件进行绑定就会有提示--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-configuration-processor<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">optional</span>&gt;</span>true<span class="tag">&lt;/<span class="name">optional</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>除了在yml中写,在properties中写也是可以的.这里不演示.</p>
<p>ConfigurationProperties这个注解可以一次性给属性从配置文件中赋值.还有另一个注解,Value,加在属性上方,单个赋值.二者区别:</p>
<table>
<thead>
<tr>
<th></th>
<th>@ConfigurationProperties</th>
<th>@Value</th>
</tr>
</thead>
<tbody><tr>
<td>功能</td>
<td>批量注入配置文件中的属性</td>
<td>一个个指定</td>
</tr>
<tr>
<td>松散绑定（松散语法）</td>
<td>支持</td>
<td>不支持</td>
</tr>
<tr>
<td>SpEL</td>
<td>不支持</td>
<td>支持</td>
</tr>
<tr>
<td>JSR303数据校验</td>
<td>支持</td>
<td>不支持</td>
</tr>
<tr>
<td>复杂类型封装</td>
<td>支持</td>
<td>不支持</td>
</tr>
</tbody></table>
<p>如果说，我们只是在某个业务逻辑中需要获取一下配置文件中的某项值，使用@Value；</p>
<p>如果说，我们专门编写了一个javaBean来和配置文件进行映射，我们就直接使用@ConfigurationProperties.</p>
<h3 id="PropertySource-amp-ImportResource-amp-Bean"><a href="#PropertySource-amp-ImportResource-amp-Bean" class="headerlink" title="@PropertySource&amp;@ImportResource&amp;@Bean"></a>@PropertySource&amp;@ImportResource&amp;@Bean</h3><p>对于Person这样的配置文件写在application.xml这个全局配置文件中并不好.因为如果其它Bean也加进来,会使配置文件越来越长.</p>
<p>所以可以把Person的配置单独写一个配置文件.然后在Person类上使用PropertySource这个注解,指定配置文件位置.</p>
<p>上面的一些操作是给Spring中的Bean组件用配置文件赋值,也就是说Bean已经在IOC容器中了.那么让Bean进入IOC容器的方式是?(上面Person演示了一种,用Conponent注解)</p>
<p>方式一:在Spring中添加组件可以使用xml文件,里面可以包扫描,写bean标签等.Spring Boot也可以通过它来添加组件</p>
<p>@<strong>ImportResource</strong>：导入Spring的配置文件,让配置文件里面的内容生效.Spring Boot里面没有Spring的配置文件，我们自己编写的配置文件，也不能自动识别,想让Spring的配置文件生效，加载进来,则用@<strong>ImportResource</strong>标注在一个配置类上.</p>
<p>这种方式并不推荐.Spring Boot推荐使用注解方式.就像原来在Spring中那样,完全使用注解即可.</p>
<p>现在需要一个配置类,这个配置类是Spring中的配置类,和Spring Boot中加了@SpringBootApplication注解的配置类没有关系.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Configuration</span>：指明当前类是一个配置类；就是来替代之前的Spring配置文件</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * 在配置文件中用&lt;bean&gt;&lt;bean/&gt;标签添加组件</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyAppConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//将方法的返回值添加到容器中；容器中这个组件默认的id就是方法名</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> HelloService <span class="title">helloService02</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"配置类@Bean给容器中添加组件了..."</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> HelloService();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="4-配置文件占位符"><a href="#4-配置文件占位符" class="headerlink" title="4.配置文件占位符"></a>4.配置文件占位符</h2><h3 id="1-随机数"><a href="#1-随机数" class="headerlink" title="1.随机数"></a>1.随机数</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">$&#123;random.value&#125;.$&#123;random.<span class="keyword">int</span>&#125;.$&#123;random.<span class="keyword">long</span>&#125;</span><br><span class="line">$&#123;random.<span class="keyword">int</span>(<span class="number">10</span>)&#125;.$&#123;random.<span class="keyword">int</span>[<span class="number">1024</span>,<span class="number">65536</span>]&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-占位符获取之前配置的值，如果没有可以是用-指定默认值"><a href="#2-占位符获取之前配置的值，如果没有可以是用-指定默认值" class="headerlink" title="2.占位符获取之前配置的值，如果没有可以是用:指定默认值"></a>2.占位符获取之前配置的值，如果没有可以是用:指定默认值</h3><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">person.last-name</span>=<span class="string">张三$&#123;random.uuid&#125;</span></span><br><span class="line"><span class="meta">person.age</span>=<span class="string">$&#123;random.int&#125;</span></span><br><span class="line"><span class="meta">person.birth</span>=<span class="string">2017/12/15</span></span><br><span class="line"><span class="meta">person.boss</span>=<span class="string">false</span></span><br><span class="line"><span class="meta">person.maps.k1</span>=<span class="string">v1</span></span><br><span class="line"><span class="meta">person.maps.k2</span>=<span class="string">14</span></span><br><span class="line"><span class="meta">person.lists</span>=<span class="string">a,b,c</span></span><br><span class="line"><span class="meta">person.dog.name</span>=<span class="string">$&#123;person.hello:hello&#125;_dog</span></span><br><span class="line"><span class="meta">person.dog.age</span>=<span class="string">15</span></span><br></pre></td></tr></table></figure>

<p>第八行想获取前面配置过的值,但实际没配置,如果没有冒号后的默认值就会把person.hello原样显示.</p>
<h2 id="5-Profile"><a href="#5-Profile" class="headerlink" title="5.Profile"></a>5.Profile</h2><p>用来做多环境切换.</p>
<h3 id="1-多Profile文件"><a href="#1-多Profile文件" class="headerlink" title="1.多Profile文件"></a>1.多Profile文件</h3><p>我们在主配置文件编写的时候,文件名可以是 application-{profile}.properties/yml</p>
<p>默认情况下使用的是application.properties这个配置文件.如果需要切换则有几种方式:</p>
<p>​    1.在配置文件中指定  spring.profiles.active=dev(yml文件中要转换写法)</p>
<p>​    2.命令行：</p>
<p>​        java -jar spring-boot-02-config-0.0.1-SNAPSHOT.jar –spring.profiles.active=dev；</p>
<p>​        可以直接在测试的时候，配置传入命令行参数</p>
<p>​    3.虚拟机参数；</p>
<p>​        -Dspring.profiles.active=dev</p>
<p>除了写几个配置文件的方式,还可以用yml文件把多个Profile写在一起:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">8081</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">profiles:</span></span><br><span class="line">    <span class="attr">active:</span> <span class="string">prod</span></span><br><span class="line"></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">8083</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">profiles:</span> <span class="string">dev</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">8084</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">profiles:</span> <span class="string">prod</span>  <span class="comment">#指定属于哪个环境</span></span><br></pre></td></tr></table></figure>

<p>所以配置文件有多种多样,启动配置文件的方式也不一样,那么优先级是什么?</p>
<h3 id="2-配置加载"><a href="#2-配置加载" class="headerlink" title="2.配置加载"></a>2.配置加载</h3><p>先看看配置文件加载顺序:</p>
<p>springboot 启动会扫描以下位置的application.properties或者application.yml文件作为Spring boot的默认配置文件</p>
<ul>
<li><p>–file:./config/</p>
</li>
<li><p>–file:./</p>
</li>
<li><p>–classpath:/config/</p>
</li>
<li><p>–classpath:/</p>
</li>
</ul>
<p>优先级由高到底，高优先级的配置会覆盖低优先级的配置.SpringBoot会加载这四个位置的配置文件,互补配置.</p>
<p><strong>项目打包好以后，我们可以使用命令行参数的形式，启动项目的时候来指定配置文件的新位置；指定配置文件和默认加载的这些配置文件共同起作用形成互补配置；</strong></p>
<p>java -jar spring-boot-02-config-02-0.0.1-SNAPSHOT.jar –spring.config.location=G:/application.properties</p>
<p>除了这几个地方加载配置文件,还有其他许多地方可以影响配置.这里简要列出:</p>
<ol>
<li><strong>命令行参数</strong></li>
</ol>
<p>所有的配置都可以在命令行上进行指定</p>
<p>java -jar spring-boot-02-config-02-0.0.1-SNAPSHOT.jar –server.port=8087  –server.context-path=/abc</p>
<p>多个配置用空格分开: –配置项=值</p>
<ol start="2">
<li><p>来自java:comp/env的JNDI属性</p>
</li>
<li><p>Java系统属性（System.getProperties()）</p>
</li>
<li><p>操作系统环境变量</p>
</li>
<li><p>RandomValuePropertySource配置的random.*属性值</p>
</li>
</ol>
<p>==<strong>由jar包外向jar包内进行寻找</strong>==</p>
<p>==<strong>优先加载带profile</strong>==</p>
<ol start="6">
<li><p>jar包外部的application-{profile}.properties或application.yml(带spring.profile)配置文件</p>
</li>
<li><p>jar包内部的application-{profile}.properties或application.yml(带spring.profile)配置文件</p>
</li>
</ol>
<p>==<strong>再来加载不带profile</strong>==</p>
<ol start="8">
<li><p>jar包外部的application.properties或application.yml(不带spring.profile)配置文件</p>
</li>
<li><p>jar包内部的application.properties或application.yml(不带spring.profile)配置文件</p>
</li>
<li><p>@Configuration注解类上的@PropertySource</p>
</li>
<li><p>通过SpringApplication.setDefaultProperties指定的默认属性</p>
</li>
</ol>
<p>以上优先级也是从高到低,可以配置互补.</p>
<p>需要注意的是打包时默认是只将类路径下的文件打包,因此在写在工程目录下的配置文件是不包含在内的.</p>
<p>所有配置来源:</p>
<p><a href="https://docs.spring.io/spring-boot/docs/1.5.9.RELEASE/reference/htmlsingle/#boot-features-external-config" target="_blank" rel="noopener">参考官方文档</a></p>
<h2 id="6-自动配置原理"><a href="#6-自动配置原理" class="headerlink" title="6.自动配置原理"></a>6.自动配置原理</h2><h3 id="1-自动配置原理"><a href="#1-自动配置原理" class="headerlink" title="1.自动配置原理"></a>1.自动配置原理</h3><p>第一节已经讲过,这里再复述一下:</p>
<p>1）、SpringBoot启动的时候加载主配置类，开启了自动配置功能 ==@EnableAutoConfiguration==</p>
<p><strong>2）、@EnableAutoConfiguration 作用：</strong></p>
<ul>
<li>利用AutoConfigurationImportSelector给容器中导入一些组件.<ul>
<li>可以查看selectImports()方法的内容.(Spring Boot 1.x中),现在是直接到getCandidateConfigurations方法</li>
<li>List&lt;String&gt; configurations = getCandidateConfigurations(annotationMetadata, attributes);获取候选的配置<img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200721173941169.png" alt="image-20200721173941169"></li>
</ul>
</li>
</ul>
<p>前面说过,读取到的”META-INF/spring.factories”里面每一个都是xxxAutoConfiguration类.它们被加入容器用来做自动配置.以HttpEncodingAutoConfiguration为例解释自动配置原理:</p>
<p>下面是该类的注解:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span>(proxyBeanMethods = <span class="keyword">false</span>)<span class="comment">//表示这是一个配置类</span></span><br><span class="line"><span class="meta">@EnableConfigurationProperties</span>(ServerProperties<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">@<span class="title">ConditionalOnWebApplication</span>(<span class="title">type</span> </span>= ConditionalOnWebApplication.Type.SERVLET)</span><br><span class="line"><span class="meta">@ConditionalOnClass</span>(CharacterEncodingFilter<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">@<span class="title">ConditionalOnProperty</span>(<span class="title">prefix</span> </span>= <span class="string">"server.servlet.encoding"</span>, value = <span class="string">"enabled"</span>, matchIfMissing = <span class="keyword">true</span>)</span><br></pre></td></tr></table></figure>

<ul>
<li>@EnableConfigurationProperties注解的作用是：使<strong>使用 @ConfigurationProperties 注解的类生效</strong>。</li>
</ul>
<p><a href="https://www.jianshu.com/p/7f54da1cb2eb" target="_blank" rel="noopener">具体参见连接</a></p>
<p>要启用的配置类的注解如下:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200721195733843.png" alt="image-20200721195733843"></p>
<p>所有在配置文件中能配置的属性都是在xxxxProperties类中封装着.</p>
<ul>
<li>@ConditionalOnWebApplication注解是依赖的Spring的Conditional注解.意为满足条件时加载Bean.这里的条件是</li>
</ul>
<p>判断当前应用是web应用,如果是,配置类生效.</p>
<ul>
<li><p>@ConditionalOnClass(CharacterEncodingFilter.class),判断当前项目有没有这个类,这个类是SpringMVC中进行乱码解决的过滤器</p>
</li>
<li><p>@ConditionalOnProperty(prefix = “spring.http.encoding”, value = “enabled”, matchIfMissing = true) ,判断配置文件中是否存在某个配置  spring.http.encoding.enabled,如果不存在，判断也是成立的.</p>
</li>
</ul>
<p>如果满足上述条件,则HttpEncodingAutoConfiguration配置类生效.</p>
<p>该配置类中使用了Bean注解,添加了两个Bean组件:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200721202003267.png" alt="image-20200721202003267"></p>
<p>第一个是字符过滤器,其代码是:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200721204407802.png" alt="image-20200721204407802"></p>
<p>可以看到它从HttpEncodingAutoConfiguration的属性properties中取得了字符集名字.具体如何取得看下图;</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200721204248240.png" alt="image-20200721204248240"></p>
<p><strong>精髓：</strong></p>
<p>​    <strong>1）、SpringBoot启动会加载大量的自动配置类</strong></p>
<p>​    <strong>2）、我们看我们需要的功能有没有SpringBoot默认写好的自动配置类</strong></p>
<p>​    <strong>3）、我们再来看这个自动配置类中到底配置了哪些组件(只要我们要用的组件有，我们就不需要再来配置了,例如上面的HttpEncodingAutoConfiguration就加入了两个组件)</strong></p>
<p>​    <strong>4）、给容器中自动配置类添加组件的时候，会从properties类中获取某些属性。我们就可以在配置文件中指定这些属性的值</strong></p>
<p>xxxxAutoConfigurartion：自动配置类,给容器中添加组件.</p>
<p>xxxxProperties:封装配置文件中相关属性.</p>
<p>在上面的代码展示中,常常出现了@Conditionalxxx这类注解,这里列举一些:</p>
<table>
<thead>
<tr>
<th>@Conditional扩展注解</th>
<th>作用（判断是否满足当前指定条件）</th>
</tr>
</thead>
<tbody><tr>
<td>@ConditionalOnJava</td>
<td>系统的java版本是否符合要求</td>
</tr>
<tr>
<td>@ConditionalOnBean</td>
<td>容器中存在指定Bean；</td>
</tr>
<tr>
<td>@ConditionalOnMissingBean</td>
<td>容器中不存在指定Bean；</td>
</tr>
<tr>
<td>@ConditionalOnExpression</td>
<td>满足SpEL表达式指定</td>
</tr>
<tr>
<td>@ConditionalOnClass</td>
<td>系统中有指定的类</td>
</tr>
<tr>
<td>@ConditionalOnMissingClass</td>
<td>系统中没有指定的类</td>
</tr>
<tr>
<td>@ConditionalOnSingleCandidate</td>
<td>容器中只有一个指定的Bean，或者这个Bean是首选Bean</td>
</tr>
<tr>
<td>@ConditionalOnProperty</td>
<td>系统中指定的属性是否有指定的值</td>
</tr>
<tr>
<td>@ConditionalOnResource</td>
<td>类路径下是否存在指定资源文件</td>
</tr>
<tr>
<td>@ConditionalOnWebApplication</td>
<td>当前是web环境</td>
</tr>
<tr>
<td>@ConditionalOnNotWebApplication</td>
<td>当前不是web环境</td>
</tr>
<tr>
<td>@ConditionalOnJndi</td>
<td>JNDI存在指定项</td>
</tr>
</tbody></table>
<h1 id="日志"><a href="#日志" class="headerlink" title="日志"></a>日志</h1><h2 id="1、日志框架"><a href="#1、日志框架" class="headerlink" title="1、日志框架"></a>1、日志框架</h2><p><strong>市面上的日志框架；</strong></p>
<p>JUL、JCL、Jboss-logging、logback、log4j、log4j2、slf4j….</p>
<table>
<thead>
<tr>
<th>日志门面  （日志的抽象层）</th>
<th>日志实现</th>
</tr>
</thead>
<tbody><tr>
<td><del>JCL（Jakarta  Commons Logging）</del>    SLF4j（Simple  Logging Facade for Java）    <strong><del>jboss-logging</del></strong></td>
<td>Log4j  JUL（java.util.logging）  Log4j2  <strong>Logback</strong></td>
</tr>
</tbody></table>
<p>左边选一个门面（抽象层）、右边来选一个实现:</p>
<p>日志门面：  SLF4J,</p>
<p>日志实现：Logback,</p>
<p>SpringBoot：底层是Spring框架，Spring框架默认是用JCL,</p>
<p>​    <strong>==SpringBoot选用 SLF4j和logback；==</strong></p>
<h2 id="2、SLF4j使用"><a href="#2、SLF4j使用" class="headerlink" title="2、SLF4j使用"></a>2、SLF4j使用</h2><h3 id="1、如何在系统中使用SLF4j-https-www-slf4j-org"><a href="#1、如何在系统中使用SLF4j-https-www-slf4j-org" class="headerlink" title="1、如何在系统中使用SLF4j   https://www.slf4j.org"></a>1、如何在系统中使用SLF4j   <a href="https://www.slf4j.org" target="_blank" rel="noopener">https://www.slf4j.org</a></h3><p>以后开发的时候，日志记录方法的调用，不应该来直接调用日志的实现类，而是调用日志抽象层里面的方法.</p>
<p>给系统里面导入slf4j的jar和  logback的实现jar</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.slf4j.Logger;</span><br><span class="line"><span class="keyword">import</span> org.slf4j.LoggerFactory;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloWorld</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    Logger logger = LoggerFactory.getLogger(HelloWorld<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    logger.info(<span class="string">"Hello World"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>图示:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/concrete-bindings.png" alt="concrete-bindings"></p>
<p>每一个日志的实现框架都有自己的配置文件.使用slf4j以后,<strong>配置文件还是做成日志实现框架自己本身的配置文件</strong>.</p>
<h3 id="2-遗留问题"><a href="#2-遗留问题" class="headerlink" title="2.遗留问题"></a>2.遗留问题</h3><p>对于不同的框架组件,用的日志实现不统一.现在可以用SLF4J提供的包进行替换:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/legacy.png" alt></p>
<p>图中的深灰色xxx-over-slf4j.jar就是中间包,用来替换原有的日志框架.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200722102024845.png" alt="image-20200722102024845"></p>
<p>图里并没有看到xxx-over-slf4j这类包.这是因为Spring Boot2有了一些改动,那两个xxx-to-slf4j就是转换包.另外,Spring5原来用的jcl被改写为slf4j(内部实际还是jcl).因此pom文件中也没有排除spring日志框架这一部分.</p>
<h2 id="3-日志使用"><a href="#3-日志使用" class="headerlink" title="3.日志使用"></a>3.日志使用</h2><h3 id="1-默认配置"><a href="#1-默认配置" class="headerlink" title="1.默认配置"></a>1.默认配置</h3><p>SpringBoot默认已经配置类日志,直接使用即可:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//记录器</span></span><br><span class="line">Logger logger = LoggerFactory.getLogger(getClass());</span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">contextLoads</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">//System.out.println();</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//日志的级别；</span></span><br><span class="line">    <span class="comment">//由低到高   trace&lt;debug&lt;info&lt;warn&lt;error</span></span><br><span class="line">    <span class="comment">//可以调整输出的日志级别；日志就只会在这个级别以以后的高级别生效</span></span><br><span class="line">    logger.trace(<span class="string">"这是trace日志..."</span>);</span><br><span class="line">    logger.debug(<span class="string">"这是debug日志..."</span>);</span><br><span class="line">    <span class="comment">//SpringBoot默认给我们使用的是info级别的，没有指定级别的就用SpringBoot默认规定的级别；root级别</span></span><br><span class="line">    logger.info(<span class="string">"这是info日志..."</span>);</span><br><span class="line">    logger.warn(<span class="string">"这是warn日志..."</span>);</span><br><span class="line">    logger.error(<span class="string">"这是error日志..."</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">日志输出格式：</span><br><span class="line">    %d表示日期时间，</span><br><span class="line">    %thread表示线程名，</span><br><span class="line">    %-5level：级别从左显示5个字符宽度</span><br><span class="line">    %logger&#123;50&#125; 表示logger名字最长50个字符，否则按照句点分割。 </span><br><span class="line">    %msg：日志消息，</span><br><span class="line">    %n是换行符</span><br><span class="line">--&gt;</span><br><span class="line">%d&#123;yyyy-MM-dd HH:mm:ss.SSS&#125; [%thread] %-5level %logger&#123;50&#125; - %msg%n</span><br></pre></td></tr></table></figure>

<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">logging:</span></span><br><span class="line">  <span class="attr">level:</span></span><br><span class="line">    <span class="attr">com.company:</span> <span class="string">trace</span></span><br><span class="line">  <span class="attr">file:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">'G:\log\sb.log'</span></span><br><span class="line">  <span class="attr">pattern:</span></span><br><span class="line">    <span class="attr">console:</span> <span class="string">"%d&#123;yyyy-MM-dd&#125; [%thread] %-5level %logger&#123;50&#125; - %msg%n"</span></span><br><span class="line">    <span class="attr">file:</span> <span class="string">"%d&#123;yyyy-MM-dd&#125; === [%thread] === %-5level === %logger&#123;50&#125; ==== %msg%n"</span></span><br></pre></td></tr></table></figure>

<p>注意单引号和双引号的使用,特别是日志格式那里,一旦出错,程序无法运行.</p>
<h3 id="2-指定配置"><a href="#2-指定配置" class="headerlink" title="2.指定配置"></a>2.指定配置</h3><p>给类路径下放上每个日志框架自己的配置文件即可</p>
<table>
<thead>
<tr>
<th>Logging System</th>
<th>Customization</th>
</tr>
</thead>
<tbody><tr>
<td>Logback</td>
<td><code>logback-spring.xml</code>, <code>logback-spring.groovy</code>, <code>logback.xml</code> or <code>logback.groovy</code></td>
</tr>
<tr>
<td>Log4j2</td>
<td><code>log4j2-spring.xml</code> or <code>log4j2.xml</code></td>
</tr>
<tr>
<td>JDK (Java Util Logging)</td>
<td><code>logging.properties</code></td>
</tr>
</tbody></table>
<p>logback.xml：这类文件名直接就被日志框架识别了,而如果使用<strong>logback-spring.xml</strong>这种文件名,日志框架是不识别的,但是SpringBoot可以识别.识别后由SpringBoot解析日志配置,可以使用SpringBoot的高级Profile功能.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">springProfile</span> <span class="attr">name</span>=<span class="string">"staging"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- configuration to be enabled when the "staging" profile is active --&gt;</span></span><br><span class="line">      可以指定某段配置只在某个环境下生效</span><br><span class="line"><span class="tag">&lt;/<span class="name">springProfile</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>例如指定日志配置文件在开发环境中生效.</p>
<p>如果说非要切换回log4j这样的框架,则需要先把替换包给排除,然后引入log4j的依赖.但是不推荐这样做.</p>
<p>slf4j+log4j的方式:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>logback-classic<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>ch.qos.logback<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>log4j-over-slf4j<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.slf4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.slf4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>slf4j-log4j12<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>切换为log4j2</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">   <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">                    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-logging<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-log4j2<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h1 id="Web开发"><a href="#Web开发" class="headerlink" title="Web开发"></a>Web开发</h1><h2 id="1-SpringBoot对静态资源的映射规则"><a href="#1-SpringBoot对静态资源的映射规则" class="headerlink" title="1.SpringBoot对静态资源的映射规则"></a>1.SpringBoot对静态资源的映射规则</h2><p>以前的Web项目会有一个webapp目录,其中可以放静态资源等文件.而在Spring Boot中没有这个目录.所以需要使用资源映射的方式.具体看图:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200722142918697.png" alt="image-20200722142918697"></p>
<p>在WebMvcAutoConfiguration类中有一个静态类,该静态类有个方法addResourceHandlers:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addResourceHandlers</span><span class="params">(ResourceHandlerRegistry registry)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="keyword">this</span>.resourceProperties.isAddMappings()) &#123;</span><br><span class="line">        logger.debug(<span class="string">"Default resource handling disabled"</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    Duration cachePeriod = <span class="keyword">this</span>.resourceProperties.getCache().getPeriod();</span><br><span class="line">    CacheControl cacheControl = <span class="keyword">this</span>.resourceProperties.getCache().getCachecontrol().toHttpCacheControl();</span><br><span class="line">    <span class="keyword">if</span> (!registry.hasMappingForPattern(<span class="string">"/webjars/**"</span>)) &#123;</span><br><span class="line">        customizeResourceHandlerRegistration(registry.addResourceHandler(<span class="string">"/webjars/**"</span>)</span><br><span class="line">                .addResourceLocations(<span class="string">"classpath:/META-INF/resources/webjars/"</span>)</span><br><span class="line">                .setCachePeriod(getSeconds(cachePeriod)).setCacheControl(cacheControl));</span><br><span class="line">    &#125;</span><br><span class="line">    String staticPathPattern = <span class="keyword">this</span>.mvcProperties.getStaticPathPattern();</span><br><span class="line">    <span class="keyword">if</span> (!registry.hasMappingForPattern(staticPathPattern)) &#123;</span><br><span class="line">        customizeResourceHandlerRegistration(registry.addResourceHandler(staticPathPattern)</span><br><span class="line">                .addResourceLocations(getResourceLocations(<span class="keyword">this</span>.resourceProperties.getStaticLocations()))</span><br><span class="line">                .setCachePeriod(getSeconds(cachePeriod)).setCacheControl(cacheControl));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>所有 <code>/webjars/**</code> (这里是指访问请求)，都去 <code>classpath:/META-INF/resources/webjars/</code> 找资源</p>
<p><code>webjars</code>：以jar包的方式引入静态资源.以jQuery为例:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.webjars<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jquery<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.5.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>请求路径:<a href="http://localhost:8080/webjars/jquery/3.5.1/jquery.js" target="_blank" rel="noopener">http://localhost:8080/webjars/jquery/3.5.1/jquery.js</a></p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200722144602179.png" alt="image-20200722144602179"></p>
<p>对于静态资源,也存在一个xxxProperties作为配置类,这里就是ResourceProperties这个类:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200722145217620.png" alt="image-20200722145217620"></p>
<p>除了webjars可以获取到jar包里的资源,但是对于其它资源就不一样了.addResourceHandlers这个方法里的第二部分就是另一种方式:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200722145648961.png" alt="image-20200722145648961"></p>
<p>这个getStaticPathPattern方法最终得到<code>private String staticPathPattern = &quot;/**&quot;;</code></p>
<p>这个路径表示访问当前项目的任何路径如果没有处理,默认都去（静态资源的文件夹）找映射,具体的文件夹如下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;classpath:&#x2F;META-INF&#x2F;resources&#x2F;&quot;, </span><br><span class="line">&quot;classpath:&#x2F;resources&#x2F;&quot;,</span><br><span class="line">&quot;classpath:&#x2F;static&#x2F;&quot;, </span><br><span class="line">&quot;classpath:&#x2F;public&#x2F;&quot; </span><br><span class="line">&quot;&#x2F;&quot;：当前项目的根路径</span><br></pre></td></tr></table></figure>

<p>(这里的类路径是指resources文件夹)</p>
<p>如果找static文件夹里的asserts文件夹里的js文件夹下的jquery.js,则是这样写:localhost:8080/asserts/js/jquery.js</p>
<p>特比的对于index页面,则被<strong>/*</strong>*映射,直接写:localhost:8080/ .index页面放在上面几个位置都可以.</p>
<p>另外还有一个图标的访问: **/favicon.ico这样的访问路径也是在上面几个文件夹找资源.</p>
<h2 id="2-模板引擎"><a href="#2-模板引擎" class="headerlink" title="2.模板引擎"></a>2.模板引擎</h2><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/template-engine.png" alt></p>
<h3 id="1-引入thymeleaf"><a href="#1-引入thymeleaf" class="headerlink" title="1.引入thymeleaf"></a>1.引入thymeleaf</h3><p>首先引入这个starter:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-thymeleaf<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后修改一下版本:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">java.version</span>&gt;</span>14<span class="tag">&lt;/<span class="name">java.version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">thymeleaf.version</span>&gt;</span>3.0.11.RELEASE<span class="tag">&lt;/<span class="name">thymeleaf.version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">thymeleaf-layout-dialect.version</span>&gt;</span>2.4.1<span class="tag">&lt;/<span class="name">thymeleaf-layout-dialect.version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-使用thymeleaf"><a href="#2-使用thymeleaf" class="headerlink" title="2.使用thymeleaf"></a>2.使用thymeleaf</h3><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200722161949638.png" alt="image-20200722161949638"></p>
<p>根据其属性类,只要我们把HTML页面放在classpath:/templates/,thymeleaf就能自动渲染.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/success"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">success</span><span class="params">(Map&lt;String, String&gt; map)</span> </span>&#123;</span><br><span class="line">    map.put(<span class="string">"hello"</span>, <span class="string">"你好"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"success"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在jsp中,map被放入请求域中,页面上直接使用$便可以取值.而使用thymeleaf后要这样写:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"en"</span> <span class="attr">xmlns:th</span>=<span class="string">"http://www.thymeleaf.org"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>success<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h1</span>&gt;</span>成功<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:text</span>=<span class="string">"$&#123;hello&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3-语法规则"><a href="#3-语法规则" class="headerlink" title="3.语法规则"></a>3.语法规则</h3><p>th:表示任意html属性.它用来替换原生属性的值.例如上面的th:text就是替换div的text属性.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/2018-02-04_123955.png" alt></p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">Simple</span> <span class="string">expressions:（表达式语法）</span></span><br><span class="line">    <span class="attr">Variable</span> <span class="string">Expressions: $&#123;...&#125;：获取变量值；OGNL,</span></span><br><span class="line">            <span class="attr">1）、获取对象的属性、调用方法</span></span><br><span class="line">            <span class="attr">2）、使用内置的基本对象：</span></span><br><span class="line"><span class="comment">                #ctx : the context object.</span></span><br><span class="line"><span class="comment">                #vars: the context variables.</span></span><br><span class="line"><span class="comment">                #locale : the context locale.</span></span><br><span class="line"><span class="comment">                #request : (only in Web Contexts) the HttpServletRequest object.</span></span><br><span class="line"><span class="comment">                #response : (only in Web Contexts) the HttpServletResponse object.</span></span><br><span class="line"><span class="comment">                #session : (only in Web Contexts) the HttpSession object.</span></span><br><span class="line"><span class="comment">                #servletContext : (only in Web Contexts) the ServletContext object.</span></span><br><span class="line"></span><br><span class="line">                <span class="attr">$&#123;session.foo&#125;</span></span><br><span class="line">            <span class="attr">3）、内置的一些工具对象：</span></span><br><span class="line"><span class="comment">                #execInfo : information about the template being processed.</span></span><br><span class="line"><span class="comment">                #messages : methods for obtaining externalized messages inside variables expressions, in the same way as they would be obtained using #&#123;…&#125; syntax.</span></span><br><span class="line"><span class="comment">                #uris : methods for escaping parts of URLs/URIs</span></span><br><span class="line"><span class="comment">                #conversions : methods for executing the configured conversion service (if any).</span></span><br><span class="line"><span class="comment">                #dates : methods for java.util.Date objects: formatting, component extraction, etc.</span></span><br><span class="line"><span class="comment">                #calendars : analogous to #dates , but for java.util.Calendar objects.</span></span><br><span class="line"><span class="comment">                #numbers : methods for formatting numeric objects.</span></span><br><span class="line"><span class="comment">                #strings : methods for String objects: contains, startsWith, prepending/appending, etc.</span></span><br><span class="line"><span class="comment">                #objects : methods for objects in general.</span></span><br><span class="line"><span class="comment">                #bools : methods for boolean evaluation.</span></span><br><span class="line"><span class="comment">                #arrays : methods for arrays.</span></span><br><span class="line"><span class="comment">                #lists : methods for lists.</span></span><br><span class="line"><span class="comment">                #sets : methods for sets.</span></span><br><span class="line"><span class="comment">                #maps : methods for maps.</span></span><br><span class="line"><span class="comment">                #aggregates : methods for creating aggregates on arrays or collections.</span></span><br><span class="line"><span class="comment">                #ids : methods for dealing with id attributes that might be repeated (for example, as a result of an iteration).</span></span><br><span class="line"></span><br><span class="line"><span class="attr">Selection</span> <span class="string">Variable Expressions: *&#123;...&#125;：选择表达式：和$&#123;&#125;在功能上是一样</span></span><br><span class="line">    <span class="meta">补充：配合</span> <span class="string">th:object="$&#123;session.user&#125;,</span></span><br><span class="line"><span class="comment">    #&lt;div th:object="$&#123;session.user&#125;"&gt;</span></span><br><span class="line"><span class="comment">    #&lt;p&gt;Name: &lt;span th:text="*&#123;firstName&#125;"&gt;Sebastian&lt;/span&gt;.&lt;/p&gt;</span></span><br><span class="line"><span class="comment">    #&lt;p&gt;Surname: &lt;span th:text="*&#123;lastName&#125;"&gt;Pepper&lt;/span&gt;.&lt;/p&gt;</span></span><br><span class="line"><span class="comment">    #&lt;p&gt;Nationality: &lt;span th:text="*&#123;nationality&#125;"&gt;Saturn&lt;/span&gt;.&lt;/p&gt;</span></span><br><span class="line"><span class="comment">    #&lt;/div&gt;</span></span><br><span class="line"><span class="attr">Message</span> <span class="string">Expressions: #&#123;...&#125;：获取国际化内容</span></span><br><span class="line"><span class="attr">Link</span> <span class="string">URL Expressions: @&#123;...&#125;：定义URL</span></span><br><span class="line">    <span class="meta">@&#123;/order/process(execId</span>=<span class="string">$&#123;execId&#125;,execType='FAST')&#125;</span></span><br><span class="line"><span class="attr">Fragment</span> <span class="string">Expressions: ~&#123;...&#125;：片段引用表达式</span></span><br><span class="line">    <span class="meta">&lt;div</span> <span class="string">th:insert="~&#123;commons :: main&#125;"&gt;...&lt;/div&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">Literals（字面量）</span></span><br><span class="line">    <span class="attr">Text</span> <span class="string">literals: 'one text' , 'Another one!' ,…</span></span><br><span class="line">    <span class="attr">Number</span> <span class="string">literals: 0 , 34 , 3.0 , 12.3 ,…</span></span><br><span class="line">    <span class="attr">Boolean</span> <span class="string">literals: true , false</span></span><br><span class="line">    <span class="attr">Null</span> <span class="string">literal: null</span></span><br><span class="line">    <span class="attr">Literal</span> <span class="string">tokens: one , sometext , main ,…</span></span><br><span class="line"><span class="attr">Text</span> <span class="string">operations:（文本操作）</span></span><br><span class="line">    <span class="attr">String</span> <span class="string">concatenation: +</span></span><br><span class="line">    <span class="attr">Literal</span> <span class="string">substitutions: |The name is $&#123;name&#125;|</span></span><br><span class="line"><span class="attr">Arithmetic</span> <span class="string">operations:（数学运算）</span></span><br><span class="line">    <span class="attr">Binary</span> <span class="string">operators: + , - , * , / , %</span></span><br><span class="line">    <span class="attr">Minus</span> <span class="string">sign (unary operator): -</span></span><br><span class="line"><span class="attr">Boolean</span> <span class="string">operations:（布尔运算）</span></span><br><span class="line">    <span class="attr">Binary</span> <span class="string">operators: and , or</span></span><br><span class="line">    <span class="attr">Boolean</span> <span class="string">negation (unary operator): ! , not</span></span><br><span class="line"><span class="attr">Comparisons</span> <span class="string">and equality:（比较运算）</span></span><br><span class="line">    <span class="attr">Comparators</span>: <span class="string">&gt; , &lt; , &gt;= , &lt;= ( gt , lt , ge , le )</span></span><br><span class="line">    <span class="attr">Equality</span> <span class="string">operators: == , != ( eq , ne )</span></span><br><span class="line"><span class="attr">Conditional</span> <span class="string">operators:条件运算（三元运算符）</span></span><br><span class="line">    <span class="meta">If-then</span>: <span class="string">(if) ? (then)</span></span><br><span class="line">    <span class="meta">If-then-else</span>: <span class="string">(if) ? (then) : (else)</span></span><br><span class="line">    <span class="attr">Default</span>: <span class="string">(value) ?: (defaultvalue)</span></span><br><span class="line"><span class="attr">Special</span> <span class="string">tokens:</span></span><br><span class="line">    <span class="meta">No-Operation</span>: <span class="string">_</span></span><br></pre></td></tr></table></figure>

<p>简单使用下:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h1</span>&gt;</span>成功<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:text</span>=<span class="string">"$&#123;hello&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">hr</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h4</span> <span class="attr">th:text</span>=<span class="string">"$&#123;user&#125;"</span> <span class="attr">th:each</span>=<span class="string">"user:$&#123;users&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">h4</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">hr</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h4</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">span</span> <span class="attr">th:each</span>=<span class="string">"user:$&#123;users&#125;"</span>&gt;</span>[[$&#123;user&#125;]]<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">h4</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="3-SpringMVC配置"><a href="#3-SpringMVC配置" class="headerlink" title="3.SpringMVC配置"></a>3.SpringMVC配置</h2><h3 id="1-自动配置举例"><a href="#1-自动配置举例" class="headerlink" title="1.自动配置举例"></a>1.自动配置举例</h3><p>同样的SpringBoot也为SpringMVC做了自动配置.</p>
<p>以下是WebMvcAutoConfiguration的一些配置:</p>
<ul>
<li><p>Inclusion of <code>ContentNegotiatingViewResolver</code> and <code>BeanNameViewResolver</code> beans.</p>
<ul>
<li>自动配置了ViewResolver（视图解析器：根据方法的返回值得到视图对象（View），视图对象决定如何渲染</li>
<li>ContentNegotiatingViewResolver：组合所有的视图解析器</li>
<li>==如何定制：我们可以自己给容器中添加一个视图解析器；自动的将其组合进来==,自定义的解析器也被组合到ContentNegotiatingViewResolver中</li>
</ul>
</li>
<li><p>Support for serving static resources, including support for WebJars,静态资源文件夹路径,webjars</p>
</li>
<li><p>Static <code>index.html</code> support. 静态首页访问</p>
</li>
<li><p>Custom <code>Favicon</code> support (see below).  favicon.ico</p>
<p>  这些上面都提过</p>
</li>
<li><p>自动注册了 <code>Converter</code>, <code>GenericConverter</code>, <code>Formatter</code> beans.</p>
<ul>
<li>Converter：转换器 ,public String hello(User user)：类型转换使用Converter</li>
<li><code>Formatter</code>  格式化器,2017.12.17==&gt;Date</li>
</ul>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@ConditionalOnProperty</span>(prefix = <span class="string">"spring.mvc"</span>, name = <span class="string">"date-format"</span>)<span class="comment">//在文件中配置日期格式化的规则</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Formatter&lt;Date&gt; <span class="title">dateFormatter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> DateFormatter(<span class="keyword">this</span>.mvcProperties.getDateFormat());<span class="comment">//日期格式化组件</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    ==自己添加的格式化器转换器，我们只需要放在容器中即可==</p>
<ul>
<li><p>Support for <code>HttpMessageConverters</code> </p>
<ul>
<li><p>HttpMessageConverter：SpringMVC用来转换Http请求和响应的；User—Json.</p>
</li>
<li><p><code>HttpMessageConverters</code> 是从容器中确定,获取所有的HttpMessageConverter.</p>
<p>  ==自己给容器中添加HttpMessageConverter，只需要将自己的组件注册容器中（@Bean,@Component）==</p>
</li>
</ul>
</li>
<li><p>Automatic registration of <code>MessageCodesResolver</code>定义错误代码生成规则</p>
</li>
<li><p>Automatic use of a <code>ConfigurableWebBindingInitializer</code> bean 用来初始化WebDataBinder,例如请求数据绑定到JavaBean上,涉及到数据转换就要调用前面的转换器</p>
<p>  ==也可以配置一个ConfigurableWebBindingInitializer来替换默认的（添加到容器）==</p>
</li>
</ul>
<h3 id="2-扩展SpringMVC"><a href="#2-扩展SpringMVC" class="headerlink" title="2.扩展SpringMVC"></a>2.扩展SpringMVC</h3><p>上述一些配置是远远不够的.在原来的SpringMVC中,可以使用xml文件进行配置:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">mvc:view-controller</span> <span class="attr">path</span>=<span class="string">"/hello"</span> <span class="attr">view-name</span>=<span class="string">"success"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mvc:interceptors</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:interceptor</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">mvc:mapping</span> <span class="attr">path</span>=<span class="string">"/hello"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">bean</span>&gt;</span><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">mvc:interceptor</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mvc:interceptors</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>用配置方法来写:编写一个配置类,并且继承WebMvcConfigurerAdapter类型(Spring5之前的方法),不能标注@EnableWebMvc.在Spring5,SpringBoot2中,这种方法过时.两种新方法:1.直接实现WebMvcConfigurerAdapter的父接口WebMvcConfigurer,该接口内的所有方法都是默认方法,需要配置什么就重写什么.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200722221948117.png" alt="image-20200722221948117"></p>
<p>重写其中一个方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addViewControllers</span><span class="params">(ViewControllerRegistry registry)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//返送/company请求也到success页面</span></span><br><span class="line">    registry.addViewController(<span class="string">"/company"</span>).setViewName(<span class="string">"success"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>事实上上面的自动配置类WebMvcAutoConfiguration中有一些方法也是通过这个接口实现的:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200722223316785.png" alt="image-20200722223316785"></p>
<p>2.另一种方式是继承WebMvcConfigurationSupport这个类.仍然是重写其中方法即可.关于这两种方式的详细信息,参见</p>
<p><a href="https://segmentfault.com/a/1190000018904390?utm_source=tag-newest" target="_blank" rel="noopener">参考链接</a></p>
<h3 id="3-全面接管SpringMVC"><a href="#3-全面接管SpringMVC" class="headerlink" title="3.全面接管SpringMVC"></a>3.全面接管SpringMVC</h3><p>如果不需要自动配置,只需要添加@EnableWebMvc.为什么加了这个自动配置就失效,看图:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200722231002039.png" alt="image-20200722231002039"></p>
<p>而@EnableWebMvc中有<code>@Import(DelegatingWebMvcConfiguration.class)</code>,而这个DelegatingWebMvcConfiguration的父类正是WebMvcConfigurationSupport.</p>
<h2 id="4-如何修改SpringBoot的默认配置"><a href="#4-如何修改SpringBoot的默认配置" class="headerlink" title="4.如何修改SpringBoot的默认配置"></a>4.如何修改SpringBoot的默认配置</h2><p>模式：</p>
<pre><code>1. SpringBoot在自动配置很多组件的时候，先看容器中有没有用户自己配置的（@Bean、@Component）如果有就用用户配置的，如果没有，才自动配置；如果有些组件可以有多个（ViewResolver）将用户配置的和自己默认的组合起来.

2. 在SpringBoot中会有非常多的xxxConfigurer帮助我们进行扩展配置

3. 在SpringBoot中会有很多的xxxCustomizer帮助我们进行定制配置</code></pre><h2 id="5-RestfulCRUD"><a href="#5-RestfulCRUD" class="headerlink" title="5.RestfulCRUD"></a>5.RestfulCRUD</h2><h3 id="1-访问默认首页"><a href="#1-访问默认首页" class="headerlink" title="1.访问默认首页"></a>1.访问默认首页</h3><p>除了使用下面这种方式:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(&#123;<span class="string">"/"</span>,<span class="string">"/login"</span>&#125;)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">index</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"login"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>还可以使用这种方式:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyMvcConfig</span> <span class="keyword">implements</span> <span class="title">WebMvcConfigurer</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addViewControllers</span><span class="params">(ViewControllerRegistry registry)</span> </span>&#123;</span><br><span class="line">        registry.addViewController(<span class="string">"/"</span>).setViewName(<span class="string">"login"</span>);</span><br><span class="line">        registry.addViewController(<span class="string">"/index"</span>).setViewName(<span class="string">"login"</span>);</span><br><span class="line">        registry.addViewController(<span class="string">"/index.html"</span>).setViewName(<span class="string">"login"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>此外,对于静态资源的访问也要做出处理:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addResourceHandlers</span><span class="params">(ResourceHandlerRegistry registry)</span> </span>&#123;</span><br><span class="line">    registry.addResourceHandler(<span class="string">"/**"</span>).addResourceLocations(<span class="string">"classpath:/static/"</span>);</span><br><span class="line">    registry.addResourceHandler(<span class="string">"/webjars/**"</span>).addResourceLocations(<span class="string">"/webjars/"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-国际化"><a href="#2-国际化" class="headerlink" title="2.国际化"></a>2.国际化</h3><ol>
<li>编写国际化配置文件</li>
</ol>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200723142610188.png" alt="image-20200723142610188"></p>
<ol start="2">
<li>Spring Boot自动配置了管理国际化资源文件的组件</li>
</ol>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200723143052726.png" alt="image-20200723143052726"></p>
<ol start="3">
<li><p>配置应用的basename:<code>spring.messages.basename=i18n.login</code></p>
</li>
<li><p>页面获取国际化的值,login就是basename</p>
</li>
</ol>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200723150150403.png" alt="image-20200723150150403"></p>
<p>原理:在SpringMVC中国际化与区域信息对象Locale相关,要获得区域信息对象则通过LocaleResolver.在Spring Boot中,WebMvcAutoConfiguration中配置了这个对象,将其作为Bean加入了容器.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200723150613065.png" alt="image-20200723150613065"></p>
<p>可以看到先试着从配置文件中获取,接着是从请求头中获取.</p>
<p>另外要实现连接按钮更改语言,则在这个按钮上加上不同的语言参数:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200723155630298.png" alt="image-20200723155630298"></p>
<p>然后需要自定义LocaleResolver,因为上面默认的LocaleResolver处理的是请求头的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyLocalResolver</span> <span class="keyword">implements</span> <span class="title">LocaleResolver</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Locale <span class="title">resolveLocale</span><span class="params">(HttpServletRequest request)</span> </span>&#123;</span><br><span class="line">        String l = request.getParameter(<span class="string">"l"</span>);</span><br><span class="line">        Locale locale = Locale.getDefault();</span><br><span class="line">        <span class="keyword">if</span> (!StringUtils.isEmpty(l)) &#123;</span><br><span class="line">            String[] split = l.split(<span class="string">"_"</span>);</span><br><span class="line">            locale = <span class="keyword">new</span> Locale(split[<span class="number">0</span>], split[<span class="number">1</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> locale;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setLocale</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Locale locale)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>写好自定义的LocaleResolver要将它加入容器,替换掉原来的.在自己的WebMvcConfiger类(MyMvcConfig implements WebMvcConfigurer)中中添加这个Bean:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> LocaleResolver <span class="title">localeResolver</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> MyLocalResolver();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-登录"><a href="#3-登录" class="headerlink" title="3.登录"></a>3.登录</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LoginController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@PostMapping</span>(<span class="string">"/user/login"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">login</span><span class="params">(@RequestParam(<span class="string">"username"</span>)</span> String username, @<span class="title">RequestParam</span><span class="params">(<span class="string">"password"</span>)</span> String password,</span></span><br><span class="line"><span class="function">                        Map&lt;String, Object&gt; map, HttpSession session) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!StringUtils.isEmpty(username)&amp;&amp;<span class="string">"123456"</span>.equals(password)) &#123;</span><br><span class="line">            session.setAttribute(<span class="string">"loginUser"</span>, username);</span><br><span class="line">            <span class="comment">//登录成功后,防止表单重复提交,要做重定向</span></span><br><span class="line">            <span class="keyword">return</span> <span class="string">"redirect:/dashboard.html"</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">            map.put(<span class="string">"msg"</span>, <span class="string">"用户名密码错误"</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"login"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>判断用户名不为空且密码为123456后重定向到dashboard页面.</p>
<p>为了防止不登录直接进入dashboard页面,要配置一个拦截器.具体做法是实现HandlerInterceptor接口并实现prehandle方法.因为是要在处理请求前拦截的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    Object loginUser = request.getSession().getAttribute(<span class="string">"loginUser"</span>);</span><br><span class="line">    <span class="keyword">if</span> (loginUser == <span class="keyword">null</span>) &#123;</span><br><span class="line">        request.setAttribute(<span class="string">"msg"</span>,<span class="string">"没有权限"</span>);</span><br><span class="line">        request.getRequestDispatcher(<span class="string">"/login.html"</span>).forward(request, response);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后还要在MyMvcConfig中添加这个Bean.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addInterceptors</span><span class="params">(InterceptorRegistry registry)</span> </span>&#123;</span><br><span class="line">    registry.addInterceptor(<span class="keyword">new</span> LoginHandlerInterceptor()).addPathPatterns(<span class="string">"/dashboard.html"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-CRUD员工列表"><a href="#4-CRUD员工列表" class="headerlink" title="4.CRUD员工列表"></a>4.CRUD员工列表</h3><table>
<thead>
<tr>
<th>实验功能</th>
<th>请求URI</th>
<th>请求方式</th>
</tr>
</thead>
<tbody><tr>
<td>查询所有员工</td>
<td>emps</td>
<td>GET</td>
</tr>
<tr>
<td>查询某个员工(来到修改页面)</td>
<td>emp/1</td>
<td>GET</td>
</tr>
<tr>
<td>来到添加页面</td>
<td>emp</td>
<td>GET</td>
</tr>
<tr>
<td>添加员工</td>
<td>emp</td>
<td>POST</td>
</tr>
<tr>
<td>来到修改页面（查出员工进行信息回显）</td>
<td>emp/1</td>
<td>GET</td>
</tr>
<tr>
<td>修改员工</td>
<td>emp</td>
<td>PUT</td>
</tr>
<tr>
<td>删除员工</td>
<td>emp/1</td>
<td>DELETE</td>
</tr>
</tbody></table>
<p>先写查询所有员工,这个最简单:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@GetMapping</span>(<span class="string">"/emps"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">list</span><span class="params">(Model model)</span> </span>&#123;</span><br><span class="line">    Collection&lt;Employee&gt; employees = employeeDao.getAll();</span><br><span class="line">    model.addAttribute(<span class="string">"emps"</span>, employees);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"emp/list"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="thymeleaf公共元素抽取"><a href="#thymeleaf公共元素抽取" class="headerlink" title="thymeleaf公共元素抽取"></a>thymeleaf公共元素抽取</h4><p>dashboard页面和list页面中有许多页面元素是相同的,如顶部条,侧栏.考虑代码复用要把公共的部分抽取出来.</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">1、抽取公共片段</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:fragment</span>=<span class="string">"copy"</span>&gt;</span></span><br><span class="line">	 2011 The Good Thymes Virtual Grocery</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line">2、引入公共片段</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:insert</span>=<span class="string">"~&#123;footer :: copy&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">~&#123;templatename::selector&#125;：模板名::选择器</span><br><span class="line">~&#123;templatename::fragmentname&#125;:模板名::片段名</span><br><span class="line"></span><br><span class="line">3、默认效果：</span><br><span class="line">insert的公共片段在div标签中</span><br><span class="line">如果使用th:insert等属性进行引入，可以不用写~&#123;&#125;：</span><br><span class="line">行内写法可以加上：[[~&#123;&#125;]];[(~&#123;&#125;)]；</span><br></pre></td></tr></table></figure>

<p>三种引入公共片段的th属性：</p>
<p><strong>th:insert</strong>：将公共片段整个插入到声明引入的元素中</p>
<p><strong>th:replace</strong>：将声明引入的元素替换为公共片段</p>
<p><strong>th:include</strong>：将被引入的片段的<strong>内容</strong>包含进这个标签中(外层标签不要)</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">footer</span> <span class="attr">th:fragment</span>=<span class="string">"copy"</span>&gt;</span></span><br><span class="line"><span class="symbol">&amp;copy;</span> 2011 The Good Thymes Virtual Grocery</span><br><span class="line"><span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"></span><br><span class="line">引入方式</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:insert</span>=<span class="string">"footer :: copy"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:replace</span>=<span class="string">"footer :: copy"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:include</span>=<span class="string">"footer :: copy"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line">效果</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">footer</span>&gt;</span></span><br><span class="line">    <span class="symbol">&amp;copy;</span> 2011 The Good Thymes Virtual Grocery</span><br><span class="line">    <span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="symbol">&amp;copy;</span> 2011 The Good Thymes Virtual Grocery</span><br><span class="line"><span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="symbol">&amp;copy;</span> 2011 The Good Thymes Virtual Grocery</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="5-员工添加"><a href="#5-员工添加" class="headerlink" title="5.员工添加"></a>5.员工添加</h3><p>在员工列表页面加一个添加按钮,该按钮添加一个跳转到添加页面的超链接.</p>
<p>艹艹艹艹!这部分太零碎了,不写了,直接跳到下个部分.</p>
<h2 id="6-错误处理机制"><a href="#6-错误处理机制" class="headerlink" title="6.错误处理机制"></a>6.错误处理机制</h2><p>Spring Boot默认的错误页面已经见过了好多次了.浏览器返回一个Whitelabel Error Page页面,客户端返回一个json.</p>
<p>默认配置是在<code>ErrorMvcAutoConfiguration</code>这个类中.</p>
<p>先看这个类装配了哪些Bean:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200725111458563.png" alt="image-20200725111458563"></p>
<ul>
<li><p>DefaultErrorAttributes:在页面共享信息</p>
</li>
<li><p>BasicErrorController:处理默认/error请求</p>
</li>
<li><p>ErrorPageCustomizer,是一个静态内部类,有一个方法:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200725132328064.png" alt="image-20200725132328064"></p>
<p>里面的getPath方法就是获得出错后去的路径:</p>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Value</span>(<span class="string">"$&#123;error.path:/error&#125;"</span>)</span><br><span class="line"><span class="keyword">private</span> String path = <span class="string">"/error"</span>;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>DefaultErrorViewResolver</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> ModelAndView <span class="title">resolveErrorView</span><span class="params">(HttpServletRequest request, HttpStatus status,</span></span></span><br><span class="line"><span class="function"><span class="params">                                     Map&lt;String, Object&gt; model)</span> </span>&#123;</span><br><span class="line">    ModelAndView modelAndView = resolve(String.valueOf(status), model);</span><br><span class="line">    <span class="keyword">if</span> (modelAndView == <span class="keyword">null</span> &amp;&amp; SERIES_VIEWS.containsKey(status.series())) &#123;</span><br><span class="line">        modelAndView = resolve(SERIES_VIEWS.get(status.series()), model);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> modelAndView;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> ModelAndView <span class="title">resolve</span><span class="params">(String viewName, Map&lt;String, Object&gt; model)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//默认SpringBoot可以去找到一个页面？  error/404</span></span><br><span class="line">    String errorViewName = <span class="string">"error/"</span> + viewName;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//模板引擎可以解析这个页面地址就用模板引擎解析</span></span><br><span class="line">    TemplateAvailabilityProvider provider = <span class="keyword">this</span>.templateAvailabilityProviders</span><br><span class="line">        .getProvider(errorViewName, <span class="keyword">this</span>.applicationContext);</span><br><span class="line">    <span class="keyword">if</span> (provider != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="comment">//模板引擎可用的情况下返回到errorViewName指定的视图地址</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ModelAndView(errorViewName, model);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//模板引擎不可用，就在静态资源文件夹下找errorViewName对应的页面   error/404.html</span></span><br><span class="line">    <span class="keyword">return</span> resolveResource(errorViewName, model);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



</li>
</ul>
<p>一旦系统出现4xx或5xx错误,ErrorPageCustomizer就会生效(定制错误的响应规则),来到/error请求.接着由BasicErrorController这个Controller来处理/error请求:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200725132832861.png" alt="image-20200725132832861"></p>
<p>这个Controller处理的请求路径一样,但是产生不同的结果:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(produces = MediaType.TEXT_HTML_VALUE)</span><br><span class="line"><span class="function"><span class="keyword">public</span> ModelAndView <span class="title">errorHtml</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> </span>&#123;</span><br><span class="line">    HttpStatus status = getStatus(request);</span><br><span class="line">    Map&lt;String, Object&gt; model = Collections</span><br><span class="line">        .unmodifiableMap(getErrorAttributes(request, getErrorAttributeOptions(request, MediaType.TEXT_HTML)));</span><br><span class="line">    response.setStatus(status.value());</span><br><span class="line">    <span class="comment">//去哪个页面做错误页面,包括页面地址和内容</span></span><br><span class="line">    ModelAndView modelAndView = resolveErrorView(request, response, status, model);</span><br><span class="line">    <span class="keyword">return</span> (modelAndView != <span class="keyword">null</span>) ? modelAndView : <span class="keyword">new</span> ModelAndView(<span class="string">"error"</span>, model);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@RequestMapping</span></span><br><span class="line"><span class="keyword">public</span> ResponseEntity&lt;Map&lt;String, Object&gt;&gt; error(HttpServletRequest request) &#123;</span><br><span class="line">    HttpStatus status = getStatus(request);</span><br><span class="line">    <span class="keyword">if</span> (status == HttpStatus.NO_CONTENT) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ResponseEntity&lt;&gt;(status);</span><br><span class="line">    &#125;</span><br><span class="line">    Map&lt;String, Object&gt; body = getErrorAttributes(request, getErrorAttributeOptions(request, MediaType.ALL));</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> ResponseEntity&lt;&gt;(body, status);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一个是HTML页面,另一个是json数据.</p>
<p>resolveErrorView方法返回了modelAndView,决定了错误页面的数据和视图.model来自于DefaultErrorAttributes.</p>
<h3 id="1-定制错误响应"><a href="#1-定制错误响应" class="headerlink" title="1.定制错误响应"></a>1.定制错误响应</h3><h4 id="①定制错误页面"><a href="#①定制错误页面" class="headerlink" title="①定制错误页面"></a>①定制错误页面</h4><p>在有模板引擎的情况下,将错误页面命名为 错误状态码.html 放在模板引擎文件夹里面的 error文件夹下,发生此状态码的错误就会来到 对应的页面.</p>
<p>可以使用4xx和5xx作为错误页面的文件名来匹配这种类型的所有错误,精确优先(优先寻找精确的状态码.html)</p>
<p>在错误页面能获取的信息:</p>
<p>​                timestamp：时间戳</p>
<p>​                status：状态码</p>
<p>​                error：错误提示</p>
<p>​                exception：异常对象</p>
<p>​                message：异常消息</p>
<p>​                errors：JSR303数据校验的错误都在这里</p>
<p>如果没有模板引擎,就在静态资源文件夹下找.</p>
<p>如果都没有,默认来到SpringBoot默认的错误提示页面.</p>
<h4 id="②定制错误数据"><a href="#②定制错误数据" class="headerlink" title="②定制错误数据"></a>②定制错误数据</h4><p>定制json数据内容.</p>
<p>需要一个自定义的异常处理器:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyExceptionHandler</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ResponseBody</span></span><br><span class="line">    <span class="meta">@ExceptionHandler</span>(UserNotExistException<span class="class">.<span class="keyword">class</span>)//处理哪种异常</span></span><br><span class="line"><span class="class">    <span class="title">public</span> <span class="title">Map</span>&lt;<span class="title">String</span>,<span class="title">Object</span>&gt; <span class="title">handleException</span>(<span class="title">Exception</span> <span class="title">e</span>)</span>&#123;</span><br><span class="line">        Map&lt;String,Object&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        map.put(<span class="string">"code"</span>,<span class="string">"user.notexist"</span>);</span><br><span class="line">        map.put(<span class="string">"message"</span>,e.getMessage());</span><br><span class="line">        <span class="keyword">return</span> map;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是这种写法让前面的自动区分浏览器和客户端的效果没了.因为不是/error请求了.所以可以转发到这个请求:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="meta">@ExceptionHandler</span>(UserNotExistException<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">String</span> <span class="title">handleException</span>(<span class="title">Exception</span> <span class="title">e</span>, <span class="title">HttpServletRequest</span> <span class="title">request</span>)</span>&#123;</span><br><span class="line">    Map&lt;String,Object&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">    <span class="comment">//传入我们自己的错误状态码  4xx 5xx，否则就不会进入定制错误页面的解析流程</span></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * Integer statusCode = (Integer) request</span></span><br><span class="line"><span class="comment">         .getAttribute("javax.servlet.error.status_code");</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">    request.setAttribute(<span class="string">"javax.servlet.error.status_code"</span>,<span class="number">500</span>);</span><br><span class="line">    map.put(<span class="string">"code"</span>,<span class="string">"user.notexist"</span>);</span><br><span class="line">    map.put(<span class="string">"message"</span>,e.getMessage());</span><br><span class="line">    <span class="comment">//转发到/error</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">"forward:/error"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是这样做拿到的数据不是map里的,而是默认的/error请求的信息.上面说过,页面的信息放在model中,这个model</p>
<p>是通过DefaultErrorAttributes获取的(getErrorAttributes方法),因此自定义一个ErrorAttributes重写方法即可.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//给容器中加入我们自己定义的ErrorAttributes</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyErrorAttributes</span> <span class="keyword">extends</span> <span class="title">DefaultErrorAttributes</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title">getErrorAttributes</span><span class="params">(RequestAttributes requestAttributes, <span class="keyword">boolean</span> includeStackTrace)</span> </span>&#123;</span><br><span class="line">        Map&lt;String, Object&gt; map = <span class="keyword">super</span>.getErrorAttributes(requestAttributes, includeStackTrace);</span><br><span class="line">        map.put(<span class="string">"company"</span>,<span class="string">"atguigu"</span>);</span><br><span class="line">        <span class="keyword">return</span> map;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样错误信息除了自定义的还有map里放的.利用map还可以把异常处理器里的数据也放进来.做法是将异常处理器里的map放入request域中,随着转发使得MyErrorAttributes可以获得这个map,然后将这个map再放入MyErrorAttributes的map,即model中.</p>
<h2 id="7-配置嵌入式Servlet"><a href="#7-配置嵌入式Servlet" class="headerlink" title="7.配置嵌入式Servlet"></a>7.配置嵌入式Servlet</h2><h3 id="1-修改配置"><a href="#1-修改配置" class="headerlink" title="1.修改配置"></a>1.修改配置</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">server.port&#x3D;8081</span><br><span class="line">server.context-path&#x3D;&#x2F;crud</span><br><span class="line"></span><br><span class="line">server.tomcat.uri-encoding&#x3D;UTF-8</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F;通用的Servlet容器设置</span><br><span class="line">server.xxx</span><br><span class="line">&#x2F;&#x2F;Tomcat的设置</span><br><span class="line">server.tomcat.xxx</span><br></pre></td></tr></table></figure>

<p>或者编写一个<strong>EmbeddedServletContainerCustomizer</strong>,嵌入式的Servlet容器的定制器来修改servlet容器的配置.在Spring Boot2中,改类被废弃.要使用<strong>WebServerFactoryCustomizer</strong>来代替.在MyConfig类中添加这个Bean:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> WebServerFactoryCustomizer&lt;ConfigurableWebServerFactory&gt; <span class="title">webServerFactoryCustomizer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> WebServerFactoryCustomizer&lt;ConfigurableWebServerFactory&gt;() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">customize</span><span class="params">(ConfigurableWebServerFactory factory)</span> </span>&#123;</span><br><span class="line">            factory.setPort(<span class="number">8099</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>或者使用更简短的方式:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> WebServerFactoryCustomizer&lt;ConfigurableWebServerFactory&gt; <span class="title">webServerFactoryCustomizer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> factory -&gt; factory.setPort(<span class="number">8099</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-注册三大组件"><a href="#2-注册三大组件" class="headerlink" title="2.注册三大组件"></a>2.注册三大组件</h3><p>用ServletRegistrationBean注册Servlet,首先先写一个Servlet:MyServlet,然后用ServletRegistrationBean注册:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> ServletRegistrationBean&lt;HttpServlet&gt; <span class="title">myServlet</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> ServletRegistrationBean&lt;HttpServlet&gt;(<span class="keyword">new</span> MyServlet(), <span class="string">"/myServlet"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>而Filter和Listener与此类似,都是添加一个xxxRegistrationBean到容器中.</p>
<p>SpringBoot自动配置SpringMVC的时候也会配置DispatcherServlet,前端控制器,也有一个配置类:DispatcherServletAutoConfiguration.</p>
<h3 id="3-替换其它嵌入式容器"><a href="#3-替换其它嵌入式容器" class="headerlink" title="3.替换其它嵌入式容器"></a>3.替换其它嵌入式容器</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   引入web模块默认就是使用嵌入式的Tomcat作为Servlet容器；</span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>默认是引入tomcat,如果要换,则要排除掉然后引入新的:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 引入web模块 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-tomcat<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!--引入其他的Servlet容器--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-jetty<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="4-使用外置Servlet容器"><a href="#4-使用外置Servlet容器" class="headerlink" title="4.使用外置Servlet容器"></a>4.使用外置Servlet容器</h3><ol>
<li><p>创建一个war项目</p>
</li>
<li><p>将将嵌入式的Tomcat指定为provided</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-tomcat<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">scope</span>&gt;</span>provided<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>必须编写一个<strong>SpringBootServletInitializer</strong>的子类,并调用configure方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ServletInitializer</span> <span class="keyword">extends</span> <span class="title">SpringBootServletInitializer</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="function"><span class="keyword">protected</span> SpringApplicationBuilder <span class="title">configure</span><span class="params">(SpringApplicationBuilder application)</span> </span>&#123;</span><br><span class="line">       <span class="comment">//传入SpringBoot应用的主程序</span></span><br><span class="line">      <span class="keyword">return</span> application.sources(SpringBoot04WebJspApplication<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



</li>
</ol>
<h1 id="Docker"><a href="#Docker" class="headerlink" title="Docker"></a>Docker</h1><h2 id="1-Docker常用命令"><a href="#1-Docker常用命令" class="headerlink" title="1.Docker常用命令"></a>1.Docker常用命令</h2><table>
<thead>
<tr>
<th>操作</th>
<th>命令</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>检索</td>
<td>docker  search 关键字  eg：docker  search redis</td>
<td>我们经常去docker  hub上检索镜像的详细信息，如镜像的TAG。</td>
</tr>
<tr>
<td>拉取</td>
<td>docker pull 镜像名:tag</td>
<td>:tag是可选的，tag表示标签，多为软件的版本，默认是latest</td>
</tr>
<tr>
<td>列表</td>
<td>docker images</td>
<td>查看所有本地镜像</td>
</tr>
<tr>
<td>删除</td>
<td>docker rmi image-id</td>
<td>删除指定的本地镜像</td>
</tr>
</tbody></table>
<p>以上是关于镜像的,根据镜像启动后就是容器,启动方法:</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run --name mytomcat -d tomcat:latest</span><br></pre></td></tr></table></figure>

<p>mytomcat是自定义名称.-d表示是后台运行,后面的是镜像名,必要时需指定tag.</p>
<p>启动镜像其实类似于安装某个程序,一旦”安装”完成,该程序的启动就不是通过镜像这个”安装程序”,而是通过容器本身.</p>
<p>接着可以使用<code>docker ps</code>查看运行中的容器.加上参数-a可以查看所有的容器,包括停止的.</p>
<p>使用<code>docker stop id</code>停止容器.<code>docker start id</code>启动容器.</p>
<p>如果直接在浏览器里访问tomcat的8080端口是不行的,因为这个8080端口是容器的而不是host的.所以需要端口映射:</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run -d -p 8888:8080 tomcat</span><br></pre></td></tr></table></figure>

<p>将host的8888端口映射到8080端口.</p>
<p>查看容器的日志<code>docker logs 容器名/容器id</code>.删除容器<code>docker rm id</code></p>
<p>用一个镜像启动多个容器:注意名字和host端口不冲突即可.</p>
<h2 id="2-安装MySQL"><a href="#2-安装MySQL" class="headerlink" title="2.安装MySQL"></a>2.安装MySQL</h2><p>安装和上面一样,只是在启动时要</p>
<p>注意端口映射和用参数-e指定用户密码:</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run -p 4406:3306 --name mysql -e MYSQL_ROOT_PASSWORD=123456 -d mysql</span><br></pre></td></tr></table></figure>

<p>其它高级操作:</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run --name mysql03 -v /conf/mysql:/etc/mysql/conf.d -e MYSQL_ROOT_PASSWORD=my-secret-pw -d mysql:tag</span><br><span class="line">把主机的/conf/mysql文件夹挂载到 mysqldocker容器的/etc/mysql/conf.d文件夹里面</span><br><span class="line">改mysql的配置文件就只需要把mysql配置文件放在自定义的文件夹下（/conf/mysql）</span><br><span class="line"></span><br><span class="line">docker run --name some-mysql -e MYSQL_ROOT_PASSWORD=my-secret-pw -d mysql:tag --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci</span><br><span class="line">指定mysql的一些配置参数</span><br></pre></td></tr></table></figure>

<h1 id="SpringBoot与数据访问"><a href="#SpringBoot与数据访问" class="headerlink" title="SpringBoot与数据访问"></a>SpringBoot与数据访问</h1><h2 id="1-JDBC"><a href="#1-JDBC" class="headerlink" title="1.JDBC"></a>1.JDBC</h2><p>SpringBoot现在默认的数据源是Hikari.</p>
<p>数据源的相关配置都在DataSourceProperties里面.</p>
<p>支持的数据源:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200726144014757.png" alt="image-20200726144014757"></p>
<p>Generic表示自定义数据源.创建数据源步骤:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function">DataSource <span class="title">dataSource</span><span class="params">(DataSourceProperties properties)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> properties.initializeDataSourceBuilder().build();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-整合Druid"><a href="#2-整合Druid" class="headerlink" title="2.整合Druid"></a>2.整合Druid</h2><p>需要Druid和log4j的依赖.</p>
<p>然后配置一个Druid的管理平台:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DruidConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.datasource"</span>)</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSource <span class="title">druid</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> DruidDataSource();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//配置一个管理后台的Servlet,并设置一些初始化参数</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ServletRegistrationBean&lt;StatViewServlet&gt; <span class="title">statViewServlet</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        ServletRegistrationBean&lt;StatViewServlet&gt; bean = <span class="keyword">new</span> ServletRegistrationBean&lt;&gt;(<span class="keyword">new</span> StatViewServlet(), <span class="string">"/druid/*"</span>);</span><br><span class="line">        bean.setInitParameters(<span class="keyword">new</span> HashMap&lt;String, String&gt;() &#123;</span><br><span class="line">            &#123;</span><br><span class="line">                put(<span class="string">"loginUsername"</span>, <span class="string">"admin"</span>);</span><br><span class="line">                put(<span class="string">"loginPassword"</span>, <span class="string">"123456"</span>);</span><br><span class="line">                put(<span class="string">"allow"</span>, <span class="string">""</span>);<span class="comment">//默认允许所有访问</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//配置一个web监控的filter</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> FilterRegistrationBean&lt;WebStatFilter&gt; <span class="title">webStatFilter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        FilterRegistrationBean&lt;WebStatFilter&gt; bean = <span class="keyword">new</span> FilterRegistrationBean&lt;&gt;();</span><br><span class="line">        bean.setFilter(<span class="keyword">new</span> WebStatFilter());</span><br><span class="line">        bean.setInitParameters(<span class="keyword">new</span> HashMap&lt;&gt;() &#123;&#123;</span><br><span class="line">            put(<span class="string">"exclusions"</span>, <span class="string">"*.js,*.css,/druid/*"</span>);</span><br><span class="line">        &#125;&#125;);</span><br><span class="line">        bean.setUrlPatterns(Collections.singletonList(<span class="string">"/*"</span>));</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-注解版"><a href="#1-注解版" class="headerlink" title="1.注解版"></a>1.注解版</h3><p>在mapper包中写SQL语句,简单快捷,但是灵活性不够:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Mapper</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">DepartmentMapper</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Select</span>(<span class="string">"select * from department where id=#&#123;id&#125; "</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Department <span class="title">getDeptById</span><span class="params">(Integer id)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Select</span>(<span class="string">"delete from department where id=#&#123;id&#125; "</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">deleteDeptById</span><span class="params">(Integer id)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Options</span>(useGeneratedKeys = <span class="keyword">true</span>, keyProperty = <span class="string">"id"</span>)<span class="comment">//表示是否有自增,哪个是自增</span></span><br><span class="line">    <span class="meta">@Select</span>(<span class="string">"insert into department(departmentName) values(#&#123;departmentName&#125; )"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">insertDept</span><span class="params">(Department department)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Select</span>(<span class="string">"update department set departmentName=#&#123;departmentName&#125; where id=#&#123;id&#125; "</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">updateDept</span><span class="params">(Department department)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接着在Controller中注入这个Mapper,然后调用即可.</p>
<p>自定义Mybatis配置:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@org</span>.springframework.context.annotation.Configuration</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBatisConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ConfigurationCustomizer <span class="title">configurationCustomizer</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ConfigurationCustomizer()&#123;</span><br><span class="line"></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">customize</span><span class="params">(Configuration configuration)</span> </span>&#123;</span><br><span class="line">                configuration.setMapUnderscoreToCamelCase(<span class="keyword">true</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在接口上加的@Mapper注解可以去掉.需要在SpringBoot的启动类上加@mapperScan,并指定扫描的包.</p>
<h3 id="2-配置文件版"><a href="#2-配置文件版" class="headerlink" title="2.配置文件版"></a>2.配置文件版</h3><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">mybatis:</span></span><br><span class="line">  <span class="attr">config-location:</span> <span class="string">classpath:mybatis/mybatis-config.xml</span> <span class="string">指定全局配置文件的位置</span></span><br><span class="line">  <span class="attr">mapper-locations:</span> <span class="string">classpath:mybatis/mapper/*.xml</span>  <span class="string">指定sql映射文件的位置</span></span><br></pre></td></tr></table></figure>

<h1 id="SpringBoot启动原理"><a href="#SpringBoot启动原理" class="headerlink" title="SpringBoot启动原理"></a>SpringBoot启动原理</h1><p>1.创建SpringApplication对象</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200726232702324.png" alt="image-20200726232702324"></p>
<p>可以看到run方法后面创建了一个<strong>SpringApplication</strong>,让后在这个对象上调用run方法.</p>
<p>来看这个创建对象的过程,即其构造器.进入该来查看:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200726235109418.png" alt="image-20200726235109418"></p>
<p>本质上调用了两个参数的构造器:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">SpringApplication</span><span class="params">(ResourceLoader resourceLoader, Class&lt;?&gt;... primarySources)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//resourceLoader加载配置资源但是没在这初始化，后面再说</span></span><br><span class="line">    <span class="keyword">this</span>.resourceLoader = resourceLoader;</span><br><span class="line">    <span class="comment">//默认是启动类，不过有可能你自定义了一个配置类放到main class运行就需要配置一下</span></span><br><span class="line">    Assert.notNull(primarySources, <span class="string">"PrimarySources must not be null"</span>);</span><br><span class="line">    <span class="comment">//springApplication配置类</span></span><br><span class="line">    <span class="keyword">this</span>.primarySources = <span class="keyword">new</span> LinkedHashSet&lt;&gt;(Arrays.asList(primarySources));</span><br><span class="line">    <span class="comment">//设置servlet环境</span></span><br><span class="line">    <span class="keyword">this</span>.webApplicationType = deduceWebApplicationType();</span><br><span class="line">    <span class="comment">////获取ApplicationContextInitializer，也是在这里开始首次加载spring.factories文件</span></span><br><span class="line">    setInitializers((Collection) getSpringFactoriesInstances(</span><br><span class="line">        ApplicationContextInitializer<span class="class">.<span class="keyword">class</span>))</span>;</span><br><span class="line">    <span class="comment">//获取监听器，这里是第二次加载spring.factories文件</span></span><br><span class="line">    setListeners((Collection) getSpringFactoriesInstances(ApplicationListener<span class="class">.<span class="keyword">class</span>))</span>;</span><br><span class="line">    <span class="keyword">this</span>.mainApplicationClass = deduceMainApplicationClass();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里面最重要的就是setInitializers,setListeners.</p>
<p>其中setInitializers中的getSpringFactoriesInstances代表着去META-INF/spring.factories找配置的所有ApplicationContextInitializer并保存起来.</p>
<p>具体看这个方法:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727001738457.png" alt="image-20200727001738457"></p>
<p>注意注释写了用名字保证唯一.接着用SpringFactoriesLoader的loadFactoryNames获得所有唯一的ApplicationContextInitializer(参数type指的就是他).查看这个loadFactoryNames方法:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727002625214.png" alt="image-20200727002625214"></p>
<p>框住的常量就是META-INF/spring.factories.</p>
<p>当获取了所有ApplicationContextInitializer构成的Set集合后,用createSpringFactoriesInstances创建实例.创建完成后还排了下序,然后返回这个List.</p>
<p>而setInitializers方法本身就是一个setter方法,这意味着返回的list被保存在<strong>SpringApplication</strong>中.</p>
<p>setListeners这个方法与之类似,只不过保存的是ApplicationListener.</p>
<p>构造器的最后一步是<code>deduceMainApplicationClass()</code>,即推断哪个是main方法.做法是:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727004122412.png" alt="image-20200727004122412"></p>
<p>为什么要判断?这是因为传进来的是数组,可以有多个配置类.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727004937998.png" alt="image-20200727004937998"></p>
<p>现在创建SpringApplication对象的过程已结束,来到执行run方法</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727005102595.png" alt="image-20200727005102595"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> ConfigurableApplicationContext <span class="title">run</span><span class="params">(String... args)</span> </span>&#123;</span><br><span class="line">		StopWatch stopWatch = <span class="keyword">new</span> StopWatch();</span><br><span class="line">		stopWatch.start();</span><br><span class="line">		ConfigurableApplicationContext context = <span class="keyword">null</span>;</span><br><span class="line">		Collection&lt;SpringBootExceptionReporter&gt; exceptionReporters = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">		configureHeadlessProperty();</span><br><span class="line">		SpringApplicationRunListeners listeners = getRunListeners(args);</span><br><span class="line">		listeners.starting();</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			ApplicationArguments applicationArguments = <span class="keyword">new</span> DefaultApplicationArguments(args);</span><br><span class="line">			ConfigurableEnvironment environment = prepareEnvironment(listeners, applicationArguments);</span><br><span class="line">			configureIgnoreBeanInfo(environment);</span><br><span class="line">			Banner printedBanner = printBanner(environment);</span><br><span class="line">			context = createApplicationContext();</span><br><span class="line">			exceptionReporters = getSpringFactoriesInstances(SpringBootExceptionReporter<span class="class">.<span class="keyword">class</span>,</span></span><br><span class="line">					new Class[] &#123; ConfigurableApplicationContext.class &#125;, context);</span><br><span class="line">			prepareContext(context, environment, listeners, applicationArguments, printedBanner);</span><br><span class="line">			refreshContext(context);</span><br><span class="line">			afterRefresh(context, applicationArguments);</span><br><span class="line">			stopWatch.stop();</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">this</span>.logStartupInfo) &#123;</span><br><span class="line">				<span class="keyword">new</span> StartupInfoLogger(<span class="keyword">this</span>.mainApplicationClass).logStarted(getApplicationLog(), stopWatch);</span><br><span class="line">			&#125;</span><br><span class="line">			listeners.started(context);</span><br><span class="line">			callRunners(context, applicationArguments);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			handleRunFailure(context, ex, exceptionReporters, listeners);</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(ex);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			listeners.running(context);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			handleRunFailure(context, ex, exceptionReporters, <span class="keyword">null</span>);</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(ex);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> context;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>前面都是些初始化操作stopwatch是计时器:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727122947429.png" alt="image-20200727122947429">挑出后面重要的来看.第七行,获取了一个非常重要的Listener:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">SpringApplicationRunListeners listeners = getRunListeners(args);</span><br></pre></td></tr></table></figure>

<p>这个Listener如何获取,进入getRunListeners一看:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727123245183.png" alt="image-20200727123245183"></p>
<p>又是前面getSpringFactoriesInstances的操作.只不过这里获取的是SpringApplicationRunListeners,和前面的ApplicationListener不是一回事.通过debug发现getSpringFactoriesInstances返回的instances实际是SpringApplicationRunListener的实现类EventPublishingRunListener.具体这个实例怎么来的呢?</p>
<p>在进一次createSpringFactoriesInstances.注意此时上一步获得名字时names里已经是EventPublishingRunListener.进入后里面使用了createSpringFactoriesInstances方法,该方法里使用<code>BeanUtils.instantiateClass(constructor, args)</code>来创建实例对象(根据构造器和参数,具体也就是通过反射得到对应的构造器方法.)这样就来到了<code>EventPublishingRunListener</code>的构造器:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">EventPublishingRunListener</span><span class="params">(SpringApplication application, String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.application = application;</span><br><span class="line">    <span class="keyword">this</span>.args = args;</span><br><span class="line">    <span class="keyword">this</span>.initialMulticaster = <span class="keyword">new</span> SimpleApplicationEventMulticaster();</span><br><span class="line">    <span class="keyword">for</span> (ApplicationListener&lt;?&gt; listener : application.getListeners()) &#123;</span><br><span class="line">        <span class="keyword">this</span>.initialMulticaster.addApplicationListener(listener);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>构造器中有个循环.大意是将刚才存在SpringApplication中的Listeners中的Listener全部添加到initialMulticaster中.而initialMulticaster的类型是SimpleApplicationEventMulticaster,它继承了抽象类AbstractApplicationEventMulticaster.这个类中有添加方法.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727153615597.png" alt="image-20200727153615597"></p>
<p>所以<code>getRunListeners(args)</code>这一句的的结果是下图,获取了一个事件监听器,该监听器内部还包含了多个监听器:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727123839514.png" alt="image-20200727123839514"></p>
<p>接着是<code>listeners.starting();</code>这句其实简单,就是把Listeners里面的每个Listener的start方法调用一下.</p>
<p>这是每个Listener(SpringApplicationRunListener)的方法:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727141240880.png" alt="image-20200727141240880"></p>
<p>而现在Listeners中的EventPublishingRunListener重写了这些方法:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727141735742.png" alt="image-20200727141735742"></p>
<p>所以调用start方法时调用的也是这里重写的.debug进这个starting方法看看:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">starting</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.initialMulticaster.multicastEvent(<span class="keyword">new</span> ApplicationStartingEvent(<span class="keyword">this</span>.application, <span class="keyword">this</span>.args));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里new了一个ApplicationStartingEvent(应用启动事件).这里涉及到事件机制,可以看下这个链接<a href="https://www.cnblogs.com/youzhibing/p/9593788.html" target="_blank" rel="noopener">事件机制</a></p>
<p>EventPublishingRunListener就是事件监听器,而事件的触发器是SpringApplication.</p>
<p>现在构建了一个ApplicationStartingEvent事件(先不关注事件对象怎么构建的).<strong>multicast</strong>表示多播,这里指将事件发布出去.看看这个multicastEvent方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">multicastEvent</span><span class="params">(<span class="keyword">final</span> ApplicationEvent event, @Nullable ResolvableType eventType)</span> </span>&#123;</span><br><span class="line">    ResolvableType type = (eventType != <span class="keyword">null</span> ? eventType : resolveDefaultEventType(event));</span><br><span class="line">    Executor executor = getTaskExecutor();</span><br><span class="line">    <span class="keyword">for</span> (ApplicationListener&lt;?&gt; listener : getApplicationListeners(event, type)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (executor != <span class="keyword">null</span>) &#123;</span><br><span class="line">            executor.execute(() -&gt; invokeListener(listener, event));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            invokeListener(listener, event);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>大意就是遍历getApplicationListeners(event, type)，然后对每个listener进行invokeListener(listener, event).</p>
<p>先看的结果:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727155023738.png" alt="image-20200727155023738"></p>
<p>而在SpringApplication中存的不止这么多,所以中间肯定有筛选.筛选条件那就是参数(event, type),event是ApplicationStartingEvent,type是:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727155451718.png" alt="image-20200727155451718"></p>
<p>进入getApplicationListeners方法,根据注释可知:该方法作用:返回与给定事件类型匹配的ApplicationListeners集合,非匹配的侦听器会被提前排除,允许根据缓存的匹配结果来返回。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> Collection&lt;ApplicationListener&lt;?&gt;&gt; getApplicationListeners(</span><br><span class="line">			ApplicationEvent event, ResolvableType eventType) &#123;</span><br><span class="line"></span><br><span class="line">		Object source = event.getSource();</span><br><span class="line">		Class&lt;?&gt; sourceType = (source != <span class="keyword">null</span> ? source.getClass() : <span class="keyword">null</span>);</span><br><span class="line">		ListenerCacheKey cacheKey = <span class="keyword">new</span> ListenerCacheKey(eventType, sourceType);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Quick check for existing entry on ConcurrentHashMap...</span></span><br><span class="line">		ListenerRetriever retriever = <span class="keyword">this</span>.retrieverCache.get(cacheKey);</span><br><span class="line">		<span class="keyword">if</span> (retriever != <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> retriever.getApplicationListeners();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.beanClassLoader == <span class="keyword">null</span> ||</span><br><span class="line">				(ClassUtils.isCacheSafe(event.getClass(), <span class="keyword">this</span>.beanClassLoader) &amp;&amp;</span><br><span class="line">						(sourceType == <span class="keyword">null</span> || ClassUtils.isCacheSafe(sourceType, <span class="keyword">this</span>.beanClassLoader)))) &#123;</span><br><span class="line">			<span class="comment">// Fully synchronized building and caching of a ListenerRetriever</span></span><br><span class="line">			<span class="keyword">synchronized</span> (<span class="keyword">this</span>.retrievalMutex) &#123;</span><br><span class="line">				retriever = <span class="keyword">this</span>.retrieverCache.get(cacheKey);</span><br><span class="line">				<span class="keyword">if</span> (retriever != <span class="keyword">null</span>) &#123;</span><br><span class="line">					<span class="keyword">return</span> retriever.getApplicationListeners();</span><br><span class="line">				&#125;</span><br><span class="line">				retriever = <span class="keyword">new</span> ListenerRetriever(<span class="keyword">true</span>);</span><br><span class="line">				Collection&lt;ApplicationListener&lt;?&gt;&gt; listeners =</span><br><span class="line">						retrieveApplicationListeners(eventType, sourceType, retriever);</span><br><span class="line">				<span class="keyword">this</span>.retrieverCache.put(cacheKey, retriever);</span><br><span class="line">				<span class="keyword">return</span> listeners;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="comment">// No ListenerRetriever caching -&gt; no synchronization necessary</span></span><br><span class="line">			<span class="keyword">return</span> retrieveApplicationListeners(eventType, sourceType, <span class="keyword">null</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>主要涉及到3个点：缓存retrieverCache、retrieveApplicationListeners已经retrieveApplicationListeners中调用的supportsEvent方法。流程是这样的：</p>
<p>1、缓存中是否有匹配的结果，有则返回</p>
<p>2、若缓存中没有匹配的结果，则从this.defaultRetriever.applicationListeners中过滤，这个this表示的EventPublishingRunListener对象的属性initialMulticaster（也就是SimpleApplicationEventMulticaster对象，而defaultRetriever.applicationListeners的值也是在EventPublishingRunListener构造方法中初始化的）</p>
<p>3、过滤过程，遍历defaultRetriever.applicationListeners集合，从中找出ApplicationStartingEvent匹配的listener，具体的匹配规则需要看各个listener的supportsEventType方法（有两个重载的方法）</p>
<p>4、将过滤的结果缓存到retrieverCache</p>
<p>5、将过滤出的结果返回回去</p>
<p>现在得到了一个ApplicationListeners集合,然后来到invokeListener方法.该方法注释是<strong>使用给定的事件调用给定的监听器</strong>.深入该方法发现里面实际执行了<code>doInvokeListener(listener, event)</code>方法.该方法实际执行了listener.onApplicationEvent方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onApplicationEvent</span><span class="params">(ApplicationEvent event)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (event <span class="keyword">instanceof</span> ApplicationStartingEvent) &#123;</span><br><span class="line">        onApplicationStartingEvent((ApplicationStartingEvent) event);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (event <span class="keyword">instanceof</span> ApplicationEnvironmentPreparedEvent) &#123;</span><br><span class="line">        onApplicationEnvironmentPreparedEvent((ApplicationEnvironmentPreparedEvent) event);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (event <span class="keyword">instanceof</span> ApplicationPreparedEvent) &#123;</span><br><span class="line">        onApplicationPreparedEvent((ApplicationPreparedEvent) event);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (event <span class="keyword">instanceof</span> ContextClosedEvent</span><br><span class="line">             &amp;&amp; ((ContextClosedEvent) event).getApplicationContext().getParent() == <span class="keyword">null</span>) &#123;</span><br><span class="line">        onContextClosedEvent();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (event <span class="keyword">instanceof</span> ApplicationFailedEvent) &#123;</span><br><span class="line">        onApplicationFailedEvent();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到这里根据事件类型调用相应的方法.那么现在就是调用onApplicationStartingEvent方法.此时的listener是LoggingApplicationListener</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727161227480.png" alt="image-20200727161227480"></p>
<p>可以看到这里做了一些和日志相关的工作,具体不深究.</p>
<p>其它几个ApplicationListener也是差不多如此:</p>
<p>LoggingApplicationListener：检测正在使用的日志系统，默认是logback，支持3种，优先级从高到低：logback &gt; log4j &gt; javalog。此时日志系统还没有初始化</p>
<p>BackgroundPreinitializer：另起一个线程实例化Initializer并调用其run方法，包括验证器、消息转换器等等</p>
<p>DelegatingApplicationListener：此时什么也没做</p>
<p>LiquibaseServiceLocatorApplicationListener：此时什么也没做</p>
<p>回顾总结起来就是listerns的start这个事件触发了上述的事件响应.</p>
<p>接着来到try内部:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727162117716.png" alt="image-20200727162117716"></p>
<p>这里先是封装了下参数,接着准备环境.查看这个方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> ConfigurableEnvironment <span class="title">prepareEnvironment</span><span class="params">(SpringApplicationRunListeners listeners,</span></span></span><br><span class="line"><span class="function"><span class="params">      ApplicationArguments applicationArguments)</span> </span>&#123;</span><br><span class="line">   <span class="comment">// Create and configure the environment</span></span><br><span class="line">   ConfigurableEnvironment environment = getOrCreateEnvironment();</span><br><span class="line">   configureEnvironment(environment, applicationArguments.getSourceArgs());</span><br><span class="line">   ConfigurationPropertySources.attach(environment);</span><br><span class="line">    <span class="comment">//这里又调用listener的方法</span></span><br><span class="line">   listeners.environmentPrepared(environment);</span><br><span class="line">   bindToSpringApplication(environment);</span><br><span class="line">   <span class="keyword">if</span> (!<span class="keyword">this</span>.isCustomEnvironment) &#123;</span><br><span class="line">      environment = <span class="keyword">new</span> EnvironmentConverter(getClassLoader()).convertEnvironmentIfNecessary(environment,</span><br><span class="line">            deduceEnvironmentClass());</span><br><span class="line">   &#125;</span><br><span class="line">   ConfigurationPropertySources.attach(environment);</span><br><span class="line">   <span class="keyword">return</span> environment;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>listeners.environmentPrepared(environment);</code>和刚才的starting类似,只不过事件变了:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727162655705.png" alt="image-20200727162655705"></p>
<p>根据刚才的分析,事件会触发事件响应,而这个事件触发的内容就不展开了<a href="https://www.cnblogs.com/youzhibing/p/9622441.html" target="_blank" rel="noopener">参考链接</a></p>
<p>环境准备完成后是打印banner.</p>
<p>接着是非常重要的<code>createApplicationContext();</code>方法里面判断了一下,然后利用<code>BeanUtils.instantiateClass(contextClass)</code>创建出来.</p>
<p>然后是一个异常报告,略过.接着是准备上下文:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">prepareContext(context, environment, listeners, applicationArguments, printedBanner);</span><br></pre></td></tr></table></figure>

<p>这个方法中也做了一些重要的事:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727164624937.png" alt="image-20200727164624937"></p>
<p>首先保存了环境,然后是后处理注册了一些组件.</p>
<p>接着的applyInitializers方法做了初始化工作:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727164958362.png" alt="image-20200727164958362"></p>
<p>这里使用了前面存在SpringApplication中的ApplicationContextInitializer.</p>
<p>接着又是<code>listeners.contextPrepared(context);</code>这样的方法,还是老套路,这回的事件又换了一个:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727165405272.png" alt="image-20200727165405272"></p>
<p>上上张图的四个方法完成后又做了一些杂七杂八的处理,最后又是一个listener:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727165906844.png" alt="image-20200727165906844"></p>
<p>这会还是大体不变,换成另一个事件:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727170040742.png" alt="image-20200727170040742"></p>
<p>prepareContext准备上下文完成后,是两个方法:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727170407820.png" alt="image-20200727170407820"></p>
<p>刷新容器,ioc容器初始化:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">refresh</span><span class="params">()</span> <span class="keyword">throws</span> BeansException, IllegalStateException </span>&#123;</span><br><span class="line">		<span class="keyword">synchronized</span> (<span class="keyword">this</span>.startupShutdownMonitor) &#123;</span><br><span class="line">			<span class="comment">// Prepare this context for refreshing.</span></span><br><span class="line">			prepareRefresh();</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Tell the subclass to refresh the internal bean factory.</span></span><br><span class="line">			ConfigurableListableBeanFactory beanFactory = obtainFreshBeanFactory();</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Prepare the bean factory for use in this context.</span></span><br><span class="line">			prepareBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">				<span class="comment">// Allows post-processing of the bean factory in context subclasses.</span></span><br><span class="line">				postProcessBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Invoke factory processors registered as beans in the context.</span></span><br><span class="line">				invokeBeanFactoryPostProcessors(beanFactory);</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Register bean processors that intercept bean creation.</span></span><br><span class="line">				registerBeanPostProcessors(beanFactory);</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Initialize message source for this context.</span></span><br><span class="line">				initMessageSource();</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Initialize event multicaster for this context.</span></span><br><span class="line">				initApplicationEventMulticaster();</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Initialize other special beans in specific context subclasses.</span></span><br><span class="line">				onRefresh();</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Check for listener beans and register them.</span></span><br><span class="line">				registerListeners();</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Instantiate all remaining (non-lazy-init) singletons.</span></span><br><span class="line">				finishBeanFactoryInitialization(beanFactory);</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Last step: publish corresponding event.</span></span><br><span class="line">				finishRefresh();</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">				<span class="keyword">if</span> (logger.isWarnEnabled()) &#123;</span><br><span class="line">					logger.warn(<span class="string">"Exception encountered during context initialization - "</span> +</span><br><span class="line">							<span class="string">"cancelling refresh attempt: "</span> + ex);</span><br><span class="line">				&#125;</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Destroy already created singletons to avoid dangling resources.</span></span><br><span class="line">				destroyBeans();</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Reset 'active' flag.</span></span><br><span class="line">				cancelRefresh(ex);</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Propagate exception to caller.</span></span><br><span class="line">				<span class="keyword">throw</span> ex;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">finally</span> &#123;</span><br><span class="line">				<span class="comment">// Reset common introspection caches in Spring's core, since we</span></span><br><span class="line">				<span class="comment">// might not ever need metadata for singleton beans anymore...</span></span><br><span class="line">				resetCommonCaches();</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>接着的afterRefresh方法是个空方法,在老版本中该方法回调用callRunners方法,现在callRunners直接挪到了run方法中.</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727172307917.png" alt="image-20200727172307917"></p>
<p>第一个方法发布了一个新事件:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727173150101.png" alt="image-20200727173150101"></p>
<p>而callRunners是两种运行方式:</p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/image-20200727173243795.png" alt="image-20200727173243795"></p>
<p>ApplicationRunner的优先级高于CommandLineRunner.</p>
<p>最后run方法返回了context,即ioc容器.</p>
<h2 id="SpringBoot自定义Starter"><a href="#SpringBoot自定义Starter" class="headerlink" title="SpringBoot自定义Starter"></a>SpringBoot自定义Starter</h2><p>SpringBoot最强大的功能就是各种预置的starter免去了大量的配置工作.但是免不了需要自定义starter.</p>
<ol>
<li>这个starter需要的依赖是什么?</li>
<li>如何编写自动配置<ul>
<li>@Configuration 指定这个类是配置类</li>
<li>@ConditionalonXXX 指定在什么条件下生效的配置</li>
<li>@AutoConfigureOrder指定顺序</li>
<li>@Bean给容器添加组件</li>
<li>@ConfigurationProperties结合xxxProperties类来绑定相关配置</li>
</ul>
</li>
</ol>
<p>将需要启动就加载的自定配置类配置在META-INF/spring.properties文件中.</p>
]]></content>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringCloud学习</title>
    <url>/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<p>记录一下学习SpringCloud的内容.</p>
<a id="more"></a>
<h1 id="SpringCloud"><a href="#SpringCloud" class="headerlink" title="SpringCloud:"></a>SpringCloud:</h1><h2 id="0-SpringCloud升级-部分组件停用"><a href="#0-SpringCloud升级-部分组件停用" class="headerlink" title="0,SpringCloud升级,部分组件停用:"></a>0,SpringCloud升级,部分组件停用:</h2><p>1,Eureka停用,可以使用zk作为服务注册中心</p>
<p>2,服务调用,Ribbon准备停更,代替为LoadBalance</p>
<p>3,Feign改为OpenFeign</p>
<p>4,Hystrix停更,改为resilence4j</p>
<p>​        或者阿里巴巴的sentienl</p>
<p>5.Zuul改为gateway</p>
<p>6,服务配置Config改为  Nacos</p>
<p>7,服务总线Bus改为Nacos</p>
<h1 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建:"></a>环境搭建:</h1><h2 id="1-创建父工程-pom依赖"><a href="#1-创建父工程-pom依赖" class="headerlink" title="1,创建父工程,pom依赖"></a>1,创建父工程,pom依赖</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">....</span><br></pre></td></tr></table></figure>

<h2 id="2-创建子模块-pay模块"><a href="#2-创建子模块-pay模块" class="headerlink" title="2,创建子模块,pay模块"></a>2,创建子模块,pay模块</h2><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%843.png" alt></p>
<h3 id="1-子模块名字"><a href="#1-子模块名字" class="headerlink" title="1,子模块名字:"></a>1,子模块名字:</h3><p>​        cloud_pay_8001</p>
<h3 id="2-pom依赖"><a href="#2-pom依赖" class="headerlink" title="2,pom依赖"></a>2,pom依赖</h3><h3 id="3-创建application-yml"><a href="#3-创建application-yml" class="headerlink" title="3,创建application.yml"></a>3,创建application.yml</h3><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">	<span class="attr">port:</span> <span class="number">8001</span>   </span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">	<span class="attr">application:</span></span><br><span class="line">		<span class="attr">name:</span> <span class="string">cloud-payment-service</span></span><br><span class="line">	<span class="attr">datasource:</span></span><br><span class="line">    <span class="comment"># 当前数据源操作类型</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">com.alibaba.druid.pool.DruidDataSource</span></span><br><span class="line">    <span class="comment"># mysql驱动类</span></span><br><span class="line">    <span class="attr">driver-class-name:</span> <span class="string">com.mysql.cj.jdbc.Driver</span></span><br><span class="line">      <span class="attr">url:</span> <span class="string">jdbc:mysql://localhost:3306/db2019?useUnicode=true&amp;characterEncoding=</span></span><br><span class="line">            <span class="string">UTF-8&amp;useSSL=false&amp;serverTimezone=GMT%2B8</span>								</span><br><span class="line">    <span class="attr">username:</span> <span class="string">root</span></span><br><span class="line">    <span class="attr">password:</span> <span class="string">root</span></span><br><span class="line"><span class="attr">mybatis:</span>			</span><br><span class="line">    <span class="attr">mapper-locations:</span> <span class="string">classpath*:mapper/*.xml</span></span><br><span class="line">   	<span class="attr">type-aliases-package:</span> <span class="string">com.eiletxie.springcloud.entities</span></span><br><span class="line">   			<span class="string">它一般对应我们的实体类所在的包，这个时候会自动取对应包中不包括包名的简单类名作为包括包名的别名。多个package之间可以用逗号或者分号等来进行分隔（value的值一定要是包的全）</span></span><br></pre></td></tr></table></figure>

<h3 id="4-主启动类"><a href="#4-主启动类" class="headerlink" title="4,主启动类"></a>4,主启动类</h3><p>​        ….</p>
<h3 id="5-业务类"><a href="#5-业务类" class="headerlink" title="5,业务类"></a>5,业务类</h3><h4 id="1-sql"><a href="#1-sql" class="headerlink" title="1,sql"></a>1,sql</h4><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%844.png" alt></p>
<h4 id="2-实体类"><a href="#2-实体类" class="headerlink" title="2,实体类"></a>2,实体类</h4><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%845.png" alt></p>
<h4 id="3-entity类"><a href="#3-entity类" class="headerlink" title="3,.entity类"></a>3,.entity类</h4><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%846.png" alt></p>
<h4 id="4-dao层"><a href="#4-dao层" class="headerlink" title="4,dao层:"></a>4,dao层:</h4><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%847.png" alt></p>
<h4 id="5-mapper配置文件类"><a href="#5-mapper配置文件类" class="headerlink" title="5,mapper配置文件类"></a>5,mapper配置文件类</h4><p>​                <strong>在resource下,创建mapper/PayMapper.xml</strong></p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%848.png" alt></p>
<h4 id="6-写service和serviceImpl"><a href="#6-写service和serviceImpl" class="headerlink" title="6,写service和serviceImpl"></a>6,写service和serviceImpl</h4><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%849.png" alt></p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%8410.png" alt="sc的9"></p>
<h4 id="7-controller"><a href="#7-controller" class="headerlink" title="7,controller"></a>7,controller</h4><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%8411.png" alt></p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%8412.png" alt></p>
<h2 id="3-热部署"><a href="#3-热部署" class="headerlink" title="3,热部署:"></a>3,热部署:</h2><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%8413.png" alt></p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/sc%E7%9A%8414.png" alt></p>
<p>…..</p>
<p>…..</p>
<p>….</p>
<h2 id="4-order模块"><a href="#4-order模块" class="headerlink" title="4,order模块"></a>4,order模块</h2><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/E:%5Cblog%5Csource_posts%5CSpringCloud%E5%AD%A6%E4%B9%A0%5Csc%E7%9A%843.png" alt></p>
<h3 id="1-pom"><a href="#1-pom" class="headerlink" title="1,pom"></a><strong>1,pom</strong></h3><h3 id="2-yml配置文件"><a href="#2-yml配置文件" class="headerlink" title="2,yml配置文件"></a><strong>2,yml配置文件</strong></h3><p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/order%E6%A8%A1%E5%9D%971.png" alt></p>
<h3 id="3-主启动类"><a href="#3-主启动类" class="headerlink" title="3,主启动类"></a><strong>3,主启动类</strong></h3><h3 id="4-复制pay模块的实体类-entity类"><a href="#4-复制pay模块的实体类-entity类" class="headerlink" title="4.复制pay模块的实体类,entity类"></a><strong>4.复制pay模块的实体类,entity类</strong></h3><h3 id="5-写controller类"><a href="#5-写controller类" class="headerlink" title="5,写controller类"></a><strong>5,写controller类</strong></h3><p>​        因为这里是消费者类,主要是消费,那么就没有service和dao,需要调用pay模块的方法</p>
<p>​        并且这里还没有微服务的远程调用,那么如果要调用另外一个模块,则需要使用基本的api调用</p>
<p>使用RestTemplate调用pay模块,</p>
<p>​    <img src="https://gitee.com/xianzhetm/photo/raw/master/img/order%E6%A8%A1%E5%9D%972.png" alt></p>
<p><img src="https://gitee.com/xianzhetm/photo/raw/master/img/order%E6%A8%A1%E5%9D%973.png" alt></p>
<p>​    将restTemplate注入到容器</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Corder%E6%A8%A1%E5%9D%974.png" alt></p>
<p>编写controller:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Corder%E6%A8%A1%E5%9D%975.png" alt></p>
<h2 id="5-重构"><a href="#5-重构" class="headerlink" title="5,重构,"></a>5,重构,</h2><p>新建一个模块,将重复代码抽取到一个公共模块中</p>
<h3 id="1-创建commons模块"><a href="#1-创建commons模块" class="headerlink" title="1,创建commons模块"></a>1,创建commons模块</h3><h3 id="2-抽取公共pom"><a href="#2-抽取公共pom" class="headerlink" title="2,抽取公共pom"></a>2,抽取公共pom</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Ccommons%E6%A8%A1%E5%9D%97.png" alt></p>
<h3 id="3-entity和实体类放入commons中"><a href="#3-entity和实体类放入commons中" class="headerlink" title="3,entity和实体类放入commons中"></a>3,entity和实体类放入commons中</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Ccommons%E6%A8%A1%E5%9D%972.png" alt></p>
<h3 id="4-使用mavne-将commone模块打包-install"><a href="#4-使用mavne-将commone模块打包-install" class="headerlink" title="4,使用mavne,将commone模块打包(install),"></a>4,使用mavne,将commone模块打包(install),</h3><p>​        其他模块引入commons</p>
<h1 id="2-服务注册与发现"><a href="#2-服务注册与发现" class="headerlink" title="2,服务注册与发现"></a>2,服务注册与发现</h1><h2 id="6-Eureka"><a href="#6-Eureka" class="headerlink" title="6,Eureka:"></a>6,Eureka:</h2><p>前面我们没有服务注册中心,也可以服务间调用,为什么还要服务注册?</p>
<p>当服务很多时,单靠代码手动管理是很麻烦的,需要一个公共组件,统一管理多服务,包括服务是否正常运行,等</p>
<p>Eureka用于<strong>==服务注册==</strong>,目前官网<strong>已经停止更新</strong></p>
<p>​    <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%841.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%842.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%843.png" alt></p>
<p> <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%844.png" alt></p>
<h3 id="单机版eureka"><a href="#单机版eureka" class="headerlink" title="单机版eureka:"></a><strong>单机版eureka:</strong></h3><h4 id="1-创建项目cloud-eureka-server-7001"><a href="#1-创建项目cloud-eureka-server-7001" class="headerlink" title="1,创建项目cloud_eureka_server_7001"></a><strong>1,创建项目cloud_eureka_server_7001</strong></h4><h4 id="2-引入pom依赖"><a href="#2-引入pom依赖" class="headerlink" title="2,引入pom依赖"></a><strong>2,引入pom依赖</strong></h4><p>​        eurka最新的依赖变了</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%845.png" alt></p>
<h4 id="3-配置文件"><a href="#3-配置文件" class="headerlink" title="3,配置文件:"></a>3,配置文件:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%846.png" alt></p>
<h4 id="4-主启动类-1"><a href="#4-主启动类-1" class="headerlink" title="4,主启动类"></a>4,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%847.png" alt></p>
<h4 id="5-此时就可以启动当前项目了"><a href="#5-此时就可以启动当前项目了" class="headerlink" title="5,此时就可以启动当前项目了"></a><strong>5,此时就可以启动当前项目了</strong></h4><h4 id="6-其他服务注册到eureka"><a href="#6-其他服务注册到eureka" class="headerlink" title="6,其他服务注册到eureka:"></a><strong>6,其他服务注册到eureka:</strong></h4><p>比如此时pay模块加入eureka:</p>
<h5 id="1-主启动类上-加注解-表示当前是eureka客户端"><a href="#1-主启动类上-加注解-表示当前是eureka客户端" class="headerlink" title="1.主启动类上,加注解,表示当前是eureka客户端"></a>1.主启动类上,加注解,表示当前是eureka客户端</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8410.png" alt></p>
<h5 id="2-修改pom-引入"><a href="#2-修改pom-引入" class="headerlink" title="2,修改pom,引入"></a>2,修改pom,引入</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%848.png" alt></p>
<h5 id="3-修改配置文件"><a href="#3-修改配置文件" class="headerlink" title="3,修改配置文件:"></a>3,修改配置文件:</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%849.png" alt></p>
<h5 id="4-pay模块重启-就可以注册到eureka中了"><a href="#4-pay模块重启-就可以注册到eureka中了" class="headerlink" title="4,pay模块重启,就可以注册到eureka中了"></a>4,pay模块重启,就可以注册到eureka中了</h5><p><strong>==order模块的注册是一样的==</strong></p>
<h3 id="集群版eureka"><a href="#集群版eureka" class="headerlink" title="集群版eureka:"></a>集群版eureka:</h3><h4 id="集群原理"><a href="#集群原理" class="headerlink" title="集群原理:"></a>集群原理:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8411.png" alt></p>
 <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span>,就是pay模块启动时,注册自己,并且自身信息也放入eureka</span><br><span class="line"><span class="number">2</span>.order模块,首先也注册自己,放入信息,当要调用pay时,先从eureka拿到pay的调用地址</span><br><span class="line"><span class="number">3</span>.通过HttpClient调用</span><br><span class="line"> 	并且还会缓存一份到本地,每<span class="number">30</span>秒更新一次</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8412.png" alt></p>
<p><strong>集群构建原理:</strong></p>
<p>​        互相注册</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8413.png" alt></p>
<h4 id="构建新erueka项目"><a href="#构建新erueka项目" class="headerlink" title="构建新erueka项目"></a><strong>构建新erueka项目</strong></h4><p>名字:cloud_eureka_server_7002</p>
<h5 id="1-pom文件"><a href="#1-pom文件" class="headerlink" title="1,pom文件:"></a>1,pom文件:</h5><p>​        粘贴7001的即可</p>
<h5 id="2-配置文件"><a href="#2-配置文件" class="headerlink" title="2,配置文件:"></a>2,配置文件:</h5><p>​        在写配置文件前,修改一下主机的hosts文件</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8414.png" alt></p>
<p>首先修改之前的7001的eureka项目,因为多个eureka需要互相注册</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8415.png" alt></p>
<p>然后修改7002</p>
<p>​            <strong>7002也是一样的,只不过端口和地址改一下</strong></p>
<h5 id="3-主启动类-1"><a href="#3-主启动类-1" class="headerlink" title="3,主启动类:"></a>3,主启动类:</h5><p>​        复制7001的即可</p>
<h5 id="4-然后启动7001-7002即可"><a href="#4-然后启动7001-7002即可" class="headerlink" title="4,然后启动7001,7002即可"></a>4,然后启动7001,7002即可</h5><p><em><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8416.png" alt></em></p>
<h4 id="将pay-order模块注册到eureka集群中"><a href="#将pay-order模块注册到eureka集群中" class="headerlink" title="将pay,order模块注册到eureka集群中:"></a>将pay,order模块注册到eureka集群中:</h4><h5 id="1-只需要修改配置文件即可"><a href="#1-只需要修改配置文件即可" class="headerlink" title="1,只需要修改配置文件即可:"></a>1,只需要修改配置文件即可:</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8417.png" alt></p>
<h5 id="2-两个模块都修改上面的都一样即可"><a href="#2-两个模块都修改上面的都一样即可" class="headerlink" title="2,两个模块都修改上面的都一样即可"></a>2,两个模块都修改上面的都一样即可</h5><p>​            然后启动两个模块</p>
<p>​            要先启动7001,7002,然后是pay模块8001,然后是order(80)</p>
<h3 id="3-将pay模块也配置为集群模式"><a href="#3-将pay模块也配置为集群模式" class="headerlink" title="3,将pay模块也配置为集群模式:"></a>3,将pay模块也配置为集群模式:</h3><h4 id="0-创建新模块-8002"><a href="#0-创建新模块-8002" class="headerlink" title="0,创建新模块,8002"></a>0,创建新模块,8002</h4><p>​    名称: cloud_pay_8002</p>
<h4 id="1-pom文件-复制8001的"><a href="#1-pom文件-复制8001的" class="headerlink" title="1,pom文件,复制8001的"></a>1,pom文件,复制8001的</h4><h4 id="2-pom文件复制8001的"><a href="#2-pom文件复制8001的" class="headerlink" title="2,pom文件复制8001的"></a>2,pom文件复制8001的</h4><h4 id="3-配置文件复制8001的"><a href="#3-配置文件复制8001的" class="headerlink" title="3,配置文件复制8001的"></a>3,配置文件复制8001的</h4><p>​        端口修改一下,改为8002</p>
<p>​        服务名称不用改,用一样的</p>
<h4 id="4-主启动类-复制8001的"><a href="#4-主启动类-复制8001的" class="headerlink" title="4.主启动类,复制8001的"></a>4.主启动类,复制8001的</h4><h4 id="5-mapper-service-controller都复制一份"><a href="#5-mapper-service-controller都复制一份" class="headerlink" title="5,mapper,service,controller都复制一份"></a>5,mapper,service,controller都复制一份</h4><p>​        然后就启动服务即可</p>
<p>​        此时访问order模块,发现并没有负载均衡到两个pay,模块中,而是只访问8001</p>
<p>​        虽然我们是使用RestTemplate访问的微服务,但是也可以负载均衡的</p>
<p>​        <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8418.png" alt></p>
<p><strong>注意这样还不可以,需要让RestTemplate开启负载均衡注解,还可以指定负载均衡算法,默认轮询</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8419.png" alt></p>
<h3 id="4-修改服务主机名和ip在eureka的web上显示"><a href="#4-修改服务主机名和ip在eureka的web上显示" class="headerlink" title="4,修改服务主机名和ip在eureka的web上显示"></a>4,修改服务主机名和ip在eureka的web上显示</h3><p>比如修改pay模块</p>
<h4 id="1-修改配置文件"><a href="#1-修改配置文件" class="headerlink" title="1,修改配置文件:"></a>1,修改配置文件:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8420.png" alt></p>
<h3 id="5-eureka服务发现"><a href="#5-eureka服务发现" class="headerlink" title="5,eureka服务发现:"></a>5,eureka服务发现:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8421.png" alt></p>
<p>以pay模块为例</p>
<h4 id="1-首先添加一个注解-在controller中"><a href="#1-首先添加一个注解-在controller中" class="headerlink" title="1,首先添加一个注解,在controller中"></a>1,首先添加一个注解,在controller中</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8422.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8423.png" alt></p>
<h4 id="2-在主启动类上添加一个注解"><a href="#2-在主启动类上添加一个注解" class="headerlink" title="2,在主启动类上添加一个注解"></a>2,在主启动类上添加一个注解</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8424.png" alt></p>
<p><strong>然后重启8001.访问/payment/discover</strong>y</p>
<h3 id="6-Eureka自我保护"><a href="#6-Eureka自我保护" class="headerlink" title="6,Eureka自我保护:"></a>6,Eureka自我保护:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8426.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8427.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8425.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8428.png" alt></p>
<p><strong>eureka服务端配置:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8429.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8430.png" alt></p>
<p>​            <strong>设置接受心跳时间间隔</strong></p>
<p><strong>客户端(比如pay模块):</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CEureka%E7%9A%8431.png" alt></p>
<p><strong>此时启动erueka和pay.此时如果直接关闭了pay,那么erueka会直接删除其注册信息</strong></p>
<h2 id="7-Zookeeper服务注册与发现"><a href="#7-Zookeeper服务注册与发现" class="headerlink" title="7,Zookeeper服务注册与发现:"></a>7,Zookeeper服务注册与发现:</h2><h3 id="1-启动zk-到linux上"><a href="#1-启动zk-到linux上" class="headerlink" title="1,启动zk,到linux上"></a>1,启动zk,到linux上</h3><h3 id="2-创建新的pay模块"><a href="#2-创建新的pay模块" class="headerlink" title="2,创建新的pay模块,"></a>2,创建新的pay模块,</h3><p>单独用于注册到zk中  </p>
<p>名字 : cloud_pay_8003</p>
<h4 id="1-pom依赖"><a href="#1-pom依赖" class="headerlink" title="1,pom依赖"></a>1,pom依赖</h4><h4 id="2-配置文件-1"><a href="#2-配置文件-1" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Czookeeper%E7%9A%843.png" alt></p>
<h4 id="3-主启动类-2"><a href="#3-主启动类-2" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Czookeeper%E7%9A%841.png" alt></p>
<h4 id="4-controller"><a href="#4-controller" class="headerlink" title="4,controller"></a>4,controller</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Czookeeper%E7%9A%842.png" alt></p>
<h4 id="5-然后就可以启动"><a href="#5-然后就可以启动" class="headerlink" title="5,然后就可以启动"></a>5,然后就可以启动</h4><p><strong>此时启动,会报错,因为jar包与我们的zk版本不匹配</strong></p>
<p>解决:<br>        修改pom文件,改为与我们zk版本匹配的jar包</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Czookeeper%E7%9A%844.png" alt></p>
<p><strong>此时8003就注册到zk中了</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">我们在zk上注册的node是临时节点,当我们的服务一定时间内没有发送心跳</span><br><span class="line">  	那么zk就会`将这个服务的node删除了</span><br></pre></td></tr></table></figure>



<p><strong>这里测试,就不写service与dao什么的了</strong></p>
<h3 id="3-创建order消费模块注册到zk"><a href="#3-创建order消费模块注册到zk" class="headerlink" title="3,创建order消费模块注册到zk"></a>3,创建order消费模块注册到zk</h3><h4 id="1-创建项目"><a href="#1-创建项目" class="headerlink" title="1,创建项目"></a>1,创建项目</h4><p>名字: cloud_order_zk_80</p>
<h4 id="2-pom"><a href="#2-pom" class="headerlink" title="2,pom"></a>2,pom</h4><h4 id="3-配置文件-1"><a href="#3-配置文件-1" class="headerlink" title="3,配置文件"></a>3,配置文件</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Czookeeper%E7%9A%845.png" alt></p>
<h4 id="4主启动类"><a href="#4主启动类" class="headerlink" title="4主启动类:"></a>4主启动类:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Czookeeper%E7%9A%841.png" alt></p>
<h4 id="5-RestTemolate"><a href="#5-RestTemolate" class="headerlink" title="5,RestTemolate"></a>5,RestTemolate</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Czookeeper%E7%9A%846.png" alt="注意,这里使用RestTemolate,要先注册它"></p>
<h4 id="6-controller"><a href="#6-controller" class="headerlink" title="6,controller"></a>6,controller</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Czookeeper%E7%9A%847.png" alt></p>
<p><strong>然后启动即可注册到zk</strong></p>
<h4 id="8-集群版zk注册"><a href="#8-集群版zk注册" class="headerlink" title="8,集群版zk注册:"></a>8,集群版zk注册:</h4><p>只需要修改配置文件:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Czookeeper%E7%9A%845.png" alt></p>
<p>这个connect-string指定多个zk地址即可</p>
<p>connect-string: 1.2.3.4,2.3.4.5</p>
<h4 id><a href="#" class="headerlink" title></a></h4><h2 id="8-Consul"><a href="#8-Consul" class="headerlink" title="8,Consul:"></a>8,Consul:</h2><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%841.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%842.png" alt></p>
<h3 id="1-按照consul"><a href="#1-按照consul" class="headerlink" title="1,按照consul"></a>1,按照consul</h3><p>需要下载一个安装包</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%843.png" alt></p>
<p>启动是一个命令行界面,需要输入consul agen-dev启动</p>
<h3 id="2-创建新的pay模块-8006"><a href="#2-创建新的pay模块-8006" class="headerlink" title="2,创建新的pay模块,8006"></a>2,创建新的pay模块,8006</h3><h4 id="1-项目名字"><a href="#1-项目名字" class="headerlink" title="1,项目名字"></a>1,项目名字</h4><p>cloud_consule_pay_8006</p>
<h4 id="2-pom依赖-1"><a href="#2-pom依赖-1" class="headerlink" title="2,pom依赖"></a>2,pom依赖</h4><h4 id="3-配置文件-2"><a href="#3-配置文件-2" class="headerlink" title="3,配置文件"></a>3,配置文件</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%844.png" alt></p>
<h4 id="4-主启动类-2"><a href="#4-主启动类-2" class="headerlink" title="4,主启动类"></a>4,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%845.png" alt></p>
<h4 id="5-controller"><a href="#5-controller" class="headerlink" title="5,controller"></a>5,controller</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%846.png" alt></p>
<h4 id="6-启动服务"><a href="#6-启动服务" class="headerlink" title="6,启动服务"></a>6,启动服务</h4><h4 id="-1"><a href="#-1" class="headerlink" title></a></h4><h3 id="3-创建新order模块"><a href="#3-创建新order模块" class="headerlink" title="3,创建新order模块"></a>3,创建新order模块</h3><p>cloud-consul-order-80</p>
<h4 id="1-pom文件-1"><a href="#1-pom文件-1" class="headerlink" title="1,pom文件"></a>1,pom文件</h4><h4 id="2-配置文件-2"><a href="#2-配置文件-2" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%847.png" alt></p>
<h4 id="3-主启动类-3"><a href="#3-主启动类-3" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%845.png" alt></p>
<h4 id="4-RestTemplate注册"><a href="#4-RestTemplate注册" class="headerlink" title="4,RestTemplate注册"></a>4,RestTemplate注册</h4><p>配置类注册</p>
<h4 id="5-controller-1"><a href="#5-controller-1" class="headerlink" title="5,controller"></a>5,controller</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%848.png" alt></p>
<h4 id="6-启动服务-测试"><a href="#6-启动服务-测试" class="headerlink" title="6,启动服务,测试"></a>6,启动服务,测试</h4><h2 id="9-三个注册中心的异同"><a href="#9-三个注册中心的异同" class="headerlink" title="9,三个注册中心的异同:"></a>9,三个注册中心的异同:</h2><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%849.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%8410.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cconsul%E7%9A%8411.png" alt></p>
<h1 id="3-服务调用"><a href="#3-服务调用" class="headerlink" title="3,服务调用"></a>3,服务调用</h1><h2 id="10-Ribbon负载均衡"><a href="#10-Ribbon负载均衡" class="headerlink" title="10,Ribbon负载均衡:"></a>10,Ribbon负载均衡:</h2><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon.png" alt></p>
<p><strong>Ribbon目前也进入维护,基本上不准备更新了</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%842.png" alt></p>
<p><strong>进程内LB(本地负载均衡)</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%845.png" alt></p>
<p><strong>集中式LB(服务端负载均衡)</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%844.png" alt></p>
<p><strong>区别</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%843.png" alt></p>
<p><strong>Ribbon就是负载均衡+RestTemplate</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%846.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%847.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%848.png" alt></p>
<h3 id="使用Ribbon"><a href="#使用Ribbon" class="headerlink" title="使用Ribbon:"></a>使用Ribbon:</h3><h4 id="1-默认我们使用eureka的新版本时-它默认集成了ribbon"><a href="#1-默认我们使用eureka的新版本时-它默认集成了ribbon" class="headerlink" title="1,默认我们使用eureka的新版本时,它默认集成了ribbon:"></a>1,默认我们使用eureka的新版本时,它默认集成了ribbon:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%849.png" alt></p>
<p><strong>==这个starter中集成了reibbon了==</strong></p>
<h4 id="2-我们也可以手动引入ribbon"><a href="#2-我们也可以手动引入ribbon" class="headerlink" title="2,我们也可以手动引入ribbon"></a>2,我们也可以手动引入ribbon</h4><p><strong>放到order模块中,因为只有order访问pay时需要负载均衡</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8410.png" alt></p>
<h4 id="3-RestTemplate类"><a href="#3-RestTemplate类" class="headerlink" title="3,RestTemplate类:"></a>3,RestTemplate类:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8411.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8412.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">RestTemplate的:</span><br><span class="line">		xxxForObject()方法,返回的是响应体中的数据</span><br><span class="line">    xxxForEntity()方法.返回的是entity对象,这个对象不仅仅包含响应体数据,还包含响应体信息(状态码等)</span><br></pre></td></tr></table></figure>





<h4 id="Ribbon常用负载均衡算法"><a href="#Ribbon常用负载均衡算法" class="headerlink" title="Ribbon常用负载均衡算法:"></a>Ribbon常用负载均衡算法:</h4><p><strong>IRule接口,Riboon使用该接口,根据特定算法从所有服务中,选择一个服务,</strong></p>
<p><strong>Rule接口有7个实现类,每个实现类代表一个负载均衡算法</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8414.png" alt></p>
<h4 id="使用Ribbon-1"><a href="#使用Ribbon-1" class="headerlink" title="使用Ribbon:"></a>使用Ribbon:</h4><p><strong>==这里使用eureka的那一套服务==</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8415.png" alt></p>
<p><strong>==也就是不能放在主启动类所在的包及子包下==</strong></p>
<h5 id="1-修改order模块"><a href="#1-修改order模块" class="headerlink" title="1,修改order模块"></a>1,修改order模块</h5><h5 id="2-额外创建一个包"><a href="#2-额外创建一个包" class="headerlink" title="2,额外创建一个包"></a>2,额外创建一个包</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8416.png" alt></p>
<h5 id="3-创建配置类-指定负载均衡算法"><a href="#3-创建配置类-指定负载均衡算法" class="headerlink" title="3,创建配置类,指定负载均衡算法"></a>3,创建配置类,指定负载均衡算法</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8417.png" alt></p>
<h5 id="4-在主启动类上加一个注解"><a href="#4-在主启动类上加一个注解" class="headerlink" title="4,在主启动类上加一个注解"></a>4,在主启动类上加一个注解</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8418.png" alt></p>
<p><strong>表示,访问CLOUD_pAYMENT_SERVICE的服务时,使用我们自定义的负载均衡算法</strong></p>
<h4 id="自定义负载均衡算法"><a href="#自定义负载均衡算法" class="headerlink" title="自定义负载均衡算法:"></a>自定义负载均衡算法:</h4><h5 id="1-ribbon的轮询算法原理"><a href="#1-ribbon的轮询算法原理" class="headerlink" title="1,ribbon的轮询算法原理"></a>1,ribbon的轮询算法原理</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8419.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8421.png" alt></p>
<h5 id="2-自定义负载均衡算法"><a href="#2-自定义负载均衡算法" class="headerlink" title="2,自定义负载均衡算法:"></a>2,自定义负载均衡算法:</h5><p><strong>1,给</strong>pay模块(8001,8002),的controller方法添加一个方法,返回当前节点端口</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8423.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8422.png" alt></p>
<p><strong>2,修改order模块</strong></p>
<p>去掉@LoadBalanced</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8424.png" alt></p>
<h5 id="3-自定义接口"><a href="#3-自定义接口" class="headerlink" title="3,自定义接口"></a>3,自定义接口</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8429.png" alt></p>
<p>​                    ==具体的算法在实现类中实现==</p>
<h5 id="4-接口实现类"><a href="#4-接口实现类" class="headerlink" title="4,接口实现类"></a>4,接口实现类</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8425.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8426.png" alt></p>
<h5 id="5-修改controller"><a href="#5-修改controller" class="headerlink" title="5,修改controller:"></a>5,修改controller:</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8427.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CRibbon%E7%9A%8428.png" alt></p>
<h5 id="6-启动服务-测试即可"><a href="#6-启动服务-测试即可" class="headerlink" title="6,启动服务,测试即可"></a>6,启动服务,测试即可</h5><h2 id="11-OpenFeign"><a href="#11-OpenFeign" class="headerlink" title="11,OpenFeign"></a>11,OpenFeign</h2><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%841.png" alt></p>
<p><strong>是一个声明式的web客户端,只需要创建一个接口,添加注解即可完成微服务之间的调用</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%842.png" alt></p>
<p>==就是A要调用B,Feign就是在A中创建一个一模一样的B对外提供服务的的接口,我们调用这个接口,就可以服务到B==</p>
<h3 id="Feign与OpenFeign区别"><a href="#Feign与OpenFeign区别" class="headerlink" title="Feign与OpenFeign区别"></a><strong>Feign与OpenFeign区别</strong></h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%843.png" alt></p>
<h3 id="使用OpenFeign"><a href="#使用OpenFeign" class="headerlink" title="使用OpenFeign"></a>使用OpenFeign</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">之前的服务间调用,我们使用的是ribbon+RestTemplate</span><br><span class="line">		现在改为使用Feign</span><br></pre></td></tr></table></figure>

<h4 id="1-新建一个order项目-用于feign测试"><a href="#1-新建一个order项目-用于feign测试" class="headerlink" title="1,新建一个order项目,用于feign测试"></a>1,新建一个order项目,用于feign测试</h4><p>名字cloud_order_feign-80</p>
<h4 id="2-pom文件"><a href="#2-pom文件" class="headerlink" title="2,pom文件"></a>2,pom文件</h4><h4 id="3-配置文件-3"><a href="#3-配置文件-3" class="headerlink" title="3,配置文件"></a>3,配置文件</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%844.png" alt></p>
<h4 id="4-主启动类-3"><a href="#4-主启动类-3" class="headerlink" title="4,主启动类"></a>4,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%845.png" alt></p>
<h4 id="5-fegin需要调用的其他的服务的接口"><a href="#5-fegin需要调用的其他的服务的接口" class="headerlink" title="5,fegin需要调用的其他的服务的接口"></a>5,fegin需要调用的其他的服务的接口</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%846.png" alt></p>
<h4 id="6-controller-1"><a href="#6-controller-1" class="headerlink" title="6,controller"></a>6,controller</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%847.png" alt></p>
<h4 id="7测试"><a href="#7测试" class="headerlink" title="7测试:"></a>7测试:</h4><p>启动两个erueka(7001,7002)</p>
<p>启动两个pay(8001,8002)</p>
<p>启动当前的order模块</p>
<p><strong>Feign默认使用ribbon实现负载均衡</strong></p>
<h3 id="OpenFeign超时机制"><a href="#OpenFeign超时机制" class="headerlink" title="OpenFeign超时机制:"></a>OpenFeign超时机制:</h3><p>==OpenFeign默认等待时间是1秒,超过1秒,直接报错==</p>
<h4 id="1-设置超时时间-修改配置文件"><a href="#1-设置超时时间-修改配置文件" class="headerlink" title="1,设置超时时间,修改配置文件:"></a>1,设置超时时间,修改配置文件:</h4><p><strong>因为OpenFeign的底层是ribbon进行负载均衡,所以它的超时时间是由ribbon控制</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%848.png" alt></p>
<h3 id="OpenFeign日志"><a href="#OpenFeign日志" class="headerlink" title="OpenFeign日志:"></a>OpenFeign日志:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%849.png" alt></p>
<p><strong>OpenFeign的日志级别有:</strong><br><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%8410.png" alt></p>
<h4 id="1-使用OpenFeign的日志"><a href="#1-使用OpenFeign的日志" class="headerlink" title="1,使用OpenFeign的日志:"></a>1,使用OpenFeign的日志:</h4><p><strong>实现在配置类中添加OpenFeign的日志类</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%8411.png" alt></p>
<h4 id="2-为指定类设置日志级别"><a href="#2-为指定类设置日志级别" class="headerlink" title="2,为指定类设置日志级别:"></a>2,为指定类设置日志级别:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%8413.png" alt></p>
<p><strong>配置文件中:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CFeign%E7%9A%8412.png" alt></p>
<h4 id="3-启动服务即可"><a href="#3-启动服务即可" class="headerlink" title="3,启动服务即可"></a>3,启动服务即可</h4><h1 id="4-服务降级"><a href="#4-服务降级" class="headerlink" title="4,服务降级:"></a>4,服务降级:</h1><h2 id="12-Hystrix服务降级"><a href="#12-Hystrix服务降级" class="headerlink" title="12,Hystrix服务降级"></a>12,Hystrix服务降级</h2><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%842.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%843.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%844.png" alt></p>
<h3 id="hystrix中的重要概念"><a href="#hystrix中的重要概念" class="headerlink" title="hystrix中的重要概念:"></a>hystrix中的重要概念:</h3><h4 id="1-服务降级"><a href="#1-服务降级" class="headerlink" title="1,服务降级"></a>1,服务降级</h4><p><strong>比如当某个服务繁忙,不能让客户端的请求一直等待,应该立刻返回给客户端一个备选方案</strong></p>
<h4 id="2-服务熔断"><a href="#2-服务熔断" class="headerlink" title="2,服务熔断"></a>2,服务熔断</h4><p><strong>当某个服务出现问题,卡死了,不能让用户一直等待,需要关闭所有对此服务的访问</strong></p>
<p>​            <strong>然后调用服务降级</strong></p>
<h4 id="3-服务限流"><a href="#3-服务限流" class="headerlink" title="3,服务限流"></a>3,服务限流</h4><p><strong>限流,比如秒杀场景,不能访问用户瞬间都访问服务器,限制一次只可以有多少请求</strong></p>
<h3 id="使用hystrix-服务降级"><a href="#使用hystrix-服务降级" class="headerlink" title="使用hystrix,服务降级:"></a>使用hystrix,服务降级:</h3><h4 id="1-创建带降级机制的pay模块"><a href="#1-创建带降级机制的pay模块" class="headerlink" title="1,创建带降级机制的pay模块 :"></a>1,创建带降级机制的pay模块 :</h4><p>名字: cloud-hystrix-pay-8007</p>
<h5 id="2-pom文件-1"><a href="#2-pom文件-1" class="headerlink" title="2,pom文件"></a>2,pom文件</h5><h5 id="3-配置文件-4"><a href="#3-配置文件-4" class="headerlink" title="3,配置文件"></a>3,配置文件</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%845.png" alt></p>
<h5 id="4-主启动类-4"><a href="#4-主启动类-4" class="headerlink" title="4,主启动类"></a>4,主启动类</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%848.png" alt></p>
<h5 id="5-service"><a href="#5-service" class="headerlink" title="5,service"></a>5,service</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%846.png" alt></p>
<h5 id="6controller"><a href="#6controller" class="headerlink" title="6controller"></a>6controller</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%847.png" alt></p>
<h5 id="7-先测试"><a href="#7-先测试" class="headerlink" title="7,先测试:"></a>7,先测试:</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">此时使用压测工具,并发<span class="number">20000</span>个请求,请求会延迟的那个方法,</span><br><span class="line">		压测中,发现,另外一个方法并没有被压测,但是我们访问它时,却需要等待</span><br><span class="line">		这就是因为被压测的方法它占用了服务器大部分资源,导致其他请求也变慢了</span><br></pre></td></tr></table></figure>



<h5 id="8-先不加入hystrix"><a href="#8-先不加入hystrix" class="headerlink" title="8,先不加入hystrix,"></a>8,先不加入hystrix,</h5><h4 id="2-创建带降级的order模块"><a href="#2-创建带降级的order模块" class="headerlink" title="2,创建带降级的order模块:"></a>2,创建带降级的order模块:</h4><h5 id="1-名字-cloud-hystrix-order-80"><a href="#1-名字-cloud-hystrix-order-80" class="headerlink" title="1,名字:  cloud-hystrix-order-80"></a>1,名字:  cloud-hystrix-order-80</h5><h5 id="2-pom-1"><a href="#2-pom-1" class="headerlink" title="2,pom"></a>2,pom</h5><h5 id="3-配置文件-5"><a href="#3-配置文件-5" class="headerlink" title="3,配置文件"></a>3,配置文件</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%849.png" alt></p>
<h5 id="4-主启动类-5"><a href="#4-主启动类-5" class="headerlink" title="4,主启动类"></a>4,主启动类</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8411.png" alt></p>
<h5 id="5-远程调用pay模块的接口"><a href="#5-远程调用pay模块的接口" class="headerlink" title="5,远程调用pay模块的接口:"></a>5,远程调用pay模块的接口:</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8412.png" alt></p>
<h5 id="6-controller-2"><a href="#6-controller-2" class="headerlink" title="6,controller:"></a>6,controller:</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8413.png" alt></p>
<h5 id="7-测试"><a href="#7-测试" class="headerlink" title="7,测试"></a>7,测试</h5><p>​            启动order模块,访问pay</p>
<p>​            再次压测2万并发,发现order访问也变慢了</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8414.png" alt></p>
<p><strong>解决:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8415.png" alt></p>
<h5 id="-2"><a href="#-2" class="headerlink" title></a><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8416.png" alt></h5><h4 id="3-配置服务降级"><a href="#3-配置服务降级" class="headerlink" title="3,配置服务降级:"></a>3,配置服务降级:</h4><h5 id="1-修改pay模块"><a href="#1-修改pay模块" class="headerlink" title="1,修改pay模块"></a>1,修改pay模块</h5><h6 id="1-为service的指定方法-会延迟的方法-添加-HystrixCommand注解"><a href="#1-为service的指定方法-会延迟的方法-添加-HystrixCommand注解" class="headerlink" title="1,为service的指定方法(会延迟的方法)添加@HystrixCommand注解"></a>1,为service的指定方法(会延迟的方法)添加@HystrixCommand注解</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8417.png" alt></p>
<h6 id="2-主启动类上-添加激活hystrix的注解"><a href="#2-主启动类上-添加激活hystrix的注解" class="headerlink" title="2,主启动类上,添加激活hystrix的注解"></a>2,主启动类上,添加激活hystrix的注解</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8418.png" alt></p>
<h6 id="3-触发异常"><a href="#3-触发异常" class="headerlink" title="3,触发异常"></a>3,触发异常</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8419.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8420.png" alt><strong>可以看到,也触发了降级</strong></p>
<h5 id="2-修改order模块-进行服务降级"><a href="#2-修改order模块-进行服务降级" class="headerlink" title="2,修改order模块,进行服务降级"></a>2,修改order模块,进行服务降级</h5><p>一般服务降级,都是放在客户端(order模块),</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8421.png" alt></p>
<h6 id="1-修改配置文件-1"><a href="#1-修改配置文件-1" class="headerlink" title="1,修改配置文件:"></a>1,修改配置文件:</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8422.png" alt></p>
<h6 id="2-主启动类添加直接-启用hystrix"><a href="#2-主启动类添加直接-启用hystrix" class="headerlink" title="2,主启动类添加直接,启用hystrix:"></a><strong>2,主启动类添加直接,启用hystrix:</strong></h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8423.png" alt></p>
<p>​    </p>
<h6 id="3-修改controller-添加降级方法什么的"><a href="#3-修改controller-添加降级方法什么的" class="headerlink" title="3,修改controller,添加降级方法什么的"></a>3,修改controller,添加降级方法什么的</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8424.png" alt></p>
<h6 id="4-测试"><a href="#4-测试" class="headerlink" title="4,测试"></a>4,测试</h6><p>启动pay模块,order模块,</p>
<p><strong>注意:,这里pay模块和order模块都开启了服务降级</strong></p>
<p>​            但是order这里,设置了1.5秒就降级,所以访问时,一定会降级</p>
<h5 id="4-重构"><a href="#4-重构" class="headerlink" title="4,重构:"></a>4,重构:</h5><p><strong>上面出现的问题:</strong><br>        1,降级方法与业务方法写在了一块,耦合度高</p>
<p>​        2.每个业务方法都写了一个降级方法,重复代码多</p>
<h5 id="解决重复代码的问题"><a href="#解决重复代码的问题" class="headerlink" title="解决重复代码的问题:"></a><strong>解决重复代码的问题</strong>:</h5><p><strong>配置一个全局的降级方法,所有方法都可以走这个降级方法,至于某些特殊创建,再单独创建方法</strong></p>
<h6 id="1-创建一个全局方法"><a href="#1-创建一个全局方法" class="headerlink" title="1,创建一个全局方法"></a>1,创建一个全局方法</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8426.png" alt></p>
<h6 id="2-使用注解指定其为全局降级方法-默认降级方法"><a href="#2-使用注解指定其为全局降级方法-默认降级方法" class="headerlink" title="2,使用注解指定其为全局降级方法(默认降级方法)"></a>2,使用注解指定其为全局降级方法(默认降级方法)</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8427.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8425.png" alt></p>
<h6 id="3-业务方法使用默认降级方法"><a href="#3-业务方法使用默认降级方法" class="headerlink" title="3,业务方法使用默认降级方法:"></a>3,业务方法使用默认降级方法:</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8428.png" alt></p>
<h6 id="4-测试-1"><a href="#4-测试-1" class="headerlink" title="4,测试:"></a>4,测试:</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8429.png" alt></p>
<h5 id="解决代码耦合度的问题"><a href="#解决代码耦合度的问题" class="headerlink" title="解决代码耦合度的问题:"></a>解决代码耦合度的问题:</h5><p>修改order模块,这里开始,pay模块就不服务降级了,服务降级写在order模块即可</p>
<h6 id="1-Payservice接口是远程调用pay模块的-我们这里创建一个类实现service接口-在实现类中统一处理异常"><a href="#1-Payservice接口是远程调用pay模块的-我们这里创建一个类实现service接口-在实现类中统一处理异常" class="headerlink" title="1,Payservice接口是远程调用pay模块的,我们这里创建一个类实现service接口,在实现类中统一处理异常"></a>1,Payservice接口是远程调用pay模块的,我们这里创建一个类实现service接口,在实现类中统一处理异常</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8430.png" alt></p>
<h6 id="2-修改配置文件-添加"><a href="#2-修改配置文件-添加" class="headerlink" title="2,修改配置文件:添加:"></a>2,修改配置文件:添加:</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8431.png" alt></p>
<h6 id="3-让PayService的实现类生效"><a href="#3-让PayService的实现类生效" class="headerlink" title="3,让PayService的实现类生效:"></a>3,让PayService的实现类生效:</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8432.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">它的运行逻辑是:</span><br><span class="line">		当请求过来,首先还是通过Feign远程调用pay模块对应的方法</span><br><span class="line">    但是如果pay模块报错,调用失败,那么就会调用PayMentFalbackService类的</span><br><span class="line">    当前同名的方法,作为降级方法</span><br></pre></td></tr></table></figure>

<h6 id="4-启动测试"><a href="#4-启动测试" class="headerlink" title="4,启动测试"></a>4,启动测试</h6><p>启动order和pay正常访问–ok</p>
<p>==此时将pay服务关闭,order再次访问==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8433.png" alt></p>
<p>可以看到,并没有报500错误,而是降级访问==实现类==的同名方法</p>
<p>这样,即使服务器挂了,用户要不要一直等待,或者报错</p>
<p>问题:</p>
<p>​        <strong>这样虽然解决了代码耦合度问题,但是又出现了过多重复代码的问题,每个方法都有一个降级方法</strong></p>
<h3 id="使用服务熔断"><a href="#使用服务熔断" class="headerlink" title="使用服务熔断:"></a>使用服务熔断:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8434.png" alt></p>
<p><strong>比如并发达到1000,我们就拒绝其他用户访问,在有用户访问,就访问降级方法</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8435.png" alt></p>
<h4 id="1-修改前面的pay模块"><a href="#1-修改前面的pay模块" class="headerlink" title="1,修改前面的pay模块"></a>1,修改前面的pay模块</h4><h5 id="1-修改Payservice接口-添加服务熔断相关的方法"><a href="#1-修改Payservice接口-添加服务熔断相关的方法" class="headerlink" title="1,修改Payservice接口,添加服务熔断相关的方法:"></a><strong>1,修改Payservice接口,添加服务熔断相关的方法:</strong></h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8437.png" alt></p>
<p>这里属性整体意思是:<br>            10秒之内(窗口,会移动),如果并发==超过==10个,或者10个并发中,失败了6个,就开启熔断器</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8443.png" alt="image-20200414152637247"></p>
<p>IdUtil是Hutool包下的类,这个Hutool就是整合了所有的常用方法,比如UUID,反射,IO流等工具方法什么的都整合了</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8436.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">断路器的打开和关闭,是按照一下<span class="number">5</span>步决定的</span><br><span class="line">  	<span class="number">1</span>,并发此时是否达到我们指定的阈值</span><br><span class="line">  	<span class="number">2</span>,错误百分比,比如我们配置了<span class="number">60</span>%,那么如果并发请求中,<span class="number">10</span>次有<span class="number">6</span>次是失败的,就开启断路器</span><br><span class="line">  	<span class="number">3</span>,上面的条件符合,断路器改变状态为open(开启)</span><br><span class="line">  	<span class="number">4</span>,这个服务的断路器开启,所有请求无法访问</span><br><span class="line">  	<span class="number">5</span>,在我们的时间窗口期,期间,尝试让一些请求通过(半开状态),如果请求还是失败,证明断路器还是开启状态,服务没有恢复</span><br><span class="line">  		如果请求成功了,证明服务已经恢复,断路器状态变为close关闭状态</span><br></pre></td></tr></table></figure>



<h5 id="2-修改controller"><a href="#2-修改controller" class="headerlink" title="2,修改controller"></a>2,修改controller</h5><p>添加一个测试方法;</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8439.png" alt></p>
<h5 id="3-测试"><a href="#3-测试" class="headerlink" title="3,测试:"></a>3,测试:</h5><p>启动pay,order模块</p>
<p>==多次访问,并且错误率超过60%:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8440.png" alt></p>
<p>此时服务熔断,此时即使访问正确的也会报错:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8441.png" alt></p>
<p><strong>但是,当过了几秒后,又恢复了</strong></p>
<p>​                因为在10秒窗口期内,它自己会尝试接收部分请求,发现服务可以正常调用,慢慢的当错误率低于60%,取消熔断</p>
<h3 id="Hystrix所有可配置的属性"><a href="#Hystrix所有可配置的属性" class="headerlink" title="Hystrix所有可配置的属性:"></a>Hystrix所有可配置的属性:</h3><p><strong>全部在这个方法中记录,以成员变量的形式记录,</strong></p>
<p>​        以后需要什么属性,查看这个类即可</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8438.png" alt></p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结:"></a>总结:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8442.png" alt></p>
<p><strong>==当断路器开启后:==</strong></p>
<p>​    <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8444.png" alt></p>
<p><strong>==其他参数:==</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8445.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8446.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8447.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8448.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8449.png" alt></p>
<p><strong>熔断整体流程:</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span>请求进来,首先查询缓存,如果缓存有,直接返回</span><br><span class="line">  	如果缓存没有,---&gt;<span class="number">2</span></span><br><span class="line"><span class="number">2</span>,查看断路器是否开启,如果开启的,Hystrix直接将请求转发到降级返回,然后返回</span><br><span class="line">  	如果断路器是关闭的,</span><br><span class="line">				判断线程池等资源是否已经满了,如果已经满了</span><br><span class="line">  					也会走降级方法</span><br><span class="line">  			如果资源没有满,判断我们使用的什么类型的Hystrix,决定调用构造方法还是run方法</span><br><span class="line">        然后处理请求</span><br><span class="line">        然后Hystrix将本次请求的结果信息汇报给断路器,因为断路器此时可能是开启的</span><br><span class="line">          			(因为断路器开启也是可以接收请求的)</span><br><span class="line">        		断路器收到信息,判断是否符合开启或关闭断路器的条件,</span><br><span class="line">				如果本次请求处理失败,又会进入降级方法</span><br><span class="line">        如果处理成功,判断处理是否超时,如果超时了,也进入降级方法</span><br><span class="line">        最后,没有超时,则本次请求处理成功,将结果返回给controller</span><br></pre></td></tr></table></figure>







<h3 id="Hystrix服务监控"><a href="#Hystrix服务监控" class="headerlink" title="Hystrix服务监控:"></a>Hystrix服务监控:</h3><h4 id="HystrixDashboard"><a href="#HystrixDashboard" class="headerlink" title="HystrixDashboard"></a>HystrixDashboard</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8451.png" alt></p>
<h4 id="2-使用HystrixDashboard"><a href="#2-使用HystrixDashboard" class="headerlink" title="2,使用HystrixDashboard:"></a>2,使用HystrixDashboard:</h4><h5 id="1-创建项目-1"><a href="#1-创建项目-1" class="headerlink" title="1,创建项目:"></a>1,创建项目:</h5><p>名字: cloud_hystrixdashboard_9001</p>
<h5 id="2-pom文件-2"><a href="#2-pom文件-2" class="headerlink" title="2,pom文件"></a>2,pom文件</h5><h5 id="3-配置文件-6"><a href="#3-配置文件-6" class="headerlink" title="3,配置文件"></a>3,配置文件</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8452.png" alt></p>
<h5 id="4-主启动类-6"><a href="#4-主启动类-6" class="headerlink" title="4,主启动类"></a>4,主启动类</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8453.png" alt></p>
<h5 id="5-修改所有pay模块-8001-8002-8003…"><a href="#5-修改所有pay模块-8001-8002-8003…" class="headerlink" title="5,修改所有pay模块(8001,8002,8003…)"></a>5,修改所有pay模块(8001,8002,8003…)</h5><p><strong>他们都添加一个pom依赖:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8454.png" alt></p>
<p>之前的pom文件中都添加过了,==这个是springboot的监控组件==</p>
<h5 id="6-启动9001即可"><a href="#6-启动9001即可" class="headerlink" title="6,启动9001即可"></a>6,启动9001即可</h5><p>​            访问: <strong>localhost:9001/hystrix</strong></p>
<h5 id="7-注意-此时仅仅是可以访问HystrixDashboard-并不代表已经监控了8001-8002"><a href="#7-注意-此时仅仅是可以访问HystrixDashboard-并不代表已经监控了8001-8002" class="headerlink" title="7,注意,此时仅仅是可以访问HystrixDashboard,并不代表已经监控了8001,8002"></a>7,注意,此时仅仅是可以访问HystrixDashboard,并不代表已经监控了8001,8002</h5><p>​                            如果要监控,还需要配置:(8001为例)</p>
<p>==8001的主启动类添加:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8455.png" alt></p>
<p><strong>其他8002,8003都是一样的</strong></p>
<h5 id="8-到此-可以启动服务"><a href="#8-到此-可以启动服务" class="headerlink" title="8,到此,可以启动服务"></a>8,到此,可以启动服务</h5><p>启动7001,8001,9001</p>
<p><strong>然后在web界面,指定9001要监控8001:</strong></p>
<h5 id="-3"><a href="#-3" class="headerlink" title></a><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8456.png" alt></h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8457.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8459.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8458.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8460.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8461.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CHystrix%E7%9A%8462.png" alt></p>
<h1 id="5-服务网关"><a href="#5-服务网关" class="headerlink" title="5,服务网关:"></a>5,服务网关:</h1><p>zuul停更了,</p>
<h2 id="13-GateWay"><a href="#13-GateWay" class="headerlink" title="13,GateWay"></a>13,GateWay</h2><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%841.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%842.png" alt></p>
<p><strong>gateway之所以性能号,因为底层使用WebFlux,而webFlux底层使用netty通信(NIO)</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%843.png" alt></p>
<h3 id="GateWay的特性"><a href="#GateWay的特性" class="headerlink" title="GateWay的特性:"></a>GateWay的特性:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%844.png" alt></p>
<h3 id="GateWay与zuul的区别"><a href="#GateWay与zuul的区别" class="headerlink" title="GateWay与zuul的区别:"></a>GateWay与zuul的区别:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%845.png" alt></p>
<h3 id="zuul1-x的模型"><a href="#zuul1-x的模型" class="headerlink" title="zuul1.x的模型:"></a>zuul1.x的模型:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%846.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%847.png" alt></p>
<h3 id="什么是webflux"><a href="#什么是webflux" class="headerlink" title="什么是webflux:"></a>什么是webflux:</h3><p><strong>是一个非阻塞的web框架,类似springmvc这样的</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%848.png" alt></p>
<h3 id="GateWay的一些概念"><a href="#GateWay的一些概念" class="headerlink" title="GateWay的一些概念:"></a>GateWay的一些概念:</h3><h4 id="1-路由"><a href="#1-路由" class="headerlink" title="1,路由:"></a>1,路由:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%849.png" alt></p>
<p>就是根据某些规则,将请求发送到指定服务上</p>
<h4 id="2-断言"><a href="#2-断言" class="headerlink" title="2,断言:"></a>2,断言:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8410.png" alt></p>
<p>就是判断,如果符合条件就是xxxx,反之yyyy</p>
<h4 id="3-过滤"><a href="#3-过滤" class="headerlink" title="3,过滤:"></a>3,过滤:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8411.png" alt></p>
<p>​    <strong>路由前后,过滤请求</strong></p>
<h3 id="GateWay的工作原理"><a href="#GateWay的工作原理" class="headerlink" title="GateWay的工作原理:"></a>GateWay的工作原理:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8412.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8413.png" alt></p>
<h3 id="使用GateWay"><a href="#使用GateWay" class="headerlink" title="使用GateWay:"></a>使用GateWay:</h3><p>想要新建一个GateWay的项目</p>
<p>名字:     cloud_gateway_9527</p>
<h4 id="1-pom-1"><a href="#1-pom-1" class="headerlink" title="1,pom"></a>1,pom</h4><h4 id="2-配置文件-3"><a href="#2-配置文件-3" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8414.png" alt></p>
<h4 id="3-主启动类-4"><a href="#3-主启动类-4" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8415.png" alt></p>
<h4 id="4-针对pay模块-设置路由"><a href="#4-针对pay模块-设置路由" class="headerlink" title="4,针对pay模块,设置路由:"></a>4,针对pay模块,设置路由:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8416.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8418.png" alt></p>
<p><strong>==修改GateWay模块(9527)的配置文件==:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8417.png" alt></p>
<p>这里表示,</p>
<p>​            当访问localhost:9527/payment/get/1时,     </p>
<p>​            路由到localhost:8001/payment/get/1</p>
<h4 id="5-开始测试"><a href="#5-开始测试" class="headerlink" title="5,开始测试"></a>5,开始测试</h4><p><strong>启动7001,8001,9527</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">如果启动GateWay报错</span><br><span class="line">  	可能是GateWay模块引入了web和监控的starter依赖,需要移除</span><br></pre></td></tr></table></figure>

<p>访问:</p>
<p>​        localhost:9527/payment/get/1</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8419.png" alt></p>
<h4 id="6-GateWay的网关配置"><a href="#6-GateWay的网关配置" class="headerlink" title="6,GateWay的网关配置,"></a>6,GateWay的网关配置,</h4><p>​        <strong>GateWay的网关配置,除了支持配置文件,还支持硬编码方式</strong></p>
<h4 id="7使用硬编码配置GateWay"><a href="#7使用硬编码配置GateWay" class="headerlink" title="7使用硬编码配置GateWay:"></a>7使用硬编码配置GateWay:</h4><h5 id="创建配置类"><a href="#创建配置类" class="headerlink" title="创建配置类:"></a>创建配置类:</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8420.png" alt></p>
<h4 id="8-然后重启服务即可"><a href="#8-然后重启服务即可" class="headerlink" title="8,然后重启服务即可"></a>8,然后重启服务即可</h4><h3 id="重构"><a href="#重构" class="headerlink" title="重构:"></a>重构:</h3><p>上面的配置虽然首先了网关,但是是在配置文件中写死了要路由的地址</p>
<p>现在需要修改,不指定地址,而是根据微服务名字进行路由,我们可以在注册中心获取某组微服务的地址</p>
<p>需要:</p>
<p>​        1个eureka,2个pay模块</p>
<h4 id="修改GateWay模块的配置文件"><a href="#修改GateWay模块的配置文件" class="headerlink" title="修改GateWay模块的配置文件:"></a>修改GateWay模块的配置文件:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8421.png" alt></p>
<h4 id="然后就可以启动微服务-测试"><a href="#然后就可以启动微服务-测试" class="headerlink" title="然后就可以启动微服务.测试"></a>然后就可以启动微服务.测试</h4><h3 id="Pridicate断言"><a href="#Pridicate断言" class="headerlink" title="Pridicate断言:"></a>Pridicate断言:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8424.png" alt></p>
<p><strong>我们之前在配置文件中配置了断言:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8422.png" alt></p>
<p><strong>这个断言表示,如果外部访问路径是指定路径,就路由到指定微服务上</strong></p>
<p>可以看到,这里有一个Path,这个是断言的一种,==断言的类型==:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8423.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">After:</span><br><span class="line">		可以指定,只有在指定时间后,才可以路由到指定微服务</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8426.png" alt></p>
<p>​                这里表示,只有在==2020年的2月21的15点51分37秒==之后,访问==才可以路由==</p>
<p>​                在此之前的访问,都会报404</p>
<p>如何获取当前时区?**</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8425.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">before:</span><br><span class="line">		与after类似,他说在指定时间之前的才可以访问</span><br><span class="line">between:</span><br><span class="line">		需要指定两个时间,在他们之间的时间才可以访问</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8427.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">cookie:</span><br><span class="line">		只有包含某些指定cookie(key,value),的请求才可以路由</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8428.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8429.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Header:</span><br><span class="line">		只有包含指定请求头的请求,才可以路由</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8431.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8432.png" alt></p>
<p>测试:<br><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8433.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">host:</span><br><span class="line">		只有指定主机的才可以访问,</span><br><span class="line">		比如我们当前的网站的域名是www.aa.com</span><br><span class="line">    那么这里就可以设置,只有用户是www.aa.com的请求,才进行路由</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8434.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8435.png" alt="gateway的34"></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8436.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8437.png" alt></p>
<p>可以看到,如果带了域名访问,就可以,但是直接访问ip地址.就报错了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">method:</span><br><span class="line">		只有指定请求才可以路由,比如get请求...</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8438.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">path:</span><br><span class="line">		只有访问指定路径,才进行路由</span><br><span class="line">     比如访问,/abc才路由</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8439.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Query:</span><br><span class="line">		必须带有请求参数才可以访问</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8440.png" alt></p>
<h3 id="Filter过滤器"><a href="#Filter过滤器" class="headerlink" title="Filter过滤器:"></a>Filter过滤器:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8441.png" alt></p>
<h4 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期:"></a>生命周期:</h4><p><strong>在请求进入路由之前,和处理请求完成,再次到达路由之前</strong></p>
<h4 id="种类"><a href="#种类" class="headerlink" title="种类:"></a>种类:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8442.png" alt></p>
<p>GateWayFilter,单一的过滤器</p>
<p><strong>与断言类似,比如闲置,请求头,只有特定的请求头才放行,反之就过滤</strong>:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8443.png" alt></p>
<p>GlobalFilter,全局过滤器:</p>
<h4 id="自定义过滤器"><a href="#自定义过滤器" class="headerlink" title="自定义过滤器:"></a><strong>自定义过滤器:</strong></h4><p>实现两个接口</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8444.png" alt></p>
<p>​    <strong>然后启动服务,即可,因为过滤器通过@COmponet已经加入到容器了</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8446.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cgateway%E7%9A%8445.png" alt></p>
<h1 id="6-服务配置"><a href="#6-服务配置" class="headerlink" title="6,服务配置:"></a>6,服务配置:</h1><h2 id="Spring-Config分布式配置中心"><a href="#Spring-Config分布式配置中心" class="headerlink" title="Spring Config分布式配置中心:"></a>Spring Config分布式配置中心:</h2><p>==微服务面临的问题==</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">可以看到,每个微服务都需要一个配置文件,并且,如果有几个微服务都需要连接数据库</span><br><span class="line">		那么就需要配<span class="number">4</span>次数据库相关配置,并且当数据库发生改动,那么需要同时修改<span class="number">4</span>个微服务的配置文件才可以</span><br></pre></td></tr></table></figure>

<p>所以有了springconfig配置中心</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%841.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%842.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%843.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%844.png" alt></p>
<h3 id="使用配置中心"><a href="#使用配置中心" class="headerlink" title="使用配置中心:"></a>使用配置中心:</h3><h4 id="0-使用github作为配置中心的仓库"><a href="#0-使用github作为配置中心的仓库" class="headerlink" title="0,使用github作为配置中心的仓库:"></a>0,使用github作为配置中心的仓库:</h4><p><strong>初始化git环境:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%845.png" alt></p>
<h4 id="1-新建config模块"><a href="#1-新建config模块" class="headerlink" title="1,新建config模块:"></a>1,新建config模块:</h4><p>名字:   cloud-config-3344</p>
<h4 id="2-pom-2"><a href="#2-pom-2" class="headerlink" title="2,pom"></a>2,pom</h4><h4 id="3-配置文件-7"><a href="#3-配置文件-7" class="headerlink" title="3,配置文件"></a>3,配置文件</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%846.png" alt></p>
<h4 id="4-主启动类-7"><a href="#4-主启动类-7" class="headerlink" title="4,主启动类"></a>4,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%847.png" alt></p>
<h4 id="5-修改hosts"><a href="#5-修改hosts" class="headerlink" title="5,修改hosts:"></a>5,修改hosts:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%848.png" alt></p>
<h4 id="6-配置完成"><a href="#6-配置完成" class="headerlink" title="6,配置完成"></a>6,配置完成</h4><p>测试,3344是否可以从github上获取配置</p>
<p>启动3344    (要先启动eureka)</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%849.png" alt></p>
<p>它实际上就是,读取到配置文件中的GitHub的地址,然后拼接上/master/config-dev.yml</p>
<h4 id="7-读取配置文件的规则"><a href="#7-读取配置文件的规则" class="headerlink" title="7,读取配置文件的规则:"></a>7,读取配置文件的规则:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8410.png" alt></p>
<p>==2,==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8411.png" alt></p>
<p><strong>这里默认会读取master分支,因为我们配置文件中配置了</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8412.png" alt></p>
<p>==3==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8413.png" alt></p>
<p>注意,这个方式读取到的配置是==json格式==的</p>
<p><strong>所有规则:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8414.png" alt></p>
<h3 id="2-创建配置中心客户端"><a href="#2-创建配置中心客户端" class="headerlink" title="2,创建配置中心客户端:"></a>2,创建配置中心客户端:</h3><h4 id="1-创建config客户端项目"><a href="#1-创建config客户端项目" class="headerlink" title="1,创建config客户端项目"></a>1,创建config客户端项目</h4><p>名字:     cloud-config-client-3355</p>
<h4 id="2-pom-3"><a href="#2-pom-3" class="headerlink" title="2,pom"></a>2,pom</h4><h4 id="3-配置文件-8"><a href="#3-配置文件-8" class="headerlink" title="3,配置文件"></a>3,配置文件</h4><p>注意这个配置文件就不是application.yml</p>
<p>​            而是bootstrap.yml</p>
<p>这个配置文件的作用是,先到配置中心加载配置,然后加载到application.yml中</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8415.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8416.png" alt></p>
<h4 id="4-主启动类-8"><a href="#4-主启动类-8" class="headerlink" title="4,主启动类:"></a>4,主启动类:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8417.png" alt></p>
<h4 id="5-controller类"><a href="#5-controller类" class="headerlink" title="5,controller类"></a>5,controller类</h4><p>就是上面提到的,以rest风格将配置对外暴露</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8418.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8419.png" alt></p>
<p><strong>如果客户端运行正常,就会读取到github上配置文件的,config.info下的配置</strong></p>
<h4 id="6-测试"><a href="#6-测试" class="headerlink" title="6,测试:"></a>6,测试:</h4><p>启动3344,3355</p>
<p>​    访问3355的  /configInfo</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8421.png" alt></p>
<h4 id="7-问题"><a href="#7-问题" class="headerlink" title="7,问题::"></a>7,问题::</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">上面<span class="number">3355</span>确实获取到了配置文件,但是如果此时配置文件修改了,<span class="number">3355</span>是获取不到的</span><br><span class="line">		<span class="number">3344</span>可以实时获取到最新配置文件,但是<span class="number">3355</span>却获取不到</span><br><span class="line">  	除非重启服务</span><br></pre></td></tr></table></figure>

<h4 id="8-实现动态刷新"><a href="#8-实现动态刷新" class="headerlink" title="8,实现动态刷新:"></a><strong>8,实现动态刷新:</strong></h4><h5 id="1-修改3355-添加一个pom依赖"><a href="#1-修改3355-添加一个pom依赖" class="headerlink" title="1,修改3355,添加一个pom依赖:"></a>1,修改3355,添加一个pom依赖:</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8422.png" alt></p>
<h5 id="2-修改配置文件-添加一个配置"><a href="#2-修改配置文件-添加一个配置" class="headerlink" title="2,修改配置文件,添加一个配置:"></a>2,修改配置文件,添加一个配置:</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8423.png" alt></p>
<h5 id="3-修改controller"><a href="#3-修改controller" class="headerlink" title="3,修改controller:"></a>3,修改controller:</h5><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8424.png" alt></p>
<h5 id="4-此时重启服务"><a href="#4-此时重启服务" class="headerlink" title="4,此时重启服务"></a>4,此时重启服务</h5><p><strong>此时3355还不可以动态获取</strong></p>
<p>因为此时,还需要==外部==发送post请求通知3355</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8425.png" alt></p>
<p><strong>此时在刷新3355,发现可以获取到最新的配置文件了,这就实现了动态获取配置文件,因为3355并没有重启</strong></p>
<p>具体流程就是:</p>
<p>​            我们启动好服务后</p>
<p>​            运维人员,修改了配置文件,然后发送一个post请求通知3355</p>
<p>​            3355就可以获取最新配置文件</p>
<p><strong>问题:</strong></p>
<p>​        如果有多个客户端怎么办(3355,3356,3357…..)</p>
<p>​                        虽然可以使用shell脚本,循环刷新</p>
<p>​        但是,可不可以使用广播,一次通知??</p>
<p>​                    这些springconfig做不到,需要使用springcloud Bus消息总线</p>
<h1 id="消息总线"><a href="#消息总线" class="headerlink" title="消息总线:"></a>消息总线:</h1><h2 id="SpringCloud-Bus"><a href="#SpringCloud-Bus" class="headerlink" title="SpringCloud Bus:"></a>SpringCloud Bus:</h2><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8426.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8427.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8431.png" alt></p>
<p>注意,这里年张图片,就代表两种广播方式</p>
<p>​            图1:        <strong>它是Bus直接通知给其中一个客户端,由这个客户端开始蔓延,传播给其他所有客户端</strong></p>
<p>​            图2:        它<strong>是通知给配置中心的服务端,有服务端广播给所有客户端</strong></p>
<p><strong>为什么被称为总线?</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8428.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">就是通过消息队列达到广播的效果</span><br><span class="line">  		我们要广播每个消息时,主要放到某个topic中,所有监听的节点都可以获取到</span><br></pre></td></tr></table></figure>





<h3 id="使用Bus"><a href="#使用Bus" class="headerlink" title="使用Bus:"></a>使用Bus:</h3><h4 id="1-配置rabbitmq环境"><a href="#1-配置rabbitmq环境" class="headerlink" title="1,配置rabbitmq环境:"></a>1,配置rabbitmq环境:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8429.png" alt></p>
<h4 id="2-之前只有一个配置中心客户端-这里在创建一个"><a href="#2-之前只有一个配置中心客户端-这里在创建一个" class="headerlink" title="2,之前只有一个配置中心客户端,这里在创建一个"></a><strong>2,之前只有一个配置中心客户端,这里在创建一个</strong></h4><p>​        ==<strong>复制3355即可,创建为3366</strong>==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8430.png" alt></p>
<p>全部复制3355的即可</p>
<h4 id="2-使用Bus实现全局广播"><a href="#2-使用Bus实现全局广播" class="headerlink" title="2,使用Bus实现全局广播"></a>2,使用Bus实现全局广播</h4><p><strong>Bus广播有两种方式:</strong></p>
<p>​        ==就是上面两个图片的两种方式==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8432.png" alt></p>
<p><strong>这两种方式,第二种跟合适,因为:</strong></p>
<p>​            ==第一种的缺点:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cspringconfig%E7%9A%8433.png" alt></p>
<h4 id="配置第二种方式"><a href="#配置第二种方式" class="headerlink" title="配置第二种方式:"></a><strong>配置第二种方式:</strong></h4><h5 id="1-配置3344-配置中心服务端"><a href="#1-配置3344-配置中心服务端" class="headerlink" title="1,配置3344(配置中心服务端):"></a><strong>1,配置3344(配置中心服务端):</strong></h5><h6 id="1-修改配置文件-2"><a href="#1-修改配置文件-2" class="headerlink" title="1,修改配置文件:"></a>1,修改配置文件:</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%841.png" alt></p>
<h6 id="2-添加pom"><a href="#2-添加pom" class="headerlink" title="2,添加pom"></a>2,添加pom</h6><p><strong>springboot的监控组件,和消息总线</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%843.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%842.png" alt></p>
<h5 id="2-修改3355-配置中心的客户端"><a href="#2-修改3355-配置中心的客户端" class="headerlink" title="2,修改3355(配置中心的客户端)"></a>2,修改3355(配置中心的客户端)</h5><h6 id="1-pom-2"><a href="#1-pom-2" class="headerlink" title="1,pom:"></a>1,pom:</h6><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%843.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%842.png" alt="Bus的2"></p>
<h6 id="2-配置文件-4"><a href="#2-配置文件-4" class="headerlink" title="2,配置文件:"></a>2,配置文件:</h6><p>==注意配置文件的名字,要改为bootstrap.yml==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%845.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%844" alt="image-20200415102708661"></p>
<h5 id="3-修改3366-也是配置中心的客户端"><a href="#3-修改3366-也是配置中心的客户端" class="headerlink" title="3,修改3366(也是配置中心的客户端)"></a>3,修改3366(也是配置中心的客户端)</h5><p>​            修改与3355是一模一样的</p>
<h5 id="4-测试-2"><a href="#4-测试-2" class="headerlink" title="4,测试"></a>4,测试</h5><p>启动7001,3344,3355,3366</p>
<p>此时修改GitHub上的配置文件</p>
<p>==此时只需要刷新3344,即可让3355,3366动态获取最新的配置文件==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%846.png" alt></p>
<p>其原理就是:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%847.png" alt></p>
<p><strong>所有客户端都监听了一个rabbitMq的topic,我们将信息放入这个topic,所有客户端都可以送到,从而实时更新</strong></p>
<h4 id="配置定点通知"><a href="#配置定点通知" class="headerlink" title="配置定点通知"></a>配置定点通知</h4><p>​        就是只通知部分服务,比如只通知3355,不通知3366</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%848.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%849.png" alt="Bus的8"></p>
<p><strong>只通知3355</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%8411.png" alt></p>
<p>​    <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CBus%E7%9A%8412.png" alt></p>
<p><strong>可以看到,实际上就是通过==微服务的名称+端口号==进行指定</strong></p>
<h1 id="8-消息驱动"><a href="#8-消息驱动" class="headerlink" title="8,消息驱动:"></a>8,消息驱动:</h1><h2 id="Spring-Cloud-Stream"><a href="#Spring-Cloud-Stream" class="headerlink" title="Spring Cloud Stream:"></a>Spring Cloud Stream:</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">现在一个很项目可能分为三部分:</span><br><span class="line">			前端---&gt;后端----&gt;大数据</span><br><span class="line">			而后端开发使用消息中间件,可能会使用RabbitMq</span><br><span class="line">			而大数据开发,一般都是使用Kafka,</span><br><span class="line">			那么一个项目中有多个消息中间件,对于程序员,因为人员都不友好</span><br></pre></td></tr></table></figure>

<p>而Spring Cloud Stream就类似jpa,屏蔽底层消息中间件的差异,程序员主要操作Spring Cloud Stream即可</p>
<p>​            不需要管底层是kafka还是rabbitMq</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%841.png" alt></p>
<h3 id="什么是Spring-Cloud-Stream"><a href="#什么是Spring-Cloud-Stream" class="headerlink" title="==什么是Spring Cloud Stream=="></a>==什么是Spring Cloud Stream==</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%842.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%843.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%844.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%845.png" alt></p>
<h3 id="Spring-Cloud-Stream是怎么屏蔽底层差异的"><a href="#Spring-Cloud-Stream是怎么屏蔽底层差异的" class="headerlink" title="==Spring Cloud Stream是怎么屏蔽底层差异的?=="></a>==<strong>Spring Cloud Stream是怎么屏蔽底层差异的?</strong>==</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%846.png" alt></p>
<p><strong>绑定器:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%847.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%848.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%849.png" alt></p>
<h3 id="Spring-Cloud-Streamd-通信模式"><a href="#Spring-Cloud-Streamd-通信模式" class="headerlink" title="Spring Cloud Streamd 通信模式:"></a><strong>Spring Cloud Streamd 通信模式:</strong></h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8410.png" alt>)<img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8411.png" alt></p>
<h3 id="Spring-Cloud-Stream的业务流程"><a href="#Spring-Cloud-Stream的业务流程" class="headerlink" title="Spring Cloud Stream的业务流程:"></a>Spring Cloud Stream的业务流程:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8412.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8414.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8413.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">类似flume中的channel,source,sink  估计是借鉴(抄袭)的</span><br><span class="line">  	source用于获取数据(要发送到mq的数据)</span><br><span class="line">  	channel类似SpringCloudStream中的中间件,用于存放source接收到的数据,或者是存放binder拉取的数据</span><br></pre></td></tr></table></figure>







<h3 id="常用注解和api"><a href="#常用注解和api" class="headerlink" title="常用注解和api:"></a>常用注解和api:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8415.png" alt></p>
<h3 id="使用SpringCloudStream"><a href="#使用SpringCloudStream" class="headerlink" title="使用SpringCloudStream:"></a>使用SpringCloudStream:</h3><p>需要创建三个项目,一个生产者,两个消费者</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8416.png" alt></p>
<h3 id="1-创建生产者"><a href="#1-创建生产者" class="headerlink" title="1,创建生产者"></a>1,创建生产者</h3><h4 id="1-pom-3"><a href="#1-pom-3" class="headerlink" title="1,pom"></a>1,pom</h4><h4 id="2-配置文件-5"><a href="#2-配置文件-5" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8417" alt="image-20200415114816133"></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8418.png" alt></p>
<h4 id="3-主启动类-5"><a href="#3-主启动类-5" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8419.png" alt></p>
<h4 id="4-service和实现类"><a href="#4-service和实现类" class="headerlink" title="4,service和实现类"></a>4,service和实现类</h4><p>service定义发送消息</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8420.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8421.png" alt></p>
<p><strong>这里,就会调用send方法,将消息发送给channel,</strong></p>
<p>​                <strong>然后channel将消费发送给binder,然后发送到rabbitmq中</strong></p>
<h4 id="5-controller-2"><a href="#5-controller-2" class="headerlink" title="5,controller"></a>5,controller</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8422.png" alt></p>
<h4 id="6-可以测试"><a href="#6-可以测试" class="headerlink" title="6,可以测试"></a>6,可以测试</h4><p><strong>启动rabbitmq</strong></p>
<p><strong>启动7001,8801</strong></p>
<p>​        确定8801后,会在rabbitmq中创建一个Exchange,就是我们配置文件中配置的exchange</p>
<p><strong>访问8801的/sendMessage</strong></p>
<h3 id="创建消费者"><a href="#创建消费者" class="headerlink" title="创建消费者:"></a>创建消费者:</h3><h4 id="1-pom文件-2"><a href="#1-pom文件-2" class="headerlink" title="1,pom文件"></a>1,pom文件</h4><h4 id="2-配置文件-6"><a href="#2-配置文件-6" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p>==<strong>这里排版一点问题</strong>==</p>
<p><strong>==input==就表示,当前服务是一个消费者,需要消费消息,下面就是指定消费哪个Exchange中的消息</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8423.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8424.png" alt></p>
<h4 id="3-主启动类-6"><a href="#3-主启动类-6" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8425.png" alt></p>
<h4 id="4-业务类-消费数据"><a href="#4-业务类-消费数据" class="headerlink" title="4,业务类(消费数据)"></a>4,业务类(消费数据)</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8426.png" alt></p>
<p><strong>生产者发送消息时,使用send方法发送,send方法发送的是一个个Message,里面封装了数据</strong></p>
<h4 id="5-测试"><a href="#5-测试" class="headerlink" title="5,测试:"></a>5,测试:</h4><p>启动7001.8801.8802</p>
<p><strong>此时使用生产者生产消息</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8427.png" alt></p>
<p>==可以看到,消费者已经接收到消息了==</p>
<h3 id="创建消费者2"><a href="#创建消费者2" class="headerlink" title="创建消费者2"></a>创建消费者2</h3><p>创建8803,</p>
<p>==与8802创建一模一样,就不写了==</p>
<p><strong>创建8803主要是为了演示重复消费等问题</strong></p>
<p>…</p>
<p>….</p>
<p>…</p>
<h3 id="重复消费问题"><a href="#重复消费问题" class="headerlink" title="==重复消费问题:=="></a>==重复消费问题:==</h3><p>此时启动7001.8801.8802.8803</p>
<p>此时生产者生产一条消息</p>
<p>但是此时查询消费者,发现8802,8803==都消费到了同一条数据==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8428.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8429.png" alt></p>
<h4 id="1-自定义分组"><a href="#1-自定义分组" class="headerlink" title="1,自定义分组"></a>1,自定义分组</h4><p><strong>修改8802,8803的配置文件</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8430.png" alt></p>
<p>![](SpringCloud学习\SpringCloudStream的31 - 副本.png)</p>
<p><strong>现在将8802,8803都分到了A组</strong></p>
<p>然后去重启02,03</p>
<p><strong>然后此时生产者生产两条消息</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8433.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8434.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CSpringCloudStream%E7%9A%8435.png" alt></p>
<p><strong>可以看到,每人只消费了一条消息,并且没有重复消费</strong></p>
<h3 id="持久化问题"><a href="#持久化问题" class="headerlink" title="持久化问题:"></a>持久化问题:</h3><p>就是当服务挂了,怎么消费没有消费的数据??</p>
<p>这里,先将8802移除A组,</p>
<p>​        然后将02,03服务关闭</p>
<p>此时生产者开启,发送3条消息</p>
<p>​        此时重启02,03</p>
<p>​        可以看到,当02退出A组后,它就获取不到在它宕机的时间段内的数据</p>
<p>​        但是03重启后,直接获取到了宕机期间它没有消费的数据,并且消费了</p>
<p>总结:<br>        也就是,当我们没有配置分组时,会出现消息漏消费的问题</p>
<p>​        而配置分组后,我们可以自动获取未消费的数据</p>
<h1 id="9-链路追踪"><a href="#9-链路追踪" class="headerlink" title="9,链路追踪:"></a>9,链路追踪:</h1><h2 id="Spring-Cloud-Sleuth"><a href="#Spring-Cloud-Sleuth" class="headerlink" title="Spring Cloud Sleuth"></a>Spring Cloud Sleuth</h2><p><strong>sleuth要解决的问题:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csleuth%E7%9A%841.png" alt></p>
<p><strong>而来sleuth就是用于追踪每个请求的整体链路</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csleuth%E7%9A%842.png" alt></p>
<h3 id="使用sleuth"><a href="#使用sleuth" class="headerlink" title="使用sleuth:"></a>使用sleuth:</h3><h4 id="1-安装zipkin"><a href="#1-安装zipkin" class="headerlink" title="1,安装zipkin:"></a>1,安装zipkin:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csleuth%E7%9A%843.png" alt></p>
<p><strong>运行jar包</strong></p>
<p>​            java -jar xxxx.jar</p>
<p><strong>然后就可以访问web界面,  默认zipkin监听的端口是9411</strong></p>
<p>​            localhost:9411/zipkin/</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csleuth%E7%9A%844.png" alt></p>
<p><strong>一条链路完整图片:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csleuth%E7%9A%845.png" alt></p>
<p><strong>精简版:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csleuth%E7%9A%846.png" alt></p>
<p><strong>可以看到,类似链表的形式</strong></p>
<h4 id="2-使用sleuth"><a href="#2-使用sleuth" class="headerlink" title="2,使用sleuth:"></a>2,使用sleuth:</h4><p>不需要额外创建项目,使用之前的8001和order的80即可</p>
<h5 id="1-修改8001"><a href="#1-修改8001" class="headerlink" title="1,修改8001"></a>1,修改8001</h5><p><strong>引入pom:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csleuth%E7%9A%847.png" alt></p>
<p>这个包虽然叫zipkin但是,里面包含了zpikin与sleuth</p>
<p><strong>修改配置文件:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csleuth%E7%9A%848.png" alt></p>
<h5 id="2-修改80"><a href="#2-修改80" class="headerlink" title="2,修改80"></a>2,修改80</h5><p><strong>添加pom</strong></p>
<p>与上面是一样的</p>
<p><strong>添加配置</strong>:</p>
<p>与上面也是一样的</p>
<h5 id="3-测试-1"><a href="#3-测试-1" class="headerlink" title="3,测试:"></a>3,测试:</h5><p>启动7001.8001,80,9411</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csleuth%E7%9A%849.png" alt></p>
<h1 id="10-Spring-CloudAlibaba"><a href="#10-Spring-CloudAlibaba" class="headerlink" title="10,Spring CloudAlibaba:"></a>10,Spring CloudAlibaba:</h1><p><strong>之所以有Spring CloudAlibaba,是因为Spring Cloud Netflix项目进入维护模式</strong></p>
<p>​        <strong>也就是,就不是不更新了,不会开发新组件了</strong></p>
<p>​        <strong>所以,某些组件都有代替版了,比如Ribbon由Loadbalancer代替,等等</strong></p>
<p>==支持的功能==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%841.png" alt></p>
<p>几乎可以将之前的Spring Cloud代替</p>
<p>==具体组件==:<br><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%842.png" alt></p>
<h2 id="Nacos"><a href="#Nacos" class="headerlink" title="Nacos:"></a>Nacos:</h2><p><strong>服务注册和配置中心的组合</strong></p>
<p>​            Nacos=erueka+config+bus</p>
<h3 id="安装Nacos"><a href="#安装Nacos" class="headerlink" title="安装Nacos:"></a>安装Nacos:</h3><p>需要java8  和 Mavne</p>
<p><strong>1,到github上下载安装包</strong></p>
<p>​        解压安装包</p>
<p><strong>2,启动Nacos</strong></p>
<p>​        在bin下,进入cod</p>
<p>​        ./startup.cmd</p>
<p><strong>3,访问Nacos</strong></p>
<p>​        Nacos默认监听8848</p>
<p>​        localhost:8848/nacos</p>
<p>​        账号密码:默认都是nacos</p>
<h3 id="使用Nacos"><a href="#使用Nacos" class="headerlink" title="使用Nacos:"></a>使用Nacos:</h3><p>新建pay模块</p>
<p>​        <strong>现在不需要额外的服务注册模块了,Nacos单独启动了</strong></p>
<p>名字: cloudalibaba-pay-9001</p>
<h4 id="1-pom-4"><a href="#1-pom-4" class="headerlink" title="1,pom"></a>1,pom</h4><p>父项目管理alibaba的依赖:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%844.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%843.png" alt></p>
<p>==9001的pom==:</p>
<p>​            另外一个文件…..</p>
<h4 id="2-配置文件-7"><a href="#2-配置文件-7" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%845.png" alt></p>
<h4 id="3-启动类"><a href="#3-启动类" class="headerlink" title="3,启动类"></a>3,启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%846.png" alt></p>
<h4 id="4-controller-1"><a href="#4-controller-1" class="headerlink" title="4,controller:"></a>4,controller:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%847.png" alt></p>
<h4 id="5-测试-1"><a href="#5-测试-1" class="headerlink" title="5,测试"></a>5,测试</h4><p>启动9001</p>
<p>然后查看Nacos的web界面,可以看到9001已经注册成功</p>
<h3 id="-4"><a href="#-4" class="headerlink" title></a></h3><h3 id="创建其他Pay模块"><a href="#创建其他Pay模块" class="headerlink" title="创建其他Pay模块"></a>创建其他Pay模块</h3><p>​        额外在创建9002,9003</p>
<p>​        直接复制上面的即可</p>
<h3 id="创建order模块"><a href="#创建order模块" class="headerlink" title="创建order模块"></a>创建order模块</h3><p>名字:  cloudalibaba-order-83</p>
<h4 id="1-pom-5"><a href="#1-pom-5" class="headerlink" title="1,pom"></a>1,pom</h4><p><strong>为什么Nacos支持负载均衡?</strong></p>
<p>​                Nacos直接集成了Ribon,所以有负载均衡</p>
<h4 id="2-配置文件-8"><a href="#2-配置文件-8" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%848.png" alt></p>
<p><strong>这个server-url的作用是,我们在controller,需要使用RestTempalte远程调用9001,</strong></p>
<p>​        <strong>这里是指定9001的地址</strong></p>
<h4 id="3-主启动类-7"><a href="#3-主启动类-7" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%849.png" alt></p>
<h4 id="4-编写配置类"><a href="#4-编写配置类" class="headerlink" title="4,编写配置类"></a>4,编写配置类</h4><p>​    ==因为Naocs要使用Ribbon进行负载均衡,那么就需要使用RestTemplate==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8410.png" alt></p>
<h4 id="5-controller-3"><a href="#5-controller-3" class="headerlink" title="5,controller:"></a>5,controller:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8411.png" alt></p>
<h4 id="6-测试-1"><a href="#6-测试-1" class="headerlink" title="6,测试"></a>6,测试</h4><p>启动83,访问9001,9002,可以看到,实现了负载均衡</p>
<h3 id="Nacos与其他服务注册的对比"><a href="#Nacos与其他服务注册的对比" class="headerlink" title="Nacos与其他服务注册的对比"></a>Nacos与其他服务注册的对比</h3><p>Nacos它既可以支持CP,也可以支持AP,可以切换</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8412.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8413.png" alt></p>
<p>==下面这个curl命令,就是切换模式==</p>
<h3 id="使用Nacos作为配置中心"><a href="#使用Nacos作为配置中心" class="headerlink" title="使用Nacos作为配置中心:"></a>使用Nacos作为配置中心:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8414.png" alt></p>
<p><strong>==需要创建配置中心的客户端模块==</strong></p>
<p>cloudalibaba-Nacos-config-client-3377</p>
<h4 id="1-pom-6"><a href="#1-pom-6" class="headerlink" title="1,pom"></a>1,pom</h4><h4 id="2-配置文件-9"><a href="#2-配置文件-9" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p>这里需要配置两个配置文件,application.ymk和bootstarp.yml</p>
<p>​            主要是为了可以与spring clodu config无缝迁移</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8415.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">可以看到</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8416.png" alt></p>
<h4 id="3-主启动类-8"><a href="#3-主启动类-8" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8418.png" alt></p>
<h4 id="4-controller-2"><a href="#4-controller-2" class="headerlink" title="4,controller"></a>4,controller</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8417.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">可以看到,这里也添加了<span class="meta">@RefreshScope</span></span><br><span class="line">  		之前在Config配置中心,也是添加这个注解实现动态刷新的</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8419.png" alt></p>
<h4 id="5-在Nacos添加配置信息"><a href="#5-在Nacos添加配置信息" class="headerlink" title="5,在Nacos添加配置信息:"></a>5,在Nacos添加配置信息:</h4><p>==<strong>Nacos的配置规则:</strong>==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8420.png" alt></p>
<p><strong>配置规则,就是我们在客户端如何指定读取配置文件,配置文件的命名的规则</strong></p>
<p>默认的命名方式:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8421.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">prefix:</span><br><span class="line">		默认就是当前服务的服务名称</span><br><span class="line"> 		也可以通过spring.cloud.necos.config.prefix配置</span><br><span class="line">spring.profile.active:</span><br><span class="line">		就是我们在application.yml中指定的,当前是开发环境还是测试等环境</span><br><span class="line">    这个可以不配置,如果不配置,那么前面的 -  也会没有</span><br><span class="line">file-extension</span><br><span class="line">     就是当前文件的格式(后缀),目前只支持yml和properties</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8424.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8425.png" alt></p>
<p>==在web UI上创建配置文件:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8422.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8423.png" alt></p>
<p>注意,DataId就是配置文件名字:</p>
<p>​        名字一定要按照上面的==规则==命名,否则客户端会读取不到配置文件</p>
<h4 id="6-测试-2"><a href="#6-测试-2" class="headerlink" title="6,测试"></a>6,测试</h4><p>重启3377客户端</p>
<p>访问3377</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8426.png" alt></p>
<p><strong>拿到了配置文件中的值</strong></p>
<h4 id="7-注意默认就开启了自动刷新"><a href="#7-注意默认就开启了自动刷新" class="headerlink" title="7,注意默认就开启了自动刷新"></a>7,注意默认就开启了自动刷新</h4><p>此时我们修改了配置文件</p>
<p>客户端是可以立即更新的</p>
<p>​            因为Nacos支持Bus总线,会自动发送命令更新所有客户端</p>
<h3 id="Nacos配置中心之分类配置"><a href="#Nacos配置中心之分类配置" class="headerlink" title="Nacos配置中心之分类配置:"></a>Nacos配置中心之分类配置:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8427.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8428.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8429.png" alt></p>
<p>NameSpace默认有一个:public名称空间</p>
<p>这三个类似java的: 包名 + 类名 + 方法名</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8430.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8431.png" alt></p>
<h4 id="1-配置不同DataId"><a href="#1-配置不同DataId" class="headerlink" title="1,配置不同DataId:"></a>1,配置不同DataId:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8432.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8433.png" alt></p>
<p>​    ==通过配置文件,实现多环境的读取:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8434.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">此时,改为dev,就会读取dev的配置文件,改为test,就会读取test的配置文件</span><br></pre></td></tr></table></figure>





<h4 id="2-配置不同的GroupID"><a href="#2-配置不同的GroupID" class="headerlink" title="2,配置不同的GroupID:"></a>2,配置不同的GroupID:</h4><p>直接在新建配置文件时指定组</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8435.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8436.png" alt></p>
<p>==在客户端配置,使用指定组的配置文件:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8437.png" alt></p>
<p><strong>这两个配置文件都要修改</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8438.png" alt></p>
<p>​    </p>
<p>重启服务,即可</p>
<h4 id="配置不同的namespace"><a href="#配置不同的namespace" class="headerlink" title="配置不同的namespace:"></a>配置不同的namespace:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8439.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8442.png" alt></p>
<p>==客户端配置使用不同名称空间:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8441.png" alt></p>
<p><strong>要通过命名空间id指定</strong></p>
<p>OK,测试</p>
<h3 id="Nacos集群和持久化配置"><a href="#Nacos集群和持久化配置" class="headerlink" title="Nacos集群和持久化配置:"></a>Nacos集群和持久化配置:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8445.png" alt></p>
<p>Nacos默认有自带嵌入式数据库,derby,但是如果做集群模式的话,就不能使用自己的数据库</p>
<p>​            不然每个节点一个数据库,那么数据就不统一了,需要使用外部的mysql</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8443.png" alt></p>
<h4 id="1-单机版-切换mysql数据库"><a href="#1-单机版-切换mysql数据库" class="headerlink" title="1,单机版,切换mysql数据库:"></a>1,单机版,切换mysql数据库:</h4><p>​                    <strong>将nacos切换到使用我们自己的mysql数据库:</strong></p>
<p><strong>1,nacos默认自带了一个sql文件,在nacos安装目录下</strong></p>
<p>​            将它放到我们的mysql执行</p>
<p><strong>2,修改Nacos安装目录下的安排application.properties,添加:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8446.png" alt></p>
<p><strong>3,此时可以重启nacos,那么就会改为使用我们自己的mysql</strong></p>
<h4 id="Linux上配置Nacos集群-Mysql数据库"><a href="#Linux上配置Nacos集群-Mysql数据库" class="headerlink" title="Linux上配置Nacos集群+Mysql数据库"></a>Linux上配置Nacos集群+Mysql数据库</h4><p>==官方架构图:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8445.png" alt></p>
<p><strong>需要一个Nginx作为VIP</strong></p>
<p>1,下载安装Nacos的Linux版安装包</p>
<p>2,进入安装目录,现在执行自带的sql文件</p>
<p>​            进入mysql,执行sql文件</p>
<p>3.修改配置文件,切换为我们的mysql</p>
<p>​            就是上面windos版要修改的几个属性</p>
<p>4,修改cluster.conf,指定哪几个节点是Nacos集群</p>
<p>​            这里使用3333,4444,5555作为三个Nacos节点监听的端口</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8447.png" alt></p>
<p>5,我们这里就不配置在不同节点上了,就放在一个节点上</p>
<p>​            既然要在一个节点上启动不同Nacos实例,就要修改startup.sh,使其根据不同端口启动不同Nacos实例</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8448.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8449.png" alt></p>
<p>可以看到,这个脚本就是通过jvm启动nacos</p>
<p>​        所以我们最后修改的就是,nohup java -Dserver.port=3344</p>
<p>6,配置Nginx:</p>
<p>​            <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8450.png" alt></p>
<p>7,启动Nacos:<br>            ./startup.sh -p 3333</p>
<p>​            ./startup.sh -p 4444</p>
<p>​            ./startup.sh -p 5555</p>
<p>7,启动nginx</p>
<p>8,测试:</p>
<p>​        访问192.168.159.121:1111</p>
<p>​        如果可以进入nacos的web界面,就证明安装成功了</p>
<p>9,将微服务注册到Nacos集群:<br><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8451.png" alt></p>
<p>10,进入Nacos的web界面</p>
<p>​        可以看到,已经注册成功</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8452.png" alt></p>
<h2 id="Sentinel"><a href="#Sentinel" class="headerlink" title="Sentinel:"></a>Sentinel:</h2><p>实现熔断与限流,就是Hystrix</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8453.png" alt></p>
<p>​    <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8454.png" alt></p>
<h3 id="使用sentinel"><a href="#使用sentinel" class="headerlink" title="==使用sentinel:=="></a>==使用sentinel:==</h3><p>1,下载sentinel的jar包</p>
<p>2,运行sentinel</p>
<p>​        由于是一个jar包,所以可以直接java -jar运行    </p>
<p>​        注意,默认sentinel占用8080端口</p>
<p>3,访问sentinel</p>
<p>​        localhost:8080</p>
<h3 id="微服务整合sentinel"><a href="#微服务整合sentinel" class="headerlink" title="微服务整合sentinel:"></a>微服务整合sentinel:</h3><h5 id="1-启动Nacos"><a href="#1-启动Nacos" class="headerlink" title="1,启动Nacos"></a>1,启动Nacos</h5><h5 id="2-新建一个项目-8401-主要用于配置sentinel"><a href="#2-新建一个项目-8401-主要用于配置sentinel" class="headerlink" title="2,新建一个项目,8401,主要用于配置sentinel"></a>2,新建一个项目,8401,主要用于配置sentinel</h5><ol>
<li><p>pom</p>
</li>
<li><p>配置文件</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8455.png" alt></p>
</li>
<li><p>主启动类</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5CAlibaba%E7%9A%8456.png" alt></p>
</li>
<li><p>controller\</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%841.png" alt></p>
</li>
<li><p>到这里就可以启动8401</p>
<p>​    此时我们到sentinel中查看,发现并8401的任何信息</p>
<p>​    是因为,sentinel是懒加载,需要我们执行一次访问,才会有信息</p>
<p>​    访问localhost/8401/testA</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%842.png" alt></p>
</li>
<li><p>可以看到.已经开始监听了</p>
</li>
</ol>
<p>​    </p>
<h3 id="sentinel的流控规则"><a href="#sentinel的流控规则" class="headerlink" title="sentinel的流控规则"></a>sentinel的流控规则</h3><p>流量限制控制规则</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%847.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%843.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%844.png" alt></p>
<p>==流控模式==:</p>
<ol>
<li><p>直接快速失败</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%849.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%845.png" alt></p>
<p>  ==直接失败的效果:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%846.png" alt></p>
</li>
<li><p>线程数:</p>
<p>​        <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%848.png" alt></p>
<p>​    <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8410.png" alt></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">比如a请求过来,处理很慢,在一直处理,此时b请求又过来了</span><br><span class="line">		此时因为a占用一个线程,此时要处理b请求就只有额外开启一个线程</span><br><span class="line">		那么就会报错</span><br></pre></td></tr></table></figure>

<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8411.png" alt></p>
</li>
</ol>
<ol start="3">
<li><p>关联:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8412.png" alt></p>
<p>==应用场景:  比如<strong>支付接口</strong>达到阈值,就要限流下<strong>订单的接口</strong>,防止一直有订单==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8413.png" alt></p>
<p><strong>当testA达到阈值,qps大于1,就让testB之后的请求直接失败</strong></p>
<p>可以使用postman压测</p>
</li>
</ol>
<p>​    </p>
<ol start="4">
<li><p>链路:<br>多个请求调用同一个微服务</p>
</li>
<li><p>预热Warm up:</p>
<p>​     <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8414.png" alt></p>
<p> <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8415.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8416.png" alt></p>
<p>==应用场景==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8417.png" alt></p>
</li>
<li><p>排队等待:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8418.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8419.png" alt></p>
</li>
</ol>
<h3 id="降级规则"><a href="#降级规则" class="headerlink" title="降级规则:"></a>降级规则:</h3><p><strong>就是熔断降级</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8421.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8420.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8422.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8423.png" alt></p>
<h4 id="1-RT配置"><a href="#1-RT配置" class="headerlink" title="1,RT配置:"></a>1,RT配置:</h4><p>新增一个请求方法用于测试</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8424.png" alt></p>
<p>==配置RT:==</p>
<p>​                这里配置的PT,默认是秒级的平均响应时间</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8425.png" alt></p>
<p>默认计算平均时间是: 1秒类进入5个请求,并且响应的平均值超过阈值(这里的200ms),就报错]</p>
<p>​            1秒5请求是Sentinel默认设置的</p>
<p>==测试==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8427.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8426.png" alt></p>
<p><strong>默认熔断后.就直接抛出异常</strong></p>
<h4 id="2-异常比例"><a href="#2-异常比例" class="headerlink" title="2,异常比例:"></a>2,异常比例:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8428.png" alt></p>
<p>修改请求方法</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8429.png" alt></p>
<p>配置:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8431.png" alt></p>
<p>==如果没触发熔断,这正常抛出异常==:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8432.png" alt></p>
<p>==触发熔断==:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8433.png" alt></p>
<h4 id="3-异常数"><a href="#3-异常数" class="headerlink" title="3, 异常数:"></a>3, 异常数:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8434.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8435.png" alt></p>
<p>一分钟之内,有5个请求发送异常,进入熔断</p>
<h3 id="热点规则"><a href="#热点规则" class="headerlink" title="热点规则:"></a>热点规则:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8436.png" alt></p>
<p>​    <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8437.png" alt></p>
<p>比如:</p>
<p>​            localhost:8080/aa?name=aa</p>
<p>​            localhost:8080/aa?name=b’b</p>
<p>​            加入两个请求中,带有参数aa的请求访问频次非常高,我们就现在name==aa的请求,但是bb的不限制</p>
<p>==如何自定义降级方法,而不是默认的抛出异常?==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8438.png" alt></p>
<p><strong>使用@SentinelResource直接实现降级方法,它等同Hystrix的@HystrixCommand</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8439.png" alt></p>
<p>==定义热点规则:==</p>
<p> <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8440.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8442.png" alt></p>
<p><strong>此时我们访问/testHotkey并且带上才是p1</strong></p>
<p>​            如果qps大于1,就会触发我们定义的降级方法</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8441.png" alt></p>
<p><strong>但是我们的参数是P2,就没有问题</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8444.png" alt></p>
<p>只有带了p1,才可能会触发热点限流</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8443.png" alt></p>
<h4 id="2-设置热点规则中的其他选项"><a href="#2-设置热点规则中的其他选项" class="headerlink" title="2,设置热点规则中的其他选项:"></a>2,设置热点规则中的其他选项:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8445.png" alt></p>
<p><strong>需求:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8446.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8447.png" alt></p>
<p>==测试==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8448.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8449.png" alt></p>
<p><strong>注意:</strong></p>
<p>参数类型只支持,8种基本类型+String类</p>
<p>==注意:==</p>
<p>如果我们程序出现异常,是不会走blockHander的降级方法的,因为这个方法只配置了热点规则,没有配置限流规则</p>
<p>我们这里配置的降级方法是sentinel针对热点规则配置的</p>
<p>只有触发热点规则才会降级</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8450.png" alt></p>
<h3 id="3-系统规则"><a href="#3-系统规则" class="headerlink" title="3,系统规则:"></a>3,系统规则:</h3><p>系统自适应限流:<br>            从整体维度对应用入口进行限流</p>
<p>对整体限流,比如设置qps到达100,这里限流会限制整个系统不可以</p>
<p><em><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8451.png" alt></em></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8452.png" alt></p>
<p>==测试==:<br><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8453.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8454.png" alt></p>
<h3 id="SentinelResource注解"><a href="#SentinelResource注解" class="headerlink" title="@SentinelResource注解:"></a>@SentinelResource注解:</h3><p><strong>用于配置降级等功能</strong></p>
<p>1,环境搭建</p>
<ol>
<li><p>为8401添加依赖</p>
<p>添加我们自己的commone包的依赖</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8455.png" alt></p>
</li>
<li><p>额外创建一个controller类</p>
<p>​     <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8456.png" alt></p>
</li>
</ol>
<ol start="3">
<li><p>配置限流</p>
<p><strong>注意,我们这里配置规则,资源名指定的是@SentinelResource注解value的值,</strong></p>
<p><strong>这样也是可以的,也就是不一定要指定访问路径</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8457.png" alt></p>
</li>
<li><p>测试.</p>
<p>可以看到已经进入降级方法了</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8458.png" alt></p>
</li>
<li><p>==此时我们关闭8401服务==</p>
<p>可以看到,这些定义的规则是临时的,关闭服务,规则就没有了</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%8459.png" alt></p>
</li>
</ol>
<p><strong>可以看到,上面配置的降级方法,又出现Hystrix遇到的问题了</strong></p>
<p>​            降级方法与业务方法耦合</p>
<p>​            每个业务方法都需要对应一个降级方法</p>
<h4 id="自定义限流处理逻辑"><a href="#自定义限流处理逻辑" class="headerlink" title="自定义限流处理逻辑:"></a>自定义限流处理逻辑:</h4><ol>
<li><p>==单独创建一个类,用于处理限流==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%841.png" alt></p>
</li>
<li><p>==在controller中,指定使用自定义类中的方法作为降级方法==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%842.png" alt></p>
</li>
<li><p>==Sentinel中定义流控规则==:</p>
<p>这里资源名,是以url指定,也可以使用@SentinelResource注解value的值指定</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%845.png" alt></p>
</li>
</ol>
<ol start="4">
<li><p>==测试==:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%843.png" alt></p>
</li>
<li><p>==整体==:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%844.png" alt></p>
</li>
<li></li>
</ol>
<h4 id="SentinelResource注解的其他属性"><a href="#SentinelResource注解的其他属性" class="headerlink" title="@SentinelResource注解的其他属性:"></a>@SentinelResource注解的其他属性:</h4><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%847.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%846.png" alt></p>
<h3 id="服务熔断"><a href="#服务熔断" class="headerlink" title="服务熔断:"></a>服务熔断:</h3><ol>
<li><p><strong>启动nacos和sentinel</strong></p>
</li>
<li><p><strong>新建两个pay模块  9003和9004</strong></p>
<ol>
<li><p>pom</p>
</li>
<li><p>配置文件</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%848.png" alt>*</p>
</li>
<li><p>主启动类 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@EnableDiscoveryClient</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PaymentMain9003</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(PaymentMain9003<span class="class">.<span class="keyword">class</span>,<span class="title">args</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>controller</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%849.png" alt></p>
<p><strong>然后启动9003.9004</strong></p>
</li>
</ol>
</li>
<li><p><strong>新建一个order-84消费者模块:</strong></p>
<ol>
<li><p>pom</p>
<p>与上面的pay一模一样</p>
</li>
<li><p>配置文件</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8410.png" alt></p>
</li>
<li><p>主启动类</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8411.png" alt></p>
</li>
<li><p>配置类</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8412.png" alt></p>
</li>
<li><p>controller</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8413.png" alt></p>
</li>
</ol>
</li>
</ol>
<pre><code>6.   **==为业务方法添加fallback来指定降级方法==**:

    ![](SpringCloud学习\sentinel的的14.png)

    ​    ==重启order==

    测试:

    ![](SpringCloud学习\sentinel的的15.png)



     ==所以,fallback是用于管理异常的,当业务方法发生异常,可以降级到指定方法==

    ​            注意,我们这里==并没有使用sentinel配置任何规则==,但是却降级成功,就是因为

    ​            fallback是用于管理异常的,当业务方法发生异常,可以降级到指定方法==



7.   **==为业务方法添加blockHandler,看看是什么效果==**

     ![](SpringCloud学习\sentinel的的16.png)

     **重启84,访问业务方法:**

    ![](SpringCloud学习\sentinel的的17.png)

     可以看到.,直接报错了,并没有降级

    ​                也就是说,blockHandler==只对sentienl定义的规则降级==



8.   **==如果fallback和blockHandler都配置呢?==**]

     ![](SpringCloud学习\sentinel的的18.png)

     **设置qps规则,阈值1**

     ![](SpringCloud学习\sentinel的的19.png)

     ==测试:==

    ![](SpringCloud学习\sentinel的的20.png)



     可以看到,当两个都同时生效时,==blockhandler优先生效==

9.  **==@SentinelResource还有一个属性,exceptionsToIgnore==**

     ![](SpringCloud学习\sentinel的的21.png)

     **exceptionsToIgnore指定一个异常类,**

    ​                    **表示如果当前方法抛出的是指定的异常,不降级,直接对用户抛出异常**

     ![](SpringCloud学习\sentinel的的22.png)</code></pre><h3 id="sentinel整合ribbon-openFeign-fallback"><a href="#sentinel整合ribbon-openFeign-fallback" class="headerlink" title="sentinel整合ribbon+openFeign+fallback"></a>sentinel整合ribbon+openFeign+fallback</h3><ol>
<li><p>修改84模块,使其支持feign</p>
<ol>
<li><p>pom</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8423.png" alt></p>
</li>
<li><p>配置文件</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8424.png" alt></p>
</li>
<li><p>主启动类,也要修改</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8425.png" alt></p>
</li>
<li><p>创建远程调用pay模块的接口</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8426.png" alt></p>
</li>
<li><p>创建这个接口的实现类,用于降级</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8427.png" alt></p>
</li>
<li><p>再次修改接口,指定降级类</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8428.png" alt></p>
</li>
<li><p>controller添加远程调用</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8429.png" alt></p>
</li>
<li><p>测试</p>
<p>启动9003,84</p>
</li>
<li><p>测试,如果关闭9003.看看84会不会降级</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8430.png" alt></p>
<p><strong>可以看到,正常降级了</strong></p>
</li>
</ol>
</li>
</ol>
<p><strong>熔断框架比较</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8431.png" alt></p>
<h3 id="sentinel持久化规则"><a href="#sentinel持久化规则" class="headerlink" title="sentinel持久化规则"></a>sentinel持久化规则</h3><p>默认规则是临时存储的,重启sentinel就会消失</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8432.png" alt></p>
<p><strong>这里以之前的8401为案例进行修改:</strong></p>
<ol>
<li><p>修改8401的pom</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">添加:</span><br><span class="line"><span class="comment">&lt;!-- SpringCloud ailibaba sentinel-datasource-nacos 持久化需要用到--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba.csp<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>sentinel-datasource-nacos<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>



</li>
</ol>
<ol start="2">
<li><p>修改配置文件:</p>
<p>添加:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8433.png" alt></p>
<p><strong>实际上就是指定,我们的规则要保证在哪个名称空间的哪个分组下</strong></p>
<pre><code>这里没有指定namespace, 但是是可以指定的</code></pre><p>​            <strong>注意,这里的dataid要与8401的服务名一致</strong></p>
</li>
<li><p><strong>在nacos中创建一个配置文件,dataId就是上面配置文件中指定的</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8434.png" alt></p>
<p>==json中,这些属性的含义:==</p>
<p>​    <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8435.png" alt></p>
</li>
</ol>
<ol start="4">
<li><p>启动8401:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8436.png" alt></p>
<p>可以看到,直接读取到了规则</p>
</li>
<li><p>关闭8401</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8437.png" alt></p>
</li>
<li><p>此时重启8401,如果sentinel又可以正常读取到规则,那么证明持久化成功</p>
<p>可以看到,又重新出现了</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Csentinel%E7%9A%84%E7%9A%8438.png" alt></p>
</li>
</ol>
<h2 id="Seata"><a href="#Seata" class="headerlink" title="Seata:"></a>Seata:</h2><p>是一个分布式事务的解决方案,</p>
<p><strong>分布式事务中的一些概念,也是seata中的概念:</strong></p>
<p>​        <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%842.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%843.png" alt></p>
<h3 id="seata安装"><a href="#seata安装" class="headerlink" title="seata安装:"></a>seata安装:</h3><ol>
<li><p><strong>下载安装seata的安装包</strong></p>
</li>
<li><p><strong>修改file.conf</strong></p>
<p> <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%844.png" alt></p>
<p> <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%845.png" alt></p>
<p> <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%846.png" alt></p>
</li>
<li><p><strong>mysql建库建表</strong></p>
<p>1,上面指定了数据库为seata,所以创建一个数据库名为seata</p>
<p>2,建表,在seata的安装目录下有一个db_store.sql,运行即可</p>
</li>
<li><p><strong>继续修改配置文件,修改registry.conf</strong></p>
<p>配置seata作为微服务,指定注册中心</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%847.png" alt></p>
</li>
<li><p>启动</p>
<p>先启动nacos</p>
<p>在启动seata-server(运行安装目录下的,seata-server.bat)</p>
</li>
</ol>
<p><strong>业务说明</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%848.png" alt></p>
<p>下单—&gt;库存—&gt;账号余额</p>
<ol>
<li><p>创建三个数据库</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%849.png" alt></p>
</li>
<li><p>创建对应的表</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8410.png" alt></p>
</li>
<li><p>创建回滚日志表,方便查看</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8411.png" alt></p>
<p><strong>注意==每个库都要执行一次==这个sql,生成回滚日志表</strong></p>
</li>
<li><p>==每个业务都创建一个微服务,也就是要有三个微服务,订单,库存,账号==</p>
<p>​     ==订单==,seta-order-2001</p>
<ol>
<li><p>pom</p>
</li>
<li><p>配置文件</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">2001</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">seata-order-service</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">alibaba:</span></span><br><span class="line">      <span class="attr">seata:</span></span><br><span class="line">        <span class="comment"># 自定义事务组名称需要与seata-server中的对应,我们之前在seata的配置文件中配置的名字</span></span><br><span class="line">        <span class="attr">tx-service-group:</span> <span class="string">fsp_tx_group</span></span><br><span class="line">    <span class="attr">nacos:</span></span><br><span class="line">      <span class="attr">discovery:</span></span><br><span class="line">        <span class="attr">server-addr:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:8848</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="comment"># 当前数据源操作类型</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">com.alibaba.druid.pool.DruidDataSource</span></span><br><span class="line">    <span class="comment"># mysql驱动类</span></span><br><span class="line">    <span class="attr">driver-class-name:</span> <span class="string">com.mysql.cj.jdbc.Driver</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">jdbc:mysql://localhost:3306/seata_order?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false&amp;serverTimezone=GMT%2B8</span></span><br><span class="line">    <span class="attr">username:</span> <span class="string">root</span></span><br><span class="line">    <span class="attr">password:</span> <span class="string">root</span></span><br><span class="line"><span class="attr">feign:</span></span><br><span class="line">  <span class="attr">hystrix:</span></span><br><span class="line">    <span class="attr">enabled:</span> <span class="literal">false</span></span><br><span class="line"><span class="attr">logging:</span></span><br><span class="line">  <span class="attr">level:</span></span><br><span class="line">    <span class="attr">io:</span></span><br><span class="line">      <span class="attr">seata:</span> <span class="string">info</span></span><br><span class="line"></span><br><span class="line"><span class="attr">mybatis:</span></span><br><span class="line">  <span class="attr">mapperLocations:</span> <span class="string">classpath*:mapper/*.xml</span></span><br></pre></td></tr></table></figure>

<p>还要额外创建其他配置文件,创建一个file.conf:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">transport &#123;</span><br><span class="line">  # tcp udt unix-domain-socket</span><br><span class="line">  type &#x3D; &quot;TCP&quot;</span><br><span class="line">  #NIO NATIVE</span><br><span class="line">  server &#x3D; &quot;NIO&quot;</span><br><span class="line">  #enable heartbeat</span><br><span class="line">  heartbeat &#x3D; true</span><br><span class="line">  #thread factory for netty</span><br><span class="line">  thread-factory &#123;</span><br><span class="line">    boss-thread-prefix &#x3D; &quot;NettyBoss&quot;</span><br><span class="line">    worker-thread-prefix &#x3D; &quot;NettyServerNIOWorker&quot;</span><br><span class="line">    server-executor-thread-prefix &#x3D; &quot;NettyServerBizHandler&quot;</span><br><span class="line">    share-boss-worker &#x3D; false</span><br><span class="line">    client-selector-thread-prefix &#x3D; &quot;NettyClientSelector&quot;</span><br><span class="line">    client-selector-thread-size &#x3D; 1</span><br><span class="line">    client-worker-thread-prefix &#x3D; &quot;NettyClientWorkerThread&quot;</span><br><span class="line">    # netty boss thread size,will not be used for UDT</span><br><span class="line">    boss-thread-size &#x3D; 1</span><br><span class="line">    #auto default pin or 8</span><br><span class="line">    worker-thread-size &#x3D; 8</span><br><span class="line">  &#125;</span><br><span class="line">  shutdown &#123;</span><br><span class="line">    # when destroy server, wait seconds</span><br><span class="line">    wait &#x3D; 3</span><br><span class="line">  &#125;</span><br><span class="line">  serialization &#x3D; &quot;seata&quot;</span><br><span class="line">  compressor &#x3D; &quot;none&quot;</span><br><span class="line">&#125;</span><br><span class="line">service &#123;</span><br><span class="line">  #vgroup-&gt;rgroup</span><br><span class="line">  # 事务组名称</span><br><span class="line">  vgroup_mapping.fsp_tx_group &#x3D; &quot;default&quot;</span><br><span class="line">  #only support single node</span><br><span class="line">  default.grouplist &#x3D; &quot;127.0.0.1:8091&quot;</span><br><span class="line">  #degrade current not support</span><br><span class="line">  enableDegrade &#x3D; false</span><br><span class="line">  #disable</span><br><span class="line">  disable &#x3D; false</span><br><span class="line">  #unit ms,s,m,h,d represents milliseconds, seconds, minutes, hours, days, default permanent</span><br><span class="line">  max.commit.retry.timeout &#x3D; &quot;-1&quot;</span><br><span class="line">  max.rollback.retry.timeout &#x3D; &quot;-1&quot;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">client &#123;</span><br><span class="line">  async.commit.buffer.limit &#x3D; 10000</span><br><span class="line">  lock &#123;</span><br><span class="line">    retry.internal &#x3D; 10</span><br><span class="line">    retry.times &#x3D; 30</span><br><span class="line">  &#125;</span><br><span class="line">  report.retry.count &#x3D; 5</span><br><span class="line">  tm.commit.retry.count &#x3D; 1</span><br><span class="line">  tm.rollback.retry.count &#x3D; 1</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">## transaction log store</span><br><span class="line">store &#123;</span><br><span class="line">  ## store mode: file、db</span><br><span class="line">  #mode &#x3D; &quot;file&quot;</span><br><span class="line">  mode &#x3D; &quot;db&quot;</span><br><span class="line"> </span><br><span class="line">  ## file store</span><br><span class="line">  file &#123;</span><br><span class="line">    dir &#x3D; &quot;sessionStore&quot;</span><br><span class="line"> </span><br><span class="line">    # branch session size , if exceeded first try compress lockkey, still exceeded throws exceptions</span><br><span class="line">    max-branch-session-size &#x3D; 16384</span><br><span class="line">    # globe session size , if exceeded throws exceptions</span><br><span class="line">    max-global-session-size &#x3D; 512</span><br><span class="line">    # file buffer size , if exceeded allocate new buffer</span><br><span class="line">    file-write-buffer-cache-size &#x3D; 16384</span><br><span class="line">    # when recover batch read size</span><br><span class="line">    session.reload.read_size &#x3D; 100</span><br><span class="line">    # async, sync</span><br><span class="line">    flush-disk-mode &#x3D; async</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  ## database store</span><br><span class="line">  db &#123;</span><br><span class="line">    ## the implement of javax.sql.DataSource, such as DruidDataSource(druid)&#x2F;BasicDataSource(dbcp) etc.</span><br><span class="line">    datasource &#x3D; &quot;dbcp&quot;</span><br><span class="line">    ## mysql&#x2F;oracle&#x2F;h2&#x2F;oceanbase etc.</span><br><span class="line">    db-type &#x3D; &quot;mysql&quot;</span><br><span class="line">    driver-class-name &#x3D; &quot;com.mysql.jdbc.Driver&quot;</span><br><span class="line">    url &#x3D; &quot;jdbc:mysql:&#x2F;&#x2F;127.0.0.1:3306&#x2F;seata&quot;</span><br><span class="line">    user &#x3D; &quot;root&quot;</span><br><span class="line">    password &#x3D; &quot;root&quot;</span><br><span class="line">    min-conn &#x3D; 1</span><br><span class="line">    max-conn &#x3D; 3</span><br><span class="line">    global.table &#x3D; &quot;global_table&quot;</span><br><span class="line">    branch.table &#x3D; &quot;branch_table&quot;</span><br><span class="line">    lock-table &#x3D; &quot;lock_table&quot;</span><br><span class="line">    query-limit &#x3D; 100</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">lock &#123;</span><br><span class="line">  ## the lock store mode: local、remote</span><br><span class="line">  mode &#x3D; &quot;remote&quot;</span><br><span class="line"> </span><br><span class="line">  local &#123;</span><br><span class="line">    ## store locks in user&#39;s database</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  remote &#123;</span><br><span class="line">    ## store locks in the seata&#39;s server</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">recovery &#123;</span><br><span class="line">  #schedule committing retry period in milliseconds</span><br><span class="line">  committing-retry-period &#x3D; 1000</span><br><span class="line">  #schedule asyn committing retry period in milliseconds</span><br><span class="line">  asyn-committing-retry-period &#x3D; 1000</span><br><span class="line">  #schedule rollbacking retry period in milliseconds</span><br><span class="line">  rollbacking-retry-period &#x3D; 1000</span><br><span class="line">  #schedule timeout retry period in milliseconds</span><br><span class="line">  timeout-retry-period &#x3D; 1000</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">transaction &#123;</span><br><span class="line">  undo.data.validation &#x3D; true</span><br><span class="line">  undo.log.serialization &#x3D; &quot;jackson&quot;</span><br><span class="line">  undo.log.save.days &#x3D; 7</span><br><span class="line">  #schedule delete expired undo_log in milliseconds</span><br><span class="line">  undo.log.delete.period &#x3D; 86400000</span><br><span class="line">  undo.log.table &#x3D; &quot;undo_log&quot;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">## metrics settings</span><br><span class="line">metrics &#123;</span><br><span class="line">  enabled &#x3D; false</span><br><span class="line">  registry-type &#x3D; &quot;compact&quot;</span><br><span class="line">  # multi exporters use comma divided</span><br><span class="line">  exporter-list &#x3D; &quot;prometheus&quot;</span><br><span class="line">  exporter-prometheus-port &#x3D; 9898</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">support &#123;</span><br><span class="line">  ## spring</span><br><span class="line">  spring &#123;</span><br><span class="line">    # auto proxy the DataSource bean</span><br><span class="line">    datasource.autoproxy &#x3D; false</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>创建registry.conf:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">registry &#123;</span><br><span class="line">  # file 、nacos 、eureka、redis、zk、consul、etcd3、sofa</span><br><span class="line">  type &#x3D; &quot;nacos&quot;</span><br><span class="line"> </span><br><span class="line">  nacos &#123;</span><br><span class="line">    #serverAddr &#x3D; &quot;localhost&quot;</span><br><span class="line">    serverAddr &#x3D; &quot;localhost:8848&quot;</span><br><span class="line">    namespace &#x3D; &quot;&quot;</span><br><span class="line">    cluster &#x3D; &quot;default&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  eureka &#123;</span><br><span class="line">    serviceUrl &#x3D; &quot;http:&#x2F;&#x2F;localhost:8761&#x2F;eureka&quot;</span><br><span class="line">    application &#x3D; &quot;default&quot;</span><br><span class="line">    weight &#x3D; &quot;1&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  redis &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;localhost:6379&quot;</span><br><span class="line">    db &#x3D; &quot;0&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  zk &#123;</span><br><span class="line">    cluster &#x3D; &quot;default&quot;</span><br><span class="line">    serverAddr &#x3D; &quot;127.0.0.1:2181&quot;</span><br><span class="line">    session.timeout &#x3D; 6000</span><br><span class="line">    connect.timeout &#x3D; 2000</span><br><span class="line">  &#125;</span><br><span class="line">  consul &#123;</span><br><span class="line">    cluster &#x3D; &quot;default&quot;</span><br><span class="line">    serverAddr &#x3D; &quot;127.0.0.1:8500&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  etcd3 &#123;</span><br><span class="line">    cluster &#x3D; &quot;default&quot;</span><br><span class="line">    serverAddr &#x3D; &quot;http:&#x2F;&#x2F;localhost:2379&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  sofa &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;127.0.0.1:9603&quot;</span><br><span class="line">    application &#x3D; &quot;default&quot;</span><br><span class="line">    region &#x3D; &quot;DEFAULT_ZONE&quot;</span><br><span class="line">    datacenter &#x3D; &quot;DefaultDataCenter&quot;</span><br><span class="line">    cluster &#x3D; &quot;default&quot;</span><br><span class="line">    group &#x3D; &quot;SEATA_GROUP&quot;</span><br><span class="line">    addressWaitTime &#x3D; &quot;3000&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  file &#123;</span><br><span class="line">    name &#x3D; &quot;file.conf&quot;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">config &#123;</span><br><span class="line">  # file、nacos 、apollo、zk、consul、etcd3</span><br><span class="line">  type &#x3D; &quot;file&quot;</span><br><span class="line"> </span><br><span class="line">  nacos &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;localhost&quot;</span><br><span class="line">    namespace &#x3D; &quot;&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  consul &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;127.0.0.1:8500&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  apollo &#123;</span><br><span class="line">    app.id &#x3D; &quot;seata-server&quot;</span><br><span class="line">    apollo.meta &#x3D; &quot;http:&#x2F;&#x2F;192.168.1.204:8801&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  zk &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;127.0.0.1:2181&quot;</span><br><span class="line">    session.timeout &#x3D; 6000</span><br><span class="line">    connect.timeout &#x3D; 2000</span><br><span class="line">  &#125;</span><br><span class="line">  etcd3 &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;http:&#x2F;&#x2F;localhost:2379&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  file &#123;</span><br><span class="line">    name &#x3D; &quot;file.conf&quot;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>==实际上,就是要将seata中的我们之前修改的两个配置文件复制到这个项目下==</p>
</li>
<li><p><strong>主启动类</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span>(exclude = DataSourceAutoConfiguration<span class="class">.<span class="keyword">class</span>) //取消数据源的自动创建</span></span><br><span class="line"><span class="class">@<span class="title">EnableDiscoveryClient</span></span></span><br><span class="line"><span class="class">@<span class="title">EnableFeignClients</span></span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">SeataOrderMain2001</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(SeataOrderMain2001<span class="class">.<span class="keyword">class</span>,<span class="title">args</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



</li>
</ol>
</li>
</ol>
<pre><code>4.   **service层**

     <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">public interface OrderService &#123;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 创建订单</span><br><span class="line">     * @param order</span><br><span class="line">     */</span><br><span class="line">    void create(Order order);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

    <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">@FeignClient(value = "seata-storage-service")</span><br><span class="line">public interface StorageService &#123;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 减库存</span><br><span class="line">     * @param productId</span><br><span class="line">     * @param count</span><br><span class="line">     * @return</span><br><span class="line">     */</span><br><span class="line">    @PostMapping(value = "/storage/decrease")</span><br><span class="line">    CommonResult decrease(@RequestParam("productId") Long productId, @RequestParam("count") Integer count);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

    <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">@FeignClient(value = "seata-account-service")</span><br><span class="line">public interface AccountService &#123;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 减余额</span><br><span class="line">     * @param userId</span><br><span class="line">     * @param money</span><br><span class="line">     * @return</span><br><span class="line">     */</span><br><span class="line">    @PostMapping(value = "/account/decrease")</span><br><span class="line">    CommonResult decrease(@RequestParam("userId") Long userId, @RequestParam("money") BigDecimal money);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

    <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">@Service</span><br><span class="line">@Slf4j</span><br><span class="line">public class OrderServiceImpl implements OrderService &#123;</span><br><span class="line"></span><br><span class="line">    @Resource</span><br><span class="line">    private OrderDao orderDao;</span><br><span class="line">    @Resource</span><br><span class="line">    private AccountService accountService;</span><br><span class="line">    @Resource</span><br><span class="line">    private StorageService storageService;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 创建订单-&gt;调用库存服务扣减库存-&gt;调用账户服务扣减账户余额-&gt;修改订单状态</span><br><span class="line">     * 简单说:</span><br><span class="line">     * 下订单-&gt;减库存-&gt;减余额-&gt;改状态</span><br><span class="line">     * GlobalTransactional seata开启分布式事务,异常时回滚,name保证唯一即可</span><br><span class="line">     * @param order 订单对象</span><br><span class="line">     */</span><br><span class="line">    @Override</span><br><span class="line">    ///@GlobalTransactional(name = "fsp-create-order", rollbackFor = Exception.class)</span><br><span class="line">    public void create(Order order) &#123;</span><br><span class="line">        // 1 新建订单</span><br><span class="line">        log.info("-----&gt;开始新建订单");</span><br><span class="line">        orderDao.create(order);</span><br><span class="line"></span><br><span class="line">        // 2 扣减库存</span><br><span class="line">        log.info("-----&gt;订单微服务开始调用库存,做扣减Count");</span><br><span class="line">        storageService.decrease(order.getProductId(), order.getCount());</span><br><span class="line">        log.info("-----&gt;订单微服务开始调用库存,做扣减End");</span><br><span class="line"></span><br><span class="line">        // 3 扣减账户</span><br><span class="line">        log.info("-----&gt;订单微服务开始调用账户,做扣减Money");</span><br><span class="line">        accountService.decrease(order.getUserId(), order.getMoney());</span><br><span class="line">        log.info("-----&gt;订单微服务开始调用账户,做扣减End");</span><br><span class="line"></span><br><span class="line">        // 4 修改订单状态,从0到1,1代表已完成</span><br><span class="line">        log.info("-----&gt;修改订单状态开始");</span><br><span class="line">        orderDao.update(order.getUserId(), 0);</span><br><span class="line"></span><br><span class="line">        log.info("-----&gt;下订单结束了,O(∩_∩)O哈哈~");</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>















5.   **dao层,也就是接口**

    <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Mapper</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">OrderDao</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 1 新建订单</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> order</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">create</span><span class="params">(Order order)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 2 修改订单状态,从0改为1</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> userId</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> status</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">update</span><span class="params">(@Param(<span class="string">"userId"</span>)</span> Long userId, @<span class="title">Param</span><span class="params">(<span class="string">"status"</span>)</span> Integer status)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

     ==在resource下创建mapper文件夹,编写mapper.xml==

    <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">mapper</span> <span class="meta-keyword">PUBLIC</span> <span class="meta-string">"-//mybatis.org//DTD Mapper 3.0//EN"</span></span></span><br><span class="line"><span class="meta">        <span class="meta-string">"http://mybatis.org/dtd/mybatis-3-mapper.dtd"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mapper</span> <span class="attr">namespace</span>=<span class="string">"com.eiletxie.springcloud.alibaba.dao.OrderDao"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"BaseResultMap"</span> <span class="attr">type</span>=<span class="string">"com.eiletxie.springcloud.alibaba.domain.Order"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">"id"</span> <span class="attr">property</span>=<span class="string">"id"</span> <span class="attr">jdbcType</span>=<span class="string">"BIGINT"</span>&gt;</span><span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"user_id"</span> <span class="attr">property</span>=<span class="string">"userId"</span> <span class="attr">jdbcType</span>=<span class="string">"BIGINT"</span>&gt;</span><span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"product_id"</span> <span class="attr">property</span>=<span class="string">"productId"</span> <span class="attr">jdbcType</span>=<span class="string">"BIGINT"</span>&gt;</span><span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"count"</span> <span class="attr">property</span>=<span class="string">"count"</span> <span class="attr">jdbcType</span>=<span class="string">"INTEGER"</span>&gt;</span><span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"money"</span> <span class="attr">property</span>=<span class="string">"money"</span> <span class="attr">jdbcType</span>=<span class="string">"DECIMAL"</span>&gt;</span><span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"status"</span> <span class="attr">property</span>=<span class="string">"status"</span> <span class="attr">jdbcType</span>=<span class="string">"INTEGER"</span>&gt;</span><span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">insert</span> <span class="attr">id</span>=<span class="string">"create"</span> <span class="attr">parameterType</span>=<span class="string">"com.eiletxie.springcloud.alibaba.domain.Order"</span> <span class="attr">useGeneratedKeys</span>=<span class="string">"true"</span></span></span><br><span class="line"><span class="tag">            <span class="attr">keyProperty</span>=<span class="string">"id"</span>&gt;</span></span><br><span class="line">        insert into t_order(user_id,product_id,count,money,status) values (#&#123;userId&#125;,#&#123;productId&#125;,#&#123;count&#125;,#&#123;money&#125;,0);</span><br><span class="line">    <span class="tag">&lt;/<span class="name">insert</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">update</span> <span class="attr">id</span>=<span class="string">"update"</span>&gt;</span></span><br><span class="line">        update t_order set status =1 where user_id =#&#123;userId&#125; and status=#&#123;status&#125;;</span><br><span class="line">   <span class="tag">&lt;/<span class="name">update</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mapper</span>&gt;</span></span><br></pre></td></tr></table></figure>

6.   **controller层**

    <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OrderController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> OrderService orderService;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 创建订单</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> order</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/order/create"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> CommonResult <span class="title">create</span><span class="params">(Order order)</span> </span>&#123;</span><br><span class="line">        orderService.create(order);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> CommonResult(<span class="number">200</span>, <span class="string">"订单创建成功"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



7.   **entity类(也叫domain类)**

     <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommonResult</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Integer code;</span><br><span class="line">    <span class="keyword">private</span> String message;</span><br><span class="line">    <span class="keyword">private</span> T data;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CommonResult</span><span class="params">(Integer code, String message)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(code, message, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

    ![](SpringCloud学习\seala的12.png)







8.   config配置类

    <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@MapperScan</span>(&#123;<span class="string">"com.eiletxie.springcloud.alibaba.dao"</span>&#125;)		指定我们的接口的位置</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBatisConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

    <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span> EiletXie</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Since</span> 2020/3/18 21:51</span></span><br><span class="line"><span class="comment"> * 使用Seata对数据源进行代理</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DataSourceProxyConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;mybatis.mapperLocations&#125;"</span>)</span><br><span class="line">    <span class="keyword">private</span> String mapperLocations;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.datasource"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSource <span class="title">druidDataSource</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> DruidDataSource();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSourceProxy <span class="title">dataSourceProxy</span><span class="params">(DataSource druidDataSource)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> DataSourceProxy(druidDataSource);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> SqlSessionFactory <span class="title">sqlSessionFactoryBean</span><span class="params">(DataSourceProxy dataSourceProxy)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        SqlSessionFactoryBean bean = <span class="keyword">new</span> SqlSessionFactoryBean();</span><br><span class="line">        bean.setDataSource(dataSourceProxy);</span><br><span class="line">        ResourcePatternResolver resolver = <span class="keyword">new</span> PathMatchingResourcePatternResolver();</span><br><span class="line">        bean.setMapperLocations(resolver.getResources(mapperLocations));</span><br><span class="line">        <span class="keyword">return</span> bean.getObject();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



9.   

10.   

11.  

  ==库存==,seta-storage-2002

**==看脑图==**

1.    pom   
2.   配置文件
3.   主启动类
4.    service层
5.    dao层
6.    controller层
7.   
8.   



 ==账号==,seta-account-2003

**==看脑图==**

1.    pom     
2.   配置文件
3.   主启动类
4.   service层
5.    dao层
6.   controller层
7.   
8.   </code></pre><ol start="5">
<li><p><strong>全局创建完成后,首先测试不加seata</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8414.png" alt></p>
</li>
</ol>
<pre><code>![](SpringCloud学习\seala的13.png)</code></pre><p>​    </p>
<p>​    </p>
<p>​     </p>
<ol start="6">
<li><p>使用seata:</p>
<p><strong>在==订单模块==的serviceImpl类中的==create方法==添加启动分布式事务的注解</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	这里添加开启分布式事务的注解,name指定当前全局事务的名称</span></span><br><span class="line"><span class="comment">	rollbackFor表示,发生什么异常需要回滚</span></span><br><span class="line"><span class="comment">	noRollbackFor:表示,发生什么异常不需要回滚</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="meta">@GlobalTransactional</span>(name = <span class="string">"fsp-create-order"</span>,rollbackFor = Exception<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">///@<span class="title">GlobalTransactional</span>(<span class="title">name</span> </span>= <span class="string">"fsp-create-order"</span>, rollbackFor = Exception<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">void</span> <span class="title">create</span>(<span class="title">Order</span> <span class="title">order</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 1 新建订单</span></span><br><span class="line">    log.info(<span class="string">"-----&gt;开始新建订单"</span>);</span><br><span class="line">    orderDao.create(order);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2 扣减库存</span></span><br><span class="line">    log.info(<span class="string">"-----&gt;订单微服务开始调用库存,做扣减Count"</span>);</span><br><span class="line">    storageService.decrease(order.getProductId(), order.getCount());</span><br><span class="line">    log.info(<span class="string">"-----&gt;订单微服务开始调用库存,做扣减End"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3 扣减账户</span></span><br><span class="line">    log.info(<span class="string">"-----&gt;订单微服务开始调用账户,做扣减Money"</span>);</span><br><span class="line">    accountService.decrease(order.getUserId(), order.getMoney());</span><br><span class="line">    log.info(<span class="string">"-----&gt;订单微服务开始调用账户,做扣减End"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4 修改订单状态,从0到1,1代表已完成</span></span><br><span class="line">    log.info(<span class="string">"-----&gt;修改订单状态开始"</span>);</span><br><span class="line">    orderDao.update(order.getUserId(), <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    log.info(<span class="string">"-----&gt;下订单结束了,O(∩_∩)O哈哈~"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



</li>
</ol>
<ol start="7">
<li><p>此时在测试</p>
<p>发现,发生异常后,直接回滚了,前面的修改操作都回滚了</p>
</li>
</ol>
<h3 id="setat原理"><a href="#setat原理" class="headerlink" title="setat原理:"></a>setat原理:</h3><p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8415.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8416.png" alt></p>
<p><strong>seata提供了四个模式:</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8417.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8418.png" alt></p>
<p>==第一阶段:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8420.png" alt></p>
<p>​    <img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8419.png" alt></p>
<p>==二阶段之提交==:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8421.png" alt></p>
<p>==二阶段之回滚:==</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8422.png" alt></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8423.png" alt></p>
<p>==断点==:</p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8424.png" alt></p>
<p><strong>可以看到,他们的xid全局事务id是一样的,证明他们在一个事务下</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8425.png" alt></p>
<p><strong>before 和 after的原理就是</strong></p>
<p><img src="/2020/08/01/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud%E5%AD%A6%E4%B9%A0%5Cseala%E7%9A%8426.png" alt></p>
<p><strong>在更新数据之前,先解析这个更新sql,然后查询要更新的数据,进行保存</strong></p>
]]></content>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>比较器（复习5）</title>
    <url>/2020/05/28/%E6%AF%94%E8%BE%83%E5%99%A8%EF%BC%88%E5%A4%8D%E4%B9%A05%EF%BC%89/</url>
    <content><![CDATA[<p>比较器复习.</p>
<a id="more"></a>

<h1 id="Comparable"><a href="#Comparable" class="headerlink" title="Comparable"></a>Comparable</h1><p>String,包装类实现了包装类接口，实现了compareTo方法，给出了比较对象的方式。默认从小打到排。</p>
<p>重写compareTo()的规则：如果调用者this大于形参，返回正数，否则负数。<br>普通对象排序只要继承接口，实现方法即可。</p>
<h1 id="Comparator"><a href="#Comparator" class="headerlink" title="Comparator"></a>Comparator</h1><p>若没有实现上面的接口，又不好修改代码，或者要临时换个排序方式，就用Comparator。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String[] arr = <span class="keyword">new</span> String[]&#123;<span class="string">"aa"</span>, <span class="string">"cc"</span>, <span class="string">"mm"</span>, <span class="string">"hh"</span>, <span class="string">"ll"</span>&#125;;</span><br><span class="line">Arrays.sort(arr, <span class="keyword">new</span> Comparator&lt;String&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(String o1, String o2)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (o1 != <span class="keyword">null</span> &amp;&amp; o2 != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> -o1.compareTo(o2);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h1 id="枚举类"><a href="#枚举类" class="headerlink" title="枚举类"></a>枚举类</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Season &#123;</span><br><span class="line">    SPRING(<span class="string">"春天"</span>, <span class="string">"春暖花开"</span>),<span class="comment">//开头就要定义好枚举类的对象，用逗号分隔</span></span><br><span class="line">    SUMMER(<span class="string">"夏天"</span>, <span class="string">"夏日炎炎"</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String seasonName;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String seasonDesc;</span><br><span class="line"></span><br><span class="line">    Season(String seasonName, String seasonDesc) &#123;</span><br><span class="line">        <span class="keyword">this</span>.seasonName = seasonName;</span><br><span class="line">        <span class="keyword">this</span>.seasonDesc = seasonDesc;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getSeasonName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> seasonName;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getSeasonDesc</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> seasonDesc;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>枚举类的父类是<code>java.lang.Enum</code>，故其tostring方法已被重写。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">show</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">display</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Season implements show&#123;<span class="comment">//枚举类实现接口</span></span><br><span class="line">    SPRING(<span class="string">"春天"</span>, <span class="string">"春暖花开"</span>)&#123;<span class="comment">//让每个对象有各自的表现形式，若是不想这样，就在后面实现display方法即可</span></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">display</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"这是春天"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    SUMMER(<span class="string">"夏天"</span>, <span class="string">"夏日炎炎"</span>)&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">display</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"这是夏天"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String seasonName;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String seasonDesc;</span><br><span class="line"></span><br><span class="line">    Season(String seasonName, String seasonDesc) &#123;</span><br><span class="line">        <span class="keyword">this</span>.seasonName = seasonName;</span><br><span class="line">        <span class="keyword">this</span>.seasonDesc = seasonDesc;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getSeasonName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> seasonName;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getSeasonDesc</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> seasonDesc;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>两个方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Season[] values = Season.values();<span class="comment">//返回枚举类中的对象列表</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; values.length; i++) &#123;</span><br><span class="line">    Season value = values[i];</span><br><span class="line">    System.out.println(value);</span><br><span class="line">&#125;</span><br><span class="line">Season spring = Season.valueOf(<span class="string">"SPRING"</span>);<span class="comment">//根据名字找对应的对象</span></span><br><span class="line">System.out.println(spring);</span><br></pre></td></tr></table></figure>

<h1 id="注解"><a href="#注解" class="headerlink" title="注解"></a>注解</h1><p>第一种：文档注解</p>
<p><img src="/2020/05/28/%E6%AF%94%E8%BE%83%E5%99%A8%EF%BC%88%E5%A4%8D%E4%B9%A05%EF%BC%89/%E5%89%AA%E8%B4%B4%E6%9D%BF%E5%9B%BE%E7%89%87.jpg" alt></p>
<p>第二种：JDK内置</p>
<p>@Override,（重写），@Deprecated,（已过时），@SuppressWarnings，（抑制编译器警告）</p>
<p>第三种：自定义注解</p>
<p>自定义注解自动继承java.lang.annotation.Annotation接口。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> MyAnnotation &#123;</span><br><span class="line">    <span class="function">String <span class="title">value</span><span class="params">()</span> <span class="keyword">default</span> "hello"</span>;<span class="comment">//内部成员通常用value,默认值用default，如果没有成员，表明是标识作用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第四种：元注解</p>
<p>对现有注解进行解释说明。</p>
<p>@Retention：标明注解的生命周期，具体在RetentionPolicy（枚举类）里面：SOURCE,CLASS(默认),RUNTIME(反射能够获取的)</p>
<p>@Target：标明注解能修饰哪些程序元素</p>
<p>@Document：标明Javadoc文档中显示注解</p>
<p>@Inherit：标明注解具有继承性</p>
<p>可重复注解：@Repeatable，在MyAnnotation上声明@Repeatable，成员值为MyAnnotations.class,MyAnnotation的Target和Retention必须要和MyAnnotations的相同。</p>
<p>类型注解：@Target注解中有个ElementType[],即使用@Target时的成员值，其中ElementType.TYPE_PARAMETER表示改注解能卸载变量的声明语句中,ElementType.TYPE_USE表示该注解能写在使用类型的任意语句中。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringCloud学习/SpringCloud</title>
    <url>/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/</url>
    <content><![CDATA[<h1 id="SpringCloud"><a href="#SpringCloud" class="headerlink" title="SpringCloud:"></a>SpringCloud:</h1><h2 id="0-SpringCloud升级-部分组件停用"><a href="#0-SpringCloud升级-部分组件停用" class="headerlink" title="0,SpringCloud升级,部分组件停用:"></a>0,SpringCloud升级,部分组件停用:</h2><p>1,Eureka停用,可以使用zk作为服务注册中心</p>
<p>2,服务调用,Ribbon准备停更,代替为LoadBalance</p>
<p>3,Feign改为OpenFeign</p>
<p>4,Hystrix停更,改为resilence4j</p>
<p>​        或者阿里巴巴的sentienl</p>
<p>5.Zuul改为gateway</p>
<p>6,服务配置Config改为  Nacos</p>
<p>7,服务总线Bus改为Nacos</p>
<h1 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建:"></a>环境搭建:</h1><h2 id="1-创建父工程-pom依赖"><a href="#1-创建父工程-pom依赖" class="headerlink" title="1,创建父工程,pom依赖"></a>1,创建父工程,pom依赖</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">代码...</span><br></pre></td></tr></table></figure>

<h2 id="2-创建子模块-pay模块"><a href="#2-创建子模块-pay模块" class="headerlink" title="2,创建子模块,pay模块"></a>2,创建子模块,pay模块</h2><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%843.png" alt></p>
<h3 id="1-子模块名字"><a href="#1-子模块名字" class="headerlink" title="1,子模块名字:"></a>1,子模块名字:</h3><p>​        cloud_pay_8001</p>
<h3 id="2-pom依赖"><a href="#2-pom依赖" class="headerlink" title="2,pom依赖"></a>2,pom依赖</h3><h3 id="3-创建application-yml"><a href="#3-创建application-yml" class="headerlink" title="3,创建application.yml"></a>3,创建application.yml</h3><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">	<span class="attr">port:</span> <span class="number">8001</span>   </span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">	<span class="attr">application:</span></span><br><span class="line">		<span class="attr">name:</span> <span class="string">cloud-payment-service</span></span><br><span class="line">	<span class="attr">datasource:</span></span><br><span class="line">    <span class="comment"># 当前数据源操作类型</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">com.alibaba.druid.pool.DruidDataSource</span></span><br><span class="line">    <span class="comment"># mysql驱动类</span></span><br><span class="line">    <span class="attr">driver-class-name:</span> <span class="string">com.mysql.cj.jdbc.Driver</span></span><br><span class="line">      <span class="attr">url:</span> <span class="string">jdbc:mysql://localhost:3306/db2019?useUnicode=true&amp;characterEncoding=</span></span><br><span class="line">            <span class="string">UTF-8&amp;useSSL=false&amp;serverTimezone=GMT%2B8</span>								</span><br><span class="line">    <span class="attr">username:</span> <span class="string">root</span></span><br><span class="line">    <span class="attr">password:</span> <span class="string">root</span></span><br><span class="line"><span class="attr">mybatis:</span>			</span><br><span class="line">    <span class="attr">mapper-locations:</span> <span class="string">classpath*:mapper/*.xml</span></span><br><span class="line">   	<span class="attr">type-aliases-package:</span> <span class="string">com.eiletxie.springcloud.entities</span></span><br><span class="line">   			<span class="string">它一般对应我们的实体类所在的包，这个时候会自动取对应包中不包括包名的简单类名作为包括包名的别名。多个package之间可以用逗号或者分号等来进行分隔（value的值一定要是包的全）</span></span><br></pre></td></tr></table></figure>

<h3 id="4-主启动类"><a href="#4-主启动类" class="headerlink" title="4,主启动类"></a>4,主启动类</h3><p>​        ….</p>
<h3 id="5-业务类"><a href="#5-业务类" class="headerlink" title="5,业务类"></a>5,业务类</h3><h4 id="1-sql"><a href="#1-sql" class="headerlink" title="1,sql"></a>1,sql</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%844.png" alt></p>
<h4 id="2-实体类"><a href="#2-实体类" class="headerlink" title="2,实体类"></a>2,实体类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%845.png" alt></p>
<h4 id="3-entity类"><a href="#3-entity类" class="headerlink" title="3,.entity类"></a>3,.entity类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%846.png" alt></p>
<h4 id="4-dao层"><a href="#4-dao层" class="headerlink" title="4,dao层:"></a>4,dao层:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%847.png" alt></p>
<h4 id="5-mapper配置文件类"><a href="#5-mapper配置文件类" class="headerlink" title="5,mapper配置文件类"></a>5,mapper配置文件类</h4><p>​                <strong>在resource下,创建mapper/PayMapper.xml</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%848.png" alt></p>
<h4 id="6-写service和serviceImpl"><a href="#6-写service和serviceImpl" class="headerlink" title="6,写service和serviceImpl"></a>6,写service和serviceImpl</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%849.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%8410.png" alt="sc的9"></p>
<h4 id="7-controller"><a href="#7-controller" class="headerlink" title="7,controller"></a>7,controller</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%8411.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%8412.png" alt></p>
<h2 id="3-热部署"><a href="#3-热部署" class="headerlink" title="3,热部署:"></a>3,热部署:</h2><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%8413.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%8414.png" alt></p>
<p>…..</p>
<p>…..</p>
<p>….</p>
<h2 id="4-order模块"><a href="#4-order模块" class="headerlink" title="4,order模块"></a>4,order模块</h2><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csc%E7%9A%843.png" alt></p>
<h3 id="1-pom"><a href="#1-pom" class="headerlink" title="1,pom"></a><strong>1,pom</strong></h3><h3 id="2-yml配置文件"><a href="#2-yml配置文件" class="headerlink" title="2,yml配置文件"></a><strong>2,yml配置文件</strong></h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Corder%E6%A8%A1%E5%9D%971.png" alt></p>
<h3 id="3-主启动类"><a href="#3-主启动类" class="headerlink" title="3,主启动类"></a><strong>3,主启动类</strong></h3><h3 id="4-复制pay模块的实体类-entity类"><a href="#4-复制pay模块的实体类-entity类" class="headerlink" title="4.复制pay模块的实体类,entity类"></a><strong>4.复制pay模块的实体类,entity类</strong></h3><h3 id="5-写controller类"><a href="#5-写controller类" class="headerlink" title="5,写controller类"></a><strong>5,写controller类</strong></h3><p>​        因为这里是消费者类,主要是消费,那么就没有service和dao,需要调用pay模块的方法</p>
<p>​        并且这里还没有微服务的远程调用,那么如果要调用另外一个模块,则需要使用基本的api调用</p>
<p>使用RestTemplate调用pay模块,</p>
<p>​    <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Corder%E6%A8%A1%E5%9D%972.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Corder%E6%A8%A1%E5%9D%973.png" alt></p>
<p>​    将restTemplate注入到容器</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Corder%E6%A8%A1%E5%9D%974.png" alt></p>
<p>编写controller:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Corder%E6%A8%A1%E5%9D%975.png" alt></p>
<h2 id="5-重构"><a href="#5-重构" class="headerlink" title="5,重构,"></a>5,重构,</h2><p>新建一个模块,将重复代码抽取到一个公共模块中</p>
<h3 id="1-创建commons模块"><a href="#1-创建commons模块" class="headerlink" title="1,创建commons模块"></a>1,创建commons模块</h3><h3 id="2-抽取公共pom"><a href="#2-抽取公共pom" class="headerlink" title="2,抽取公共pom"></a>2,抽取公共pom</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Ccommons%E6%A8%A1%E5%9D%97.png" alt></p>
<h3 id="3-entity和实体类放入commons中"><a href="#3-entity和实体类放入commons中" class="headerlink" title="3,entity和实体类放入commons中"></a>3,entity和实体类放入commons中</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Ccommons%E6%A8%A1%E5%9D%972.png" alt></p>
<h3 id="4-使用mavne-将commone模块打包-install"><a href="#4-使用mavne-将commone模块打包-install" class="headerlink" title="4,使用mavne,将commone模块打包(install),"></a>4,使用mavne,将commone模块打包(install),</h3><p>​        其他模块引入commons</p>
<h1 id="2-服务注册与发现"><a href="#2-服务注册与发现" class="headerlink" title="2,服务注册与发现"></a>2,服务注册与发现</h1><h2 id="6-Eureka"><a href="#6-Eureka" class="headerlink" title="6,Eureka:"></a>6,Eureka:</h2><p>前面我们没有服务注册中心,也可以服务间调用,为什么还要服务注册?</p>
<p>当服务很多时,单靠代码手动管理是很麻烦的,需要一个公共组件,统一管理多服务,包括服务是否正常运行,等</p>
<p>Eureka用于<strong>==服务注册==</strong>,目前官网<strong>已经停止更新</strong></p>
<p>​    <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%841.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%842.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%843.png" alt></p>
<p> <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%844.png" alt></p>
<h3 id="单机版eureka"><a href="#单机版eureka" class="headerlink" title="单机版eureka:"></a><strong>单机版eureka:</strong></h3><h4 id="1-创建项目cloud-eureka-server-7001"><a href="#1-创建项目cloud-eureka-server-7001" class="headerlink" title="1,创建项目cloud_eureka_server_7001"></a><strong>1,创建项目cloud_eureka_server_7001</strong></h4><h4 id="2-引入pom依赖"><a href="#2-引入pom依赖" class="headerlink" title="2,引入pom依赖"></a><strong>2,引入pom依赖</strong></h4><p>​        eurka最新的依赖变了</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%845.png" alt></p>
<h4 id="3-配置文件"><a href="#3-配置文件" class="headerlink" title="3,配置文件:"></a>3,配置文件:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%846.png" alt></p>
<h4 id="4-主启动类-1"><a href="#4-主启动类-1" class="headerlink" title="4,主启动类"></a>4,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%847.png" alt></p>
<h4 id="5-此时就可以启动当前项目了"><a href="#5-此时就可以启动当前项目了" class="headerlink" title="5,此时就可以启动当前项目了"></a><strong>5,此时就可以启动当前项目了</strong></h4><h4 id="6-其他服务注册到eureka"><a href="#6-其他服务注册到eureka" class="headerlink" title="6,其他服务注册到eureka:"></a><strong>6,其他服务注册到eureka:</strong></h4><p>比如此时pay模块加入eureka:</p>
<h5 id="1-主启动类上-加注解-表示当前是eureka客户端"><a href="#1-主启动类上-加注解-表示当前是eureka客户端" class="headerlink" title="1.主启动类上,加注解,表示当前是eureka客户端"></a>1.主启动类上,加注解,表示当前是eureka客户端</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8410.png" alt></p>
<h5 id="2-修改pom-引入"><a href="#2-修改pom-引入" class="headerlink" title="2,修改pom,引入"></a>2,修改pom,引入</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%848.png" alt></p>
<h5 id="3-修改配置文件"><a href="#3-修改配置文件" class="headerlink" title="3,修改配置文件:"></a>3,修改配置文件:</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%849.png" alt></p>
<h5 id="4-pay模块重启-就可以注册到eureka中了"><a href="#4-pay模块重启-就可以注册到eureka中了" class="headerlink" title="4,pay模块重启,就可以注册到eureka中了"></a>4,pay模块重启,就可以注册到eureka中了</h5><p><strong>==order模块的注册是一样的==</strong></p>
<h3 id="集群版eureka"><a href="#集群版eureka" class="headerlink" title="集群版eureka:"></a>集群版eureka:</h3><h4 id="集群原理"><a href="#集群原理" class="headerlink" title="集群原理:"></a>集群原理:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8411.png" alt></p>
 <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span>,就是pay模块启动时,注册自己,并且自身信息也放入eureka</span><br><span class="line"><span class="number">2</span>.order模块,首先也注册自己,放入信息,当要调用pay时,先从eureka拿到pay的调用地址</span><br><span class="line"><span class="number">3</span>.通过HttpClient调用</span><br><span class="line"> 	并且还会缓存一份到本地,每<span class="number">30</span>秒更新一次</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8412.png" alt></p>
<p><strong>集群构建原理:</strong></p>
<p>​        互相注册</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8413.png" alt></p>
<h4 id="构建新erueka项目"><a href="#构建新erueka项目" class="headerlink" title="构建新erueka项目"></a><strong>构建新erueka项目</strong></h4><p>名字:cloud_eureka_server_7002</p>
<h5 id="1-pom文件"><a href="#1-pom文件" class="headerlink" title="1,pom文件:"></a>1,pom文件:</h5><p>​        粘贴7001的即可</p>
<h5 id="2-配置文件"><a href="#2-配置文件" class="headerlink" title="2,配置文件:"></a>2,配置文件:</h5><p>​        在写配置文件前,修改一下主机的hosts文件</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8414.png" alt></p>
<p>首先修改之前的7001的eureka项目,因为多个eureka需要互相注册</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8415.png" alt></p>
<p>然后修改7002</p>
<p>​            <strong>7002也是一样的,只不过端口和地址改一下</strong></p>
<h5 id="3-主启动类-1"><a href="#3-主启动类-1" class="headerlink" title="3,主启动类:"></a>3,主启动类:</h5><p>​        复制7001的即可</p>
<h5 id="4-然后启动7001-7002即可"><a href="#4-然后启动7001-7002即可" class="headerlink" title="4,然后启动7001,7002即可"></a>4,然后启动7001,7002即可</h5><p><em><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8416.png" alt></em></p>
<h4 id="将pay-order模块注册到eureka集群中"><a href="#将pay-order模块注册到eureka集群中" class="headerlink" title="将pay,order模块注册到eureka集群中:"></a>将pay,order模块注册到eureka集群中:</h4><h5 id="1-只需要修改配置文件即可"><a href="#1-只需要修改配置文件即可" class="headerlink" title="1,只需要修改配置文件即可:"></a>1,只需要修改配置文件即可:</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8417.png" alt></p>
<h5 id="2-两个模块都修改上面的都一样即可"><a href="#2-两个模块都修改上面的都一样即可" class="headerlink" title="2,两个模块都修改上面的都一样即可"></a>2,两个模块都修改上面的都一样即可</h5><p>​            然后启动两个模块</p>
<p>​            要先启动7001,7002,然后是pay模块8001,然后是order(80)</p>
<h3 id="3-将pay模块也配置为集群模式"><a href="#3-将pay模块也配置为集群模式" class="headerlink" title="3,将pay模块也配置为集群模式:"></a>3,将pay模块也配置为集群模式:</h3><h4 id="0-创建新模块-8002"><a href="#0-创建新模块-8002" class="headerlink" title="0,创建新模块,8002"></a>0,创建新模块,8002</h4><p>​    名称: cloud_pay_8002</p>
<h4 id="1-pom文件-复制8001的"><a href="#1-pom文件-复制8001的" class="headerlink" title="1,pom文件,复制8001的"></a>1,pom文件,复制8001的</h4><h4 id="2-pom文件复制8001的"><a href="#2-pom文件复制8001的" class="headerlink" title="2,pom文件复制8001的"></a>2,pom文件复制8001的</h4><h4 id="3-配置文件复制8001的"><a href="#3-配置文件复制8001的" class="headerlink" title="3,配置文件复制8001的"></a>3,配置文件复制8001的</h4><p>​        端口修改一下,改为8002</p>
<p>​        服务名称不用改,用一样的</p>
<h4 id="4-主启动类-复制8001的"><a href="#4-主启动类-复制8001的" class="headerlink" title="4.主启动类,复制8001的"></a>4.主启动类,复制8001的</h4><h4 id="5-mapper-service-controller都复制一份"><a href="#5-mapper-service-controller都复制一份" class="headerlink" title="5,mapper,service,controller都复制一份"></a>5,mapper,service,controller都复制一份</h4><p>​        然后就启动服务即可</p>
<p>​        此时访问order模块,发现并没有负载均衡到两个pay,模块中,而是只访问8001</p>
<p>​        虽然我们是使用RestTemplate访问的微服务,但是也可以负载均衡的</p>
<p>​        <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8418.png" alt></p>
<p><strong>注意这样还不可以,需要让RestTemplate开启负载均衡注解,还可以指定负载均衡算法,默认轮询</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8419.png" alt></p>
<h3 id="4-修改服务主机名和ip在eureka的web上显示"><a href="#4-修改服务主机名和ip在eureka的web上显示" class="headerlink" title="4,修改服务主机名和ip在eureka的web上显示"></a>4,修改服务主机名和ip在eureka的web上显示</h3><p>比如修改pay模块</p>
<h4 id="1-修改配置文件"><a href="#1-修改配置文件" class="headerlink" title="1,修改配置文件:"></a>1,修改配置文件:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8420.png" alt></p>
<h3 id="5-eureka服务发现"><a href="#5-eureka服务发现" class="headerlink" title="5,eureka服务发现:"></a>5,eureka服务发现:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8421.png" alt></p>
<p>以pay模块为例</p>
<h4 id="1-首先添加一个注解-在controller中"><a href="#1-首先添加一个注解-在controller中" class="headerlink" title="1,首先添加一个注解,在controller中"></a>1,首先添加一个注解,在controller中</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8422.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8423.png" alt></p>
<h4 id="2-在主启动类上添加一个注解"><a href="#2-在主启动类上添加一个注解" class="headerlink" title="2,在主启动类上添加一个注解"></a>2,在主启动类上添加一个注解</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8424.png" alt></p>
<p><strong>然后重启8001.访问/payment/discover</strong>y</p>
<h3 id="6-Eureka自我保护"><a href="#6-Eureka自我保护" class="headerlink" title="6,Eureka自我保护:"></a>6,Eureka自我保护:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8426.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8427.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8425.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8428.png" alt></p>
<p><strong>eureka服务端配置:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8429.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8430.png" alt></p>
<p>​            <strong>设置接受心跳时间间隔</strong></p>
<p><strong>客户端(比如pay模块):</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CEureka%E7%9A%8431.png" alt></p>
<p><strong>此时启动erueka和pay.此时如果直接关闭了pay,那么erueka会直接删除其注册信息</strong></p>
<h2 id="7-Zookeeper服务注册与发现"><a href="#7-Zookeeper服务注册与发现" class="headerlink" title="7,Zookeeper服务注册与发现:"></a>7,Zookeeper服务注册与发现:</h2><h3 id="1-启动zk-到linux上"><a href="#1-启动zk-到linux上" class="headerlink" title="1,启动zk,到linux上"></a>1,启动zk,到linux上</h3><h3 id="2-创建新的pay模块"><a href="#2-创建新的pay模块" class="headerlink" title="2,创建新的pay模块,"></a>2,创建新的pay模块,</h3><p>单独用于注册到zk中  </p>
<p>名字 : cloud_pay_8003</p>
<h4 id="1-pom依赖"><a href="#1-pom依赖" class="headerlink" title="1,pom依赖"></a>1,pom依赖</h4><h4 id="2-配置文件-1"><a href="#2-配置文件-1" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Czookeeper%E7%9A%843.png" alt></p>
<h4 id="3-主启动类-2"><a href="#3-主启动类-2" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Czookeeper%E7%9A%841.png" alt></p>
<h4 id="4-controller"><a href="#4-controller" class="headerlink" title="4,controller"></a>4,controller</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Czookeeper%E7%9A%842.png" alt></p>
<h4 id="5-然后就可以启动"><a href="#5-然后就可以启动" class="headerlink" title="5,然后就可以启动"></a>5,然后就可以启动</h4><p><strong>此时启动,会报错,因为jar包与我们的zk版本不匹配</strong></p>
<p>解决:<br>        修改pom文件,改为与我们zk版本匹配的jar包</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Czookeeper%E7%9A%844.png" alt></p>
<p><strong>此时8003就注册到zk中了</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">我们在zk上注册的node是临时节点,当我们的服务一定时间内没有发送心跳</span><br><span class="line">  	那么zk就会`将这个服务的node删除了</span><br></pre></td></tr></table></figure>



<p><strong>这里测试,就不写service与dao什么的了</strong></p>
<h3 id="3-创建order消费模块注册到zk"><a href="#3-创建order消费模块注册到zk" class="headerlink" title="3,创建order消费模块注册到zk"></a>3,创建order消费模块注册到zk</h3><h4 id="1-创建项目"><a href="#1-创建项目" class="headerlink" title="1,创建项目"></a>1,创建项目</h4><p>名字: cloud_order_zk_80</p>
<h4 id="2-pom"><a href="#2-pom" class="headerlink" title="2,pom"></a>2,pom</h4><h4 id="3-配置文件-1"><a href="#3-配置文件-1" class="headerlink" title="3,配置文件"></a>3,配置文件</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Czookeeper%E7%9A%845.png" alt></p>
<h4 id="4主启动类"><a href="#4主启动类" class="headerlink" title="4主启动类:"></a>4主启动类:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Czookeeper%E7%9A%841.png" alt></p>
<h4 id="5-RestTemolate"><a href="#5-RestTemolate" class="headerlink" title="5,RestTemolate"></a>5,RestTemolate</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Czookeeper%E7%9A%846.png" alt="注意,这里使用RestTemolate,要先注册它"></p>
<h4 id="6-controller"><a href="#6-controller" class="headerlink" title="6,controller"></a>6,controller</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Czookeeper%E7%9A%847.png" alt></p>
<p><strong>然后启动即可注册到zk</strong></p>
<h4 id="8-集群版zk注册"><a href="#8-集群版zk注册" class="headerlink" title="8,集群版zk注册:"></a>8,集群版zk注册:</h4><p>只需要修改配置文件:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Czookeeper%E7%9A%845.png" alt></p>
<p>这个connect-string指定多个zk地址即可</p>
<p>connect-string: 1.2.3.4,2.3.4.5</p>
<h4 id><a href="#" class="headerlink" title></a></h4><h2 id="8-Consul"><a href="#8-Consul" class="headerlink" title="8,Consul:"></a>8,Consul:</h2><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%841.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%842.png" alt></p>
<h3 id="1-按照consul"><a href="#1-按照consul" class="headerlink" title="1,按照consul"></a>1,按照consul</h3><p>需要下载一个安装包</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%843.png" alt></p>
<p>启动是一个命令行界面,需要输入consul agen-dev启动</p>
<h3 id="2-创建新的pay模块-8006"><a href="#2-创建新的pay模块-8006" class="headerlink" title="2,创建新的pay模块,8006"></a>2,创建新的pay模块,8006</h3><h4 id="1-项目名字"><a href="#1-项目名字" class="headerlink" title="1,项目名字"></a>1,项目名字</h4><p>cloud_consule_pay_8006</p>
<h4 id="2-pom依赖-1"><a href="#2-pom依赖-1" class="headerlink" title="2,pom依赖"></a>2,pom依赖</h4><h4 id="3-配置文件-2"><a href="#3-配置文件-2" class="headerlink" title="3,配置文件"></a>3,配置文件</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%844.png" alt></p>
<h4 id="4-主启动类-2"><a href="#4-主启动类-2" class="headerlink" title="4,主启动类"></a>4,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%845.png" alt></p>
<h4 id="5-controller"><a href="#5-controller" class="headerlink" title="5,controller"></a>5,controller</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%846.png" alt></p>
<h4 id="6-启动服务"><a href="#6-启动服务" class="headerlink" title="6,启动服务"></a>6,启动服务</h4><h4 id="-1"><a href="#-1" class="headerlink" title></a></h4><h3 id="3-创建新order模块"><a href="#3-创建新order模块" class="headerlink" title="3,创建新order模块"></a>3,创建新order模块</h3><p>cloud-consul-order-80</p>
<h4 id="1-pom文件-1"><a href="#1-pom文件-1" class="headerlink" title="1,pom文件"></a>1,pom文件</h4><h4 id="2-配置文件-2"><a href="#2-配置文件-2" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%847.png" alt></p>
<h4 id="3-主启动类-3"><a href="#3-主启动类-3" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%845.png" alt></p>
<h4 id="4-RestTemplate注册"><a href="#4-RestTemplate注册" class="headerlink" title="4,RestTemplate注册"></a>4,RestTemplate注册</h4><p>配置类注册</p>
<h4 id="5-controller-1"><a href="#5-controller-1" class="headerlink" title="5,controller"></a>5,controller</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%848.png" alt></p>
<h4 id="6-启动服务-测试"><a href="#6-启动服务-测试" class="headerlink" title="6,启动服务,测试"></a>6,启动服务,测试</h4><h2 id="9-三个注册中心的异同"><a href="#9-三个注册中心的异同" class="headerlink" title="9,三个注册中心的异同:"></a>9,三个注册中心的异同:</h2><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%849.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%8410.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cconsul%E7%9A%8411.png" alt></p>
<h1 id="3-服务调用"><a href="#3-服务调用" class="headerlink" title="3,服务调用"></a>3,服务调用</h1><h2 id="10-Ribbon负载均衡"><a href="#10-Ribbon负载均衡" class="headerlink" title="10,Ribbon负载均衡:"></a>10,Ribbon负载均衡:</h2><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon.png" alt></p>
<p><strong>Ribbon目前也进入维护,基本上不准备更新了</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%842.png" alt></p>
<p><strong>进程内LB(本地负载均衡)</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%845.png" alt></p>
<p><strong>集中式LB(服务端负载均衡)</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%844.png" alt></p>
<p><strong>区别</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%843.png" alt></p>
<p><strong>Ribbon就是负载均衡+RestTemplate</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%846.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%847.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%848.png" alt></p>
<h3 id="使用Ribbon"><a href="#使用Ribbon" class="headerlink" title="使用Ribbon:"></a>使用Ribbon:</h3><h4 id="1-默认我们使用eureka的新版本时-它默认集成了ribbon"><a href="#1-默认我们使用eureka的新版本时-它默认集成了ribbon" class="headerlink" title="1,默认我们使用eureka的新版本时,它默认集成了ribbon:"></a>1,默认我们使用eureka的新版本时,它默认集成了ribbon:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%849.png" alt></p>
<p><strong>==这个starter中集成了reibbon了==</strong></p>
<h4 id="2-我们也可以手动引入ribbon"><a href="#2-我们也可以手动引入ribbon" class="headerlink" title="2,我们也可以手动引入ribbon"></a>2,我们也可以手动引入ribbon</h4><p><strong>放到order模块中,因为只有order访问pay时需要负载均衡</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8410.png" alt></p>
<h4 id="3-RestTemplate类"><a href="#3-RestTemplate类" class="headerlink" title="3,RestTemplate类:"></a>3,RestTemplate类:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8411.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8412.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">RestTemplate的:</span><br><span class="line">		xxxForObject()方法,返回的是响应体中的数据</span><br><span class="line">    xxxForEntity()方法.返回的是entity对象,这个对象不仅仅包含响应体数据,还包含响应体信息(状态码等)</span><br></pre></td></tr></table></figure>





<h4 id="Ribbon常用负载均衡算法"><a href="#Ribbon常用负载均衡算法" class="headerlink" title="Ribbon常用负载均衡算法:"></a>Ribbon常用负载均衡算法:</h4><p><strong>IRule接口,Riboon使用该接口,根据特定算法从所有服务中,选择一个服务,</strong></p>
<p><strong>Rule接口有7个实现类,每个实现类代表一个负载均衡算法</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8414.png" alt></p>
<h4 id="使用Ribbon-1"><a href="#使用Ribbon-1" class="headerlink" title="使用Ribbon:"></a>使用Ribbon:</h4><p><strong>==这里使用eureka的那一套服务==</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8415.png" alt></p>
<p><strong>==也就是不能放在主启动类所在的包及子包下==</strong></p>
<h5 id="1-修改order模块"><a href="#1-修改order模块" class="headerlink" title="1,修改order模块"></a>1,修改order模块</h5><h5 id="2-额外创建一个包"><a href="#2-额外创建一个包" class="headerlink" title="2,额外创建一个包"></a>2,额外创建一个包</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8416.png" alt></p>
<h5 id="3-创建配置类-指定负载均衡算法"><a href="#3-创建配置类-指定负载均衡算法" class="headerlink" title="3,创建配置类,指定负载均衡算法"></a>3,创建配置类,指定负载均衡算法</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8417.png" alt></p>
<h5 id="4-在主启动类上加一个注解"><a href="#4-在主启动类上加一个注解" class="headerlink" title="4,在主启动类上加一个注解"></a>4,在主启动类上加一个注解</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8418.png" alt></p>
<p><strong>表示,访问CLOUD_pAYMENT_SERVICE的服务时,使用我们自定义的负载均衡算法</strong></p>
<h4 id="自定义负载均衡算法"><a href="#自定义负载均衡算法" class="headerlink" title="自定义负载均衡算法:"></a>自定义负载均衡算法:</h4><h5 id="1-ribbon的轮询算法原理"><a href="#1-ribbon的轮询算法原理" class="headerlink" title="1,ribbon的轮询算法原理"></a>1,ribbon的轮询算法原理</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8419.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8421.png" alt></p>
<h5 id="2-自定义负载均衡算法"><a href="#2-自定义负载均衡算法" class="headerlink" title="2,自定义负载均衡算法:"></a>2,自定义负载均衡算法:</h5><p><strong>1,给</strong>pay模块(8001,8002),的controller方法添加一个方法,返回当前节点端口</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8423.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8422.png" alt></p>
<p><strong>2,修改order模块</strong></p>
<p>去掉@LoadBalanced</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8424.png" alt></p>
<h5 id="3-自定义接口"><a href="#3-自定义接口" class="headerlink" title="3,自定义接口"></a>3,自定义接口</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8429.png" alt></p>
<p>​                    ==具体的算法在实现类中实现==</p>
<h5 id="4-接口实现类"><a href="#4-接口实现类" class="headerlink" title="4,接口实现类"></a>4,接口实现类</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8425.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8426.png" alt></p>
<h5 id="5-修改controller"><a href="#5-修改controller" class="headerlink" title="5,修改controller:"></a>5,修改controller:</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8427.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CRibbon%E7%9A%8428.png" alt></p>
<h5 id="6-启动服务-测试即可"><a href="#6-启动服务-测试即可" class="headerlink" title="6,启动服务,测试即可"></a>6,启动服务,测试即可</h5><h2 id="11-OpenFeign"><a href="#11-OpenFeign" class="headerlink" title="11,OpenFeign"></a>11,OpenFeign</h2><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%841.png" alt></p>
<p><strong>是一个声明式的web客户端,只需要创建一个接口,添加注解即可完成微服务之间的调用</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%842.png" alt></p>
<p>==就是A要调用B,Feign就是在A中创建一个一模一样的B对外提供服务的的接口,我们调用这个接口,就可以服务到B==</p>
<h3 id="Feign与OpenFeign区别"><a href="#Feign与OpenFeign区别" class="headerlink" title="Feign与OpenFeign区别"></a><strong>Feign与OpenFeign区别</strong></h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%843.png" alt></p>
<h3 id="使用OpenFeign"><a href="#使用OpenFeign" class="headerlink" title="使用OpenFeign"></a>使用OpenFeign</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">之前的服务间调用,我们使用的是ribbon+RestTemplate</span><br><span class="line">		现在改为使用Feign</span><br></pre></td></tr></table></figure>

<h4 id="1-新建一个order项目-用于feign测试"><a href="#1-新建一个order项目-用于feign测试" class="headerlink" title="1,新建一个order项目,用于feign测试"></a>1,新建一个order项目,用于feign测试</h4><p>名字cloud_order_feign-80</p>
<h4 id="2-pom文件"><a href="#2-pom文件" class="headerlink" title="2,pom文件"></a>2,pom文件</h4><h4 id="3-配置文件-3"><a href="#3-配置文件-3" class="headerlink" title="3,配置文件"></a>3,配置文件</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%844.png" alt></p>
<h4 id="4-主启动类-3"><a href="#4-主启动类-3" class="headerlink" title="4,主启动类"></a>4,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%845.png" alt></p>
<h4 id="5-fegin需要调用的其他的服务的接口"><a href="#5-fegin需要调用的其他的服务的接口" class="headerlink" title="5,fegin需要调用的其他的服务的接口"></a>5,fegin需要调用的其他的服务的接口</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%846.png" alt></p>
<h4 id="6-controller-1"><a href="#6-controller-1" class="headerlink" title="6,controller"></a>6,controller</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%847.png" alt></p>
<h4 id="7测试"><a href="#7测试" class="headerlink" title="7测试:"></a>7测试:</h4><p>启动两个erueka(7001,7002)</p>
<p>启动两个pay(8001,8002)</p>
<p>启动当前的order模块</p>
<p><strong>Feign默认使用ribbon实现负载均衡</strong></p>
<h3 id="OpenFeign超时机制"><a href="#OpenFeign超时机制" class="headerlink" title="OpenFeign超时机制:"></a>OpenFeign超时机制:</h3><p>==OpenFeign默认等待时间是1秒,超过1秒,直接报错==</p>
<h4 id="1-设置超时时间-修改配置文件"><a href="#1-设置超时时间-修改配置文件" class="headerlink" title="1,设置超时时间,修改配置文件:"></a>1,设置超时时间,修改配置文件:</h4><p><strong>因为OpenFeign的底层是ribbon进行负载均衡,所以它的超时时间是由ribbon控制</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%848.png" alt></p>
<h3 id="OpenFeign日志"><a href="#OpenFeign日志" class="headerlink" title="OpenFeign日志:"></a>OpenFeign日志:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%849.png" alt></p>
<p><strong>OpenFeign的日志级别有:</strong><br><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%8410.png" alt></p>
<h4 id="1-使用OpenFeign的日志"><a href="#1-使用OpenFeign的日志" class="headerlink" title="1,使用OpenFeign的日志:"></a>1,使用OpenFeign的日志:</h4><p><strong>实现在配置类中添加OpenFeign的日志类</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%8411.png" alt></p>
<h4 id="2-为指定类设置日志级别"><a href="#2-为指定类设置日志级别" class="headerlink" title="2,为指定类设置日志级别:"></a>2,为指定类设置日志级别:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%8413.png" alt></p>
<p><strong>配置文件中:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CFeign%E7%9A%8412.png" alt></p>
<h4 id="3-启动服务即可"><a href="#3-启动服务即可" class="headerlink" title="3,启动服务即可"></a>3,启动服务即可</h4><h1 id="4-服务降级"><a href="#4-服务降级" class="headerlink" title="4,服务降级:"></a>4,服务降级:</h1><h2 id="12-Hystrix服务降级"><a href="#12-Hystrix服务降级" class="headerlink" title="12,Hystrix服务降级"></a>12,Hystrix服务降级</h2><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%842.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%843.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%844.png" alt></p>
<h3 id="hystrix中的重要概念"><a href="#hystrix中的重要概念" class="headerlink" title="hystrix中的重要概念:"></a>hystrix中的重要概念:</h3><h4 id="1-服务降级"><a href="#1-服务降级" class="headerlink" title="1,服务降级"></a>1,服务降级</h4><p><strong>比如当某个服务繁忙,不能让客户端的请求一直等待,应该立刻返回给客户端一个备选方案</strong></p>
<h4 id="2-服务熔断"><a href="#2-服务熔断" class="headerlink" title="2,服务熔断"></a>2,服务熔断</h4><p><strong>当某个服务出现问题,卡死了,不能让用户一直等待,需要关闭所有对此服务的访问</strong></p>
<p>​            <strong>然后调用服务降级</strong></p>
<h4 id="3-服务限流"><a href="#3-服务限流" class="headerlink" title="3,服务限流"></a>3,服务限流</h4><p><strong>限流,比如秒杀场景,不能访问用户瞬间都访问服务器,限制一次只可以有多少请求</strong></p>
<h3 id="使用hystrix-服务降级"><a href="#使用hystrix-服务降级" class="headerlink" title="使用hystrix,服务降级:"></a>使用hystrix,服务降级:</h3><h4 id="1-创建带降级机制的pay模块"><a href="#1-创建带降级机制的pay模块" class="headerlink" title="1,创建带降级机制的pay模块 :"></a>1,创建带降级机制的pay模块 :</h4><p>名字: cloud-hystrix-pay-8007</p>
<h5 id="2-pom文件-1"><a href="#2-pom文件-1" class="headerlink" title="2,pom文件"></a>2,pom文件</h5><h5 id="3-配置文件-4"><a href="#3-配置文件-4" class="headerlink" title="3,配置文件"></a>3,配置文件</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%845.png" alt></p>
<h5 id="4-主启动类-4"><a href="#4-主启动类-4" class="headerlink" title="4,主启动类"></a>4,主启动类</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%848.png" alt></p>
<h5 id="5-service"><a href="#5-service" class="headerlink" title="5,service"></a>5,service</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%846.png" alt></p>
<h5 id="6controller"><a href="#6controller" class="headerlink" title="6controller"></a>6controller</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%847.png" alt></p>
<h5 id="7-先测试"><a href="#7-先测试" class="headerlink" title="7,先测试:"></a>7,先测试:</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">此时使用压测工具,并发<span class="number">20000</span>个请求,请求会延迟的那个方法,</span><br><span class="line">		压测中,发现,另外一个方法并没有被压测,但是我们访问它时,却需要等待</span><br><span class="line">		这就是因为被压测的方法它占用了服务器大部分资源,导致其他请求也变慢了</span><br></pre></td></tr></table></figure>



<h5 id="8-先不加入hystrix"><a href="#8-先不加入hystrix" class="headerlink" title="8,先不加入hystrix,"></a>8,先不加入hystrix,</h5><h4 id="2-创建带降级的order模块"><a href="#2-创建带降级的order模块" class="headerlink" title="2,创建带降级的order模块:"></a>2,创建带降级的order模块:</h4><h5 id="1-名字-cloud-hystrix-order-80"><a href="#1-名字-cloud-hystrix-order-80" class="headerlink" title="1,名字:  cloud-hystrix-order-80"></a>1,名字:  cloud-hystrix-order-80</h5><h5 id="2-pom-1"><a href="#2-pom-1" class="headerlink" title="2,pom"></a>2,pom</h5><h5 id="3-配置文件-5"><a href="#3-配置文件-5" class="headerlink" title="3,配置文件"></a>3,配置文件</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%849.png" alt></p>
<h5 id="4-主启动类-5"><a href="#4-主启动类-5" class="headerlink" title="4,主启动类"></a>4,主启动类</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8411.png" alt></p>
<h5 id="5-远程调用pay模块的接口"><a href="#5-远程调用pay模块的接口" class="headerlink" title="5,远程调用pay模块的接口:"></a>5,远程调用pay模块的接口:</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8412.png" alt></p>
<h5 id="6-controller-2"><a href="#6-controller-2" class="headerlink" title="6,controller:"></a>6,controller:</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8413.png" alt></p>
<h5 id="7-测试"><a href="#7-测试" class="headerlink" title="7,测试"></a>7,测试</h5><p>​            启动order模块,访问pay</p>
<p>​            再次压测2万并发,发现order访问也变慢了</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8414.png" alt></p>
<p><strong>解决:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8415.png" alt></p>
<h5 id="-2"><a href="#-2" class="headerlink" title></a><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8416.png" alt></h5><h4 id="3-配置服务降级"><a href="#3-配置服务降级" class="headerlink" title="3,配置服务降级:"></a>3,配置服务降级:</h4><h5 id="1-修改pay模块"><a href="#1-修改pay模块" class="headerlink" title="1,修改pay模块"></a>1,修改pay模块</h5><h6 id="1-为service的指定方法-会延迟的方法-添加-HystrixCommand注解"><a href="#1-为service的指定方法-会延迟的方法-添加-HystrixCommand注解" class="headerlink" title="1,为service的指定方法(会延迟的方法)添加@HystrixCommand注解"></a>1,为service的指定方法(会延迟的方法)添加@HystrixCommand注解</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8417.png" alt></p>
<h6 id="2-主启动类上-添加激活hystrix的注解"><a href="#2-主启动类上-添加激活hystrix的注解" class="headerlink" title="2,主启动类上,添加激活hystrix的注解"></a>2,主启动类上,添加激活hystrix的注解</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8418.png" alt></p>
<h6 id="3-触发异常"><a href="#3-触发异常" class="headerlink" title="3,触发异常"></a>3,触发异常</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8419.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8420.png" alt><strong>可以看到,也触发了降级</strong></p>
<h5 id="2-修改order模块-进行服务降级"><a href="#2-修改order模块-进行服务降级" class="headerlink" title="2,修改order模块,进行服务降级"></a>2,修改order模块,进行服务降级</h5><p>一般服务降级,都是放在客户端(order模块),</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8421.png" alt></p>
<h6 id="1-修改配置文件-1"><a href="#1-修改配置文件-1" class="headerlink" title="1,修改配置文件:"></a>1,修改配置文件:</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8422.png" alt></p>
<h6 id="2-主启动类添加直接-启用hystrix"><a href="#2-主启动类添加直接-启用hystrix" class="headerlink" title="2,主启动类添加直接,启用hystrix:"></a><strong>2,主启动类添加直接,启用hystrix:</strong></h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8423.png" alt></p>
<p>​    </p>
<h6 id="3-修改controller-添加降级方法什么的"><a href="#3-修改controller-添加降级方法什么的" class="headerlink" title="3,修改controller,添加降级方法什么的"></a>3,修改controller,添加降级方法什么的</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8424.png" alt></p>
<h6 id="4-测试"><a href="#4-测试" class="headerlink" title="4,测试"></a>4,测试</h6><p>启动pay模块,order模块,</p>
<p><strong>注意:,这里pay模块和order模块都开启了服务降级</strong></p>
<p>​            但是order这里,设置了1.5秒就降级,所以访问时,一定会降级</p>
<h5 id="4-重构"><a href="#4-重构" class="headerlink" title="4,重构:"></a>4,重构:</h5><p><strong>上面出现的问题:</strong><br>        1,降级方法与业务方法写在了一块,耦合度高</p>
<p>​        2.每个业务方法都写了一个降级方法,重复代码多</p>
<h5 id="解决重复代码的问题"><a href="#解决重复代码的问题" class="headerlink" title="解决重复代码的问题:"></a><strong>解决重复代码的问题</strong>:</h5><p><strong>配置一个全局的降级方法,所有方法都可以走这个降级方法,至于某些特殊创建,再单独创建方法</strong></p>
<h6 id="1-创建一个全局方法"><a href="#1-创建一个全局方法" class="headerlink" title="1,创建一个全局方法"></a>1,创建一个全局方法</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8426.png" alt></p>
<h6 id="2-使用注解指定其为全局降级方法-默认降级方法"><a href="#2-使用注解指定其为全局降级方法-默认降级方法" class="headerlink" title="2,使用注解指定其为全局降级方法(默认降级方法)"></a>2,使用注解指定其为全局降级方法(默认降级方法)</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8427.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8425.png" alt></p>
<h6 id="3-业务方法使用默认降级方法"><a href="#3-业务方法使用默认降级方法" class="headerlink" title="3,业务方法使用默认降级方法:"></a>3,业务方法使用默认降级方法:</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8428.png" alt></p>
<h6 id="4-测试-1"><a href="#4-测试-1" class="headerlink" title="4,测试:"></a>4,测试:</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8429.png" alt></p>
<h5 id="解决代码耦合度的问题"><a href="#解决代码耦合度的问题" class="headerlink" title="解决代码耦合度的问题:"></a>解决代码耦合度的问题:</h5><p>修改order模块,这里开始,pay模块就不服务降级了,服务降级写在order模块即可</p>
<h6 id="1-Payservice接口是远程调用pay模块的-我们这里创建一个类实现service接口-在实现类中统一处理异常"><a href="#1-Payservice接口是远程调用pay模块的-我们这里创建一个类实现service接口-在实现类中统一处理异常" class="headerlink" title="1,Payservice接口是远程调用pay模块的,我们这里创建一个类实现service接口,在实现类中统一处理异常"></a>1,Payservice接口是远程调用pay模块的,我们这里创建一个类实现service接口,在实现类中统一处理异常</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8430.png" alt></p>
<h6 id="2-修改配置文件-添加"><a href="#2-修改配置文件-添加" class="headerlink" title="2,修改配置文件:添加:"></a>2,修改配置文件:添加:</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8431.png" alt></p>
<h6 id="3-让PayService的实现类生效"><a href="#3-让PayService的实现类生效" class="headerlink" title="3,让PayService的实现类生效:"></a>3,让PayService的实现类生效:</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8432.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">它的运行逻辑是:</span><br><span class="line">		当请求过来,首先还是通过Feign远程调用pay模块对应的方法</span><br><span class="line">    但是如果pay模块报错,调用失败,那么就会调用PayMentFalbackService类的</span><br><span class="line">    当前同名的方法,作为降级方法</span><br></pre></td></tr></table></figure>

<h6 id="4-启动测试"><a href="#4-启动测试" class="headerlink" title="4,启动测试"></a>4,启动测试</h6><p>启动order和pay正常访问–ok</p>
<p>==此时将pay服务关闭,order再次访问==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8433.png" alt></p>
<p>可以看到,并没有报500错误,而是降级访问==实现类==的同名方法</p>
<p>这样,即使服务器挂了,用户要不要一直等待,或者报错</p>
<p>问题:</p>
<p>​        <strong>这样虽然解决了代码耦合度问题,但是又出现了过多重复代码的问题,每个方法都有一个降级方法</strong></p>
<h3 id="使用服务熔断"><a href="#使用服务熔断" class="headerlink" title="使用服务熔断:"></a>使用服务熔断:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8434.png" alt></p>
<p><strong>比如并发达到1000,我们就拒绝其他用户访问,在有用户访问,就访问降级方法</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8435.png" alt></p>
<h4 id="1-修改前面的pay模块"><a href="#1-修改前面的pay模块" class="headerlink" title="1,修改前面的pay模块"></a>1,修改前面的pay模块</h4><h5 id="1-修改Payservice接口-添加服务熔断相关的方法"><a href="#1-修改Payservice接口-添加服务熔断相关的方法" class="headerlink" title="1,修改Payservice接口,添加服务熔断相关的方法:"></a><strong>1,修改Payservice接口,添加服务熔断相关的方法:</strong></h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8437.png" alt></p>
<p>这里属性整体意思是:<br>            10秒之内(窗口,会移动),如果并发==超过==10个,或者10个并发中,失败了6个,就开启熔断器</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8443.png" alt="image-20200414152637247"></p>
<p>IdUtil是Hutool包下的类,这个Hutool就是整合了所有的常用方法,比如UUID,反射,IO流等工具方法什么的都整合了</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8436.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">断路器的打开和关闭,是按照一下<span class="number">5</span>步决定的</span><br><span class="line">  	<span class="number">1</span>,并发此时是否达到我们指定的阈值</span><br><span class="line">  	<span class="number">2</span>,错误百分比,比如我们配置了<span class="number">60</span>%,那么如果并发请求中,<span class="number">10</span>次有<span class="number">6</span>次是失败的,就开启断路器</span><br><span class="line">  	<span class="number">3</span>,上面的条件符合,断路器改变状态为open(开启)</span><br><span class="line">  	<span class="number">4</span>,这个服务的断路器开启,所有请求无法访问</span><br><span class="line">  	<span class="number">5</span>,在我们的时间窗口期,期间,尝试让一些请求通过(半开状态),如果请求还是失败,证明断路器还是开启状态,服务没有恢复</span><br><span class="line">  		如果请求成功了,证明服务已经恢复,断路器状态变为close关闭状态</span><br></pre></td></tr></table></figure>



<h5 id="2-修改controller"><a href="#2-修改controller" class="headerlink" title="2,修改controller"></a>2,修改controller</h5><p>添加一个测试方法;</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8439.png" alt></p>
<h5 id="3-测试"><a href="#3-测试" class="headerlink" title="3,测试:"></a>3,测试:</h5><p>启动pay,order模块</p>
<p>==多次访问,并且错误率超过60%:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8440.png" alt></p>
<p>此时服务熔断,此时即使访问正确的也会报错:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8441.png" alt></p>
<p><strong>但是,当过了几秒后,又恢复了</strong></p>
<p>​                因为在10秒窗口期内,它自己会尝试接收部分请求,发现服务可以正常调用,慢慢的当错误率低于60%,取消熔断</p>
<h3 id="Hystrix所有可配置的属性"><a href="#Hystrix所有可配置的属性" class="headerlink" title="Hystrix所有可配置的属性:"></a>Hystrix所有可配置的属性:</h3><p><strong>全部在这个方法中记录,以成员变量的形式记录,</strong></p>
<p>​        以后需要什么属性,查看这个类即可</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8438.png" alt></p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结:"></a>总结:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8442.png" alt></p>
<p><strong>==当断路器开启后:==</strong></p>
<p>​    <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8444.png" alt></p>
<p><strong>==其他参数:==</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8445.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8446.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8447.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8448.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8449.png" alt></p>
<p><strong>熔断整体流程:</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span>请求进来,首先查询缓存,如果缓存有,直接返回</span><br><span class="line">  	如果缓存没有,---&gt;<span class="number">2</span></span><br><span class="line"><span class="number">2</span>,查看断路器是否开启,如果开启的,Hystrix直接将请求转发到降级返回,然后返回</span><br><span class="line">  	如果断路器是关闭的,</span><br><span class="line">				判断线程池等资源是否已经满了,如果已经满了</span><br><span class="line">  					也会走降级方法</span><br><span class="line">  			如果资源没有满,判断我们使用的什么类型的Hystrix,决定调用构造方法还是run方法</span><br><span class="line">        然后处理请求</span><br><span class="line">        然后Hystrix将本次请求的结果信息汇报给断路器,因为断路器此时可能是开启的</span><br><span class="line">          			(因为断路器开启也是可以接收请求的)</span><br><span class="line">        		断路器收到信息,判断是否符合开启或关闭断路器的条件,</span><br><span class="line">				如果本次请求处理失败,又会进入降级方法</span><br><span class="line">        如果处理成功,判断处理是否超时,如果超时了,也进入降级方法</span><br><span class="line">        最后,没有超时,则本次请求处理成功,将结果返回给controller</span><br></pre></td></tr></table></figure>







<h3 id="Hystrix服务监控"><a href="#Hystrix服务监控" class="headerlink" title="Hystrix服务监控:"></a>Hystrix服务监控:</h3><h4 id="HystrixDashboard"><a href="#HystrixDashboard" class="headerlink" title="HystrixDashboard"></a>HystrixDashboard</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8451.png" alt></p>
<h4 id="2-使用HystrixDashboard"><a href="#2-使用HystrixDashboard" class="headerlink" title="2,使用HystrixDashboard:"></a>2,使用HystrixDashboard:</h4><h5 id="1-创建项目-1"><a href="#1-创建项目-1" class="headerlink" title="1,创建项目:"></a>1,创建项目:</h5><p>名字: cloud_hystrixdashboard_9001</p>
<h5 id="2-pom文件-2"><a href="#2-pom文件-2" class="headerlink" title="2,pom文件"></a>2,pom文件</h5><h5 id="3-配置文件-6"><a href="#3-配置文件-6" class="headerlink" title="3,配置文件"></a>3,配置文件</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8452.png" alt></p>
<h5 id="4-主启动类-6"><a href="#4-主启动类-6" class="headerlink" title="4,主启动类"></a>4,主启动类</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8453.png" alt></p>
<h5 id="5-修改所有pay模块-8001-8002-8003…"><a href="#5-修改所有pay模块-8001-8002-8003…" class="headerlink" title="5,修改所有pay模块(8001,8002,8003…)"></a>5,修改所有pay模块(8001,8002,8003…)</h5><p><strong>他们都添加一个pom依赖:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8454.png" alt></p>
<p>之前的pom文件中都添加过了,==这个是springboot的监控组件==</p>
<h5 id="6-启动9001即可"><a href="#6-启动9001即可" class="headerlink" title="6,启动9001即可"></a>6,启动9001即可</h5><p>​            访问: <strong>localhost:9001/hystrix</strong></p>
<h5 id="7-注意-此时仅仅是可以访问HystrixDashboard-并不代表已经监控了8001-8002"><a href="#7-注意-此时仅仅是可以访问HystrixDashboard-并不代表已经监控了8001-8002" class="headerlink" title="7,注意,此时仅仅是可以访问HystrixDashboard,并不代表已经监控了8001,8002"></a>7,注意,此时仅仅是可以访问HystrixDashboard,并不代表已经监控了8001,8002</h5><p>​                            如果要监控,还需要配置:(8001为例)</p>
<p>==8001的主启动类添加:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8455.png" alt></p>
<p><strong>其他8002,8003都是一样的</strong></p>
<h5 id="8-到此-可以启动服务"><a href="#8-到此-可以启动服务" class="headerlink" title="8,到此,可以启动服务"></a>8,到此,可以启动服务</h5><p>启动7001,8001,9001</p>
<p><strong>然后在web界面,指定9001要监控8001:</strong></p>
<h5 id="-3"><a href="#-3" class="headerlink" title></a><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8456.png" alt></h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8457.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8459.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8458.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8460.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8461.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CHystrix%E7%9A%8462.png" alt></p>
<h1 id="5-服务网关"><a href="#5-服务网关" class="headerlink" title="5,服务网关:"></a>5,服务网关:</h1><p>zuul停更了,</p>
<h2 id="13-GateWay"><a href="#13-GateWay" class="headerlink" title="13,GateWay"></a>13,GateWay</h2><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%841.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%842.png" alt></p>
<p><strong>gateway之所以性能号,因为底层使用WebFlux,而webFlux底层使用netty通信(NIO)</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%843.png" alt></p>
<h3 id="GateWay的特性"><a href="#GateWay的特性" class="headerlink" title="GateWay的特性:"></a>GateWay的特性:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%844.png" alt></p>
<h3 id="GateWay与zuul的区别"><a href="#GateWay与zuul的区别" class="headerlink" title="GateWay与zuul的区别:"></a>GateWay与zuul的区别:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%845.png" alt></p>
<h3 id="zuul1-x的模型"><a href="#zuul1-x的模型" class="headerlink" title="zuul1.x的模型:"></a>zuul1.x的模型:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%846.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%847.png" alt></p>
<h3 id="什么是webflux"><a href="#什么是webflux" class="headerlink" title="什么是webflux:"></a>什么是webflux:</h3><p><strong>是一个非阻塞的web框架,类似springmvc这样的</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%848.png" alt></p>
<h3 id="GateWay的一些概念"><a href="#GateWay的一些概念" class="headerlink" title="GateWay的一些概念:"></a>GateWay的一些概念:</h3><h4 id="1-路由"><a href="#1-路由" class="headerlink" title="1,路由:"></a>1,路由:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%849.png" alt></p>
<p>就是根据某些规则,将请求发送到指定服务上</p>
<h4 id="2-断言"><a href="#2-断言" class="headerlink" title="2,断言:"></a>2,断言:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8410.png" alt></p>
<p>就是判断,如果符合条件就是xxxx,反之yyyy</p>
<h4 id="3-过滤"><a href="#3-过滤" class="headerlink" title="3,过滤:"></a>3,过滤:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8411.png" alt></p>
<p>​    <strong>路由前后,过滤请求</strong></p>
<h3 id="GateWay的工作原理"><a href="#GateWay的工作原理" class="headerlink" title="GateWay的工作原理:"></a>GateWay的工作原理:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8412.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8413.png" alt></p>
<h3 id="使用GateWay"><a href="#使用GateWay" class="headerlink" title="使用GateWay:"></a>使用GateWay:</h3><p>想要新建一个GateWay的项目</p>
<p>名字:     cloud_gateway_9527</p>
<h4 id="1-pom-1"><a href="#1-pom-1" class="headerlink" title="1,pom"></a>1,pom</h4><h4 id="2-配置文件-3"><a href="#2-配置文件-3" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8414.png" alt></p>
<h4 id="3-主启动类-4"><a href="#3-主启动类-4" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8415.png" alt></p>
<h4 id="4-针对pay模块-设置路由"><a href="#4-针对pay模块-设置路由" class="headerlink" title="4,针对pay模块,设置路由:"></a>4,针对pay模块,设置路由:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8416.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8418.png" alt></p>
<p><strong>==修改GateWay模块(9527)的配置文件==:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8417.png" alt></p>
<p>这里表示,</p>
<p>​            当访问localhost:9527/payment/get/1时,     </p>
<p>​            路由到localhost:8001/payment/get/1</p>
<h4 id="5-开始测试"><a href="#5-开始测试" class="headerlink" title="5,开始测试"></a>5,开始测试</h4><p><strong>启动7001,8001,9527</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">如果启动GateWay报错</span><br><span class="line">  	可能是GateWay模块引入了web和监控的starter依赖,需要移除</span><br></pre></td></tr></table></figure>

<p>访问:</p>
<p>​        localhost:9527/payment/get/1</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8419.png" alt></p>
<h4 id="6-GateWay的网关配置"><a href="#6-GateWay的网关配置" class="headerlink" title="6,GateWay的网关配置,"></a>6,GateWay的网关配置,</h4><p>​        <strong>GateWay的网关配置,除了支持配置文件,还支持硬编码方式</strong></p>
<h4 id="7使用硬编码配置GateWay"><a href="#7使用硬编码配置GateWay" class="headerlink" title="7使用硬编码配置GateWay:"></a>7使用硬编码配置GateWay:</h4><h5 id="创建配置类"><a href="#创建配置类" class="headerlink" title="创建配置类:"></a>创建配置类:</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8420.png" alt></p>
<h4 id="8-然后重启服务即可"><a href="#8-然后重启服务即可" class="headerlink" title="8,然后重启服务即可"></a>8,然后重启服务即可</h4><h3 id="重构"><a href="#重构" class="headerlink" title="重构:"></a>重构:</h3><p>上面的配置虽然首先了网关,但是是在配置文件中写死了要路由的地址</p>
<p>现在需要修改,不指定地址,而是根据微服务名字进行路由,我们可以在注册中心获取某组微服务的地址</p>
<p>需要:</p>
<p>​        1个eureka,2个pay模块</p>
<h4 id="修改GateWay模块的配置文件"><a href="#修改GateWay模块的配置文件" class="headerlink" title="修改GateWay模块的配置文件:"></a>修改GateWay模块的配置文件:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8421.png" alt></p>
<h4 id="然后就可以启动微服务-测试"><a href="#然后就可以启动微服务-测试" class="headerlink" title="然后就可以启动微服务.测试"></a>然后就可以启动微服务.测试</h4><h3 id="Pridicate断言"><a href="#Pridicate断言" class="headerlink" title="Pridicate断言:"></a>Pridicate断言:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8424.png" alt></p>
<p><strong>我们之前在配置文件中配置了断言:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8422.png" alt></p>
<p><strong>这个断言表示,如果外部访问路径是指定路径,就路由到指定微服务上</strong></p>
<p>可以看到,这里有一个Path,这个是断言的一种,==断言的类型==:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8423.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">After:</span><br><span class="line">		可以指定,只有在指定时间后,才可以路由到指定微服务</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8426.png" alt></p>
<p>​                这里表示,只有在==2020年的2月21的15点51分37秒==之后,访问==才可以路由==</p>
<p>​                在此之前的访问,都会报404</p>
<p>如何获取当前时区?**</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8425.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">before:</span><br><span class="line">		与after类似,他说在指定时间之前的才可以访问</span><br><span class="line">between:</span><br><span class="line">		需要指定两个时间,在他们之间的时间才可以访问</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8427.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">cookie:</span><br><span class="line">		只有包含某些指定cookie(key,value),的请求才可以路由</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8428.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8429.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Header:</span><br><span class="line">		只有包含指定请求头的请求,才可以路由</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8431.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8432.png" alt></p>
<p>测试:<br><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8433.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">host:</span><br><span class="line">		只有指定主机的才可以访问,</span><br><span class="line">		比如我们当前的网站的域名是www.aa.com</span><br><span class="line">    那么这里就可以设置,只有用户是www.aa.com的请求,才进行路由</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8434.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8435.png" alt="gateway的34"></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8436.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8437.png" alt></p>
<p>可以看到,如果带了域名访问,就可以,但是直接访问ip地址.就报错了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">method:</span><br><span class="line">		只有指定请求才可以路由,比如get请求...</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8438.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">path:</span><br><span class="line">		只有访问指定路径,才进行路由</span><br><span class="line">     比如访问,/abc才路由</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8439.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Query:</span><br><span class="line">		必须带有请求参数才可以访问</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8440.png" alt></p>
<h3 id="Filter过滤器"><a href="#Filter过滤器" class="headerlink" title="Filter过滤器:"></a>Filter过滤器:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8441.png" alt></p>
<h4 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期:"></a>生命周期:</h4><p><strong>在请求进入路由之前,和处理请求完成,再次到达路由之前</strong></p>
<h4 id="种类"><a href="#种类" class="headerlink" title="种类:"></a>种类:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8442.png" alt></p>
<p>GateWayFilter,单一的过滤器</p>
<p><strong>与断言类似,比如闲置,请求头,只有特定的请求头才放行,反之就过滤</strong>:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8443.png" alt></p>
<p>GlobalFilter,全局过滤器:</p>
<h4 id="自定义过滤器"><a href="#自定义过滤器" class="headerlink" title="自定义过滤器:"></a><strong>自定义过滤器:</strong></h4><p>实现两个接口</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8444.png" alt></p>
<p>​    <strong>然后启动服务,即可,因为过滤器通过@COmponet已经加入到容器了</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8446.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cgateway%E7%9A%8445.png" alt></p>
<h1 id="6-服务配置"><a href="#6-服务配置" class="headerlink" title="6,服务配置:"></a>6,服务配置:</h1><h2 id="Spring-Config分布式配置中心"><a href="#Spring-Config分布式配置中心" class="headerlink" title="Spring Config分布式配置中心:"></a>Spring Config分布式配置中心:</h2><p>==微服务面临的问题==</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">可以看到,每个微服务都需要一个配置文件,并且,如果有几个微服务都需要连接数据库</span><br><span class="line">		那么就需要配<span class="number">4</span>次数据库相关配置,并且当数据库发生改动,那么需要同时修改<span class="number">4</span>个微服务的配置文件才可以</span><br></pre></td></tr></table></figure>

<p>所以有了springconfig配置中心</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%841.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%842.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%843.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%844.png" alt></p>
<h3 id="使用配置中心"><a href="#使用配置中心" class="headerlink" title="使用配置中心:"></a>使用配置中心:</h3><h4 id="0-使用github作为配置中心的仓库"><a href="#0-使用github作为配置中心的仓库" class="headerlink" title="0,使用github作为配置中心的仓库:"></a>0,使用github作为配置中心的仓库:</h4><p><strong>初始化git环境:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%845.png" alt></p>
<h4 id="1-新建config模块"><a href="#1-新建config模块" class="headerlink" title="1,新建config模块:"></a>1,新建config模块:</h4><p>名字:   cloud-config-3344</p>
<h4 id="2-pom-2"><a href="#2-pom-2" class="headerlink" title="2,pom"></a>2,pom</h4><h4 id="3-配置文件-7"><a href="#3-配置文件-7" class="headerlink" title="3,配置文件"></a>3,配置文件</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%846.png" alt></p>
<h4 id="4-主启动类-7"><a href="#4-主启动类-7" class="headerlink" title="4,主启动类"></a>4,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%847.png" alt></p>
<h4 id="5-修改hosts"><a href="#5-修改hosts" class="headerlink" title="5,修改hosts:"></a>5,修改hosts:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%848.png" alt></p>
<h4 id="6-配置完成"><a href="#6-配置完成" class="headerlink" title="6,配置完成"></a>6,配置完成</h4><p>测试,3344是否可以从github上获取配置</p>
<p>启动3344    (要先启动eureka)</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%849.png" alt></p>
<p>它实际上就是,读取到配置文件中的GitHub的地址,然后拼接上/master/config-dev.yml</p>
<h4 id="7-读取配置文件的规则"><a href="#7-读取配置文件的规则" class="headerlink" title="7,读取配置文件的规则:"></a>7,读取配置文件的规则:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8410.png" alt></p>
<p>==2,==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8411.png" alt></p>
<p><strong>这里默认会读取master分支,因为我们配置文件中配置了</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8412.png" alt></p>
<p>==3==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8413.png" alt></p>
<p>注意,这个方式读取到的配置是==json格式==的</p>
<p><strong>所有规则:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8414.png" alt></p>
<h3 id="2-创建配置中心客户端"><a href="#2-创建配置中心客户端" class="headerlink" title="2,创建配置中心客户端:"></a>2,创建配置中心客户端:</h3><h4 id="1-创建config客户端项目"><a href="#1-创建config客户端项目" class="headerlink" title="1,创建config客户端项目"></a>1,创建config客户端项目</h4><p>名字:     cloud-config-client-3355</p>
<h4 id="2-pom-3"><a href="#2-pom-3" class="headerlink" title="2,pom"></a>2,pom</h4><h4 id="3-配置文件-8"><a href="#3-配置文件-8" class="headerlink" title="3,配置文件"></a>3,配置文件</h4><p>注意这个配置文件就不是application.yml</p>
<p>​            而是bootstrap.yml</p>
<p>这个配置文件的作用是,先到配置中心加载配置,然后加载到application.yml中</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8415.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8416.png" alt></p>
<h4 id="4-主启动类-8"><a href="#4-主启动类-8" class="headerlink" title="4,主启动类:"></a>4,主启动类:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8417.png" alt></p>
<h4 id="5-controller类"><a href="#5-controller类" class="headerlink" title="5,controller类"></a>5,controller类</h4><p>就是上面提到的,以rest风格将配置对外暴露</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8418.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8419.png" alt></p>
<p><strong>如果客户端运行正常,就会读取到github上配置文件的,config.info下的配置</strong></p>
<h4 id="6-测试"><a href="#6-测试" class="headerlink" title="6,测试:"></a>6,测试:</h4><p>启动3344,3355</p>
<p>​    访问3355的  /configInfo</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8421.png" alt></p>
<h4 id="7-问题"><a href="#7-问题" class="headerlink" title="7,问题::"></a>7,问题::</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">上面<span class="number">3355</span>确实获取到了配置文件,但是如果此时配置文件修改了,<span class="number">3355</span>是获取不到的</span><br><span class="line">		<span class="number">3344</span>可以实时获取到最新配置文件,但是<span class="number">3355</span>却获取不到</span><br><span class="line">  	除非重启服务</span><br></pre></td></tr></table></figure>

<h4 id="8-实现动态刷新"><a href="#8-实现动态刷新" class="headerlink" title="8,实现动态刷新:"></a><strong>8,实现动态刷新:</strong></h4><h5 id="1-修改3355-添加一个pom依赖"><a href="#1-修改3355-添加一个pom依赖" class="headerlink" title="1,修改3355,添加一个pom依赖:"></a>1,修改3355,添加一个pom依赖:</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8422.png" alt></p>
<h5 id="2-修改配置文件-添加一个配置"><a href="#2-修改配置文件-添加一个配置" class="headerlink" title="2,修改配置文件,添加一个配置:"></a>2,修改配置文件,添加一个配置:</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8423.png" alt></p>
<h5 id="3-修改controller"><a href="#3-修改controller" class="headerlink" title="3,修改controller:"></a>3,修改controller:</h5><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8424.png" alt></p>
<h5 id="4-此时重启服务"><a href="#4-此时重启服务" class="headerlink" title="4,此时重启服务"></a>4,此时重启服务</h5><p><strong>此时3355还不可以动态获取</strong></p>
<p>因为此时,还需要==外部==发送post请求通知3355</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8425.png" alt></p>
<p><strong>此时在刷新3355,发现可以获取到最新的配置文件了,这就实现了动态获取配置文件,因为3355并没有重启</strong></p>
<p>具体流程就是:</p>
<p>​            我们启动好服务后</p>
<p>​            运维人员,修改了配置文件,然后发送一个post请求通知3355</p>
<p>​            3355就可以获取最新配置文件</p>
<p><strong>问题:</strong></p>
<p>​        如果有多个客户端怎么办(3355,3356,3357…..)</p>
<p>​                        虽然可以使用shell脚本,循环刷新</p>
<p>​        但是,可不可以使用广播,一次通知??</p>
<p>​                    这些springconfig做不到,需要使用springcloud Bus消息总线</p>
<h1 id="消息总线"><a href="#消息总线" class="headerlink" title="消息总线:"></a>消息总线:</h1><h2 id="SpringCloud-Bus"><a href="#SpringCloud-Bus" class="headerlink" title="SpringCloud Bus:"></a>SpringCloud Bus:</h2><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8426.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8427.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8431.png" alt></p>
<p>注意,这里年张图片,就代表两种广播方式</p>
<p>​            图1:        <strong>它是Bus直接通知给其中一个客户端,由这个客户端开始蔓延,传播给其他所有客户端</strong></p>
<p>​            图2:        它<strong>是通知给配置中心的服务端,有服务端广播给所有客户端</strong></p>
<p><strong>为什么被称为总线?</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8428.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">就是通过消息队列达到广播的效果</span><br><span class="line">  		我们要广播每个消息时,主要放到某个topic中,所有监听的节点都可以获取到</span><br></pre></td></tr></table></figure>





<h3 id="使用Bus"><a href="#使用Bus" class="headerlink" title="使用Bus:"></a>使用Bus:</h3><h4 id="1-配置rabbitmq环境"><a href="#1-配置rabbitmq环境" class="headerlink" title="1,配置rabbitmq环境:"></a>1,配置rabbitmq环境:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8429.png" alt></p>
<h4 id="2-之前只有一个配置中心客户端-这里在创建一个"><a href="#2-之前只有一个配置中心客户端-这里在创建一个" class="headerlink" title="2,之前只有一个配置中心客户端,这里在创建一个"></a><strong>2,之前只有一个配置中心客户端,这里在创建一个</strong></h4><p>​        ==<strong>复制3355即可,创建为3366</strong>==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8430.png" alt></p>
<p>全部复制3355的即可</p>
<h4 id="2-使用Bus实现全局广播"><a href="#2-使用Bus实现全局广播" class="headerlink" title="2,使用Bus实现全局广播"></a>2,使用Bus实现全局广播</h4><p><strong>Bus广播有两种方式:</strong></p>
<p>​        ==就是上面两个图片的两种方式==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8432.png" alt></p>
<p><strong>这两种方式,第二种跟合适,因为:</strong></p>
<p>​            ==第一种的缺点:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cspringconfig%E7%9A%8433.png" alt></p>
<h4 id="配置第二种方式"><a href="#配置第二种方式" class="headerlink" title="配置第二种方式:"></a><strong>配置第二种方式:</strong></h4><h5 id="1-配置3344-配置中心服务端"><a href="#1-配置3344-配置中心服务端" class="headerlink" title="1,配置3344(配置中心服务端):"></a><strong>1,配置3344(配置中心服务端):</strong></h5><h6 id="1-修改配置文件-2"><a href="#1-修改配置文件-2" class="headerlink" title="1,修改配置文件:"></a>1,修改配置文件:</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%841.png" alt></p>
<h6 id="2-添加pom"><a href="#2-添加pom" class="headerlink" title="2,添加pom"></a>2,添加pom</h6><p><strong>springboot的监控组件,和消息总线</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%843.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%842.png" alt></p>
<h5 id="2-修改3355-配置中心的客户端"><a href="#2-修改3355-配置中心的客户端" class="headerlink" title="2,修改3355(配置中心的客户端)"></a>2,修改3355(配置中心的客户端)</h5><h6 id="1-pom-2"><a href="#1-pom-2" class="headerlink" title="1,pom:"></a>1,pom:</h6><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%843.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%842.png" alt="Bus的2"></p>
<h6 id="2-配置文件-4"><a href="#2-配置文件-4" class="headerlink" title="2,配置文件:"></a>2,配置文件:</h6><p>==注意配置文件的名字,要改为bootstrap.yml==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%845.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%844" alt="image-20200415102708661"></p>
<h5 id="3-修改3366-也是配置中心的客户端"><a href="#3-修改3366-也是配置中心的客户端" class="headerlink" title="3,修改3366(也是配置中心的客户端)"></a>3,修改3366(也是配置中心的客户端)</h5><p>​            修改与3355是一模一样的</p>
<h5 id="4-测试-2"><a href="#4-测试-2" class="headerlink" title="4,测试"></a>4,测试</h5><p>启动7001,3344,3355,3366</p>
<p>此时修改GitHub上的配置文件</p>
<p>==此时只需要刷新3344,即可让3355,3366动态获取最新的配置文件==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%846.png" alt></p>
<p>其原理就是:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%847.png" alt></p>
<p><strong>所有客户端都监听了一个rabbitMq的topic,我们将信息放入这个topic,所有客户端都可以送到,从而实时更新</strong></p>
<h4 id="配置定点通知"><a href="#配置定点通知" class="headerlink" title="配置定点通知"></a>配置定点通知</h4><p>​        就是只通知部分服务,比如只通知3355,不通知3366</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%848.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%849.png" alt="Bus的8"></p>
<p><strong>只通知3355</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%8411.png" alt></p>
<p>​    <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CBus%E7%9A%8412.png" alt></p>
<p><strong>可以看到,实际上就是通过==微服务的名称+端口号==进行指定</strong></p>
<h1 id="8-消息驱动"><a href="#8-消息驱动" class="headerlink" title="8,消息驱动:"></a>8,消息驱动:</h1><h2 id="Spring-Cloud-Stream"><a href="#Spring-Cloud-Stream" class="headerlink" title="Spring Cloud Stream:"></a>Spring Cloud Stream:</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">现在一个很项目可能分为三部分:</span><br><span class="line">			前端---&gt;后端----&gt;大数据</span><br><span class="line">			而后端开发使用消息中间件,可能会使用RabbitMq</span><br><span class="line">			而大数据开发,一般都是使用Kafka,</span><br><span class="line">			那么一个项目中有多个消息中间件,对于程序员,因为人员都不友好</span><br></pre></td></tr></table></figure>

<p>而Spring Cloud Stream就类似jpa,屏蔽底层消息中间件的差异,程序员主要操作Spring Cloud Stream即可</p>
<p>​            不需要管底层是kafka还是rabbitMq</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%841.png" alt></p>
<h3 id="什么是Spring-Cloud-Stream"><a href="#什么是Spring-Cloud-Stream" class="headerlink" title="==什么是Spring Cloud Stream=="></a>==什么是Spring Cloud Stream==</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%842.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%843.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%844.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%845.png" alt></p>
<h3 id="Spring-Cloud-Stream是怎么屏蔽底层差异的"><a href="#Spring-Cloud-Stream是怎么屏蔽底层差异的" class="headerlink" title="==Spring Cloud Stream是怎么屏蔽底层差异的?=="></a>==<strong>Spring Cloud Stream是怎么屏蔽底层差异的?</strong>==</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%846.png" alt></p>
<p><strong>绑定器:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%847.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%848.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%849.png" alt></p>
<h3 id="Spring-Cloud-Streamd-通信模式"><a href="#Spring-Cloud-Streamd-通信模式" class="headerlink" title="Spring Cloud Streamd 通信模式:"></a><strong>Spring Cloud Streamd 通信模式:</strong></h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8410.png" alt>)<img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8411.png" alt></p>
<h3 id="Spring-Cloud-Stream的业务流程"><a href="#Spring-Cloud-Stream的业务流程" class="headerlink" title="Spring Cloud Stream的业务流程:"></a>Spring Cloud Stream的业务流程:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8412.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8414.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8413.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">类似flume中的channel,source,sink  估计是借鉴(抄袭)的</span><br><span class="line">  	source用于获取数据(要发送到mq的数据)</span><br><span class="line">  	channel类似SpringCloudStream中的中间件,用于存放source接收到的数据,或者是存放binder拉取的数据</span><br></pre></td></tr></table></figure>







<h3 id="常用注解和api"><a href="#常用注解和api" class="headerlink" title="常用注解和api:"></a>常用注解和api:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8415.png" alt></p>
<h3 id="使用SpringCloudStream"><a href="#使用SpringCloudStream" class="headerlink" title="使用SpringCloudStream:"></a>使用SpringCloudStream:</h3><p>需要创建三个项目,一个生产者,两个消费者</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8416.png" alt></p>
<h3 id="1-创建生产者"><a href="#1-创建生产者" class="headerlink" title="1,创建生产者"></a>1,创建生产者</h3><h4 id="1-pom-3"><a href="#1-pom-3" class="headerlink" title="1,pom"></a>1,pom</h4><h4 id="2-配置文件-5"><a href="#2-配置文件-5" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8417" alt="image-20200415114816133"></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8418.png" alt></p>
<h4 id="3-主启动类-5"><a href="#3-主启动类-5" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8419.png" alt></p>
<h4 id="4-service和实现类"><a href="#4-service和实现类" class="headerlink" title="4,service和实现类"></a>4,service和实现类</h4><p>service定义发送消息</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8420.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8421.png" alt></p>
<p><strong>这里,就会调用send方法,将消息发送给channel,</strong></p>
<p>​                <strong>然后channel将消费发送给binder,然后发送到rabbitmq中</strong></p>
<h4 id="5-controller-2"><a href="#5-controller-2" class="headerlink" title="5,controller"></a>5,controller</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8422.png" alt></p>
<h4 id="6-可以测试"><a href="#6-可以测试" class="headerlink" title="6,可以测试"></a>6,可以测试</h4><p><strong>启动rabbitmq</strong></p>
<p><strong>启动7001,8801</strong></p>
<p>​        确定8801后,会在rabbitmq中创建一个Exchange,就是我们配置文件中配置的exchange</p>
<p><strong>访问8801的/sendMessage</strong></p>
<h3 id="创建消费者"><a href="#创建消费者" class="headerlink" title="创建消费者:"></a>创建消费者:</h3><h4 id="1-pom文件-2"><a href="#1-pom文件-2" class="headerlink" title="1,pom文件"></a>1,pom文件</h4><h4 id="2-配置文件-6"><a href="#2-配置文件-6" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p>==<strong>这里排版一点问题</strong>==</p>
<p><strong>==input==就表示,当前服务是一个消费者,需要消费消息,下面就是指定消费哪个Exchange中的消息</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8423.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8424.png" alt></p>
<h4 id="3-主启动类-6"><a href="#3-主启动类-6" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8425.png" alt></p>
<h4 id="4-业务类-消费数据"><a href="#4-业务类-消费数据" class="headerlink" title="4,业务类(消费数据)"></a>4,业务类(消费数据)</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8426.png" alt></p>
<p><strong>生产者发送消息时,使用send方法发送,send方法发送的是一个个Message,里面封装了数据</strong></p>
<h4 id="5-测试"><a href="#5-测试" class="headerlink" title="5,测试:"></a>5,测试:</h4><p>启动7001.8801.8802</p>
<p><strong>此时使用生产者生产消息</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8427.png" alt></p>
<p>==可以看到,消费者已经接收到消息了==</p>
<h3 id="创建消费者2"><a href="#创建消费者2" class="headerlink" title="创建消费者2"></a>创建消费者2</h3><p>创建8803,</p>
<p>==与8802创建一模一样,就不写了==</p>
<p><strong>创建8803主要是为了演示重复消费等问题</strong></p>
<p>…</p>
<p>….</p>
<p>…</p>
<h3 id="重复消费问题"><a href="#重复消费问题" class="headerlink" title="==重复消费问题:=="></a>==重复消费问题:==</h3><p>此时启动7001.8801.8802.8803</p>
<p>此时生产者生产一条消息</p>
<p>但是此时查询消费者,发现8802,8803==都消费到了同一条数据==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8428.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8429.png" alt></p>
<h4 id="1-自定义分组"><a href="#1-自定义分组" class="headerlink" title="1,自定义分组"></a>1,自定义分组</h4><p><strong>修改8802,8803的配置文件</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8430.png" alt></p>
<p>![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\SpringCloudStream的31 - 副本.png)</p>
<p><strong>现在将8802,8803都分到了A组</strong></p>
<p>然后去重启02,03</p>
<p><strong>然后此时生产者生产两条消息</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8433.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8434.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CSpringCloudStream%E7%9A%8435.png" alt></p>
<p><strong>可以看到,每人只消费了一条消息,并且没有重复消费</strong></p>
<h3 id="持久化问题"><a href="#持久化问题" class="headerlink" title="持久化问题:"></a>持久化问题:</h3><p>就是当服务挂了,怎么消费没有消费的数据??</p>
<p>这里,先将8802移除A组,</p>
<p>​        然后将02,03服务关闭</p>
<p>此时生产者开启,发送3条消息</p>
<p>​        此时重启02,03</p>
<p>​        可以看到,当02退出A组后,它就获取不到在它宕机的时间段内的数据</p>
<p>​        但是03重启后,直接获取到了宕机期间它没有消费的数据,并且消费了</p>
<p>总结:<br>        也就是,当我们没有配置分组时,会出现消息漏消费的问题</p>
<p>​        而配置分组后,我们可以自动获取未消费的数据</p>
<h1 id="9-链路追踪"><a href="#9-链路追踪" class="headerlink" title="9,链路追踪:"></a>9,链路追踪:</h1><h2 id="Spring-Cloud-Sleuth"><a href="#Spring-Cloud-Sleuth" class="headerlink" title="Spring Cloud Sleuth"></a>Spring Cloud Sleuth</h2><p><strong>sleuth要解决的问题:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csleuth%E7%9A%841.png" alt></p>
<p><strong>而来sleuth就是用于追踪每个请求的整体链路</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csleuth%E7%9A%842.png" alt></p>
<h3 id="使用sleuth"><a href="#使用sleuth" class="headerlink" title="使用sleuth:"></a>使用sleuth:</h3><h4 id="1-安装zipkin"><a href="#1-安装zipkin" class="headerlink" title="1,安装zipkin:"></a>1,安装zipkin:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csleuth%E7%9A%843.png" alt></p>
<p><strong>运行jar包</strong></p>
<p>​            java -jar xxxx.jar</p>
<p><strong>然后就可以访问web界面,  默认zipkin监听的端口是9411</strong></p>
<p>​            localhost:9411/zipkin/</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csleuth%E7%9A%844.png" alt></p>
<p><strong>一条链路完整图片:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csleuth%E7%9A%845.png" alt></p>
<p><strong>精简版:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csleuth%E7%9A%846.png" alt></p>
<p><strong>可以看到,类似链表的形式</strong></p>
<h4 id="2-使用sleuth"><a href="#2-使用sleuth" class="headerlink" title="2,使用sleuth:"></a>2,使用sleuth:</h4><p>不需要额外创建项目,使用之前的8001和order的80即可</p>
<h5 id="1-修改8001"><a href="#1-修改8001" class="headerlink" title="1,修改8001"></a>1,修改8001</h5><p><strong>引入pom:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csleuth%E7%9A%847.png" alt></p>
<p>这个包虽然叫zipkin但是,里面包含了zpikin与sleuth</p>
<p><strong>修改配置文件:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csleuth%E7%9A%848.png" alt></p>
<h5 id="2-修改80"><a href="#2-修改80" class="headerlink" title="2,修改80"></a>2,修改80</h5><p><strong>添加pom</strong></p>
<p>与上面是一样的</p>
<p><strong>添加配置</strong>:</p>
<p>与上面也是一样的</p>
<h5 id="3-测试-1"><a href="#3-测试-1" class="headerlink" title="3,测试:"></a>3,测试:</h5><p>启动7001.8001,80,9411</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csleuth%E7%9A%849.png" alt></p>
<h1 id="10-Spring-CloudAlibaba"><a href="#10-Spring-CloudAlibaba" class="headerlink" title="10,Spring CloudAlibaba:"></a>10,Spring CloudAlibaba:</h1><p><strong>之所以有Spring CloudAlibaba,是因为Spring Cloud Netflix项目进入维护模式</strong></p>
<p>​        <strong>也就是,就不是不更新了,不会开发新组件了</strong></p>
<p>​        <strong>所以,某些组件都有代替版了,比如Ribbon由Loadbalancer代替,等等</strong></p>
<p>==支持的功能==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%841.png" alt></p>
<p>几乎可以将之前的Spring Cloud代替</p>
<p>==具体组件==:<br><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%842.png" alt></p>
<h2 id="Nacos"><a href="#Nacos" class="headerlink" title="Nacos:"></a>Nacos:</h2><p><strong>服务注册和配置中心的组合</strong></p>
<p>​            Nacos=erueka+config+bus</p>
<h3 id="安装Nacos"><a href="#安装Nacos" class="headerlink" title="安装Nacos:"></a>安装Nacos:</h3><p>需要java8  和 Mavne</p>
<p><strong>1,到github上下载安装包</strong></p>
<p>​        解压安装包</p>
<p><strong>2,启动Nacos</strong></p>
<p>​        在bin下,进入cod</p>
<p>​        ./startup.cmd</p>
<p><strong>3,访问Nacos</strong></p>
<p>​        Nacos默认监听8848</p>
<p>​        localhost:8848/nacos</p>
<p>​        账号密码:默认都是nacos</p>
<h3 id="使用Nacos"><a href="#使用Nacos" class="headerlink" title="使用Nacos:"></a>使用Nacos:</h3><p>新建pay模块</p>
<p>​        <strong>现在不需要额外的服务注册模块了,Nacos单独启动了</strong></p>
<p>名字: cloudalibaba-pay-9001</p>
<h4 id="1-pom-4"><a href="#1-pom-4" class="headerlink" title="1,pom"></a>1,pom</h4><p>父项目管理alibaba的依赖:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%844.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%843.png" alt></p>
<p>==9001的pom==:</p>
<p>​            另外一个文件…..</p>
<h4 id="2-配置文件-7"><a href="#2-配置文件-7" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%845.png" alt></p>
<h4 id="3-启动类"><a href="#3-启动类" class="headerlink" title="3,启动类"></a>3,启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%846.png" alt></p>
<h4 id="4-controller-1"><a href="#4-controller-1" class="headerlink" title="4,controller:"></a>4,controller:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%847.png" alt></p>
<h4 id="5-测试-1"><a href="#5-测试-1" class="headerlink" title="5,测试"></a>5,测试</h4><p>启动9001</p>
<p>然后查看Nacos的web界面,可以看到9001已经注册成功</p>
<h3 id="-4"><a href="#-4" class="headerlink" title></a></h3><h3 id="创建其他Pay模块"><a href="#创建其他Pay模块" class="headerlink" title="创建其他Pay模块"></a>创建其他Pay模块</h3><p>​        额外在创建9002,9003</p>
<p>​        直接复制上面的即可</p>
<h3 id="创建order模块"><a href="#创建order模块" class="headerlink" title="创建order模块"></a>创建order模块</h3><p>名字:  cloudalibaba-order-83</p>
<h4 id="1-pom-5"><a href="#1-pom-5" class="headerlink" title="1,pom"></a>1,pom</h4><p><strong>为什么Nacos支持负载均衡?</strong></p>
<p>​                Nacos直接集成了Ribon,所以有负载均衡</p>
<h4 id="2-配置文件-8"><a href="#2-配置文件-8" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%848.png" alt></p>
<p><strong>这个server-url的作用是,我们在controller,需要使用RestTempalte远程调用9001,</strong></p>
<p>​        <strong>这里是指定9001的地址</strong></p>
<h4 id="3-主启动类-7"><a href="#3-主启动类-7" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%849.png" alt></p>
<h4 id="4-编写配置类"><a href="#4-编写配置类" class="headerlink" title="4,编写配置类"></a>4,编写配置类</h4><p>​    ==因为Naocs要使用Ribbon进行负载均衡,那么就需要使用RestTemplate==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8410.png" alt></p>
<h4 id="5-controller-3"><a href="#5-controller-3" class="headerlink" title="5,controller:"></a>5,controller:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8411.png" alt></p>
<h4 id="6-测试-1"><a href="#6-测试-1" class="headerlink" title="6,测试"></a>6,测试</h4><p>启动83,访问9001,9002,可以看到,实现了负载均衡</p>
<h3 id="Nacos与其他服务注册的对比"><a href="#Nacos与其他服务注册的对比" class="headerlink" title="Nacos与其他服务注册的对比"></a>Nacos与其他服务注册的对比</h3><p>Nacos它既可以支持CP,也可以支持AP,可以切换</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8412.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8413.png" alt></p>
<p>==下面这个curl命令,就是切换模式==</p>
<h3 id="使用Nacos作为配置中心"><a href="#使用Nacos作为配置中心" class="headerlink" title="使用Nacos作为配置中心:"></a>使用Nacos作为配置中心:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8414.png" alt></p>
<p><strong>==需要创建配置中心的客户端模块==</strong></p>
<p>cloudalibaba-Nacos-config-client-3377</p>
<h4 id="1-pom-6"><a href="#1-pom-6" class="headerlink" title="1,pom"></a>1,pom</h4><h4 id="2-配置文件-9"><a href="#2-配置文件-9" class="headerlink" title="2,配置文件"></a>2,配置文件</h4><p>这里需要配置两个配置文件,application.ymk和bootstarp.yml</p>
<p>​            主要是为了可以与spring clodu config无缝迁移</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8415.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">可以看到</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8416.png" alt></p>
<h4 id="3-主启动类-8"><a href="#3-主启动类-8" class="headerlink" title="3,主启动类"></a>3,主启动类</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8418.png" alt></p>
<h4 id="4-controller-2"><a href="#4-controller-2" class="headerlink" title="4,controller"></a>4,controller</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8417.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">可以看到,这里也添加了<span class="meta">@RefreshScope</span></span><br><span class="line">  		之前在Config配置中心,也是添加这个注解实现动态刷新的</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8419.png" alt></p>
<h4 id="5-在Nacos添加配置信息"><a href="#5-在Nacos添加配置信息" class="headerlink" title="5,在Nacos添加配置信息:"></a>5,在Nacos添加配置信息:</h4><p>==<strong>Nacos的配置规则:</strong>==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8420.png" alt></p>
<p><strong>配置规则,就是我们在客户端如何指定读取配置文件,配置文件的命名的规则</strong></p>
<p>默认的命名方式:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8421.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">prefix:</span><br><span class="line">		默认就是当前服务的服务名称</span><br><span class="line"> 		也可以通过spring.cloud.necos.config.prefix配置</span><br><span class="line">spring.profile.active:</span><br><span class="line">		就是我们在application.yml中指定的,当前是开发环境还是测试等环境</span><br><span class="line">    这个可以不配置,如果不配置,那么前面的 -  也会没有</span><br><span class="line">file-extension</span><br><span class="line">     就是当前文件的格式(后缀),目前只支持yml和properties</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8424.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8425.png" alt></p>
<p>==在web UI上创建配置文件:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8422.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8423.png" alt></p>
<p>注意,DataId就是配置文件名字:</p>
<p>​        名字一定要按照上面的==规则==命名,否则客户端会读取不到配置文件</p>
<h4 id="6-测试-2"><a href="#6-测试-2" class="headerlink" title="6,测试"></a>6,测试</h4><p>重启3377客户端</p>
<p>访问3377</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8426.png" alt></p>
<p><strong>拿到了配置文件中的值</strong></p>
<h4 id="7-注意默认就开启了自动刷新"><a href="#7-注意默认就开启了自动刷新" class="headerlink" title="7,注意默认就开启了自动刷新"></a>7,注意默认就开启了自动刷新</h4><p>此时我们修改了配置文件</p>
<p>客户端是可以立即更新的</p>
<p>​            因为Nacos支持Bus总线,会自动发送命令更新所有客户端</p>
<h3 id="Nacos配置中心之分类配置"><a href="#Nacos配置中心之分类配置" class="headerlink" title="Nacos配置中心之分类配置:"></a>Nacos配置中心之分类配置:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8427.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8428.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8429.png" alt></p>
<p>NameSpace默认有一个:public名称空间</p>
<p>这三个类似java的: 包名 + 类名 + 方法名</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8430.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8431.png" alt></p>
<h4 id="1-配置不同DataId"><a href="#1-配置不同DataId" class="headerlink" title="1,配置不同DataId:"></a>1,配置不同DataId:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8432.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8433.png" alt></p>
<p>​    ==通过配置文件,实现多环境的读取:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8434.png" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">此时,改为dev,就会读取dev的配置文件,改为test,就会读取test的配置文件</span><br></pre></td></tr></table></figure>





<h4 id="2-配置不同的GroupID"><a href="#2-配置不同的GroupID" class="headerlink" title="2,配置不同的GroupID:"></a>2,配置不同的GroupID:</h4><p>直接在新建配置文件时指定组</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8435.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8436.png" alt></p>
<p>==在客户端配置,使用指定组的配置文件:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8437.png" alt></p>
<p><strong>这两个配置文件都要修改</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8438.png" alt></p>
<p>​    </p>
<p>重启服务,即可</p>
<h4 id="配置不同的namespace"><a href="#配置不同的namespace" class="headerlink" title="配置不同的namespace:"></a>配置不同的namespace:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8439.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8442.png" alt></p>
<p>==客户端配置使用不同名称空间:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8441.png" alt></p>
<p><strong>要通过命名空间id指定</strong></p>
<p>OK,测试</p>
<h3 id="Nacos集群和持久化配置"><a href="#Nacos集群和持久化配置" class="headerlink" title="Nacos集群和持久化配置:"></a>Nacos集群和持久化配置:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8445.png" alt></p>
<p>Nacos默认有自带嵌入式数据库,derby,但是如果做集群模式的话,就不能使用自己的数据库</p>
<p>​            不然每个节点一个数据库,那么数据就不统一了,需要使用外部的mysql</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8443.png" alt></p>
<h4 id="1-单机版-切换mysql数据库"><a href="#1-单机版-切换mysql数据库" class="headerlink" title="1,单机版,切换mysql数据库:"></a>1,单机版,切换mysql数据库:</h4><p>​                    <strong>将nacos切换到使用我们自己的mysql数据库:</strong></p>
<p><strong>1,nacos默认自带了一个sql文件,在nacos安装目录下</strong></p>
<p>​            将它放到我们的mysql执行</p>
<p><strong>2,修改Nacos安装目录下的安排application.properties,添加:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8446.png" alt></p>
<p><strong>3,此时可以重启nacos,那么就会改为使用我们自己的mysql</strong></p>
<h4 id="Linux上配置Nacos集群-Mysql数据库"><a href="#Linux上配置Nacos集群-Mysql数据库" class="headerlink" title="Linux上配置Nacos集群+Mysql数据库"></a>Linux上配置Nacos集群+Mysql数据库</h4><p>==官方架构图:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8445.png" alt></p>
<p><strong>需要一个Nginx作为VIP</strong></p>
<p>1,下载安装Nacos的Linux版安装包</p>
<p>2,进入安装目录,现在执行自带的sql文件</p>
<p>​            进入mysql,执行sql文件</p>
<p>3.修改配置文件,切换为我们的mysql</p>
<p>​            就是上面windos版要修改的几个属性</p>
<p>4,修改cluster.conf,指定哪几个节点是Nacos集群</p>
<p>​            这里使用3333,4444,5555作为三个Nacos节点监听的端口</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8447.png" alt></p>
<p>5,我们这里就不配置在不同节点上了,就放在一个节点上</p>
<p>​            既然要在一个节点上启动不同Nacos实例,就要修改startup.sh,使其根据不同端口启动不同Nacos实例</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8448.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8449.png" alt></p>
<p>可以看到,这个脚本就是通过jvm启动nacos</p>
<p>​        所以我们最后修改的就是,nohup java -Dserver.port=3344</p>
<p>6,配置Nginx:</p>
<p>​            <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8450.png" alt></p>
<p>7,启动Nacos:<br>            ./startup.sh -p 3333</p>
<p>​            ./startup.sh -p 4444</p>
<p>​            ./startup.sh -p 5555</p>
<p>7,启动nginx</p>
<p>8,测试:</p>
<p>​        访问192.168.159.121:1111</p>
<p>​        如果可以进入nacos的web界面,就证明安装成功了</p>
<p>9,将微服务注册到Nacos集群:<br><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8451.png" alt></p>
<p>10,进入Nacos的web界面</p>
<p>​        可以看到,已经注册成功</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8452.png" alt></p>
<h2 id="Sentinel"><a href="#Sentinel" class="headerlink" title="Sentinel:"></a>Sentinel:</h2><p>实现熔断与限流,就是Hystrix</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8453.png" alt></p>
<p>​    <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8454.png" alt></p>
<h3 id="使用sentinel"><a href="#使用sentinel" class="headerlink" title="==使用sentinel:=="></a>==使用sentinel:==</h3><p>1,下载sentinel的jar包</p>
<p>2,运行sentinel</p>
<p>​        由于是一个jar包,所以可以直接java -jar运行    </p>
<p>​        注意,默认sentinel占用8080端口</p>
<p>3,访问sentinel</p>
<p>​        localhost:8080</p>
<h3 id="微服务整合sentinel"><a href="#微服务整合sentinel" class="headerlink" title="微服务整合sentinel:"></a>微服务整合sentinel:</h3><h5 id="1-启动Nacos"><a href="#1-启动Nacos" class="headerlink" title="1,启动Nacos"></a>1,启动Nacos</h5><h5 id="2-新建一个项目-8401-主要用于配置sentinel"><a href="#2-新建一个项目-8401-主要用于配置sentinel" class="headerlink" title="2,新建一个项目,8401,主要用于配置sentinel"></a>2,新建一个项目,8401,主要用于配置sentinel</h5><ol>
<li><p>pom</p>
</li>
<li><p>配置文件</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8455.png" alt></p>
</li>
<li><p>主启动类</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5CAlibaba%E7%9A%8456.png" alt></p>
</li>
<li><p>controller\</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%841.png" alt></p>
</li>
<li><p>到这里就可以启动8401</p>
<p>​    此时我们到sentinel中查看,发现并8401的任何信息</p>
<p>​    是因为,sentinel是懒加载,需要我们执行一次访问,才会有信息</p>
<p>​    访问localhost/8401/testA</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%842.png" alt></p>
</li>
<li><p>可以看到.已经开始监听了</p>
</li>
</ol>
<p>​    </p>
<h3 id="sentinel的流控规则"><a href="#sentinel的流控规则" class="headerlink" title="sentinel的流控规则"></a>sentinel的流控规则</h3><p>流量限制控制规则</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%847.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%843.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%844.png" alt></p>
<p>==流控模式==:</p>
<ol>
<li><p>直接快速失败</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%849.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%845.png" alt></p>
<p>  ==直接失败的效果:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%846.png" alt></p>
</li>
<li><p>线程数:</p>
<p>​        <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%848.png" alt></p>
<p>​    <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8410.png" alt></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">比如a请求过来,处理很慢,在一直处理,此时b请求又过来了</span><br><span class="line">		此时因为a占用一个线程,此时要处理b请求就只有额外开启一个线程</span><br><span class="line">		那么就会报错</span><br></pre></td></tr></table></figure>

<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8411.png" alt></p>
</li>
</ol>
<ol start="3">
<li><p>关联:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8412.png" alt></p>
<p>==应用场景:  比如<strong>支付接口</strong>达到阈值,就要限流下<strong>订单的接口</strong>,防止一直有订单==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8413.png" alt></p>
<p><strong>当testA达到阈值,qps大于1,就让testB之后的请求直接失败</strong></p>
<p>可以使用postman压测</p>
</li>
</ol>
<p>​    </p>
<ol start="4">
<li><p>链路:<br>多个请求调用同一个微服务</p>
</li>
<li><p>预热Warm up:</p>
<p>​     <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8414.png" alt></p>
<p> <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8415.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8416.png" alt></p>
<p>==应用场景==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8417.png" alt></p>
</li>
<li><p>排队等待:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8418.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8419.png" alt></p>
</li>
</ol>
<h3 id="降级规则"><a href="#降级规则" class="headerlink" title="降级规则:"></a>降级规则:</h3><p><strong>就是熔断降级</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8421.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8420.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8422.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8423.png" alt></p>
<h4 id="1-RT配置"><a href="#1-RT配置" class="headerlink" title="1,RT配置:"></a>1,RT配置:</h4><p>新增一个请求方法用于测试</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8424.png" alt></p>
<p>==配置RT:==</p>
<p>​                这里配置的PT,默认是秒级的平均响应时间</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8425.png" alt></p>
<p>默认计算平均时间是: 1秒类进入5个请求,并且响应的平均值超过阈值(这里的200ms),就报错]</p>
<p>​            1秒5请求是Sentinel默认设置的</p>
<p>==测试==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8427.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8426.png" alt></p>
<p><strong>默认熔断后.就直接抛出异常</strong></p>
<h4 id="2-异常比例"><a href="#2-异常比例" class="headerlink" title="2,异常比例:"></a>2,异常比例:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8428.png" alt></p>
<p>修改请求方法</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8429.png" alt></p>
<p>配置:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8431.png" alt></p>
<p>==如果没触发熔断,这正常抛出异常==:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8432.png" alt></p>
<p>==触发熔断==:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8433.png" alt></p>
<h4 id="3-异常数"><a href="#3-异常数" class="headerlink" title="3, 异常数:"></a>3, 异常数:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8434.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8435.png" alt></p>
<p>一分钟之内,有5个请求发送异常,进入熔断</p>
<h3 id="热点规则"><a href="#热点规则" class="headerlink" title="热点规则:"></a>热点规则:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8436.png" alt></p>
<p>​    <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8437.png" alt></p>
<p>比如:</p>
<p>​            localhost:8080/aa?name=aa</p>
<p>​            localhost:8080/aa?name=b’b</p>
<p>​            加入两个请求中,带有参数aa的请求访问频次非常高,我们就现在name==aa的请求,但是bb的不限制</p>
<p>==如何自定义降级方法,而不是默认的抛出异常?==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8438.png" alt></p>
<p><strong>使用@SentinelResource直接实现降级方法,它等同Hystrix的@HystrixCommand</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8439.png" alt></p>
<p>==定义热点规则:==</p>
<p> <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8440.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8442.png" alt></p>
<p><strong>此时我们访问/testHotkey并且带上才是p1</strong></p>
<p>​            如果qps大于1,就会触发我们定义的降级方法</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8441.png" alt></p>
<p><strong>但是我们的参数是P2,就没有问题</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8444.png" alt></p>
<p>只有带了p1,才可能会触发热点限流</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8443.png" alt></p>
<h4 id="2-设置热点规则中的其他选项"><a href="#2-设置热点规则中的其他选项" class="headerlink" title="2,设置热点规则中的其他选项:"></a>2,设置热点规则中的其他选项:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8445.png" alt></p>
<p><strong>需求:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8446.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8447.png" alt></p>
<p>==测试==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8448.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8449.png" alt></p>
<p><strong>注意:</strong></p>
<p>参数类型只支持,8种基本类型+String类</p>
<p>==注意:==</p>
<p>如果我们程序出现异常,是不会走blockHander的降级方法的,因为这个方法只配置了热点规则,没有配置限流规则</p>
<p>我们这里配置的降级方法是sentinel针对热点规则配置的</p>
<p>只有触发热点规则才会降级</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8450.png" alt></p>
<h3 id="3-系统规则"><a href="#3-系统规则" class="headerlink" title="3,系统规则:"></a>3,系统规则:</h3><p>系统自适应限流:<br>            从整体维度对应用入口进行限流</p>
<p>对整体限流,比如设置qps到达100,这里限流会限制整个系统不可以</p>
<p><em><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8451.png" alt></em></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8452.png" alt></p>
<p>==测试==:<br><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8453.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8454.png" alt></p>
<h3 id="SentinelResource注解"><a href="#SentinelResource注解" class="headerlink" title="@SentinelResource注解:"></a>@SentinelResource注解:</h3><p><strong>用于配置降级等功能</strong></p>
<p>1,环境搭建</p>
<ol>
<li><p>为8401添加依赖</p>
<p>添加我们自己的commone包的依赖</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8455.png" alt></p>
</li>
<li><p>额外创建一个controller类</p>
<p>​     <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8456.png" alt></p>
</li>
</ol>
<ol start="3">
<li><p>配置限流</p>
<p><strong>注意,我们这里配置规则,资源名指定的是@SentinelResource注解value的值,</strong></p>
<p><strong>这样也是可以的,也就是不一定要指定访问路径</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8457.png" alt></p>
</li>
<li><p>测试.</p>
<p>可以看到已经进入降级方法了</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8458.png" alt></p>
</li>
<li><p>==此时我们关闭8401服务==</p>
<p>可以看到,这些定义的规则是临时的,关闭服务,规则就没有了</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%8459.png" alt></p>
</li>
</ol>
<p><strong>可以看到,上面配置的降级方法,又出现Hystrix遇到的问题了</strong></p>
<p>​            降级方法与业务方法耦合</p>
<p>​            每个业务方法都需要对应一个降级方法</p>
<h4 id="自定义限流处理逻辑"><a href="#自定义限流处理逻辑" class="headerlink" title="自定义限流处理逻辑:"></a>自定义限流处理逻辑:</h4><ol>
<li><p>==单独创建一个类,用于处理限流==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%841.png" alt></p>
</li>
<li><p>==在controller中,指定使用自定义类中的方法作为降级方法==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%842.png" alt></p>
</li>
<li><p>==Sentinel中定义流控规则==:</p>
<p>这里资源名,是以url指定,也可以使用@SentinelResource注解value的值指定</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%845.png" alt></p>
</li>
</ol>
<ol start="4">
<li><p>==测试==:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%843.png" alt></p>
</li>
<li><p>==整体==:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%844.png" alt></p>
</li>
<li></li>
</ol>
<h4 id="SentinelResource注解的其他属性"><a href="#SentinelResource注解的其他属性" class="headerlink" title="@SentinelResource注解的其他属性:"></a>@SentinelResource注解的其他属性:</h4><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%847.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%846.png" alt></p>
<h3 id="服务熔断"><a href="#服务熔断" class="headerlink" title="服务熔断:"></a>服务熔断:</h3><ol>
<li><p><strong>启动nacos和sentinel</strong></p>
</li>
<li><p><strong>新建两个pay模块  9003和9004</strong></p>
<ol>
<li><p>pom</p>
</li>
<li><p>配置文件</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%848.png" alt>*</p>
</li>
<li><p>主启动类 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@EnableDiscoveryClient</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PaymentMain9003</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(PaymentMain9003<span class="class">.<span class="keyword">class</span>,<span class="title">args</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>controller</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%849.png" alt></p>
<p><strong>然后启动9003.9004</strong></p>
</li>
</ol>
</li>
<li><p><strong>新建一个order-84消费者模块:</strong></p>
<ol>
<li><p>pom</p>
<p>与上面的pay一模一样</p>
</li>
<li><p>配置文件</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8410.png" alt></p>
</li>
<li><p>主启动类</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8411.png" alt></p>
</li>
<li><p>配置类</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8412.png" alt></p>
</li>
<li><p>controller</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8413.png" alt></p>
</li>
</ol>
</li>
</ol>
<pre><code>6.   **==为业务方法添加fallback来指定降级方法==**:

    ![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\sentinel的的14.png)

    ​    ==重启order==

    测试:

    ![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\sentinel的的15.png)



     ==所以,fallback是用于管理异常的,当业务方法发生异常,可以降级到指定方法==

    ​            注意,我们这里==并没有使用sentinel配置任何规则==,但是却降级成功,就是因为

    ​            fallback是用于管理异常的,当业务方法发生异常,可以降级到指定方法==



7.   **==为业务方法添加blockHandler,看看是什么效果==**

     ![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\sentinel的的16.png)

     **重启84,访问业务方法:**

    ![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\sentinel的的17.png)

     可以看到.,直接报错了,并没有降级

    ​                也就是说,blockHandler==只对sentienl定义的规则降级==



8.   **==如果fallback和blockHandler都配置呢?==**]

     ![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\sentinel的的18.png)

     **设置qps规则,阈值1**

     ![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\sentinel的的19.png)

     ==测试:==

    ![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\sentinel的的20.png)



     可以看到,当两个都同时生效时,==blockhandler优先生效==

9.  **==@SentinelResource还有一个属性,exceptionsToIgnore==**

     ![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\sentinel的的21.png)

     **exceptionsToIgnore指定一个异常类,**

    ​                    **表示如果当前方法抛出的是指定的异常,不降级,直接对用户抛出异常**

     ![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\sentinel的的22.png)</code></pre><h3 id="sentinel整合ribbon-openFeign-fallback"><a href="#sentinel整合ribbon-openFeign-fallback" class="headerlink" title="sentinel整合ribbon+openFeign+fallback"></a>sentinel整合ribbon+openFeign+fallback</h3><ol>
<li><p>修改84模块,使其支持feign</p>
<ol>
<li><p>pom</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8423.png" alt></p>
</li>
<li><p>配置文件</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8424.png" alt></p>
</li>
<li><p>主启动类,也要修改</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8425.png" alt></p>
</li>
<li><p>创建远程调用pay模块的接口</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8426.png" alt></p>
</li>
<li><p>创建这个接口的实现类,用于降级</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8427.png" alt></p>
</li>
<li><p>再次修改接口,指定降级类</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8428.png" alt></p>
</li>
<li><p>controller添加远程调用</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8429.png" alt></p>
</li>
<li><p>测试</p>
<p>启动9003,84</p>
</li>
<li><p>测试,如果关闭9003.看看84会不会降级</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8430.png" alt></p>
<p><strong>可以看到,正常降级了</strong></p>
</li>
</ol>
</li>
</ol>
<p><strong>熔断框架比较</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8431.png" alt></p>
<h3 id="sentinel持久化规则"><a href="#sentinel持久化规则" class="headerlink" title="sentinel持久化规则"></a>sentinel持久化规则</h3><p>默认规则是临时存储的,重启sentinel就会消失</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8432.png" alt></p>
<p><strong>这里以之前的8401为案例进行修改:</strong></p>
<ol>
<li><p>修改8401的pom</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">添加:</span><br><span class="line"><span class="comment">&lt;!-- SpringCloud ailibaba sentinel-datasource-nacos 持久化需要用到--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba.csp<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>sentinel-datasource-nacos<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>



</li>
</ol>
<ol start="2">
<li><p>修改配置文件:</p>
<p>添加:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8433.png" alt></p>
<p><strong>实际上就是指定,我们的规则要保证在哪个名称空间的哪个分组下</strong></p>
<pre><code>这里没有指定namespace, 但是是可以指定的</code></pre><p>​            <strong>注意,这里的dataid要与8401的服务名一致</strong></p>
</li>
<li><p><strong>在nacos中创建一个配置文件,dataId就是上面配置文件中指定的</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8434.png" alt></p>
<p>==json中,这些属性的含义:==</p>
<p>​    <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8435.png" alt></p>
</li>
</ol>
<ol start="4">
<li><p>启动8401:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8436.png" alt></p>
<p>可以看到,直接读取到了规则</p>
</li>
<li><p>关闭8401</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8437.png" alt></p>
</li>
<li><p>此时重启8401,如果sentinel又可以正常读取到规则,那么证明持久化成功</p>
<p>可以看到,又重新出现了</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Csentinel%E7%9A%84%E7%9A%8438.png" alt></p>
</li>
</ol>
<h2 id="Seata"><a href="#Seata" class="headerlink" title="Seata:"></a>Seata:</h2><p>是一个分布式事务的解决方案,</p>
<p><strong>分布式事务中的一些概念,也是seata中的概念:</strong></p>
<p>​        <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%842.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%843.png" alt></p>
<h3 id="seata安装"><a href="#seata安装" class="headerlink" title="seata安装:"></a>seata安装:</h3><ol>
<li><p><strong>下载安装seata的安装包</strong></p>
</li>
<li><p><strong>修改file.conf</strong></p>
<p> <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%844.png" alt></p>
<p> <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%845.png" alt></p>
<p> <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%846.png" alt></p>
</li>
<li><p><strong>mysql建库建表</strong></p>
<p>1,上面指定了数据库为seata,所以创建一个数据库名为seata</p>
<p>2,建表,在seata的安装目录下有一个db_store.sql,运行即可</p>
</li>
<li><p><strong>继续修改配置文件,修改registry.conf</strong></p>
<p>配置seata作为微服务,指定注册中心</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%847.png" alt></p>
</li>
<li><p>启动</p>
<p>先启动nacos</p>
<p>在启动seata-server(运行安装目录下的,seata-server.bat)</p>
</li>
</ol>
<p><strong>业务说明</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%848.png" alt></p>
<p>下单—&gt;库存—&gt;账号余额</p>
<ol>
<li><p>创建三个数据库</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%849.png" alt></p>
</li>
<li><p>创建对应的表</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8410.png" alt></p>
</li>
<li><p>创建回滚日志表,方便查看</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8411.png" alt></p>
<p><strong>注意==每个库都要执行一次==这个sql,生成回滚日志表</strong></p>
</li>
<li><p>==每个业务都创建一个微服务,也就是要有三个微服务,订单,库存,账号==</p>
<p>​     ==订单==,seta-order-2001</p>
<ol>
<li><p>pom</p>
</li>
<li><p>配置文件</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">2001</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">seata-order-service</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">alibaba:</span></span><br><span class="line">      <span class="attr">seata:</span></span><br><span class="line">        <span class="comment"># 自定义事务组名称需要与seata-server中的对应,我们之前在seata的配置文件中配置的名字</span></span><br><span class="line">        <span class="attr">tx-service-group:</span> <span class="string">fsp_tx_group</span></span><br><span class="line">    <span class="attr">nacos:</span></span><br><span class="line">      <span class="attr">discovery:</span></span><br><span class="line">        <span class="attr">server-addr:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:8848</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="comment"># 当前数据源操作类型</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">com.alibaba.druid.pool.DruidDataSource</span></span><br><span class="line">    <span class="comment"># mysql驱动类</span></span><br><span class="line">    <span class="attr">driver-class-name:</span> <span class="string">com.mysql.cj.jdbc.Driver</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">jdbc:mysql://localhost:3306/seata_order?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false&amp;serverTimezone=GMT%2B8</span></span><br><span class="line">    <span class="attr">username:</span> <span class="string">root</span></span><br><span class="line">    <span class="attr">password:</span> <span class="string">root</span></span><br><span class="line"><span class="attr">feign:</span></span><br><span class="line">  <span class="attr">hystrix:</span></span><br><span class="line">    <span class="attr">enabled:</span> <span class="literal">false</span></span><br><span class="line"><span class="attr">logging:</span></span><br><span class="line">  <span class="attr">level:</span></span><br><span class="line">    <span class="attr">io:</span></span><br><span class="line">      <span class="attr">seata:</span> <span class="string">info</span></span><br><span class="line"></span><br><span class="line"><span class="attr">mybatis:</span></span><br><span class="line">  <span class="attr">mapperLocations:</span> <span class="string">classpath*:mapper/*.xml</span></span><br></pre></td></tr></table></figure>

<p>还要额外创建其他配置文件,创建一个file.conf:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">transport &#123;</span><br><span class="line">  # tcp udt unix-domain-socket</span><br><span class="line">  type &#x3D; &quot;TCP&quot;</span><br><span class="line">  #NIO NATIVE</span><br><span class="line">  server &#x3D; &quot;NIO&quot;</span><br><span class="line">  #enable heartbeat</span><br><span class="line">  heartbeat &#x3D; true</span><br><span class="line">  #thread factory for netty</span><br><span class="line">  thread-factory &#123;</span><br><span class="line">    boss-thread-prefix &#x3D; &quot;NettyBoss&quot;</span><br><span class="line">    worker-thread-prefix &#x3D; &quot;NettyServerNIOWorker&quot;</span><br><span class="line">    server-executor-thread-prefix &#x3D; &quot;NettyServerBizHandler&quot;</span><br><span class="line">    share-boss-worker &#x3D; false</span><br><span class="line">    client-selector-thread-prefix &#x3D; &quot;NettyClientSelector&quot;</span><br><span class="line">    client-selector-thread-size &#x3D; 1</span><br><span class="line">    client-worker-thread-prefix &#x3D; &quot;NettyClientWorkerThread&quot;</span><br><span class="line">    # netty boss thread size,will not be used for UDT</span><br><span class="line">    boss-thread-size &#x3D; 1</span><br><span class="line">    #auto default pin or 8</span><br><span class="line">    worker-thread-size &#x3D; 8</span><br><span class="line">  &#125;</span><br><span class="line">  shutdown &#123;</span><br><span class="line">    # when destroy server, wait seconds</span><br><span class="line">    wait &#x3D; 3</span><br><span class="line">  &#125;</span><br><span class="line">  serialization &#x3D; &quot;seata&quot;</span><br><span class="line">  compressor &#x3D; &quot;none&quot;</span><br><span class="line">&#125;</span><br><span class="line">service &#123;</span><br><span class="line">  #vgroup-&gt;rgroup</span><br><span class="line">  # 事务组名称</span><br><span class="line">  vgroup_mapping.fsp_tx_group &#x3D; &quot;default&quot;</span><br><span class="line">  #only support single node</span><br><span class="line">  default.grouplist &#x3D; &quot;127.0.0.1:8091&quot;</span><br><span class="line">  #degrade current not support</span><br><span class="line">  enableDegrade &#x3D; false</span><br><span class="line">  #disable</span><br><span class="line">  disable &#x3D; false</span><br><span class="line">  #unit ms,s,m,h,d represents milliseconds, seconds, minutes, hours, days, default permanent</span><br><span class="line">  max.commit.retry.timeout &#x3D; &quot;-1&quot;</span><br><span class="line">  max.rollback.retry.timeout &#x3D; &quot;-1&quot;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">client &#123;</span><br><span class="line">  async.commit.buffer.limit &#x3D; 10000</span><br><span class="line">  lock &#123;</span><br><span class="line">    retry.internal &#x3D; 10</span><br><span class="line">    retry.times &#x3D; 30</span><br><span class="line">  &#125;</span><br><span class="line">  report.retry.count &#x3D; 5</span><br><span class="line">  tm.commit.retry.count &#x3D; 1</span><br><span class="line">  tm.rollback.retry.count &#x3D; 1</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">## transaction log store</span><br><span class="line">store &#123;</span><br><span class="line">  ## store mode: file、db</span><br><span class="line">  #mode &#x3D; &quot;file&quot;</span><br><span class="line">  mode &#x3D; &quot;db&quot;</span><br><span class="line"> </span><br><span class="line">  ## file store</span><br><span class="line">  file &#123;</span><br><span class="line">    dir &#x3D; &quot;sessionStore&quot;</span><br><span class="line"> </span><br><span class="line">    # branch session size , if exceeded first try compress lockkey, still exceeded throws exceptions</span><br><span class="line">    max-branch-session-size &#x3D; 16384</span><br><span class="line">    # globe session size , if exceeded throws exceptions</span><br><span class="line">    max-global-session-size &#x3D; 512</span><br><span class="line">    # file buffer size , if exceeded allocate new buffer</span><br><span class="line">    file-write-buffer-cache-size &#x3D; 16384</span><br><span class="line">    # when recover batch read size</span><br><span class="line">    session.reload.read_size &#x3D; 100</span><br><span class="line">    # async, sync</span><br><span class="line">    flush-disk-mode &#x3D; async</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  ## database store</span><br><span class="line">  db &#123;</span><br><span class="line">    ## the implement of javax.sql.DataSource, such as DruidDataSource(druid)&#x2F;BasicDataSource(dbcp) etc.</span><br><span class="line">    datasource &#x3D; &quot;dbcp&quot;</span><br><span class="line">    ## mysql&#x2F;oracle&#x2F;h2&#x2F;oceanbase etc.</span><br><span class="line">    db-type &#x3D; &quot;mysql&quot;</span><br><span class="line">    driver-class-name &#x3D; &quot;com.mysql.jdbc.Driver&quot;</span><br><span class="line">    url &#x3D; &quot;jdbc:mysql:&#x2F;&#x2F;127.0.0.1:3306&#x2F;seata&quot;</span><br><span class="line">    user &#x3D; &quot;root&quot;</span><br><span class="line">    password &#x3D; &quot;root&quot;</span><br><span class="line">    min-conn &#x3D; 1</span><br><span class="line">    max-conn &#x3D; 3</span><br><span class="line">    global.table &#x3D; &quot;global_table&quot;</span><br><span class="line">    branch.table &#x3D; &quot;branch_table&quot;</span><br><span class="line">    lock-table &#x3D; &quot;lock_table&quot;</span><br><span class="line">    query-limit &#x3D; 100</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">lock &#123;</span><br><span class="line">  ## the lock store mode: local、remote</span><br><span class="line">  mode &#x3D; &quot;remote&quot;</span><br><span class="line"> </span><br><span class="line">  local &#123;</span><br><span class="line">    ## store locks in user&#39;s database</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  remote &#123;</span><br><span class="line">    ## store locks in the seata&#39;s server</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">recovery &#123;</span><br><span class="line">  #schedule committing retry period in milliseconds</span><br><span class="line">  committing-retry-period &#x3D; 1000</span><br><span class="line">  #schedule asyn committing retry period in milliseconds</span><br><span class="line">  asyn-committing-retry-period &#x3D; 1000</span><br><span class="line">  #schedule rollbacking retry period in milliseconds</span><br><span class="line">  rollbacking-retry-period &#x3D; 1000</span><br><span class="line">  #schedule timeout retry period in milliseconds</span><br><span class="line">  timeout-retry-period &#x3D; 1000</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">transaction &#123;</span><br><span class="line">  undo.data.validation &#x3D; true</span><br><span class="line">  undo.log.serialization &#x3D; &quot;jackson&quot;</span><br><span class="line">  undo.log.save.days &#x3D; 7</span><br><span class="line">  #schedule delete expired undo_log in milliseconds</span><br><span class="line">  undo.log.delete.period &#x3D; 86400000</span><br><span class="line">  undo.log.table &#x3D; &quot;undo_log&quot;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">## metrics settings</span><br><span class="line">metrics &#123;</span><br><span class="line">  enabled &#x3D; false</span><br><span class="line">  registry-type &#x3D; &quot;compact&quot;</span><br><span class="line">  # multi exporters use comma divided</span><br><span class="line">  exporter-list &#x3D; &quot;prometheus&quot;</span><br><span class="line">  exporter-prometheus-port &#x3D; 9898</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">support &#123;</span><br><span class="line">  ## spring</span><br><span class="line">  spring &#123;</span><br><span class="line">    # auto proxy the DataSource bean</span><br><span class="line">    datasource.autoproxy &#x3D; false</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>创建registry.conf:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">registry &#123;</span><br><span class="line">  # file 、nacos 、eureka、redis、zk、consul、etcd3、sofa</span><br><span class="line">  type &#x3D; &quot;nacos&quot;</span><br><span class="line"> </span><br><span class="line">  nacos &#123;</span><br><span class="line">    #serverAddr &#x3D; &quot;localhost&quot;</span><br><span class="line">    serverAddr &#x3D; &quot;localhost:8848&quot;</span><br><span class="line">    namespace &#x3D; &quot;&quot;</span><br><span class="line">    cluster &#x3D; &quot;default&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  eureka &#123;</span><br><span class="line">    serviceUrl &#x3D; &quot;http:&#x2F;&#x2F;localhost:8761&#x2F;eureka&quot;</span><br><span class="line">    application &#x3D; &quot;default&quot;</span><br><span class="line">    weight &#x3D; &quot;1&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  redis &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;localhost:6379&quot;</span><br><span class="line">    db &#x3D; &quot;0&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  zk &#123;</span><br><span class="line">    cluster &#x3D; &quot;default&quot;</span><br><span class="line">    serverAddr &#x3D; &quot;127.0.0.1:2181&quot;</span><br><span class="line">    session.timeout &#x3D; 6000</span><br><span class="line">    connect.timeout &#x3D; 2000</span><br><span class="line">  &#125;</span><br><span class="line">  consul &#123;</span><br><span class="line">    cluster &#x3D; &quot;default&quot;</span><br><span class="line">    serverAddr &#x3D; &quot;127.0.0.1:8500&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  etcd3 &#123;</span><br><span class="line">    cluster &#x3D; &quot;default&quot;</span><br><span class="line">    serverAddr &#x3D; &quot;http:&#x2F;&#x2F;localhost:2379&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  sofa &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;127.0.0.1:9603&quot;</span><br><span class="line">    application &#x3D; &quot;default&quot;</span><br><span class="line">    region &#x3D; &quot;DEFAULT_ZONE&quot;</span><br><span class="line">    datacenter &#x3D; &quot;DefaultDataCenter&quot;</span><br><span class="line">    cluster &#x3D; &quot;default&quot;</span><br><span class="line">    group &#x3D; &quot;SEATA_GROUP&quot;</span><br><span class="line">    addressWaitTime &#x3D; &quot;3000&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  file &#123;</span><br><span class="line">    name &#x3D; &quot;file.conf&quot;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">config &#123;</span><br><span class="line">  # file、nacos 、apollo、zk、consul、etcd3</span><br><span class="line">  type &#x3D; &quot;file&quot;</span><br><span class="line"> </span><br><span class="line">  nacos &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;localhost&quot;</span><br><span class="line">    namespace &#x3D; &quot;&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  consul &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;127.0.0.1:8500&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  apollo &#123;</span><br><span class="line">    app.id &#x3D; &quot;seata-server&quot;</span><br><span class="line">    apollo.meta &#x3D; &quot;http:&#x2F;&#x2F;192.168.1.204:8801&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  zk &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;127.0.0.1:2181&quot;</span><br><span class="line">    session.timeout &#x3D; 6000</span><br><span class="line">    connect.timeout &#x3D; 2000</span><br><span class="line">  &#125;</span><br><span class="line">  etcd3 &#123;</span><br><span class="line">    serverAddr &#x3D; &quot;http:&#x2F;&#x2F;localhost:2379&quot;</span><br><span class="line">  &#125;</span><br><span class="line">  file &#123;</span><br><span class="line">    name &#x3D; &quot;file.conf&quot;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>==实际上,就是要将seata中的我们之前修改的两个配置文件复制到这个项目下==</p>
</li>
<li><p><strong>主启动类</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span>(exclude = DataSourceAutoConfiguration<span class="class">.<span class="keyword">class</span>) //取消数据源的自动创建</span></span><br><span class="line"><span class="class">@<span class="title">EnableDiscoveryClient</span></span></span><br><span class="line"><span class="class">@<span class="title">EnableFeignClients</span></span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">SeataOrderMain2001</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(SeataOrderMain2001<span class="class">.<span class="keyword">class</span>,<span class="title">args</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



</li>
</ol>
</li>
</ol>
<pre><code>4.   **service层**

     <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">public interface OrderService &#123;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 创建订单</span><br><span class="line">     * @param order</span><br><span class="line">     */</span><br><span class="line">    void create(Order order);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

    <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">@FeignClient(value = "seata-storage-service")</span><br><span class="line">public interface StorageService &#123;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 减库存</span><br><span class="line">     * @param productId</span><br><span class="line">     * @param count</span><br><span class="line">     * @return</span><br><span class="line">     */</span><br><span class="line">    @PostMapping(value = "/storage/decrease")</span><br><span class="line">    CommonResult decrease(@RequestParam("productId") Long productId, @RequestParam("count") Integer count);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

    <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">@FeignClient(value = "seata-account-service")</span><br><span class="line">public interface AccountService &#123;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 减余额</span><br><span class="line">     * @param userId</span><br><span class="line">     * @param money</span><br><span class="line">     * @return</span><br><span class="line">     */</span><br><span class="line">    @PostMapping(value = "/account/decrease")</span><br><span class="line">    CommonResult decrease(@RequestParam("userId") Long userId, @RequestParam("money") BigDecimal money);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

    <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">@Service</span><br><span class="line">@Slf4j</span><br><span class="line">public class OrderServiceImpl implements OrderService &#123;</span><br><span class="line"></span><br><span class="line">    @Resource</span><br><span class="line">    private OrderDao orderDao;</span><br><span class="line">    @Resource</span><br><span class="line">    private AccountService accountService;</span><br><span class="line">    @Resource</span><br><span class="line">    private StorageService storageService;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 创建订单-&gt;调用库存服务扣减库存-&gt;调用账户服务扣减账户余额-&gt;修改订单状态</span><br><span class="line">     * 简单说:</span><br><span class="line">     * 下订单-&gt;减库存-&gt;减余额-&gt;改状态</span><br><span class="line">     * GlobalTransactional seata开启分布式事务,异常时回滚,name保证唯一即可</span><br><span class="line">     * @param order 订单对象</span><br><span class="line">     */</span><br><span class="line">    @Override</span><br><span class="line">    ///@GlobalTransactional(name = "fsp-create-order", rollbackFor = Exception.class)</span><br><span class="line">    public void create(Order order) &#123;</span><br><span class="line">        // 1 新建订单</span><br><span class="line">        log.info("-----&gt;开始新建订单");</span><br><span class="line">        orderDao.create(order);</span><br><span class="line"></span><br><span class="line">        // 2 扣减库存</span><br><span class="line">        log.info("-----&gt;订单微服务开始调用库存,做扣减Count");</span><br><span class="line">        storageService.decrease(order.getProductId(), order.getCount());</span><br><span class="line">        log.info("-----&gt;订单微服务开始调用库存,做扣减End");</span><br><span class="line"></span><br><span class="line">        // 3 扣减账户</span><br><span class="line">        log.info("-----&gt;订单微服务开始调用账户,做扣减Money");</span><br><span class="line">        accountService.decrease(order.getUserId(), order.getMoney());</span><br><span class="line">        log.info("-----&gt;订单微服务开始调用账户,做扣减End");</span><br><span class="line"></span><br><span class="line">        // 4 修改订单状态,从0到1,1代表已完成</span><br><span class="line">        log.info("-----&gt;修改订单状态开始");</span><br><span class="line">        orderDao.update(order.getUserId(), 0);</span><br><span class="line"></span><br><span class="line">        log.info("-----&gt;下订单结束了,O(∩_∩)O哈哈~");</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>















5.   **dao层,也就是接口**

    <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Mapper</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">OrderDao</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 1 新建订单</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> order</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">create</span><span class="params">(Order order)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 2 修改订单状态,从0改为1</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> userId</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> status</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">update</span><span class="params">(@Param(<span class="string">"userId"</span>)</span> Long userId, @<span class="title">Param</span><span class="params">(<span class="string">"status"</span>)</span> Integer status)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

     ==在resource下创建mapper文件夹,编写mapper.xml==

    <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">mapper</span> <span class="meta-keyword">PUBLIC</span> <span class="meta-string">"-//mybatis.org//DTD Mapper 3.0//EN"</span></span></span><br><span class="line"><span class="meta">        <span class="meta-string">"http://mybatis.org/dtd/mybatis-3-mapper.dtd"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mapper</span> <span class="attr">namespace</span>=<span class="string">"com.eiletxie.springcloud.alibaba.dao.OrderDao"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"BaseResultMap"</span> <span class="attr">type</span>=<span class="string">"com.eiletxie.springcloud.alibaba.domain.Order"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">"id"</span> <span class="attr">property</span>=<span class="string">"id"</span> <span class="attr">jdbcType</span>=<span class="string">"BIGINT"</span>&gt;</span><span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"user_id"</span> <span class="attr">property</span>=<span class="string">"userId"</span> <span class="attr">jdbcType</span>=<span class="string">"BIGINT"</span>&gt;</span><span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"product_id"</span> <span class="attr">property</span>=<span class="string">"productId"</span> <span class="attr">jdbcType</span>=<span class="string">"BIGINT"</span>&gt;</span><span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"count"</span> <span class="attr">property</span>=<span class="string">"count"</span> <span class="attr">jdbcType</span>=<span class="string">"INTEGER"</span>&gt;</span><span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"money"</span> <span class="attr">property</span>=<span class="string">"money"</span> <span class="attr">jdbcType</span>=<span class="string">"DECIMAL"</span>&gt;</span><span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"status"</span> <span class="attr">property</span>=<span class="string">"status"</span> <span class="attr">jdbcType</span>=<span class="string">"INTEGER"</span>&gt;</span><span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">insert</span> <span class="attr">id</span>=<span class="string">"create"</span> <span class="attr">parameterType</span>=<span class="string">"com.eiletxie.springcloud.alibaba.domain.Order"</span> <span class="attr">useGeneratedKeys</span>=<span class="string">"true"</span></span></span><br><span class="line"><span class="tag">            <span class="attr">keyProperty</span>=<span class="string">"id"</span>&gt;</span></span><br><span class="line">        insert into t_order(user_id,product_id,count,money,status) values (#&#123;userId&#125;,#&#123;productId&#125;,#&#123;count&#125;,#&#123;money&#125;,0);</span><br><span class="line">    <span class="tag">&lt;/<span class="name">insert</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">update</span> <span class="attr">id</span>=<span class="string">"update"</span>&gt;</span></span><br><span class="line">        update t_order set status =1 where user_id =#&#123;userId&#125; and status=#&#123;status&#125;;</span><br><span class="line">   <span class="tag">&lt;/<span class="name">update</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mapper</span>&gt;</span></span><br></pre></td></tr></table></figure>

6.   **controller层**

    <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OrderController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> OrderService orderService;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 创建订单</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> order</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/order/create"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> CommonResult <span class="title">create</span><span class="params">(Order order)</span> </span>&#123;</span><br><span class="line">        orderService.create(order);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> CommonResult(<span class="number">200</span>, <span class="string">"订单创建成功"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



7.   **entity类(也叫domain类)**

     <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CommonResult</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Integer code;</span><br><span class="line">    <span class="keyword">private</span> String message;</span><br><span class="line">    <span class="keyword">private</span> T data;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CommonResult</span><span class="params">(Integer code, String message)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(code, message, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

    ![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\seala的12.png)







8.   config配置类

    <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@MapperScan</span>(&#123;<span class="string">"com.eiletxie.springcloud.alibaba.dao"</span>&#125;)		指定我们的接口的位置</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBatisConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

    <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span> EiletXie</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Since</span> 2020/3/18 21:51</span></span><br><span class="line"><span class="comment"> * 使用Seata对数据源进行代理</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DataSourceProxyConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;mybatis.mapperLocations&#125;"</span>)</span><br><span class="line">    <span class="keyword">private</span> String mapperLocations;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.datasource"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSource <span class="title">druidDataSource</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> DruidDataSource();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSourceProxy <span class="title">dataSourceProxy</span><span class="params">(DataSource druidDataSource)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> DataSourceProxy(druidDataSource);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> SqlSessionFactory <span class="title">sqlSessionFactoryBean</span><span class="params">(DataSourceProxy dataSourceProxy)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        SqlSessionFactoryBean bean = <span class="keyword">new</span> SqlSessionFactoryBean();</span><br><span class="line">        bean.setDataSource(dataSourceProxy);</span><br><span class="line">        ResourcePatternResolver resolver = <span class="keyword">new</span> PathMatchingResourcePatternResolver();</span><br><span class="line">        bean.setMapperLocations(resolver.getResources(mapperLocations));</span><br><span class="line">        <span class="keyword">return</span> bean.getObject();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



9.   

10.   

11.  

  ==库存==,seta-storage-2002

**==看脑图==**

1.    pom   
2.   配置文件
3.   主启动类
4.    service层
5.    dao层
6.    controller层
7.   
8.   



 ==账号==,seta-account-2003

**==看脑图==**

1.    pom     
2.   配置文件
3.   主启动类
4.   service层
5.    dao层
6.   controller层
7.   
8.   </code></pre><ol start="5">
<li><p><strong>全局创建完成后,首先测试不加seata</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8414.png" alt></p>
</li>
</ol>
<pre><code>![](C:\Users\陈超\Desktop\Linux\springboot+cloud\springcloud\seala的13.png)</code></pre><ol start="6">
<li><p>使用seata:</p>
<p><strong>在==订单模块==的serviceImpl类中的==create方法==添加启动分布式事务的注解</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	这里添加开启分布式事务的注解,name指定当前全局事务的名称</span></span><br><span class="line"><span class="comment">	rollbackFor表示,发生什么异常需要回滚</span></span><br><span class="line"><span class="comment">	noRollbackFor:表示,发生什么异常不需要回滚</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="meta">@GlobalTransactional</span>(name = <span class="string">"fsp-create-order"</span>,rollbackFor = Exception<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">///@<span class="title">GlobalTransactional</span>(<span class="title">name</span> </span>= <span class="string">"fsp-create-order"</span>, rollbackFor = Exception<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">void</span> <span class="title">create</span>(<span class="title">Order</span> <span class="title">order</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 1 新建订单</span></span><br><span class="line">    log.info(<span class="string">"-----&gt;开始新建订单"</span>);</span><br><span class="line">    orderDao.create(order);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2 扣减库存</span></span><br><span class="line">    log.info(<span class="string">"-----&gt;订单微服务开始调用库存,做扣减Count"</span>);</span><br><span class="line">    storageService.decrease(order.getProductId(), order.getCount());</span><br><span class="line">    log.info(<span class="string">"-----&gt;订单微服务开始调用库存,做扣减End"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3 扣减账户</span></span><br><span class="line">    log.info(<span class="string">"-----&gt;订单微服务开始调用账户,做扣减Money"</span>);</span><br><span class="line">    accountService.decrease(order.getUserId(), order.getMoney());</span><br><span class="line">    log.info(<span class="string">"-----&gt;订单微服务开始调用账户,做扣减End"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4 修改订单状态,从0到1,1代表已完成</span></span><br><span class="line">    log.info(<span class="string">"-----&gt;修改订单状态开始"</span>);</span><br><span class="line">    orderDao.update(order.getUserId(), <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    log.info(<span class="string">"-----&gt;下订单结束了,O(∩_∩)O哈哈~"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



</li>
</ol>
<ol start="7">
<li><p>此时在测试</p>
<p>发现,发生异常后,直接回滚了,前面的修改操作都回滚了</p>
</li>
</ol>
<h3 id="setat原理"><a href="#setat原理" class="headerlink" title="setat原理:"></a>setat原理:</h3><p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8415.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8416.png" alt></p>
<p><strong>seata提供了四个模式:</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8417.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8418.png" alt></p>
<p>==第一阶段:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8420.png" alt></p>
<p>​    <img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8419.png" alt></p>
<p>==二阶段之提交==:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8421.png" alt></p>
<p>==二阶段之回滚:==</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8422.png" alt></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8423.png" alt></p>
<p>==断点==:</p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8424.png" alt></p>
<p><strong>可以看到,他们的xid全局事务id是一样的,证明他们在一个事务下</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8425.png" alt></p>
<p><strong>before 和 after的原理就是</strong></p>
<p><img src="/2020/04/16/SpringCloud%E5%AD%A6%E4%B9%A0/SpringCloud/C:%5CUsers%5C%E9%99%88%E8%B6%85%5CDesktop%5CLinux%5Cspringboot+cloud%5Cspringcloud%5Cseala%E7%9A%8426.png" alt></p>
<p><strong>在更新数据之前,先解析这个更新sql,然后查询要更新的数据,进行保存</strong></p>
]]></content>
  </entry>
</search>
